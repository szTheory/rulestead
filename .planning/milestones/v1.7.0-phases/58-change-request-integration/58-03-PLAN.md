---
phase: 58-change-request-integration
plan: 58-03
type: feature
wave: 2
depends_on:
  - 58-01
files_modified:
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/test/rulestead/governance/audience_mutation_change_request_contract_test.exs
autonomous: true
requirements:
  - CRQ-02
must_haves:
  truths:
    - Ecto submit_change_request validates audience mutation same as Fake
    - Ecto execute_change_request applies audience mutation with governed_apply bypass
  artifacts:
    - rulestead/lib/rulestead/store/ecto.ex
---

# Plan 58-03: Ecto Store Change Request Integration

<objective>
Mirror Fake audience-mutation change-request submit validation and governed execute in `Rulestead.Store.Ecto`.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-58-07: Fake/Ecto behavioral drift | medium | Shared AudienceMutationChangeRequest module; contract tests both adapters |
</threat_model>

<tasks>

<task id="58-03-01" type="execute">
  <name>Ecto submit validation hook</name>
  <read_first>
    - rulestead/lib/rulestead/store/ecto.ex
    - search: def submit_change_request
    - rulestead/lib/rulestead/fake.ex
    - rulestead/lib/rulestead/governance/audience_mutation_change_request.ex
  </read_first>
  <action>
In `ecto.ex` `submit_change_request/1`:

Before `Multi.run(:change_request, ...)`:

When `command.action == :apply_audience_mutation`:

1. Load audience + environment in repo transaction (or pre-multi with) to build `current_preview` using same logic as `apply_confirmed_audience_mutation` preview step.

2. `AudienceMutationChangeRequest.validate_submit(command, current_preview)` — on error return `{:error, error}` early.

3. Merge `build_submission_metadata/2` into command metadata before insert.

4. Ensure insert uses `resource_type: "audience"` and `resource_key` from audience_key.

Match Fake field names on `insert_change_request` row map.
  </action>
  <verify>
    <automated>cd rulestead && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `ecto.ex` contains `AudienceMutationChangeRequest.validate_submit`
  </acceptance_criteria>
</task>

<task id="58-03-02" type="execute">
  <name>Ecto execute governed audience mutation</name>
  <read_first>
    - rulestead/lib/rulestead/store/ecto.ex
    - search: execute_change_request, execute_governed, apply_confirmed_audience_mutation, validate_blast_radius_threshold
  </read_first>
  <action>
In `ecto.ex` governed execute dispatcher (same structure as Fake):

1. Add `"apply_audience_mutation"` / `:apply_audience_mutation` branch.

2. Rebuild `ApplyAudienceMutation` from `change_request.command_snapshot`.

3. Call `apply_confirmed_audience_mutation(command, governed_apply?: true)` or pass flag through Multi pipeline.

4. Update `validate_blast_radius_threshold/2` to accept opts and forward `governed_apply?: true` from governed execute path only.

Do not change direct `apply_audience_mutation/1` caller path (no governed flag).
  </action>
  <verify>
    <automated>cd rulestead && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `ecto.ex` contains `apply_audience_mutation` in execute path
    - `validate_blast_radius_threshold` accepts governed_apply option
  </acceptance_criteria>
</task>

<task id="58-03-03" type="execute">
  <name>Ecto contract tests in shared file</name>
  <read_first>
    - rulestead/test/rulestead/governance/audience_mutation_change_request_contract_test.exs
    - rulestead/test/rulestead/store/governance_adapter_contract_test.exs
  </read_first>
  <action>
Extend `audience_mutation_change_request_contract_test.exs`:

- Add `@adapters [Rulestead.Fake, Rulestead.Store.Ecto]` pattern from governance_adapter_contract_test
- `use Rulestead.RepoCase` for Ecto sections
- Duplicate submit/approve/execute and stale-preview tests for both adapters
- Call `ensure_phase9_schema!()` in setup for Ecto (copy from governance_adapter_contract_test)

Assert parity: both adapters return `:executed` and updated audience on happy path.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/governance/audience_mutation_change_request_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Contract test references `Rulestead.Store.Ecto`
    - Test file exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `mix test test/rulestead/governance/audience_mutation_change_request_contract_test.exs` green for both adapters
</verification>
