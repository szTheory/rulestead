---
phase: 58-change-request-integration
plan: 58-04
type: feature
wave: 3
depends_on:
  - 58-02
  - 58-03
files_modified:
  - rulestead/lib/rulestead/fake.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead.ex
  - rulestead/lib/rulestead/admin/authorizer.ex
  - rulestead/test/rulestead/governance/audience_mutation_change_request_contract_test.exs
  - rulestead/test/rulestead/admin_governance_policy_test.exs
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CRQ-01
  - CRQ-02
  - CRQ-03
must_haves:
  truths:
    - Reject and cancel leave audience definition unchanged with auditable evidence
    - Facade and authorizer recognize apply_audience_mutation governance action
    - Phase 58 requirements covered by green contract suite
  artifacts:
    - rulestead/test/rulestead/governance/audience_mutation_change_request_contract_test.exs
---

# Plan 58-04: Terminal States, Facade, And Phase Proof

<objective>
Complete CRQ-03 reject/cancel audit evidence, ensure facade/policy seams, update roadmap plan count, and run full Phase 58 verification.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-58-04: Reject path mutates audience | high | Contract asserts before/after audience definition equal on reject/cancel |
| T-58-08: PII in rejection audit | low | Only assessment summary + reference keys in metadata, no predicate blobs |
</threat_model>

<tasks>

<task id="58-04-01" type="execute">
  <name>Reject/cancel audit metadata</name>
  <read_first>
    - rulestead/lib/rulestead/fake.ex
    - search: reject_change_request, cancel_change_request, governance_audit_command
    - rulestead/lib/rulestead/store/ecto.ex
  </read_first>
  <action>
On `reject_change_request` and `cancel_change_request` when `change_request.governed_action == "apply_audience_mutation"`:

Merge into audit event metadata (both Fake and Ecto):
```elixir
%{
  "blast_radius_assessment" => get_in(change_request, [:metadata, "blast_radius_assessment"]),
  "affected_reference_summary" => get_in(change_request, [:metadata, "affected_reference_summary"]),
  "terminal_reason" => command.reason
}
```

Do not invoke audience mutation apply on these paths.
  </action>
  <verify>
    <automated>cd rulestead && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `fake.ex` reject/cancel audit path includes `blast_radius_assessment` for audience CRs
  </acceptance_criteria>
</task>

<task id="58-04-02" type="execute">
  <name>CRQ-03 contract tests</name>
  <read_first>
    - rulestead/test/rulestead/governance/audience_mutation_change_request_contract_test.exs
    - rulestead/test/rulestead/store/governance_adapter_contract_test.exs
  </read_first>
  <action>
Add tests (both adapters):

1. **reject leaves audience unchanged**: capture `before_definition` from audience, submit+reject CR, fetch audience — definition map equals `before_definition`.

2. **cancel leaves audience unchanged**: submit, approve optional, cancel — audience unchanged.

3. **audit evidence**: `fetch_change_request` audit_events include `change_request.rejected` or `change_request.cancelled` with metadata key `"blast_radius_assessment"`.

4. **execute rejected fails**: `{:error, %Rulestead.Error{type: :invalid_command}}` when executing rejected CR.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/governance/audience_mutation_change_request_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Contract file contains `blast_radius_assessment` in reject/cancel test
    - Contract file contains `unchanged` or `before_definition`
    - Tests exit 0
  </acceptance_criteria>
</task>

<task id="58-04-03" type="execute">
  <name>Facade and authorizer action mapping</name>
  <read_first>
    - rulestead/lib/rulestead.ex
    - search: command_action, submit_change_request, apply_audience_mutation
    - rulestead/lib/rulestead/admin/authorizer.ex
    - rulestead/test/rulestead/admin_governance_policy_test.exs
  </read_first>
  <action>
1. In `rulestead.ex`, ensure `command_action(:submit_change_request, %{action: :apply_audience_mutation})` maps to a governance-capable admin action (grep existing `:publish_ruleset` pattern — add `:submit_audience_change_request` or reuse `:submit_change_request` if already generic).

2. Update authorizer allowed actions if an explicit allowlist blocks new action atoms.

3. Add/adjust test in `admin_governance_policy_test.exs` that `:apply_audience_mutation` submit is policy-governed when `change_request_required?: true`.

If authorizer already allows any `submit_change_request`, document in test comment only — still add contract-level proof in governance test.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/admin_governance_policy_test.exs --failed</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/rulestead/admin_governance_policy_test.exs` exits 0
    - `rulestead.ex` or authorizer references `apply_audience_mutation`
  </acceptance_criteria>
</task>

<task id="58-04-04" type="execute">
  <name>Phase proof and roadmap update</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/58-change-request-integration/58-VALIDATION.md
  </read_first>
  <action>
1. Run full phase command:
   `cd rulestead && mix test test/rulestead/governance/audience_mutation_change_request_test.exs test/rulestead/governance/audience_mutation_change_request_contract_test.exs test/rulestead/governance/change_request_contract_test.exs`

2. Update `.planning/ROADMAP.md` Phase 58 section: `**Plans**: 4 plans` (replace TBD).

3. Update progress table: Phase 58 plans 0/4 → document as 4 plans defined (status remains Not started until execute).
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/governance/audience_mutation_change_request_test.exs test/rulestead/governance/audience_mutation_change_request_contract_test.exs test/rulestead/governance/change_request_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Full test command exits 0
    - ROADMAP.md Phase 58 contains `4 plans` without `TBD`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Full phase test command green
- [ ] CRQ-01, CRQ-02, CRQ-03 covered by contract + unit tests
</verification>
