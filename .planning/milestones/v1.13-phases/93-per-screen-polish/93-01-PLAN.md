---
phase: 93-per-screen-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead_admin/priv/static/css/rulestead_admin.css
  - examples/demo/frontend/tests/design-system.spec.ts
autonomous: true
requirements: [A11Y-01, SCRN-01]

must_haves:
  truths:
    - "Accent badge text in light mode is WCAG AA (≥4.5:1) — confirmed by design-system spec at the normal threshold"
    - "Accent badge text in dark mode remains WCAG AA — dark --rs-accent (#e8834a) unchanged and gate still green"
    - "design-system.spec.ts accent-light entry uses --rs-accent = #9a3f12, threshold normal, no `large` workaround"
    - "Both-theme sweep of representative screens shows no straggler (broken surface, unreadable text, light-bleed)"
    - "Synced-pair check prints IDENTICAL (light Block 1 and Block 4 carry identical --rs-accent value)"
    - "All 28/28 Playwright specs pass; mix compile --warnings-as-errors exits 0"
    - "Literal-scan gate stays at 0 hardcoded color values"
  artifacts:
    - path: "rulestead_admin/priv/static/css/rulestead_admin.css"
      provides: "--rs-accent in Block 1 (.rs-shell,[data-rulestead]) and Block 4 ([data-theme=light]) both set to #9a3f12"
      contains: "#9a3f12"
    - path: "examples/demo/frontend/tests/design-system.spec.ts"
      provides: "accent badge light pair asserts fg=#9a3f12 bg=#fde8dc at normal threshold"
      contains: "#9a3f12"
  key_links:
    - from: ".rs-badge[data-tone='accent'], .rs-badge[data-tone='draft']"
      to: "--rs-accent"
      via: "color: var(--rs-accent)"
      pattern: "color.*var\\(--rs-accent\\)"
    - from: "design-system.spec.ts"
      to: "rulestead_admin.css --rs-accent Block 1/4"
      via: "literal hex assertion"
      pattern: "9a3f12"
---

<objective>
Fix the one concrete A11Y-01 straggler (accent badge in light mode fails 4.5:1) and confirm the
broadened both-theme real-screen sweep shows no additional straggler (SCRN-01 evidence).

Purpose: Phase 91's design-system gate revealed that `--rs-accent` `#c45c26` on `--rs-accent-soft`
`#fde8dc` yields 3.62:1 — below the normal-text AA bar for badge text rendered at
`--rs-text-2xs`. This plan fixes it by darkening `--rs-accent` to `#9a3f12` (5.74:1) in the two
light-mode cascade blocks, updates the design-system spec to enforce the real value at the normal
4.5 threshold, then runs the broadened sweep to confirm no further stragglers.

Output:
- `rulestead_admin.css` — `--rs-accent` in Blocks 1 and 4 changed from `#c45c26` to `#9a3f12`
- `design-system.spec.ts` — accent-light pair updated to `#9a3f12`, threshold changed from
  `"large"` to `"normal"` (default), comment revised to match
- Both-theme sweep screenshots at `/tmp/rs-shots/screens/` (broadened coverage)
- Verified: synced-pair IDENTICAL, 28/28 specs, compile clean, literal-scan 0
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-design-system-consolidation/91-VERIFICATION.md
@.planning/phases/93-per-screen-polish/93-CONTEXT.md
@.planning/phases/93-per-screen-polish/93-VALIDATION.md

<interfaces>
From rulestead_admin/priv/static/css/rulestead_admin.css — the four cascade blocks:

  Block 1 (light default) — .rs-shell, [data-rulestead]  ~line 225
    --rs-accent:      #c45c26;    ← CHANGE to #9a3f12
    --rs-accent-soft: #fde8dc;    ← unchanged

  Block 2 (system dark) — @media (prefers-color-scheme: dark) .rs-shell:not([data-theme])  ~line 341
    --rs-accent:      #e8834a;    ← unchanged (dark passes at 5.38:1)
    --rs-accent-soft: rgba(232, 131, 74, 0.12);

  Block 3 (explicit dark pin) — .rs-shell[data-theme="dark"]  ~line 423
    --rs-accent:      #e8834a;    ← unchanged (SYNCED PAIR with Block 2)
    --rs-accent-soft: rgba(232, 131, 74, 0.12);

  Block 4 (explicit light pin) — .rs-shell[data-theme="light"]  ~line 505
    --rs-accent:      #c45c26;    ← CHANGE to #9a3f12 (SYNCED PAIR with Block 1)
    --rs-accent-soft: #fde8dc;    ← unchanged

Badge rule consuming --rs-accent (line ~2720):
  .rs-badge[data-tone="accent"],
  .rs-badge[data-tone="draft"] {
    border-color: var(--rs-accent);
    background: var(--rs-accent-soft);
    color: var(--rs-accent);         ← badge text color — the AA-failing path in light
  }

From design-system.spec.ts — the failing assertion to fix (~line 55):
  { label: "accent badge: --rs-accent on --rs-accent-soft (large)",  fg: "#c45c26", bg: "#fde8dc", level: "large" },
  ↓ becomes:
  { label: "accent badge: --rs-accent on --rs-accent-soft",  fg: "#9a3f12", bg: "#fde8dc" },
  (no `level` key → defaults to "normal" ≥4.5:1; comment above batch updated to remove the exception note)

Dark badge assertion (line ~104) stays unchanged:
  { label: "accent badge dark: --rs-accent on surface-muted",  fg: "#e8834a", bg: "#1f2a38" },

Contrast math (verified):
  #9a3f12 on #fde8dc = 5.74:1  ✓ normal AA
  #e8834a on #1f2a38 = 5.38:1  ✓ normal AA (existing dark, unchanged)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Darken --rs-accent in light Blocks 1 and 4; restore design-system spec to normal threshold</name>
  <files>
    rulestead_admin/priv/static/css/rulestead_admin.css,
    examples/demo/frontend/tests/design-system.spec.ts
  </files>

  <read_first>
    - rulestead_admin/priv/static/css/rulestead_admin.css — read both light blocks (Block 1 ~line 259 and Block 4 ~line 505) to confirm current `--rs-accent: #c45c26` before editing; do not re-read after edit
    - examples/demo/frontend/tests/design-system.spec.ts — read the "light: badge text on soft surfaces passes AA" test (~line 46) and its `assertAABatch` call to see the exact current entry before editing
  </read_first>

  <action>
    CSS changes (rulestead_admin.css):

    In Block 1 (.rs-shell, [data-rulestead]) Brand section:
      Change: --rs-accent:  #c45c26;
      To:     --rs-accent:  #9a3f12;
    Leave --rs-accent-soft: #fde8dc unchanged.

    In Block 4 (.rs-shell[data-theme="light"], [data-rulestead][data-theme="light"]) Brand section:
      Change: --rs-accent:  #c45c26;
      To:     --rs-accent:  #9a3f12;
    Leave --rs-accent-soft: #fde8dc unchanged.

    Do NOT touch Blocks 2 or 3 (dark — unchanged). Do NOT touch the badge rule itself;
    `color: var(--rs-accent)` already points at the token. Do NOT add any new token
    or badge-specific rule — the single token change is sufficient (per D-01 in CONTEXT.md).

    Spec changes (design-system.spec.ts):

    In the "light: badge text on soft surfaces passes AA" test, find the accent batch entry:
      { label: "accent badge: --rs-accent on --rs-accent-soft (large)",  fg: "#c45c26", bg: "#fde8dc", level: "large" },

    Replace it with:
      { label: "accent badge: --rs-accent on --rs-accent-soft",  fg: "#9a3f12", bg: "#fde8dc" },
    (Remove `level: "large"` — omitting `level` defaults the assertAABatch helper to "normal" ≥4.5:1.)

    Also update the comment block immediately above that assertAABatch call. Currently it reads:
      // success / warning / critical / neutral all achieve normal-text AA (≥ 4.5:1).
      // accent badge (#c45c26 on #fde8dc = 3.62:1) is large/UI compliant (≥ 3.0:1) only —
      // this is the known shipped value; it is gated at the large threshold here so that
      // any future regression (toward a lower ratio) will fail this spec.
    Replace with:
      // All five badge tones achieve normal-text AA (≥ 4.5:1) in light mode.
      // accent: #9a3f12 on #fde8dc = 5.74:1 — fixed in Phase 93 (A11Y-01).

    No other changes to the spec file.
  </action>

  <verify>
    <automated>
      cd /Users/jon/projects/rulestead && python3 scripts/check_synced_pair.py
    </automated>
    <automated>
      cd /Users/jon/projects/rulestead/examples/demo/frontend && npx playwright test tests/design-system.spec.ts --reporter=line 2>&1 | tail -20
    </automated>
    <automated>
      cd /Users/jon/projects/rulestead/rulestead_admin && mix compile --warnings-as-errors 2>&1 | tail -10
    </automated>
    <automated>
      grep -v '^[[:space:]]*/\*\|^[[:space:]]*//' /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css | grep -E 'rgba\([[:space:]]*[0-9]+[[:space:]]*,[[:space:]]*[0-9]+[[:space:]]*,[[:space:]]*[0-9]+[[:space:]]*\)|#[0-9a-fA-F]{3,6}' | grep -v 'var(--rs-\|color-scheme\|rs-shadow\|rs-focus-ring\|rs-overlay\|rs-scrim\|rs-disabled\|rs-primary-ring\|neutral-[0-9]' | grep -cE 'rgba|#' || true
    </automated>
  </verify>

  <acceptance_criteria>
    - `python3 scripts/check_synced_pair.py` prints IDENTICAL (Block 1 and Block 4 are verbatim-identical, now both carrying #9a3f12)
    - design-system.spec.ts accent-light assertion uses fg="#9a3f12", bg="#fde8dc", no `level` key → passes at normal (≥4.5:1) threshold; wcagRatio("#9a3f12","#fde8dc") = 5.74:1
    - 28/28 Playwright specs pass (0 failures, no skips), including the previously-failing accent-light assertion now passing at normal threshold
    - `mix compile --warnings-as-errors` exits 0
    - The `large` workaround comment and `level: "large"` entry are absent from design-system.spec.ts
    - Dark accent entry in spec unchanged: fg="#e8834a" on bg="#1f2a38"
  </acceptance_criteria>

  <done>
    --rs-accent in light Blocks 1 and 4 is #9a3f12 (5.74:1 on accent-soft); design-system spec
    enforces it at the normal 4.5 threshold; synced-pair IDENTICAL; 28/28 specs green; compile clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Broadened both-theme screenshot sweep (SCRN-01 evidence); fix any genuine straggler</name>
  <files>
    rulestead_admin/priv/static/css/rulestead_admin.css
  </files>

  <read_first>
    - .planning/phases/91-design-system-consolidation/91-VERIFICATION.md — existing 13-screen sweep coverage (home, flags, flag-detail, rules, rollouts, explain, audit, experiments, audiences, schedule, diagnostics, compare, change-requests)
    - .planning/phases/93-per-screen-polish/93-CONTEXT.md — established Playwright sign-in→screenshot pattern; localStorage preset; demo backend http://127.0.0.1:60485
  </read_first>

  <action>
    The existing sweep covered 13 screens. Extend it to the remaining screen TYPES not yet captured.
    Use the established pattern from `examples/demo/frontend/tests/support/admin.ts` (sign-in→navigate→screenshot).
    Preset theme via `addInitScript` setting `localStorage["rulestead_admin.theme"]` for dark screenshots.

    Target additional screen types (choose from these patterns to maximize TYPE coverage):
      - simulate screen (e.g. /admin/flags/:id/simulate or /admin/flags/:id/testing)
      - kill-switch / archive screen (e.g. /admin/flags/:id/kill or flag archive state)
      - change-request detail (e.g. /admin/change_requests/:id)
      - audience detail (e.g. /admin/audiences/:id)
      - scheduled-execution detail (e.g. /admin/schedule/:id or similar)
      - experiment detail (e.g. /admin/experiments/:id)

    Screenshot each in BOTH themes (light default + dark pin). Save to `/tmp/rs-shots/screens/`.
    Name files descriptively: e.g. `93-simulate-light.png`, `93-simulate-dark.png`.

    Visually inspect each screenshot pair for:
      - Broken elevation (a card/panel that should be raised reads as sunken or flush)
      - Unreadable text (low contrast — any text that is visually hard to read)
      - Light-bleed (a light-mode color leaking into dark mode)
      - Missing icons or broken layout (overflow, clipped content)

    If ANY genuine defect is found: fix it token-driven in rulestead_admin.css (no new literals;
    re-use existing semantic tokens). Re-screenshot to confirm fix. If no defect: record "sweep
    clean — no straggler found" in the task summary.

    If the demo backend is not running at :60485, note it and use the file:// harness instead for
    the design-system fixture only (the screen sweep requires the live backend).

    Literal-scan after any CSS edit:
      grep -v '^[[:space:]]*/\*' rulestead_admin/priv/static/css/rulestead_admin.css | \
        grep -c 'rgba([0-9]' || true
    Confirm count has not increased from baseline (it should be the same as post-Phase-88 baseline).
  </action>

  <verify>
    <automated>
      ls /tmp/rs-shots/screens/ | grep '^93-' | wc -l
    </automated>
    <automated>
      cd /Users/jon/projects/rulestead/examples/demo/frontend && npx playwright test --reporter=line 2>&1 | tail -5
    </automated>
    <human-check>
      Inspect /tmp/rs-shots/screens/93-*.png screenshot pairs (light + dark) for each screen type.
      Confirm: correct elevation, legible text, no light-bleed, no layout breaks in both themes.
    </human-check>
  </verify>

  <acceptance_criteria>
    - At least 8 new screenshots exist at /tmp/rs-shots/screens/93-*.png (4+ screen types × 2 themes)
    - Visual inspection confirms no straggler: all screenshotted screens show correct elevation, legible text, proper dark surfaces, no light-bleed
    - Any genuine straggler found is fixed token-driven (no literals); if CSS was edited, synced-pair re-checked IDENTICAL
    - 28/28 specs still green after any CSS edits
    - Literal-scan count unchanged from baseline (no new hardcoded color literals introduced)
    - Summary records outcome: "sweep clean" or lists the specific straggler(s) fixed with before/after token change
  </acceptance_criteria>

  <done>
    Broadened both-theme sweep covers representative screen types beyond the 13 already on file;
    any genuine straggler is fixed token-driven; no new literals; all gates green.
    SCRN-01 evidence is on record at /tmp/rs-shots/screens/.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token edit | Author changes a hex value in a static stylesheet — no runtime execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Tampering | rulestead_admin.css synced-pair | mitigate | `python3 scripts/check_synced_pair.py` → IDENTICAL required before commit; task acceptance criteria block on this |
| T-93-SC | Tampering | npm/playwright invocation | accept | No new npm packages installed; existing Playwright harness only |
</threat_model>

<verification>
Phase-complete checklist (run in order):

1. `python3 scripts/check_synced_pair.py` → prints IDENTICAL
2. `cd examples/demo/frontend && npx playwright test tests/design-system.spec.ts --reporter=line` → 28/28, 0 failures
3. `cd rulestead_admin && mix compile --warnings-as-errors` → exit 0
4. `grep -v '^[[:space:]]*/\*' rulestead_admin/priv/static/css/rulestead_admin.css | grep -c '#9a3f12'` → 2 (one per light block)
5. `grep 'large' examples/demo/frontend/tests/design-system.spec.ts | grep accent` → empty (workaround removed)
6. Visual: /tmp/rs-shots/screens/93-* screenshot pairs show no straggler
</verification>

<success_criteria>
- design-system gate reports 0 violations in both themes at the real thresholds (normal ≥4.5 for accent badge)
- `--rs-accent` in light Blocks 1 and 4 = `#9a3f12` (5.74:1 on accent-soft = `#fde8dc`)
- design-system.spec.ts accent-light entry: fg="#9a3f12", no `level` key, passes "normal" threshold
- synced-pair IDENTICAL
- Broadened both-theme sweep at /tmp/rs-shots/screens/: all representative screen types correct and on-brand
- 28/28 specs pass; mix compile clean; literal-scan 0
</success_criteria>

<output>
Create `.planning/phases/93-per-screen-polish/93-01-SUMMARY.md` when done.
</output>
