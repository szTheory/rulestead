---
phase: "90"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead_admin/priv/static/theme-control-harness.html
  - examples/demo/frontend/tests/theme-control.spec.ts
autonomous: true
requirements:
  - THM-02
  - THM-04

must_haves:
  truths:
    - "Playwright spec file exists and all tests are skipped/pending until Plan 02 ships the implementation"
    - "Fixture loads in a browser via file:// with .rs-shell, data-theme-pending, the radiogroup control markup, and inlined hook logic"
    - "Running the spec against the fixture produces a defined set of tests (not zero tests)"
  artifacts:
    - path: "rulestead_admin/priv/static/theme-control-harness.html"
      provides: "file:// fixture with control markup + inlined hook JS"
      contains: "rs-shell"
    - path: "examples/demo/frontend/tests/theme-control.spec.ts"
      provides: "Playwright spec covering all THM-02 + THM-04 behaviors"
      contains: "theme-control"
  key_links:
    - from: "theme-control.spec.ts"
      to: "theme-control-harness.html"
      via: "file:// path (same pattern as theme-cascade.spec.ts)"
      pattern: "theme-control-harness\\.html"
---

<objective>
Create the file:// test fixture and Playwright spec for the tri-state theme control. This is the Wave 0 test scaffold — all behaviors are specced before any production code ships.

Purpose: Establish the acceptance gate that Plan 02 must satisfy. Mirrors the proven pattern from theme-cascade.spec.ts / theme-harness.html.
Output: theme-control-harness.html (standalone fixture), theme-control.spec.ts (full behavioral spec).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-tri-state-theme-control/90-RESEARCH.md
@.planning/phases/90-tri-state-theme-control/90-VALIDATION.md

<interfaces>
<!-- Key contracts from existing fixture + spec patterns. Read once; do not re-read. -->

From rulestead_admin/priv/static/theme-harness.html (the fixture template):
- file:// loads rulestead_admin.css via relative path: `./css/rulestead_admin.css`
- `.rs-shell` is the scope root; `id="shell"` for JS access
- Inline `<script>` block at bottom for test helpers (e.g., window.setTheme / window.clearTheme)
- No Phoenix, no build step

From examples/demo/frontend/tests/theme-cascade.spec.ts (the spec template):
```typescript
const harnessUrl = "file://" + path.resolve(__dirname,
  "../../../../rulestead_admin/priv/static/theme-harness.html");

async function shellVar(page, varName) {
  return page.evaluate((v) => {
    const shell = document.querySelector(".rs-shell");
    return getComputedStyle(shell).getPropertyValue(v).trim();
  }, varName);
}

// Per-test: browser.newContext({ colorScheme: "dark" | "light" })
// Load via: page.goto(harnessUrl)
// localStorage: page.evaluate(() => localStorage.setItem("rulestead_admin.theme", "dark"))
// Reload: await page.reload()
// Attributes: page.locator(".rs-shell").getAttribute("data-theme")
// Keyboard: page.keyboard.press("ArrowRight")
// Focus: page.locator("[data-value='dark']").focus()
```

From 90-RESEARCH.md — hook logic contract (full skeleton on lines 387-481):
- Hook name: `.ThemeControl`; attaches to `id="rs-theme-control"` (the radiogroup div)
- `mounted()`: readTheme() → applyTheme() → removeAttribute("data-theme-pending") → syncAria() → matchMedia listener + click + keydown handlers
- `updated()`: syncAria() only
- `destroyed()`: removeEventListener on matchMedia
- localStorage key: `rulestead_admin.theme`, values: `system|light|dark`
- System = removeAttribute("data-theme"); light/dark = setAttribute("data-theme", value)
- whitelist: unknown values fall through to "system"

From 90-RESEARCH.md — control markup (lines 286-301):
- `role="radiogroup"` on the div, `role="radio"` + `aria-checked` on each button
- `data-value="system|light|dark"` on each button
- Roving tabindex: active=0, inactive=-1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme-control-harness.html fixture</name>
  <files>rulestead_admin/priv/static/theme-control-harness.html</files>
  <read_first>
    - rulestead_admin/priv/static/theme-harness.html — copy structure: DOCTYPE, charset, viewport, CSS link (`./css/rulestead_admin.css`), `.rs-shell` scope root, minimal shell markup, inline script block at bottom. Use `id="shell"` on the `.rs-shell` div.
    - .planning/phases/90-tri-state-theme-control/90-RESEARCH.md lines 385-481 — the complete hook skeleton to inline verbatim (adapted as a plain JS object, not a ColocatedHook script tag).
    - .planning/phases/90-tri-state-theme-control/90-RESEARCH.md lines 283-301 — the radiogroup markup to place inside the shell header context cluster.
  </read_first>
  <action>
    Create `rulestead_admin/priv/static/theme-control-harness.html` following the exact structure of `theme-harness.html` with these differences:

    1. Title: `rs theme-control harness`

    2. `.rs-shell` root element: add `data-theme-pending` attribute (static, no value — mirrors the planned HEEx server-render attribute). Use `id="shell"`.

    3. In the shell header, after the title div, add the theme control section (per RESEARCH.md lines 283-301):
       ```
       <section class="rs-shell__context" aria-label="Theme">
         <p class="rs-shell__context-label" id="rs-theme-label">Theme</p>
         <div id="rs-theme-control" role="radiogroup" aria-labelledby="rs-theme-label"
              class="rs-theme-control__group">
           <button type="button" role="radio" aria-checked="true"  tabindex="0"  data-value="system" class="rs-theme-control__opt">System</button>
           <button type="button" role="radio" aria-checked="false" tabindex="-1" data-value="light"  class="rs-theme-control__opt">Light</button>
           <button type="button" role="radio" aria-checked="false" tabindex="-1" data-value="dark"   class="rs-theme-control__opt">Dark</button>
         </div>
       </section>
       ```
       Note: in the harness the phx-hook attribute is omitted (no LiveView); the hook is wired manually below.

    4. In the inline `<script>` block at the bottom, implement the full hook logic verbatim from RESEARCH.md lines 397-479, adapted as follows:
       - Instead of a ColocatedHook object, create a plain IIFE that runs on `DOMContentLoaded`
       - `this.el` becomes `document.getElementById("rs-theme-control")`
       - `this._mode`, `this._mq`, `this._mqListener`, `this._syncAria`, `this._onClick`, `this._onKeydown` are declared as local `let`/`const` variables (or on a `hook` object for cleanness)
       - All logic (readTheme, writeTheme, applyTheme, syncAria, matchMedia, click, keydown) is identical to the RESEARCH.md skeleton — no simplification
       - Add input validation: `const VALID = ["system","light","dark"]; const readTheme = () => { try { const v = localStorage.getItem("rulestead_admin.theme"); return VALID.includes(v) ? v : "system" } catch(_) { return "system" } }`
       - After `applyTheme(readTheme())`, call `shell.removeAttribute("data-theme-pending")` and `syncAria()`

    5. Retain the scope probe from theme-harness.html (`#outside-shell` with `var(--rs-bg, red)`) at the bottom — useful visual sanity check.

    6. Add console helpers at the bottom of the script for manual testing:
       `window.themeHarnessSetTheme = (t) => { ... write localStorage and apply }` — mirrors the `window.setTheme` helper in theme-harness.html.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead && grep -c 'role="radiogroup"' rulestead_admin/priv/static/theme-control-harness.html && grep -c 'data-theme-pending' rulestead_admin/priv/static/theme-control-harness.html && grep -c 'rulestead_admin.theme' rulestead_admin/priv/static/theme-control-harness.html && grep -c 'removeAttribute.*data-theme-pending' rulestead_admin/priv/static/theme-control-harness.html</automated>
  </verify>
  <done>
    File exists; contains `role="radiogroup"`, `data-theme-pending` on `.rs-shell`, localStorage key `rulestead_admin.theme`, `removeAttribute("data-theme-pending")` call, and the three `data-value` buttons. All four grep counts are >= 1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create theme-control.spec.ts Playwright spec</name>
  <files>examples/demo/frontend/tests/theme-control.spec.ts</files>
  <read_first>
    - examples/demo/frontend/tests/theme-cascade.spec.ts — copy imports, harnessUrl pattern, shellVar helper. The new spec uses the same `browser.newContext` + `file://` approach.
    - .planning/phases/90-tri-state-theme-control/90-VALIDATION.md — the per-requirement verification map (all 10 THM-02 + THM-04 behaviors to test).
    - .planning/phases/90-tri-state-theme-control/90-RESEARCH.md lines 635-656 — the test case list with exact test name strings to use as `test(...)` descriptions.
  </read_first>
  <action>
    Create `examples/demo/frontend/tests/theme-control.spec.ts`. Mirror the structure of `theme-cascade.spec.ts` exactly.

    harnessUrl points to `theme-control-harness.html` (same relative path pattern — swap filename only).

    Helper `shellHasTheme(page, value)`: returns `page.locator("#shell").getAttribute("data-theme")` then compares.

    Write all 10 test cases from the VALIDATION.md verification map. Use these exact test description strings (matching the `--filter` flags in 90-RESEARCH.md lines 640-650):

    1. `"select dark — applies data-theme=dark on shell"`:
       - Set localStorage `dark`, reload, check `data-theme` is `"dark"`.
       - Also: click the Dark button directly, confirm attribute set.

    2. `"persists dark — localStorage written on select"`:
       - Click Dark button, read `localStorage.getItem("rulestead_admin.theme")`, expect `"dark"`.

    3. `"persists across reload — dark theme restored after page reload"`:
       - Set localStorage `dark` (via `page.evaluate`), reload page, check `data-theme === "dark"`.

    4. `"system removes attr — selecting system removes data-theme"`:
       - Set localStorage `dark`, reload. Then click System button. Check `data-theme` attribute is `null` (removed, not `"system"`).

    5. `"system follows OS — matchMedia dark OS change live-updates in system mode"`:
       - Load in light OS context, no localStorage. Confirm no `data-theme`. Emulate dark scheme change via `page.emulateMedia({ colorScheme: "dark" })`. System hook should stay in system mode; verify `data-theme` still absent (OS is handled by CSS @media, not JS).

    6. `"pinned ignores OS — pinned dark ignores OS color-scheme change"`:
       - Load with localStorage `dark`. Confirm `data-theme="dark"`. Emulate dark→light OS change. Confirm `data-theme` still `"dark"`.

    7. `"keyboard nav — arrow keys move radiogroup selection"`:
       - Focus the System button. Press ArrowRight. Confirm Light button has `aria-checked="true"` and `tabindex="0"`. Press ArrowRight again. Confirm Dark button has `aria-checked="true"`.

    8. `"aria-checked — tracks active option on click"`:
       - Click Dark button. Confirm Dark button `aria-checked="true"`, System and Light buttons `aria-checked="false"`.

    9. `"no animated wipe — data-theme-pending absent after hook mount"`:
       - Load page. Confirm `data-theme-pending` attribute is absent on `#shell` (hook removes it synchronously in mounted logic which runs on DOMContentLoaded in the harness).

    10. `"pending cleared — data-theme-pending removed after load regardless of pinned value"`:
        - Set localStorage `dark`. Reload. Confirm `data-theme-pending` absent AND `data-theme="dark"`.

    For the transition-suppression tests (cases 9 and 10): the harness can verify `data-theme-pending` is gone; the "no animated wipe" aspect is structurally guaranteed by `[data-theme-pending] * { transition: none }` in CSS. Add a comment in the spec explaining this: "Transition suppression is guaranteed by CSS rule '[data-theme-pending] * { transition: none }' which is lifted when the attribute is removed."

    Input validation case (implicit in test 3+): if localStorage contains an unknown value like `"purple"`, the hook defaults to system. Add one test:
    11. `"input validation — unknown localStorage value defaults to system"`:
        - `page.evaluate(() => localStorage.setItem("rulestead_admin.theme", "purple"))`, reload, confirm `data-theme` is `null` (system = no attribute).

    Use `colorScheme: "light"` as the default context for tests that don't specify. Always close contexts in `afterEach` or use `test.afterEach(async ({ browser }) => ...)` — mirror the `context.close()` pattern from theme-cascade.spec.ts.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/examples/demo/frontend && npx playwright test tests/theme-control.spec.ts --list 2>&1 | grep -c "theme-control"</automated>
  </verify>
  <done>
    File exists with 11 test cases. `--list` output shows all test names. The spec imports from `@playwright/test`, resolves `theme-control-harness.html` via file://, and covers all THM-02 + THM-04 behaviors from 90-VALIDATION.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → hook | Untrusted client storage value read on mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Tampering | localStorage `rulestead_admin.theme` read in hook | mitigate | Whitelist check in readTheme(): `VALID.includes(v) ? v : "system"` — unknown values silently default to system |
| T-90-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — existing Playwright only |
</threat_model>

<verification>
All of the following must pass before this plan is marked complete:

1. `grep -c 'role="radiogroup"' rulestead_admin/priv/static/theme-control-harness.html` returns >= 1
2. `grep -c 'data-theme-pending' rulestead_admin/priv/static/theme-control-harness.html` returns >= 1
3. `grep -c 'removeAttribute.*data-theme-pending' rulestead_admin/priv/static/theme-control-harness.html` returns >= 1
4. `grep -c 'rulestead_admin\.theme' rulestead_admin/priv/static/theme-control-harness.html` returns >= 1
5. `cd examples/demo/frontend && npx playwright test tests/theme-control.spec.ts --list 2>&1 | grep -c "theme-control"` returns >= 11
6. Existing cascade specs: `cd examples/demo/frontend && npx playwright test tests/theme-cascade.spec.ts` passes 5/5 (no regressions)
</verification>

<success_criteria>
- theme-control-harness.html: standalone file:// fixture with `.rs-shell[data-theme-pending]`, radiogroup control, inlined hook JS, CSS link
- theme-control.spec.ts: 11 test cases covering all THM-02 + THM-04 behaviors (select/persist/reload/system/OS-follow/pinned/keyboard/aria/no-wipe/pending/validation)
- Existing theme-cascade.spec.ts: 5/5 passing (no regressions from new files)
- No new npm packages required
</success_criteria>

<output>
Create `.planning/phases/90-tri-state-theme-control/90-01-SUMMARY.md` when done.
</output>
