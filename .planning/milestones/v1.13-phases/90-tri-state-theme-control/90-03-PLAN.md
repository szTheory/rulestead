---
phase: "90"
plan: "03"
type: execute
wave: 2
depends_on:
  - "90-01"
files_modified:
  - prompts/rulestead-host-app-integration-seam.md
autonomous: true
requirements:
  - THM-02
  - THM-04

must_haves:
  truths:
    - "Integration guide documents theme_default attr on Shell.page/1 (what it is, why, default value)"
    - "Integration guide documents the optional host <head> fast-path snippet (clearly marked optional, copy-paste ready)"
    - "Guide documents CSP nonce requirement for the runtime ColocatedHook script tag"
    - "Guide is accurate: no host LiveSocket wiring required; theme control is fully self-contained"
  artifacts:
    - path: "prompts/rulestead-host-app-integration-seam.md"
      provides: "Integration guide section covering theme persistence + FOUC + optional fast-path"
      contains: "theme_default"
  key_links:
    - from: "prompts/rulestead-host-app-integration-seam.md"
      to: "Shell.page/1 theme_default attr"
      via: "inline documentation reference"
      pattern: "theme_default"
---

<objective>
Document the theme_default attribute and the optional host <head> fast-path snippet in the integration guide. This is the "documented" half of CONTEXT.md's requirement: the optional layer-3 head script is documented, NOT required.

Purpose: Satisfies the ROADMAP Phase 90 success criterion 5 ("The optional theme_default attribute on shell.ex page/1 is documented; a host that supplies it gets the correct initial token without JS").
Output: Updated prompts/rulestead-host-app-integration-seam.md with a new theme persistence section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/90-tri-state-theme-control/90-CONTEXT.md
@.planning/phases/90-tri-state-theme-control/90-RESEARCH.md
@.planning/phases/90-tri-state-theme-control/90-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document theme_default and optional fast-path in integration guide</name>
  <files>prompts/rulestead-host-app-integration-seam.md</files>
  <read_first>
    - prompts/rulestead-host-app-integration-seam.md — read the full file to find the correct insertion point (end of file, or after any existing "Theme" section if present). Match the existing heading style and document conventions.
    - .planning/phases/90-tri-state-theme-control/90-CONTEXT.md — decisions section: the optional host head-script (Layer 3) and theme_default attr spec.
    - .planning/phases/90-tri-state-theme-control/90-RESEARCH.md lines 531-548 — the optional host <head> snippet (copy-paste ready).
    - .planning/phases/90-tri-state-theme-control/90-RESEARCH.md lines 581-590 — the theme_default attr recommendation (how the hook reads it as a fallback).
    - .planning/phases/90-tri-state-theme-control/90-RESEARCH.md lines 684-688 — CSP nonce note.
  </read_first>
  <action>
    Read prompts/rulestead-host-app-integration-seam.md in full. If a "Theme" or "Dark mode" section already exists, extend it. Otherwise append a new top-level section at the end.

    Add the following content (adapt heading level to match existing document style):

    ---

    ## Theme Persistence and Dark Mode

    `rulestead_admin` ships a built-in tri-state theme control (System / Light / Dark) in the shell header. No host configuration is required for it to work. This section documents optional host-side integrations for advanced scenarios.

    ### How it works (no host action required)

    - **System mode** (default): the CSS `@media (prefers-color-scheme: dark)` rule fires at first paint with no JavaScript. System users never see a flash.
    - **Pinned mode** (Light or Dark): the operator's choice is stored in `localStorage["rulestead_admin.theme"]` (origin-scoped, per-device). The `.ThemeControl` colocated hook reads this value synchronously in `mounted()`, sets `data-theme` on `.rs-shell`, and removes a `data-theme-pending` attribute — producing an instant snap with no animated wipe and no host round-trip.

    No host `LiveSocket` configuration is needed. The `.ThemeControl` hook is a `.`-prefixed runtime `ColocatedHook` and self-registers automatically.

    ### `theme_default` attribute (optional)

    `Shell.page/1` accepts an optional `theme_default` attribute:

    ```elixir
    attr(:theme_default, :string, default: "system")
    ```

    Pass it when you want the admin to default to a specific theme for all users who have not yet made a manual selection:

    ```heex
    <Shell.page theme_default="dark" ...>
    ```

    Valid values: `"system"` (default), `"light"`, `"dark"`. Unknown values are silently treated as `"system"`.

    The hook reads `theme_default` from a `data-theme-default` attribute on the control element. `localStorage` takes precedence over `theme_default` — once an operator has manually selected a theme, their choice wins on subsequent loads.

    ### Optional: host `<head>` fast-path (layer 3)

    > **This is optional.** The baseline (`data-theme-pending` + hook snap) already produces an invisible correction for pinned users on modern connections. This snippet eliminates the brief window between HTML paint and LiveView mount on slow connections or slow devices.

    Copy-paste into your host app's `<head>` (before `<body>`) if you want zero flash for pinned-mismatch on all connections:

    ```html
    <!-- Optional: include in host app's <head> to eliminate pinned-mismatch flash
         even before LiveView connects. The .ThemeControl hook will later take
         ownership and manage all theme state normally. -->
    <script>
      (function() {
        try {
          var t = localStorage.getItem("rulestead_admin.theme");
          if (t === "dark" || t === "light") {
            var shell = document.querySelector(".rs-shell");
            if (shell) shell.setAttribute("data-theme", t);
          }
        } catch (_) {}
      })();
    </script>
    ```

    This script is safe to include unconditionally — it no-ops for System users and for users with no stored preference. It does not interfere with the `.ThemeControl` hook.

    ### CSP considerations

    The `.ThemeControl` hook ships as a `<script>` tag inside the mounted package (a Phoenix `ColocatedHook` with the `runtime` attribute). If your host app enforces a strict `Content-Security-Policy` with `script-src` that excludes `'unsafe-inline'`, you must ensure the script nonce is propagated.

    Pass the CSP nonce via the live session assigns to ensure coverage. See the [Phoenix LiveView CSP documentation](https://hexdocs.pm/phoenix_live_view/security-model.html) for nonce propagation details.

    The optional layer-3 `<head>` snippet above also requires a nonce if `'unsafe-inline'` is not allowed. Add `nonce="<%= @csp_nonce %>"` to the `<script>` tag.

    ---

    Preserve all existing content. Only add; do not modify existing sections.
  </action>
  <verify>
    <automated>grep -c 'theme_default' /Users/jon/projects/rulestead/prompts/rulestead-host-app-integration-seam.md && grep -c 'data-theme-pending' /Users/jon/projects/rulestead/prompts/rulestead-host-app-integration-seam.md && grep -c 'rulestead_admin.theme' /Users/jon/projects/rulestead/prompts/rulestead-host-app-integration-seam.md</automated>
  </verify>
  <done>
    File contains: `theme_default` (>= 2 occurrences — attr decl + usage example), `data-theme-pending` (>= 1), `rulestead_admin.theme` (>= 1). The optional head-script snippet is present and clearly marked "Optional". CSP note is present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → host developer | Documentation accuracy is the trust surface — wrong docs produce misconfigured hosts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-05 | Information Disclosure | CSP nonce in optional head script | accept | The nonce pattern is documented; the script itself is inert (read localStorage, set attribute); no PII handled |
| T-90-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
All of the following must pass before this plan is marked complete:

1. `grep -c 'theme_default' prompts/rulestead-host-app-integration-seam.md` returns >= 2
2. `grep -c 'data-theme-pending' prompts/rulestead-host-app-integration-seam.md` returns >= 1
3. `grep -c 'rulestead_admin\.theme' prompts/rulestead-host-app-integration-seam.md` returns >= 1
4. `grep -c 'Optional' prompts/rulestead-host-app-integration-seam.md` returns >= 1 (the fast-path is marked optional)
5. `grep -c 'CSP\|nonce' prompts/rulestead-host-app-integration-seam.md` returns >= 1
</verification>

<success_criteria>
- Integration guide updated with: theme persistence overview (system vs pinned), theme_default attr spec with example, optional layer-3 head-script (clearly marked optional, copy-paste ready), CSP nonce note
- No existing guide content removed or modified
- Phase 90 success criterion 5 satisfied: "The optional theme_default attribute on shell.ex page/1 is documented; a host that supplies it gets the correct initial token without JS"
</success_criteria>

<output>
Create `.planning/phases/90-tri-state-theme-control/90-03-SUMMARY.md` when done.
</output>
