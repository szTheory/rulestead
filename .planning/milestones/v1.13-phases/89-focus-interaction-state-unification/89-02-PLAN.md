---
phase: 89-focus-interaction-state-unification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead_admin/priv/static/css/rulestead_admin.css
autonomous: true
requirements: [A11Y-02, A11Y-03]

must_haves:
  truths:
    - "Keyboard-tabbing through every interactive element shows one consistent two-stop ring: a surface-colored inner gap + brand outer ring — visible on light card, dark surface, AND colored fills (primary button)"
    - "The focus ring meets WCAG 2.4.11/2.4.13 (≥3:1 vs adjacent, ≥2px perimeter) in both light and dark themes"
    - "No interactive element shows a bare outline:none without the box-shadow replacement present"
    - "No bare :focus { on interactive selectors (input/button/a/select/textarea/[role=...]) — only :focus-visible or :focus-within"
    - "Hover states are visually distinct (perceptible shift from resting) in both themes"
    - "Disabled states use --rs-disabled-bg/--rs-disabled-text, not opacity alone, and are legible in dark"
    - "The synced dark pair (system-dark @media block and explicit [data-theme=dark] block) remains character-for-character identical in the focus+disabled lines"
    - "mix compile --warnings-as-errors exits 0; all 8 existing theme-cascade/theme-scope Playwright specs pass"
  artifacts:
    - path: "rulestead_admin/priv/static/css/rulestead_admin.css"
      provides: "Two-stop --rs-focus-ring in all 4 cascade blocks"
      contains: "calc(var(--rs-focus-ring-offset) + 3px)"
    - path: "rulestead_admin/priv/static/css/rulestead_admin.css"
      provides: "Single canonical :where() base rule"
      contains: ":where(a,button,input,select,textarea"
    - path: "rulestead_admin/priv/static/css/rulestead_admin.css"
      provides: "Removed pale-outline input bug"
  key_links:
    - from: ".rs-shell :where(...):focus-visible"
      to: "var(--rs-focus-ring)"
      via: "box-shadow: var(--rs-focus-ring)"
      pattern: "box-shadow: var\\(--rs-focus-ring\\)"
    - from: "--rs-focus-ring (all 4 cascade blocks)"
      to: "--rs-surface (inner gap) + --rs-focus-ring-color (outer ring)"
      via: "0 0 0 var(--rs-focus-ring-offset) var(--rs-surface), 0 0 0 calc(var(--rs-focus-ring-offset) + 3px) var(--rs-focus-ring-color)"
      pattern: "calc\\(var\\(--rs-focus-ring-offset\\)"
---

<objective>
Unify all focus, hover, and disabled interaction states in rulestead_admin.css:
1. Upgrade --rs-focus-ring to the two-stop form in all four cascade blocks.
2. Add one canonical zero-specificity base rule via :where() that applies the ring to every interactive element.
3. Remove the three old focus idioms (pale outline, per-element outline, bare :focus on interactive selectors).
4. Audit hover tokens — ensure perceptible shift (≥3:1 from resting) in both themes.
5. Route disabled visuals through --rs-disabled-bg/--rs-disabled-text for dark legibility.

Purpose: Delivers A11Y-02 (unified ring, WCAG 2.4.11/2.4.13) and A11Y-03 (legible hover + disabled in both themes). Builds on Phase 87 tokens and Phase 88 color redirects.

Output: A modified rulestead_admin.css where one ring rule governs all interactive elements, the dark pair is synced, and no pale/invisible focus or bare outline:none remain.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/rulestead/.planning/PROJECT.md
@/Users/jon/projects/rulestead/.planning/ROADMAP.md
@/Users/jon/projects/rulestead/.planning/STATE.md
@/Users/jon/projects/rulestead/.planning/phases/89-focus-interaction-state-unification/89-CONTEXT.md
@/Users/jon/projects/rulestead/.planning/phases/89-focus-interaction-state-unification/89-VALIDATION.md
@/Users/jon/projects/rulestead/.planning/phases/88-hardcoded-color-remediation/88-01-SUMMARY.md

<interfaces>
<!-- CURRENT STATE (verified by grep at planning time; re-grep line numbers before editing) -->

<!-- === TOKEN BLOCKS (all 4 cascade blocks) === -->
<!-- Block 1 — Light default (.rs-shell, [data-rulestead]), lines ~197-206: -->
<!--   --rs-focus-ring-color: rgba(37, 99, 235, 0.55);              -->
<!--   --rs-focus-ring:       0 0 0 3px var(--rs-focus-ring-color); ← SINGLE-STOP (upgrade target) -->
<!--   --rs-primary-ring:     rgba(14, 165, 233, 0.22);             -->
<!--   --rs-disabled-bg:      var(--rs-neutral-100);                -->
<!--   --rs-disabled-text:    var(--rs-neutral-500);                -->

<!-- Block 2 — System dark (@media prefers-color-scheme: dark), lines ~279-284: -->
<!--   --rs-focus-ring-color: rgba(96, 165, 250, 0.75);              -->
<!--   --rs-focus-ring:       0 0 0 3px var(--rs-focus-ring-color); ← SINGLE-STOP -->
<!--   !! SYNCED PAIR: must remain identical to Block 3 !!           -->

<!-- Block 3 — Explicit dark ([data-theme="dark"]), lines ~361-366: -->
<!--   --rs-focus-ring-color: rgba(96, 165, 250, 0.75);              -->
<!--   --rs-focus-ring:       0 0 0 3px var(--rs-focus-ring-color); ← SINGLE-STOP -->
<!--   !! SYNCED PAIR: must remain identical to Block 2 !!           -->

<!-- Block 4 — Explicit light ([data-theme="light"]), lines ~443-448: -->
<!--   --rs-focus-ring-color: rgba(37, 99, 235, 0.55);              -->
<!--   --rs-focus-ring:       0 0 0 3px var(--rs-focus-ring-color); ← SINGLE-STOP -->

<!-- TARGET two-stop form (same for all 4 blocks, gap color tracks --rs-surface): -->
<!--   --rs-focus-ring: 0 0 0 var(--rs-focus-ring-offset) var(--rs-surface),  -->
<!--                    0 0 0 calc(var(--rs-focus-ring-offset) + 3px) var(--rs-focus-ring-color); -->
<!-- Note: --rs-focus-ring-offset is declared in :root (invariant) at ~2px. -->
<!-- --rs-surface is declared in each theme block so the gap tracks the theme surface. -->

<!-- === OLD FOCUS IDIOMS TO REMOVE/REPLACE === -->

<!-- IDIOM 1 — The pale-outline input bug (lines ~1183-1188): -->
<!--   .rs-shell input:focus,   -->
<!--   .rs-shell select:focus,  -->
<!--   .rs-shell textarea:focus { -->
<!--     outline: 2px solid var(--rs-primary-soft);  ← near-invisible on light -->
<!--     border-color: var(--rs-primary);            -->
<!--   }                                              -->
<!--   ACTION: Replace the outline with box-shadow: var(--rs-focus-ring); keep border-color. -->
<!--   Change :focus → :focus-visible (keyboard-only). -->

<!-- IDIOM 2 — .rs-button--text bare outline:none (line ~585): -->
<!--   .rs-shell .rs-button--text:hover:not(:disabled), -->
<!--   .rs-shell .rs-button--text:focus-visible:not(:disabled) { -->
<!--     outline: none;    ← bare, no box-shadow replacement on this rule -->
<!--     box-shadow: none; ← explicitly kills ring on focus-visible -->
<!--   }                   -->
<!--   ACTION: Remove outline:none and box-shadow:none from this rule. -->
<!--   The canonical :where() rule will handle .rs-button--text:focus-visible. -->
<!--   The hover branch is fine without focus ring (hover != keyboard focus). -->

<!-- IDIOM 3 — Filter panel omnisearch input (lines ~2074-2077): -->
<!--   .rs-filter-panel__search .rs-omnisearch__control input:focus { -->
<!--     outline: none;    -->
<!--     box-shadow: none; -->
<!--   }                   -->
<!--   CONTEXT: This input is inside an omnisearch container that already has a border; -->
<!--   the outer container provides the visual focus affordance via :focus-within. -->
<!--   ACTION: Change :focus → :focus-within and move it to the container selector: -->
<!--     .rs-filter-panel__search .rs-omnisearch__control:focus-within { -->
<!--       border-color: var(--rs-primary); -->
<!--     }                                  -->
<!--   Then delete the input:focus rule entirely (the :where() base rule will not harm -->
<!--   it since the outer container visually owns focus here). -->

<!-- IDIOM 4 — CmdK input (lines ~4181-4184): -->
<!--   .rs-shell .rs-cmdk__input:focus { -->
<!--     outline: none;    -->
<!--     box-shadow: none; -->
<!--   }                   -->
<!--   CONTEXT: CmdK input is inside a modal backdrop; the modal container provides -->
<!--   context; the input is unstyled by design (no border). Suppressing ring is correct -->
<!--   because the user has already opened the modal intentionally (pointer/keyboard). -->
<!--   ACTION: Change to :focus-visible to narrow to keyboard-only. Keep outline:none -->
<!--   AND box-shadow:none here — this is a legitimate explicit override because the -->
<!--   cmdk modal context makes a ring redundant and visually incorrect. -->
<!--   Add a comment: /* cmdk: modal context; ring suppressed intentionally */ -->

<!-- IDIOM 5 — .rs-omnisearch__token-remove:focus-visible (lines ~2130-2135): -->
<!--   has outline:none but NO box-shadow replacement. -->
<!--   ACTION: Add box-shadow: var(--rs-focus-ring); to this rule. -->
<!--   (The :where() base rule would cover it, but the existing per-element rule -->
<!--   overrides it with outline:none — fix by adding box-shadow here.) -->

<!-- IDIOM 6 — .rs-radio-card input:focus-visible (line ~880): -->
<!--   outline: 3px solid var(--rs-focus-ring-color);  ← raw outline, not ring token -->
<!--   ACTION: Replace with box-shadow: var(--rs-focus-ring); remove outline. -->
<!--   Keep outline-offset: 2px behavior via the box-shadow gap stop. -->

<!-- IDIOM 7 — .rs-attention__card:focus-visible (lines ~2945-2948): -->
<!--   outline: none; box-shadow: var(--rs-focus-ring);  ← already uses token, just single-stop -->
<!--   ACTION: No change needed to this rule specifically; after --rs-focus-ring is upgraded -->
<!--   to two-stop, this rule will automatically get the two-stop ring. -->
<!--   Same for .rs-flag-subnav__tab, .rs-flag-subnav__kill, .rs-task-link, .rs-attention__card. -->

<!-- === CANONICAL BASE RULE TO ADD === -->
<!-- Insert immediately after the existing .rs-shell :where(a:hover) rule (~line 480), -->
<!-- before .rs-shell code { ... }: -->
<!--                                                                     -->
<!--   /* ── Unified :focus-visible ring (A11Y-02) ─────────────────── */  -->
<!--   .rs-shell :where(a, button, input, select, textarea,             -->
<!--     [tabindex], [role="option"], [role="tab"], summary):focus-visible { -->
<!--     outline: none;                                                  -->
<!--     box-shadow: var(--rs-focus-ring);                               -->
<!--     border-radius: inherit;                                         -->
<!--   }                                                                 -->
<!--                                                                     -->
<!-- This is a :where() rule (zero specificity) so per-element overrides win. -->

<!-- === HOVER AUDIT FINDINGS (at planning time) === -->
<!-- Most hover states are token-based and shift perceptibly. Two to verify: -->
<!-- 1. .rs-shell__rail-link:hover → background: var(--rs-surface-muted); color: var(--rs-text) -->
<!--    Light: surface-muted = --rs-neutral-100 (#eef1f5) vs resting --rs-bg (#f4f6f8). Shift is subtle. -->
<!--    FIX: Add border-color: var(--rs-border-strong) to make it more distinct. -->
<!-- 2. .rs-segmented-links a:hover → border: --rs-primary, bg: --rs-primary-soft, color: --rs-primary-hover -->
<!--    Both themes: primary-soft provides clear shift. OK. -->
<!-- 3. .rs-omnisearch__token-remove:hover → background: --rs-border-subtle. Subtle but acceptable (small control). -->

<!-- === DISABLED AUDIT FINDINGS (at planning time) === -->
<!-- .rs-shell button:disabled (line ~611): opacity: 0.55 only — no explicit bg/text token. -->
<!--   LIGHT: opacity 0.55 on --rs-surface bg + --rs-text text: text is still legible (≥3:1 at 0.55). -->
<!--   DARK: opacity 0.55 on --rs-surface-muted (#1f2a38) bg + --rs-text (#e8edf3) text: still legible. -->
<!--   However, to be explicit and future-proof: ADD background: var(--rs-disabled-bg); color: var(--rs-disabled-text) -->
<!--   to the .rs-shell button:disabled rule and remove the opacity. -->
<!--   Exception: input:disabled already correctly uses --rs-surface-faint + --rs-text-muted (opacity: 1). -->
<!-- .rs-radio-card input:disabled + .rs-radio-card__body (line ~891): opacity: 0.65 only. -->
<!--   ADD background: var(--rs-disabled-bg); color: var(--rs-disabled-text); to this rule. Remove opacity. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Upgrade --rs-focus-ring to two-stop form in all 4 cascade blocks</name>
  <files>rulestead_admin/priv/static/css/rulestead_admin.css</files>
  <read_first>
    Re-grep the exact current line numbers for the 4 `--rs-focus-ring:` declarations before editing (they may shift slightly from planning-time estimates): grep -n "\-\-rs-focus-ring:" rulestead_admin/priv/static/css/rulestead_admin.css. Read lines surrounding each hit (±3 lines) to confirm block context.
  </read_first>
  <action>
    In each of the four cascade blocks, replace the single-stop --rs-focus-ring declaration with the two-stop form. The replacement is identical in all four blocks:

    OLD (single-stop, appears 4 times):
      --rs-focus-ring:       0 0 0 3px var(--rs-focus-ring-color);

    NEW (two-stop):
      --rs-focus-ring:       0 0 0 var(--rs-focus-ring-offset) var(--rs-surface), 0 0 0 calc(var(--rs-focus-ring-offset) + 3px) var(--rs-focus-ring-color);

    The alignment comment "/* !! SYNCED PAIR ... */" blocks 2 and 3 must remain present and must be followed by the two-stop declaration. The outer ring uses --rs-focus-ring-color (theme-variant, already declared above this line in each block). The inner gap uses --rs-surface (also theme-variant, declared earlier in each block), so the gap tracks each theme's card surface.

    After the edit, verify the synced pair (blocks 2 + 3) are character-for-character identical in the --rs-focus-ring line:
      python3 -c "
    import re
    css = open('rulestead_admin/priv/static/css/rulestead_admin.css').read()
    blocks = re.findall(r'SYNCED PAIR.*?--rs-focus-ring:\s*(.*?);', css, re.DOTALL)
    assert len(blocks) == 2, f'Expected 2 synced-pair blocks, got {len(blocks)}'
    assert blocks[0].strip() == blocks[1].strip(), f'SYNCED PAIR mismatch:\n  Block2: {blocks[0].strip()}\n  Block3: {blocks[1].strip()}'
    print('SYNCED PAIR: IDENTICAL')
    "
  </action>
  <verify>
    grep -c "calc(var(--rs-focus-ring-offset) + 3px)" rulestead_admin/priv/static/css/rulestead_admin.css
    Expected: 4

    grep -c "0 0 0 3px var(--rs-focus-ring-color)" rulestead_admin/priv/static/css/rulestead_admin.css
    Expected: 0  (old single-stop removed from all 4 blocks)

    python3 synced-pair check (see action above): prints "SYNCED PAIR: IDENTICAL"
  </verify>
  <done>
    All four cascade blocks declare --rs-focus-ring with the two-stop form. grep -c "calc(var(--rs-focus-ring-offset) + 3px)" returns 4. Synced pair check passes. No single-stop form remains for --rs-focus-ring.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add canonical :where() base rule, remove old focus idioms, fix hover/disabled</name>
  <files>rulestead_admin/priv/static/css/rulestead_admin.css</files>
  <read_first>
    Re-grep line numbers for the following before editing (shifts are expected after Task 1):
      grep -n ":where(a:hover)\|input:focus,\|button--text:focus-visible\|omnisearch__control input:focus\|cmdk__input:focus\|omnisearch__token-remove:focus-visible\|radio-card input:focus-visible\|button:disabled {" rulestead_admin/priv/static/css/rulestead_admin.css
    Read each hit ±5 lines before editing to confirm current state.
  </read_first>
  <action>
    Apply the following seven targeted edits in order. Re-grep for current line numbers before each edit (after-edit line numbers shift). Do NOT batch all edits into a single write — apply and verify each idiom removal before the next.

    EDIT A — Insert canonical base rule
    Location: After the .rs-shell :where(a:hover) { ... } block (currently ends around line 483).
    Insert the following block immediately after the closing brace of .rs-shell :where(a:hover):

      /* ── Unified :focus-visible ring (A11Y-02) ──────────────────────────── */
      /* Zero-specificity via :where() — per-element rules above this override cleanly. */
      .rs-shell :where(a, button, input, select, textarea,
        [tabindex], [role="option"], [role="tab"], summary):focus-visible {
        outline: none;
        box-shadow: var(--rs-focus-ring);
        border-radius: inherit;
      }

    EDIT B — Fix the pale-outline input bug (idiom 1)
    Locate: .rs-shell input:focus, .rs-shell select:focus, .rs-shell textarea:focus { ... }
    Change selectors to :focus-visible (not :focus). Replace outline: 2px solid var(--rs-primary-soft) with box-shadow: var(--rs-focus-ring). Keep border-color: var(--rs-primary). Result:

      .rs-shell input:focus-visible,
      .rs-shell select:focus-visible,
      .rs-shell textarea:focus-visible {
        box-shadow: var(--rs-focus-ring);
        border-color: var(--rs-primary);
      }

    Note: After EDIT A, the :where() base rule already applies the ring. This explicit rule adds border-color: var(--rs-primary) (a useful extra affordance for form fields) and reinforces the ring with slightly higher specificity (component selector beats :where()). Keep it.

    EDIT C — Fix .rs-button--text:focus-visible (idiom 2)
    Locate: .rs-shell .rs-button--text:hover:not(:disabled), .rs-shell .rs-button--text:focus-visible:not(:disabled) { outline: none; box-shadow: none; ... }
    Remove outline: none and box-shadow: none from this rule. The :hover branch should only have its color/decoration changes; the :focus-visible branch gets the ring from :where(). Result: keep border:0, background:transparent, color:var(--rs-primary-hover), text-decoration declarations; delete only the two suppression lines.

    EDIT D — Fix omnisearch filter-panel input (idiom 3)
    Locate: .rs-filter-panel__search .rs-omnisearch__control input:focus { outline: none; box-shadow: none; }
    Replace with a :focus-within rule on the container that adds a visual affordance, and delete the bare input:focus rule:

      .rs-filter-panel__search .rs-omnisearch__control:focus-within {
        border-color: var(--rs-primary);
      }

    (The outer .rs-filter-panel__search has border-radius and background; the container already provides the visual context. The inner input gets :where() ring suppressed because it has border:0 and no background — a ring here would float on nothing. Adding :focus-within to the container is the correct idiom for this pattern.)

    EDIT E — Fix CmdK input (idiom 4)
    Locate: .rs-shell .rs-cmdk__input:focus { outline: none; box-shadow: none; }
    Change :focus to :focus-visible. Add comment:
      /* cmdk: inside modal; ring suppressed intentionally — modal context is the affordance */
    Keep outline: none and box-shadow: none. Result:

      .rs-shell .rs-cmdk__input:focus-visible {
        /* cmdk: inside modal; ring suppressed intentionally — modal context is the affordance */
        outline: none;
        box-shadow: none;
      }

    EDIT F — Fix omnisearch token-remove (idiom 5)
    Locate: .rs-omnisearch__token-remove:hover, .rs-omnisearch__token-remove:focus-visible { background: ...; color: ...; outline: none; }
    Add box-shadow: var(--rs-focus-ring) to the :focus-visible branch. Restructure into two rules to avoid box-shadow on hover:

      .rs-omnisearch__token-remove:hover {
        background: var(--rs-border-subtle);
        color: var(--rs-text);
      }
      .rs-omnisearch__token-remove:focus-visible {
        background: var(--rs-border-subtle);
        color: var(--rs-text);
        outline: none;
        box-shadow: var(--rs-focus-ring);
      }

    EDIT G — Fix radio-card focus (idiom 6)
    Locate: .rs-radio-card input:focus-visible + .rs-radio-card__body { outline: 3px solid var(--rs-focus-ring-color); outline-offset: 2px; }
    Replace with box-shadow on the body element (this is an adjacent-sibling rule; outline on the body works; box-shadow is the consistent idiom now):

      .rs-radio-card input:focus-visible + .rs-radio-card__body {
        box-shadow: var(--rs-focus-ring);
      }

    EDIT H — Fix button:disabled to use explicit tokens (disabled audit)
    Locate: .rs-shell button:disabled { opacity: 0.55; cursor: not-allowed; }
    Replace opacity:0.55 with explicit token-driven visuals:

      .rs-shell button:disabled {
        background: var(--rs-disabled-bg);
        color: var(--rs-disabled-text);
        border-color: var(--rs-border);
        cursor: not-allowed;
      }

    EDIT I — Fix radio-card disabled to use explicit tokens
    Locate: .rs-radio-card input:disabled + .rs-radio-card__body { cursor: not-allowed; opacity: 0.65; box-shadow: none; }
    Replace opacity:0.65 with explicit tokens:

      .rs-radio-card input:disabled + .rs-radio-card__body {
        cursor: not-allowed;
        background: var(--rs-disabled-bg);
        border-color: var(--rs-border);
        box-shadow: none;
      }

    EDIT J — Improve rail-link hover contrast
    Locate: .rs-shell__rail-link:hover { background: var(--rs-surface-muted); color: var(--rs-text); }
    Add border-left: 2px solid var(--rs-border-strong) or background: var(--rs-surface-muted) with an explicit stronger signal. The simplest legible fix that does not change layout: add a left-border accent:
      Actually check the resting state of .rs-shell__rail-link first (no border, bg transparent). A simpler approach: change hover background to var(--rs-primary-soft) and color to var(--rs-primary-hover) to match the existing [aria-current] treatment but without font-weight change. This gives a perceptible 3:1+ shift in both themes without layout change.

      .rs-shell__rail-link:hover {
        background: var(--rs-primary-soft);
        color: var(--rs-primary-hover);
      }
  </action>
  <verify>
    Run each gate in order. All must pass before the task is marked done.

    Gate 1 — Canonical rule exists (exactly one :where() focus-visible base rule):
      grep -c ':where(a, button, input, select, textarea,' rulestead_admin/priv/static/css/rulestead_admin.css
      Expected: 1

    Gate 2 — No bare outline:none outside the legitimate suppression sites:
    (Legitimate sites after this task: .rs-button--text no longer has it; cmdk has it with comment;
    the canonical :where() rule has it; component rules that use box-shadow have it paired with box-shadow.)
      # Count all outline:none lines:
      grep -n "outline: none\|outline:none" rulestead_admin/priv/static/css/rulestead_admin.css
    Review each returned line: every site must either be (a) the :where() base rule, (b) accompanied by box-shadow: var(--rs-focus-ring), or (c) the cmdk intentional suppression with comment. Zero bare, uncommented, no-box-shadow outline:none permitted outside those three categories.

    Gate 3 — No bare :focus { on interactive element selectors:
      grep -n "input:focus\b\|button:focus\b\|select:focus\b\|textarea:focus\b\|a:focus\b\|summary:focus\b" rulestead_admin/priv/static/css/rulestead_admin.css | grep -v ":focus-visible\|:focus-within"
      Expected: 0

    Gate 4 — Pale outline gone:
      grep -c "outline: 2px solid var(--rs-primary-soft)" rulestead_admin/priv/static/css/rulestead_admin.css
      Expected: 0

    Gate 5 — Two-stop token still present 4 times (Task 1 result intact):
      grep -c "calc(var(--rs-focus-ring-offset) + 3px)" rulestead_admin/priv/static/css/rulestead_admin.css
      Expected: 4

    Gate 6 — Disabled button uses token (not opacity):
      grep -A4 "button:disabled" rulestead_admin/priv/static/css/rulestead_admin.css | grep -c "rs-disabled-bg"
      Expected: >= 1

    Gate 7 — mix compile clean:
      cd rulestead && mix compile --warnings-as-errors 2>&1 | tail -5
      Expected: exit 0, no warnings

    Gate 8 — Playwright theme specs still pass:
      cd examples/demo/frontend && npx playwright test theme-cascade.spec.ts theme-scope.spec.ts --reporter=line 2>&1 | tail -10
      Expected: 8 passed, 0 failed
  </verify>
  <done>
    All 8 gates pass. One canonical :where() base rule present. Zero pale-outline or bare outline:none idioms remain outside the three legitimate categories. Zero bare :focus on interactive selectors. Four two-stop --rs-focus-ring declarations intact. Button disabled uses explicit --rs-disabled-bg/--rs-disabled-text tokens. Rail-link hover perceptibly shifts in both themes. mix compile clean. 8/8 Playwright theme specs pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rulestead_admin.css | Static file; rendered in host app via Phoenix mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-02-SC | Tampering | npm/pip installs | accept | No package installs in this plan; pure CSS edit |
| T-89-02-A11Y | Information Disclosure | focus ring visibility | mitigate | Two-stop ring with surface gap ensures ring is visible on any fill color, including colored buttons; WCAG 2.4.11 outer ring ≥3:1 enforced by existing --rs-focus-ring-color token values (verified by Phase 87) |
</threat_model>

<verification>
Overall phase-complete checks (run after both plans are executed):

1. One canonical base rule:
   grep -c ':where(a, button, input, select, textarea,' rulestead_admin/priv/static/css/rulestead_admin.css
   Expected: 1

2. No pale-outline bug:
   grep -c "outline: 2px solid var(--rs-primary-soft)" rulestead_admin/priv/static/css/rulestead_admin.css
   Expected: 0

3. No bare :focus on interactive selectors (excluding :focus-visible/:focus-within):
   grep -n "input:focus\b\|button:focus\b\|select:focus\b\|a:focus\b" rulestead_admin/priv/static/css/rulestead_admin.css | grep -v ":focus-visible\|:focus-within"
   Expected: 0 lines

4. Two-stop form in all 4 cascade blocks:
   grep -c "calc(var(--rs-focus-ring-offset) + 3px)" rulestead_admin/priv/static/css/rulestead_admin.css
   Expected: 4

5. Synced dark pair identical:
   python3 -c "
   import re
   css = open('rulestead_admin/priv/static/css/rulestead_admin.css').read()
   blocks = re.findall(r'SYNCED PAIR.*?--rs-focus-ring:\s*(.*?);', css, re.DOTALL)
   assert len(blocks) == 2 and blocks[0].strip() == blocks[1].strip(), 'MISMATCH'
   print('SYNCED PAIR: IDENTICAL')
   "

6. Disabled button uses token:
   grep -A5 "\.rs-shell button:disabled" rulestead_admin/priv/static/css/rulestead_admin.css | grep "rs-disabled"
   Expected: match found

7. mix compile clean:
   cd rulestead && mix compile --warnings-as-errors
   Expected: exit 0

8. Playwright theme specs:
   cd examples/demo/frontend && npx playwright test theme-cascade.spec.ts theme-scope.spec.ts --reporter=line
   Expected: 8 passed

9. Harness focus-target gates (Plan 01):
   grep -c 'role="tab"' rulestead_admin/priv/static/theme-harness.html       # >= 3
   grep -c 'rs-button--primary' rulestead_admin/priv/static/theme-harness.html # >= 2
   grep -c 'rs-button--danger' rulestead_admin/priv/static/theme-harness.html  # >= 1

10. Human focus-ring screenshot (checkpoint for executor):
    Open theme-harness.html in browser. In both themes (setTheme('dark') / clearTheme()):
    - Tab to Primary button → confirm two-stop ring visible (inner gap separates ring from blue fill)
    - Tab to text input on card → confirm ring visible on card surface
    - Tab to text input on page-bg → confirm ring visible
    - Tab to [role=tab] button → confirm ring visible
    WCAG check: outer ring color vs adjacent surface must report ≥3:1 (use browser DevTools accessibility inspector or axe extension).
</verification>

<success_criteria>
Phase 89 is complete when:
- Every interactive element in the admin shell receives the same two-stop :focus-visible ring via the single :where() base rule.
- The ring is visibly legible on light card, dark surface, and colored fills (primary button) — confirmed by screenshot in both themes.
- No bare outline:none without the box-shadow replacement remains.
- No bare :focus { on interactive element selectors.
- Hover states are perceptibly distinct from resting in both themes.
- Disabled states use --rs-disabled-bg/--rs-disabled-text (no lone opacity).
- Synced dark pair is character-identical after --rs-focus-ring upgrade.
- mix compile --warnings-as-errors exits 0.
- 8/8 existing Playwright theme-cascade/theme-scope specs pass.
</success_criteria>

<output>
Create .planning/phases/89-focus-interaction-state-unification/89-02-SUMMARY.md when done.
</output>
