---
phase: "91"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead_admin/priv/static/css/rulestead_admin.css
  - guides/flows/admin-ui.md
autonomous: true
requirements:
  - DSY-02

must_haves:
  truths:
    - "The CSS token section opens with a header comment that documents: the invariant-vs-variant split, the 4-block cascade structure with selector names, the :not([data-theme]) precedence rule, the SYNCED-PAIR maintenance rule with the Python verification command, and a 'How to add a token' recipe."
    - "The guide section in guides/flows/admin-ui.md references the token categories and links to the CSS as source of truth — no duplicated token list."
    - "A literal-scan for hardcoded color values outside the token blocks returns zero matches (confirming Phase 88 is complete and no new one-offs slipped in)."
    - "The synced dark pair remains identical after any new tokens (python3 SYNCED PAIR check prints IDENTICAL)."
  artifacts:
    - path: "rulestead_admin/priv/static/css/rulestead_admin.css"
      provides: "Token-contract header comment covering invariant/variant split, cascade, SYNCED-PAIR rule, scale categories, add-a-token recipe"
      contains: "SYNCED-PAIR"
    - path: "guides/flows/admin-ui.md"
      provides: "Token-contract reference section linking to CSS as source of truth"
      contains: "rulestead_admin.css"
  key_links:
    - from: "guides/flows/admin-ui.md#Design Token Contract"
      to: "rulestead_admin/priv/static/css/rulestead_admin.css"
      via: "explicit link in guide — CSS is source of truth, guide does not duplicate the token list"
      pattern: "rulestead_admin.css"
---

<objective>
Document the completed token contract directly in the CSS and in the admin-ui guide, and verify (via literal-scan) that Phase 88 left no one-off color values in the component region.

Purpose: DSY-02 documentation half. Future contributors and the automated gate (Plan 02) need a clear statement of invariant-vs-variant tokens, cascade precedence, the SYNCED-PAIR invariant, and a recipe for adding new tokens correctly. The guide links to the CSS so there is only one place to update when tokens change.

Output:
- `rulestead_admin.css` — a multi-section block comment at the top of the THEME LAYER, before Block 1 (light default).
- `guides/flows/admin-ui.md` — new "## Design Token Contract" section (concise, links to CSS, no duplicated token list).
- Zero one-off color literals in the component region (verified, not modified if already clean).
</objective>

<execution_context>
@/Users/jon/projects/rulestead/.planning/phases/91-design-system-consolidation/91-CONTEXT.md
@/Users/jon/projects/rulestead/.planning/phases/91-design-system-consolidation/91-VALIDATION.md
</execution_context>

<context>
@/Users/jon/projects/rulestead/.planning/PROJECT.md
@/Users/jon/projects/rulestead/.planning/ROADMAP.md

<interfaces>
<!-- Key structures the executor will extend/insert into -->

rulestead_admin/priv/static/css/rulestead_admin.css — THEME LAYER section (lines 121–455):

  Existing header at line 122:
    /* ─── THEME LAYER ────────────────────────────────────────────────────────
       All color, surface, border, text, shadow, and focus tokens are declared
       here — once per theme. Re-theming = redeclaring these tokens on the
       correct scope selector. Theme scope: .rs-shell / [data-rulestead] ONLY.
       NEVER declare theme tokens on :root or <html> (mounted-package discipline).
    ─────────────────────────────────────────────────────────────────────────── */

  Block 1 selector at line 129:  .rs-shell, [data-rulestead]   (light default)
  Block 2 at line 210:  @media (prefers-color-scheme: dark)  .rs-shell:not([data-theme]) ...
  Block 3 at line 293:  .rs-shell[data-theme="dark"], [data-rulestead][data-theme="dark"]
  Block 4 at line 374:  .rs-shell[data-theme="light"], [data-rulestead][data-theme="light"]
  Closing at line 455:  /* ─── END THEME LAYER ─── */

  :root invariant block at lines 39–119:
    Categories: Typography (families, scale, leading, weight, tracking),
    Radius, Layout+spacing (--rs-space-1..10, --rs-section-gap, --rs-page-gap),
    Control sizing, Focus structural scalars, Z-index, Motion (durations+easing)

  Theme-variant categories per block (declared in each of the 4 blocks):
    Neutral ramp (--rs-neutral-0/25/50/100/200/300/400/500/600/900)
    Surface/border/text aliases (--rs-bg, --rs-surface, --rs-surface-muted,
      --rs-surface-faint, --rs-border, --rs-border-subtle, --rs-border-strong,
      --rs-text, --rs-text-muted, --rs-text-placeholder)
    Brand (--rs-primary, --rs-primary-hover, --rs-primary-soft, --rs-on-primary,
      --rs-accent, --rs-accent-soft)
    Status success (--rs-success, --rs-success-hover, --rs-success-soft,
      --rs-success-bg-subtle, --rs-success-text, --rs-success-border)
    Status warning (--rs-warning, --rs-warning-hover, --rs-warning-soft,
      --rs-warning-text, --rs-warning-border)
    Status error/critical (--rs-error, --rs-error-hover, --rs-critical,
      --rs-error-soft, --rs-error-bg-subtle, --rs-error-text,
      --rs-error-text-strong, --rs-error-border, --rs-error-border-strong)
    Shadows (--rs-shadow-sm, --rs-shadow, --rs-shadow-panel)
    Focus + disabled (--rs-focus-ring-color, --rs-focus-ring, --rs-primary-ring,
      --rs-disabled-bg, --rs-disabled-text)
    Overlay/scrim (--rs-overlay-veil, --rs-scrim)

  SYNCED-PAIR rule: Block 2 (@media dark) and Block 3 ([data-theme="dark"]) must
    contain verbatim-identical --rs- declarations. Same for Block 1 and Block 4.
    Verification command (python3 brace-counter — fixed version from 87-03-PLAN):
      python3 - <<'PY'
      import sys
      css=open('rulestead_admin/priv/static/css/rulestead_admin.css').read()
      def decls(sel):
          i=css.find(sel)
          if i<0: return None
          j=css.find('{',i); depth=0; k=j
          while k<len(css):
              if css[k]=='{': depth+=1
              elif css[k]=='}':
                  depth-=1
                  if depth==0: break
              k+=1
          return sorted(l.strip() for l in css[j+1:k].splitlines() if l.strip().startswith('--rs-'))
      a=decls('@media (prefers-color-scheme: dark)')
      b=decls('.rs-shell[data-theme="dark"]')
      if a and a==b:
          print(f'SYNCED PAIR IDENTICAL ({len(a)} tokens)')
      else:
          print('SYNCED PAIR MISMATCH'); sys.exit(1)
      PY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add token-contract header comment to CSS THEME LAYER</name>
  <files>rulestead_admin/priv/static/css/rulestead_admin.css</files>

  <read_first>
    - rulestead_admin/priv/static/css/rulestead_admin.css lines 1–455 (already loaded above via context interfaces block — confirm the existing THEME LAYER header at line 122 and the four block selectors)
    - .planning/phases/91-design-system-consolidation/91-CONTEXT.md (decisions section — documents what to cover)
  </read_first>

  <action>
  Replace the existing thin THEME LAYER comment (lines 122–127) with an expanded multi-section header comment that documents the complete token contract. Keep the existing text as the opening sentence; add the following sections directly below it.

  The expanded comment must cover ALL of these (in this order):

  1. INVARIANT vs VARIANT SPLIT
     - Invariant tokens live in :root — not theme-sensitive. Categories: typography (families, scale, leading, weight, tracking), radius, layout/spacing (--rs-space-1..10, --rs-section-gap, --rs-page-gap), control sizing (--rs-control-h/h-sm/h-lg/px, --rs-touch-target-min), focus structural scalars (--rs-focus-ring-offset, --rs-disabled-opacity), z-index ladder (--rs-z-*), motion (--rs-motion-*, --rs-ease-*).
     - Variant tokens are declared per-theme in the 4 blocks below. Categories: neutral ramp, surface/border/text aliases, brand, status (success/warning/error), shadows, focus+disabled colors, overlay/scrim.

  2. CASCADE BLOCKS — list all four selectors, one per line, with a one-line description:
     Block 1: .rs-shell, [data-rulestead]              — light default (applies when no @media or data-theme)
     Block 2: @media (prefers-color-scheme: dark) .rs-shell:not([data-theme])  — system dark (active when OS is dark AND no explicit pin)
     Block 3: .rs-shell[data-theme="dark"]             — explicit dark pin (beats OS in both directions)
     Block 4: .rs-shell[data-theme="light"]            — explicit light pin (re-asserts light over dark OS)

     Precedence note: The :not([data-theme]) guard on Block 2 means an explicit pin (Block 3 or 4) ALWAYS wins over the @media rule regardless of OS setting. Block 1 and Block 4 contain identical light-mode declarations (SYNCED PAIR). Block 2 and Block 3 contain identical dark-mode declarations (SYNCED PAIR).

  3. SYNCED-PAIR RULE
     - Block 1 (light default) and Block 4 ([data-theme="light"]) are kept verbatim-identical. Block 2 (@media dark) and Block 3 ([data-theme="dark"]) are kept verbatim-identical. When you change a token value in one block of a pair, you MUST update the partner block to the identical value.
     - Verification command (run from repo root):
         python3 - <<'PY'
         import sys
         css=open('rulestead_admin/priv/static/css/rulestead_admin.css').read()
         def decls(sel):
             i=css.find(sel); j=css.find('{',i); depth=0; k=j
             while k<len(css):
                 if css[k]=='{': depth+=1
                 elif css[k]=='}':
                     depth-=1;
                     if depth==0: break
                 k+=1
             return sorted(l.strip() for l in css[j+1:k].splitlines() if l.strip().startswith('--rs-'))
         a=decls('@media (prefers-color-scheme: dark)')
         b=decls('.rs-shell[data-theme="dark"]')
         if a and a==b: print(f'SYNCED PAIR IDENTICAL ({len(a)} tokens)')
         else: print('SYNCED PAIR MISMATCH'); sys.exit(1)
         PY

  4. SCALE CATEGORIES (token names by category — invariant):
     Typography: --rs-font-display/sans/mono, --rs-text-2xs through 2xl, --rs-leading-*, --rs-weight-*, --rs-tracking-*
     Spacing: --rs-space-1 through 10, --rs-section-gap, --rs-page-gap
     Radius: --rs-radius-sm/md/lg/xl/full
     Z-index: --rs-z-base/raised/dropdown/sticky/overlay/modal/toast
     Motion: --rs-motion-fast/base/slow/slower, --rs-ease-standard/out/in/in-out/emphasis
     Control: --rs-control-h/h-sm/h-lg/px, --rs-touch-target-min
     Focus structural: --rs-focus-ring-offset, --rs-disabled-opacity

  5. SCALE CATEGORIES (variant — re-declared per block):
     Neutral ramp: --rs-neutral-0/25/50/100/200/300/400/500/600/900
     Surface/border/text: --rs-bg, --rs-surface, --rs-surface-muted, --rs-surface-faint, --rs-border, --rs-border-subtle, --rs-border-strong, --rs-text, --rs-text-muted, --rs-text-placeholder
     Brand: --rs-primary, --rs-primary-hover, --rs-primary-soft, --rs-on-primary, --rs-accent, --rs-accent-soft
     Status success: --rs-success, --rs-success-hover, --rs-success-soft, --rs-success-bg-subtle, --rs-success-text, --rs-success-border
     Status warning: --rs-warning, --rs-warning-hover, --rs-warning-soft, --rs-warning-text, --rs-warning-border
     Status error: --rs-error, --rs-error-hover, --rs-critical, --rs-error-soft, --rs-error-bg-subtle, --rs-error-text, --rs-error-text-strong, --rs-error-border, --rs-error-border-strong
     Shadows: --rs-shadow-sm, --rs-shadow, --rs-shadow-panel
     Focus+disabled: --rs-focus-ring-color, --rs-focus-ring, --rs-primary-ring, --rs-disabled-bg, --rs-disabled-text
     Overlay/scrim: --rs-overlay-veil, --rs-scrim

  6. HOW TO ADD A TOKEN (recipe — must appear verbatim in the comment):
     a. Decide: is the value the same in both themes? → invariant, add to :root only.
     b. If theme-sensitive: add the light value to Block 1 AND Block 4 (same value; update BOTH).
        Add the dark value to Block 2 AND Block 3 (same value; update BOTH).
     c. Run the SYNCED-PAIR verification command above and confirm IDENTICAL before committing.
     d. Add the token to the design-system fixture (design-system.html) and re-run design-system.spec.ts.

  Do NOT touch any token values, selectors, or component rules. Only insert/replace the comment block. The comment must be a standard CSS block comment ( /* ... */ ) spanning multiple lines. Keep it readable — use alignment and blank lines within the comment for the sections.

  After editing, run the one-off literal scan to confirm Phase 88 is complete and no one-offs have crept into the component section (lines 456 onward):

      awk 'NR>455' rulestead_admin/priv/static/css/rulestead_admin.css \
        | grep -E "rgba\(|#[0-9a-fA-F]{3,6}" \
        | grep -v "var(--rs-\|^\s*/\*\|^\s*\*" \
        | wc -l

  Expected: 0. If non-zero, tokenize the remaining literals (add to all 4 blocks per the recipe above), but do NOT change any values — only wrap in a token.
  </action>

  <verify>
    <automated>
      cd /Users/jon/projects/rulestead && python3 - &lt;&lt;'PY'
import sys
css=open('rulestead_admin/priv/static/css/rulestead_admin.css').read()
def decls(sel):
    i=css.find(sel); j=css.find('{',i); depth=0; k=j
    while k&lt;len(css):
        if css[k]=='{': depth+=1
        elif css[k]=='}':
            depth-=1
            if depth==0: break
        k+=1
    return sorted(l.strip() for l in css[j+1:k].splitlines() if l.strip().startswith('--rs-'))
a=decls('@media (prefers-color-scheme: dark)')
b=decls('.rs-shell[data-theme="dark"]')
if a and a==b: print(f'SYNCED PAIR IDENTICAL ({len(a)} tokens)')
else: print('SYNCED PAIR MISMATCH'); sys.exit(1)
PY
    </automated>
  </verify>

  <acceptance_criteria>
    - The CSS THEME LAYER header comment contains all six sections listed in the action: invariant-vs-variant split, cascade blocks (all 4 selectors + precedence note), SYNCED-PAIR rule with python verification command, scale categories invariant, scale categories variant, how-to-add-a-token recipe (6 steps).
    - grep -c "SYNCED-PAIR" rulestead_admin/priv/static/css/rulestead_admin.css returns >= 3 (one in the new header comment + the two existing inline !SYNCED PAIR! markers in blocks 2 and 3).
    - grep -c "How to add a token\|HOW TO ADD" rulestead_admin/priv/static/css/rulestead_admin.css returns >= 1.
    - grep -c "python3" rulestead_admin/priv/static/css/rulestead_admin.css returns >= 1 (the verification command is embedded in the comment).
    - SYNCED PAIR python script prints "SYNCED PAIR IDENTICAL" (exit 0).
    - Literal-scan on component region (lines 456+) returns 0: awk 'NR>455' rulestead_admin/priv/static/css/rulestead_admin.css | grep -E "rgba\(|#[0-9a-fA-F]{3,6}" | grep -v "var(--rs-" | grep -v "^\s*/\*\|^\s*\*" | wc -l outputs 0.
    - No token values changed: spot-check grep -m1 "rs-neutral-900.*1a2332" rulestead_admin.css returns a match (light #1a2332 unchanged) and grep -m1 "rs-neutral-900.*e8edf3" returns a match (dark #e8edf3 unchanged).
    - cd rulestead_admin && mix compile --warnings-as-errors exits 0.
  </acceptance_criteria>

  <done>CSS THEME LAYER header comment present with all six sections; SYNCED-PAIR check passes; literal-scan on component region = 0; mix compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add "Design Token Contract" section to guides/flows/admin-ui.md</name>
  <files>guides/flows/admin-ui.md</files>

  <read_first>
    - guides/flows/admin-ui.md (full file — needed to choose insertion point after the Stylesheet section)
    - rulestead_admin/priv/static/css/rulestead_admin.css lines 121–160 (the new header comment from Task 1 — link to its contents)
  </read_first>

  <action>
  Append a new "## Design Token Contract" section to guides/flows/admin-ui.md. Place it directly after the existing "## Stylesheet" section (around line 62). The section must be concise (under 40 lines) and must NOT duplicate the full token list — the CSS is the source of truth.

  The section must cover:
  1. One paragraph explaining the token system: `rulestead_admin.css` uses a two-level token model — theme-invariant tokens (typography, spacing, radius, z-index, motion, control sizing) declared once in `:root`, and theme-variant tokens (color, surface, border, shadow, focus, overlay) declared per-cascade-block inside `.rs-shell` / `[data-rulestead]`. All design tokens are prefixed `--rs-`.

  2. A table or list of the 4 cascade blocks (selector, when active):
     | Selector | When active |
     |----------|-------------|
     | `.rs-shell, [data-rulestead]` | Light default (no explicit pin or @media) |
     | `@media (prefers-color-scheme: dark) .rs-shell:not([data-theme])` | System dark (OS is dark, no explicit pin) |
     | `.rs-shell[data-theme="dark"]` | Explicit dark pin — overrides OS in both directions |
     | `.rs-shell[data-theme="light"]` | Explicit light pin — re-asserts light over dark OS |

  3. A brief paragraph on the SYNCED-PAIR rule: Blocks 1 + 4 (light) and Blocks 2 + 3 (dark) are kept verbatim-identical. When modifying a token, update both members of the pair. The CSS header comment includes a Python verification command.

  4. A "Adding a token" micro-recipe (3–5 bullet points matching the CSS recipe — paraphrase, not copy-paste):
     - Invariant values (same in both themes): add to `:root` only.
     - Theme-sensitive values: add the light value to Blocks 1 and 4; add the dark value to Blocks 2 and 3.
     - Run the SYNCED-PAIR check in the CSS comment header before committing.
     - Add the token to `design-system.html` and re-run `design-system.spec.ts`.

  5. A closing sentence linking to the CSS as source of truth:
     "The complete token catalog with exact names and values lives in `rulestead_admin/priv/static/css/rulestead_admin.css` under the INVARIANT TOKENS and THEME LAYER sections."

  Write only the new section — do not alter any other existing content in the guide.
  </action>

  <verify>
    <automated>grep -c "Design Token Contract" /Users/jon/projects/rulestead/guides/flows/admin-ui.md</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "Design Token Contract" guides/flows/admin-ui.md returns 1.
    - grep -c "SYNCED.PAIR\|synced.pair\|SYNCED_PAIR" guides/flows/admin-ui.md returns >= 1 (the rule is mentioned).
    - grep -c "rulestead_admin.css\|rulestead_admin/priv" guides/flows/admin-ui.md returns >= 2 (at least one existing reference + the new source-of-truth link).
    - The section does NOT contain a verbatim list of all 80+ token names (the CSS is source of truth — the guide only lists categories).
    - grep -c "design-system.html\|design-system.spec" guides/flows/admin-ui.md returns >= 1 (the fixture+spec are referenced in the add-a-token recipe).
    - The existing guide content above and below the new section is unchanged: grep -c "Mount Seam\|Stable Mounted Seam" guides/flows/admin-ui.md returns the same counts as before editing.
  </acceptance_criteria>

  <done>guides/flows/admin-ui.md has a "Design Token Contract" section covering the two-level model, 4-block cascade table, SYNCED-PAIR rule, add-a-token recipe, and link to CSS as source of truth. No existing content altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source files | CSS and markdown docs only — no runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Tampering | rulestead_admin.css comment block | accept | Doc-only edit; SYNCED-PAIR python check confirms token values unchanged |
| T-91-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Run from /Users/jon/projects/rulestead:

1. SYNCED-PAIR check exits 0 and prints IDENTICAL:
   python3 - (see python command in task 1 action)

2. Literal-scan on component section returns 0:
   awk 'NR>455' rulestead_admin/priv/static/css/rulestead_admin.css | grep -E "rgba\(|#[0-9a-fA-F]{3,6}" | grep -v "var(--rs-" | grep -v "^\s*/\*\|^\s*\*" | wc -l

3. Mix compile clean:
   cd rulestead_admin && mix compile --warnings-as-errors

4. Existing Playwright specs still pass:
   cd examples/demo/frontend && npx playwright test theme-cascade.spec.ts theme-scope.spec.ts theme-control.spec.ts

5. Header comment sections present:
   grep -c "SYNCED-PAIR\|How to add a token\|python3" rulestead_admin/priv/static/css/rulestead_admin.css (each >= 1)

6. Guide section present and DRY:
   grep -c "Design Token Contract" guides/flows/admin-ui.md (returns 1)
</verification>

<success_criteria>
- Token-contract header comment present in CSS with all 6 sections (invariant-vs-variant split, cascade blocks, SYNCED-PAIR rule + python command, invariant scale categories, variant scale categories, add-a-token recipe).
- Guide section "Design Token Contract" present in guides/flows/admin-ui.md — concise, links to CSS, no duplicated token list.
- SYNCED-PAIR python check prints IDENTICAL.
- Literal-scan on component region = 0.
- mix compile --warnings-as-errors exits 0.
- Existing theme-cascade, theme-scope, theme-control Playwright specs remain green.
</success_criteria>

<output>
Create `.planning/phases/91-design-system-consolidation/91-01-SUMMARY.md` when done.
</output>
