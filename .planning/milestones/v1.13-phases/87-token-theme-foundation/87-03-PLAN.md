---
phase: "87"
plan: "03"
type: execute
wave: 2
depends_on:
  - "87-02"
files_modified:
  - rulestead_admin/priv/static/css/rulestead_admin.css
autonomous: false
requirements:
  - THM-01
  - THM-03
  - THM-06

must_haves:
  truths:
    - "Toggling data-theme=dark on .rs-shell re-themes to mineral-dark: base ~#10161f, off-white text ~#e8edf3, elevation via lighter surfaces, no pure black."
    - "The system-dark @media block fires on a device with prefers-color-scheme:dark and no data-theme attribute (THM-01)."
    - "The explicit data-theme=dark beats a light OS; data-theme=light beats a dark OS (THM-03 explicit-wins)."
    - "Blocks 2 (system @media dark) and 3 (explicit [data-theme=dark]) contain verbatim-identical dark token declarations, marked SYNCED PAIR."
    - "Every text/surface WCAG AA pair passes: text >=4.5:1, large-text/UI >=3:1, in both themes."
    - "--rs-surface-faint is DARKER than --rs-surface in dark mode (neutral-0 vs neutral-25)."
    - "All 5 cascade-precedence cases from VALIDATION.md render correctly."
  artifacts:
    - path: "rulestead_admin/priv/static/css/rulestead_admin.css"
      provides: "Complete four-block theme layer: light default + system dark + explicit dark + explicit light"
      contains: "[data-theme=\"dark\"]"
  key_links:
    - from: "@media (prefers-color-scheme: dark)"
      to: ".rs-shell:not([data-theme])"
      via: ":not([data-theme]) guard — system dark fires only when no explicit choice is pinned"
      pattern: "not.*data-theme"
    - from: ".rs-shell[data-theme=dark]"
      to: "dark token set"
      via: "attribute selector beats @media by specificity (0,1,1 vs 0,1,0)"
      pattern: "data-theme.*dark"
---

<objective>
Author the complete dark token set and three remaining cascade blocks in rulestead_admin.css:
- Block 2: system-dark @media (prefers-color-scheme: dark) with :not([data-theme]) guard
- Block 3: explicit [data-theme="dark"] — verbatim copy of Block 2 dark tokens, marked SYNCED PAIR
- Block 4: explicit [data-theme="light"] — verbatim copy of Block 1 light tokens, marked SYNCED PAIR

All dark hex values from RESEARCH.md §3 are proposals. This plan MUST run WCAG contrast
verification before writing CSS and adjust any failing values upward until ratios clear.

Purpose: Completes THM-01 (system dark fires automatically), THM-03 (explicit-wins cascade),
and THM-06 (on-brand mineral-dark palette, WCAG AA in both themes). After this plan,
toggling data-theme="dark" produces correct mineral-dark theming across all shell components.

Output: Modified rulestead_admin.css with all four cascade blocks fully authored.
</objective>

<execution_context>
@/Users/jon/projects/rulestead/.planning/phases/87-token-theme-foundation/87-RESEARCH.md
@/Users/jon/projects/rulestead/.planning/phases/87-token-theme-foundation/87-CONTEXT.md
@/Users/jon/projects/rulestead/.planning/phases/87-token-theme-foundation/87-VALIDATION.md
</execution_context>

<context>
@/Users/jon/projects/rulestead/.planning/PROJECT.md
@/Users/jon/projects/rulestead/.planning/ROADMAP.md
@/Users/jon/projects/rulestead/.planning/phases/87-token-theme-foundation/87-02-SUMMARY.md

<interfaces>
<!--
After Plan 02 the CSS token section is:

  :root {
    -- invariant: typography, spacing, radius, z-index, motion,
       --rs-focus-ring-offset: 2px, --rs-disabled-opacity: 0.55
  }

  /* THEME LAYER */
  /* 1. Light default */
  .rs-shell,
  [data-rulestead] {
    color-scheme: light;
    --rs-neutral-0: #ffffff; ... --rs-neutral-900: #1a2332;
    --rs-bg through --rs-text-placeholder (surface/border/text aliases)
    --rs-primary: #2563eb; ... --rs-error-border-strong: #fecaca;
    --rs-warning-hover: #92400e;  <-- newly added
    --rs-shadow-sm, --rs-shadow, --rs-shadow-panel
    --rs-focus-ring-color: rgba(37,99,235,0.55);
    --rs-focus-ring: 0 0 0 3px var(--rs-focus-ring-color);
    --rs-disabled-bg: var(--rs-neutral-100);
    --rs-disabled-text: var(--rs-neutral-500);
    --rs-overlay-veil: rgba(238,241,245,0.9);
    --rs-scrim: rgba(15,23,35,0.45);
  }

  /* 2-4. System dark, explicit dark, explicit light -- added by Plan 03 */
  /* END THEME LAYER */

Dark proposals from RESEARCH.md §3 (verify AA before using):

  Neutral ramp direction FLIPS (0 = DARKEST):
    --rs-neutral-0:   #10161f   (deepest surface; rs-surface-faint alias)
    --rs-neutral-25:  #141c27   (rs-surface alias)
    --rs-neutral-50:  #19222e   (rs-bg alias)
    --rs-neutral-100: #1f2a38   (rs-surface-muted)
    --rs-neutral-200: #253243   (rs-border-subtle; rs-disabled-bg)
    --rs-neutral-300: #2e3d52   (rs-border)
    --rs-neutral-400: #3d5168   (rs-border-strong)
    --rs-neutral-500: #7a8fa3   (rs-text-placeholder — need 3:1 on #141c27)
    --rs-neutral-600: #a8b9ca   (rs-text-muted — need 4.5:1 on #141c27)
    --rs-neutral-900: #e8edf3   (rs-text — plan anchor)
    [no --rs-neutral-800 — gap preserved from light ramp]

  CRITICAL semantic alias flip:
    --rs-surface-faint: var(--rs-neutral-0) = #10161f  DARKEST (recessed)
    --rs-surface:       var(--rs-neutral-25) = #141c27 one step LIGHTER
    surface-faint MUST be darker than surface (RESEARCH §8 pitfall 6)

  Brand dark:
    --rs-primary: #2e74f0  (#fff on #2e74f0 = ~4.75:1 PASS — NOTE: #3b82f6 FAILS at 3.68:1;
                            white-on-blue needs a DARKER blue, not lighter)
    --rs-primary-hover: #5a96f5  (hover may be lighter — fill only, not a white-text gate)
    --rs-primary-soft: rgba(59,130,246,0.12)  NOTE: rgba not flat hex (pitfall 4)
    --rs-on-primary: #ffffff
    --rs-accent: #e8834a
    --rs-accent-soft: rgba(232,131,74,0.12)

  Status success dark: #4ade80 (need 4.5:1 on #141c27)
    --rs-success-hover: #86efac
    --rs-success-soft: rgba(74,222,128,0.12)
    --rs-success-bg-subtle: rgba(74,222,128,0.06)
    --rs-success-text: #4ade80
    --rs-success-border: #166534

  Status warning dark: #fbbf24 (need 4.5:1 on #141c27)
    --rs-warning-hover: #fcd34d
    --rs-warning-soft: rgba(251,191,36,0.12)
    --rs-warning-text: #fbbf24
    --rs-warning-border: #78350f

  Status error dark: #f87171 (need 4.5:1 on #141c27)
    --rs-error-hover: #fca5a5
    --rs-critical: #f87171
    --rs-error-soft: rgba(248,113,113,0.12)
    --rs-error-bg-subtle: rgba(248,113,113,0.06)
    --rs-error-text: #f87171
    --rs-error-text-strong: #fca5a5
    --rs-error-border: #7f1d1d
    --rs-error-border-strong: #991b1b

  Shadows dark (near-black keys + inset top-edge hairline — pitfall 1):
    --rs-shadow-sm:    0 1px 2px rgba(0,0,0,0.35), inset 0 1px 0 rgba(255,255,255,0.04)
    --rs-shadow:       0 1px 3px rgba(0,0,0,0.4), 0 8px 24px rgba(0,0,0,0.32), inset 0 1px 0 rgba(255,255,255,0.05)
    --rs-shadow-panel: 0 1px 3px rgba(0,0,0,0.45), 0 12px 32px rgba(0,0,0,0.38), inset 0 1px 0 rgba(255,255,255,0.06)

  Focus + disabled dark:
    --rs-focus-ring-color: rgba(96,165,250,0.75)
    --rs-focus-ring: 0 0 0 3px var(--rs-focus-ring-color)
    --rs-disabled-bg: #253243
    --rs-disabled-text: #6b84a0   (raised from #4a5e72 which was 2.56:1; #6b84a0 keeps disabled
                                   text legible. Disabled text is WCAG 1.4.3-EXEMPT, so it is NOT
                                   part of the blocking 7-pair gate — but kept legible on purpose.)

  Overlay dark:
    --rs-overlay-veil: rgba(20,28,39,0.85)
    --rs-scrim: rgba(0,0,0,0.65)

  WCAG AA pairs to verify before writing CSS (surface = #141c27):
    Text (>=4.5:1): #e8edf3, #a8b9ca, #4ade80, #fbbf24, #f87171 on #141c27
    Large/UI (>=3:1): #7a8fa3 on #141c27
    White on primary: #ffffff on #3b82f6 (>=4.5:1)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify WCAG contrast ratios; record adjusted values if needed</name>
  <files></files>

  <read_first>
    - /Users/jon/projects/rulestead/.planning/phases/87-token-theme-foundation/87-RESEARCH.md §3 (dark proposals + WCAG formula), §8 pitfall 6 (surface-faint direction)
    - /Users/jon/projects/rulestead/.planning/phases/87-token-theme-foundation/87-VALIDATION.md §Per-Task Verification Map
  </read_first>

  <action>
    Before writing any CSS, compute WCAG contrast ratios for all required dark token pairs.
    Use the W3C formula via Node.js:

    Linearize each channel: c_lin = c <= 0.04045 ? c/12.92 : ((c+0.055)/1.055)^2.4
    Relative luminance: L = 0.2126*R_lin + 0.7152*G_lin + 0.0722*B_lin
    Contrast ratio: (Lmax + 0.05) / (Lmin + 0.05)

    Required pairs (dark surface = #141c27 for all text checks):
      1. #e8edf3 on #141c27   need >= 4.5:1   (--rs-text)
      2. #a8b9ca on #141c27   need >= 4.5:1   (--rs-text-muted)
      3. #7a8fa3 on #141c27   need >= 3.0:1   (--rs-text-placeholder / large UI)
      4. #4ade80 on #141c27   need >= 4.5:1   (--rs-success / badge text)
      5. #fbbf24 on #141c27   need >= 4.5:1   (--rs-warning / badge text)
      6. #f87171 on #141c27   need >= 4.5:1   (--rs-error / badge text)
      7. #ffffff on #2e74f0   need >= 4.5:1   (--rs-on-primary on --rs-primary)

    For any failing TEXT pair (1-6): incrementally lighten the foreground until the ratio clears.
    For pair 7 (white on primary fill): white-on-blue contrast DROPS as the blue gets lighter.
    If it fails, go DARKER, not lighter: #2e74f0 = ~4.75:1, #2563eb = ~5.17:1. Do NOT try
    #3b82f6 / #60a5fa (they fail at 3.68 / 2.54). Pick the darkest-acceptable that still
    reads as a vivid primary fill on the dark surface.

    NOTE: --rs-disabled-text is intentionally low-contrast and WCAG 1.4.3-EXEMPT (disabled
    controls). It is NOT in this 7-pair blocking gate. Keep it at ~#6b84a0 for legibility.

    This task has no file output. Record verified hex values for Task 2.
    Print PASS/FAIL, ratio, and final hex for each pair before proceeding.
  </action>

  <verify>
    <automated>node -e "function lum(h){var r=parseInt(h.slice(1,3),16)/255,g=parseInt(h.slice(3,5),16)/255,b=parseInt(h.slice(5,7),16)/255,f=function(c){return c<=0.04045?c/12.92:Math.pow((c+0.055)/1.055,2.4)};return 0.2126*f(r)+0.7152*f(g)+0.0722*f(b)}function ratio(a,b){var arr=[lum(a),lum(b)].sort(function(x,y){return y-x});return(arr[0]+0.05)/(arr[1]+0.05)}var s=String.fromCharCode(35)+'141c27';var pairs=[[String.fromCharCode(35)+'e8edf3',s,4.5],[String.fromCharCode(35)+'a8b9ca',s,4.5],[String.fromCharCode(35)+'7a8fa3',s,3.0],[String.fromCharCode(35)+'4ade80',s,4.5],[String.fromCharCode(35)+'fbbf24',s,4.5],[String.fromCharCode(35)+'f87171',s,4.5],[String.fromCharCode(35)+'ffffff',String.fromCharCode(35)+'2e74f0',4.5]];var ok=true;pairs.forEach(function(p){var r=ratio(p[0],p[1]);if(r<p[2])ok=false;console.log(r>=p[2]?'PASS':'FAIL',r.toFixed(2),p[0])});process.exit(ok?0:1)"
    </automated>
  </verify>

  <acceptance_criteria>
    - All 7 contrast pairs produce PASS with ratio >= the required threshold.
    - Any adjusted hex values are noted for use in Task 2.
    - No CSS file is modified in this task.
  </acceptance_criteria>

  <done>All dark token WCAG AA pairs verified; final hex values confirmed before any CSS is written.</done>
</task>

<task type="auto">
  <name>Task 2: Author Blocks 2, 3, 4 in rulestead_admin.css</name>
  <files>rulestead_admin/priv/static/css/rulestead_admin.css</files>

  <read_first>
    - /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css (full read after Plan 02 — locate the Plan 03 placeholder comment)
    - /Users/jon/projects/rulestead/.planning/phases/87-token-theme-foundation/87-RESEARCH.md §2 (cascade mechanics + specificity), §7 (file section order), §8 pitfalls 3 4 5 6
    - /Users/jon/projects/rulestead/.planning/phases/87-token-theme-foundation/87-CONTEXT.md §decisions (duplicate dark blocks, synced pair comment)
    - Task 1 verified hex values (use these, not the raw proposals)
  </read_first>

  <action>
    Replace the placeholder comment "/* 2-4. System dark, explicit dark, explicit light -- added by Plan 03 */" with Blocks 2, 3, and 4.

    Block 2 — System dark:
      @media (prefers-color-scheme: dark) {
        .rs-shell:not([data-theme]),
        [data-rulestead]:not([data-theme]) {
          color-scheme: dark;
          /* !! SYNCED PAIR: keep this block identical to [data-theme="dark"] below !! */
          [complete dark token set from verified values]
        }
      }

    Block 3 — Explicit dark (higher specificity, beats Block 2):
      .rs-shell[data-theme="dark"],
      [data-rulestead][data-theme="dark"] {
        color-scheme: dark;
        /* !! SYNCED PAIR: keep this block identical to @media dark above !! */
        [verbatim copy of dark tokens from Block 2]
      }

    Block 4 — Explicit light (re-asserts light over dark OS):
      .rs-shell[data-theme="light"],
      [data-rulestead][data-theme="light"] {
        color-scheme: light;
        /* !! SYNCED PAIR: keep this block identical to .rs-shell default above !! */
        [verbatim copy of light token set from Block 1]
      }

    The dark token set (identical in Blocks 2 AND 3) must declare in order:
      1. color-scheme: dark
      2. SYNCED PAIR comment
      3. Neutral ramp (0 through 900, no 800 stop) — verified hex from Task 1
         Direction: 0 = #10161f (darkest), 900 = #e8edf3 (lightest)
      4. Semantic aliases:
         --rs-bg: var(--rs-neutral-50)
         --rs-surface: var(--rs-neutral-25)      ONE STEP LIGHTER than bg
         --rs-surface-muted: var(--rs-neutral-100)
         --rs-surface-faint: var(--rs-neutral-0) MAPS TO DARKEST (recessed)
         --rs-border through --rs-text-placeholder
      5. Brand tokens — primary-soft and accent-soft MUST use rgba() form
      6. Status tokens (success, warning, error families)
      7. Shadow tokens with rgba(0,0,0,...) keys + inset top-edge hairline
         DO NOT use rgba(26,35,50,...) — near-invisible on dark surfaces (pitfall 1)
      8. Focus + disabled tokens (focus-ring-color updated for dark)
      9. Overlay tokens (veil + scrim dark values)

    CONSTRAINTS:
    - Do not modify Block 1 (light default) written by Plan 02.
    - Do not modify any component rules below the THEME LAYER section.
    - Block 2 and Block 3 dark token declarations must be byte-for-byte identical.
    - Block 4 light declarations must be a verbatim copy of Block 1.
    - Keep the /* END THEME LAYER */ comment after Block 4.
    - Add section header comments matching the style from RESEARCH.md §7.
  </action>

  <verify>
    <automated>grep -c "SYNCED PAIR" /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css</automated>
    <!-- Synced-pair INTEGRITY (MEDIUM-3 fix v2): the two dark blocks must contain byte-identical
         token declarations, not merely both carry the comment. Brace-counting extraction (NOT a
         substring-boundary awk — the SYNCED PAIR *comment* contains the literal [data-theme="dark"]
         which a naive awk boundary trips on; that was the v1 false-negative). Compares the sorted
         `--rs-` declaration lines of the @media-dark block vs the [data-theme="dark"] block. -->
    <automated>cd /Users/jon/projects/rulestead && python3 - <<'PY'
import sys
css=open('rulestead_admin/priv/static/css/rulestead_admin.css').read()
def decls(sel):
    i=css.find(sel)
    if i<0: return None
    j=css.find('{',i); depth=0; k=j
    while k<len(css):
        if css[k]=='{': depth+=1
        elif css[k]=='}':
            depth-=1
            if depth==0: break
        k+=1
    return sorted(l.strip() for l in css[j+1:k].splitlines() if l.strip().startswith('--rs-'))
a=decls('@media (prefers-color-scheme: dark)')
b=decls('.rs-shell[data-theme="dark"]')
if a and a==b:
    print(f'SYNCED PAIR IDENTICAL ({len(a)} tokens)')
else:
    print('SYNCED PAIR MISMATCH'); sys.exit(1)
PY</automated>
  </verify>

  <acceptance_criteria>
    - SYNCED PAIR INTEGRITY: the diff-based check prints "SYNCED PAIR IDENTICAL" — Block 2 and Block 3 dark token declarations are byte-identical (not merely that the comment exists).
    - SYNCED PAIR comment appears at least twice (Block 2 + Block 3).
      grep -c "SYNCED PAIR" rulestead_admin.css returns >= 2.
    - Explicit dark selector present: grep -c "data-theme.*dark" returns >= 2.
    - Explicit light selector present: grep -c "data-theme.*light" returns >= 1.
    - :not([data-theme]) guard on @media block selector.
      grep -c "not.*data-theme" rulestead_admin.css returns >= 1.
    - color-scheme: dark appears in file.
      grep -c "color-scheme: dark" rulestead_admin.css returns >= 2.
    - Surface-faint maps to neutral-0 in dark (DARKER than surface).
      In the dark block: rs-surface-faint references neutral-0 (not neutral-25).
    - No rgba(26,35,50 inside the dark token blocks (only in light block shadows and component rules).
      awk from color-scheme:dark to END THEME LAYER | grep "rgba(26" returns 0 lines.
    - --rs-primary-soft in dark block uses rgba() not flat hex.
      grep "rs-primary-soft" shows the dark occurrence as rgba(...).
    - Plan 03 placeholder comment is gone.
      grep -c "added by Plan 03" rulestead_admin.css returns 0.
    - Component rules unchanged: grep -c "font-family: var(--rs-font-sans)" returns 1.
  </acceptance_criteria>

  <done>All three cascade blocks authored; Blocks 2 and 3 are SYNCED PAIR verbatim; surface-faint darker than surface in dark; shadows use near-black rgba; primary-soft is rgba tint; no component rules touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 02 and 03 have refactored the CSS token layer into four cascade blocks.
    The mineral-dark palette is fully authored with WCAG-AA-verified hex values.
    Both themes should be visually verifiable via the standalone harness.
  </what-built>

  <how-to-verify>
    Open rulestead_admin/priv/static/theme-harness.html in a browser:
      cd /Users/jon/projects/rulestead/rulestead_admin/priv/static
      python3 -m http.server 9191
      Then open: http://localhost:9191/theme-harness.html

    Step 1 - Light default: page loads showing light theme (neutral background, dark text).
    Step 2 - Pinned dark: in console type setTheme("dark")
      Expected: deep blue-grey base (not pure black), off-white text, card surfaces
      visibly lighter than page background, badge tones colorful and legible.
    Step 3 - Surface direction: confirm surface-faint swatch is visually darker/more
      recessed than surface swatch in dark mode.
    Step 4 - Explicit light override: setTheme("light") snaps back to full light theme.
    Step 5 - System mode: clearTheme() then switch OS preference (or use devtools
      Rendering > Emulate CSS media > prefers-color-scheme: dark).
      With dark OS: dark theme. With light OS: light theme.
    Step 6 - Scope containment: in devtools select the html element, check Computed
      styles — --rs-neutral-0 should NOT appear. The #outside-shell div should
      have red background (not themed).

    Run Playwright specs if demo is available:
      cd examples/demo/frontend
      npx playwright test theme-cascade.spec.ts theme-scope.spec.ts
  </how-to-verify>

  <resume-signal>Type "approved" or describe issues (colors, surface direction, scope leak, cascade failure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .rs-shell scope | Theme tokens contained within; host app outside not affected |
| color-scheme property | Set on .rs-shell only — not :root — scoped to mounted admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-04 | Tampering | color-scheme on :root | mitigate | Not placed on :root; Plan 02 acceptance criteria verify with grep; this plan does not re-introduce it |
| T-87-05 | Information Disclosure | client-side theme persistence | accept | Deferred to Phase 90 (localStorage hook). This phase is CSS-only; no JS, no storage, no cookies. |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | No new packages. Playwright and Node.js are already in the project. |
</threat_model>

<verification>
Phase 87 is complete when all of the following are true:

  # 1. Four-block cascade structure
  grep -c "data-theme" /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css
  (expect >= 4)

  # 2. SYNCED PAIR markers present
  grep -c "SYNCED PAIR" /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css
  (expect >= 2)

  # 3. No color tokens on :root
  awk '/^:root/,/^}/' /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css | grep -v "^[[:space:]]*//" | grep -c "rs-neutral\|rs-bg\|rs-surface\|rs-primary\|rs-shadow\|--rs-focus-ring:"
  (expect 0)

  # 4. Surface-faint dark direction
  grep "rs-surface-faint" /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css
  (dark occurrence must reference neutral-0; light occurrence references neutral-25)

  # 5. Playwright cascade + scope specs
  cd /Users/jon/projects/rulestead/examples/demo/frontend && npx playwright test theme-cascade.spec.ts theme-scope.spec.ts 2>&1 | tail -10

  # 6. Human visual verification (checkpoint task) approved.
</verification>

<success_criteria>
- All four cascade blocks exist: light default, system-dark @media with :not([data-theme]), explicit dark, explicit light.
- Blocks 2 and 3 are verbatim-identical dark token sets, each carrying SYNCED PAIR comment.
- --rs-surface-faint maps to --rs-neutral-0 (darkest) in dark; --rs-surface maps to --rs-neutral-25.
- All WCAG AA pairs pass: text/surface >=4.5:1, placeholder/large-UI >=3:1, white-on-primary >=4.5:1.
- Dark shadows use rgba(0,0,0,...) keys not rgba(26,35,50,...). Inset hairline present.
- --rs-primary-soft uses rgba() tint in dark, not opaque near-white hex.
- color-scheme:dark appears only on .rs-shell/[data-rulestead] blocks, never on :root.
- Human verification confirms mineral-dark aesthetic: ~#10161f base, off-white text, elevation reads.
- Playwright theme-cascade.spec.ts: all 5 cascade-precedence cases pass.
- Playwright theme-scope.spec.ts: all 3 scope-containment checks pass.
</success_criteria>

<output>
Create .planning/phases/87-token-theme-foundation/87-03-SUMMARY.md when done.
</output>
