---
phase: 99
plan: 02
type: execute
wave: 2
depends_on:
  - "99-01"
files_modified:
  - brandbook/assets/specimens/components.svg
  - brandbook/assets/specimens/code-block.svg
autonomous: true
requirements:
  - SPEC-02

must_haves:
  truths:
    - "components.svg shows buttons (default/primary/danger/text), a card, and all badge variants in the mineral light palette"
    - "code-block.svg shows Rulestead Elixir API on mineral dark background with IBM Plex Mono syntax coloring"
    - "Both files have zero embedded raster and carry the accessible skeleton"
    - "Both files are ≤51200 bytes after SVGO optimization"
  artifacts:
    - path: "brandbook/assets/specimens/components.svg"
      provides: "UI component specimen — buttons, card, badges in mineral light palette"
      contains: "#3A6F8F"
    - path: "brandbook/assets/specimens/code-block.svg"
      provides: "Code block specimen — Elixir API on mineral dark background"
      contains: "#10161f"
  key_links:
    - from: "brandbook/assets/specimens/components.svg"
      to: "rulestead_admin/priv/static/css/rulestead_admin.css"
      via: "component shapes resolved from admin CSS Block 1 var() references via tokens.json admin_css_mapping.light"
      pattern: "#3A6F8F|#d8dee6|#f4f6f8"
    - from: "brandbook/assets/specimens/code-block.svg"
      to: "brandbook/tokens.json"
      via: "dark neutral ramp hex values from tokens.json primitive.neutral-ramp dark section"
      pattern: "#10161f|#19222e|#e8edf3"
---

<objective>
Author the first two SPEC-02 specimens — UI components and code block. These show the
re-skinned mineral admin palette applied to real UI shapes and demonstrate the code
presentation style with mineral-dark syntax coloring.

Purpose: Give brand consumers concrete geometric references for button/card/badge shapes and
the code-block visual language, grounded in the Phase 98 mineral palette.

Output:
- brandbook/assets/specimens/components.svg (buttons, card, badges — light mineral palette)
- brandbook/assets/specimens/code-block.svg (Elixir snippet, mineral dark background)
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-specimens/99-RESEARCH.md
@.planning/phases/99-specimens/99-PATTERNS.md
@.planning/phases/99-specimens/99-01-SUMMARY.md
</context>

<interfaces>
<!-- Resolved var() values for component specimen — from PATTERNS.md Key Resolved Hex Values table -->

Component geometry hex values (resolved from tokens.json admin_css_mapping.light):
  Button bg default:     #f4f6f8   (--rs-surface = neutral-50)
  Button border default: #d8dee6   (--rs-border  = neutral-300)
  Button text default:   #1a2332   (--rs-text    = neutral-900)
  Button bg primary:     #3A6F8F   (--rs-primary)
  Button text primary:   #ffffff   (--rs-on-primary)
  Button bg danger:      #fee2e2   (--rs-error-soft)
  Button border danger:  #fca5a5   (--rs-error-border)
  Button text danger:    #B44949   (--rs-error / danger.white-rt)
  Text button color:     #3A6F8F   (--rs-primary)
  Card bg:               #ffffff   (neutral-0)
  Card border:           #d8dee6   (--rs-border)
  Card radius:           14px      (--rs-radius-lg)
  Badge neutral bg:      #eef1f5   (--rs-surface-muted = neutral-100)
  Badge neutral border:  #d8dee6
  Badge neutral text:    #5c6b7a   (--rs-text-muted = neutral-600)
  Badge positive bg:     #dcfce7
  Badge positive border: #86efac
  Badge positive text:   #2d7753   (success.canonical)
  Badge warning bg:      #fef3c7
  Badge warning border:  #fcd34d
  Badge warning text:    #8f601a   (warning.canonical)
  Badge critical bg:     #fee2e2
  Badge critical border: #fca5a5
  Badge critical text:   #B44949
  Badge accent bg:       #fde8dc   (--rs-accent-soft)
  Badge accent border:   #9b5931   (--rs-accent = ember-copper.base)
  Badge accent text:     #9b5931
  Button/badge radius:   6px / 999px

Code block hex values (from tokens.json dark neutral ramp):
  Background:   #10161f  (dark-0)
  Header bar:   #19222e  (dark-50)
  Base text:    #e8edf3  (dark-900)
  Comments:     #a8b9ca  (dark-600)
  Keywords:     #5885a0  (stead-blue.dark)
  Strings/vals: #ba6b3c  (ember-copper.dark)
  Line nums:    #7a8fa3  (dark-500)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author components.svg</name>
  <files>brandbook/assets/specimens/components.svg</files>
  <read_first>
    - brandbook/tokens.json — read admin_css_mapping.light section to confirm all resolved hex values; read primitive section for any primitives not in admin_css_mapping
    - rulestead_admin/priv/static/css/rulestead_admin.css — read lines 598–730 (button shapes), 1574–1585 (card shapes), 2646–2730 (badge shapes) to confirm geometric properties (radius, height, border-width) before hardcoding in SVG
    - brandbook/assets/logo/rs-mark.svg — accessible SVG skeleton pattern; note SVGO config reuse
    - brandbook/assets/logo/svgo.config.mjs — confirm overrides (removeDesc:false, cleanupIds:false, convertColors:false)
    - .planning/phases/99-specimens/99-PATTERNS.md — Pattern Assignment for components.svg: exact button/card/badge geometry with resolved hex values
    - scripts/ci/lint.sh — confirm 51200-byte threshold (line 43)
  </read_first>
  <action>
    Author brandbook/assets/specimens/components.svg showing the re-skinned mineral admin
    component palette as geometric SVG approximations — not a pixel-perfect screenshot but an
    accurate styleguide reference.

    Accessible SVG skeleton: viewBox="0 0 800 560", aria-labelledby="t d", role="img",
    title id="t" "Rulestead — Components Specimen",
    desc id="d" "Buttons (default, primary, danger, text), card, and badge variants in light mineral palette."
    All visual content inside <g aria-hidden="true">.

    SECTION 1 — Buttons (y≈40 baseline):
    Section label: "Buttons" — Inter 11px fill="#5c6b7a" at y=28
    - Default:  rect x=24 y=40 w=120 h=40 fill="#f4f6f8" stroke="#d8dee6" stroke-width="1" rx="6"
                text x=84 y=65 Inter 14px 500 fill="#1a2332" text-anchor="middle" "Default"
    - Primary:  rect x=160 y=40 w=120 h=40 fill="#3A6F8F" rx="6" (no stroke)
                text x=220 y=65 Inter 14px 500 fill="#ffffff" text-anchor="middle" "Primary"
    - Danger:   rect x=296 y=40 w=120 h=40 fill="#fee2e2" stroke="#fca5a5" stroke-width="1" rx="6"
                text x=356 y=65 Inter 14px 500 fill="#B44949" text-anchor="middle" "Danger"
    - Text:     text x=440 y=65 Inter 14px 600 fill="#3A6F8F" text-decoration="underline" "Text link"

    SECTION 2 — Card (y≈110 area):
    Section label: "Card" — Inter 11px fill="#5c6b7a" at y=105
    rect x=24 y=115 w=360 h=120 fill="#ffffff" stroke="#d8dee6" stroke-width="1" rx="14"
    Card title text: Inter 14px 500 fill="#1a2332" at x=40 y=145 "Feature Flags"
    Card body text: Inter 13px 400 fill="#5c6b7a" at x=40 y=167 "Runtime decisions, made clear."
    Card badge inline: rect x=40 y=185 w=76 h=22 fill="#eef1f5" stroke="#d8dee6" stroke-width="1" rx="999"
                       IBM Plex Mono 10px 600 fill="#5c6b7a" text-anchor="middle" x=78 y=200 "enabled"

    SECTION 3 — Badges (y≈260 area):
    Section label: "Badges" — Inter 11px fill="#5c6b7a" at y=255
    Five badge rows from left to right, each rect h=22 rx=999, text IBM Plex Mono 10px 600 centered:
    - Neutral: x=24 fill="#eef1f5" stroke="#d8dee6" text fill="#5c6b7a" "neutral"
    - Positive: x=108 fill="#dcfce7" stroke="#86efac" text fill="#2d7753" "enabled"
    - Warning: x=192 fill="#fef3c7" stroke="#fcd34d" text fill="#8f601a" "warning"
    - Critical: x=276 fill="#fee2e2" stroke="#fca5a5" text fill="#B44949" "critical"
    - Accent: x=360 fill="#fde8dc" stroke="#9b5931" text fill="#9b5931" "draft"

    Size each badge rect to fit its text + 12px horizontal padding (approximately 72–88px wide).
    Center text horizontally within each badge rect using text-anchor="middle" at badge cx.

    ALL fills/strokes are hard-coded hex literals. NEVER use var(--rs-*).
    NO base64. NO <image>. NO <use href="external...">.

    SVGO optimize:
    `npx svgo --config brandbook/assets/logo/svgo.config.mjs brandbook/assets/specimens/components.svg`

    Verify size:
    `wc -c < brandbook/assets/specimens/components.svg`
    Must be ≤51200.
  </action>
  <verify>
    <automated>
      test -f brandbook/assets/specimens/components.svg &amp;&amp; echo "file ok" &amp;&amp;
      grep -cE '#3[Aa]6[Ff]8[Ff]|#3A6F8F' brandbook/assets/specimens/components.svg &amp;&amp;
      grep -c 'base64' brandbook/assets/specimens/components.svg | grep -q '^0$' &amp;&amp; echo "no base64" &amp;&amp;
      grep -c '&lt;title' brandbook/assets/specimens/components.svg | grep -qv '^0$' &amp;&amp; echo "has title" &amp;&amp;
      test $(wc -c &lt; brandbook/assets/specimens/components.svg | tr -d ' ') -le 51200 &amp;&amp; echo "size ok"
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f brandbook/assets/specimens/components.svg` exits 0
    - `grep -cE '#3[Aa]6[Ff]8[Ff]|#3A6F8F' brandbook/assets/specimens/components.svg` returns ≥1 (Stead Blue primary button)
    - `grep -c '#f4f6f8' brandbook/assets/specimens/components.svg` returns ≥1 (default button bg)
    - `grep -c 'base64' brandbook/assets/specimens/components.svg` returns 0
    - `grep -c '<title' brandbook/assets/specimens/components.svg` returns ≥1
    - `grep -c 'role="img"' brandbook/assets/specimens/components.svg` returns ≥1
    - `wc -c < brandbook/assets/specimens/components.svg` is ≤51200
  </acceptance_criteria>
  <done>components.svg committed with default/primary/danger/text buttons, card with title+body+badge, and all 5 badge variants (neutral/positive/warning/critical/accent); all mineral light palette hex values; accessible skeleton; size ≤51200 bytes after SVGO.</done>
</task>

<task type="auto">
  <name>Task 2: Author code-block.svg</name>
  <files>brandbook/assets/specimens/code-block.svg</files>
  <read_first>
    - brandbook/tokens.json — read primitive.neutral-ramp section for dark background colors: dark-0 #10161f, dark-50 #19222e, dark-500 #7a8fa3, dark-600 #a8b9ca, dark-900 #e8edf3; read stead-blue.dark #5885a0 and ember-copper.dark #ba6b3c for syntax accent colors
    - brandbook/assets/logo/rs-mark.svg — accessible SVG skeleton pattern (title/desc/role/aria)
    - brandbook/assets/logo/svgo.config.mjs — confirm config for SVGO reuse
    - .planning/phases/99-specimens/99-PATTERNS.md — Pattern Assignment for code-block.svg: container geometry, header bar, syntax color palette, Rulestead Elixir code snippet
    - scripts/ci/lint.sh — confirm 51200-byte threshold
  </read_first>
  <action>
    Author brandbook/assets/specimens/code-block.svg showing a Rulestead Elixir API snippet
    on a mineral dark background with IBM Plex Mono syntax-style text coloring.

    Accessible SVG skeleton: viewBox="0 0 680 280", aria-labelledby="t d", role="img",
    title id="t" "Rulestead — Code Block Specimen",
    desc id="d" "Code block specimen: Rulestead Elixir API on mineral dark background with IBM Plex Mono syntax coloring."
    All visual content inside <g aria-hidden="true">.

    CONTAINER:
    rect x=0 y=0 width=680 height=280 fill="#10161f" rx="10"

    HEADER BAR (filename tab):
    rect x=0 y=0 width=680 height=36 fill="#19222e" rx="10"
    rect x=0 y=18 width=680 height=18 fill="#19222e"  (fill lower half square to flatten bottom)
    text x=20 y=23 IBM Plex Mono 11px fill="#7a8fa3" "feature_flags.ex"

    Three window-control dots (decorative):
    circle cx=652 cy=18 r=5 fill="#2e3d52"
    circle cx=636 cy=18 r=5 fill="#2e3d52"
    circle cx=620 cy=18 r=5 fill="#2e3d52"

    CODE LINES (IBM Plex Mono 12px, x=20, line height 22px starting at y=65):

    Line 1 y=65:   fill="#a8b9ca"  "# Evaluate a feature flag"
    Line 2 y=87:   Compose a mixed-color line using tspan elements:
                   "{" text fill="#e8edf3"; ":ok" fill="#5885a0"; ", result} = " fill="#e8edf3";
                   "Rulestead" fill="#e8edf3"; "." fill="#e8edf3";
                   "evaluate" fill="#5885a0"; "(" fill="#e8edf3";
                   ":dark_mode" fill="#ba6b3c"; ", ctx)" fill="#e8edf3"
                   (Simplification: render as single text fill="#e8edf3" if tspan composition
                   is complex; primary goal is readable code sample, not precise token coloring)
    Line 3 y=109:  fill="#e8edf3"  "IO.inspect(result.value)     # => true"
                   (inline comment portion in fill="#a8b9ca" if using tspan, else single color)
    Line 4 y=131:  fill="#e8edf3"  'IO.inspect(result.reason)    # => "matched rule 3"'
    Line 5 y=175:  fill="#a8b9ca"  "# Check rollout percentage"
    Line 6 y=197:  fill="#e8edf3"  "{:ok, pct} = Rulestead.rollout_percentage(:beta, ctx)"
    Line 7 y=219:  fill="#e8edf3"  "IO.inspect(pct)  # => 25"

    Note: tspan-based syntax highlighting is optional polish. A readable single-color Elixir
    snippet with the mineral dark background is the core deliverable. Prefer correctness and
    simplicity over elaborate tspan nesting.

    ALL fills are hard-coded hex literals. NO var(--rs-*). NO base64. NO <image>.

    SVGO optimize:
    `npx svgo --config brandbook/assets/logo/svgo.config.mjs brandbook/assets/specimens/code-block.svg`

    Verify size:
    `wc -c < brandbook/assets/specimens/code-block.svg`
    Must be ≤51200. Code block SVG will be well under 5 KB; no risk of budget breach.
  </action>
  <verify>
    <automated>
      test -f brandbook/assets/specimens/code-block.svg &amp;&amp; echo "file ok" &amp;&amp;
      grep -c '#10161f' brandbook/assets/specimens/code-block.svg &amp;&amp;
      grep -c 'base64' brandbook/assets/specimens/code-block.svg | grep -q '^0$' &amp;&amp; echo "no base64" &amp;&amp;
      grep -c '&lt;title' brandbook/assets/specimens/code-block.svg | grep -qv '^0$' &amp;&amp; echo "has title" &amp;&amp;
      test $(wc -c &lt; brandbook/assets/specimens/code-block.svg | tr -d ' ') -le 51200 &amp;&amp; echo "size ok"
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f brandbook/assets/specimens/code-block.svg` exits 0
    - `grep -c '#10161f' brandbook/assets/specimens/code-block.svg` returns ≥1 (mineral dark base)
    - `grep -c '#e8edf3' brandbook/assets/specimens/code-block.svg` returns ≥1 (dark neutral-900 text)
    - `grep -c 'base64' brandbook/assets/specimens/code-block.svg` returns 0
    - `grep -c '<title' brandbook/assets/specimens/code-block.svg` returns ≥1
    - `grep -c 'role="img"' brandbook/assets/specimens/code-block.svg` returns ≥1
    - `grep -c 'IBM Plex Mono' brandbook/assets/specimens/code-block.svg` returns ≥1
    - `wc -c < brandbook/assets/specimens/code-block.svg` is ≤51200
  </acceptance_criteria>
  <done>code-block.svg committed with mineral dark container (#10161f), header bar (#19222e), Rulestead Elixir code snippet, IBM Plex Mono font; accessible skeleton; no base64; size ≤51200 bytes after SVGO.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed SVG → git repo | Static hand-authored assets; no user input, no runtime, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Tampering | components.svg / code-block.svg (embedded raster or external entity) | mitigate | `grep -c base64` = 0 per file; no `<image>` or `<use href="external">` — enforced by acceptance_criteria |
| T-99-02 | Tampering | svgo via npx (supply chain) | accept | svgo@4.0.1 audited Phase 97 with 12-year track record; no package.json install |
| T-99-03 | Information Disclosure | committed font binary | mitigate | Both specimens use `<text>` elements only; no TTF committed; `file *.svg` shows XML |
| T-99-SC | Tampering | npm/npx svgo install | accept | Existing Phase 97 audit; no new packages; slopcheck N/A |

**Surface assessment:** Identical to Plan 01 — static SVG authoring, no runtime. Minimal surface. The base64 grep assertion is the enforced guardrail.
</threat_model>

<verification>
After both tasks complete:

```bash
# File existence
test -f brandbook/assets/specimens/components.svg && echo "components ok"
test -f brandbook/assets/specimens/code-block.svg && echo "code-block ok"

# No base64
grep -c 'base64' brandbook/assets/specimens/components.svg
grep -c 'base64' brandbook/assets/specimens/code-block.svg
# Both must return 0

# Key hex values present
grep -cE '#3[Aa]6[Ff]8[Ff]|#3A6F8F' brandbook/assets/specimens/components.svg
grep -c '#10161f' brandbook/assets/specimens/code-block.svg

# Accessible titles
grep -c '<title' brandbook/assets/specimens/components.svg
grep -c '<title' brandbook/assets/specimens/code-block.svg

# Size budget
wc -c < brandbook/assets/specimens/components.svg
wc -c < brandbook/assets/specimens/code-block.svg
# Both must be ≤51200
```
</verification>

<success_criteria>
- components.svg present with buttons (default/primary/danger/text), card, and 5 badge variants in mineral light palette; all hex literals; accessible; ≤51200 bytes
- code-block.svg present with mineral dark background (#10161f), IBM Plex Mono code, Rulestead Elixir snippet; all hex literals; accessible; ≤51200 bytes
- Both files SVGO-optimized using brandbook/assets/logo/svgo.config.mjs
</success_criteria>

<output>
Create `.planning/phases/99-specimens/99-02-SUMMARY.md` when done.
</output>
