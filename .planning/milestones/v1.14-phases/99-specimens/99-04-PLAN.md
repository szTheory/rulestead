---
phase: 99
plan: 04
type: execute
wave: 3
depends_on:
  - "99-01"
  - "99-02"
  - "99-03"
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02

must_haves:
  truths:
    - "All 6 specimen SVGs exist in brandbook/assets/specimens/ and are SVGO-optimized ≤51200 bytes each"
    - "bash scripts/ci/lint.sh exits 0 and prints SVG SIZE BUDGET OK"
    - "Every specimen has zero embedded base64 and carries role=img + title + desc + aria-labelledby"
    - "REQUIREMENTS.md marks SPEC-01 and SPEC-02 as done"
    - "ROADMAP.md marks Phase 99 complete with all 4 success criteria checked"
    - "STATE.md reflects Phase 99 complete and records the 4 key decisions from this phase"
  artifacts:
    - path: "brandbook/assets/specimens/palette.svg"
      provides: "Brand palette specimen — final state"
      contains: "#3A6F8F"
    - path: "brandbook/assets/specimens/typography.svg"
      provides: "Type ramp specimen — final state"
      contains: "<text"
    - path: "brandbook/assets/specimens/components.svg"
      provides: "UI components specimen — final state"
      contains: "#3A6F8F"
    - path: "brandbook/assets/specimens/code-block.svg"
      provides: "Code block specimen — final state"
      contains: "#10161f"
    - path: "brandbook/assets/specimens/readme-header.svg"
      provides: "README header specimen — final state"
      contains: "#3a6f8f"
    - path: "brandbook/assets/specimens/social-card.svg"
      provides: "Social card design reference — final state"
      contains: "#183247"
  key_links:
    - from: "brandbook/assets/specimens/*.svg"
      to: "scripts/ci/lint.sh"
      via: "size-budget loop lines 41-48; nullglob + 51200-byte threshold"
      pattern: "SVG SIZE BUDGET OK"
---

<objective>
Final phase-close plan: run a batch SVGO pass over all 6 specimens (catch any file that was
not yet optimized), execute the full lint.sh guard sweep to confirm all specimen files are
within the 51200-byte budget, and update the three planning docs to record Phase 99 as
complete.

Purpose: Confirm the six-specimen set is lint-passing and size-clean as a single committed
state. Update planning artifacts so Phase 100 can safely depend on Phase 99 completion.

Output:
- All 6 specimens re-optimized (idempotent — SVGO multipass on already-optimized files is safe)
- bash scripts/ci/lint.sh exits 0 with "SVG SIZE BUDGET OK"
- .planning/STATE.md: Phase 99 complete with decisions recorded
- .planning/ROADMAP.md: Phase 99 checked complete; 4 SCs verified
- .planning/REQUIREMENTS.md: SPEC-01, SPEC-02 marked done with traceability row updated
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/99-specimens/99-01-SUMMARY.md
@.planning/phases/99-specimens/99-02-SUMMARY.md
@.planning/phases/99-specimens/99-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SVGO batch optimize all 6 specimens + full lint.sh guard sweep</name>
  <files>brandbook/assets/specimens/palette.svg brandbook/assets/specimens/typography.svg brandbook/assets/specimens/components.svg brandbook/assets/specimens/code-block.svg brandbook/assets/specimens/readme-header.svg brandbook/assets/specimens/social-card.svg</files>
  <read_first>
    - brandbook/assets/logo/svgo.config.mjs — confirm the config is unchanged: multipass:true, overrides: removeDesc:false, cleanupIds:false, convertColors:false; this config must be used for all 6 SVGO runs
    - scripts/ci/lint.sh — read the full file; confirm: (1) the specimens loop at lines 41-48 uses threshold 51200; (2) the loop variable is ${RULESTEAD_REPO}/brandbook/assets/specimens/*.svg; (3) the script exits with "SVG SIZE BUDGET OK" when all files pass; (4) RULESTEAD_REPO defaults to pwd — run from repo root
    - brandbook/assets/specimens/ — list all SVG files present; confirm all 6 exist before running SVGO
  </read_first>
  <action>
    Run a final idempotent SVGO pass on every specimen, then execute the full lint.sh guard sweep.
    Running SVGO on already-optimized SVGs is safe (multipass converges quickly).

    Step 1 — Verify all 6 specimens exist:
    ls brandbook/assets/specimens/palette.svg
    ls brandbook/assets/specimens/typography.svg
    ls brandbook/assets/specimens/components.svg
    ls brandbook/assets/specimens/code-block.svg
    ls brandbook/assets/specimens/readme-header.svg
    ls brandbook/assets/specimens/social-card.svg

    If any file is missing, STOP and report which plan did not complete.

    Step 2 — SVGO batch pass (from repo root):
    for f in brandbook/assets/specimens/*.svg; do
      npx svgo --config brandbook/assets/logo/svgo.config.mjs "$f"
    done

    Step 3 — Spot-check sizes for all 6 (all must be ≤51200):
    wc -c brandbook/assets/specimens/*.svg

    If any file exceeds 51200 bytes after SVGO:
    - For palette.svg: reduce swatch count (drop duplicate neutral ramp rows)
    - For components.svg: simplify badge count or reduce card content
    - For social-card.svg: switch wordmark to live <text> (Option B) then re-run SVGO

    Step 4 — Verify no base64 in any specimen:
    grep -c 'base64' brandbook/assets/specimens/*.svg
    All must return 0. If any returns non-zero, remove the embedded data and re-run SVGO.

    Step 5 — Verify accessible skeleton in all 6:
    grep -c '<title' brandbook/assets/specimens/palette.svg
    grep -c '<title' brandbook/assets/specimens/typography.svg
    grep -c '<title' brandbook/assets/specimens/components.svg
    grep -c '<title' brandbook/assets/specimens/code-block.svg
    grep -c '<title' brandbook/assets/specimens/readme-header.svg
    grep -c '<title' brandbook/assets/specimens/social-card.svg
    All must return ≥1.

    Step 6 — Full lint.sh guard sweep (runs from repo root):
    bash scripts/ci/lint.sh 2>&1 | tail -20

    The lint.sh run includes mix deps.get, format check, compile, credo, dialyzer, and ALL
    three guard scripts (check_synced_pair.py, check_brand_tokens.py, check_tokens_css.py),
    followed by the SVG size-budget loop. All must pass. The final output line must be
    "SVG SIZE BUDGET OK".

    If lint.sh fails on a mix step (not SVG-related), note the failure and continue — the
    SVG size-budget gate is the Phase 99 gate. If the SVG SIZE BUDGET OK line is missing or
    any SVG budget-exceeded error appears, fix the offending specimen before proceeding.

    NOTE: If lint.sh fails due to unrelated CI issues (mix compile warnings, dialyzer PLT
    missing), it is acceptable to run just the SVG budget loop directly to confirm the Phase 99
    gate passes:
    shopt -s nullglob
    for f in brandbook/assets/specimens/*.svg; do
      size=$(wc -c < "$f" | tr -d ' ')
      if [ "$size" -gt 51200 ]; then echo "FAIL: $f is ${size} bytes"; exit 1; fi
    done
    echo "SVG SIZE BUDGET OK"
  </action>
  <verify>
    <automated>
      ls brandbook/assets/specimens/palette.svg brandbook/assets/specimens/typography.svg brandbook/assets/specimens/components.svg brandbook/assets/specimens/code-block.svg brandbook/assets/specimens/readme-header.svg brandbook/assets/specimens/social-card.svg &amp;&amp; echo "all 6 exist" &amp;&amp;
      for f in brandbook/assets/specimens/*.svg; do size=$(wc -c &lt; "$f" | tr -d ' '); [ "$size" -le 51200 ] || (echo "FAIL: $f ${size} bytes" &amp;&amp; exit 1); done &amp;&amp; echo "all sizes ok" &amp;&amp;
      for f in brandbook/assets/specimens/*.svg; do count=$(grep -c 'base64' "$f"); [ "$count" -eq 0 ] || (echo "FAIL: base64 in $f" &amp;&amp; exit 1); done &amp;&amp; echo "no base64" &amp;&amp;
      bash scripts/ci/lint.sh 2&gt;&amp;1 | grep -q 'SVG SIZE BUDGET OK' &amp;&amp; echo "lint gate passed"
    </automated>
  </verify>
  <acceptance_criteria>
    - `ls brandbook/assets/specimens/{palette,typography,components,code-block,readme-header,social-card}.svg` exits 0 (all 6 files present)
    - `for f in brandbook/assets/specimens/*.svg; do wc -c < "$f"; done` — all 6 values ≤51200
    - `grep -c 'base64' brandbook/assets/specimens/*.svg` — all 6 return 0
    - `grep -c '<title' brandbook/assets/specimens/palette.svg` returns ≥1
    - `grep -c '<title' brandbook/assets/specimens/typography.svg` returns ≥1
    - `grep -c '<title' brandbook/assets/specimens/components.svg` returns ≥1
    - `grep -c '<title' brandbook/assets/specimens/code-block.svg` returns ≥1
    - `grep -c '<title' brandbook/assets/specimens/readme-header.svg` returns ≥1
    - `grep -c '<title' brandbook/assets/specimens/social-card.svg` returns ≥1
    - `bash scripts/ci/lint.sh 2>&1 | grep 'SVG SIZE BUDGET OK'` matches (lint exits with this message)
  </acceptance_criteria>
  <done>All 6 specimens SVGO-optimized and ≤51200 bytes; zero base64 in any specimen; lint.sh exits 0 with "SVG SIZE BUDGET OK".</done>
</task>

<task type="auto">
  <name>Task 2: Update planning docs — Phase 99 complete (STATE + ROADMAP + REQUIREMENTS)</name>
  <files>.planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md — read the full file; focus on: "Current Position" section (update Phase/Plan/Status), "Accumulated Context > Decisions" section (add Phase 99 decisions), "Operator Next Steps" section (update to Phase 100), "Progress bar" line
    - .planning/ROADMAP.md — read the Phase 99 detail section (Goal, Success Criteria 1-4, Plans list) and the Progress Table at the bottom; update Phase 99 status and plan checklist
    - .planning/REQUIREMENTS.md — read the Specimens (SPEC) section and the Traceability table at the bottom; mark SPEC-01 and SPEC-02 as done
  </read_first>
  <action>
    Update the three planning documents to record Phase 99 completion.

    STATE.md updates:
    1. "Current Position" section:
       - Phase: 100 (or "100 — not yet started")
       - Plan: Not started
       - Status: Ready to plan
       - Last activity: today's date
    2. "stopped_at" frontmatter: update to "Phase 99 complete (4/4) — ready to discuss Phase 100"
    3. "Progress bar": update from "57%" to approximately 71% (5/7 phases complete):
       `[ #######░░░░ ] 71% — 5/7 phases`
    4. "Accumulated Context > Decisions" — append Phase 99 decision entries:
       - [Phase 99]: 6 SVG specimens committed to brandbook/assets/specimens/ — palette, typography, components, code-block, readme-header, social-card
       - [Phase 99]: All specimens use hard-coded hex literals (no var(--rs-*)); live <text> elements for typography.svg (not outlined paths); specimens are self-contained (no external <use href>)
       - [Phase 99]: SVGO optimized with brandbook/assets/logo/svgo.config.mjs (removeDesc:false, cleanupIds:false, convertColors:false); all 6 files ≤51200 bytes
       - [Phase 99]: social-card.svg in specimens/ is a new design reference distinct from Phase 97 brandbook/assets/logo/rs-social-card.svg (production asset untouched)
    5. "Operator Next Steps":
       - Replace Phase 99 item with: "Start Phase 100 with /gsd:plan-phase 100 (Marketing Copy + Repo Artifact Plan)"
       - Add note: "Phase 99 complete: all 6 specimens committed, lint.sh exits 0 with SVG SIZE BUDGET OK"
    6. frontmatter "status": change from "ready_to_plan" to "executing" (Phase 100 not yet started)
       Actually: leave as "ready_to_plan" since Phase 100 has not been planned yet.

    ROADMAP.md updates:
    1. Phase 99 summary line: change `- [ ] **Phase 99: Specimens**` to `- [x] **Phase 99: Specimens**`
    2. Phase 99 Plans list: mark all 4 plans as done:
       - [x] 99-01-PLAN.md
       - [x] 99-02-PLAN.md
       - [x] 99-03-PLAN.md
       - [x] 99-04-PLAN.md
    3. Progress Table row for Phase 99: update from "0/0 | Not started | -" to "4/4 | Complete | today's date"
    4. Phase 99 details — add completion note after the Success Criteria (read the 4 SCs and confirm all are met per what was built; do not invent new claims)

    REQUIREMENTS.md updates:
    1. SPEC-01 line: change `- [ ] **SPEC-01**` to `- [x] **SPEC-01**`
    2. SPEC-02 line: change `- [ ] **SPEC-02**` to `- [x] **SPEC-02**`
    3. Traceability table: update the two SPEC rows from "Pending" to "Complete"

    Preserve all other content in all three files. Make minimal, targeted edits.
  </action>
  <verify>
    <automated>
      grep -c 'Phase 99 complete' .planning/STATE.md &amp;&amp;
      grep -c '\[x\].*SPEC-01' .planning/REQUIREMENTS.md &amp;&amp;
      grep -c '\[x\].*SPEC-02' .planning/REQUIREMENTS.md &amp;&amp;
      grep -c 'SPEC-01.*Complete' .planning/REQUIREMENTS.md &amp;&amp;
      grep -c '\[x\].*Phase 99: Specimens' .planning/ROADMAP.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 99 complete' .planning/STATE.md` returns ≥1
    - `grep -c '\[x\].*SPEC-01' .planning/REQUIREMENTS.md` returns ≥1 (SPEC-01 checked done)
    - `grep -c '\[x\].*SPEC-02' .planning/REQUIREMENTS.md` returns ≥1 (SPEC-02 checked done)
    - `grep -c 'SPEC-01.*Complete' .planning/REQUIREMENTS.md` returns ≥1 (traceability row updated)
    - `grep -c 'SPEC-02.*Complete' .planning/REQUIREMENTS.md` returns ≥1
    - `grep -c '\[x\].*Phase 99' .planning/ROADMAP.md` returns ≥1 (Phase 99 checked in roadmap phases list)
    - `grep -c '99-04-PLAN' .planning/ROADMAP.md` returns ≥1 (99-04 plan listed)
    - `grep -c '5/7 phases\|71%' .planning/STATE.md` returns ≥1 (progress updated)
    - .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md are each readable and valid markdown (no broken structure)
  </acceptance_criteria>
  <done>STATE.md records Phase 99 complete + 4 specimen decisions; ROADMAP.md marks Phase 99 [x] complete with 4 plans checked + Progress Table updated; REQUIREMENTS.md marks SPEC-01 and SPEC-02 [x] done with traceability rows updated to Complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed SVGs → git repo | Static assets already authored in prior plans; this plan only re-runs SVGO and reads file sizes |
| planning docs → git repo | STATE.md / ROADMAP.md / REQUIREMENTS.md are internal planning docs; no user-supplied data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Tampering | Any specimen (residual embedded raster from prior plans) | mitigate | Batch `grep -c base64 brandbook/assets/specimens/*.svg` = 0 for all 6 files; enforced in acceptance_criteria |
| T-99-02 | Tampering | svgo via npx (supply chain) | accept | svgo@4.0.1 audited Phase 97; 12-year track record; no package.json install |
| T-99-03 | Tampering | SVGO stripping accessibility metadata | mitigate | Config uses removeDesc:false, cleanupIds:false — verified by reading svgo.config.mjs before running; post-SVGO `grep -c '<title'` = ≥1 per file |
| T-99-04 | Repudiation | Planning docs reflect incorrect completion state | mitigate | grep assertions confirm [x] SPEC-01, [x] SPEC-02, [x] Phase 99 in each target file |
| T-99-SC | Tampering | npm/npx svgo install | accept | Phase 97 audit; no new packages; slopcheck N/A |

**Surface assessment:** Static SVG assets and internal planning documents. No runtime code, no user input, no network. Surface is minimal. The key enforced guardrail is `bash scripts/ci/lint.sh` producing "SVG SIZE BUDGET OK" — this is the Phase 99 CI gate.
</threat_model>

<verification>
Phase 99 complete — full verification suite:

```bash
# 1. All 6 specimen files exist
ls brandbook/assets/specimens/{palette,typography,components,code-block,readme-header,social-card}.svg

# 2. Size budget — all 6 must be ≤51200 bytes
wc -c brandbook/assets/specimens/*.svg

# 3. No base64 in any specimen (all must return 0)
grep -c 'base64' brandbook/assets/specimens/*.svg

# 4. Accessible titles in every specimen
grep -c '<title' brandbook/assets/specimens/palette.svg
grep -c '<title' brandbook/assets/specimens/typography.svg
grep -c '<title' brandbook/assets/specimens/components.svg
grep -c '<title' brandbook/assets/specimens/code-block.svg
grep -c '<title' brandbook/assets/specimens/readme-header.svg
grep -c '<title' brandbook/assets/specimens/social-card.svg
# All must return ≥1

# 5. Stead Blue hex present in palette (SC-1 assertion)
grep -cE '#3[Aa]6[Ff]8[Ff]|#3A6F8F' brandbook/assets/specimens/palette.svg

# 6. Live text elements in typography (SC-2 assertion; not outlined paths)
grep -c '<text' brandbook/assets/specimens/typography.svg

# 7. Phase 97 production asset untouched
git diff --quiet brandbook/assets/logo/rs-social-card.svg && echo "logo intact"

# 8. Full lint.sh guard sweep (Phase 99 CI gate)
bash scripts/ci/lint.sh 2>&1 | grep 'SVG SIZE BUDGET OK'

# 9. Planning doc updates
grep '\[x\].*SPEC-01' .planning/REQUIREMENTS.md
grep '\[x\].*SPEC-02' .planning/REQUIREMENTS.md
grep '\[x\].*Phase 99' .planning/ROADMAP.md
```

All 9 checks must pass before Phase 99 is considered complete.
</verification>

<success_criteria>
Phase 99 is complete when:
1. `ls brandbook/assets/specimens/palette.svg typography.svg components.svg code-block.svg readme-header.svg social-card.svg` all exit 0 (SC-3 of Phase 99 ROADMAP criteria)
2. `bash scripts/ci/lint.sh 2>&1 | grep 'SVG SIZE BUDGET OK'` prints the success string (SC-4 — all specimens ≤51200 bytes)
3. `grep -c '#3A6F8F\|#3a6f8f' brandbook/assets/specimens/palette.svg` returns ≥1 (SC-1 — palette.svg has annotated swatches)
4. `grep -c '<text' brandbook/assets/specimens/typography.svg` returns ≥1 (SC-2 — typography.svg uses live text with token labels)
5. REQUIREMENTS.md: SPEC-01 and SPEC-02 both show [x] done
6. ROADMAP.md: Phase 99 marked [x] complete with 4/4 plans checked
7. STATE.md: Phase 99 complete decisions recorded; operator next steps updated to Phase 100
</success_criteria>

<output>
Create `.planning/phases/99-specimens/99-04-SUMMARY.md` when done.
</output>
