---
phase: 99
plan: 03
type: execute
wave: 2
depends_on:
  - "99-01"
files_modified:
  - brandbook/assets/specimens/readme-header.svg
  - brandbook/assets/specimens/social-card.svg
autonomous: true
requirements:
  - SPEC-02

must_haves:
  truths:
    - "readme-header.svg shows the Rulestead mark geometry inline + wordmark text + tagline on a light background"
    - "social-card.svg (NEW file in specimens/) is distinct from brandbook/assets/logo/rs-social-card.svg (Phase 97 production asset — NEVER modified)"
    - "social-card.svg shows the 1200×630 social card layout with Ink Blue background, mark, wordmark, and minimal token annotations"
    - "Both files have zero embedded raster and carry the accessible skeleton"
    - "Both files are ≤51200 bytes after SVGO optimization"
  artifacts:
    - path: "brandbook/assets/specimens/readme-header.svg"
      provides: "README header specimen — mark geometry + wordmark + tagline"
      contains: "#3a6f8f"
    - path: "brandbook/assets/specimens/social-card.svg"
      provides: "Social card design reference (1200×630) with token annotations"
      contains: "#183247"
  key_links:
    - from: "brandbook/assets/specimens/readme-header.svg"
      to: "brandbook/assets/logo/rs-mark.svg"
      via: "mark geometry copied verbatim inline (no external <use href> — self-contained)"
      pattern: "rect.*fill=#3a6f8f|circle.*fill=#9b5931"
    - from: "brandbook/assets/specimens/social-card.svg"
      to: "brandbook/assets/logo/rs-social-card.svg"
      via: "layout reference — same viewBox/background/mark transform; distinct file; adds token annotations"
      pattern: "#183247"
---

<objective>
Author the second pair of SPEC-02 specimens — the README header mock and the social card
design reference. Both reuse Phase 97 mark geometry inline (self-contained, no external
references) and demonstrate brand application at distinct display contexts.

Purpose: Provide brand consumers with concrete layout references for the README header
(mark + wordmark + tagline) and the social card (1200×630 Ink Blue layout with mark and
word-mark). The social-card.svg specimen is a NEW design reference, distinct from the
Phase 97 production asset rs-social-card.svg which MUST NOT be modified.

Output:
- brandbook/assets/specimens/readme-header.svg
- brandbook/assets/specimens/social-card.svg
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-specimens/99-RESEARCH.md
@.planning/phases/99-specimens/99-PATTERNS.md
@.planning/phases/99-specimens/99-01-SUMMARY.md
</context>

<interfaces>
<!-- Mark geometry extracted from brandbook/assets/logo/rs-mark.svg (actual file on disk) -->
<!-- Copy this verbatim into readme-header.svg and into social-card.svg's mark group -->

rs-mark.svg mark geometry (light mode — Stead Blue #3a6f8f, Ember Copper #9b5931, Quarry #c4ccd1):
  <rect width="7.5" height="39.5" x="30" y="13" fill="#3a6f8f" rx="1"/>
  <rect width="20" height="7.5" x="12" y="28.25" fill="#3a6f8f" rx="1"/>
  <circle cx="12" cy="32" r="6.5" fill="#3a6f8f"/>
  <rect width="14" height="7.5" x="32" y="12.25" fill="#9b5931" rx="1"/>
  <circle cx="50" cy="16" r="6.5" fill="#9b5931"/>
  <rect width="14" height="7.5" x="32" y="28.25" fill="#3a6f8f" rx="1"/>
  <circle cx="50" cy="32" r="6.5" fill="#c4ccd1"/>
  <rect width="14" height="7.5" x="32" y="44.25" fill="#3a6f8f" rx="1"/>
  <circle cx="50" cy="48" r="6.5" fill="#c4ccd1"/>

rs-social-card.svg reference (read but DO NOT modify this file):
  viewBox="0 0 1200 630"
  background: <path fill="#183247" d="M0 0h1200v630H0z"/>
  mark group: <g transform="matrix(4 0 0 4 120 187)"> ... same geometry as rs-mark.svg but
    with dark-mode colors — rects/top-route fill="#5885a0", active node fill="#9b5931",
    off-nodes fill="#c4ccd1"
  The wordmark in rs-social-card.svg uses OUTLINED PATHS (no live text);
    the specimen may use live <text> for the wordmark (specimen is not a distribution artifact)

Key hex values for these specimens:
  readme-header background: #ffffff or #F5F7F6 (rain-tint)
  readme-header mark:       light-mode colors (#3a6f8f, #9b5931, #c4ccd1)
  readme-header wordmark:   #183247 (ink-blue — matches rs-wordmark.svg outlined path fill)
  readme-header tagline:    #5c6b7a (neutral-600)
  social-card background:   #183247 (ink-blue.base)
  social-card mark:         dark-mode colors (#5885a0, #9b5931, #c4ccd1)
  social-card text:         #e8edf3 (neutral-900 dark)
  social-card annotation:   #e8edf3 at 50% opacity
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author readme-header.svg</name>
  <files>brandbook/assets/specimens/readme-header.svg</files>
  <read_first>
    - brandbook/assets/logo/rs-mark.svg — READ the actual file to extract the exact path/rect/circle geometry from the <g aria-hidden="true"> block; copy this verbatim into readme-header.svg (inline geometry — no external reference)
    - brandbook/tokens.json — confirm ink-blue.base #183247 (wordmark color) and neutral-600 #5c6b7a (tagline)
    - brandbook/assets/logo/svgo.config.mjs — config to reuse for SVGO
    - .planning/phases/99-specimens/99-PATTERNS.md — Pattern Assignment for readme-header.svg: viewBox, mark placement, wordmark text, tagline text; note that live <text> is used for the wordmark in the specimen (LOGO-04 outlined-text applies to logo files only)
    - scripts/ci/lint.sh — confirm 51200-byte threshold
  </read_first>
  <action>
    Author brandbook/assets/specimens/readme-header.svg. This specimen shows the README header
    mock: Rulestead mark on the left + wordmark text on the right + tagline below.

    Critical: Copy the mark geometry INLINE from rs-mark.svg. Do NOT use
    <use href="../logo/rs-mark.svg"> or any external SVG reference — specimens must be
    self-contained. Read rs-mark.svg and paste the <g aria-hidden="true"> element contents
    (the 9 rect/circle elements) into readme-header.svg's mark group.

    Accessible SVG skeleton: viewBox="0 0 480 96", aria-labelledby="t d", role="img",
    title id="t" "Rulestead — README Header",
    desc id="d" "README header mock showing the Rulestead mark and wordmark with tagline: Runtime decisions, made clear."
    All visual content inside <g aria-hidden="true">.

    BACKGROUND:
    rect x=0 y=0 width=480 height=96 fill="#F5F7F6"  (rain-tint — clean light background)

    MARK GROUP (inline geometry from rs-mark.svg, light-mode colors):
    The rs-mark.svg viewBox is "0 0 64 64"; place it at the left edge of the header.
    No transform needed if the geometry coordinates are already in 64×64 space and the
    SVG viewBox is 480×96 — the mark will render at top-left. Use a <g> wrapper positioned
    at x=16 y=16 to center the mark vertically with a slight margin:
    <g transform="translate(16, 16)">
      [paste the 9 rect/circle elements verbatim from rs-mark.svg]
    </g>
    Note: the mark geometry occupies approx x=12..57, y=13..55 within its own coordinate space.
    With translate(16,16) the mark renders within the 96px height. Adjust translate y if needed
    to center visually — e.g. translate(16, 12) may look better.

    WORDMARK TEXT (live <text> — specimen only; LOGO-04 outlined-text is logo-file-only):
    text x=96 y=44 font-family="Sora, Inter, ui-sans-serif, system-ui, sans-serif"
         font-size="32" font-weight="700" fill="#183247"
    "Rulestead"

    TAGLINE:
    text x=96 y=68 font-family="Inter, ui-sans-serif, system-ui, sans-serif"
         font-size="14" font-weight="400" fill="#5c6b7a"
    "Runtime decisions, made clear."

    ALL fills are hard-coded hex literals. NO var(--rs-*). NO base64. NO <use href="external">.

    SVGO optimize:
    `npx svgo --config brandbook/assets/logo/svgo.config.mjs brandbook/assets/specimens/readme-header.svg`

    Verify size:
    `wc -c < brandbook/assets/specimens/readme-header.svg`
    Must be ≤51200. This specimen will be well under 5 KB.
  </action>
  <verify>
    <automated>
      test -f brandbook/assets/specimens/readme-header.svg &amp;&amp; echo "file ok" &amp;&amp;
      grep -c '#3a6f8f\|#3A6F8F' brandbook/assets/specimens/readme-header.svg &amp;&amp;
      grep -c '#183247' brandbook/assets/specimens/readme-header.svg &amp;&amp;
      grep -c 'base64' brandbook/assets/specimens/readme-header.svg | grep -q '^0$' &amp;&amp; echo "no base64" &amp;&amp;
      grep -c '&lt;title' brandbook/assets/specimens/readme-header.svg | grep -qv '^0$' &amp;&amp; echo "has title" &amp;&amp;
      test $(wc -c &lt; brandbook/assets/specimens/readme-header.svg | tr -d ' ') -le 51200 &amp;&amp; echo "size ok"
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f brandbook/assets/specimens/readme-header.svg` exits 0
    - `grep -c '#3a6f8f' brandbook/assets/specimens/readme-header.svg` returns ≥1 (mark Stead Blue inline)
    - `grep -c '#183247' brandbook/assets/specimens/readme-header.svg` returns ≥1 (wordmark Ink Blue)
    - `grep -c 'base64' brandbook/assets/specimens/readme-header.svg` returns 0
    - `grep -c '<title' brandbook/assets/specimens/readme-header.svg` returns ≥1
    - `grep -c 'role="img"' brandbook/assets/specimens/readme-header.svg` returns ≥1
    - `grep -c 'use href' brandbook/assets/specimens/readme-header.svg` returns 0 (no external references)
    - `wc -c < brandbook/assets/specimens/readme-header.svg` is ≤51200
    - brandbook/assets/logo/rs-mark.svg is UNCHANGED (verify with git diff)
  </acceptance_criteria>
  <done>readme-header.svg committed with inline mark geometry (copied from rs-mark.svg), live text wordmark "Rulestead" in Ink Blue #183247, tagline in neutral-600 #5c6b7a; accessible skeleton; no external references; size ≤51200 bytes after SVGO.</done>
</task>

<task type="auto">
  <name>Task 2: Author social-card.svg (specimens/ — distinct from logo/rs-social-card.svg)</name>
  <files>brandbook/assets/specimens/social-card.svg</files>
  <read_first>
    - brandbook/assets/logo/rs-social-card.svg — READ this file to extract: exact background path, mark group transform and geometry, and existing wordmark path data (for Option A fidelity) or confirm it has outlined paths. This file MUST NOT be modified — it is the Phase 97 production asset.
    - brandbook/tokens.json — confirm ink-blue.base #183247 (background), neutral-900 dark #e8edf3 (text)
    - brandbook/assets/logo/svgo.config.mjs — config to reuse for SVGO
    - .planning/phases/99-specimens/99-PATTERNS.md — Pattern Assignment for social-card.svg: PATTERNS.md covers the exact background path, mark group transform, and the annotation approach; Option A (copy outlined paths) vs Option B (live text) decision; recommendation is Option A for fidelity but live text is acceptable if it keeps the file under budget
    - scripts/ci/lint.sh — confirm 51200-byte threshold; social-card at 1200×630 may be larger — check SVGO can get it under 51200
  </read_first>
  <action>
    Author brandbook/assets/specimens/social-card.svg as a NEW file — a design reference
    that demonstrates the social card layout with token annotations. This is DISTINCT from
    brandbook/assets/logo/rs-social-card.svg which is the Phase 97 production OG/Twitter
    card. NEVER modify the file at brandbook/assets/logo/rs-social-card.svg.

    Accessible SVG skeleton: width="1200" height="630" viewBox="0 0 1200 630",
    aria-labelledby="t d", role="img",
    title id="t" "Rulestead — Social Card Specimen",
    desc id="d" "Social card design reference (1200×630). Ink Blue background with mark, wordmark, and tagline. Annotated with key token names."
    All visual content inside <g aria-hidden="true">.

    BACKGROUND:
    <path fill="#183247" d="M0 0h1200v630H0z"/>

    MARK GROUP (inline, dark-mode colors — copied from rs-social-card.svg mark section):
    Read rs-social-card.svg to get the exact mark group transform and geometry.
    From PATTERNS.md the mark is: <g transform="matrix(4 0 0 4 120 187)"> with dark-mode colors
    (rects fill="#5885a0" except top-route rects fill="#9b5931", off-node circles fill="#c4ccd1").
    Copy this group verbatim from rs-social-card.svg into specimens/social-card.svg.

    WORDMARK + TAGLINE:
    Use live <text> elements for the specimen (smaller than outlined paths; acceptable for
    a design reference that is not a distribution asset):
    text x=444 y=310 font-family="Sora, Inter, ui-sans-serif, system-ui, sans-serif"
         font-size="84" font-weight="700" fill="#e8edf3" "Rulestead"
    text x=444 y=390 font-family="Inter, ui-sans-serif, system-ui, sans-serif"
         font-size="36" font-weight="400" fill="#e8edf3" opacity="0.75"
         "Runtime decisions, made clear."

    If copying outlined wordmark paths from rs-social-card.svg (Option A for fidelity):
    Read and copy the <g fill="#e8edf3" transform="matrix(3 0 0 3 444 195)"> block verbatim.
    Check post-SVGO size before committing — if >51200 bytes, fall back to live <text> above.

    TOKEN ANNOTATION LABEL (minimal — per RESEARCH.md Pattern 7 recommendation):
    text x=36 y=608 font-family="IBM Plex Mono, ui-monospace, monospace"
         font-size="16" fill="#e8edf3" opacity="0.45"
         "bg: #183247 · ink-blue.base · mark: #5885a0 · stead-blue.dark"

    ALL fills are hard-coded hex literals. NO var(--rs-*). NO base64. NO <image>.
    The mark geometry is INLINE — do not reference rs-social-card.svg via <use href>.

    SVGO optimize:
    `npx svgo --config brandbook/assets/logo/svgo.config.mjs brandbook/assets/specimens/social-card.svg`

    CRITICAL size check:
    `wc -c < brandbook/assets/specimens/social-card.svg`
    Social card is 1200×630 — larger canvas. If using outlined wordmark paths (Option A) and
    post-SVGO size exceeds 51200, switch to live <text> for wordmark (Option B). Run SVGO again.
    Both options keep the file comfortably under 51200 bytes.

    Safety check: Confirm rs-social-card.svg (Phase 97 production asset) is UNCHANGED:
    `git diff brandbook/assets/logo/rs-social-card.svg`
    Must show no changes.
  </action>
  <verify>
    <automated>
      test -f brandbook/assets/specimens/social-card.svg &amp;&amp; echo "file ok" &amp;&amp;
      grep -c '#183247' brandbook/assets/specimens/social-card.svg &amp;&amp;
      grep -c 'base64' brandbook/assets/specimens/social-card.svg | grep -q '^0$' &amp;&amp; echo "no base64" &amp;&amp;
      grep -c '&lt;title' brandbook/assets/specimens/social-card.svg | grep -qv '^0$' &amp;&amp; echo "has title" &amp;&amp;
      test $(wc -c &lt; brandbook/assets/specimens/social-card.svg | tr -d ' ') -le 51200 &amp;&amp; echo "size ok" &amp;&amp;
      git diff --quiet brandbook/assets/logo/rs-social-card.svg &amp;&amp; echo "logo unchanged"
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f brandbook/assets/specimens/social-card.svg` exits 0
    - `grep -c '#183247' brandbook/assets/specimens/social-card.svg` returns ≥1 (Ink Blue background)
    - `grep -c 'base64' brandbook/assets/specimens/social-card.svg` returns 0
    - `grep -c '<title' brandbook/assets/specimens/social-card.svg` returns ≥1
    - `grep -c 'role="img"' brandbook/assets/specimens/social-card.svg` returns ≥1
    - `wc -c < brandbook/assets/specimens/social-card.svg` is ≤51200
    - `git diff --quiet brandbook/assets/logo/rs-social-card.svg` exits 0 (Phase 97 asset untouched)
  </acceptance_criteria>
  <done>brandbook/assets/specimens/social-card.svg committed as a NEW file with 1200×630 viewBox, Ink Blue (#183247) background, dark-mode mark geometry inline, wordmark + tagline, and token annotation label; brandbook/assets/logo/rs-social-card.svg is untouched; size ≤51200 bytes after SVGO.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed SVG → git repo | Static hand-authored assets; no user input, no runtime, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Tampering | readme-header.svg / social-card.svg (embedded raster or external entity) | mitigate | `grep -c base64` = 0 per file; no `<image>` or `<use href="external">` — enforced by acceptance_criteria |
| T-99-02 | Tampering | svgo via npx (supply chain) | accept | svgo@4.0.1 audited Phase 97; no package.json install |
| T-99-03 | Information Disclosure | accidental overwrite of rs-social-card.svg (Phase 97 production asset) | mitigate | `git diff --quiet brandbook/assets/logo/rs-social-card.svg` asserted in acceptance_criteria; social-card specimen is written to different directory (specimens/) |
| T-99-04 | Tampering | external SVG reference via <use href> in self-contained specimen | mitigate | `grep -c 'use href' readme-header.svg` = 0 asserted; mark geometry is inlined |
| T-99-SC | Tampering | npm/npx svgo install | accept | Existing Phase 97 audit; no new packages |

**Surface assessment:** Static SVG authoring. The one additional risk vs Plans 01/02 is accidental modification of the Phase 97 production rs-social-card.svg — mitigated by explicit git diff assertion in acceptance_criteria.
</threat_model>

<verification>
After both tasks complete:

```bash
# File existence
test -f brandbook/assets/specimens/readme-header.svg && echo "readme-header ok"
test -f brandbook/assets/specimens/social-card.svg && echo "social-card ok"

# No base64
grep -c 'base64' brandbook/assets/specimens/readme-header.svg
grep -c 'base64' brandbook/assets/specimens/social-card.svg
# Both must return 0

# Key hex values
grep -c '#3a6f8f' brandbook/assets/specimens/readme-header.svg   # mark Stead Blue
grep -c '#183247' brandbook/assets/specimens/social-card.svg      # Ink Blue background

# Self-contained (no external use references)
grep -c 'use href' brandbook/assets/specimens/readme-header.svg   # must be 0

# Phase 97 production asset UNCHANGED
git diff --quiet brandbook/assets/logo/rs-social-card.svg && echo "logo intact"

# Size budget
wc -c < brandbook/assets/specimens/readme-header.svg
wc -c < brandbook/assets/specimens/social-card.svg
# Both must be ≤51200
```
</verification>

<success_criteria>
- readme-header.svg present with inline mark geometry, live text wordmark (#183247), tagline (#5c6b7a); no external references; accessible; ≤51200 bytes
- social-card.svg present as a NEW file in specimens/ (NOT logo/) with 1200×630 viewBox, Ink Blue background, dark-mode mark, token annotation; accessible; ≤51200 bytes
- brandbook/assets/logo/rs-social-card.svg is completely unchanged (Phase 97 production asset)
- Both files SVGO-optimized using brandbook/assets/logo/svgo.config.mjs
</success_criteria>

<output>
Create `.planning/phases/99-specimens/99-03-SUMMARY.md` when done.
</output>
