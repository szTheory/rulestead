---
phase: 98-admin-re-skin-css-cascade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check_synced_pair.py
  - scripts/check_brand_tokens.py
  - scripts/ci/lint.sh
autonomous: true
requirements:
  - SKIN-02
  - SKIN-03
must_haves:
  truths:
    - "check_synced_pair.py asserts both the dark pair (Block 2≡3) AND the light pair (Block 1≡4) — exits 0 with both pairs confirmed"
    - "check_brand_tokens.py diffs both Block 1 vs admin_css_mapping.light AND Block 3 vs admin_css_mapping.dark — exits 1 only because CSS not yet re-skinned (or 0 if already done)"
    - "lint.sh CWD bug is fixed — guard scripts can be invoked via lint.sh without FileNotFoundError"
    - "All existing success strings (SYNCED PAIR IDENTICAL, BRAND TOKENS SYNCED) and exit codes are preserved"
    - "Implements CONTEXT decision D-05 (the two additive guard-script extensions D-05a + D-05b) and D-04a (case-insensitive hex comparison preserved)"
  artifacts:
    - path: "scripts/check_synced_pair.py"
      provides: "Block 1≡4 light-pair assertion added additively alongside existing Block 2≡3 check"
    - path: "scripts/check_brand_tokens.py"
      provides: "Block 3 dark-diff against admin_css_mapping.dark added additively alongside existing light diff"
    - path: "scripts/ci/lint.sh"
      provides: "cd ${RULESTEAD_REPO} inserted before guard script block to restore CWD after mix cd"
  key_links:
    - from: "scripts/check_synced_pair.py"
      to: "rulestead_admin/priv/static/css/rulestead_admin.css"
      via: "decls() brace-walk on comment-stripped CSS"
      pattern: "decls.*\\.rs-shell"
    - from: "scripts/check_brand_tokens.py"
      to: "brandbook/tokens.json admin_css_mapping.dark"
      via: "extract_css_decls() on Block 3 selector"
      pattern: "admin_css_mapping.*dark"
---

<objective>
Extend both CI guard scripts additively (D-05a, D-05b) and fix the lint.sh CWD bug — creating the test infrastructure Wave 0 that makes SC-2 and SC-4 machine-verifiable before any CSS edits land.

Purpose: Close the two verification gaps identified in VALIDATION.md: Block 1≡4 light pair is currently unguarded (check_synced_pair.py only checks 2≡3); Block 3 dark hex correctness is unguarded (check_brand_tokens.py is light-only). Without these extensions, a wrong dark hex or a light-pair desync could ship silently.

Output: Three modified files. No new files. No new packages. All changes additive — existing output strings, exit codes, and lint.sh parse points preserved.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/rulestead/.planning/PROJECT.md
@/Users/jon/projects/rulestead/.planning/ROADMAP.md
@/Users/jon/projects/rulestead/.planning/STATE.md
@/Users/jon/projects/rulestead/.planning/phases/98-admin-re-skin-css-cascade/98-CONTEXT.md
@/Users/jon/projects/rulestead/.planning/phases/98-admin-re-skin-css-cascade/98-RESEARCH.md
@/Users/jon/projects/rulestead/.planning/phases/98-admin-re-skin-css-cascade/98-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from direct file inspection. -->

From scripts/check_synced_pair.py (full source — 63 lines):
  CSS = "rulestead_admin/priv/static/css/rulestead_admin.css"  (relative path, run from repo root)
  decls(css, sel) → sorted list of "--rs-*: value;" strings for the block matched by sel
    - uses re.sub(r"/\*.*?\*/", "", raw, flags=re.S) to strip comments BEFORE calling decls()
    - brace-depth walk from first "{" after sel; collects lines starting with "--rs-"
  main() current logic (lines 42-58):
    - media = decls(css, "@media (prefers-color-scheme: dark)")
    - attr  = decls(css, '.rs-shell[data-theme="dark"]')
    - if media and media == attr: print(f"SYNCED PAIR IDENTICAL ({len(media)} tokens)"); return 0
    - else: print("SYNCED PAIR MISMATCH"); per-token diff; return 1
  D-05a extension: add Block 1≡4 check after existing 2≡3 check, using same decls() pattern:
    light_default = decls(css, ".rs-shell,")
    light_pinned  = decls(css, '.rs-shell[data-theme="light"]')
  If light pair mismatches: print "SYNCED PAIR MISMATCH (light)"; return 1

From scripts/check_brand_tokens.py (full source — 86 lines):
  TOKENS_JSON = "brandbook/tokens.json"
  CSS = "rulestead_admin/priv/static/css/rulestead_admin.css"
  extract_css_decls(css, sel) → dict {name: value} for --rs-* decls in the matched block
    - CALLER must pass comment-stripped css (re.sub pattern same as check_synced_pair.py)
  main() current logic (lines 49-81):
    - tokens = json.load(open(TOKENS_JSON))
    - mapping = tokens["admin_css_mapping"]["light"]
    - css = re.sub(r"/\*.*?\*/", "", raw, flags=re.S)
    - css_decls = extract_css_decls(css, ".rs-shell,")
    - per-token diff loop with css_val.lower() != expected.lower()  (case-insensitive, line 69)
    - mismatches list; matched counter
    - if not mismatches: print(f"BRAND TOKENS SYNCED ({matched} tokens)"); return 0
    - else: print("BRAND TOKEN DRIFT DETECTED"); per-token lines; return 1
  D-05b extension: add dark block diff after existing light diff:
    - mapping_dark = tokens["admin_css_mapping"]["dark"]
    - css_dark = extract_css_decls(css, '.rs-shell[data-theme="dark"],')
    - fold mismatches into same mismatches list (prefix [dark] for readability)
    - increment matched for dark matches
    - same exit logic (not mismatches → 0, else → 1)
    - success message may update count: e.g. "BRAND TOKENS SYNCED (N light + M dark tokens)"

From scripts/ci/lint.sh (45 lines):
  Line 6:  cd "${RULESTEAD_REPO}/rulestead"   ← CWD moves here for Elixir commands
  Line 17: # Synced-pair guard comment
  Line 18: python3 "${RULESTEAD_REPO}/scripts/check_synced_pair.py"
  Line 21: # Brand token drift comment
  Line 22: python3 "${RULESTEAD_REPO}/scripts/check_brand_tokens.py"
  Line 26: # Tokens.css mirror comment
  Line 27: python3 "${RULESTEAD_REPO}/scripts/check_tokens_css.py"
  CWD fix: insert "cd \"${RULESTEAD_REPO}\"" as a new line between line 16 and line 17
    (after dialyzer at line 15, before the guard comment at line 17)
    The guard scripts use relative paths internally — need CWD = repo root to resolve
    "rulestead_admin/priv/static/css/..." and "brandbook/tokens.json"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend check_synced_pair.py — add Block 1≡4 light-pair assertion (D-05a)</name>
  <files>scripts/check_synced_pair.py</files>
  <read_first>
    - scripts/check_synced_pair.py — read the full 63-line source; the existing 2≡3 main() logic at lines 42-58 is the direct pattern to replicate
    - rulestead_admin/priv/static/css/rulestead_admin.css lines 225-303 (Block 1 selector: ".rs-shell,") and lines 469-549 (Block 4 selector: ".rs-shell[data-theme=\"light\"]," — note the trailing comma on Block 4's multi-selector opening)
  </read_first>
  <action>
    Add a second pair check in main() immediately after the existing 2≡3 check block (after line 58, before `return 1`). The new check uses decls() exactly as-is — no changes to the function.

    Call decls(css, ".rs-shell,") to extract Block 1 (light default). Call decls(css, '.rs-shell[data-theme="light"]') to extract Block 4 (explicit light). Note: the Block 4 CSS rule opens with ".rs-shell[data-theme=\"light\"]," (two selectors including [data-rulestead][data-theme="light"]) — the decls() find() call needs only the first part of the selector string that uniquely identifies the block start; '.rs-shell[data-theme="light"]' (without trailing comma) is sufficient since it uniquely matches after comment-stripping.

    If both light lists are non-empty and equal: print a success line (e.g., "SYNCED PAIR IDENTICAL (light: N tokens)") and continue to return 0 at the end of main().

    If mismatch or either list is None: print "SYNCED PAIR MISMATCH (light)" with per-token diff lines (same shape as existing only_media / only_attr logic), then return 1.

    Restructure main() so both pair checks must pass before the function returns 0 — failing either returns 1 immediately. The existing dark-pair success print is preserved verbatim. The docstring must be updated to reflect that the script now guards both pairs (light + dark).

    Critical constraint: pass the comment-stripped `css` variable to decls() — not `raw`. The header comment at lines 186-193 of rulestead_admin.css documents all four block selectors by name; without comment stripping, find() matches inside the comment (Pitfall 2 in RESEARCH.md).

    Do not change the function signature of decls(), the module docstring import block, the CSS constant, the re.sub comment-strip line, or any existing output string.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead && python3 scripts/check_synced_pair.py</automated>
  </verify>
  <acceptance_criteria>
    - Script exits 0 and prints both pair confirmation lines (dark pair line unchanged, new light pair line present)
    - Script exits 1 with "SYNCED PAIR MISMATCH (light)" if Block 1 and Block 4 tokens differ (verifiable by temporarily adding a token to one block and reverting)
    - Existing "SYNCED PAIR IDENTICAL" dark-pair output line is preserved verbatim (content unchanged; token count still 56 if CSS unchanged)
    - No new imports; no third-party packages; python3 stdlib only
  </acceptance_criteria>
  <done>check_synced_pair.py exits 0 with both pair lines printed; existing 2≡3 success output preserved; new 1≡4 assertion present</done>
</task>

<task type="auto">
  <name>Task 2: Extend check_brand_tokens.py — add Block 3 dark diff (D-05b) + fix lint.sh CWD</name>
  <files>scripts/check_brand_tokens.py, scripts/ci/lint.sh</files>
  <read_first>
    - scripts/check_brand_tokens.py — read the full 86-line source; the existing main() light-diff loop at lines 49-81 is the direct pattern; note extract_css_decls() is called with the comment-stripped css variable
    - brandbook/tokens.json lines 295-388 — the admin_css_mapping section; confirm "dark" key structure at lines 348-387 (31 tokens including --rs-disabled-bg and --rs-disabled-text which are dark-only hex literals)
    - scripts/ci/lint.sh — read the full 45-line source; identify where line 6 performs cd and where line 17 begins the guard section
    - rulestead_admin/priv/static/css/rulestead_admin.css lines 388-467 (Block 3 selector: ".rs-shell[data-theme=\"dark\"],")
  </read_first>
  <action>
    Part A — check_brand_tokens.py dark extension:

    In main(), after the existing light-diff block (after the final `for name, expected in sorted(mapping.items())` loop, before the final `if not mismatches:` check), add the dark block diff. Load the dark mapping: `mapping_dark = tokens["admin_css_mapping"]["dark"]`. Extract Block 3: `css_dark = extract_css_decls(css, '.rs-shell[data-theme="dark"],')`. Guard: if css_dark is None, append an error and return 1.

    Run the same per-token diff loop over sorted(mapping_dark.items()): skip non-"--rs-" keys, compare css_dark.get(name) vs expected using .lower() (case-insensitive per D-04a). Append mismatches with a "[dark]" prefix (e.g., "  [dark] --rs-success-border: tokens.json=#166634  css=#166534") for readable output. Increment the existing `matched` counter for dark matches.

    The final `if not mismatches:` / `print("BRAND TOKENS SYNCED")` / return logic is UNCHANGED — it now covers the combined light+dark mismatch list. The success message may update the count to reflect both light and dark tokens (e.g., "BRAND TOKENS SYNCED (N tokens)" where N = matched light + matched dark). Do not change "BRAND TOKENS SYNCED" prefix or "BRAND TOKEN DRIFT DETECTED" string — these are stable output contracts.

    The --rs-success-border change is `#166534` → `#166634` — call this out in your edit as a comment or with particular care when wiring the dark mapping loop: this is the highest-risk one-digit diff (RESEARCH.md Pitfall 1).

    Do not change the extract_css_decls() function, the TOKENS_JSON / CSS constants, the comment-strip line, or the existing light-block logic above.

    Part B — lint.sh CWD fix:

    In scripts/ci/lint.sh, insert `cd "${RULESTEAD_REPO}"` as a new line between the end of the Elixir commands (line 15: `mix dialyzer --format github`) and the start of the guard section (line 17: `# Synced-pair guard comment`). Insert it as line 17, shifting the current lines 17+ down by one. Add a brief inline comment: `# Restore CWD to repo root — guard scripts use relative paths (rulestead_admin/..., brandbook/...)`. Do not change any other line in lint.sh.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead && python3 scripts/check_brand_tokens.py</automated>
  </verify>
  <acceptance_criteria>
    - check_brand_tokens.py reports mismatches for all 8 dark tokens AND all 7 light tokens (total 15 mismatches expected pre-re-skin) when run against the un-modified CSS; exits 1 with "BRAND TOKEN DRIFT DETECTED"
    - "[dark] --rs-success-border: tokens.json=#166634  css=#166534" appears in the mismatch output (confirms the one-digit dark fix is actively caught)
    - When run post-re-skin (CSS updated), exits 0 with "BRAND TOKENS SYNCED"
    - lint.sh: `grep -n "cd.*RULESTEAD_REPO" scripts/ci/lint.sh` shows two matches — one at line 6 (cd to rulestead/) and one new line (cd back to repo root) before the guard invocations
    - No other lines in lint.sh changed (verify with git diff)
    - python3 stdlib only; no new imports in check_brand_tokens.py
  </acceptance_criteria>
  <done>check_brand_tokens.py exits 1 with all 15 pre-re-skin mismatches including [dark] --rs-success-border; lint.sh has the CWD restore line before guard block</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI script → Python stdlib scripts | lint.sh invokes check_synced_pair.py and check_brand_tokens.py; exit code determines pass/fail |
| Python scripts → CSS/JSON files | Scripts read repo-root-relative files; CWD must be repo root at invocation time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | check_synced_pair.py extension | mitigate | Extension must be additive — preserve existing dark-pair exit semantics and success string; any change that causes the existing 2≡3 check to stop failing on mismatch is a regression. Verified by: existing dark-pair logic unchanged in code review. |
| T-98-02 | Tampering | check_brand_tokens.py extension | mitigate | Extension must fold dark mismatches into the same mismatches list as light mismatches; the final exit condition (not mismatches → 0) must remain unchanged. A refactor that exits 0 when dark tokens are wrong silently weakens CI. Verified by: run script pre-re-skin, confirm exits 1 with 15 mismatches. |
| T-98-03 | Denial of Service (CI) | lint.sh CWD fix | mitigate | New `cd "${RULESTEAD_REPO}"` line must not affect the Elixir commands above it (mix dialyzer etc.) — those already completed and relied on the rulestead/ CWD from line 6. Inserting after line 15 (dialyzer) is safe. Verified by: git diff shows only one new line, existing lines unchanged. |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan. Python stdlib only. No supply-chain surface. |
</threat_model>

<verification>
Run from repo root after both tasks complete:

```
python3 scripts/check_synced_pair.py
python3 scripts/check_brand_tokens.py
python3 scripts/check_tokens_css.py
python3 scripts/check_contrast.py
```

Expected results pre-re-skin:
- check_synced_pair.py: exits 0, prints two SYNCED PAIR IDENTICAL lines (dark + light)
- check_brand_tokens.py: exits 1, prints "BRAND TOKEN DRIFT DETECTED" with 15 mismatches (7 light + 8 dark including [dark] --rs-success-border #166634 vs #166534)
- check_tokens_css.py: exits 0, "TOKENS.CSS MIRROR SYNCED (68 tokens)" — unchanged
- check_contrast.py: exits 0, "CONTRAST CHECK PASS (18 checks)" — unchanged
</verification>

<success_criteria>
- check_synced_pair.py exits 0 with both pair lines present; dark-pair line unchanged
- check_brand_tokens.py exits 1 with 15-mismatch output (light + dark); [dark] --rs-success-border mismatch present
- lint.sh has cd "${RULESTEAD_REPO}" before guard block; all other lines unchanged
- git diff shows only additive changes in the three files; no existing logic removed
</success_criteria>

<output>
Create `.planning/phases/98-admin-re-skin-css-cascade/98-01-SUMMARY.md` when done
</output>
