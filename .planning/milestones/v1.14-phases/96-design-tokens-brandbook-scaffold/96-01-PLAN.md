---
phase: 96-design-tokens-brandbook-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tokens.json
  - brandbook/tokens.css
autonomous: true
requirements:
  - TOK-01
  - TOK-02
  - TOK-03
  - TOK-04

must_haves:
  truths:
    - "brandbook/tokens.json is valid JSON in DTCG 2025.10 format with $value/$type/$description leaves"
    - "tokens.json has a top-level light/dark group split (D-02) — NOT $extensions modes"
    - "tokens.json has an admin_css_mapping section with light (37 tokens) and dark (31 tokens) sub-dicts"
    - "All admin_css_mapping values are Phase-98 mineral targets from 95-PALETTE-RECONCILIATION.md §4/§8"
    - "tokens.css is a valid CSS file with :root invariant block + two-block .rs-shell/[data-rulestead] light/dark pair"
    - "tokens.css scope never uses :root or <html> for color (D-05)"
    - "tokens.css ends with a commented-out Tailwind excerpt (D-06)"
    - "admin_css_mapping maps only hex-literal --rs-* tokens (no var(), rgba(), shadow composites)"
  artifacts:
    - path: "brandbook/tokens.json"
      provides: "DTCG machine-readable token canonical record"
      contains: "admin_css_mapping"
    - path: "brandbook/tokens.css"
      provides: "CSS --rs-* reference mirror for light and dark"
      contains: "--rs-primary"
  key_links:
    - from: "brandbook/tokens.json admin_css_mapping.light"
      to: "scripts/check_brand_tokens.py (Plan 03)"
      via: "json.load() + mapping extraction"
      pattern: "admin_css_mapping"
    - from: "brandbook/tokens.css"
      to: "rulestead_admin/priv/static/css/rulestead_admin.css"
      via: "mirrors --rs-* token shape; two-block vs four-block documented in comment"
      pattern: "--rs-primary:"
---

<objective>
Author brandbook/tokens.json (DTCG 2025.10, three-tier primitive/semantic/state model + invariant group + top-level light/dark split + admin_css_mapping) and brandbook/tokens.css (hand-authored --rs-* reference mirror with :root invariant block, two-block light/dark, and commented Tailwind excerpt).

Purpose: Establish the canonical machine-readable token record that Phase 98 re-skins toward and Phase 03 of this phase validates against. All hex values are encoded verbatim from the Phase 95 locked palette — no recomputation.

Output: brandbook/tokens.json and brandbook/tokens.css committed.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-design-tokens-brandbook-scaffold/96-CONTEXT.md
@.planning/phases/96-design-tokens-brandbook-scaffold/96-RESEARCH.md
@.planning/phases/96-design-tokens-brandbook-scaffold/96-PATTERNS.md
@.planning/phases/95-brand-audit-palette-reconciliation/95-PALETTE-RECONCILIATION.md

<interfaces>
<!-- Key patterns extracted from existing codebase for executor reference. -->

From rulestead_admin/priv/static/css/rulestead_admin.css (Block 1 hex-literal tokens — complete list for admin_css_mapping.light):
--rs-neutral-0: #ffffff
--rs-neutral-25: #f8fafc
--rs-neutral-50: #f4f6f8
--rs-neutral-100: #eef1f5
--rs-neutral-200: #e7ebf0
--rs-neutral-300: #d8dee6
--rs-neutral-400: #b8c2cf
--rs-neutral-500: #99a3af
--rs-neutral-600: #5c6b7a
--rs-neutral-700: #263241  [LIGHT-ONLY — absent from Block 3]
--rs-neutral-900: #1a2332
--rs-primary: #2563eb      [CURRENT generic — target: #3A6F8F]
--rs-primary-hover: #1d4ed8 [CURRENT generic — target: #2d5f7c]
--rs-primary-soft: #dbeafe
--rs-on-primary: #ffffff
--rs-accent: #9a3f12       [CURRENT generic — target: #9b5931]
--rs-accent-soft: #fde8dc
--rs-success: #15803d      [CURRENT generic — target: #2d7753]
--rs-success-hover: #166534
--rs-success-soft: #dcfce7
--rs-success-bg-subtle: #f7fff9
--rs-success-text: #047857
--rs-success-border: #86efac
--rs-warning: #b45309      [CURRENT generic — target: #8f601a]
--rs-warning-hover: #92400e
--rs-warning-soft: #fef3c7
--rs-warning-text: #a16207
--rs-warning-border: #fcd34d
--rs-error: #b91c1c        [CURRENT generic — target: #B44949]
--rs-error-hover: #991b1b
--rs-error-soft: #fee2e2
--rs-error-bg-subtle: #fff7f7
--rs-error-text: #be123c
--rs-error-text-strong: #7f1d1d
--rs-error-border: #fca5a5
--rs-error-border-strong: #fecaca
--rs-critical: #b91c1c     [CURRENT generic — target: #B44949]

Block 3 hex-literal tokens for admin_css_mapping.dark (31 tokens):
--rs-neutral-0: #10161f
--rs-neutral-25: #141c27
--rs-neutral-50: #19222e
--rs-neutral-100: #1f2a38
--rs-neutral-200: #253243
--rs-neutral-300: #2e3d52
--rs-neutral-400: #3d5168
--rs-neutral-500: #7a8fa3
--rs-neutral-600: #a8b9ca
--rs-neutral-900: #e8edf3
--rs-primary: #2563eb      [target: #5885a0]
--rs-primary-hover: #5a96f5 [target: #4a7d9c — darkened from #5885a0 for hover]
--rs-on-primary: #ffffff
--rs-accent: #e8834a       [target: #ba6b3c]
--rs-success: #4ade80      [target: #488d6b]
--rs-success-hover: #86efac
--rs-success-text: #4ade80
--rs-success-border: #166634
--rs-warning: #fbbf24      [target: #B57A21]
--rs-warning-hover: #fcd34d
--rs-warning-text: #fbbf24
--rs-warning-border: #78350f
--rs-error: #f87171        [target: #bf6464]
--rs-error-hover: #fca5a5
--rs-error-text: #f87171
--rs-error-text-strong: #fca5a5
--rs-error-border: #7f1d1d
--rs-error-border-strong: #991b1b
--rs-critical: #f87171     [target: #bf6464]
--rs-disabled-bg: #253243  [DARK-ONLY hex-literal]
--rs-disabled-text: #6b84a0 [DARK-ONLY hex-literal]

Locked canonical hexes from 95-PALETTE-RECONCILIATION.md §4/§8:
Light: Stead Blue #3A6F8F | Ember Copper #9b5931 | Warning #8f601a | Moss Grey #606d66
       Success canonical #2d7753 (SM) | Danger canonical #b04848 (SM) | Info #356E8C
Dark:  Stead Blue #5885a0 | Ember Copper #ba6b3c | Success #488d6b | Danger #bf6464
       Info #55859e | Moss Grey #75827b | Warning #B57A21 | Signal Gold #D2A94E
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author brandbook/tokens.json (DTCG 2025.10 with admin_css_mapping)</name>
  <files>brandbook/tokens.json</files>
  <read_first>
    - .planning/phases/95-brand-audit-palette-reconciliation/95-PALETTE-RECONCILIATION.md §3, §4, §5, §6, §8 — ALL canonical hex values; use verbatim
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-RESEARCH.md §"DTCG 2025.10 Format Specifics" + §"DTCG tokens.json Full Group Structure" + §"Token Inventory" — JSON skeleton, $type rules, admin_css_mapping scope decision
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-PATTERNS.md §"brandbook/tokens.json" — top-level layout, leaf vs group rules, $type inheritance pattern, asymmetry handling
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-CONTEXT.md decisions D-01, D-02, D-03, D-04
  </read_first>
  <action>
    Create brandbook/tokens.json with the following exact structure (per D-01/D-02/D-03/D-04):

    TOP-LEVEL GROUPS (in order): "primitive", "light", "dark", "invariant", "admin_css_mapping"

    "primitive" group ($type: "color" inherited by sub-groups):
      - "stead-blue": base #3A6F8F (description: passes all light surfaces 5.45:1 W / 4.57:1 SM / 5.07:1 RT), dark #5885a0 (4.563:1 on #10161f)
      - "ember-copper": base #9b5931 (canonical SM 4.550:1), dark #ba6b3c (4.545:1; OKLCH drift 0.37deg)
      - "ink-blue": base #183247 ($description: deep contrast, dark-on-light)
      - "slate-stead": base #24313D ($description: secondary dark UI / structural)
      - "basalt": base #0F1720 ($description: deepest dark; visually indistinguishable from #10161f)
      - "signal-gold": base #D2A94E ($description: decorative-only — NEVER as normal-weight text; passes dark at 8.24:1)
      - "moss-grey": base #606d66 (SM canonical 4.539:1), dark #75827b (4.527:1)
      - "stone-mist": base #E8ECE8
      - "rain-tint": base #F5F7F6
      - "quarry": base #C4CCD1
      - "success": canonical #2d7753 ($description: Gap-2 Stone Mist canonical 4.540:1; primary canonical), white-rt #2F7D57 ($description: use on White/Rain Tint only), dark #488d6b (4.581:1)
      - "danger": canonical #b04848 ($description: Gap-2 Stone Mist canonical 4.550:1; primary canonical), white-rt #B44949 ($description: use on White/Rain Tint only), dark #bf6464 (4.515:1)
      - "warning": canonical #8f601a (SM 4.563:1), dark #B57A21 (passes dark at 4.99:1)
      - "info": base #356E8C (all light surfaces pass), dark #55859e (4.526:1)
      - "neutral-ramp": all ramp slots light (#ffffff…#1a2332) and dark ramp (#10161f…#e8edf3) matching shipped CSS values from Block 1 / Block 3

    "light" group ($type: "color" — semantic + state aliases):
      Roles: default, hover, active, focus, disabled, selected, success, success-stone-mist, warning, error, danger-stone-mist, info, subtle, muted
      - default: alias "{primitive.stead-blue.base}"
      - hover: alias "{primitive.stead-blue.base}" (D-04: interim; Phase 98 may refine)
      - active: alias "{primitive.stead-blue.base}"
      - focus: alias "{primitive.stead-blue.base}"
      - disabled: alias "{primitive.neutral-ramp.light-500}" (or nearest neutral ramp leaf)
      - selected: alias "{primitive.stead-blue.base}"
      - success: alias "{primitive.success.canonical}" ($description: canonical Stone Mist target; use white-rt variant on White/Rain Tint)
      - success-stone-mist: alias "{primitive.success.canonical}" ($description: Gap 2 — use on Stone Mist surface only)
      - warning: alias "{primitive.warning.canonical}"
      - error: alias "{primitive.danger.canonical}" ($description: Gap 2 canonical Stone Mist; use white-rt on White/Rain Tint)
      - danger-stone-mist: alias "{primitive.danger.canonical}" ($description: Gap 2 — use on Stone Mist surface only)
      - info: alias "{primitive.info.base}"
      - subtle: alias for moss-grey canonical
      - muted: suitable neutral ramp leaf

    "dark" group ($type: "color" — parallel semantic aliases for dark surface):
      Same roles referencing primitive dark variants.

    "invariant" group (NOT re-skinned; these are the :root scalars):
      - "spacing" ($type: "dimension"): space-1 0.25rem through space-10 2.5rem (map all 10 rulestead_admin.css --rs-space-* values)
      - "radius" ($type: "dimension"): sm 6px, md 10px, lg 14px, xl 18px, full 999px
      - "shadow" ($type: "shadow"): sm, md, panel (use DTCG composite shadow shape from RESEARCH.md)
      - "focus-ring" ($type: "dimension"): offset 2px
      - "code-block": font ($type: "fontFamily", value "IBM Plex Mono, ui-monospace, SFMono-Regular, Menlo, monospace")
      - "callout": radius ($type: "dimension", value "10px")

    "admin_css_mapping":
      CRITICAL RULES (per D-03 and Pitfall 4):
      - Map ONLY tokens whose Block 1/3 CSS value is a LITERAL HEX STRING
      - EXCLUDE all var(), rgba(), and shadow composite tokens — string comparison cannot resolve them
      - "light" sub-object: 37 hex-literal tokens from Block 1 (see interface section above)
      - "dark" sub-object: 31 hex-literal tokens from Block 3 (see interface section above)
      - Include --rs-neutral-700: #263241 in light ONLY (light-only asymmetry)
      - Include --rs-disabled-bg: #253243 and --rs-disabled-text: #6b84a0 in dark ONLY (dark-only hex-literal)
      - DO NOT include --rs-info-* (no such tokens exist in shipped CSS)

      For admin_css_mapping.light, the SIX PRIMARY MISMATCH tokens that guarantee exit 1:
        "--rs-primary": "#3A6F8F"
        "--rs-primary-hover": "#2d5f7c"
        "--rs-accent": "#9b5931"
        "--rs-success": "#2d7753"
        "--rs-warning": "#8f601a"
        "--rs-error": "#B44949"
        "--rs-critical": "#B44949"

      For tokens NOT changing in Phase 98 (neutral ramp, soft tints, etc.), encode the CURRENT shipped CSS value as the target so those tokens do not contribute extra noise to the exit-1 diff. The 7 primary mismatches above are sufficient to guarantee exit 1.

      For admin_css_mapping.dark: encode the Phase-98 mineral targets for the six changed tokens; keep shipped values for neutral ramp and generic soft tokens that Phase 98 may not change immediately.

    DTCG leaf rule: every leaf MUST have "$value"; groups MUST NOT have "$value". Validate mental model against RESEARCH.md "Token Leaf vs Group Rule".
  </action>
  <verify>
    <automated>python3 -c "import json; t=json.load(open('brandbook/tokens.json')); assert 'admin_css_mapping' in t, 'missing admin_css_mapping'; assert 'light' in t['admin_css_mapping'], 'missing light'; assert 'dark' in t['admin_css_mapping'], 'missing dark'; m=t['admin_css_mapping']['light']; assert '--rs-primary' in m, 'missing --rs-primary'; assert m['--rs-primary'].upper()=='#3A6F8F', f'wrong primary: {m[\"--rs-primary\"]}'; assert '--rs-neutral-700' in m, 'missing --rs-neutral-700 (light-only)'; assert '--rs-neutral-700' not in t['admin_css_mapping']['dark'], '--rs-neutral-700 should not be in dark'; print('tokens.json OK:', len(m), 'light tokens,', len(t[\"admin_css_mapping\"][\"dark\"]), 'dark tokens')"</automated>
  </verify>
  <done>brandbook/tokens.json parses as valid JSON; admin_css_mapping.light has 37 keys; admin_css_mapping.dark has 31 keys; --rs-primary is #3A6F8F; --rs-neutral-700 is in light only; --rs-disabled-bg/#disabled-text are in dark only; python3 -c "import json; json.load(open('brandbook/tokens.json'))" exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Author brandbook/tokens.css (hand-authored --rs-* reference mirror)</name>
  <files>brandbook/tokens.css</files>
  <read_first>
    - rulestead_admin/priv/static/css/rulestead_admin.css lines 39–119 (invariant :root block — copy token names and values verbatim for the reference mirror)
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-CONTEXT.md D-05, D-06
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-PATTERNS.md §"brandbook/tokens.css" — header comment pattern, two-block pattern, TOK-04 Tailwind excerpt pattern
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-RESEARCH.md §"tokens.css Structure"
    - brandbook/tokens.json (Task 1 output — admin_css_mapping.light values are the light block source; admin_css_mapping.dark values are the dark block source)
  </read_first>
  <action>
    Create brandbook/tokens.css with exactly three sections in order:

    SECTION 1 — Header comment block (see PATTERNS.md for exact template):
      State: "Source of truth: brandbook/tokens.json (DTCG 2025.10)"
      State: "Admin cascade: rulestead_admin/priv/static/css/rulestead_admin.css"
      Note: the four-block @media + explicit-pin cascade is a live-app concern; this file is the reference mirror only
      Scope note: ".rs-shell / [data-rulestead] only — never :root or <html> for color"

    SECTION 2 — :root invariant block:
      Selector: ":root"
      Read rulestead_admin.css lines 39–119 and copy ALL invariant token names and values verbatim.
      Include: font families (--rs-font-display/sans/mono), type scale (--rs-text-2xs...-2xl), leading, weight, tracking, radius (--rs-radius-sm/md/lg/xl/full), layout/spacing (--rs-shell-max, --rs-space-1...-10, --rs-section-gap, --rs-page-gap), control (--rs-control-h/-sm/-lg, --rs-control-px, --rs-touch-target-min), focus-structural (--rs-focus-ring-offset, --rs-disabled-opacity), z-index (--rs-z-*), motion durations + easing (--rs-motion-*, --rs-ease-*)
      Comment inside block: "INVARIANT — theme-insensitive scalars; these are NOT re-skinned in Phase 98"

    SECTION 3 — Light block (mineral palette):
      Selector: ".rs-shell,\n[data-rulestead]"
      Comment: "LIGHT — mineral palette (Phase-98 targets)"
      Emit all 37 hex-literal tokens from admin_css_mapping.light in tokens.json in the same categorical order as rulestead_admin.css Block 1: neutral ramp first, then brand/status tokens.
      Each token on its own line with two-space indent and trailing semicolon.
      SCOPE ENFORCEMENT: Never set color tokens on :root — they belong here only.

    SECTION 4 — Dark block (mineral palette):
      Selector: ".rs-shell[data-theme=\"dark\"],\n[data-rulestead][data-theme=\"dark\"]"
      Comment: "DARK — mineral palette (Phase-98 targets)"
      Emit all 31 hex-literal tokens from admin_css_mapping.dark in tokens.json.
      Note after block in a CSS comment: "Note: --rs-neutral-700 is light-only (absent in dark). --rs-disabled-bg and --rs-disabled-text are dark-only hex-literals."
      Note that @media (prefers-color-scheme: dark) block is handled in rulestead_admin.css only.

    SECTION 5 — Commented-out Tailwind excerpt (D-06, TOK-04):
      A trailing CSS block comment containing the Tailwind v3/v4 config excerpt from PATTERNS.md/RESEARCH.md.
      Must include: rs-stead-blue #3A6F8F, rs-ember-copper #9b5931, rs-stone-mist #E8ECE8, rs-basalt #0F1720, rs-signal-gold #D2A94E, rs-success #2d7753, rs-warning #8f601a, rs-danger #B44949, rs-info #356E8C, rs-moss-grey #606d66.
      The block is wrapped in /* ... */ so the file is valid, side-effect-free CSS.
      No --rs-* tokens are redefined inside this block.
  </action>
  <verify>
    <automated>grep -c '\-\-rs-primary:' brandbook/tokens.css</automated>
    <automated>python3 -c "
import re
with open('brandbook/tokens.css') as f: css = f.read()
assert ':root' in css, 'missing :root block'
assert '.rs-shell,' in css, 'missing .rs-shell light selector'
assert 'data-theme=\"dark\"' in css, 'missing dark selector'
assert '--rs-primary: #3A6F8F' in css, 'wrong primary in light block'
assert 'html' not in css.lower().replace('<!-- ', ''), 'html scope violation'
# Scope: no color token in :root block
root_block = css[css.find(':root'):css.find('}', css.find(':root'))+1]
assert '--rs-primary' not in root_block, '--rs-primary must not be in :root'
# Tailwind comment present
assert 'rs-stead-blue' in css, 'Tailwind excerpt missing'
print('tokens.css structure OK')
"</automated>
  </verify>
  <done>brandbook/tokens.css is valid CSS (no parse errors); :root block contains invariant tokens; .rs-shell light block contains --rs-primary: #3A6F8F and 36 other hex-literal tokens; dark block present; no color token in :root; Tailwind commented-out excerpt present at end of file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo checkout → committed JSON/CSS | Source-controlled config files; no network surface; no runtime execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Tampering | brandbook/tokens.json admin_css_mapping | mitigate | Map only hex-literal tokens (exclude var()/rgba()/shadow composites) so Phase 98 drift check cannot produce false-green from unresolved CSS values |
| T-96-02 | Tampering | brandbook/tokens.css scope | mitigate | Verify no color token under :root (only .rs-shell/[data-rulestead] selectors); add scope comment; automated verify asserts root_block does not contain --rs-primary |
| T-96-03 | Repudiation | admin_css_mapping.light values | accept | All 37 values are verbatim from D-11-signed-off palette record; hex provenance is auditable via git history + PALETTE-RECONCILIATION.md §8 |
| T-96-04 | Information Disclosure | tokens.json committed to repo | accept | No secrets; all values are brand palette colors intended to be public |
| T-96-05 | Denial of Service | malformed JSON in tokens.json | mitigate | json.load() in check_brand_tokens.py will raise JSONDecodeError and exit non-zero; automated verify runs json.load before task is marked done |
</threat_model>

<verification>
After both tasks complete:

1. brandbook/tokens.json parses as valid DTCG JSON: `python3 -c "import json; json.load(open('brandbook/tokens.json'))" && echo OK`
2. admin_css_mapping.light has exactly 37 keys: `python3 -c "import json; m=json.load(open('brandbook/tokens.json'))['admin_css_mapping']['light']; print(len(m), 'light tokens')"`
3. admin_css_mapping.dark has exactly 31 keys: `python3 -c "import json; m=json.load(open('brandbook/tokens.json'))['admin_css_mapping']['dark']; print(len(m), 'dark tokens')"`
4. tokens.css light block contains --rs-primary: #3A6F8F: `grep -c '#3A6F8F' brandbook/tokens.css`
5. tokens.css dark block contains --rs-primary: #5885a0: `grep -c '#5885a0' brandbook/tokens.css`
6. tokens.css has Tailwind excerpt: `grep -c 'rs-stead-blue' brandbook/tokens.css`
7. No color token in :root: `python3 -c "css=open('brandbook/tokens.css').read(); root=css[css.find(':root'):css.find('}',css.find(':root'))+1]; assert '--rs-primary' not in root; print('scope OK')"`
8. check_synced_pair.py still passes (not disturbed by this plan): `python3 scripts/check_synced_pair.py`
</verification>

<success_criteria>
- brandbook/tokens.json exists with valid DTCG 2025.10 structure: primitive/light/dark/invariant/admin_css_mapping top-level groups
- admin_css_mapping.light maps 37 hex-literal tokens with Phase-98 mineral targets including --rs-primary: #3A6F8F
- admin_css_mapping.dark maps 31 hex-literal tokens; --rs-neutral-700 absent from dark; --rs-disabled-bg/#disabled-text present only in dark
- brandbook/tokens.css: :root invariant block + .rs-shell light block + dark block + commented Tailwind excerpt
- tokens.css light block uses mineral targets (--rs-primary: #3A6F8F, --rs-accent: #9b5931, --rs-success: #2d7753, --rs-warning: #8f601a, --rs-error: #B44949)
- No var(), rgba(), or shadow composites in admin_css_mapping
- python3 scripts/check_synced_pair.py still exits 0 (this plan does not touch rulestead_admin.css)
</success_criteria>

<output>
Create .planning/phases/96-design-tokens-brandbook-scaffold/96-01-SUMMARY.md when done.
</output>
