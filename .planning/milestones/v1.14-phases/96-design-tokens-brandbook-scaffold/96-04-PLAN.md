---
phase: 96-design-tokens-brandbook-scaffold
plan: 04
type: execute
wave: 3
depends_on:
  - 96-01
  - 96-02
  - 96-03
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TOK-01
  - TOK-02
  - TOK-03
  - TOK-04

must_haves:
  truths:
    - "All five phase success criteria are mechanically verifiable via the commands in this plan"
    - "check_brand_tokens.py exits 1 with BRAND TOKEN DRIFT DETECTED + ≥7 per-token diff lines"
    - "check_synced_pair.py exits 0 (rulestead_admin.css Blocks 2≡3 synced pair undisturbed)"
    - "All SC grep assertions pass (tokens.json parses, tokens.css structure, lint.sh additions, brand-book relocation)"
    - "STATE.md and ROADMAP.md updated to reflect Phase 96 completion"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated project state — Phase 96 complete, Phase 97 ready"
      contains: "Phase 97"
    - path: ".planning/ROADMAP.md"
      provides: "Updated roadmap — Phase 96 plan list with 4 plans"
      contains: "96-01-PLAN.md"
  key_links:
    - from: "ROADMAP.md Phase 96 plans list"
      to: ".planning/phases/96-design-tokens-brandbook-scaffold/96-0*-PLAN.md"
      via: "plan list entries"
      pattern: "96-0"
---

<objective>
Run all phase success-criteria assertions from VALIDATION.md and ROADMAP.md SC-1 through SC-5, confirm the intentional exit-1 from check_brand_tokens.py is the phase's gate-green state, and update STATE.md and ROADMAP.md to mark Phase 96 complete.

Purpose: Close the phase with auditable proof that the guard mechanism works (exit 1) and that all five success criteria pass. Transition state to Phase 97 ready.

Output: Verified assertions, updated STATE.md and ROADMAP.md.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-design-tokens-brandbook-scaffold/96-CONTEXT.md
@.planning/phases/96-design-tokens-brandbook-scaffold/96-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all SC assertions — confirm exit-1 is the phase gate-green state</name>
  <files></files>
  <read_first>
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-VALIDATION.md — per-task verification map and the critical note: "exit 1 is the GREEN state for Phase 96"
    - .planning/ROADMAP.md Phase 96 Success Criteria SC-1 through SC-5 — authoritative success criteria
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-RESEARCH.md §"Validation Architecture" — all SC verification commands
  </read_first>
  <action>
    Run each of the following verification commands in order. Record the output. If any assertion FAILS (unexpected result), stop and investigate — do not proceed to STATE/ROADMAP updates until all assertions pass.

    CRITICAL FRAMING: For Phase 96, "python3 scripts/check_brand_tokens.py exits 1" IS the success state. It must exit 1 against the un-re-skinned admin CSS. Phase 98 is what makes it go green. Do NOT treat exit 1 as a failure of Phase 96.

    SC-1 Assertions (tokens.json DTCG format):
      python3 -c "import json; t=json.load(open('brandbook/tokens.json')); print('admin_css_mapping' in t)"
        → Expected: True
      python3 -c "import json; t=json.load(open('brandbook/tokens.json')); m=t['admin_css_mapping']['light']; print(len(m), 'light tokens')"
        → Expected: 37 light tokens
      python3 -c "import json; t=json.load(open('brandbook/tokens.json')); m=t['admin_css_mapping']['dark']; print(len(m), 'dark tokens')"
        → Expected: 31 dark tokens
      python3 -c "import json; t=json.load(open('brandbook/tokens.json')); assert '--rs-neutral-700' in t['admin_css_mapping']['light']; assert '--rs-neutral-700' not in t['admin_css_mapping']['dark']; print('light-only asymmetry OK')"
        → Expected: light-only asymmetry OK
      python3 -c "import json; t=json.load(open('brandbook/tokens.json')); assert t['admin_css_mapping']['light']['--rs-primary'].upper()=='#3A6F8F'; print('primary hex OK')"
        → Expected: primary hex OK

    SC-2 Assertions (tokens.css mirror):
      grep -c '\-\-rs-primary:' brandbook/tokens.css
        → Expected: ≥ 1 (both light and dark blocks contain --rs-primary)
      grep -c '#3A6F8F' brandbook/tokens.css
        → Expected: ≥ 1
      grep -c '#5885a0' brandbook/tokens.css
        → Expected: ≥ 1 (dark block)
      python3 -c "css=open('brandbook/tokens.css').read(); root=css[css.find(':root'):css.find('}',css.find(':root'))+1]; assert '--rs-primary' not in root; print('scope OK — no color in :root')"
        → Expected: scope OK — no color in :root
      grep -c 'rs-stead-blue' brandbook/tokens.css
        → Expected: ≥ 1 (Tailwind excerpt present)

    SC-3 Assertions (check_brand_tokens.py exits 1 — THIS IS THE PHASE GATE-GREEN STATE):
      python3 scripts/check_brand_tokens.py
        → Expected: exits 1, prints "BRAND TOKEN DRIFT DETECTED" + per-token diff
      python3 scripts/check_brand_tokens.py 2>&1 | grep -c 'BRAND TOKEN DRIFT DETECTED'
        → Expected: 1
      python3 scripts/check_brand_tokens.py 2>&1 | grep '\-\-rs-primary:'
        → Expected: line containing "tokens.json=#3A6F8F  css=#2563eb"

    SC-4 Assertions (lint.sh additions + docs):
      grep -c 'check_brand_tokens.py' scripts/ci/lint.sh
        → Expected: 1
      grep -c 'SVG SIZE BUDGET OK' scripts/ci/lint.sh
        → Expected: 1
      grep -c 'check_synced_pair.py' scripts/ci/lint.sh
        → Expected: 1
      test -f brandbook/docs/brand-usage.md && echo ok
        → Expected: ok
      grep -c 'brandbook/brand-book.md' prompts/rulestead-brand-book.md
        → Expected: ≥ 1

    SC-5 Assertions (brand-book relocation + §12 hex reconciliation):
      test -f brandbook/brand-book.md && echo ok
        → Expected: ok
      grep -c '#9b5931' brandbook/brand-book.md
        → Expected: ≥ 2 (Ember Copper in entry + Gradients section)
      grep -c '#B96A3A' brandbook/brand-book.md
        → Expected: 0 (old hex fully replaced — ZERO)
      grep -c 'Runtime decisions, made clear' brandbook/brand-book.md
        → Expected: ≥ 1 (§8 tagline unchanged)
      wc -l < prompts/rulestead-brand-book.md
        → Expected: < 20 (pointer stub, not the full book)
      git log --follow brandbook/brand-book.md | grep -c "^commit"
        → Expected: ≥ 2 (history preserved via git mv)

    BONUS GUARD — synced pair still intact (DO NOT skip this):
      python3 scripts/check_synced_pair.py
        → Expected: exits 0, prints "SYNCED PAIR IDENTICAL (N tokens)"
  </action>
  <acceptance_criteria>
    - python3 scripts/check_brand_tokens.py exits 1 (verified with echo "exit: $?")
    - python3 scripts/check_brand_tokens.py output contains "BRAND TOKEN DRIFT DETECTED"
    - python3 scripts/check_brand_tokens.py output contains "--rs-primary: tokens.json=#3A6F8F"
    - python3 scripts/check_synced_pair.py exits 0 (output contains "SYNCED PAIR IDENTICAL")
    - brandbook/tokens.json parses as JSON with admin_css_mapping.light count = 37
    - brandbook/tokens.css contains #3A6F8F and #5885a0 and is valid CSS
    - brandbook/tokens.css :root block does not contain --rs-primary
    - scripts/ci/lint.sh contains check_brand_tokens.py, check_synced_pair.py, SVG SIZE BUDGET OK
    - brandbook/brand-book.md exists; grep '#B96A3A' → 0; grep '#9b5931' → ≥ 2
    - prompts/rulestead-brand-book.md line count < 20
    - brandbook/docs/brand-usage.md exists
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/check_brand_tokens.py; BRAND_EXIT=$?; if [ "$BRAND_EXIT" -eq 1 ]; then echo "SC-3 PASS: check_brand_tokens.py exits 1 (intended)"; else echo "SC-3 FAIL: expected exit 1, got $BRAND_EXIT"; exit 1; fi</automated>
    <automated>python3 scripts/check_synced_pair.py && echo "synced-pair guard OK"</automated>
    <automated>python3 -c "
import json, subprocess, sys

# SC-1: tokens.json
t = json.load(open('brandbook/tokens.json'))
assert 'admin_css_mapping' in t, 'SC-1 FAIL: admin_css_mapping missing'
light = t['admin_css_mapping']['light']
dark = t['admin_css_mapping']['dark']
assert len(light) == 37, f'SC-1 FAIL: expected 37 light tokens, got {len(light)}'
assert len(dark) == 31, f'SC-1 FAIL: expected 31 dark tokens, got {len(dark)}'
assert '--rs-neutral-700' in light and '--rs-neutral-700' not in dark, 'SC-1 FAIL: neutral-700 asymmetry'
assert light['--rs-primary'].upper() == '#3A6F8F', f'SC-1 FAIL: wrong primary {light[\"--rs-primary\"]}'
print('SC-1 PASS')

# SC-2: tokens.css
css = open('brandbook/tokens.css').read()
assert '--rs-primary: #3A6F8F' in css, 'SC-2 FAIL: light primary missing'
assert '--rs-primary: #5885a0' in css, 'SC-2 FAIL: dark primary missing'
root = css[css.find(':root'):css.find('}', css.find(':root'))+1]
assert '--rs-primary' not in root, 'SC-2 FAIL: color in :root scope'
assert 'rs-stead-blue' in css, 'SC-2 FAIL: Tailwind excerpt missing'
print('SC-2 PASS')

# SC-4: lint.sh
lint = open('scripts/ci/lint.sh').read()
assert 'check_brand_tokens.py' in lint, 'SC-4 FAIL: brand token check missing'
assert 'SVG SIZE BUDGET OK' in lint, 'SC-4 FAIL: SVG budget missing'
assert 'check_synced_pair.py' in lint, 'SC-4 FAIL: synced pair check missing'
import os
assert os.path.isfile('brandbook/docs/brand-usage.md'), 'SC-4 FAIL: brand-usage.md missing'
pointer = open('prompts/rulestead-brand-book.md').read()
assert 'brandbook/brand-book.md' in pointer, 'SC-4 FAIL: pointer missing'
print('SC-4 PASS')

# SC-5: brand-book
assert os.path.isfile('brandbook/brand-book.md'), 'SC-5 FAIL: brand-book.md missing'
book = open('brandbook/brand-book.md').read()
assert '#9b5931' in book, 'SC-5 FAIL: Ember Copper canonical missing'
assert '#B96A3A' not in book, 'SC-5 FAIL: old Ember Copper hex still present'
assert 'Runtime decisions, made clear' in book, 'SC-5 FAIL: tagline changed'
stub_lines = open('prompts/rulestead-brand-book.md').readlines()
assert len(stub_lines) < 20, f'SC-5 FAIL: pointer stub too long: {len(stub_lines)} lines'
print('SC-5 PASS')
print('ALL SC ASSERTIONS PASSED')
"</automated>
  </verify>
  <done>All SC-1 through SC-5 assertions pass. check_brand_tokens.py exits 1 (the phase gate-green state). check_synced_pair.py exits 0. Zero regression in synced-pair invariant.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md and ROADMAP.md to reflect Phase 96 completion</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md — read current state; update Current Position + Progress bar + Operator Next Steps
    - .planning/ROADMAP.md — read Phase 96 plans list; update with actual plan names; update Progress Table
  </read_first>
  <action>
    UPDATE .planning/STATE.md:

    1. Change "Current focus" to: "Phase 97 — logo & mark SVG system"
    2. Change Current Position to:
         Phase: 97
         Plan: Not started
         Status: Ready to plan
         Last activity: 2026-06-04
    3. Update Progress bar to: `[ ##░░░░░░░░ ] 17% — 1/6 phases` (Phase 96 complete = same 17% as Phase 95 had after completing; the bar was already at 17% after Phase 95 which was the first phase)
       Actually: 2 of 6 phases complete = 33%: `[ ###░░░░░░░ ] 33% — 2/6 phases`
    4. Update Operator Next Steps:
         - Start Phase 97 with `/gsd:plan-phase 97`
         - Note: Phase 97 has a mid-phase checkpoint — maintainer must select logo concept A/B/C before full lockup is produced
         - Note: Phase 96 check_brand_tokens.py intentionally exits 1 until Phase 98 re-skins rulestead_admin.css
    5. Add to Accumulated Context > Decisions:
         - [Phase 96]: brandbook/ scaffold committed — tokens.json (DTCG 2025.10), tokens.css (--rs-* mirror), check_brand_tokens.py (exits 1 intentionally against generic CSS), lint.sh extended additively
         - [Phase 96]: brand-book.md relocated from prompts/ via git mv; §12 hexes reconciled to AA-verified canonicals; Gap-2 per-surface notes added for Success/Danger on Stone Mist
         - [Phase 96]: check_synced_pair.py wired into lint.sh (was dev-only; now CI guard)

    UPDATE .planning/ROADMAP.md:

    1. Phase 96 Progress Table entry: change `0/0` to `4/4`, status to "Complete", date to "2026-06-04"
    2. Phase 96 Plans section: replace the placeholder list (which currently copies Phase 95 plans verbatim — a known ROADMAP placeholder error) with:
         **Wave 1** *(parallel)*
         - [x] 96-01-PLAN.md — tokens.json + tokens.css (DTCG scaffold + CSS mirror)
         - [x] 96-02-PLAN.md — brand-book relocation + §12 rework + brandbook/README.md + docs/brand-usage.md

         **Wave 2** *(blocked on Wave 1 completion)*
         - [x] 96-03-PLAN.md — check_brand_tokens.py + lint.sh extension

         **Wave 3** *(blocked on Wave 2 completion)*
         - [x] 96-04-PLAN.md — phase SC verification + STATE/ROADMAP update

    3. Mark Phase 96 checkbox as [x] in the Phases list.
  </action>
  <acceptance_criteria>
    - `grep -c 'Phase 97' .planning/STATE.md` → ≥ 2 (current focus + next steps)
    - `grep -c '96-01-PLAN.md' .planning/ROADMAP.md` → ≥ 1
    - `grep -c '4/4' .planning/ROADMAP.md` → ≥ 1 (Phase 96 progress)
    - `grep -c 'check_brand_tokens.py' .planning/STATE.md` → ≥ 1 (decision recorded)
    - `grep -c '33%' .planning/STATE.md` → ≥ 1 (progress bar updated)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Phase 97' .planning/STATE.md && grep -c '96-01-PLAN.md' .planning/ROADMAP.md && echo "STATE and ROADMAP updated OK"</automated>
  </verify>
  <done>STATE.md current focus = Phase 97; Operator Next Steps points to /gsd:plan-phase 97; Phase 96 decisions recorded. ROADMAP.md Phase 96 checkbox marked [x], plans list shows 4 plans with wave structure, progress table shows 4/4 Complete 2026-06-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification assertions → CI gate | All verification is read-only assertion; no mutations; exit codes are the signals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-15 | Tampering | Verification masking exit 1 as pass | mitigate | BRAND_EXIT=$? captured explicitly; assert "$BRAND_EXIT" -eq 1 — if exit code is 0 (unexpectedly passing) the assert fails loudly; the intent is documented |
| T-96-16 | Repudiation | Phase docs updated without full SC verification | mitigate | Task 1 (assertions) must complete before Task 2 (docs update); wave 3 depends on waves 1+2; if any assertion fails, stop — do not proceed to STATE/ROADMAP update |
| T-96-17 | Tampering | ROADMAP.md Phase 96 plans list left as Phase 95 placeholder | mitigate | Task 2 action explicitly overwrites the placeholder plan list (which erroneously copied Phase 95 plan names) with the actual 96-0{1..4}-PLAN.md entries |
</threat_model>

<verification>
All assertions in Task 1 must pass before this plan is considered complete:

Phase-gate summary (all must be TRUE):
1. check_brand_tokens.py exits 1 → PASS (intentional — Phase 98 makes it 0)
2. check_synced_pair.py exits 0 → PASS (Block 2≡3 invariant undisturbed)
3. tokens.json admin_css_mapping.light has 37 tokens including #3A6F8F for --rs-primary → PASS
4. tokens.css light block has --rs-primary: #3A6F8F, no color in :root, Tailwind excerpt present → PASS
5. lint.sh has check_brand_tokens.py + check_synced_pair.py + SVG SIZE BUDGET OK → PASS
6. brandbook/brand-book.md exists; old hex #B96A3A absent; #9b5931 present ≥2 → PASS
7. prompts/rulestead-brand-book.md pointer stub < 20 lines → PASS
8. brandbook/docs/brand-usage.md and brandbook/README.md exist → PASS
</verification>

<success_criteria>
- All SC-1 through SC-5 assertions pass without exception
- check_brand_tokens.py exits 1 (the intended pre-Phase-98 state) — this is the phase gate-green signal
- check_synced_pair.py exits 0 — rulestead_admin.css synced-pair invariant intact
- STATE.md updated: current focus = Phase 97, Phase 96 decisions recorded
- ROADMAP.md updated: Phase 96 [x] complete, 4/4 plans, correct plan list with wave structure
- No regressions: python3 scripts/check_synced_pair.py and python3 scripts/check_contrast.py (from Phase 95) still exit 0
</success_criteria>

<output>
Create .planning/phases/96-design-tokens-brandbook-scaffold/96-04-SUMMARY.md when done.
</output>
