---
phase: 96-design-tokens-brandbook-scaffold
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/brand-book.md
  - brandbook/README.md
  - brandbook/docs/brand-usage.md
  - prompts/rulestead-brand-book.md
autonomous: true
requirements:
  - TOK-01
  - TOK-02

must_haves:
  truths:
    - "brandbook/brand-book.md exists as the canonical brand book (relocated from prompts/ via git mv)"
    - "git log --follow brandbook/brand-book.md shows ≥2 commits (history preserved)"
    - "brandbook/brand-book.md §12 has AA-verified canonical hexes (no book-literal failures)"
    - "brandbook/brand-book.md §12 has Gap-2 per-surface note for Success and Danger on Stone Mist"
    - "prompts/rulestead-brand-book.md is a pointer stub under 10 lines"
    - "brandbook/README.md exists as a directory index cross-linking the other files"
    - "brandbook/docs/brand-usage.md exists with check script instructions and synced-pair rule"
  artifacts:
    - path: "brandbook/brand-book.md"
      provides: "Canonical Rulestead brand book — identity, palette, typography, voice"
      contains: "#9b5931"
    - path: "brandbook/README.md"
      provides: "brandbook/ directory index"
      contains: "tokens.json"
    - path: "brandbook/docs/brand-usage.md"
      provides: "Re-skin guide and check_brand_tokens.py usage notes"
      contains: "check_brand_tokens.py"
    - path: "prompts/rulestead-brand-book.md"
      provides: "Pointer stub — redirects to brandbook/brand-book.md"
      contains: "brandbook/brand-book.md"
  key_links:
    - from: "prompts/rulestead-brand-book.md"
      to: "brandbook/brand-book.md"
      via: "pointer comment + markdown link"
      pattern: "brandbook/brand-book.md"
    - from: "brandbook/docs/brand-usage.md"
      to: "scripts/check_brand_tokens.py"
      via: "usage instructions referencing the script path"
      pattern: "check_brand_tokens.py"
---

<objective>
Commit the working-tree edit to prompts/rulestead-brand-book.md, relocate it to brandbook/brand-book.md via git mv (preserving history), apply §12 hex reconciliation to the AA-verified canonicals, add Gap-2 per-surface notes, write a pointer stub in prompts/, and author the brandbook/README.md directory index and brandbook/docs/brand-usage.md usage doc.

Purpose: Establish the canonical brand book location and ensure all color references in §12 reflect the D-11 signed-off AA-verified palette. Scaffold the minimal supporting docs (REPO-01) that later phases (98, 100) build on.

Output: brandbook/brand-book.md (relocated + §12 reconciled), prompts/rulestead-brand-book.md (pointer stub), brandbook/README.md, brandbook/docs/brand-usage.md.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-design-tokens-brandbook-scaffold/96-CONTEXT.md
@.planning/phases/96-design-tokens-brandbook-scaffold/96-RESEARCH.md
@.planning/phases/96-design-tokens-brandbook-scaffold/96-PATTERNS.md
@.planning/phases/95-brand-audit-palette-reconciliation/95-PALETTE-RECONCILIATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit working-tree edit, git mv brand-book to brandbook/, apply §12 hex rework, write pointer stub</name>
  <files>brandbook/brand-book.md, prompts/rulestead-brand-book.md</files>
  <read_first>
    - prompts/rulestead-brand-book.md — read the FULL current working-tree content (M in git status); this is the source for git mv
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-CONTEXT.md D-11, D-12 — relocation sequence and §12 rework rules
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-RESEARCH.md §"brand-book.md Relocation + §12 Rework" + §"§12 Hex Reconciliation Table" — exact step-by-step and substitution table
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-PATTERNS.md §"brandbook/brand-book.md" + §"prompts/rulestead-brand-book.md" — exact pointer stub pattern + Gap-2 note format
    - .planning/phases/95-brand-audit-palette-reconciliation/95-PALETTE-RECONCILIATION.md §4, §8 — confirm all 5 replacement hexes + Gap-2 canonicals
  </read_first>
  <action>
    STEP 1 — Commit working-tree edit to prompts/rulestead-brand-book.md (it is M/modified, not staged):
      Run: git add prompts/rulestead-brand-book.md
      Run: git commit -m "chore: commit full brand-book content before Phase 96 relocation"
      This creates a clean, auditable commit that preserves the full 27-section brand book content at its old path before the move.

    STEP 2 — git mv to brandbook/brand-book.md (creates brandbook/ directory if not already present):
      Ensure brandbook/ directory exists (mkdir -p brandbook/docs).
      Run: git mv prompts/rulestead-brand-book.md brandbook/brand-book.md
      Do NOT directly copy + delete — git mv preserves the rename in git history so `git log --follow brandbook/brand-book.md` shows the full history.

    STEP 3 — Apply §12 hex replacements in brandbook/brand-book.md (D-12):
      These are EXACT find-and-replace substitutions — do not change any hex NOT in this list:
        #B96A3A → #9b5931   (Ember Copper; occurs in #### Ember Copper entry AND ### Gradients section — replace ALL occurrences)
        #B57A21 → #8f601a   (Warning)
        #6C7A73 → #606d66   (Moss Grey)
        #2F7D57 → #2d7753   (Success — replace in §12 entry only; then add Gap-2 note)
        #B44949 → #b04848   (Danger — replace in §12 entry only; then add Gap-2 note)

      After the Success entry in §12, insert a blockquote Gap-2 note:
        > **Gap 2 (Stone Mist surfaces):** Use `#2d7753` on Stone Mist (`#E8ECE8`) only. Use `#2F7D57` on White (`#FFFFFF`) or Rain Tint (`#F5F7F6`) surfaces.

      After the Danger entry in §12, insert a blockquote Gap-2 note:
        > **Gap 2 (Stone Mist surfaces):** Use `#b04848` on Stone Mist (`#E8ECE8`) only. Use `#B44949` on White (`#FFFFFF`) or Rain Tint (`#F5F7F6`) surfaces.

      DO NOT change §8 tagline — it already reads "Runtime decisions, made clear." — leave untouched.
      DO NOT change these hexes (already correct or decorative): #0F1720, #24313D, #E8ECE8, #C4CCD1, #3A6F8F, #F5F7F6, #183247, #D2A94E, #356E8C.

    STEP 4 — Write pointer stub to prompts/rulestead-brand-book.md:
      Replace the (now-moved) file with a minimal pointer. The file should be under 10 lines:
        <!-- Moved to `brandbook/brand-book.md` — canonical as of Phase 96. -->
        <!-- This file is a pointer only. Do not edit here. -->

        See [brandbook/brand-book.md](../brandbook/brand-book.md) for the canonical Rulestead Brand Book.

    STEP 5 — Stage and commit the rework batch:
      git add brandbook/brand-book.md prompts/rulestead-brand-book.md
      git commit -m "chore(96): relocate brand-book, reconcile §12 hexes to AA-verified canonicals (D-11/D-12)"
  </action>
  <acceptance_criteria>
    - `test -f brandbook/brand-book.md && echo ok` → ok
    - `grep -c '#9b5931' brandbook/brand-book.md` → ≥ 2 (Ember Copper in entry + Gradients)
    - `grep -c '#B96A3A' brandbook/brand-book.md` → 0 (old hex fully replaced)
    - `grep -c '#8f601a' brandbook/brand-book.md` → ≥ 1 (Warning replaced)
    - `grep -c '#606d66' brandbook/brand-book.md` → ≥ 1 (Moss Grey replaced)
    - `grep -c '#2d7753' brandbook/brand-book.md` → ≥ 1 (Success replaced)
    - `grep -c '#b04848' brandbook/brand-book.md` → ≥ 1 (Danger on SM)
    - `grep -c 'Gap 2' brandbook/brand-book.md` → ≥ 2 (both Success and Danger Gap-2 notes added)
    - `grep -c 'Runtime decisions, made clear' brandbook/brand-book.md` → ≥ 1 (§8 tagline unchanged)
    - `wc -l < prompts/rulestead-brand-book.md` → < 10
    - `grep -c 'brandbook/brand-book.md' prompts/rulestead-brand-book.md` → ≥ 1 (pointer present)
    - `git log --follow brandbook/brand-book.md | grep -c commit` → ≥ 2 (history preserved via git mv)
  </acceptance_criteria>
  <verify>
    <automated>test -f brandbook/brand-book.md && grep -c '#9b5931' brandbook/brand-book.md && grep -c '#B96A3A' brandbook/brand-book.md | python3 -c "import sys; lines=sys.stdin.readlines(); print('old hex still present' if lines[-1].strip()!='0' else 'hex replacement OK')"</automated>
    <automated>python3 -c "
import subprocess
# Verify all 5 replacements
book = open('brandbook/brand-book.md').read()
old = ['#B96A3A', '#B57A21', '#6C7A73']
new = ['#9b5931', '#8f601a', '#606d66']
for o in old:
    assert o not in book, f'old hex still present: {o}'
    print(f'{o} → replaced OK')
assert '#2d7753' in book, 'Success replacement missing'
assert '#b04848' in book, 'Danger replacement missing'
assert 'Gap 2' in book, 'Gap-2 note missing'
assert 'Runtime decisions, made clear' in book, 'tagline changed — must not be'
print('All §12 hex replacements verified')
"</automated>
  </verify>
  <done>brandbook/brand-book.md exists with §12 hexes reconciled to AA canonicals; all 5 old hexes absent; Gap-2 notes present for Success and Danger; prompts/rulestead-brand-book.md is a pointer stub under 10 lines; git log --follow shows ≥2 commits.</done>
</task>

<task type="auto">
  <name>Task 2: Author brandbook/README.md directory index and brandbook/docs/brand-usage.md</name>
  <files>brandbook/README.md, brandbook/docs/brand-usage.md</files>
  <read_first>
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-CONTEXT.md D-09 — what README and brand-usage.md must contain
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-PATTERNS.md §"brandbook/README.md" + §"brandbook/docs/brand-usage.md" — pattern, section structure, content requirements
    - .planning/phases/96-design-tokens-brandbook-scaffold/96-RESEARCH.md §"Common Pitfalls" Pitfall 1 (synced-pair) + Pitfall 2 (drift check failing is deliverable) — these are the critical content for brand-usage.md
    - guides/introduction/product-boundary.md — markdown structure analog (H2 sections, tables, callouts)
  </read_first>
  <action>
    FILE: brandbook/README.md (keep under 30 lines — stub to be expanded in Phase 100):
      H1: "brandbook/"
      Short paragraph: "Directory index for the Rulestead brand system (v1.14+)."
      Table with three columns (File | Purpose | Status):
        brand-book.md | Canonical brand book — identity, palette, typography, voice | Active
        tokens.json | Machine-readable design tokens (DTCG 2025.10) | Active
        tokens.css | CSS custom-property reference mirror (--rs-* shape) | Active
        docs/brand-usage.md | Re-skin guide + check_brand_tokens.py usage | Active
      Note about Phase 97/99/100 additions (assets/, VOICE.md, RELEASE-TEMPLATE.md, BUDGET.md — coming in later phases).
      Cross-link: "Admin cascade: rulestead_admin/priv/static/css/rulestead_admin.css"
      This file will be replaced with a full directory index in Phase 100.

    FILE: brandbook/docs/brand-usage.md (REPO-01; minimal-but-real):
      H1: "Brand Token Usage"

      H2: "Check script"
      Explain that python3 scripts/check_brand_tokens.py reads brandbook/tokens.json admin_css_mapping.light and compares each --rs-* value against rulestead_admin.css Block 1.
      Show usage: "python3 scripts/check_brand_tokens.py" (run from repo root).
      Show expected Phase-96 output (exit 1, BRAND TOKEN DRIFT DETECTED + 7+ token diffs).
      Show expected Phase-98 output (exit 0, BRAND TOKENS SYNCED (37 tokens)).

      H2: "Intentional CI failure (Phase 96 → Phase 98)"
      Explain clearly: the token drift check INTENTIONALLY exits 1 against the current admin CSS. This is by design — it proves the guard mechanism works. Phase 98 re-skins rulestead_admin.css to make it pass. DO NOT fix the failure by removing the check or editing the CSS in Phase 96 or 97.

      H2: "Synced-pair rule"
      Explain: rulestead_admin.css has four cascade blocks. Blocks 1 and 4 are a SYNCED PAIR (light values; must be byte-identical). Blocks 2 and 3 are a SYNCED PAIR (dark values; must be byte-identical).
      When editing a token value in one block of a pair, you MUST update the partner block to the same value before committing.
      Run `python3 scripts/check_synced_pair.py` to verify the dark pair (Blocks 2≡3).
      Phase 98 must update both Block 1 and Block 4 for light changes, and both Block 2 and Block 3 for dark changes.

      H2: "Adding a new token"
      Briefly: Decide invariant vs variant. Invariant tokens (font, spacing, radius, etc.) go in the :root block of both rulestead_admin.css and tokens.css. Variant (color) tokens go in all four cascade blocks of rulestead_admin.css and the corresponding light/dark block of tokens.css. Add to tokens.json. If it is a hex-literal token, add to admin_css_mapping.

      H2: "Admin CSS cross-reference"
      Link: rulestead_admin/priv/static/css/rulestead_admin.css — the four cascade blocks and the invariant :root block. Block 1 (light) is the check_brand_tokens.py comparison target.
  </action>
  <acceptance_criteria>
    - `test -f brandbook/README.md && echo ok` → ok
    - `test -f brandbook/docs/brand-usage.md && echo ok` → ok
    - `grep -c 'tokens.json' brandbook/README.md` → ≥ 1
    - `grep -c 'check_brand_tokens.py' brandbook/docs/brand-usage.md` → ≥ 2 (appears in multiple sections)
    - `grep -c 'SYNCED PAIR' brandbook/docs/brand-usage.md` → ≥ 1 (synced-pair rule documented)
    - `grep -c 'Intentional' brandbook/docs/brand-usage.md` → ≥ 1 (intentional failure documented)
    - `grep -c 'Phase 98' brandbook/docs/brand-usage.md` → ≥ 2 (Phase 98 context present)
    - `wc -l < brandbook/README.md` → < 35 (stub, not exhaustive)
  </acceptance_criteria>
  <verify>
    <automated>test -f brandbook/README.md && test -f brandbook/docs/brand-usage.md && echo "docs exist"</automated>
    <automated>python3 -c "
readme = open('brandbook/README.md').read()
usage = open('brandbook/docs/brand-usage.md').read()
assert 'tokens.json' in readme, 'README missing tokens.json reference'
assert 'check_brand_tokens.py' in usage, 'brand-usage missing check script reference'
assert 'SYNCED PAIR' in usage or 'synced pair' in usage.lower(), 'synced-pair rule missing'
assert 'intentional' in usage.lower(), 'intentional failure note missing'
assert 'Phase 98' in usage, 'Phase 98 context missing'
print('docs content OK')
"</automated>
  </verify>
  <done>brandbook/README.md exists as a directory index table (under 35 lines) cross-linking brand-book.md, tokens.json, tokens.css, docs/brand-usage.md. brandbook/docs/brand-usage.md exists with check script usage, intentional-CI-failure note, synced-pair rule, and new-token guide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git working tree → committed history | git mv preserves rename history; pointer stub prevents dangling references |
| brand-book.md §12 hex content → downstream phases | Hex values are locked by D-11 sign-off; this task encodes them, not re-derives them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-06 | Tampering | §12 hex replacements | mitigate | Verify all 5 old hexes absent AND all 5 new canonicals present; acceptance_criteria includes grep assertions for both old (count=0) and new (count≥1) |
| T-96-07 | Tampering | §8 tagline | mitigate | Acceptance criteria includes grep for "Runtime decisions, made clear." — assert count ≥1 to confirm no accidental edit |
| T-96-08 | Repudiation | git mv history | mitigate | git log --follow assertion (≥2 commits) verifies history preserved; two-step sequence (commit first, then git mv) is explicit in action |
| T-96-09 | Tampering | binary blob committed to brandbook/ | mitigate | brand-book.md is plain markdown; README.md and brand-usage.md are plain markdown; no binary content; repo binary policy: only favicon.ico/png + apple-touch-icon.png allowed |
</threat_model>

<verification>
After both tasks complete:

1. Brand book relocated: `test -f brandbook/brand-book.md && echo ok`
2. History preserved: `git log --follow brandbook/brand-book.md | grep -c "^commit"`  → ≥ 2
3. Old hex absent: `grep -c '#B96A3A' brandbook/brand-book.md` → 0
4. New canonical present: `grep -c '#9b5931' brandbook/brand-book.md` → ≥ 2
5. Gap-2 notes: `grep -c 'Gap 2' brandbook/brand-book.md` → ≥ 2
6. §8 tagline: `grep -c 'Runtime decisions, made clear' brandbook/brand-book.md` → ≥ 1
7. Pointer stub: `wc -l < prompts/rulestead-brand-book.md` → < 10
8. Pointer references new path: `grep -c 'brandbook/brand-book.md' prompts/rulestead-brand-book.md` → ≥ 1
9. Docs exist: `test -f brandbook/README.md && test -f brandbook/docs/brand-usage.md && echo ok`
10. check_synced_pair.py still green: `python3 scripts/check_synced_pair.py` (rulestead_admin.css untouched)
</verification>

<success_criteria>
- brandbook/brand-book.md is the canonical brand book (relocated from prompts/ via git mv, history preserved)
- §12 hexes reconciled: old #B96A3A/#B57A21/#6C7A73/#2F7D57/#B44949 replaced with AA-verified canonicals; Gap-2 notes added
- prompts/rulestead-brand-book.md is a pointer stub under 10 lines referencing brandbook/brand-book.md
- brandbook/README.md is a directory index cross-linking all brandbook/ files and the admin CSS
- brandbook/docs/brand-usage.md covers: check script usage + expected outputs for Phase 96 (exit 1) and Phase 98 (exit 0); synced-pair rule with Block 1≡4 / 2≡3 explanation; intentional CI failure note
- python3 scripts/check_synced_pair.py exits 0 (rulestead_admin.css not touched)
</success_criteria>

<output>
Create .planning/phases/96-design-tokens-brandbook-scaffold/96-02-SUMMARY.md when done.
</output>
