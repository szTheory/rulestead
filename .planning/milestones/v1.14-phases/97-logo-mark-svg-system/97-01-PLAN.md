---
phase: 97-logo-mark-svg-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/assets/logo/concepts/rs-mark-concept-a.svg
  - brandbook/assets/logo/concepts/rs-mark-concept-b.svg
  - brandbook/assets/logo/concepts/rs-mark-concept-c.svg
  - brandbook/assets/logo/concepts/svgo.config.mjs
  - .planning/phases/97-logo-mark-svg-system/97-CONCEPT-REVIEW.md
autonomous: true
requirements: [LOGO-01]
user_setup: []

must_haves:
  truths:
    - "Three distinct mark concepts (A structured path / B stead frame / C layered field) exist as SVG files"
    - "Each concept uses only locked mineral palette hexes (no Signal Gold as fill, no phoenix/flame/shield/bolt/hexagon)"
    - "A maintainer-facing review doc presents all three concepts for A/B/C selection"
  artifacts:
    - path: "brandbook/assets/logo/concepts/rs-mark-concept-a.svg"
      provides: "Concept A — structured path mark (ordered/stepped lines)"
      contains: "<title"
    - path: "brandbook/assets/logo/concepts/rs-mark-concept-b.svg"
      provides: "Concept B — stead frame (architectural enclosure)"
      contains: "<title"
    - path: "brandbook/assets/logo/concepts/rs-mark-concept-c.svg"
      provides: "Concept C — layered field (layered contour/grid)"
      contains: "<title"
    - path: ".planning/phases/97-logo-mark-svg-system/97-CONCEPT-REVIEW.md"
      provides: "Maintainer selection doc presenting A/B/C"
      contains: "Concept"
  key_links:
    - from: "brandbook/assets/logo/concepts/*.svg"
      to: "brandbook/tokens.json primitives"
      via: "fill hex values copied from primitives"
      pattern: "#3A6F8F|#183247|#24313D"
---

<objective>
Author three SVG mark concepts (A structured path / B stead frame / C layered field) per brand book §14, each as an accessible, SVGO-clean SVG using only locked mineral palette hexes, and present them in a maintainer-facing `97-CONCEPT-REVIEW.md` for the A/B/C selection gate.

Purpose: LOGO-01 requires three concepts produced for maintainer selection. The selection (next plan) cannot be automated — this plan front-loads all the creative authoring so the gate is a pure decision.
Output: 3 concept SVGs + an SVGO config scoped to the concepts dir + a concept-review doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-logo-mark-svg-system/97-RESEARCH.md
@.planning/phases/97-logo-mark-svg-system/97-VALIDATION.md
@brandbook/brand-book.md
@brandbook/tokens.json

<interfaces>
<!-- Exact fill hexes from brandbook/tokens.json primitives. Use these directly; do not invent colors. -->
Light mark fill (primary):   #3A6F8F  (primitive.stead-blue.base)
Ink contrast / structure:    #183247  (primitive.ink-blue.base)
Structural secondary:        #24313D  (primitive.slate-stead.base)
Mid-tone neutral accent:     #C4CCD1  (primitive.quarry.base)
Dark-mode mark fill:         #5885a0  (primitive.stead-blue.dark)
Dark-mode light text:        #e8edf3  (primitive.neutral-ramp.dark-900)
FORBIDDEN as fill: #D2A94E (Signal Gold — decorative-only, fails all light surfaces, PAL-04)

Accessible SVG skeleton (RESEARCH.md Pattern 1):
  <svg xmlns role="img" aria-labelledby="<title-id> <desc-id>" viewBox="0 0 N N">
    <title id="...">...</title><desc id="...">...</desc>
    <g aria-hidden="true"> ...paths... </g>
  </svg>

SVGO config (RESEARCH.md Pattern 2 / Code Examples): preset-default with overrides
  removeTitle:false, removeDesc:false, removeViewBox:false, cleanupIds:false
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author three mark concept SVGs (A/B/C)</name>
  <files>brandbook/assets/logo/concepts/rs-mark-concept-a.svg, brandbook/assets/logo/concepts/rs-mark-concept-b.svg, brandbook/assets/logo/concepts/rs-mark-concept-c.svg, brandbook/assets/logo/concepts/svgo.config.mjs</files>
  <read_first>
    - brandbook/brand-book.md §14 (Logo direction — the A/B/C definitions, logo constraints, wordmark character)
    - .planning/phases/97-logo-mark-svg-system/97-RESEARCH.md (Pattern 1 accessible skeleton, Pattern 2 SVGO config, Anti-Patterns)
    - brandbook/tokens.json (exact fill hexes — primitives section)
  </read_first>
  <action>
    Create `brandbook/assets/logo/concepts/` and hand-author three square icon-only mark SVGs at viewBox="0 0 64 64" (mark territory only — no wordmark text in concepts). Each per brand book §14:
    - rs-mark-concept-a.svg — Structured path mark: ordered/stepped geometric lines suggesting rule evaluation + decision flow + controlled progression. Fill #3A6F8F with #183247 for the structural emphasis path.
    - rs-mark-concept-b.svg — Stead frame: a calm rectangular/architectural enclosure motif suggesting stable operating ground + governed boundaries. Fill #3A6F8F frame with #24313D inner element.
    - rs-mark-concept-c.svg — Layered field: layered contour/grid motif suggesting environments + rule layers + topology of change. Fill #3A6F8F primary layers with #C4CCD1 receding layer.
    Each SVG MUST: use the accessible skeleton (role="img", aria-labelledby pointing to <title id> + <desc id>, paths wrapped in <g aria-hidden="true">); use ONLY hexes from the interfaces block; honor §14 constraints (NO phoenix/flame, NO shield, NO lightning bolt, NO generic cube/hexagon, NO literal flag). Use fills only (no strokes) so concepts read at small sizes. Keep each file ≤20480 bytes (lint budget).
    Also write `brandbook/assets/logo/concepts/svgo.config.mjs` exactly as RESEARCH.md Pattern 2 specifies (preset-default + overrides removeTitle:false, removeDesc:false, removeViewBox:false, cleanupIds:false; multipass:true). Then run SVGO over the three concept files:
    `npx svgo --config brandbook/assets/logo/concepts/svgo.config.mjs brandbook/assets/logo/concepts/rs-mark-concept-a.svg brandbook/assets/logo/concepts/rs-mark-concept-b.svg brandbook/assets/logo/concepts/rs-mark-concept-c.svg`
    After SVGO, confirm each file still carries its <title>. Do NOT install any npm/hex package; `npx svgo` runs ad-hoc (no package.json).
  </action>
  <acceptance_criteria>
    - `ls brandbook/assets/logo/concepts/rs-mark-concept-a.svg brandbook/assets/logo/concepts/rs-mark-concept-b.svg brandbook/assets/logo/concepts/rs-mark-concept-c.svg | wc -l` returns 3
    - `for f in brandbook/assets/logo/concepts/rs-mark-concept-*.svg; do grep -q '<title' "$f" || echo "MISSING TITLE: $f"; done` prints nothing
    - `grep -c '<text' brandbook/assets/logo/concepts/rs-mark-concept-*.svg` returns 0 for every file
    - `grep -c 'base64' brandbook/assets/logo/concepts/rs-mark-concept-*.svg` returns 0 for every file
    - `grep -l 'D2A94E' brandbook/assets/logo/concepts/rs-mark-concept-*.svg` prints nothing (no Signal Gold fill)
    - `for f in brandbook/assets/logo/concepts/rs-mark-concept-*.svg; do [ "$(wc -c < "$f")" -le 20480 ] || echo "OVER BUDGET: $f"; done` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>ls brandbook/assets/logo/concepts/rs-mark-concept-{a,b,c}.svg | wc -l | grep -q '^[[:space:]]*3$' && echo OK</automated>
  </verify>
  <done>Three concept SVGs exist, each accessible (title/desc/role=img), text-free, raster-free, within budget, using only mineral palette hexes.</done>
</task>

<task type="auto">
  <name>Task 2: Write 97-CONCEPT-REVIEW.md for the A/B/C selection gate</name>
  <files>.planning/phases/97-logo-mark-svg-system/97-CONCEPT-REVIEW.md</files>
  <read_first>
    - .planning/phases/97-logo-mark-svg-system/97-RESEARCH.md (Wave / Human Checkpoint Structure)
    - brandbook/brand-book.md §14 (so the review doc maps each concept back to its stated metaphor)
    - the three SVGs authored in Task 1
  </read_first>
  <action>
    Write `97-CONCEPT-REVIEW.md` presenting all three concepts for maintainer selection. For each concept include: its letter (A/B/C), its brand-book §14 name (Structured path / Stead frame / Layered field), the design rationale (what metaphor it encodes), the exact relative file path to its SVG, and an inline `<img src="...">` reference (relative path) so the maintainer can render it. State explicitly that the maintainer must reply with a single letter (A, B, or C) and that the next plan (97-02) is gated on this selection. Note which file replaces the demo logo downstream (rs-mark.svg, icon-only, per RESEARCH.md Open Question 1) so the maintainer understands the mark must read at 36px. Do NOT modify any SVG in this task.
  </action>
  <acceptance_criteria>
    - `ls .planning/phases/97-logo-mark-svg-system/97-CONCEPT-REVIEW.md` succeeds
    - `grep -c -i 'concept' .planning/phases/97-logo-mark-svg-system/97-CONCEPT-REVIEW.md` returns > 0
    - `grep -o 'rs-mark-concept-[abc].svg' .planning/phases/97-logo-mark-svg-system/97-CONCEPT-REVIEW.md | sort -u | wc -l` returns 3 (all three concept files referenced)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/97-logo-mark-svg-system/97-CONCEPT-REVIEW.md && grep -o 'rs-mark-concept-[abc].svg' .planning/phases/97-logo-mark-svg-system/97-CONCEPT-REVIEW.md | sort -u | wc -l | grep -q '^[[:space:]]*3$' && echo OK</automated>
  </verify>
  <done>Review doc presents A/B/C with rationale, file paths, and inline renders; instructs maintainer to select one letter; references all three concept SVGs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → committed SVG | Hand-authored SVG files enter the repo as brand source. No user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering / Elevation | concept SVGs containing `<script>` | mitigate | SVGO preset-default `removeScripts` strips any script; verify `grep -c '<script' brandbook/assets/logo/concepts/*.svg` = 0 post-SVGO. Files are served via `<img>` downstream (no inline script execution). |
| T-97-02 | Information Disclosure | external `http`/`href` refs in SVG | mitigate | Concepts use fills only, no external refs; verify `grep -c 'http' brandbook/assets/logo/concepts/*.svg` = 0. |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installed — `npx svgo` runs ad-hoc against the already-resolved svgo binary (npm, 12-year track record, RESEARCH.md Package Legitimacy Audit approved). No new dependency committed. |
</threat_model>

<verification>
- `ls brandbook/assets/logo/concepts/rs-mark-concept-{a,b,c}.svg | wc -l` = 3
- `grep -c '<text' brandbook/assets/logo/concepts/*.svg` all 0
- `grep -c 'base64' brandbook/assets/logo/concepts/*.svg` all 0
- `grep -c '<script' brandbook/assets/logo/concepts/*.svg` all 0
- `97-CONCEPT-REVIEW.md` references all three concept files
</verification>

<success_criteria>
Three mineral-palette, accessible, text-free, raster-free concept SVGs (A/B/C) exist in `brandbook/assets/logo/concepts/`, each within the 20480-byte logo budget, presented in `97-CONCEPT-REVIEW.md` for maintainer selection.
</success_criteria>

<output>
Create `.planning/phases/97-logo-mark-svg-system/97-01-SUMMARY.md` when done.
</output>
