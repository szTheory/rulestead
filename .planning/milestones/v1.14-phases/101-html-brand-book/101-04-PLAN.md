---
phase: 101
plan: "04"
type: execute
wave: 4
depends_on:
  - "101-03"
files_modified:
  - examples/demo/frontend/tests/brandbook.spec.ts
  - brandbook/README.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02
must_haves:
  truths:
    - "D-14: Phase 101 closes v1.14 only after generator, drift, budget, lint, and browser/static verification pass."
    - "D-15: closeout keeps the linked-version, two-package release shape unchanged and does not publish or prepare to publish rulestead_admin."
    - "Browser evidence reuses the existing examples/demo/frontend Playwright stack with a file:// spec and is not wired into scripts/ci/lint.sh."
    - "brandbook/README.md links the generated index.html and documents generator/checker commands."
    - "BOOK-01 and BOOK-02 are marked complete only after the full guard suite and browser/static checks pass."
  artifacts:
    - path: "examples/demo/frontend/tests/brandbook.spec.ts"
      provides: "Targeted file:// browser evidence for required sections, theme control, inline assets, and JavaScript-disabled baseline."
    - path: "brandbook/README.md"
      provides: "Directory index and maintenance commands for generated HTML brand book."
    - path: ".planning/PROJECT.md"
      provides: "v1.14 shipped milestone status after verification."
    - path: ".planning/STATE.md"
      provides: "Phase 101 complete and v1.14 shipped state after verification."
    - path: ".planning/ROADMAP.md"
      provides: "Phase 101 complete, four plans listed, progress table updated, v1.14 shipped."
    - path: ".planning/REQUIREMENTS.md"
      provides: "BOOK-01 and BOOK-02 checked complete and traceability rows added."
  key_links:
    - from: "examples/demo/frontend/tests/brandbook.spec.ts"
      to: "brandbook/index.html"
      via: "file:// browser checks against generated static artifact"
    - from: "brandbook/README.md"
      to: "scripts/gen_brandbook_html.py and scripts/check_brandbook_html.py"
      via: "maintenance commands"
    - from: ".planning/PROJECT.md and .planning/STATE.md"
      to: "101-VALIDATION.md"
      via: "closeout only after full guard and browser/static evidence"
---

<objective>
Add browser evidence, update the brandbook directory index, run the final verification gate,
and close Phase 101 plus milestone v1.14 in planning docs.

This wave is the capstone closeout. It must not start closeout edits until the generated
HTML, generator, checker, CI wiring, budget, lint, and browser/static verification are
green.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/rulestead/.planning/PROJECT.md
@/Users/jon/projects/rulestead/.planning/STATE.md
@/Users/jon/projects/rulestead/.planning/ROADMAP.md
@/Users/jon/projects/rulestead/.planning/REQUIREMENTS.md
@/Users/jon/projects/rulestead/.planning/phases/101-html-brand-book/101-CONTEXT.md
@/Users/jon/projects/rulestead/.planning/phases/101-html-brand-book/101-UI-SPEC.md
@/Users/jon/projects/rulestead/.planning/phases/101-html-brand-book/101-RESEARCH.md
@/Users/jon/projects/rulestead/.planning/phases/101-html-brand-book/101-VALIDATION.md
@/Users/jon/projects/rulestead/.planning/phases/101-html-brand-book/101-PATTERNS.md
@/Users/jon/projects/rulestead/examples/demo/frontend/tests/theme-control.spec.ts
@/Users/jon/projects/rulestead/examples/demo/frontend/tests/design-system.spec.ts
</context>

<threat_model>
ASVS level 1 posture is sufficient because this wave adds local browser verification and
planning/docs updates after static checks pass.

Threats and mitigations:
- Source-truth drift: closeout could happen even if `index.html` is stale. Mitigate by making `python3 scripts/check_brandbook_html.py` and `bash scripts/ci/lint.sh` blocking before planning doc edits.
- Unsafe HTML/SVG embedding: browser spec cannot replace static safety checks. Mitigate by requiring `check_brandbook_html.py` static assertions to pass before and after README/closeout edits.
- External asset dependency: browser evidence could accidentally rely on a dev server or remote previews. Mitigate by using `file://<repo>/brandbook/index.html` and inline SVG assertions; no server required.
- Broken no-JS/browser rendering: mitigate with Playwright JavaScript-disabled context that verifies content, nav, and inline SVG visibility.
- Premature milestone closeout: mitigate with an explicit task order: browser/static evidence and full guard suite first, then `.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md`.
- Publication/posture drift: closeout could imply package publishing or change `rulestead_admin` posture. Mitigate by editing only listed docs, no `mix.exs`, no release workflow, no Hex publish prep, no branch changes.
- High severity block condition: any failed guard, failed browser evidence, missing BOOK traceability, Phase 8-only doc creation, or package publication edit blocks milestone closeout.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add narrow Playwright file:// brandbook evidence spec</name>
  <files>examples/demo/frontend/tests/brandbook.spec.ts</files>
  <read_first>
    - examples/demo/frontend/package.json - `test:e2e` command
    - examples/demo/frontend/tests/theme-control.spec.ts - file:// URL, theme-control, localStorage, and keyboard patterns
    - examples/demo/frontend/tests/design-system.spec.ts - file:// fixture load pattern
    - brandbook/index.html - generated artifact under test
    - .planning/phases/101-html-brand-book/101-RESEARCH.md - Browser and Static Verification recommendations
    - .planning/phases/101-html-brand-book/101-PATTERNS.md - optional `brandbook.spec.ts` pattern
  </read_first>
  <action>
    Add `examples/demo/frontend/tests/brandbook.spec.ts` using the existing Playwright stack.

    Concrete spec behavior:
    - Build `brandbookUrl` as `file://` plus `path.resolve(__dirname, "../../../../brandbook/index.html")`.
    - Test desktop load at a wide viewport: assert `[data-rulestead-brandbook]`, `header`, `nav`, `main`, `#overview`, `#voice-and-messaging`, `#color`, `#typography`, `#logo`, `#layout-and-components`, `#iconography-and-imagery`, `#motion`, and `#assets-and-maintenance` are visible.
    - Test mobile load at a narrow viewport: assert the wrapper, nav, `#overview`, `#color`, and `#assets-and-maintenance` are visible and no horizontal layout failure is obvious from Playwright visibility checks.
    - Test theme control: click `[data-value='dark']` and expect wrapper `data-theme="dark"` plus dark option `aria-checked="true"`; click `[data-value='light']` and expect wrapper `data-theme="light"`; click `[data-value='system']` and expect the wrapper not to have `data-theme`.
    - Test JavaScript-disabled baseline with a new context `{ javaScriptEnabled: false, colorScheme: "dark" }`: load the file URL and assert wrapper, nav, `#color`, `#logo`, `#assets-and-maintenance`, and at least one inline `svg` are visible.
    - Test keyboard focus: focus a nav link and a theme button, then assert focus is on the expected element after keyboard navigation.
    - Assert no required preview uses `<img>` by expecting `page.locator("img")` count to be 0 or by asserting required asset preview figures contain inline `svg`.
    - Do not add this spec to `scripts/ci/lint.sh`.
  </action>
  <verify>
    <automated>cd examples/demo/frontend &amp;&amp; npm run test:e2e -- brandbook.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `examples/demo/frontend/tests/brandbook.spec.ts` exists.
    - `rg -n "brandbook/index.html|data-rulestead-brandbook|javaScriptEnabled: false|data-value='dark'|data-value='system'" examples/demo/frontend/tests/brandbook.spec.ts` returns matches.
    - `cd examples/demo/frontend && npm run test:e2e -- brandbook.spec.ts` exits 0.
    - `rg -n "brandbook.spec.ts|test:e2e|playwright" scripts/ci/lint.sh` returns no matches.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update brandbook README for generated HTML artifact</name>
  <files>brandbook/README.md</files>
  <read_first>
    - brandbook/README.md - current directory index and Phase 101 placeholder paragraph
    - brandbook/index.html - generated artifact to link
    - scripts/gen_brandbook_html.py - generator command
    - scripts/check_brandbook_html.py - drift and budget checker command
    - brandbook/BUDGET.md - generated HTML budget policy
    - .planning/phases/101-html-brand-book/101-PATTERNS.md - README update recommendation
  </read_first>
  <action>
    Update `brandbook/README.md` after `brandbook/index.html` and `scripts/check_brandbook_html.py` exist.

    Concrete edits:
    - Add a directory-index row for `index.html` with purpose `Generated, source-controlled HTML brand book for browser review. Regenerate with scripts/gen_brandbook_html.py; guarded by scripts/check_brandbook_html.py.` and status `Active`.
    - Add `python3 scripts/gen_brandbook_html.py` and `python3 scripts/check_brandbook_html.py` to the Maintenance Commands block before `bash scripts/ci/lint.sh`.
    - Replace the current Phase 101 future-tense paragraph with present-tense source-of-truth guidance: `index.html` is generated from canonical sources and should not be edited by hand.
    - Link `BUDGET.md` as the generated HTML and SVG budget policy.
  </action>
  <verify>
    <automated>rg -n "index.html|gen_brandbook_html.py|check_brandbook_html.py|should not be edited by hand" brandbook/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\\[index.html\\]\\(index.html\\)" brandbook/README.md` returns one directory-index row.
    - `rg -n "python3 scripts/gen_brandbook_html.py" brandbook/README.md` returns a maintenance command.
    - `rg -n "python3 scripts/check_brandbook_html.py" brandbook/README.md` returns a maintenance command.
    - `rg -n "Phase 101 will add" brandbook/README.md` returns no matches.
    - README text states that `index.html` is generated and should not be edited by hand.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run final generator, drift, lint, and browser/static verification gate</name>
  <files>examples/demo/frontend/tests/brandbook.spec.ts brandbook/README.md</files>
  <read_first>
    - .planning/phases/101-html-brand-book/101-VALIDATION.md - full suite command and browser evidence requirements
    - scripts/gen_brandbook_html.py - generator
    - scripts/check_brandbook_html.py - generated HTML drift, budget, and static assertions
    - scripts/ci/lint.sh - full lint entry point
    - examples/demo/frontend/tests/brandbook.spec.ts - browser evidence spec
    - brandbook/README.md - generated artifact index
  </read_first>
  <action>
    Run the final verification gate from repo root before editing milestone closeout files.

    Commands:
    - `python3 scripts/gen_brandbook_html.py`
    - `python3 scripts/check_brandbook_html.py`
    - `python3 scripts/check_synced_pair.py`
    - `python3 scripts/check_brand_tokens.py`
    - `python3 scripts/check_tokens_css.py`
    - `bash scripts/ci/lint.sh`
    - `cd examples/demo/frontend && npm run test:e2e -- brandbook.spec.ts`

    Required success evidence to capture in the execution summary:
    - `BRANDBOOK HTML SYNCED (N bytes)`
    - `BRAND TOKENS SYNCED`
    - `TOKENS.CSS MIRROR SYNCED`
    - `SVG SIZE BUDGET OK`
    - Playwright reports the `brandbook.spec.ts` tests passed.

    If any command fails, stop before closeout edits and fix the failing generator/check/spec/doc issue within Phase 101 scope.
  </action>
  <verify>
    <automated>python3 scripts/gen_brandbook_html.py &amp;&amp; python3 scripts/check_brandbook_html.py &amp;&amp; python3 scripts/check_synced_pair.py &amp;&amp; python3 scripts/check_brand_tokens.py &amp;&amp; python3 scripts/check_tokens_css.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/check_brandbook_html.py` exits 0 and prints `BRANDBOOK HTML SYNCED`.
    - `bash scripts/ci/lint.sh` exits 0 and prints `SVG SIZE BUDGET OK`.
    - `cd examples/demo/frontend && npm run test:e2e -- brandbook.spec.ts` exits 0.
    - Verification happened after the final generator, checker, README, and browser spec edits.
    - No `.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, or `.planning/REQUIREMENTS.md` closeout edit is made until the above checks pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Close Phase 101 and mark v1.14 shipped in planning docs</name>
  <files>.planning/PROJECT.md .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/PROJECT.md - current v1.14 milestone text and shipped milestone sections
    - .planning/STATE.md - current focus, current position, accumulated decisions, and operator next steps
    - .planning/ROADMAP.md - Phase 101 entry, Phase Details, Plans, and Progress Table
    - .planning/REQUIREMENTS.md - BOOK-01 and BOOK-02 checkboxes plus Traceability table
    - .planning/phases/101-html-brand-book/101-CONTEXT.md - D-14/D-15 milestone close decisions
    - .planning/phases/101-html-brand-book/101-VALIDATION.md - final verification contract
    - AGENTS.md - no branch changes, no publish prep, linked-version two-package release design
  </read_first>
  <action>
    Update planning docs only after Task 3 passes.

    Concrete edits:
    - `.planning/REQUIREMENTS.md`: change `BOOK-01` and `BOOK-02` checkboxes from `[ ]` to `[x]`; add traceability rows `BOOK-01 | Phase 101 | Complete` and `BOOK-02 | Phase 101 | Complete` if they are missing.
    - `.planning/ROADMAP.md`: mark Phase 101 checked `[x]` with completion date; set Phase 101 plans to 4/4 with `101-01` through `101-04` listed and checked; update the Progress Table row for Phase 101 to `4/4`, `Complete`, and today's date; update v1.14 milestone status from in progress to shipped/complete.
    - `.planning/STATE.md`: update current position to Phase 101 complete; status to v1.14 shipped or milestone complete; record final verification evidence in accumulated decisions, including `BRANDBOOK HTML SYNCED`, `bash scripts/ci/lint.sh` green, and `brandbook.spec.ts` passing; update next steps so Phase 101 is no longer "ready to plan".
    - `.planning/PROJECT.md`: move v1.14 from current milestone posture to shipped milestone posture, preserving the no-new-runtime-API and linked sibling-package release shape. Add a concise shipped summary that mentions generated `brandbook/index.html`, stdlib generator, drift/budget guard, CI wiring, browser/static evidence, and no package/API/publish posture changes.
    - Do not create Phase 8-only docs.
    - Do not edit `rulestead/mix.exs`, `rulestead_admin/mix.exs`, release workflows, package versions, or publish docs in this task.
  </action>
  <verify>
    <automated>rg -n "BOOK-01|BOOK-02|Phase 101|v1.14|BRANDBOOK HTML SYNCED|brandbook.spec.ts" .planning/PROJECT.md .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` has checked `[x]` BOOK-01 and BOOK-02 requirement lines.
    - `.planning/REQUIREMENTS.md` traceability includes `BOOK-01 | Phase 101 | Complete` and `BOOK-02 | Phase 101 | Complete`.
    - `.planning/ROADMAP.md` marks Phase 101 complete, lists 4/4 plans, and records v1.14 as shipped/complete.
    - `.planning/STATE.md` no longer says Phase 101 is ready to plan and records final verification evidence.
    - `.planning/PROJECT.md` no longer presents v1.14 as the active current milestone and records it as shipped without new runtime APIs.
    - `git diff -- rulestead/mix.exs rulestead_admin/mix.exs .github scripts/ci/lint.sh` shows no package-version or publish-preparation edits from this task beyond the already planned lint guard from 101-03.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run from repo root before declaring Phase 101 complete:

1. `python3 scripts/gen_brandbook_html.py`
2. `python3 scripts/check_brandbook_html.py`
3. `python3 scripts/check_synced_pair.py`
4. `python3 scripts/check_brand_tokens.py`
5. `python3 scripts/check_tokens_css.py`
6. `bash scripts/ci/lint.sh`
7. `cd examples/demo/frontend && npm run test:e2e -- brandbook.spec.ts`
8. `rg -n "BOOK-01|BOOK-02|Phase 101|v1.14" .planning/PROJECT.md .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md`
</verification>

<success_criteria>
- Browser evidence proves `brandbook/index.html` loads from `file://`, exposes required landmarks and sections, supports Light/Dark/System theme control, and remains usable with JavaScript disabled.
- `brandbook/README.md` includes `index.html`, generator, and checker maintenance commands.
- Full generator, drift, budget, lint, token, SVG, and browser checks pass after all implementation/docs edits.
- BOOK-01 and BOOK-02 are complete in requirements and traceability.
- Phase 101 and v1.14 are marked shipped only after verification, with no runtime API, schema, package-version, branch, or publish-posture changes.
</success_criteria>
