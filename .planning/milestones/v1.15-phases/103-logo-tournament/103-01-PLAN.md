---
phase: 103
plan: 01
wave: 1
autonomous: false
objective: "Round 1 of the logo tournament: produce 12 integrated lockup candidates across axes A-D, assemble light+dark gallery sheets with the incumbent control, render via the Phase 102 harness, and checkpoint the maintainer for keep/cut feedback."
files_modified:
  - .planning/phases/103-logo-tournament/candidates/*.svg
  - .planning/phases/103-logo-tournament/round-1-studio.html
  - .planning/phases/103-logo-tournament/103-TOURNAMENT.md
---

# Plan 103-01 — Tournament Round 1

## Context
- 103-CONTEXT.md rules R-01..R-09 are locked. 102-AUDIT.md Appendix is the entry frame.
- Requirements: LOGO-07 (round mechanics + checkpoint), LOGO-08 (candidate guarantee).

## Tasks

### Task 1 — Produce 12 candidates (4 parallel design tracks, 3 each)
**type:** design/implementation (subagents, one per axis; no commits by subagents)
**files:** `.planning/phases/103-logo-tournament/candidates/{a1,a2,a3,b1,b2,b3,c1,c2,c3,d1,d2,d3}.svg`
**action:** Each axis agent authors 3 standalone candidate SVGs per the axis brief
(orchestrator prompt), built from `gen_glyph_paths.py` per-glyph outlines, modified per
R-08, palette per R-07, transparent background, no rect containers, no taglines. Each
agent self-renders its candidates with a scratch studio + `render_studio.sh` pattern and
iterates until forms are clean at 480px and survive a 36px strip look.
**verify:** every SVG parses (xmllint or python xml), contains no `<text>`, no `<image>`,
no full-canvas `<rect>`; fills only from the R-07 hex set.

### Task 2 — Assemble + render Round 1 gallery sheets
**type:** implementation (orchestrator)
**files:** `round-1-studio.html`, rendered `round-1-light.png`, `round-1-dark.png` (git-ignored)
**action:** Grid all 12 + CTRL at ~480px cells, light sheet (#FFFFFF cards) and dark sheet
(#10161F cards, candidate hexes mechanically swapped #183247→#e8edf3 and #C4CCD1→#3d4a55).
Render both via headless Chrome at scale-factor 2.
**verify:** PNGs exist, nonzero, 13 labeled cells visible.

### Task 3 — CHECKPOINT: maintainer review (autonomous: false — NEVER auto-decide)
**action:** Show the maintainer both sheets. For each candidate: keep/cut + what
specifically works/fails + new directions to inject. Log verbatim in 103-TOURNAMENT.md.
Outcome ITERATE → author 103-02-PLAN.md (Round 2) from survivors × feedback; outcome
WINNER → author terminal plan freezing 103-WINNER.md.

## Verification
- All four rejection criteria pass by construction for all 12 candidates (R-01).
- All four axes represented (LOGO-08 / SC-3).
- 103-TOURNAMENT.md updated with concepts + render manifest before checkpoint.

## Success criteria mapping
- LOGO-07: Task 2 renders + Task 3 checkpoint + bracket log.
- LOGO-08: Task 1 candidate guarantee verification.
