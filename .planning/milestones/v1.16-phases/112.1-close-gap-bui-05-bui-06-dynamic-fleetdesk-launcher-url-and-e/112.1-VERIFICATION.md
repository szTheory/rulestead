# Phase 112.1 Verification

Verified: 2026-06-13T01:07:03Z

## Command Outcomes

| Command | Outcome | Evidence |
| --- | --- | --- |
| `cd examples/demo/backend && mix compile --warnings-as-errors` | PASS | Exit 0; compiled 1 file and generated `rulestead_demo`. |
| `cd examples/demo/backend && mix test test/rulestead_demo_web/controllers/page_controller_test.exs --max-cases 1` | PASS | Exit 0; 3 tests, 0 failures. |
| `cd examples/demo/backend && bash -lc '! rg "href=\"http://localhost:3000|port 3000" lib/rulestead_demo_web/controllers/page_html/home.html.heex lib/rulestead_demo_web/components/layouts.ex'` | PASS | Exit 0; no hardcoded FleetDesk `localhost:3000` href or `port 3000` launcher copy remains in the Phoenix-owned surfaces. |
| `cd examples/demo/frontend && DEMO_BACKEND_URL="$DEMO_BACKEND_URL" DEMO_FRONTEND_URL="$DEMO_FRONTEND_URL" npm run test:e2e -- brand-ui-evidence.spec.ts --grep "demo launcher|FleetDesk launcher"` | PASS | Compose-selected URLs were `http://127.0.0.1:57547` backend and `http://127.0.0.1:57548` frontend; 8 tests passed in 2.5s. |
| `bash scripts/demo/verify.sh` | PASS | Compose smoke passed; full Playwright proof ran 105 tests with 105 passed in 31.8s; script ended with `compose smoke and browser proof passed`. |

The first targeted Playwright attempt reached a healthy compose stack but local `npm ci` failed while removing generated `node_modules` state (`ENOTEMPTY` under `node_modules/@sinclair`). Clearing `examples/demo/frontend/node_modules` and rerunning the same compose-backed command passed. No tracked source files changed for that recovery.

## Decision Coverage

| Decision | Verification |
| --- | --- |
| D-01 | `RulesteadDemo.DemoUrls.fleetdesk_frontend_url/0` uses `DEMO_FRONTEND_URL` runtime config with the unaided local fallback; backend tests prove a non-default URL renders. |
| D-02 | Plan 01 implementation reads through Phoenix application config and does not derive from request origin, endpoint config, CORS, or frontend port. |
| D-03 | `docker-compose.yml`, `docker-compose.proxy.yml`, Phoenix rendering, and Playwright all carry the selected `DEMO_FRONTEND_URL`; `scripts/demo/verify.sh` proved the compose-selected origin end to end. |
| D-04 | Backend tests and Playwright assertions cover both Phoenix-owned entry points: the `FleetDesk app` launcher card and exact-name `FleetDesk` shared layout nav link. |
| D-05 | The URL is passed through explicit controller assigns and `Layouts.app` attrs; no global plug or hidden template state was added. |
| D-06 | The hardcoded `port 3000` copy guard passed against `home.html.heex` and `layouts.ex`. |
| D-07 | Existing `brand-ui-evidence.spec.ts` was extended; no new visual regression program was introduced. |
| D-08 | Browser evidence normalizes `process.env.DEMO_FRONTEND_URL ?? "http://127.0.0.1:3000"` through `new URL(...).href` and asserts both Phoenix-rendered FleetDesk href values. |
| D-09 | `FleetDesk launcher click-through reaches selected origin` clicks from the Phoenix launcher to FleetDesk and verifies `.fd-brand-name`, `.fd-brand-mark`, and `Live map`. |
| D-10 | The curated admin evidence matrix includes `/admin/flags/fleet-map-v2/rollouts?env=staging`. |
| D-11 | Phase work stayed limited to URL plumbing, rendered links, targeted backend/browser proof, compose URL propagation, and verification truth. |
| D-12 | No FleetDesk rebranding, Rulestead palette/logo/token edits, public API/schema/config contract changes, release workflow edits, component framework adoption, `rulestead_admin` publish prep, or Phase 8-only docs were added. |

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| BUI-05 | 112.1-01-PLAN.md | Demo launcher and shared layout FleetDesk links use the live selected `DEMO_FRONTEND_URL` while FleetDesk remains host-branded. | passed | Backend tests prove non-3000 configured URL rendering; hardcoded-port source guard passes; Playwright asserts launcher/nav hrefs and clicks through to FleetDesk host-brand content. |
| BUI-06 | 112.1-02-PLAN.md | Browser evidence proves dynamic FleetDesk navigation and includes a build/release route row in the curated evidence matrix. | passed | Targeted Playwright passed 8/8 launcher tests against compose-selected backend/frontend URLs; full `bash scripts/demo/verify.sh` passed 105/105 browser tests; matrix includes `/admin/flags/fleet-map-v2/rollouts?env=staging`. |

## Audit Alignment

- The dynamic launcher blocker is addressed by Phase 112.1: Phoenix-owned FleetDesk links now render from `DEMO_FRONTEND_URL`, backend tests prove non-3000 rendering, and browser evidence proves hrefs plus click-through to the selected FleetDesk origin.
- The build/release evidence warning is addressed for this inserted phase by adding `/admin/flags/fleet-map-v2/rollouts?env=staging` to the curated Playwright evidence matrix.
- Broader phase-verification backfill for phases 107-112 is not closed by this inserted phase and remains outside Phase 112.1 unless a separate plan says otherwise.

## Scope Check

- `REQUIREMENTS.md` statuses were not changed.
- Package versions and lockfiles were not changed.
- Release workflow files were not changed.
- Public API/schema docs were not changed.
- `rulestead_admin` publishing posture files were not changed.
- Phase 8-only docs remain absent.
