---
phase: 64-proof-docs-and-support-truth
plan: 64-01
type: verification
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/mix/tasks/verify.phase64.ex
  - rulestead/mix.exs
autonomous: true
requirements:
  - VER-01
must_haves:
  truths:
    - mix verify.phase64 runs v1.7 core union plus v1.8 auto-advance delta in one maintainer command
    - Admin subprocess covers rollouts and timeline LiveView contract tests
    - Task does not delegate to verify.phase60 or other sub-tasks (no duplicate runs)
  artifacts:
    - rulestead/lib/mix/tasks/verify.phase64.ex
    - rulestead/mix.exs (preferred_envs entry)
---

# Plan 64-01: mix verify.phase64 Merge Gate

<objective>
Add `Mix.Tasks.Verify.Phase64` as the v1.8 merge gate — flat union of phase60 core tests plus auto-advance delta, with admin subprocess for rollouts and timeline contract tests.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-64-01: Delegating to verify.phase60 duplicates test runs | medium | Flat union only; never call sub-tasks |
| T-64-02: Missing auto-advance tests leaves VER-01 unproven | high | Explicit 5 core + 2 admin delta paths from CONTEXT D-01 |
| T-64-03: Omitting phase60 regression paths breaks v1.7 proof | high | Copy all 22 `@phase60_core_tests` paths verbatim |
</threat_model>

<tasks>

<task id="64-01-01" type="execute">
  <name>Create Mix.Tasks.Verify.Phase64</name>
  <read_first>
    - rulestead/lib/mix/tasks/verify.phase60.ex
    - .planning/phases/64-proof-docs-and-support-truth/64-CONTEXT.md (D-01)
    - .planning/phases/63-mounted-auto-advance-workflows/63-VERIFICATION.md (Automated proof section)
  </read_first>
  <action>
Create `rulestead/lib/mix/tasks/verify.phase64.ex` mirroring `verify.phase60.ex` structure:

**Module:** `Mix.Tasks.Verify.Phase64`  
**@shortdoc:** `"Runs Phase 64 guarded rollout auto-advance verification suite"`

**Comment at top:** Union of verify.phase60 core + v1.8 auto-advance delta — do not call verify.phase60 or other sub-tasks (avoids duplicate runs).

**@phase64_core_tests** — all 22 paths from `@phase60_core_tests` in verify.phase60.ex PLUS these 5 v1.8 delta paths:
```
"test/rulestead/rollout_auto_advance_contract_test.exs",
"test/rulestead/rollout_auto_advance_orchestration_contract_test.exs",
"test/rulestead/guardrails/auto_advance_test.exs",
"test/rulestead/guarded_rollout_test.exs",
"test/rulestead/scheduled_execution_conflict_test.exs"
```

**@admin_test_paths** — all entries from `@admin_test_paths` in verify.phase60.ex PLUS:
```
"test/rulestead_admin/live/flag_live/rollouts_test.exs",
"test/rulestead_admin/live/flag_live/timeline_test.exs"
```

**run/1:** Same pattern as phase60 — `Mix.Task.run("test", @phase64_core_tests)` then admin subprocess via `Path.expand("../../../../rulestead_admin", __DIR__)`.
  </action>
  <verify>
    <automated>cd rulestead && mix verify.phase64</automated>
  </verify>
  <acceptance_criteria>
    - File contains `defmodule Mix.Tasks.Verify.Phase64`
    - File contains all 5 auto-advance delta core test paths
    - File contains `rollouts_test.exs` and `timeline_test.exs` in admin paths
    - File does NOT contain `verify.phase60` or `Mix.Task.run("verify.phase60"`
    - `mix verify.phase64` exits 0
  </acceptance_criteria>
</task>

<task id="64-01-02" type="execute">
  <name>Register verify.phase64 in mix.exs preferred_envs</name>
  <read_first>
    - rulestead/mix.exs (preferred_envs line ~25)
  </read_first>
  <action>
In `rulestead/mix.exs`, extend `preferred_envs` tuple to include:
```elixir
{:"verify.phase64", :test}
```
alongside existing verify.phase54, verify.phase55, verify.phase56, verify.phase60 entries.
  </action>
  <verify>
    <automated>grep -q 'verify.phase64' rulestead/mix.exs && cd rulestead && mix help verify.phase64</automated>
  </verify>
  <acceptance_criteria>
    - `rulestead/mix.exs` contains `{:"verify.phase64", :test}`
    - `mix help verify.phase64` shows task without error
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead && mix verify.phase64
grep -q 'verify.phase64' rulestead/mix.exs
! grep -q 'verify.phase60' rulestead/lib/mix/tasks/verify.phase64.ex
```
</verification>

<success_criteria>
- VER-01: Repo-local proof covers healthy auto-advance, fail-closed non-advance, protected-env governance, idempotency races, and stale-signal behavior in one command
- `mix verify.phase64` is the v1.8 superset; `mix verify.phase60` remains valid for v1.7 regression
</success_criteria>
