---
phase: 64-proof-docs-and-support-truth
plan: 64-02
type: docs
wave: 2
depends_on:
  - 64-01
files_modified:
  - rulestead/test/rulestead/release_contract_test.exs
  - README.md
  - rulestead/README.md
  - rulestead_admin/README.md
  - MAINTAINING.md
autonomous: true
requirements:
  - VER-02
  - VER-03
must_haves:
  truths:
    - release_contract_test.exs enforces auto-advance support truth across all maintainer docs
    - Stale forbidden phrases removed from guarded-rollout and blast-radius blocks
    - Root README Proof today adds v1.8 entry alongside verify.phase60 and verify.phase56
  artifacts:
    - release_contract_test.exs (new test block + forbidden phrase updates)
    - README.md, rulestead/README.md, rulestead_admin/README.md, MAINTAINING.md
---

# Plan 64-02: Release Contract Drift Guards And Support Truth READMEs

<objective>
Extend `release_contract_test.exs` with guarded-rollout auto-advance support-truth block, remove stale forbidden phrases, and sync root/package READMEs plus MAINTAINING.md so public docs match bounded v1.8 scope.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-64-04: Docs claim built-in observability or fleet dashboards | high | Forbidden phrases + host-owned metrics/signals language |
| T-64-05: Docs imply standalone admin product | high | Assert mounted companion posture; forbid standalone phrases |
| T-64-06: Removing verify.phase60/56 breaks prior milestone stories | medium | Add v1.8 entry alongside; never replace prior entries |
</threat_model>

<tasks>

<task id="64-02-01" type="execute">
  <name>Add auto-advance support truth test block and update forbidden phrases</name>
  <read_first>
    - rulestead/test/rulestead/release_contract_test.exs (guarded rollout block ~L282, blast-radius block ~L410)
    - .planning/phases/64-proof-docs-and-support-truth/64-CONTEXT.md (D-02)
  </read_first>
  <action>
**Step 1 — Update guarded rollout block (~L320 forbidden_phrases):**
Remove `"auto-advance"` from the forbidden list — bounded auto-advance is now shipped.

**Step 2 — Update blast-radius block (~L450 forbidden_phrases):**
Remove `"auto-advance guarded rollouts"` from the forbidden list — v1.8 delivers ROL-04.

**Step 3 — Add new test after blast-radius block:**

**Test name:** `"guarded rollout auto-advance support truth stays bounded across root package and maintainer docs"`

**Assert root README contains:**
- `mix verify.phase64`
- `observation window` or `observation-window`
- `authored next-stage` or `next-stage plan`
- `guardrail_automation`
- `fail closed`
- `host-owned`
- `mix verify.phase60` (v1.7 entry preserved)
- `mix verify.phase56` (v1.6 entry preserved)
- `RULESTEAD_TEST_SCOPE=guarded_rollout_auto_advance bash scripts/ci/test.sh`

**Assert rulestead/README.md contains:**
- `mix verify.phase64`
- `domain`, `validation`, `contracts`
- `fail closed`
- `host-owned`
- `observation window` or `observation-window`

**Assert rulestead_admin/README.md contains:**
- `mounted companion` or `mounted presentation`
- `not a standalone`
- `auto-advance` or `auto advance` (bounded claim allowed in admin README)
- `guardrail_automation` or `guardrail automation`

**Assert MAINTAINING.md contains:**
- `Guarded Rollout Auto-Advance Proof` (section heading)
- `mix verify.phase64`
- `VER-01`
- `61-auto-advance-authored-contract` or `61-`
- `RULESTEAD_TEST_SCOPE=guarded_rollout_auto_advance bash scripts/ci/test.sh`

**forbidden_phrases** (refute in root_readme, admin_readme, maintaining):
```
"standalone rulestead_admin",
"standalone control plane",
"built-in observability",
"Rulestead dashboard",
"metrics ingestion",
"fleet dashboard",
"self-healing rollouts",
"time-based percentage rollout",
"automatic progressive delivery platform"
```
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/release_contract_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Test block name contains `guarded rollout auto-advance support truth`
    - Guarded rollout forbidden list no longer contains bare `"auto-advance"`
    - Blast-radius forbidden list no longer contains `"auto-advance guarded rollouts"`
    - New test asserts `mix verify.phase64` in root_readme
    - New test asserts `mix verify.phase60` and `mix verify.phase56` still in root_readme
  </acceptance_criteria>
</task>

<task id="64-02-02" type="execute">
  <name>Update README and MAINTAINING support truth</name>
  <read_first>
    - README.md (Proof today section ~L163)
    - rulestead/README.md
    - rulestead_admin/README.md
    - MAINTAINING.md (Blast Radius Governance Proof section as template)
    - .planning/phases/64-proof-docs-and-support-truth/64-CONTEXT.md (D-02)
  </read_first>
  <action>
**README.md — Proof today section:** Add new bullet after blast radius governance entry:

```markdown
- **Guarded rollout auto-advance (v1.8):** `cd rulestead && mix verify.phase64`
  proves opt-in per-rollout auto-advance policy with **observation window** and
  **authored next-stage plan**, healthy scheduled-tick advance, fail-closed
  non-advance on weak or stale signals, protected-environment **change request**
  routing at tick execute, idempotency under concurrent manual advance, and
  mounted admin presentation of pending observation state. Timeline entries
  distinguish **`guardrail_automation`** from manual actions. Metrics and signal
  facts remain **host-owned**; Rulestead evaluates normalized facts only — not a
  built-in observability product.
- `RULESTEAD_TEST_SCOPE=guarded_rollout_auto_advance bash scripts/ci/test.sh`
  reruns the v1.8 guarded rollout auto-advance proof bar in CI.
```

Do NOT remove existing `verify.phase60`, `verify.phase56`, or guarded_rollout_foundations bullets.

**rulestead/README.md:** Add proof command reference (`mix verify.phase64`) and auto-advance vocabulary (observation window, fail closed, host-owned signals) in appropriate section.

**rulestead_admin/README.md:** Add note that admin owns mounted auto-advance panel presentation; core owns policy + orchestration; timeline labels distinguish automation from manual.

**MAINTAINING.md:** Add section `## Guarded Rollout Auto-Advance Proof` with:
- Bounded scope description (no metrics pipelines, no fleet dashboards, no time-based percentage rollout)
- Commands: `mix verify.phase64`, `RULESTEAD_TEST_SCOPE=guarded_rollout_auto_advance bash scripts/ci/test.sh`
- Upstream handoff refs: `.planning/phases/61-auto-advance-authored-contract/`, `62-orchestration-and-governed-execution/`, `63-mounted-auto-advance-workflows/`
- VER-01 requirement trace
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/release_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `mix verify.phase64` and `auto-advance (v1.8)` or `auto-advance (v1.8)`
    - README.md still contains `mix verify.phase60` and `mix verify.phase56`
    - MAINTAINING.md contains `## Guarded Rollout Auto-Advance Proof`
    - `mix test test/rulestead/release_contract_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead && mix test test/rulestead/release_contract_test.exs
grep -q 'mix verify.phase64' README.md
grep -q 'mix verify.phase60' README.md
grep -q 'Guarded Rollout Auto-Advance Proof' MAINTAINING.md
```
</verification>

<success_criteria>
- VER-02: Public docs and release-contract checks describe bounded auto-advance scope, observation-window semantics, and host-owned metrics responsibilities
- VER-03: Linked-version sibling-package release model unchanged; bounded auto-advance claims only where implemented
</success_criteria>
