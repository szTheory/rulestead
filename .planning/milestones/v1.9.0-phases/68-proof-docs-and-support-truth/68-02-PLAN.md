---
phase: 68-proof-docs-and-support-truth
plan: 68-02
type: docs
wave: 2
depends_on:
  - 68-01
files_modified:
  - rulestead/test/rulestead/release_contract_test.exs
  - README.md
  - rulestead/README.md
  - rulestead_admin/README.md
  - MAINTAINING.md
autonomous: true
requirements:
  - VER-02
  - VER-03
must_haves:
  truths:
    - release_contract_test.exs enforces preview-evidence support truth across maintainer docs
    - Root README Proof today adds v1.9 entry alongside verify.phase64, verify.phase60, verify.phase56
    - MAINTAINING.md Host Preview Evidence Proof section matches verify.phase68 and CI scope
  artifacts:
    - release_contract_test.exs (new test block)
    - README.md, rulestead/README.md, rulestead_admin/README.md, MAINTAINING.md
---

# Plan 68-02: Release Contract Drift Guards And Support Truth READMEs

<objective>
Extend `release_contract_test.exs` with host preview evidence support-truth block and sync root/package READMEs plus MAINTAINING.md so public docs match bounded v1.9 scope.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-68-04: Docs claim authoritative population or fleet analytics | high | Forbidden phrases + `authoritative_population_count` language |
| T-68-05: Docs imply Rulestead-owned observability product | high | Forbid fleet dashboard, metrics ingestion, impression analytics platform |
| T-68-06: Removing verify.phase64/60/56 breaks prior milestone stories | medium | Add v1.9 entry alongside; never replace prior entries |
</threat_model>

<tasks>

<task id="68-02-01" type="execute">
  <name>Add host preview evidence support truth test block</name>
  <read_first>
    - rulestead/test/rulestead/release_contract_test.exs (auto-advance block ~L468)
    - .planning/phases/68-proof-docs-and-support-truth/68-RESEARCH.md
    - .planning/phases/65-host-preview-evidence-contract/65-CONTEXT.md
  </read_first>
  <action>
Add new test after the `"guarded rollout auto-advance support truth stays bounded"` block (~L524):

**Test name:** `"host preview evidence support truth stays bounded across root package and maintainer docs"`

**Assert root README contains:**
- `mix verify.phase68`
- `preview_evidence_resolver` or `PreviewEvidenceResolver`
- `host-supplied` or `host supplied`
- `sample cohort` or `Sample cohort`
- `impression summary` or `Impression summary`
- `authoritative_population_count` or `authoritative population`
- `fail closed`
- `mix verify.phase64` (v1.8 preserved)
- `mix verify.phase60` (v1.7 preserved)
- `mix verify.phase56` (v1.6 preserved)
- `RULESTEAD_TEST_SCOPE=host_preview_evidence bash scripts/ci/test.sh`

**Assert rulestead/README.md contains:**
- `mix verify.phase68`
- `preview_evidence_resolver` or `PreviewEvidenceResolver`
- `fail closed`
- `host-supplied` or `host supplied`

**Assert rulestead_admin/README.md contains:**
- `mounted companion` or `mounted presentation`
- `not a standalone`
- `preview evidence` or `preview-evidence` or `host-supplied evidence`

**Assert MAINTAINING.md contains:**
- `Host Preview Evidence Proof` (section heading)
- `mix verify.phase68`
- `VER-01`
- reference to phases `65-` or `65-host-preview-evidence`
- `RULESTEAD_TEST_SCOPE=host_preview_evidence bash scripts/ci/test.sh`

**forbidden_phrases** (operator_docs = root, admin, maintaining):
```
"authoritative population counts",
"fleet-wide population",
"built-in observability",
"Rulestead dashboard",
"metrics ingestion",
"fleet dashboard",
"impression analytics platform",
"population analytics product"
```

Do **not** modify v1.8 auto-advance forbidden-phrase relaxations from Phase 64.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/release_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `release_contract_test.exs` contains test name `host preview evidence support truth stays bounded`
    - `mix test test/rulestead/release_contract_test.exs` exits 0
    - New test asserts `mix verify.phase68` in root README
    - New test refutes at least `fleet dashboard` in operator docs
  </acceptance_criteria>
</task>

<task id="68-02-02" type="execute">
  <name>Sync README and MAINTAINING bounded v1.9 claims</name>
  <read_first>
    - README.md (Proof today section)
    - rulestead/README.md
    - rulestead_admin/README.md
    - MAINTAINING.md (Guarded Rollout Auto-Advance Proof section as template)
    - rulestead/test/rulestead/release_contract_test.exs (new block from 68-02-01)
  </read_first>
  <action>
**README.md — Proof today:** Add v1.9 bullet after v1.8 auto-advance entry:
- **Host preview evidence (v1.9):** `cd rulestead && mix verify.phase68`
- One sentence: bounded host-supplied sample cohort and impression summary on audience previews; resolver opt-in; no authoritative population counts; `authoritative_population_count?: false`

**rulestead/README.md:** Add section **Host preview evidence contract** with:
- `mix verify.phase68` command
- `Rulestead.PreviewEvidenceResolver` behaviour (host implements)
- Config key `:preview_evidence_resolver`
- Fail-closed on invalid/oversized/policy-denied evidence
- Preserve existing `mix verify.phase64` and `mix verify.phase60` sections

**rulestead_admin/README.md:** Add paragraph under mounted companion posture:
- Audience preview surfaces render bounded sample/impression evidence when host configures resolver
- Not observability product; not standalone admin

**MAINTAINING.md:** Add section **Host Preview Evidence Proof** after Guarded Rollout Auto-Advance Proof:
- Rerun: `RULESTEAD_TEST_SCOPE=host_preview_evidence bash scripts/ci/test.sh`
- Remediation: `cd rulestead && mix verify.phase68`
- Bounded scope: resolver wiring, redaction, fingerprint/stale, GOV-05 boundary, mounted rendering
- Non-claims: no warehouse ingestion, no fleet dashboards, no authoritative population analytics
- List core delta test paths and admin `audience_components_test.exs` + audience_live preview tests (align with verify.phase68 union)
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/release_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'mix verify.phase68' README.md` exits 0
    - `grep -q 'Host Preview Evidence Proof' MAINTAINING.md` exits 0
    - `grep -q 'mix verify.phase64' README.md` exits 0 (v1.8 preserved)
    - `mix test test/rulestead/release_contract_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead && mix test test/rulestead/release_contract_test.exs
grep -q 'mix verify.phase68' README.md
grep -q 'Host Preview Evidence Proof' MAINTAINING.md
grep -q 'mix verify.phase64' README.md
```
</verification>

<success_criteria>
- VER-02: Host-app integration seam docs path prepared; MAINTAINING mounted proof list will match CI in 68-03/68-04
- VER-03: Release-contract and public docs allow bounded host-supplied preview evidence claims only
</success_criteria>
