---
phase: 68-proof-docs-and-support-truth
plan: 68-01
type: verification
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/mix/tasks/verify.phase68.ex
  - rulestead/mix.exs
autonomous: true
requirements:
  - VER-01
must_haves:
  truths:
    - mix verify.phase68 runs v1.8 core union plus v1.9 preview-evidence delta in one maintainer command
    - Admin subprocess includes audience_components_test.exs alongside existing audience_live preview tests
    - Task does not delegate to verify.phase64 or other sub-tasks (no duplicate runs)
  artifacts:
    - rulestead/lib/mix/tasks/verify.phase68.ex
    - rulestead/mix.exs (preferred_envs entry)
---

# Plan 68-01: mix verify.phase68 Merge Gate

<objective>
Add `Mix.Tasks.Verify.Phase68` as the v1.9 merge gate — flat union of phase64 core tests plus preview-evidence delta, with admin subprocess for `audience_components_test.exs`.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-68-01: Delegating to verify.phase64 duplicates test runs | medium | Flat union only; never call sub-tasks |
| T-68-02: Missing preview-evidence tests leaves VER-01 unproven | high | Explicit 3 core + 1 admin delta paths from RESEARCH |
| T-68-03: Omitting phase64 regression paths breaks v1.8 proof | high | Copy all 27 `@phase64_core_tests` paths verbatim |
</threat_model>

<tasks>

<task id="68-01-01" type="execute">
  <name>Create Mix.Tasks.Verify.Phase68</name>
  <read_first>
    - rulestead/lib/mix/tasks/verify.phase64.ex
    - rulestead/lib/mix/tasks/verify.phase60.ex
    - .planning/phases/68-proof-docs-and-support-truth/68-RESEARCH.md
    - .planning/phases/65-host-preview-evidence-contract/65-04-SUMMARY.md
    - .planning/phases/66-evidence-carry-through-and-governance-boundary/66-04-SUMMARY.md
    - .planning/phases/67-mounted-preview-evidence-workflows/67-VERIFICATION.md
  </read_first>
  <action>
Create `rulestead/lib/mix/tasks/verify.phase68.ex` mirroring `verify.phase64.ex` structure:

**Module:** `Mix.Tasks.Verify.Phase68`  
**@shortdoc:** `"Runs Phase 68 host preview evidence verification suite"`

**Comment at top:** Union of verify.phase64 core + v1.9 preview-evidence delta — do not call verify.phase64, verify.phase60, or other sub-tasks (avoids duplicate runs).

**@phase68_core_tests** — all 27 paths from `@phase64_core_tests` in `verify.phase64.ex` PLUS these 3 v1.9 delta paths:
```
"test/rulestead/targeting/preview_evidence_contract_test.exs",
"test/rulestead/targeting/preview_evidence_test.exs",
"test/rulestead/governance/preview_evidence_governance_contract_test.exs"
```

**@admin_test_paths** — all entries from `@admin_test_paths` in `verify.phase64.ex` PLUS:
```
"test/rulestead_admin/components/audience_components_test.exs"
```

**run/1:** Same pattern as phase64 — `Mix.Task.run("test", @phase68_core_tests)` then admin subprocess via `Path.expand("../../../../rulestead_admin", __DIR__)`.
  </action>
  <verify>
    <automated>cd rulestead && mix verify.phase68</automated>
  </verify>
  <acceptance_criteria>
    - File contains `defmodule Mix.Tasks.Verify.Phase68`
    - File contains all 3 preview-evidence delta core test paths
    - File contains `audience_components_test.exs` in admin paths
    - File does NOT contain `verify.phase64` or `Mix.Task.run("verify.phase64"`
    - `mix verify.phase68` exits 0
  </acceptance_criteria>
</task>

<task id="68-01-02" type="execute">
  <name>Register verify.phase68 in mix.exs preferred_envs</name>
  <read_first>
    - rulestead/mix.exs (preferred_envs ~line 25)
  </read_first>
  <action>
In `rulestead/mix.exs`, extend `preferred_envs` tuple to include:
```elixir
{:"verify.phase68", :test}
```
alongside existing `verify.phase64` and `verify.phase60` entries.
  </action>
  <verify>
    <automated>grep -q 'verify.phase68' rulestead/mix.exs && cd rulestead && mix help verify.phase68</automated>
  </verify>
  <acceptance_criteria>
    - `rulestead/mix.exs` contains `{:"verify.phase68", :test}`
    - `mix help verify.phase68` shows task without error
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead && mix verify.phase68
grep -q 'verify.phase68' rulestead/mix.exs
! grep -q 'verify.phase64' rulestead/lib/mix/tasks/verify.phase68.ex
```
</verification>

<success_criteria>
- VER-01: Repo-local proof covers resolver wiring, redaction, fingerprint determinism, stale rejection with evidence, governance boundary, and mounted rendering in one command
- `mix verify.phase68` is the v1.9 superset; `mix verify.phase64` remains valid for v1.8 regression
</success_criteria>
