---
phase: 65-host-preview-evidence-contract
plan: 65-04
type: feature
wave: 4
depends_on:
  - 65-03
files_modified:
  - rulestead/test/rulestead/targeting/preview_evidence_contract_test.exs
  - rulestead/test/rulestead/store/audience_impact_contract_test.exs
  - rulestead/test/rulestead/governance/blast_radius_threshold_test.exs
autonomous: true
requirements:
  - IMP-05
  - IMP-06
must_haves:
  truths:
    - Fake and Ecto share contract tests for evidence, stale rejection, fail-closed
    - Richer host evidence cannot bypass stale preview on apply
    - Blast-radius assessment ignores impression summaries (GOV-05 boundary in Phase 65)
  artifacts:
    - rulestead/test/rulestead/targeting/preview_evidence_contract_test.exs
---

# Plan 65-04: Contract Tests — Evidence, Stale, Fail-Closed, Parity

<objective>
Prove ROADMAP success criteria #3–4 and IMP-06 stale-token behavior across adapters per CONTEXT D-09, D-10, D-11.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-65-07: Evidence drift bypasses apply | high | Contract: change resolver output → apply with old fingerprint fails |
| T-65-08: Impression-weighted governance | high | Regression: assess/2 unchanged when impression_evidence present |
| T-65-09: Ecto-only redaction drift | medium | `@adapters` loop for all evidence contracts |
</threat_model>

<tasks>

<task id="65-04-01" type="execute">
  <name>preview_evidence_contract_test.exs</name>
  <read_first>
    - rulestead/test/rulestead/store/audience_impact_contract_test.exs
    - rulestead/test/rulestead/guarded_rollout_test.exs (@adapters, reset_adapter!)
    - .planning/phases/65-host-preview-evidence-contract/65-CONTEXT.md (D-09, D-11)
  </read_first>
  <action>
Create `rulestead/test/rulestead/targeting/preview_evidence_contract_test.exs`:

```elixir
defmodule Rulestead.Targeting.PreviewEvidenceContractTest do
  use Rulestead.RepoCase, async: false

  alias Rulestead.Store.Command
  alias Rulestead.Store.Ecto, as: StoreEcto

  @adapters [Rulestead.Fake, StoreEcto]
```

Implement configurable stub:

```elixir
defmodule Rulestead.Test.PreviewEvidenceContractStub do
  @behaviour Rulestead.Targeting.PreviewEvidence
  def resolve(_), do: Application.get_env(:rulestead, :preview_evidence_stub_result, {:ok, %{}})
end
```

**Tests (each iterates `@adapters` where store-level):**

1. **"preview with resolver returns schema v2 and host basis"** — configure stub with samples + impression; preview → assert fingerprint present, basis `authored_state_with_host_evidence`

2. **"oversized evidence fails closed on preview"** — stub returns 30 sample rows → `{:error, %Rulestead.Error{type: :invalid_command}}` with metadata code `preview_evidence_oversized`

3. **"invalid impression key fails closed"** — stub returns `%{"email" => "x"}` inside impression_summary → `preview_evidence_invalid`

4. **"policy denied fails closed"** — stub returns `%{policy_denied: true}` → `preview_evidence_policy_denied`

5. **"apply rejects stale fingerprint when host evidence changes"** (IMP-06):
   - Preview mutation; capture `preview_fingerprint` and `preview_schema_version: 2`
   - Change stub to different `matched_impressions` count
   - Attempt apply with old fingerprint → error message contains `"stale"` or metadata `expected_preview_fingerprint`

6. **"explicit command samples preserved with resolver"** — pass `samples: [%{"actor_key" => "operator-1"}]` on command; stub adds another row → both present in `sample_evidence` (operator first)

7. **"no resolver matches pre-v1.9 semantics"** — delete env; preview without resolver → basis `authored_state_and_explicit_samples`, empty `impression_evidence`

Use `StoreFixtures` audience/flag seed helpers from audience_impact_contract_test.

Copy `reset_adapter!/1`, `adapter_suffix/1` patterns from existing contract tests.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/targeting/preview_evidence_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File `preview_evidence_contract_test.exs` exists
    - Test `"apply rejects stale fingerprint when host evidence changes"` exists
    - Test uses `@adapters [Rulestead.Fake, StoreEcto]`
    - `mix test test/rulestead/targeting/preview_evidence_contract_test.exs` exits 0
  </acceptance_criteria>
</task>

<task id="65-04-02" type="execute">
  <name>GOV-05 boundary regression</name>
  <read_first>
    - rulestead/lib/rulestead/governance/blast_radius_threshold.ex
    - rulestead/test/rulestead/governance/blast_radius_threshold_test.exs
    - .planning/phases/65-host-preview-evidence-contract/65-CONTEXT.md (D-10)
  </read_first>
  <action>
Add test to `blast_radius_threshold_test.exs`:

**"assess ignores impression_evidence and sample_evidence for verdict"**

- Build preview attrs map with `affected_references` count above/below threshold
- Add large `impression_summary` with high `matched_impressions` that would imply huge population if misused
- Assert `assess/2` verdict depends only on reference keys / dependency entries — same verdict with and without impression map

Do NOT add impression fields to `BlastRadiusThreshold` module.

Optionally grep `blast_radius_threshold.ex` to confirm no `impression` string references added in Phase 65 implementation.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/governance/blast_radius_threshold_test.exs test/rulestead/targeting/preview_evidence_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test `"assess ignores impression_evidence"` exists in blast_radius_threshold_test.exs
    - `blast_radius_threshold.ex` has no new `impression` scoring logic (manual grep in acceptance: file must not contain `impression_summary` in assess path)
    - Combined test command exits 0
  </acceptance_criteria>
</task>

<task id="65-04-03" type="execute">
  <name>Full phase test slice</name>
  <read_first>
    - .planning/phases/65-host-preview-evidence-contract/65-VALIDATION.md
  </read_first>
  <action>
Run full phase validation command and fix any schema v1 assumptions in related tests:

`cd rulestead && mix test test/rulestead/targeting/impact_preview_test.exs test/rulestead/targeting/preview_evidence_test.exs test/rulestead/targeting/preview_evidence_contract_test.exs test/rulestead/store/audience_impact_contract_test.exs test/rulestead/governance/blast_radius_threshold_test.exs`

Update any failing tests outside this list that hardcode `preview_schema_version: 1` for audience mutations (grep repo for `preview_schema_version: 1`).

Do **not** add `mix verify.phase65` (deferred to Phase 68 per CONTEXT).
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/targeting/impact_preview_test.exs test/rulestead/targeting/preview_evidence_test.exs test/rulestead/targeting/preview_evidence_contract_test.exs test/rulestead/store/audience_impact_contract_test.exs test/rulestead/governance/blast_radius_threshold_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Full combined test command exits 0
    - No remaining `preview_schema_version: 1` in audience mutation test fixtures (grep returns empty or only comments)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] All contract tests pass on Fake and Ecto
- [ ] Stale evidence rejection proven (IMP-06)
- [ ] GOV-05 reference-count-only boundary proven
</verification>
