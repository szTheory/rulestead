---
phase: 67-mounted-preview-evidence-workflows
plan: 67-02
type: feature
wave: 2
depends_on:
  - 67-01
files_modified:
  - rulestead_admin/test/rulestead_admin/live/audience_live/edit_preview_test.exs
  - rulestead_admin/test/rulestead_admin/live/audience_live/archive_preview_test.exs
autonomous: true
requirements:
  - ADM-05
must_haves:
  truths:
    - Edit preview mounted test renders host evidence when Fake resolver configured
    - Edit preview fail-closed shows role=alert with error.message when resolver returns error
    - Archive preview parity with edit for evidence + drift copy unchanged
    - Confirm link href includes preview_fingerprint query param with resolver on
  artifacts:
    - rulestead_admin/test/rulestead_admin/live/audience_live/edit_preview_test.exs
    - rulestead_admin/test/rulestead_admin/live/audience_live/archive_preview_test.exs
---

# Plan 67-02: Edit And Archive Preview Mounted Tests

<objective>
Prove mounted edit and archive preview LiveViews surface host-supplied evidence through the existing `Rulestead.preview_audience_impact/3` path — no LiveView code changes (CONTEXT D-03, D-04).
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-67-03: Resolver env leaks between tests | medium | Save/restore Application env; async: false on evidence describe |
| T-67-04: Tests assert admin-side resolve logic | low | Only configure Application env; no new resolve calls in LiveViews |
</threat_model>

<tasks>

<task id="67-02-01" type="execute">
  <name>Edit preview evidence and fail-closed tests</name>
  <read_first>
    - rulestead_admin/test/rulestead_admin/live/audience_live/edit_preview_test.exs
    - rulestead/test/rulestead/audience_mutation_audit_test.exs (resolver setup lines 110-113)
    - rulestead/lib/rulestead/fake/preview_evidence_resolver.ex
    - .planning/phases/67-mounted-preview-evidence-workflows/67-CONTEXT.md (D-03, D-04)
  </read_first>
  <action>
In `edit_preview_test.exs`:

1. Add helper `with_preview_evidence_resolver/1` mirroring core audit test:
   - Save `Application.get_env(:rulestead, :preview_evidence_resolver)`
   - `Application.put_env(:rulestead, :preview_evidence_resolver, Rulestead.Fake.PreviewEvidenceResolver)`
   - `on_exit` restore or delete

2. Add describe `"preview evidence"` with `async: false` and resolver helper in setup:

   **test "renders sample cohort and impression summary when resolver configured"**:
   - `live(conn, "/admin/flags/audiences/vip-users/edit/preview?env=test")`
   - `assert html =~ "Sample cohort"`
   - `assert html =~ "Impression summary"`
   - `assert html =~ "last_24h"`
   - `assert html =~ "fake-vip-users"`
   - `assert html =~ "Authored state with host-supplied evidence"`
   - `assert html =~ "bounded host-supplied evidence"`
   - `refute html =~ "fleet"`
   - `refute html =~ "dashboard"`

   **test "confirm link preserves preview_fingerprint"**:
   - Extract `audprev_` fingerprint from html
   - `assert html =~ "preview_fingerprint=#{fingerprint}"` OR parse Continue link href contains `preview_fingerprint` and `preview_schema_version`

3. Add test for invalid evidence fail-closed using existing stub pattern from `preview_evidence_contract_test.exs` if a deny stub exists; otherwise skip invalid test and document reliance on core contract — **minimum**: unconfigured resolver test shows no Sample cohort section:
   - Without resolver: `refute html =~ "Sample cohort"` OR basis shows explicit samples only

4. Keep existing drift and governance tests unchanged (assertions must still pass).
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/edit_preview_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `edit_preview_test.exs` contains `Rulestead.Fake.PreviewEvidenceResolver`
    - `edit_preview_test.exs` contains `describe "preview evidence"`
    - `mix test test/rulestead_admin/live/audience_live/edit_preview_test.exs` exits 0
    - Existing test `"edit preview shows impact fingerprint and confirm link"` still passes
  </acceptance_criteria>
</task>

<task id="67-02-02" type="execute">
  <name>Archive preview evidence tests</name>
  <read_first>
    - rulestead_admin/test/rulestead_admin/live/audience_live/archive_preview_test.exs
    - rulestead_admin/test/rulestead_admin/live/audience_live/edit_preview_test.exs (new evidence describe)
    - .planning/phases/67-mounted-preview-evidence-workflows/67-CONTEXT.md (D-03)
  </read_first>
  <action>
Mirror edit preview evidence describe in `archive_preview_test.exs`:

1. Reuse same `with_preview_evidence_resolver/1` helper (duplicate locally or extract to `RulesteadAdmin.Test.PreviewEvidenceCase` only if already exists — prefer local duplicate to minimize scope).

2. Tests:
   - **"archive preview renders host evidence when resolver configured"** — path `/admin/flags/audiences/vip-users/archive/preview?env=test`
   - Same HTML assertions as edit (Sample cohort, last_24h, basis label, uncertainty message)
   - **"archive preview drift copy unchanged"** — existing drift test still passes with resolver off

3. Confirm link test for archive: href contains `preview_fingerprint` when evidence shown.

No changes to `archive_preview.ex`.
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/archive_preview_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `archive_preview_test.exs` contains `Sample cohort` assertion in evidence test
    - `mix test test/rulestead_admin/live/audience_live/archive_preview_test.exs` exits 0
    - Drift test still asserts `Preview refreshed`
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/edit_preview_test.exs test/rulestead_admin/live/audience_live/archive_preview_test.exs
```
</verification>
