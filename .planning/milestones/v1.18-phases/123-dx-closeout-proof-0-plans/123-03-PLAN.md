---
phase: "123"
plan: "03"
type: execute
wave: 3
depends_on:
  - "123-01"
  - "123-02"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CIDX-08
  - CIDX-10

must_haves:
  truths:
    - "bash scripts/ci/lint.sh exits 0 (doc/brand/asset guards green after all Wave 1+2 edits)"
    - "cd rulestead && mix deps.get && mix test test/rulestead/release_contract_test.exs exits 0 (doc-drift guard passes — mandatory for any MAINTAINING.md edit)"
    - "REQUIREMENTS.md traceability table shows CIDX-08 and CIDX-10 as Complete"
    - "ROADMAP.md Phase 123 checklist entry is marked [x] and traceability table shows CIDX-08 and CIDX-10 as Complete"
    - "STATE.md frontmatter shows completed_phases: 5 and percent: 100"
    - "STATE.md no longer states 'Phase 122 was final phase' — Phase 123 is recorded as the actual final phase"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table with CIDX-08 and CIDX-10 flipped to Complete"
      contains: "CIDX-08 | Phase 123 | Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 123 marked complete in checklist and progress/traceability tables"
      contains: "[x] Phase 123"
    - path: ".planning/STATE.md"
      provides: "Milestone done-state: completed_phases: 5, percent: 100, Phase 123 as actual final phase"
  key_links:
    - from: ".planning/REQUIREMENTS.md traceability table :63,65"
      to: "CIDX-08 / CIDX-10 Phase 123 rows"
      via: "Status column flip Pending → Complete"
      pattern: "CIDX-08 | Phase 123 | Complete"
    - from: ".planning/STATE.md frontmatter"
      to: "completed_phases: 5, percent: 100"
      via: "YAML frontmatter edit"
      pattern: "completed_phases: 5"
---

<objective>
Run the mandatory final verification gates (D-15..D-18) and flip traceability and STATE to the done state (D-19/D-20).

Purpose: Prove the Wave 1+2 edits are green against the actual CI gate guards, then close the milestone with internally consistent planning-artifact truth so the next milestone kickoff reads a clean STATE.

Output: Lint + doc-drift test green (reported in SUMMARY); `REQUIREMENTS.md`, `ROADMAP.md`, and `STATE.md` updated to Complete/100% state.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/123-dx-closeout-proof-0-plans/123-CONTEXT.md
@.planning/phases/123-dx-closeout-proof-0-plans/123-01-SUMMARY.md
@.planning/phases/123-dx-closeout-proof-0-plans/123-02-SUMMARY.md

<interfaces>
<!-- Exact edit targets for D-19/D-20. Read these files before editing. -->

From .planning/REQUIREMENTS.md (traceability table):
- Line 63: `| CIDX-08 | Phase 123 | Pending |`  → change to `| CIDX-08 | Phase 123 | Complete |`
- Line 65: `| CIDX-10 | Phase 123 | Pending |`  → change to `| CIDX-10 | Phase 123 | Complete |`
- Also flip the inline checkbox at top of file:
  Line 27: `- [ ] **CIDX-08**: ...` → `- [x] **CIDX-08**: ...`
  Line 28: `- [ ] **CIDX-10**: ...` → `- [x] **CIDX-10**: ...`

From .planning/ROADMAP.md:
- Line 17: `- [ ] Phase 123: DX + Closeout Proof (0/? plans)`
    → `- [x] Phase 123: DX + Closeout Proof (3/3 plans) (completed 2026-06-17)`
- Line 147 (progress table): `| 123. DX + Closeout Proof | v1.18 | 0/? | Pending | — |`
    → `| 123. DX + Closeout Proof | v1.18 | 3/3 | Complete | 2026-06-17 |`
- Line 160: `| CIDX-08 | Phase 123 | Pending |` → `| CIDX-08 | Phase 123 | Complete |`
- Line 162: `| CIDX-10 | Phase 123 | Pending |` → `| CIDX-10 | Phase 123 | Complete |`

From .planning/STATE.md:
- Frontmatter line 11: `completed_phases: 4` → `completed_phases: 5`
- Frontmatter line 13: `percent: 80` → `percent: 100`
- Body line 37 (under "## Current Position"): currently says something like "Phase 122 was final phase"
  → update to reflect Phase 123 as the actual completed final phase (e.g., "Phase 123 complete — v1.18 milestone closed")
  Confirm exact line/wording by reading STATE.md:34-40 before editing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run mandatory verification gates (D-15..D-18)</name>
  <files></files>
  <read_first>
    - .planning/phases/123-dx-closeout-proof-0-plans/123-VALIDATION.md (per-task verification map; automated commands)
    - .planning/phases/122-browser-demo-integration-determinism-0-plans/122-VERIFICATION.md (lines 89-91 — cite-as-skipped precedent for honest-non-execution)
    - .planning/phases/123-dx-closeout-proof-0-plans/123-CONTEXT.md (D-15..D-18 exact scope)
  </read_first>
  <action>
    Run the two mandatory verification gates in order. Record all outputs in the SUMMARY. Do NOT run the skipped-by-design lanes — record them explicitly as cited-not-executed per D-16/D-17.

    GATE 1 (mandatory per D-15): `bash scripts/ci/lint.sh`
    Purpose: doc/brand/asset guards green after all Wave 1+2 planning and MAINTAINING.md edits.
    Expectation: exits 0. If it fails, fix the failing guard before proceeding to Gate 2.

    GATE 2 (mandatory per D-15 — the central footgun): `cd rulestead && mix deps.get && mix test test/rulestead/release_contract_test.exs`
    Purpose: The doc-drift guard. `release_contract_test.exs` asserts MAINTAINING.md content including the D-14 guard assertions added in Wave 2. This guard lives OUTSIDE `lint.sh` — running only lint.sh would claim "verified" while leaving the doc-drift guard unrun. This is the step that must be run explicitly.
    Expectation: exits 0. If it fails with a DB error, run `cd rulestead && mix ecto.create` first, then retry. Do NOT run `ecto.create` pre-emptively (D-18 — the test is `async: true` file-content asserts with no DB dependency).

    GATE 3 (conditional per D-15): `cd rulestead && mix verify.adopter`
    Run ONLY IF any adopter-facing guide (README, UPGRADING.md, quickstart guide) was modified in any Wave 1 or 2 task. If only MAINTAINING.md shift-left section and the closeout ledger changed, this gate is skipped as not-applicable (not silently omitted — record the decision in the SUMMARY).

    CITED-AS-NOT-RUNNABLE (D-17 — explicitly record in SUMMARY, do NOT silently skip):
    - `publish-hex` / `mix hex.publish` — irreversible, gated, no version change in this phase
    - `verify-published-release` / `mix verify.release_publish` — live hex.pm network; `published_hex_smoke` stays opt-in via `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE`
    - Live branch-protection / `gh api` reconciliation — docs-only, no gh-api writes (119 handoff:316)
    - CI matrix timing / `release_gate` aggregation — observable only in GitHub Actions; mirrored locally by lint+test

    CITED-AS-SKIPPED-BY-DESIGN (D-16 — explicitly record in SUMMARY):
    - Mounted/openfeature companion proofs — no signal for a docs-only diff
    - DB-backed product suites — no signal for a docs-only diff
    - Demo backend — no signal for a docs-only diff

    This mirrors the honest-non-execution posture from `122-VERIFICATION.md:89-91`.
  </action>
  <verify>
    <automated>bash scripts/ci/lint.sh</automated>
    Must exit 0.
    <automated>cd rulestead && mix deps.get && mix test test/rulestead/release_contract_test.exs</automated>
    Must exit 0.
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/lint.sh` exits 0.
    - `cd rulestead && mix deps.get && mix test test/rulestead/release_contract_test.exs` exits 0.
    - Both gate results (command + exit code + key output) recorded in the plan SUMMARY.
    - Cited-as-not-runnable lanes (publish-hex, verify-published-release, gh api, CI matrix timing) explicitly listed as not-runnable with reason.
    - Cited-as-skipped-by-design lanes (mounted proof, DB suites, demo) explicitly listed as skipped with reason.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Flip CIDX-08/CIDX-10 to Complete and update ROADMAP + STATE (D-19/D-20)</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 27-28 inline checkboxes, lines 63 and 65 traceability table — read before editing to confirm exact current wording and line numbers)
    - .planning/ROADMAP.md (line 17 checklist entry, line 147 progress table row, lines 160 and 162 traceability table — read before editing)
    - .planning/STATE.md (frontmatter lines 11 and 13 for completed_phases/percent; lines 34-40 for the "Current Position" block containing the inaccurate stopped_at / final-phase statement — read before editing to confirm exact wording)
  </read_first>
  <action>
    Make targeted edits to three files per D-19 and D-20. Run verification gate (Task 1) MUST have passed before these edits — these are the "done-state" writes that make the milestone internally consistent.

    REQUIREMENTS.md edits (D-19):
    - Line 27: `- [ ] **CIDX-08**:` → `- [x] **CIDX-08**:`
    - Line 28: `- [ ] **CIDX-10**:` → `- [x] **CIDX-10**:`
    - Line 63: change `Pending` to `Complete` in the CIDX-08 traceability row
    - Line 65: change `Pending` to `Complete` in the CIDX-10 traceability row

    ROADMAP.md edits (D-19 + plan count):
    - Line 17 (phase checklist): change `- [ ] Phase 123: DX + Closeout Proof (0/? plans)` to `- [x] Phase 123: DX + Closeout Proof (3/3 plans) (completed 2026-06-17)`
    - Line 147 (progress table): change `0/?` to `3/3`, `Pending` to `Complete`, `—` to `2026-06-17`
    - Line 160: change CIDX-08 status from `Pending` to `Complete`
    - Line 162: change CIDX-10 status from `Pending` to `Complete`

    STATE.md edits (D-20):
    - Frontmatter: `completed_phases: 4` → `completed_phases: 5`
    - Frontmatter: `percent: 80` → `percent: 100`
    - Body "Current Position" block: find the line(s) that say the equivalent of "Phase 122 was final phase" or describe Phase 122 as the current/final position. Read the block first; correct it to reflect Phase 123 as the actual completed final phase and v1.18 CI/CD Reliability as the closed milestone (e.g., update "Phase: 122" to "Phase: 123", "Plan: Not started" to "Plans: 3/3 complete", "Status: Milestone complete" — read the exact current wording before editing to avoid introducing inconsistency).

    After edits: the `completed_phases / percent / current position` triplet must be self-consistent: 5/100/Phase 123 complete.
  </action>
  <verify>
    <automated>grep "completed_phases\|percent:" .planning/STATE.md</automated>
    Must show `completed_phases: 5` and `percent: 100`.
    <automated>grep -A1 "CIDX-08\|CIDX-10" .planning/REQUIREMENTS.md</automated>
    Both rows must show `Complete`.
    <automated>grep "Phase 123" .planning/ROADMAP.md | grep "\[x\]"</automated>
    Must match (Phase 123 checklist entry marked done).
    <automated>grep -A1 "CIDX-08\|CIDX-10" .planning/ROADMAP.md</automated>
    Both rows must show `Complete`.
  </verify>
  <acceptance_criteria>
    - `REQUIREMENTS.md` traceability table shows both CIDX-08 and CIDX-10 as `Complete`; inline checkboxes at lines 27-28 are `[x]`.
    - `ROADMAP.md` line 17 shows `[x] Phase 123: DX + Closeout Proof (3/3 plans) (completed 2026-06-17)`.
    - `ROADMAP.md` progress table row 147 shows `3/3 | Complete | 2026-06-17`.
    - `ROADMAP.md` traceability table rows 160 and 162 show `Complete` for CIDX-08 and CIDX-10 respectively.
    - `STATE.md` frontmatter has `completed_phases: 5` and `percent: 100`.
    - `STATE.md` current-position block reflects Phase 123 as the completed final phase; no line says "Phase 122 was final phase".
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification gate → STATE flip | STATE.md must not be flipped to 100% before both mandatory gates (lint.sh + release_contract_test.exs) exit 0 — a premature flip creates a false done signal for the next milestone kickoff |
| Planning docs → release trust | ROADMAP / REQUIREMENTS edits are declarative only; they do not change workflow YAML, release logic, or publish posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-08 | Repudiation | STATE.md flipped before verification gates pass | mitigate | Task ordering enforced: Task 1 (verification) must exit 0 before Task 2 (STATE flip) runs; SUMMARY records both gate results |
| T-123-09 | Tampering | Irreversible publish steps executed during verification | mitigate | D-17 cited-as-not-runnable protocol: publish-hex and verify-published-release are explicitly listed as not re-runnable in this environment; no mix hex.publish or live gh api writes in this plan |
| T-123-10 | Information Disclosure | STATE.md inaccuracy (completed_phases: 4 / percent: 80 / Phase 122 was final) persists to next milestone | mitigate | D-20 corrects all three fields atomically in Task 2; verification above confirms the values via grep |
| T-123-11 | Denial of Service | release_contract_test.exs D-14 guard assertions break on a Wave 2 typo | mitigate | Task 1 Gate 2 (`mix test release_contract_test.exs`) runs before STATE flip; if it fails, fix and re-run before proceeding to Task 2 |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; verification uses existing mix and bash tools only |

**Release-trust boundary affirmed (D-16/D-17):** Mounted/openfeature companion proofs, DB-backed product suites, demo backend, publish-hex, verify-published-release, and gh api branch-protection writes are explicitly cited-as-not-runnable or cited-as-skipped-by-design — never silently omitted. This mirrors `122-VERIFICATION.md:89-91`. The MAINTAINING.md triage table language ("release-trust gate, not a speed target") actively guards against the blind-rerun anti-pattern this posture is designed to counter.
</threat_model>

<verification>
Milestone gate — all four must pass before the SUMMARY is written:

1. `bash scripts/ci/lint.sh` — exits 0 (doc/brand/asset guards green)
2. `cd rulestead && mix deps.get && mix test test/rulestead/release_contract_test.exs` — exits 0 (doc-drift guard green; MANDATORY for MAINTAINING.md edit)
3. `grep "completed_phases\|percent:" .planning/STATE.md` — shows `completed_phases: 5` and `percent: 100`
4. `grep -A1 "CIDX-08\|CIDX-10" .planning/REQUIREMENTS.md .planning/ROADMAP.md` — all four rows show `Complete`
</verification>

<success_criteria>
- Both mandatory verification gates exit 0 with results recorded in SUMMARY.
- Cited-as-not-runnable and cited-as-skipped-by-design lanes are explicitly listed (not silently omitted).
- REQUIREMENTS.md, ROADMAP.md, and STATE.md reflect the milestone done-state: CIDX-08/CIDX-10 Complete; Phase 123 complete with 3/3 plans; completed_phases: 5; percent: 100.
- STATE.md no longer contains any statement that Phase 122 is the final phase.
- v1.18 CI/CD Reliability milestone is internally consistent and ready for next milestone kickoff.
</success_criteria>

<output>
Create `.planning/phases/123-dx-closeout-proof-0-plans/123-03-SUMMARY.md` when done.
</output>
