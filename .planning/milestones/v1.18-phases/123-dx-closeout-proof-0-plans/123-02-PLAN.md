---
phase: "123"
plan: "02"
type: execute
wave: 2
depends_on:
  - "123-01"
files_modified:
  - MAINTAINING.md
  - rulestead/test/rulestead/release_contract_test.exs
autonomous: true
requirements:
  - CIDX-08
  - CIDX-10

must_haves:
  truths:
    - "MAINTAINING.md shift-left gate section names the canonical command ladder (fast = cd rulestead && mix ci; full = bash scripts/ci/local.sh; reruns = mix verify.adopter + RULESTEAD_TEST_SCOPE=... bash scripts/ci/test.sh)"
    - "MAINTAINING.md contains a ## CI Failure Triage section with a single 6-column table"
    - "Triage table rows are ordered in release_gate pipeline order: lint → test → integration-placeholder → adopter-contract → mounted-proof → openfeature-companion → publish-hex → verify-published-release → repo-hygiene"
    - "Each triage row leads with the immutable ci.yml job id"
    - "Rerun commands in the triage table match the microcopy in scripts/ci/test.sh verbatim"
    - "release_contract_test.exs passes after MAINTAINING.md edits (D-14 guard assertions present)"
  artifacts:
    - path: "MAINTAINING.md"
      provides: "Command ladder statement + CI Failure Triage table"
      contains: "## CI Failure Triage"
    - path: "rulestead/test/rulestead/release_contract_test.exs"
      provides: "D-14 anti-drift guard: asserts triage section heading + mounted-proof and openfeature-companion job ids + scope-wrapper pattern present in MAINTAINING.md"
  key_links:
    - from: "MAINTAINING.md ## CI Failure Triage"
      to: "scripts/ci/test.sh print_mounted_failure_guidance:67-90"
      via: "verbatim microcopy lift per D-12"
      pattern: "RULESTEAD_TEST_SCOPE=mounted_admin_contract bash scripts/ci/test.sh"
    - from: "rulestead/test/rulestead/release_contract_test.exs"
      to: "MAINTAINING.md"
      via: "File.read!(@maintaining_path) assert maintaining =~ pattern"
      pattern: "assert maintaining =~.*CI Failure Triage"
---

<objective>
Add the canonical command ladder statement and CI Failure Triage table to the `MAINTAINING.md` shift-left gate section (D-07/D-08/D-10..D-13), then extend `release_contract_test.exs` with the D-14 anti-drift guard assertions.

Purpose: Give contributors a single authoritative lookup table for failing CI lanes; protect it from silent rot.

Output: `MAINTAINING.md` with ladder statement + 9-row triage table in a new `## CI Failure Triage` section; `release_contract_test.exs` extended with 3-5 `assert maintaining =~` guard lines.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-dx-closeout-proof-0-plans/123-CONTEXT.md
@.planning/phases/123-dx-closeout-proof-0-plans/123-RESEARCH.md
@.planning/phases/123-dx-closeout-proof-0-plans/123-01-SUMMARY.md

<interfaces>
<!-- Key sections the executor must read before editing. -->

From MAINTAINING.md (current state):
- :32-61  — branch-protection section (DO NOT TOUCH)
- :63-78  — cache table section (DO NOT TOUCH)
- :80-106 — "## Shift-left contributor gate" section (NARROW EDIT TARGET: add ladder statement here per D-08)
             Current content: fast loop `cd rulestead && mix ci`, full gate `bash scripts/ci/local.sh`,
             `--fast` flag, repo hygiene commands block, final paragraph about `mix ci` covering
             format/compile/credo/tests/docs and local gates catching failures earlier.
- :106    — end of shift-left section; insert `## CI Failure Triage` immediately after this section.
- :121-228 — release runbook (DO NOT TOUCH)

From scripts/ci/test.sh (microcopy to lift verbatim per D-12):
- print_mounted_failure_guidance at lines 67-90:
    "mounted_admin_contract failure category: ${category}"
    "Expected support boundary: mounted companion only; host app owns the router/session prerequisite contract."
    "Rerun: RULESTEAD_TEST_SCOPE=mounted_admin_contract bash scripts/ci/test.sh"
- run_openfeature_companion at lines 124-129:
    scope rerun: `RULESTEAD_TEST_SCOPE=openfeature_companion bash scripts/ci/test.sh`

From .github/workflows/ci.yml (release_gate.needs at lines 318-325):
  job ids in pipeline order: lint, test, integration-placeholder, adopter-contract, mounted-proof,
  openfeature-companion. Additional separate-workflow lanes: publish-hex, verify-published-release, repo-hygiene.

From .planning/phases/123-dx-closeout-proof-0-plans/123-RESEARCH.md (Triage Lane Inventory at lines 183-193):
  Exact rerun commands per lane already verified:
  - lint:                    bash scripts/ci/lint.sh
  - test:                    bash scripts/ci/contributor.sh (fast) or bash scripts/ci/local.sh (full)
  - integration-placeholder: bash scripts/demo/verify.sh
  - adopter-contract:        cd rulestead && mix verify.adopter
  - mounted-proof:           RULESTEAD_TEST_SCOPE=mounted_admin_contract bash scripts/ci/test.sh
  - openfeature-companion:   RULESTEAD_TEST_SCOPE=openfeature_companion bash scripts/ci/test.sh
  - publish-hex:             NOT re-runnable (cite as irreversible gate)
  - verify-published-release: bash scripts/ci/verify_published_release.sh <version> (post-publish, live network)
  - repo-hygiene:            ./scripts/maintainer/repo_hygiene_check.sh

From rulestead/test/rulestead/release_contract_test.exs (existing assertion pattern at lines 272-299):
  test "maintainer guidance matches the shipped release and support truth" do
    maintaining = File.read!(@maintaining_path)
    assert maintaining =~ "RULESTEAD_TEST_SCOPE=mounted_admin_contract bash scripts/ci/test.sh"
    assert maintaining =~ "integration-placeholder"
    ...
  end
  The D-14 guard assertions are added inside this same test function, following the same pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add command ladder statement + CI Failure Triage table to MAINTAINING.md (D-07/D-08/D-10..D-13)</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md (lines 80-120 — full shift-left gate section and the paragraph immediately after it; confirm exact line boundaries before editing)
    - scripts/ci/test.sh (lines 67-90 — print_mounted_failure_guidance verbatim microcopy; lines 124-129 — run_openfeature_companion scope)
    - .github/workflows/ci.yml (lines 316-325 — release_gate.needs job id list; confirms ordering)
    - .planning/phases/119-baseline-expert-audit-0-plans/119-CI-CD-AUDIT.md (lines 249-257 — D-20 microcopy contract 5-slot table shape)
    - .planning/phases/123-dx-closeout-proof-0-plans/123-RESEARCH.md (lines 183-193 — verified rerun commands per lane)
  </read_first>
  <action>
    Make TWO narrow additions to MAINTAINING.md (D-08). All edits are within or immediately after the shift-left gate section (:80-106). Do NOT modify lines :32-61 (branch-protection), :63-78 (cache table), or :121-228 (release runbook).

    ADDITION 1 — Command ladder statement inside the shift-left gate section (:80-106):

    After the existing `## Shift-left contributor gate` code block (the block ending with `scripts/demo/proof.sh`), add a short prose paragraph that names the canonical command ladder per D-07:
    - Fast local loop: `cd rulestead && mix ci` (the named alias for `scripts/ci/contributor.sh` — covers format, compile, Credo, tests, and docs for the core package).
    - Full local gate: `bash scripts/ci/local.sh` (`--fast` skips mounted and OpenFeature companion scopes).
    - CI lane reruns: `cd rulestead && mix verify.adopter` for the adopter-contract lane; `RULESTEAD_TEST_SCOPE=<scope> bash scripts/ci/test.sh` for proof scopes (see CI Failure Triage section below).
    - State that `mix ci` is the named alias for `bash scripts/ci/contributor.sh` so contributors see one canonical fast-loop command.
    - Mention post-Phase 121: default suite is ~5s (dominant slow test is now behind `@tag :published_hex_smoke` / `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE` for the opt-in lane).
    Keep this paragraph concise (4-6 lines). Imperative/factual tone per D-13.

    ADDITION 2 — New `## CI Failure Triage` section (D-10):

    Insert this section AFTER the shift-left gate section (:106), BEFORE the release runbook (:121).

    The section contains a single markdown table with these 6 columns (D-10):
    `Lane (ci.yml job id) | What failed | Boundary it protects | Exact rerun | Likely remediation | When to stop rather than bypass`

    9 rows in release_gate pipeline order (D-11):

    Row 1 — `lint`:
    - What failed: lint, format, compile, Credo, Dialyzer, or Python guards (brand/token/asset/evidence)
    - Boundary: Code quality and doc-drift — changes that break contracts surface here before they hit the test matrix
    - Exact rerun: `bash scripts/ci/lint.sh`
    - Likely remediation: Fix format (`mix format`), address compile warning, resolve Credo violation, or fix the guard that failed (brand/token/asset/evidence guards name the file and assertion in stdout)
    - When to stop: Stop before bypass if Dialyzer or `release_contract_test.exs` assertions break — those guard the published API contract and doc-drift

    Row 2 — `test`:
    - What failed: Core or matrix test failure (`async: false` or `async: true` modules)
    - Boundary: Core package correctness across Elixir 1.17.3/OTP 26.2.5 and 1.19.2/OTP 28.4.3
    - Exact rerun: `cd rulestead && mix ci` (fast; excludes slow proof scopes)
    - Likely remediation: Run `cd rulestead && mix test --failed` to isolate; check for Fake singleton or Application.put_env mutation in failing module
    - When to stop: Stop before bypass if the failure is in a contract, property, or integration test — those guard adopter/runtime boundaries

    Row 3 — `integration-placeholder`:
    - What failed: FleetDesk Playwright / demo compose proof
    - Boundary: Compose-backed browser adoption proof — FleetDesk launcher URL, backend CORS, frontend-to-backend round trip
    - Exact rerun: `bash scripts/demo/verify.sh`
    - Likely remediation: Check `DEMO_BACKEND_URL`/`DEMO_FRONTEND_URL` env; ensure Docker health polling passes before Playwright runs
    - When to stop: Stop before bypass if the demo compose stack fails to start — a green test run against a dead backend is misleading

    Row 4 — `adopter-contract`:
    - What failed: Adopter-contract proof (`RULESTEAD_TEST_SCOPE=post_ga_band_closure`)
    - Boundary: Post-GA adopter contract — installability, API boundary, and support-truth docs
    - Exact rerun: `cd rulestead && mix verify.adopter`
    - Likely remediation: Run `cd rulestead && mix verify.adopter` and inspect which contract test failed; check if a doc path (README, MAINTAINING) was edited without running `release_contract_test.exs`
    - When to stop: Stop before bypass if any adopter-contract assertion fails — this gate directly protects published API and support-truth docs

    Row 5 — `mounted-proof`:
    - What failed: Mounted companion router/session/admin boundary (lifted verbatim from print_mounted_failure_guidance:67-90)
    - Boundary: Mounted companion only; host app owns the router/session prerequisite contract (verbatim from scripts/ci/test.sh:73)
    - Exact rerun: `RULESTEAD_TEST_SCOPE=mounted_admin_contract bash scripts/ci/test.sh` (verbatim from scripts/ci/test.sh:74)
    - Likely remediation: Inspect the mounted lifecycle, route, or permission contract regression from the raw failure output; if `setup/prerequisite failure`, install deps for both sibling packages first
    - When to stop: `mounted-proof` is a release-trust gate, not a speed target — a red here means the mounted companion lifecycle contract is broken; do not bypass

    Row 6 — `openfeature-companion`:
    - What failed: OpenFeature provider compatibility
    - Boundary: OpenFeature provider compatibility with the core Rulestead runtime
    - Exact rerun: `RULESTEAD_TEST_SCOPE=openfeature_companion bash scripts/ci/test.sh` (verbatim from scripts/ci/test.sh line 127 usage)
    - Likely remediation: Check `open_feature_rulestead/` for context-mapper or provider contract drift; run `cd open_feature_rulestead && mix deps.get && mix test` for focused isolation
    - When to stop: OpenFeature companion failure means published OpenFeature integration does not satisfy the provider contract; stop before bypass

    Row 7 — `publish-hex`:
    - What failed: Hex publish (irreversible)
    - Boundary: Gated Hex publish with protected `hex-publish` environment approval; `rulestead` publishes before `rulestead_admin`
    - Exact rerun: NOT re-runnable — `publish-hex` is an irreversible release gate requiring explicit maintainer approval in the `hex-publish` environment
    - Likely remediation: Review publish logs; if the publish partially succeeded (one sibling published), follow the linked-version release runbook in the `## Release Please flow` section
    - When to stop (D-13 tone): `publish-hex` is a release-trust gate, not a speed target — a red here means published artifacts may not satisfy the install/mount contract; cut a corrected release rather than republishing from an untagged commit

    Row 8 — `verify-published-release`:
    - What failed: Post-publish verification (live hex.pm network)
    - Boundary: Published Hex package installability and linked-version proof
    - Exact rerun: `bash scripts/ci/verify_published_release.sh <version>` (post-publish, live network — requires `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1` for the published-Hex smoke test)
    - Likely remediation: Check hex.pm for the published package; run `bash scripts/ci/verify_published_release.sh <version>` locally if hex.pm propagation is delayed
    - When to stop: Stop and investigate if `verify-published-release` fails — a broken post-publish verification means adopters may encounter install or compile failures

    Row 9 — `repo-hygiene`:
    - What failed: Weekly scheduled repo hygiene checks
    - Boundary: Repo health — dependency freshness, security advisories, Dependabot coverage
    - Exact rerun: `./scripts/maintainer/repo_hygiene_check.sh`
    - Likely remediation: Address the flagged dependency or advisory; check Dependabot for available bumps
    - When to stop: Repo-hygiene failures are advisory for scheduled runs; stop before suppressing if a security advisory affects a published package

    Microcopy tone per D-13: imperative, factual, boundary-anchored. "When to stop" is a release-trust fact, not a warning or suggestion.
  </action>
  <verify>
    <automated>grep -A2 "CI Failure Triage" MAINTAINING.md</automated>
    Must show the section heading and the table header line.
    <automated>grep -c "Lane (ci.yml job id)" MAINTAINING.md</automated>
    Must return 1 (exactly one triage table).
    <automated>grep "mounted-proof\|openfeature-companion\|publish-hex\|verify-published-release\|repo-hygiene" MAINTAINING.md | wc -l</automated>
    Must be >= 5 (all five non-core job ids present in the triage table).
    <automated>grep "RULESTEAD_TEST_SCOPE=mounted_admin_contract bash scripts/ci/test.sh" MAINTAINING.md</automated>
    Must match (verbatim microcopy from scripts/ci/test.sh:74 per D-12).
    <automated>grep "RULESTEAD_TEST_SCOPE=openfeature_companion bash scripts/ci/test.sh" MAINTAINING.md</automated>
    Must match (verbatim OpenFeature scope wrapper per D-12).
    <automated>grep "cd rulestead && mix ci" MAINTAINING.md</automated>
    Must match (ladder statement per D-07).
  </verify>
  <acceptance_criteria>
    - MAINTAINING.md has a new `## CI Failure Triage` section (inserted between the shift-left gate section and the release runbook; does not overlap with :32-61, :63-78, or :121-228).
    - The triage table has exactly 6 columns: `Lane (ci.yml job id) | What failed | Boundary it protects | Exact rerun | Likely remediation | When to stop rather than bypass`.
    - Rows appear in pipeline order: lint → test → integration-placeholder → adopter-contract → mounted-proof → openfeature-companion → publish-hex → verify-published-release → repo-hygiene (9 rows).
    - Each row leads with the immutable ci.yml job id.
    - `mounted-proof` row rerun command matches `scripts/ci/test.sh:74` verbatim: `RULESTEAD_TEST_SCOPE=mounted_admin_contract bash scripts/ci/test.sh`.
    - `openfeature-companion` row rerun command matches `scripts/ci/test.sh` verbatim: `RULESTEAD_TEST_SCOPE=openfeature_companion bash scripts/ci/test.sh`.
    - `publish-hex` and `verify-published-release` "when to stop" cells state that these are release-trust gates not speed targets.
    - Shift-left gate section now includes a command ladder paragraph naming `cd rulestead && mix ci` as the fast-loop alias and post-121 ~5s default suite posture.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add D-14 anti-drift guard to release_contract_test.exs</name>
  <files>rulestead/test/rulestead/release_contract_test.exs</files>
  <read_first>
    - rulestead/test/rulestead/release_contract_test.exs (lines 272-299 — existing test "maintainer guidance matches the shipped release and support truth"; this is the exact test to extend)
    - MAINTAINING.md (confirm the exact strings the assertions will match — run after Task 1 so the section heading and job ids are present)
  </read_first>
  <action>
    Extend the existing test `"maintainer guidance matches the shipped release and support truth"` in `release_contract_test.exs` (lines 272-299) with 4-5 additional `assert maintaining =~` lines per D-14 (Option A). Follow the identical pattern already used at lines 283-294.

    Add these assertions inside the existing test block, after the current assertions on `maintaining`:

    1. `assert maintaining =~ "## CI Failure Triage"` — guards the triage section heading (highest drift risk: section could be renamed or moved)
    2. `assert maintaining =~ "mounted-proof"` — guards the mounted-proof job id (path-gated, most likely to be renamed)
    3. `assert maintaining =~ "openfeature-companion"` — guards the openfeature-companion job id (path-gated, most likely to be renamed)
    4. `assert maintaining =~ "RULESTEAD_TEST_SCOPE=openfeature_companion bash scripts/ci/test.sh"` — guards the scope-wrapper rerun command pattern (single source of truth with scripts/ci/test.sh)
    5. `assert maintaining =~ "release-trust gate"` — guards the "when to stop" boundary framing in the publish-hex and verify-published-release rows

    Do NOT add any new test functions, new module attributes, or `use` statements. The D-14 guard is 4-5 lines inside the existing test. The module is `async: true` and has no DB dependencies — it must remain that way.

    After adding the assertions, the test must still pass by construction (Task 1 already added all the content these assertions check). Verify with the explicit targeted run in the verification step.
  </action>
  <verify>
    <automated>cd rulestead && mix deps.get && mix test test/rulestead/release_contract_test.exs</automated>
    Must exit 0. This is the load-bearing D-15 doc-drift verification step — do NOT skip or substitute with lint.sh.
    <automated>grep "CI Failure Triage\|mounted-proof\|openfeature-companion\|release-trust gate" rulestead/test/rulestead/release_contract_test.exs | wc -l</automated>
    Must be >= 4 (confirms all D-14 guard assertions are present in the source file).
  </verify>
  <acceptance_criteria>
    - `release_contract_test.exs` test "maintainer guidance matches the shipped release and support truth" now contains `assert maintaining =~ "## CI Failure Triage"`.
    - Guards for `"mounted-proof"`, `"openfeature-companion"`, `"RULESTEAD_TEST_SCOPE=openfeature_companion bash scripts/ci/test.sh"`, and `"release-trust gate"` are present.
    - No new test functions, module attributes, or `use`/`import` statements were added.
    - `cd rulestead && mix deps.get && mix test test/rulestead/release_contract_test.exs` exits 0.
    - The module still uses `async: true` and has no DB dependencies.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MAINTAINING.md triage microcopy → reader trust | If the triage table names wrong commands, a maintainer during a CI outage may issue a wrong rerun, wasting time or mis-scoping the fix |
| release_contract_test.exs → MAINTAINING.md | File-content assertions guard against doc drift; if assertions are too loose they provide false assurance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-04 | Tampering | MAINTAINING.md triage rerun commands rotting out of sync with scripts/ci/test.sh | mitigate | D-12 lifts microcopy verbatim from scripts/ci/test.sh functions; D-14 guard in release_contract_test.exs asserts the scope-wrapper pattern stays present |
| T-123-05 | Tampering | Protected MAINTAINING.md sections (:32-61 branch-protection, :63-78 cache, :121-228 release runbook) inadvertently edited | mitigate | D-08 specifies the narrow edit boundary explicitly; release_contract_test.exs existing assertions catch accidental doc-drift regressions |
| T-123-06 | Repudiation | D-14 guard too weak to catch meaningful triage table rot | mitigate | Guard asserts section heading + both path-gated job ids (highest rename risk) + scope-wrapper pattern + "release-trust gate" phrase; covers the four most drift-prone surface tokens |
| T-123-07 | Elevation of Privilege | Blind-rerun anti-pattern encouraged by vague "when to stop" column | mitigate | D-13 microcopy is imperative and names specific trust boundaries: publish-hex/verify-published-release explicitly labeled "release-trust gate, not a speed target"; this directly counters the blind-rerun anti-pattern documented in 119-CI-CD-AUDIT.md:257 |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; ExUnit test extension follows existing file-content assertion pattern |

**Release-trust boundary affirmed (D-16/D-17):** No workflow YAML, cache keys, action pins, permissions, or publish steps are touched in this wave. The `publish-hex` and `verify-published-release` triage rows explicitly state irreversibility and call out the "not a speed target" posture — actively reinforcing rather than weakening the release-trust boundary.
</threat_model>

<verification>
After both tasks complete:

1. `grep -A2 "CI Failure Triage" MAINTAINING.md` — shows section heading and table header
2. `grep "RULESTEAD_TEST_SCOPE=mounted_admin_contract bash scripts/ci/test.sh" MAINTAINING.md` — matches
3. `grep "RULESTEAD_TEST_SCOPE=openfeature_companion bash scripts/ci/test.sh" MAINTAINING.md` — matches
4. `grep "cd rulestead && mix ci" MAINTAINING.md` — matches (command ladder statement)
5. `cd rulestead && mix deps.get && mix test test/rulestead/release_contract_test.exs` — exits 0 (MANDATORY — the D-15 doc-drift guard, not covered by lint.sh)
6. `bash scripts/ci/lint.sh` — exits 0 (doc/brand/asset guards green)
</verification>

<success_criteria>
- MAINTAINING.md has the canonical command ladder statement in the shift-left gate section and a new 9-row `## CI Failure Triage` table with 6 columns.
- Triage table rows are in pipeline order, each led by the immutable ci.yml job id, with verbatim microcopy from scripts/ci/test.sh.
- `release_contract_test.exs` extends the existing maintainer-guidance test with 4-5 D-14 guard assertions; the test exits 0.
- No protected MAINTAINING.md sections were touched; no behavioral changes to any script or workflow.
</success_criteria>

<output>
Create `.planning/phases/123-dx-closeout-proof-0-plans/123-02-SUMMARY.md` when done.
</output>
