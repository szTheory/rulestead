---
phase: 121-mix-exunit-performance-test-value-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-ASYNC-AUDIT.md
autonomous: true
requirements: [CIDX-06]
must_haves:
  truths:
    - "Every async:false RepoCase candidate has a documented flip/keep verdict with cited hazard evidence"
    - "No module is marked async:true without proven absence of global-state/DB-ownership hazards"
    - "The known-serial trio is explicitly preserved, not flipped"
    - "Any flip (if made) is proven flake-free under async by running it twice"
  artifacts:
    - path: ".planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-ASYNC-AUDIT.md"
      provides: "Per-module async verdict record with greppable hazard evidence and net-flip count"
      contains: "KEEP SERIAL"
  key_links:
    - from: "121-ASYNC-AUDIT.md verdicts"
      to: "RESEARCH.md Per-Module Async Audit table"
      via: "per-module hazard citation"
      pattern: "code_refs_plug_test"
---

<objective>
Execute the evidence-gated async audit (D-01/D-02): for every `async: false`
module that uses `Rulestead.RepoCase`, record a flip/keep verdict backed by a
greppable hazard check. The research verdict is 0 recommended flips — every
RepoCase candidate carries a disqualifying global-state hazard except the lone
borderline `code_refs_plug_test.exs` (DDL-in-setup, recommended KEEP SERIAL).
A small net-new-async count (0–3, plausibly 0) is a SUCCESS, not a shortfall.

Purpose: Honors the milestone's correctness-first, narrow-change posture —
async is widened only where provably safe, never as a blind sweep. This produces
the documented audit that satisfies success criterion #1.
Output: `121-ASYNC-AUDIT.md` with per-module verdicts, plus 0 (recommended) or
at most 1 actual source flip, fully proven if made.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-CONTEXT.md
@.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-RESEARCH.md
@.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-PATTERNS.md

<interfaces>
<!-- The async-path safety contract and the hazard gate. Re-read before judging any module. -->

rulestead/test/support/repo_case.ex:18-28 — async path is per-process isolated (sets {:shared, self()} ONLY `unless tags[:async]`). RepoCase itself is NOT the blocker.

Greppable hazard gate (run from rulestead/ on each candidate; ANY hit = leave serial):
  grep -nE "Application\.(put_env|delete_env)" FILE          # process-global app env -> SERIAL
  grep -nE "Rulestead\.Fake|Fake\.Control|@adapters .*Fake|Fake\.reset" FILE  # global named singleton -> SERIAL
  grep -nE ":telemetry\.(attach|attach_many)" FILE           # telemetry handler -> SERIAL
  grep -nE "capture_log|capture_io" FILE                     # log/IO capture -> SERIAL
  grep -nE "System\.cmd" FILE                                # subprocess -> SERIAL
  grep -nE "File\.(write|mkdir|rm|cp|touch)" FILE            # filesystem write -> SERIAL
  grep -nE ":ets\.|Process\.register|start_supervised!\(\{?[A-Z]" FILE  # named/ETS singleton -> SERIAL
  grep -nE "CREATE TABLE|ALTER TABLE|DROP TABLE|Repo\.query!\(.*CREATE" FILE  # DDL on sandbox conn -> SERIAL (judgment)

Clean async shape (the ONLY qualifying header): `use Rulestead.RepoCase, async: true` with none of the above hits — see analytics/query_test.exs:2.

RESEARCH.md Per-Module Async Audit (lines 277-323): pre-verified verdict table for all 23 async:false RepoCase candidates — every one KEEP SERIAL except code_refs_plug_test.exs (KEEP SERIAL recommended; FLIP only with documented DDL-under-async justification + twice-run flake check).

Do-not-flip trio (CONTEXT D-02): oban/stale_flag_worker_test.exs, analytics/batcher_test.exs, webhooks/inbound_http_test.exs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the greppable hazard gate and record per-module verdicts</name>
  <files>.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-ASYNC-AUDIT.md</files>
  <read_first>
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-RESEARCH.md (Per-Module Async Audit table lines 277-323, greppable methodology lines 282-292, Pitfalls 3-4 lines 231-238)
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-PATTERNS.md (correctness-first conservatism + code_refs_plug section, lines 165-207)
    - rulestead/test/support/repo_case.ex (the async-path safety contract, lines 18-28)
    - rulestead/test/rulestead/analytics/query_test.exs (the clean async header analog, lines 1-6)
  </read_first>
  <action>
    Independently re-confirm the audit rather than trusting the table blind: from `rulestead/`, enumerate the modules that `use Rulestead.RepoCase` and are `async: false` (or have no flag = serial). For each, run the greppable hazard gate commands listed in the interfaces block. ANY hit = KEEP SERIAL with the specific hazard (grep line) cited. Cross-check against RESEARCH.md's pre-verified table (lines 294-321) and reconcile any discrepancy by reading the actual file.

    Write `121-ASYNC-AUDIT.md` capturing: (1) the candidate universe count and method; (2) a per-module verdict table (module | disqualifying hazard with file:line | KEEP SERIAL / FLIP); (3) explicit confirmation the do-not-flip trio (oban/stale_flag_worker_test.exs, analytics/batcher_test.exs, webhooks/inbound_http_test.exs) is preserved; (4) the special-case verdict for `webhooks/code_refs_plug_test.exs` (the only no-Fake/no-app-env/no-telemetry candidate — but `Repo.query!("CREATE TABLE IF NOT EXISTS ...")` DDL in setup at ~lines 188-197 = the "DB ownership" disqualifier D-01 names; recommended verdict KEEP SERIAL per D-02 "when in doubt, leave serial"); (5) the net-flip count and an explicit statement that 0–3 (plausibly 0) is the intended, successful outcome (D-02), not a shortfall; (6) the pre-existing latent-risk observations (store/webhook_adapter_contract_test.exs and webhook_outbound_contract_test.exs are ALREADY async:true yet call Fake.reset — note only, out of scope to change per RESEARCH Pitfall 3). Do NOT flip any source file in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-ASYNC-AUDIT.md && grep -q "KEEP SERIAL" .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-ASYNC-AUDIT.md && grep -q "code_refs_plug_test" .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-ASYNC-AUDIT.md && grep -Eq "stale_flag_worker_test|batcher_test|inbound_http_test" .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-ASYNC-AUDIT.md && echo "OK: audit complete with verdicts, borderline case, and trio"</automated>
  </verify>
  <acceptance_criteria>
    - Source: 121-ASYNC-AUDIT.md exists and contains a per-module verdict for every async:false RepoCase candidate, each KEEP-SERIAL verdict citing a specific hazard at file:line.
    - Source: the audit explicitly lists the do-not-flip trio (oban/stale_flag_worker_test.exs, analytics/batcher_test.exs, webhooks/inbound_http_test.exs) and confirms they are NOT flipped.
    - Source: the audit gives an explicit verdict for `webhooks/code_refs_plug_test.exs` referencing the `CREATE TABLE IF NOT EXISTS` DDL-in-setup hazard, with the recommended KEEP SERIAL rationale.
    - Source: the audit states the net-flip count and frames 0–3 (plausibly 0) as a success per D-02.
    - Behavior: running the greppable hazard gate against any module the audit marks KEEP SERIAL produces at least one match for the cited hazard pattern.
  </acceptance_criteria>
  <done>Every candidate has an evidence-backed verdict; trio preserved; borderline case decided; net-flip count recorded as an intended success; no unsafe flip made.</done>
</task>

<task type="auto">
  <name>Task 2: Apply zero or the single defensible flip — and prove it flake-free if applied</name>
  <files>.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-ASYNC-AUDIT.md</files>
  <read_first>
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-ASYNC-AUDIT.md (the verdicts produced in Task 1)
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-RESEARCH.md (Open Question 2 lines 359-362, Pitfall 4 lines 235-238, Assumption A1 line 449)
    - rulestead/test/rulestead/webhooks/code_refs_plug_test.exs (ONLY if the audit recommends flipping it — read the full setup + DDL block before any change)
  </read_first>
  <action>
    Default action (recommended by research): apply ZERO source flips. In that case, append a short "Decision" section to 121-ASYNC-AUDIT.md recording that 0 modules were flipped on correctness-first grounds (D-02), and that this satisfies success criterion #1 (async marked true only where provably safe — here, nowhere new was provably safe). No source file changes; no further verification beyond the existing default suite staying green.

    Conditional action (only if Task 1's audit makes the explicit, documented judgment to flip `webhooks/code_refs_plug_test.exs`): change ONLY its header from serial to `use Rulestead.RepoCase, async: true`. Then prove it flake-free by running the file under async TWICE consecutively (`cd rulestead && mix test test/rulestead/webhooks/code_refs_plug_test.exs --warnings-as-errors`, twice), both green. Record in 121-ASYNC-AUDIT.md: the DDL-under-async analysis (per-test transaction, IF NOT EXISTS idempotency, table provenance — test-local vs missing migration), both passing run outputs, and the explicit acceptance of Assumption A1's risk. Do NOT flip any module in the do-not-flip trio. Do NOT flip any module the audit marked KEEP SERIAL for a Fake/app-env/telemetry/capture/ETS/System.cmd/File hazard.
  </action>
  <verify>
    <automated>cd rulestead && mix test --warnings-as-errors --slowest 5 2>&1 | tail -5 | grep -Eq "0 failures|, 0 failures" && echo "OK: default suite green after async decision" || { echo "FAIL: suite not green"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `cd rulestead && mix test --warnings-as-errors` is green after the decision (0 failures).
    - Source: 121-ASYNC-AUDIT.md has a "Decision" section stating the actual net flips (0 recommended) and tying it to success criterion #1.
    - Conditional (only if a flip was made): the flipped file's header reads `use Rulestead.RepoCase, async: true`; the audit records two consecutive green async runs of that file and the DDL-under-async justification; NO other source file changed.
    - Negative: no module in the do-not-flip trio is async:true; no Fake/app-env/telemetry-hazard module was flipped.
  </acceptance_criteria>
  <done>Net flips applied match the audit decision (0 recommended); suite green; any flip is documented and proven flake-free twice; trio and hazard modules untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serial test (shared global state) → concurrent test scheduling | Flipping a module to async:true grants it a per-process sandbox connection but exposes any global state (named Fake, app-env, ETS, telemetry) to concurrent clobbering — an incorrect flip introduces nondeterministic failures masquerading as flakes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-03 | Tampering with signal (false flake from unsafe concurrency) | Any async:false RepoCase module flipped without hazard proof | mitigate | Evidence-gated allowlist (D-01): every flip requires a clean greppable hazard gate; recommended net flips = 0; the one borderline DDL-in-setup module defaults to KEEP SERIAL; any actual flip is proven by two consecutive green async runs. |
| T-121-SC | Tampering | npm/pip/cargo installs | accept | N/A — no external packages installed in this phase. |
</threat_model>

<verification>
1. `cd rulestead && mix test --warnings-as-errors` green (0 failures).
2. `121-ASYNC-AUDIT.md` lists a verdict for every candidate, preserves the trio, decides the borderline case, and records the net-flip count.
3. If any flip was made: two consecutive green async runs of the flipped file are recorded.
</verification>

<success_criteria>
- Async marked true only where proven free of global state / DB ownership / ports / filesystem / logger / telemetry / app-env mutation (success criterion #1; D-01).
- Small net-new-async count (0–3, plausibly 0) recorded as a success, not a shortfall (D-02).
- Default suite stays green; no demote of pre-existing async modules (RESEARCH Pitfall 3 noted only).
</success_criteria>

<output>
Create `.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-02-SUMMARY.md` when done. Record: net flips applied (expected 0), the borderline-module decision, and that the do-not-flip trio is preserved. (`121-ASYNC-AUDIT.md` is the durable evidence artifact.)
</output>
