---
phase: 121-mix-exunit-performance-test-value-cleanup
plan: 03
type: execute
wave: 2
depends_on: [121-01]
files_modified:
  - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-MEASUREMENT.md
autonomous: true
requirements: [CIDX-06]
must_haves:
  truths:
    - "Before/after slowest-test and wall-clock notes are recorded using the exact Phase 119 commands"
    - "mix test --partitions is explicitly rejected with evidence"
    - "No-module-split (D-05), no-Dialyzer-change (D-07), and the xref-cycle note (D-10) are recorded as decisions, not actions"
  artifacts:
    - path: ".planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-MEASUREMENT.md"
      provides: "Before/after wall-clock + slowest record, partitioning-rejection evidence, and the D-05/D-07/D-10 decision notes for Phase 123 closeout"
      contains: "slowest"
  key_links:
    - from: "121-MEASUREMENT.md before/after"
      to: "119-CI-CD-AUDIT.md baseline (~42s real, dominant ~27.95s)"
      via: "exact --slowest 25 / --slowest-modules 25 commands"
      pattern: "slowest-modules"
---

<objective>
Produce the phase's evidence record (D-09 + D-05 + D-06 + D-07 + D-10):
record before/after slowest-test and wall-clock numbers using the EXACT Phase 119
commands so the delta is directly comparable, document the evidenced rejection of
`mix test --partitions` (D-06), and capture the no-action decisions
(no module split D-05, no Dialyzer/PLT change D-07, xref-cycle note D-10).

Purpose: This is success criterion #5 (before/after notes in the phase summary)
and criterion #3 (partitioning rejected with evidence), and it feeds the Phase 123
closeout metrics. Runs in Wave 2 because the before/after delta requires Plan 01's
default-exclude to already be in place.
Output: `121-MEASUREMENT.md` — the durable measurement + decision record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-CONTEXT.md
@.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-RESEARCH.md
@.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-01-SUMMARY.md

<interfaces>
<!-- The locked D-09 measurement commands and the baseline to beat. -->

D-09 EXACT commands (use verbatim — 119-CI-CD-AUDIT.md:153-154):
  cd rulestead && mix test --warnings-as-errors --slowest 25
  cd rulestead && mix test --warnings-as-errors --slowest-modules 25
  Run BOTH: default (dominant test excluded after Plan 01) AND with RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1 (dominant test included = baseline-comparable).

Baseline to beat (119-CI-CD-AUDIT.md:144-154): ~42s real, 587 tests + 8 properties, dominant ~27.95s, 18 schedulers online. Expected post-change default ~14s.

D-06 partitioning rejection evidence (RESEARCH lines 325-332): single dominant serial network test (partitions distribute modules not intra-module work); suite overwhelmingly async:false via global Fake + process-global app-env; single Postgres sandbox + single named Fake would need per-partition DB/Fake isolation = fragility; no partition config in mix.exs; 18 schedulers already absorb the tiny async set. Mapped to FUT-01 (deferred, reversible).

D-05 (no module splits): next-slowest module ~1.6s after the dominant test is set aside; concurrency-benefit bar unmet.
D-07 (no Dialyzer/PLT change): Phase 120 already scoped the PLT key; Dialyzer runs in the lint lane, off the test wall-clock critical path.
D-10 (xref note): length-47 compile-connected cycle centered on lib/rulestead.ex — record as architectural evidence only, no refactor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record before/after wall-clock and slowest-test measurements (D-09)</name>
  <files>.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-01-SUMMARY.md (confirm the tag/env names actually used in Plan 01)
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-RESEARCH.md (D-09 commands lines 266-275, baseline lines 273-275)
    - .planning/phases/119-baseline-expert-audit-0-plans/119-CI-CD-AUDIT.md (lines 144-154 — the exact baseline numbers to compare against)
  </read_first>
  <action>
    Run the locked D-09 measurement commands verbatim and capture `real` wall-clock + the slowest-test/slowest-module lines for each of these four runs (use whatever env var name Plan 01 actually chose, confirmed from 121-01-SUMMARY.md):
    1. `cd rulestead && mix test --warnings-as-errors --slowest 25` (default = dominant EXCLUDED)
    2. `cd rulestead && mix test --warnings-as-errors --slowest-modules 25` (default = dominant EXCLUDED)
    3. `cd rulestead && RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1 mix test --warnings-as-errors --slowest 25` (dominant INCLUDED = baseline-comparable)
    4. `cd rulestead && RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1 mix test --warnings-as-errors --slowest-modules 25` (dominant INCLUDED)

    Capture each run's `real` wall-clock (via `time` or the ExUnit "Finished in" line). Write `121-MEASUREMENT.md` recording: the four command outputs (slowest lines + wall-clock), a before/after comparison table vs the Phase 119 baseline (~42s real, dominant ~27.95s, 587 tests + 8 properties, 18 schedulers), the measured default-lane delta (expected ~42s → ~14s), and the scheduler count observed. If the network to hex.pm is unavailable for runs 3-4, record the offline limitation and use the Phase 119 baseline (~27.95s dominant) as the documented "with-dominant" reference, clearly labeled as inherited not freshly measured. Do NOT modify any source/test file.
  </action>
  <verify>
    <automated>test -f .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-MEASUREMENT.md && grep -qi "slowest" .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-MEASUREMENT.md && grep -Eq "real|Finished in" .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-MEASUREMENT.md && echo "OK: measurement record has slowest + wall-clock"</automated>
  </verify>
  <acceptance_criteria>
    - Source: 121-MEASUREMENT.md records both default (excluded) and included wall-clock numbers, the slowest-test and slowest-module lines, and a before/after comparison against the Phase 119 ~42s / ~27.95s baseline.
    - Source: the measurement explicitly used the exact D-09 commands (`mix test --warnings-as-errors --slowest 25` and `--slowest-modules 25`); the env var name matches the one recorded in 121-01-SUMMARY.md.
    - Behavior: the default-lane wall-clock is materially lower than the with-dominant wall-clock (the relocation delta is demonstrated, or the offline limitation is documented with the inherited baseline labeled as such).
  </acceptance_criteria>
  <done>Before/after slowest-test and wall-clock notes recorded with the exact Phase 119 commands; default-lane improvement demonstrated against baseline (success criterion #5).</done>
</task>

<task type="auto">
  <name>Task 2: Record the partitioning rejection and the no-action decisions (D-06/D-05/D-07/D-10)</name>
  <files>.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-MEASUREMENT.md (the file being appended to — Task 1's measurement section)
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-RESEARCH.md (Partitioning Rejection Evidence lines 325-332)
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-CONTEXT.md (D-05, D-06, D-07, D-10 decision text lines 26-42)
  </read_first>
  <action>
    Append decision sections to 121-MEASUREMENT.md:
    1. D-06 Partitioning Rejected (with evidence): record the five verified premises — single dominant serial network test (partitions distribute modules not intra-module work); overwhelmingly async:false suite via global Fake + process-global app-env so each partition still serializes; single Postgres sandbox + single named Fake would force per-partition DB/Fake isolation = fragility the No-Go guardrails caution against; no partition config in mix.exs today; 18 schedulers already absorb the tiny async set. State it is mapped to FUT-01 (deferred) and reversible if the suite grows materially. This satisfies success criterion #3 via rejection-with-evidence.
    2. D-05 No module splits: record that the next-slowest module is ~1.6s once the dominant test is set aside, so success criterion #2's "meaningful concurrency benefit" bar is unmet; splitting would churn history against the narrow-changes rule.
    3. D-07 No Dialyzer/PLT change: record that Phase 120 already scoped the PLT key correctness-safely and Dialyzer runs in the lint lane off the test wall-clock path — no safe lever remains in Phase 121's boundary.
    4. D-10 Xref cycle note: record the length-47 compile-connected cycle centered on `lib/rulestead.ex` as architectural evidence only, explicitly NOT refactored in Phase 121.
    Do NOT implement partitioning, split any module, change Dialyzer/PLT config, or refactor the xref cycle — these are recorded decisions, not actions.
  </action>
  <verify>
    <automated>F=.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-MEASUREMENT.md; grep -qi "partition" "$F" && grep -Eqi "module split|no module split|D-05" "$F" && grep -Eqi "dialyzer|D-07" "$F" && grep -Eqi "xref|D-10" "$F" && echo "OK: all four decisions recorded"</automated>
  </verify>
  <acceptance_criteria>
    - Source: 121-MEASUREMENT.md contains a D-06 section listing all five verified partitioning-rejection premises and the FUT-01 deferral.
    - Source: 121-MEASUREMENT.md records the D-05 (no module split, ~1.6s next-slowest), D-07 (no Dialyzer/PLT change), and D-10 (xref cycle noted, not refactored) decisions.
    - Negative: no source/config change was made — `mix.exs` has no new partition config, no module was split, no Dialyzer/PLT key changed, `lib/rulestead.ex` xref cycle not refactored (these are documented decisions only).
  </acceptance_criteria>
  <done>Partitioning rejected with evidence (criterion #3); the D-05/D-07/D-10 no-action decisions recorded for Phase 123 closeout; no source/config touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measurement record → Phase 123 closeout consumers | The before/after numbers feed downstream closeout metrics; a cosmetic or unverified speedup claim would misrepresent release-trust posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-04 | Repudiation (misleading speedup claim) | 121-MEASUREMENT.md before/after record | mitigate | Use the EXACT locked Phase 119 commands and report BOTH the default (excluded) and the with-dominant (included) numbers, so the speedup is shown as relocation (the proof still runs on its named scope) — not a deletion or a hidden cost. Offline limitations are labeled, not silently substituted. |
| T-121-SC | Tampering | npm/pip/cargo installs | accept | N/A — no external packages installed in this phase. |
</threat_model>

<verification>
1. `121-MEASUREMENT.md` exists with the four D-09 runs (slowest lines + wall-clock) and a before/after table vs the Phase 119 baseline.
2. The partitioning rejection and the D-05/D-07/D-10 no-action decisions are recorded.
3. No source/config file changed by this plan (verify `git status` shows only `121-MEASUREMENT.md` for this plan's files).
</verification>

<success_criteria>
- Before/after slowest-test and wall-clock notes recorded with the exact Phase 119 commands (success criterion #5).
- Test partitioning explicitly rejected with evidence (success criterion #3 via rejection).
- D-05, D-07, D-10 recorded as deliberate no-action decisions; no scope creep.
</success_criteria>

<output>
Create `.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-03-SUMMARY.md` when done. Record: the measured default vs with-dominant wall-clock, the delta vs the ~42s baseline, and confirmation that partitioning/split/Dialyzer/xref were decisions-not-actions. (`121-MEASUREMENT.md` is the durable evidence artifact for Phase 123.)
</output>
