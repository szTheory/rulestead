---
phase: 121-mix-exunit-performance-test-value-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/test/test_helper.exs
  - rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs
  - scripts/ci/test.sh
autonomous: true
requirements: [CIDX-06]
must_haves:
  truths:
    - "Default `mix test` no longer runs the ~28s published-Hex test"
    - "The published-Hex installability proof still executes on a named scope"
    - "All other (fast) tests in verify_release_publish_test.exs still run in the default lane"
    - "test.sh keeps its scope dispatch structure and per-scope failure microcopy"
  artifacts:
    - path: "rulestead/test/test_helper.exs"
      provides: "Env-conditional default exclude extended with the new slow-test tag (install_integration gate preserved)"
      contains: "RULESTEAD_RUN_PUBLISHED_HEX_SMOKE"
    - path: "rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs"
      provides: "Dominant test tagged default-excluded with explicit timeout; no blind retry added"
      contains: "@tag :published_hex_smoke"
    - path: "scripts/ci/test.sh"
      provides: "Named scope opts the proof back in with intact microcopy"
      contains: "RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1"
  key_links:
    - from: "rulestead/test/test_helper.exs"
      to: "verify_release_publish_test.exs published_hex_smoke tag"
      via: "ExUnit.start exclude list"
      pattern: "published_hex_smoke"
    - from: "scripts/ci/test.sh run_guarded_rollout_foundations"
      to: "verify_release_publish_test.exs dominant test case"
      via: "RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1 + --include"
      pattern: "RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1 run_mix rulestead test --include published_hex_smoke"
---

<objective>
Relocate the single dominant ~27.95s published-Hex test off the default `mix test`
hot loop behind an opt-in ExUnit tag (D-03), while keeping the supply-chain
installability proof reachable on a named `scripts/ci/test.sh` scope (D-03/D-08).
Do NOT add a blind retry to mask the live-hex network flake; an explicit
documented per-test timeout is the only sanctioned hardening (D-04).

Purpose: This is the phase's one genuinely high-impact wall-clock lever — the
default lane drops ~20–28s (~42s → ~14s expected) without deleting or weakening
the release-trust proof.
Output: Tagged test + extended default exclude + a named scope that demonstrably
executes the proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-CONTEXT.md
@.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-RESEARCH.md
@.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-PATTERNS.md

<interfaces>
<!-- Verified current-state anchors. Executor must re-read before editing; line numbers may drift. -->

rulestead/test/test_helper.exs:1-8 (the env-conditional exclude to EXTEND, not replace):
  exunit_opts =
    if System.get_env("RULESTEAD_RUN_INSTALL_INTEGRATION") == "1" do
      []
    else
      [exclude: [install_integration: true]]
    end
  ExUnit.start(exunit_opts)
  (Lines 10-24 set global app env + named Rulestead.Fake + Sandbox.mode(:manual) — DO NOT TOUCH.)

rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs:199-217
  @published_smoke_version "0.1.4"   (line 199 — maintained constant, do NOT auto-bump)
  test "admin consumer fixture compiles against published Hex packages" do  (line 201)
    ... System.cmd(check.cmd, check.args, ...) (live deps.get/compile vs hex.pm, lines 209-210)
  The NEXT test "verify.release_publish can plan with the shared fixture helper" (line 219) is fast — must stay default-included, so the tag is PER-TEST not @moduletag.

rulestead/test/rulestead/integration/install_smoke_test.exs:6-7 (the tag+timeout precedent to mirror):
  @moduletag :install_integration
  @moduletag timeout: 300_000

scripts/ci/test.sh:173-186 run_guarded_rollout_foundations() — already lists verify_release_publish_test.exs (line 181).
scripts/ci/test.sh:528-531 all) scope — runs `mix test --warnings-as-errors --exclude install_integration` (CLI exclude does NOT cover the new tag; the test_helper exclude does). Leave this arm unchanged.
scripts/ci/test.sh:542-544 guarded_rollout_foundations) case arm.
scripts/ci/test.sh:572 "Supported scopes:" microcopy list.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag the dominant test default-excluded and extend the test_helper exclude</name>
  <files>rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs, rulestead/test/test_helper.exs</files>
  <read_first>
    - rulestead/test/test_helper.exs (the file being modified — lines 1-8 are the exclude block; lines 10-24 are untouchable global setup)
    - rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs (the file being modified — confirm the test at ~line 201 and the next fast test at ~line 219)
    - rulestead/test/rulestead/integration/install_smoke_test.exs (the @moduletag :install_integration + @moduletag timeout: 300_000 analog, lines 6-7)
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-RESEARCH.md (Pattern 1, lines 119-160 — the exact extend-not-replace shape and tag semantics)
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-PATTERNS.md (test_helper + verify_release_publish sections, lines 26-117)
  </read_first>
  <action>
    Add a PER-TEST tag `@tag :published_hex_smoke` immediately above the `test "admin consumer fixture compiles against published Hex packages"` line (currently line 201) in verify_release_publish_test.exs. Do NOT use `@moduletag` — the next test (`"verify.release_publish can plan with the shared fixture helper"`, ~line 219) is fast and MUST stay in the default lane. Per D-04, also add a per-test explicit timeout via `@tag timeout: 300_000` on the SAME dominant test (mirroring install_smoke_test.exs:7's value, but as a per-test `@tag` so the module's other tests do not inherit 300s). Do NOT wrap the `System.cmd` (lines 209-210) in any retry loop. Do NOT change `@published_smoke_version "0.1.4"` (line 199).

    In test_helper.exs, EXTEND (do not replace) the existing `exunit_opts` env-conditional block (lines 1-8) so the default exclude list contains BOTH `install_integration: true` AND `published_hex_smoke: true`, each removed only when its env var is `"1"`: `RULESTEAD_RUN_INSTALL_INTEGRATION` for the existing tag, and the new `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE` for `published_hex_smoke`. Keep the flat, auditable style of the existing block. The load-bearing invariant: excluded by default across BOTH `mix test` and `mix test <file>` invocation styles; included only when the env is set. Leave lines 10-24 (global app env, named Fake, Sandbox.mode) completely untouched.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/mix/tasks/verify_release_publish_test.exs --warnings-as-errors --slowest 5 2>&1 | tee /tmp/121-default.txt; grep -q "admin consumer fixture compiles against published Hex packages" /tmp/121-default.txt && { echo "FAIL: dominant test ran in default lane"; exit 1; } || echo "OK: dominant test excluded by default"</automated>
  </verify>
  <acceptance_criteria>
    - Source: verify_release_publish_test.exs contains the exact string `@tag :published_hex_smoke` directly above the `"admin consumer fixture compiles against published Hex packages"` test, AND a `@tag timeout: 300_000` on that same test.
    - Source: verify_release_publish_test.exs contains NO new retry/loop wrapper around the `System.cmd` call (D-04); `@published_smoke_version "0.1.4"` unchanged.
    - Source: test_helper.exs default (no-env) exclude list contains both `install_integration: true` and `published_hex_smoke: true`; the string `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE` is present; lines 10-24 byte-identical to before.
    - Behavior: `cd rulestead && mix test test/rulestead/mix/tasks/verify_release_publish_test.exs --slowest 5` output does NOT contain the dominant test name; the other (fast) tests in that file still run (non-zero test count, exit 0).
    - Behavior: `cd rulestead && RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1 mix test test/rulestead/mix/tasks/verify_release_publish_test.exs --slowest 5` output DOES contain `admin consumer fixture compiles against published Hex packages` (requires network to hex.pm; if offline, note as a documented limitation and verify the `--include` mechanism via `--only published_hex_smoke` listing the test).
  </acceptance_criteria>
  <done>Default lane excludes the dominant test; env opt-in re-includes it; install_integration gate preserved; fast sibling tests unaffected; no retry added.</done>
</task>

<task type="auto">
  <name>Task 2: Wire a named test.sh scope to keep the published-Hex proof reachable</name>
  <files>scripts/ci/test.sh</files>
  <read_first>
    - scripts/ci/test.sh (the file being modified — read run_guarded_rollout_foundations at lines 173-186, the all) arm at 528-531, the guarded_rollout_foundations) case arm at 542-544, the "Supported scopes:" line at 572, and at least one print_*_failure_guidance/*_failure_category pair to preserve the microcopy shape)
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-RESEARCH.md (Pattern 2, lines 162-176 + Pitfalls 1-2, lines 221-229 — the lane-reachability trap)
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-PATTERNS.md (test.sh section, lines 120-162)
  </read_first>
  <action>
    In `run_guarded_rollout_foundations()` (lines 173-186), the existing `run_mix rulestead test ... verify_release_publish_test.exs` invocation (line 176-181) inherits the test_helper exclude and will now SILENTLY skip the dominant case (RESEARCH Pitfall 1). Add an explicit opt-in invocation so the proof actually executes on this named scope: a line `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1 run_mix rulestead test --include published_hex_smoke test/rulestead/mix/tasks/verify_release_publish_test.exs`. Keep this scope's existing failure microcopy intact (category + boundary + exact `Rerun:` command + matrix-aware output); update/extend the relevant `Rerun:` microcopy so it mentions the `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1` env needed to reproduce the proof locally.

    Do NOT modify the `all)` arm (line 528-531) — the test_helper exclude now covers the new tag there automatically (D-08: keep test.sh structural). Do NOT introduce a brand-new scope name (reusing `guarded_rollout_foundations` is the lowest-churn, on-theme home per RESEARCH Open Q1); therefore the `case` arm (542-544) and the "Supported scopes:" list (572) need no new entries. Preserve the `case "${TEST_SCOPE}"` dispatch exactly.
  </action>
  <verify>
    <automated>RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh 2>&1 | tee /tmp/121-scope.txt; grep -q "admin consumer fixture compiles against published Hex packages" /tmp/121-scope.txt && echo "OK: proof executes on named scope" || { echo "FAIL: proof did not execute on guarded_rollout_foundations"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - Source: scripts/ci/test.sh contains the exact string `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1 run_mix rulestead test --include published_hex_smoke` within `run_guarded_rollout_foundations`.
    - Source: the `all)` arm still reads `run_mix rulestead test --warnings-as-errors --exclude install_integration` (unchanged); the `case "${TEST_SCOPE}"` dispatch and the "Supported scopes:" list at line 572 are unchanged (no new scope added).
    - Source: the `guarded_rollout_foundations` failure microcopy still contains category + boundary + `Rerun:` + remediation, and the `Rerun:` text now references `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1`.
    - Behavior: `RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh` run output CONTAINS the test name `admin consumer fixture compiles against published Hex packages` (the CASE executes, not just the file). Requires network to hex.pm; if offline, document and instead assert via `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1 mix test --only published_hex_smoke test/rulestead/mix/tasks/verify_release_publish_test.exs` that exactly 1 test is selected.
  </acceptance_criteria>
  <done>At least one named scope demonstrably executes the dominant proof case; default lanes do not; test.sh structure + microcopy preserved; supply-chain proof not silently lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo test suite → published hex.pm packages | The dominant test runs live `deps.get`/`compile` against published packages; this is the supply-chain installability proof crossing into untrusted network territory. |
| default test lane ↔ named release/adopter scope | A change that default-excludes a proof can silently remove it from ALL lanes — the integrity of "the proof still runs" depends on explicit re-inclusion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-01 | Repudiation / supply-chain blind spot | verify_release_publish_test.exs published-Hex proof + scripts/ci/test.sh scope wiring | mitigate | Task 2 explicitly opts the case back in on a named scope; the verify step ASSERTS the test NAME appears in that scope's run output (not just the file), so the proof cannot silently vanish (RESEARCH Pitfalls 1-2). |
| T-121-02 | Tampering with signal | live-hex `System.cmd` (verify_release_publish_test.exs:209-210) | mitigate | D-04: NO blind retry — a real network/dependency flake stays visible. Only an explicit, documented per-test `@tag timeout: 300_000` is added (failure-clarity hardening, not flake masking). |
| T-121-SC | Tampering | npm/pip/cargo installs | accept | N/A — Phase 121 installs no external packages (RESEARCH Package Legitimacy Audit). No package-manager surface is touched. |
</threat_model>

<verification>
1. `cd rulestead && mix test --warnings-as-errors --slowest 5` — dominant test ABSENT from default lane; suite green.
2. `RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh` — dominant test PRESENT and passing on the named scope.
3. `bash scripts/ci/local.sh` (or the `all` scope) green — release_gate-relevant lanes intact, test.sh microcopy preserved.
</verification>

<success_criteria>
- The default `mix test` lane no longer runs the ~28s published-Hex test (success criterion #5 input; CIDX-06 efficiency).
- The published-Hex installability proof still executes on at least one named scope with intact failure microcopy (success criterion #4; T-121-01 mitigated).
- The `install_integration` gate and all of test.sh's scope contract are preserved (D-08).
- No blind retry added (D-04; T-121-02 mitigated).
</success_criteria>

<output>
Create `.planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-01-SUMMARY.md` when done. Record: the exact tag/env names used, which named scope owns the relocated proof, and confirmation that the proof case executed (test name observed in the scope run).
</output>
