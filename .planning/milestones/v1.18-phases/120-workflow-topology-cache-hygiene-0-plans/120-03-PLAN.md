---
phase: 120-workflow-topology-cache-hygiene
plan: 03
type: execute
wave: 3
depends_on: ["120-01", "120-02"]
files_modified:
  - MAINTAINING.md
autonomous: true
requirements: [CIDX-04, CIDX-07, CIDX-09]
must_haves:
  truths:
    - "MAINTAINING.md documents a one-line cache-busting rule for each CI cache (which key component change forces a rebuild)"
    - "The CI caching docs reflect the final Phase 120 cache shape: lint/PLT keyed on rulestead/mix.lock; multi-package lanes keyed on both built locks; cross-lane fallback removed"
    - "MAINTAINING.md states the exact intended branch-protection triad (release_gate, Validate PR title, dependency-review) with actionlint excluded as path-filtered"
    - "MAINTAINING.md notes that live main currently returns Branch not protected (404) and that the settings must be applied manually by a maintainer — no gh api write occurs"
    - "release_gate's documented aggregation scope reflects the newly-wired openfeature companion proof"
  artifacts:
    - path: "MAINTAINING.md"
      provides: "per-cache busting rules (D-07) + branch-protection reconciliation note (D-11)"
      contains: "Branch not protected"
  key_links:
    - from: "MAINTAINING.md CI caching section"
      to: "ci.yml cache keys"
      via: "documented busting rule per cache"
      pattern: "busting|rebuild|forces"
    - from: "MAINTAINING.md Branch protection settings"
      to: "live main protection state"
      via: "manual-application note"
      pattern: "Branch not protected"
---

<objective>
Reconcile `MAINTAINING.md` documentation to the final Phase 120 state (DOCS ONLY): document a one-line cache-busting rule per CI cache (D-07), refresh the CI caching section to the post-D-05/D-06 key shape, and reconcile the branch-protection section to the exact intended triad while noting that live `main` currently returns `Branch not protected` (404) and must be applied manually (D-11). No `gh api` write, no live repo-settings mutation.

Purpose: CIDX-07 requires cache busting rules to be documented; CIDX-04 requires the required-check posture (including the newly-wired openfeature proof from Plan 01) to be honestly documented; CIDX-09's preserve posture is upheld by making zero workflow/script edits here. This is the last commit in the phase, so the docs describe the cache keys exactly as Plans 01–02 left them.

Output: An edited `MAINTAINING.md` whose "CI caching" and "Branch protection settings" sections match the live workflow and document the manual-application gap, verified by `grep` doc-presence checks and a `git diff --name-only` proving only `MAINTAINING.md` changed.

Scope fence (D-12): this plan touches ONLY the surfaces named above — it does NOT touch ExUnit async/sharding (Phase 121), browser/Playwright/demo determinism (Phase 122), contributor-command/closeout docs (Phase 123), product runtime APIs/schemas, or admin publish posture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-workflow-topology-cache-hygiene-0-plans/120-CONTEXT.md
@.planning/phases/120-workflow-topology-cache-hygiene-0-plans/120-PATTERNS.md
@.planning/phases/120-workflow-topology-cache-hygiene-0-plans/120-01-SUMMARY.md
@.planning/phases/120-workflow-topology-cache-hygiene-0-plans/120-02-SUMMARY.md

<interfaces>
<!-- Existing MAINTAINING.md sections to reconcile (the doc analogs). Read MAINTAINING.md directly. -->

MAINTAINING.md:32-52  "Branch protection settings" — already lists the triad
  (release_gate, Validate PR title, dependency-review) with actionlint excluded as
  path-filtered. D-11 adds: the live-404 note + manual-application note. The release_gate
  aggregation wording (MAINTAINING.md:37-39) should now also mention the openfeature
  companion proof (wired in Plan 01).

MAINTAINING.md:54-63  "CI caching" — current text describes Mix deps/build + Dialyzer PLT
  caches. D-07 adds a one-line busting rule per cache and reflects the final key shape:
    - lint deps/build + Dialyzer PLT: busted by rulestead/mix.lock or .tool-versions change
    - test matrix: busted by any mix.lock, OTP, or Elixir change; matrix-scoped restore only
      (cross-lane fallback removed in Plan 02)
    - adopter/mounted: busted by any mix.lock or .tool-versions change (both built sibling locks)
    - openfeature: busted by open_feature_rulestead/mix.lock or .tool-versions change (if scoped in Plan 02)

Final cache shape is whatever Plans 01–02 committed — read 120-02-SUMMARY.md and ci.yml to
confirm the exact keys before writing the doc so the docs are not aspirational.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document per-cache busting rules (D-07)</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md (read the "CI caching" section :54-63 — the structural template to extend)
    - .github/workflows/ci.yml (read the final cache keys post-Plan-02: lint ci.yml:109, PLT ci.yml:116/124, test matrix ci.yml:174-176, adopter ci.yml:232, openfeature ci.yml:257, mounted ci.yml:284 — document the ACTUAL committed shape)
    - .planning/phases/120-workflow-topology-cache-hygiene-0-plans/120-02-SUMMARY.md (confirm which keys were scoped vs left broad)
  </read_first>
  <action>
    Extend the existing "CI caching" section (MAINTAINING.md:54-63) with a one-line cache-busting rule per CI cache — the single key component whose change forces a rebuild — describing the cache keys exactly as Plans 01–02 left them (do NOT document aspirational keys; read ci.yml for the committed shape). At minimum cover: lint Mix deps/build + Dialyzer PLT (busted by `rulestead/mix.lock` or `.tool-versions`); the test matrix Mix cache (busted by any `mix.lock`, OTP, or Elixir change; note the matrix-scoped restore key is the only fallback after the cross-lane fallback removal); adopter-contract and mounted-proof (busted by any built sibling `mix.lock` or `.tool-versions`); openfeature-companion (reflect whichever scope Plan 02 committed). Keep the existing "Cache keys intentionally exclude `.planning/`, `prompts/`, and guide-only edits" line. This is the one D-07 home; do not also duplicate the rules inline in `ci.yml` (single source of truth in docs is the planner's chosen location). Edit ONLY `MAINTAINING.md` — no workflow or script change in this plan.
  </action>
  <verify>
    <automated>grep -A40 'CI caching' MAINTAINING.md | grep -Eiq 'bust|rebuild|forces'</automated>
    <automated>grep -A40 'CI caching' MAINTAINING.md | grep -q 'rulestead/mix.lock'</automated>
    <automated>grep -A40 'CI caching' MAINTAINING.md | grep -Eq 'OTP|Elixir|matrix'</automated>
    <automated>git diff --name-only | grep -vE '^\.planning/' | grep -vx 'MAINTAINING.md'; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - The "CI caching" section contains a per-cache busting rule (grep finds `bust`/`rebuild`/`forces` within the section).
    - The section references `rulestead/mix.lock` and the matrix OTP/Elixir busting dimension.
    - The documented keys match the committed `ci.yml` keys (no aspirational or not-yet-implemented scope).
    - `git diff --name-only` (excluding `.planning/`) shows ONLY `MAINTAINING.md` changed — no workflow/script regression.
  </acceptance_criteria>
  <done>Each CI cache has a one-line documented busting rule in MAINTAINING.md matching the committed ci.yml key shape; only MAINTAINING.md changed.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile branch-protection docs to intended triad + live-404 note (D-11)</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md (read the "Branch protection settings" section :32-52 — already lists the triad; reconcile wording + add the 404/manual note)
    - .planning/phases/119-baseline-expert-audit-0-plans/119-CI-CD-AUDIT.md (the audit captured the live `Branch not protected` 404 evidence for D-11)
    - 120-PATTERNS.md ("branch-protection reconciliation (D-11, docs only)" section)
  </read_first>
  <action>
    Reconcile the "Branch protection settings" section (MAINTAINING.md:32-52) DOCS ONLY (D-11): (1) keep the intended required-check triad exactly — `release_gate`, `Validate PR title`, `dependency-review` — and keep `actionlint` documented as NOT required because it is path-filtered and would sit Pending on non-workflow PRs; (2) update the `release_gate` aggregation wording (MAINTAINING.md:37-39) to also mention the openfeature companion proof now wired into `release_gate` (Plan 01); (3) add a note that live `main` currently returns `Branch not protected` (404) per the Phase 119 audit, so these documented settings must be applied manually by a maintainer. Do NOT run `gh api` or any command that writes live repo settings — this is documentation reconciliation only. Keep the OpenFeature/mounted proof scope framing narrow (the companion proof is merge-blocking only for the Elixir provider package contract it covers, not browser/demo glue or publish choreography). Edit ONLY `MAINTAINING.md`.
  </action>
  <verify>
    <automated>grep -q 'Branch not protected' MAINTAINING.md</automated>
    <automated>grep -Eiq 'manual|maintainer' MAINTAINING.md && grep -q 'release_gate' MAINTAINING.md && grep -q 'Validate PR title' MAINTAINING.md && grep -q 'dependency-review' MAINTAINING.md</automated>
    <automated>grep -Eiq 'actionlint .*not .*required|not a required status check' MAINTAINING.md</automated>
    <automated>grep -A20 'Branch protection settings' MAINTAINING.md | grep -Eiq 'openfeature|open feature'</automated>
    <automated>git diff --name-only | grep -vE '^\.planning/' | grep -vx 'MAINTAINING.md'; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - MAINTAINING.md contains the literal `Branch not protected` 404 note plus a manual-application/maintainer note.
    - The triad `release_gate`, `Validate PR title`, `dependency-review` is documented; `actionlint` is documented as not required (path-filtered).
    - The `release_gate` aggregation wording mentions the openfeature companion proof.
    - `git diff --name-only` (excluding `.planning/`) shows ONLY `MAINTAINING.md` changed; no `gh api` / live repo-settings write occurred.
  </acceptance_criteria>
  <done>Branch-protection section states the exact intended triad, the openfeature aggregation, and the live-404 manual-application gap; docs-only, no live settings write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → live repo settings | Docs must describe intended protection WITHOUT mutating live settings (D-11 deferral) |
| docs edit → workflow/script surfaces | A docs plan must not touch any CI/workflow/supply-chain file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-docdrift | Repudiation / trust | MAINTAINING.md vs live branch protection | mitigate | D-11 documents the live 404 + manual-application requirement so the doc cannot falsely imply protection is active; no `gh api` write attempted |
| T-120-bypass | Tampering / availability | required-check posture documentation | mitigate | Triad documented with actionlint explicitly excluded as path-filtered (avoids documenting a Pending-trap check as required) |
| T-120-supply | Tampering / EoP | protected workflows, pins, permissions, secrets | mitigate | This plan edits ONLY MAINTAINING.md; git-diff acceptance check proves no workflow/script/supply-chain surface changed |
| T-120-SC | Tampering | npm/pip/cargo installs | accept | No package installs — docs-only edit (RESEARCH audit: N/A) |
</threat_model>

<verification>
- `grep` confirms per-cache busting rules and the branch-protection triad + 404/manual note + openfeature aggregation wording.
- `git diff --name-only` confirms ONLY `MAINTAINING.md` changed (no workflow, script, or supply-chain surface).
- `release_gate` is unaffected by this docs-only commit (workflow unchanged → stays green).
</verification>

<success_criteria>
- Each CI cache has a documented one-line busting rule matching the committed ci.yml key shape.
- Branch-protection section states the intended triad, the openfeature aggregation, and the live-404 manual-application gap.
- No `gh api` write; no workflow/script/supply-chain file touched.
- `release_gate` stays green at this commit.
</success_criteria>

<output>
Create `.planning/phases/120-workflow-topology-cache-hygiene-0-plans/120-03-SUMMARY.md` when done.
</output>
