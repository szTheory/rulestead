---
phase: 120-workflow-topology-cache-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [CIDX-04, CIDX-09]
must_haves:
  truths:
    - "release_gate aggregates the openfeature-companion result as a merge-blocking input when companion paths changed"
    - "When openfeature paths did not change, the skipped openfeature-companion job is transformed to success so the required gate never sits on a false-fail"
    - "release_gate.sh exits 0 when all job=result pairs are success and exits 1 when any pair (including openfeature-companion) is non-success"
    - "No release/supply-chain trust surface is weakened: SHA pins, permissions, publish workflows, and secret boundaries are unchanged"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "openfeature-companion wired into release_gate needs + Evaluate gate transform + release_gate.sh pair"
      contains: "openfeature-companion"
  key_links:
    - from: "ci.yml release_gate.needs"
      to: "openfeature-companion job"
      via: "needs list entry"
      pattern: "- openfeature-companion"
    - from: "ci.yml Evaluate gate step"
      to: "scripts/ci/release_gate.sh"
      via: "openfeature-companion=${openfeature_result} argument"
      pattern: "openfeature-companion=\\$\\{openfeature_result\\}"
---

<objective>
Wire the `openfeature-companion` job into the `release_gate` aggregate (D-03), mirroring the existing `mounted-proof` not-relevant→success transform, so the OpenFeature provider proof bar becomes merge-blocking when companion paths change and advisory (skipped→success) otherwise.

Purpose: CIDX-04 requires the OpenFeature proof bar to be a preserved, trustworthy PR gate. Today `openfeature-companion` runs but is NOT in `release_gate.needs`, so its failure does not block merge. This plan closes that gap using the exact pattern already shipped for `mounted-proof`, with zero change to `scripts/ci/release_gate.sh`.

Output: An edited `.github/workflows/ci.yml` whose `release_gate` job gates on `openfeature-companion`, proven offline via `actionlint` + a `release_gate.sh` argument-matrix, with a non-regression assertion that no protected supply-chain surface (D-09/D-10) changed.

Scope fence (D-12): this plan touches ONLY the surfaces named above — it does NOT touch ExUnit async/sharding (Phase 121), browser/Playwright/demo determinism (Phase 122), contributor-command/closeout docs (Phase 123), product runtime APIs/schemas, or admin publish posture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-workflow-topology-cache-hygiene-0-plans/120-CONTEXT.md
@.planning/phases/120-workflow-topology-cache-hygiene-0-plans/120-PATTERNS.md

<interfaces>
<!-- The shipped mounted-proof precedent the executor mirrors exactly. Read ci.yml directly; these are the contracts. -->

release_gate.needs (ci.yml:296-302) currently lists:
  changes, lint, test, integration-placeholder, adopter-contract, mounted-proof

Evaluate gate result vars (ci.yml:309-313) — hyphenated job ids use bracket accessors:
  mounted_proof_result="${{ needs['mounted-proof'].result }}"     # ci.yml:313

Mounted-proof not-relevant→success transform (ci.yml:321-323) — the EXACT template:
  if [[ "${{ needs.changes.outputs.mounted-proof }}" != "true" && "${mounted_proof_result}" == "skipped" ]]; then
    mounted_proof_result="success"
  fi

release_gate.sh call final argument (ci.yml:332):
  "mounted-proof=${mounted_proof_result}"

release_gate.sh loop (release_gate.sh:29-37) — DO NOT EDIT — exits 1 on any non-success pair:
  for pair in "$@": job_result="${pair#*=}"; [[ != success ]] -> exit 1

The changes job already emits the openfeature-companion output (ci.yml:33,75-81); the
openfeature-companion job already exists (ci.yml:238-261, if: openfeature-companion=='true').
No new filter, no new job, no script change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire openfeature-companion into release_gate (D-03)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the file being modified — read the release_gate block ci.yml:294-332 and the openfeature-companion job ci.yml:238-261 for current state)
    - scripts/ci/release_gate.sh (read :29-37 to confirm the arbitrary job=result loop — this file is NOT edited)
    - 120-PATTERNS.md (the three mounted-proof analogs to mirror: needs entry, bracket-accessor + transform, release_gate.sh pair)
  </read_first>
  <action>
    Make three additive edits to `.github/workflows/ci.yml`, mirroring the `mounted-proof` wiring exactly (per D-03):
    (1) In `release_gate.needs` (ci.yml:296-302), append `- openfeature-companion` after `- mounted-proof`.
    (2) In the Evaluate gate `run:` block (ci.yml:307-332): add a result var `openfeature_result="${{ needs['openfeature-companion'].result }}"` alongside the other result vars (use the BRACKET accessor — the job id is hyphenated, identical to the existing `needs['mounted-proof']` form at ci.yml:313); then add the not-relevant→success transform immediately after the mounted-proof transform (ci.yml:321-323): `if [[ "${{ needs.changes.outputs.openfeature-companion }}" != "true" && "${openfeature_result}" == "skipped" ]]; then openfeature_result="success"; fi`.
    (3) In the `scripts/ci/release_gate.sh` invocation (ci.yml:325-332), add a final argument `"openfeature-companion=${openfeature_result}"` after the existing `"mounted-proof=${mounted_proof_result}"` pair (ci.yml:332). Keep `--skip-phase7` and all existing pairs unchanged.
    Do NOT edit `scripts/ci/release_gate.sh` (its loop already fails any non-success pair). Do NOT rename any `jobs:` id (the job-id contract at ci.yml:1-3 is immutable). Do NOT add `paths:`/`paths-ignore:` at the `on:` level. Do NOT re-pin or bump any `uses:` action SHA.
  </action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml</automated>
    <automated>bash scripts/ci/release_gate.sh --skip-phase7 changes=success lint=success test=success integration-placeholder=success adopter-contract=success mounted-proof=success openfeature-companion=success; test $? -eq 0</automated>
    <automated>bash scripts/ci/release_gate.sh --skip-phase7 changes=success lint=success test=success integration-placeholder=success adopter-contract=success mounted-proof=success openfeature-companion=failure; test $? -eq 1</automated>
    <automated>grep -q "openfeature-companion=\${openfeature_result}" .github/workflows/ci.yml</automated>
    <automated>grep -q "needs\['openfeature-companion'\].result" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/ci.yml` exits 0 (no expression/context/schema errors).
    - `release_gate.sh --skip-phase7` with all pairs including `openfeature-companion=success` exits 0.
    - `release_gate.sh --skip-phase7 ... openfeature-companion=failure` exits 1 and names the job.
    - `ci.yml` contains the bracket-accessor `needs['openfeature-companion'].result`, the changes-output transform `needs.changes.outputs.openfeature-companion`, and the gate-script argument `openfeature-companion=${openfeature_result}`.
    - `- openfeature-companion` appears in the `release_gate.needs` list.
  </acceptance_criteria>
  <done>release_gate aggregates openfeature-companion: merge-blocking when companion paths changed, skipped→success when not relevant; release_gate.sh proven 0/1 offline; actionlint green; no script or job-id change.</done>
</task>

<task type="auto">
  <name>Task 2: Assert no release/supply-chain regression (D-09/D-10)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (confirm `permissions:` block ci.yml:22-25 and all `uses:` SHA pins are byte-unchanged from the pre-edit state)
    - 120-PATTERNS.md ("Supply-chain non-regression (D-09/D-10 — PRESERVE)" section listing protected surfaces)
  </read_first>
  <action>
    Run a non-regression check proving D-03's edit weakened no supply-chain trust surface (D-09/D-10): confirm via `git diff --name-only` that the working-tree change set touches ONLY `.github/workflows/ci.yml` (and this plan's docs), and explicitly excludes `.github/workflows/publish-hex.yml`, `.github/workflows/verify-published-release.yml`, and `.github/dependabot.yml`; confirm the `permissions:` block at ci.yml:22-25 still reads `contents/actions/checks: read` (not broadened); confirm no `uses:` action SHA pin was changed or converted to a tag. This is the security non-regression gate for this plan — there is no app-layer attack surface added; the control is preservation. Record the assertion result in the SUMMARY.
  </action>
  <verify>
    <automated>git diff --name-only | grep -vE '^\.planning/' | grep -E 'publish-hex\.yml|verify-published-release\.yml|dependabot\.yml'; test $? -eq 1</automated>
    <automated>git diff .github/workflows/ci.yml | grep -E '^[-+].*permissions:|^[-+].*contents:|^[-+].*actions:|^[-+].*checks:'; test $? -eq 1</automated>
    <automated>git diff .github/workflows/ci.yml | grep -E '^[-+].*uses:.*@'; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` (excluding `.planning/`) does NOT include `publish-hex.yml`, `verify-published-release.yml`, or `dependabot.yml` (grep finds nothing → exit 1).
    - No added/removed line in the `ci.yml` diff touches the `permissions:` block or its `contents/actions/checks` keys.
    - No added/removed line in the `ci.yml` diff changes any `uses: ...@<sha>` pin.
  </acceptance_criteria>
  <done>git diff proves the D-03 edit is confined to the release_gate aggregation in ci.yml; no protected workflow, permission, SHA pin, or secret boundary changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted PR → CI workflow | A PR can change workflow files; required-check posture must not be weakenable by a benign-looking edit |
| CI → Hex publish (publish-hex.yml) | Release trust crosses here; preserve-only in Phase 120 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-bypass | Tampering / availability | required-check Pending trap via workflow path filters | mitigate | D-01/D-02 enforced: NO `on:` `paths:`/`paths-ignore:` added; selectivity stays in job `if:` + aggregate skipped→success transform; openfeature wired the same way as mounted-proof |
| T-120-01 | Tampering | release_gate gating logic | mitigate | openfeature-companion transform fires success ONLY when `needs.changes.outputs.openfeature-companion != 'true'`; when relevant, a non-success result blocks merge via release_gate.sh exit 1 (proven offline) |
| T-120-supply | Tampering / Info Disclosure / EoP | SHA pins, `permissions:`, publish-hex.yml, HEX_API_KEY, dependabot.yml | mitigate | Task 2 git-diff non-regression: change set excludes protected workflows; `permissions:` unbroadened; no SHA re-pin. block-on-high: any weakening fails the acceptance check |
| T-120-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A) — only YAML edits using already-pinned actions |
</threat_model>

<verification>
- `actionlint .github/workflows/ci.yml` exits 0.
- `release_gate.sh --skip-phase7` returns 0 for all-success (incl. `openfeature-companion=success`) and 1 for one `=failure`.
- `git diff` confirms no protected supply-chain surface changed.
</verification>

<success_criteria>
- openfeature-companion is in `release_gate.needs` with the not-relevant→success transform mirroring mounted-proof.
- `scripts/ci/release_gate.sh` is unchanged; the new pair is passed correctly.
- No regression in SHA pins, permissions, publish workflows, or secret boundaries.
- `release_gate` stays green at this commit (all-success arg-matrix exits 0).
</success_criteria>

<output>
Create `.planning/phases/120-workflow-topology-cache-hygiene-0-plans/120-01-SUMMARY.md` when done.
</output>
