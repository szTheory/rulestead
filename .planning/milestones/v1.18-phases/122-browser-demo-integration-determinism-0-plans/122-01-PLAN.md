---
phase: 122-browser-demo-integration-determinism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/demo/frontend/playwright.config.ts
  - scripts/demo/verify.sh
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CIDX-05

must_haves:
  truths:
    - "Playwright trace/retry config mismatch is fixed at root cause: retain-on-failure for trace/video/screenshot, retries stays 0"
    - "HTML report directory is created on failure and is uploadable from CI"
    - "Demo-script readiness audit records no-rework evidence (sound polling, zero waitForTimeout)"
    - "verify.sh prints exact URLs, artifact paths, and rerun commands when Playwright fails"
    - "integration-placeholder job uploads playwright-report/ and test-results/ on failure with a pinned SHA action"
    - "All 15 specs are KEEP — no demotions; artifact hygiene (gitignored dirs, no committed baselines) is preserved"
    - "release_gate aggregate is unaffected by the upload step"
  artifacts:
    - path: "examples/demo/frontend/playwright.config.ts"
      provides: "Playwright determinism config — retain-on-failure for trace/video/screenshot, html+list reporter, retries:0 unchanged, no webServer block"
      contains: "retain-on-failure"
    - path: "scripts/demo/verify.sh"
      provides: "Playwright failure-output block printing URLs, artifact paths, rerun commands via || { ... ; exit 1 } idiom"
      contains: "playwright show-report"
    - path: ".github/workflows/ci.yml"
      provides: "upload-artifact step pinned to full SHA on integration-placeholder job, if:failure(), if-no-files-found:ignore"
      contains: "upload-artifact@"
  key_links:
    - from: "examples/demo/frontend/playwright.config.ts"
      to: "examples/demo/frontend/playwright-report/"
      via: "html reporter with open:never generates the report dir on failure"
      pattern: "reporter.*html.*never"
    - from: "scripts/demo/verify.sh"
      to: "examples/demo/frontend/playwright-report/"
      via: "failure block references the artifact path the operator should inspect"
      pattern: "playwright-report"
    - from: ".github/workflows/ci.yml integration-placeholder"
      to: "examples/demo/frontend/playwright-report/"
      via: "upload-artifact path field scoped to repo-root-relative dir"
      pattern: "examples/demo/frontend/playwright-report"
---

<objective>
Apply three surgical file edits — Playwright config determinism (D-01/D-02), verify.sh failure ergonomics (D-04), and CI artifact upload (D-05) — and record the demo-script readiness and spec-keep audit evidence (D-03/D-06).

Purpose: The Phase 119 audit identified that playwright.config.ts configures trace: "on-first-retry" against retries: 0, so traces never fire and no HTML report directory is created for CI to upload. This phase fixes that root-cause mismatch and adds the complementary failure-output and artifact-upload infrastructure without adding retries, checking in baselines, or demoting any spec.

Output: Updated playwright.config.ts (retain-on-failure, html+list reporter), verify.sh with || {} failure block, ci.yml with pinned upload-artifact step, phase SUMMARY.md with D-03/D-06 audit evidence.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/rulestead/.planning/PROJECT.md
@/Users/jon/projects/rulestead/.planning/ROADMAP.md
@/Users/jon/projects/rulestead/.planning/STATE.md
@/Users/jon/projects/rulestead/.planning/phases/122-browser-demo-integration-determinism-0-plans/122-CONTEXT.md
@/Users/jon/projects/rulestead/.planning/phases/122-browser-demo-integration-determinism-0-plans/122-RESEARCH.md
@/Users/jon/projects/rulestead/.planning/phases/122-browser-demo-integration-determinism-0-plans/122-PATTERNS.md
@/Users/jon/projects/rulestead/.planning/phases/122-browser-demo-integration-determinism-0-plans/122-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs — extracted from the modified files. No codebase exploration needed. -->

From examples/demo/frontend/playwright.config.ts (current, 21 lines):
  Line 10: retries: 0   — MUST remain unchanged
  Line 15: trace: "on-first-retry"   — CHANGE to "retain-on-failure"
  No reporter key   — ADD: reporter: [["html", { open: "never" }], ["list"]]
  No screenshot/video keys in use:   — ADD both inside use: block

From scripts/demo/verify.sh (current, 44 lines):
  Line 2: set -euo pipefail
  Line 14: trap cleanup EXIT INT TERM   — MUST remain untouched
  Lines 38-42: subshell npm test invocation — APPEND || { ... ; exit 1 } after closing )
  Line 44: success echo — MUST remain on success path, unaffected

From .github/workflows/ci.yml, integration-placeholder job (lines 188-200):
  After line 200 (Run FleetDesk adoption lab step) — APPEND upload-artifact step
  Indentation: 6 spaces for step keys (- name:, if:, uses:, with:), 10 spaces for with: sub-keys
  Existing pin style: uses: owner/action@<40-char-sha> # vX.Y.Z

House || {} idiom from scripts/demo/smoke.sh (lines 69-72):
  command || {
    dump_failure_logs
    exit 1
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Playwright config (D-01/D-02) — retain-on-failure + html reporter, retries untouched, no webServer</name>
  <files>examples/demo/frontend/playwright.config.ts</files>
  <read_first>
    Read examples/demo/frontend/playwright.config.ts in full (21 lines). The current state is exactly as in 122-PATTERNS.md. Confirm line 10 is retries: 0 and line 15 is trace: "on-first-retry" before editing. No other file needs reading for this task.
  </read_first>
  <action>
    Edit examples/demo/frontend/playwright.config.ts with three precise changes and no others:

    1. After the fullyParallel line (currently line 12), insert a new top-level key: reporter: [["html", { open: "never" }], ["list"]]

    2. Inside the use: block, change trace: "on-first-retry" to trace: "retain-on-failure"

    3. Inside the use: block, after the trace line, insert two new lines: screenshot: "only-on-failure" and video: "retain-on-failure"

    Per D-01: the html reporter is the load-bearing change that creates playwright-report/ so CI can upload it. Per D-02: do NOT add a webServer block. Per D-01: retries: 0 at line 10 is UNCHANGED — any change to retries violates the no-blind-retry No-Go guardrail from 119-CI-CD-AUDIT.md. Use double-quoted strings throughout, matching the existing file style. The metadata: block (frontendUrl/backendUrl) is untouched.
  </action>
  <verify>
    <automated>
      grep -c "retain-on-failure" examples/demo/frontend/playwright.config.ts
      # must output 2 (trace + video)
      grep -c "only-on-failure" examples/demo/frontend/playwright.config.ts
      # must output 1 (screenshot)
      grep -c "on-first-retry" examples/demo/frontend/playwright.config.ts
      # must output 0
      grep "reporter" examples/demo/frontend/playwright.config.ts
      # must show: reporter: [["html", { open: "never" }], ["list"]]
      grep "retries: 0" examples/demo/frontend/playwright.config.ts
      # must be present
      grep -c "webServer" examples/demo/frontend/playwright.config.ts
      # must output 0
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c "retain-on-failure" returns 2 (trace and video both use retain-on-failure)
    - grep -c "only-on-failure" returns 1 (screenshot)
    - grep -c "on-first-retry" returns 0 (old value gone)
    - reporter key present with html (open: "never") and list
    - retries: 0 still present at original position
    - grep -c "webServer" returns 0 (no webServer block added, per D-02)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add failure-output block to verify.sh (D-04) — print URLs, artifact paths, rerun commands; trap intact</name>
  <files>scripts/demo/verify.sh</files>
  <read_first>
    Read scripts/demo/verify.sh in full (44 lines). Confirm line 14 has "trap cleanup EXIT INT TERM" and lines 38-42 contain the subshell npm test invocation ending with a bare closing ). Read scripts/demo/smoke.sh lines 69-72 briefly to confirm the || { ... ; exit 1 } idiom shape used elsewhere.
  </read_first>
  <action>
    Edit scripts/demo/verify.sh: replace lines 38-42 (the Playwright subshell block) by appending a || { ... ; exit 1 } failure handler after the closing ) of the subshell.

    The replacement keeps the subshell (cd examples/demo/frontend && npm run test:e2e) exactly as-is — do not change the CI=true, DEMO_BACKEND_URL, DEMO_FRONTEND_URL env prefix, or the npm run test:e2e command. After the closing ) of the subshell, append per D-04:

    The failure block must: print a blank line, print "[verify] Playwright failed.", print "  Frontend URL : ${DEMO_FRONTEND_URL}", print "  Backend URL  : ${DEMO_BACKEND_URL}", print "  Artifacts    : examples/demo/frontend/playwright-report/", print "                 examples/demo/frontend/test-results/", print "  Local report : cd examples/demo/frontend && npx playwright show-report", print "  Local rerun  : cd examples/demo/frontend && npm run test:e2e", print a blank line, then call exit 1.

    CRITICAL: Do NOT add a new trap or modify the existing trap cleanup EXIT INT TERM at line 14. The || {} form is safe with set -euo pipefail because exit 1 inside the block re-raises failure explicitly, which fires the existing trap (Docker cleanup). The success echo at line 44 (echo "[verify] compose smoke and browser proof passed") must remain on the success path, unaffected. Per D-04: artifact paths in the failure block are repo-root-relative (examples/demo/frontend/playwright-report/ and examples/demo/frontend/test-results/) to match D-05's upload paths.
  </action>
  <verify>
    <automated>
      grep "playwright show-report" scripts/demo/verify.sh
      # must be present
      grep -c "DEMO_FRONTEND_URL" scripts/demo/verify.sh
      # must be >= 2 (env read at export line + failure print)
      grep "trap cleanup" scripts/demo/verify.sh
      # must be present (line 14 trap untouched)
      grep "compose smoke and browser proof passed" scripts/demo/verify.sh
      # must be present (success path unaffected)
      grep "playwright-report" scripts/demo/verify.sh
      # must be present (artifact path in failure block)
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "playwright show-report" present — local report command printed on failure per D-04
    - grep -c "DEMO_FRONTEND_URL" >= 2 — env var read once and echoed in failure block
    - grep "trap cleanup" present — existing cleanup trap at line 14 is untouched
    - grep "compose smoke and browser proof passed" present — success path not altered
    - grep "playwright-report" present — artifact path in failure block matches D-05 upload path
    - bash -n scripts/demo/verify.sh exits 0 (syntax clean)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify upload-artifact SHA before writing CI YAML (D-05 freshness gate)</name>
  <what-built>
    Tasks 1 and 2 are complete. Before writing the CI YAML change in Task 4, the executor must verify the latest stable v4 SHA for actions/upload-artifact to confirm whether ea165f8d65b6e75b540449e92b4886f43607fa02 (v4.6.2) is still current or a newer patch exists.

    Run this command in the repo shell:
      curl -s https://api.github.com/repos/actions/upload-artifact/releases/latest | jq '.tag_name,.target_commitish'

    This prints the latest release tag and its commit SHA.
  </what-built>
  <how-to-verify>
    1. Run: curl -s https://api.github.com/repos/actions/upload-artifact/releases/latest | jq '.tag_name,.target_commitish'
    2. Note the tag name (e.g., "v4.6.2" or a newer "v4.x.y") and the target_commitish SHA.
    3. If target_commitish is a branch name (e.g., "main") rather than a SHA, resolve it: curl -s https://api.github.com/repos/actions/upload-artifact/git/refs/tags/TAG | jq '.object.sha' — then follow the tag's object SHA to get the commit SHA (tags may be annotated; use curl -s https://api.github.com/repos/actions/upload-artifact/git/tags/OBJECT_SHA | jq '.object.sha' to unwrap).
    4. If v4.6.2 is still latest and target_commitish matches ea165f8d65b6e75b540449e92b4886f43607fa02, type "confirmed v4.6.2 SHA ea165f8d... use as-is".
    5. If a newer v4.x.y exists, provide the new tag and full 40-char SHA so Task 4 can use the correct pin.
  </how-to-verify>
  <resume-signal>Type the confirmed SHA and version string — e.g. "confirmed v4.6.2 SHA ea165f8d65b6e75b540449e92b4886f43607fa02" or "use v4.6.3 SHA abc123..." — so the executor uses the correct pin in Task 4.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Add upload-artifact step to integration-placeholder job in ci.yml (D-05) — pinned SHA, if:failure(), if-no-files-found:ignore</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    Read .github/workflows/ci.yml lines 185-205 to locate the integration-placeholder job and the "Run FleetDesk adoption lab" step. Confirm the step ends at approximately line 200 with "run: scripts/ci/integration_placeholder.sh". Note the exact indentation (6 spaces for step-level keys, 10 spaces for with: sub-keys) matching the existing job steps.

    Use the SHA confirmed in Task 3's checkpoint output for the upload-artifact pin.
  </read_first>
  <action>
    Edit .github/workflows/ci.yml: append one new step after the "Run FleetDesk adoption lab (compose + Playwright)" step, inside the integration-placeholder job's steps list.

    The step per D-05:
    - name: Upload Playwright report and test artifacts
    - if: failure()
    - uses: actions/upload-artifact@SHA # vX.Y.Z  (use the SHA and version confirmed in Task 3)
    - with.name: playwright-report-${{ github.run_id }}
    - with.path: multiline block containing "examples/demo/frontend/playwright-report/" and "examples/demo/frontend/test-results/" on separate lines
    - with.retention-days: 7
    - with.if-no-files-found: ignore

    Indentation rules (matching the existing ci.yml convention confirmed in 122-PATTERNS.md): 6 spaces before the dash for the step (i.e., "      - name:"), 8 spaces for if:/uses:/with: keys, 10 spaces for with: sub-keys. Do NOT alter any other job or step. The step is appended after the Run FleetDesk step — it must not appear inside the adopter-contract job that follows.

    Per D-05: if: failure() is correct (not if: always()) because D-01 sets retain-on-failure — meaningful artifacts only exist on failure. Per D-05 and the threat model: the paths are exactly examples/demo/frontend/playwright-report/ and examples/demo/frontend/test-results/ — not the repo root, not any path that could contain env dumps or secrets. if-no-files-found: ignore prevents the upload step from failing if the reporter dir was never created (e.g., cancelled run). The upload step running after a failing main step does NOT change the job result from failure to success — the job result is already failure; this is standard GitHub Actions behavior protecting release_gate integrity.

    After editing, run: python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" — must exit 0 with no parse error.
  </action>
  <verify>
    <automated>
      grep "upload-artifact" .github/workflows/ci.yml
      # must show the new uses: line
      grep -A1 "Upload Playwright" .github/workflows/ci.yml | grep "failure()"
      # must find if: failure() immediately after the step name
      grep "examples/demo/frontend/playwright-report" .github/workflows/ci.yml
      # must be present
      grep "examples/demo/frontend/test-results" .github/workflows/ci.yml
      # must be present
      grep "if-no-files-found" .github/workflows/ci.yml
      # must be present with value: ignore
      grep "upload-artifact@" .github/workflows/ci.yml | grep -v "@ea165f8\|@[0-9a-f]\{40\}"
      # must return zero lines (i.e., pin is a full SHA, not a bare tag)
      python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"
      # must exit 0
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "upload-artifact" present in ci.yml
    - "if: failure()" immediately follows the Upload Playwright step name line
    - Both examples/demo/frontend/playwright-report/ and examples/demo/frontend/test-results/ appear in path block
    - if-no-files-found: ignore present
    - SHA on the uses: line is exactly 40 hex characters with a # vX.Y.Z comment (no bare @v4 tag)
    - python3 YAML parse exits 0 — no structural error
    - git ls-files -- 'examples/demo/frontend/test-results' 'examples/demo/frontend/playwright-report' returns empty (artifact hygiene preserved, D-06)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Record D-03/D-06 audit evidence in phase SUMMARY</name>
  <files>.planning/phases/122-browser-demo-integration-determinism-0-plans/122-01-SUMMARY.md</files>
  <read_first>
    Read .planning/phases/122-browser-demo-integration-determinism-0-plans/122-RESEARCH.md sections "D-03 Audit Evidence" and "D-06: Spec Inventory" to extract the evidence facts to cite verbatim. Read the SUMMARY template from execution_context.
  </read_first>
  <action>
    Create .planning/phases/122-browser-demo-integration-determinism-0-plans/122-01-SUMMARY.md using the repo's standard SUMMARY template.

    The summary MUST include a dedicated "Audit Evidence (D-03/D-06)" section recording:

    D-03 no-rework evidence — cite these facts from RESEARCH.md:
    - smoke.sh:25-47 polls docker inspect .State.Health.Status (real health check, not a fixed sleep)
    - sleep 2 calls inside bounded polling loops, not free-standing
    - retry_command (smoke.sh:49-64) retries curl probes against an eventually-consistent backend
    - cleanup traps on every script (smoke.sh:23/89, verify.sh:14, proxy-smoke.sh:64/90) with dump_failure_logs
    - Zero waitForTimeout or setTimeout in any spec — confirmed by grep
    - Verdict: demo-script readiness is sound; D-03 no-rework determination is evidence-backed

    D-06 KEEP evidence — cite these facts from RESEARCH.md:
    - 15 specs total (not 17+; count was verified by directory listing)
    - 10 functional-journey specs (live Compose stack, distinct user flows)
    - 5 visual-evidence specs (screenshot artifacts via testInfo.outputPath into ignored test-results/)
    - brandbook.spec.ts uses file:// against static HTML — distinct target from live stack
    - No two specs cover the same assertion surface
    - ui-matrix.spec.ts:401-411 anti-baseline guard is self-enforcing (reads own source, asserts no forbidden terms)
    - Zero committed .png/trace/report files confirmed by git ls-files
    - Verdict: CIDX-05 demotion bar not met for any spec; all 15 are KEEP per D-06

    Also record decision coverage D-01..D-06, files changed, verification commands run, and ROADMAP success criteria 1-5 satisfaction.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/122-browser-demo-integration-determinism-0-plans/122-01-SUMMARY.md
      grep "D-03" .planning/phases/122-browser-demo-integration-determinism-0-plans/122-01-SUMMARY.md
      grep "D-06" .planning/phases/122-browser-demo-integration-determinism-0-plans/122-01-SUMMARY.md
      grep "wait_for_health" .planning/phases/122-browser-demo-integration-determinism-0-plans/122-01-SUMMARY.md
      grep "15 specs" .planning/phases/122-browser-demo-integration-determinism-0-plans/122-01-SUMMARY.md
    </automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY file exists
    - D-03 section present with smoke.sh health-polling evidence cited
    - D-06 section present with spec count (15) and no-redundancy verdict
    - wait_for_health cited as evidence of legitimate polling (not a sleep)
    - All six decisions D-01..D-06 referenced in the summary
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → artifact storage | Playwright report and test-results dirs uploaded to GitHub artifact storage; must not include env vars or secrets |
| Action pin → GitHub Actions runtime | Floating action tag could be substituted; SHA pin is the supply-chain control |
| CI job result → release_gate | upload-artifact step must not mask a failing job result and elevate it to success |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Information Disclosure | CI artifact upload (D-05) — wrong path could leak env dumps or secrets | mitigate | Upload step paths are explicitly scoped to examples/demo/frontend/playwright-report/ and examples/demo/frontend/test-results/ — both are Playwright output dirs with no env export or secret material. Executor must confirm path: field contains exactly these two paths and nothing else. |
| T-122-02 | Tampering | Floating action tag on upload-artifact allows tag reassignment to malicious commit | mitigate | Per D-05: uses: actions/upload-artifact@<40-char-sha> # vX.Y.Z — full commit SHA pin, not @v4 alone. SHA confirmed via curl at Task 3 human checkpoint before commit. Matches repo's existing CIDX-09 supply-chain posture. |
| T-122-03 | Integrity | upload-artifact step with if: failure() running after failed main step could theoretically surface as job success, bypassing release_gate | accept | Standard GitHub Actions behavior: job result is determined by the first failing step. A subsequent step with if: failure() that itself succeeds does NOT change the job result. This is well-established GHA semantics (confirmed in RESEARCH.md A1). The release_gate reads needs['integration-placeholder'].result, which stays failure. Risk accepted — behavior is deterministic and well-documented. |
| T-122-04 | Tampering | verify.sh trap overwrite could disable Docker cleanup, leaving compose containers running | mitigate | D-04 uses the || { ... ; exit 1 } idiom (not a new trap) — existing trap cleanup EXIT INT TERM at line 14 is preserved. exit 1 inside the block fires the existing trap. Executor grep check confirms trap cleanup remains present post-edit. |
| T-122-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. playwright.config.ts, verify.sh, and ci.yml are config/shell/YAML edits only. No slopcheck required. |
</threat_model>

<verification>
Run all static checks after completing tasks 1-4:

D-01/D-02 (playwright.config.ts):
  grep -c "retain-on-failure" examples/demo/frontend/playwright.config.ts       # expect 2
  grep -c "only-on-failure" examples/demo/frontend/playwright.config.ts         # expect 1
  grep -c "on-first-retry" examples/demo/frontend/playwright.config.ts          # expect 0
  grep "reporter" examples/demo/frontend/playwright.config.ts                   # shows html+list
  grep "retries: 0" examples/demo/frontend/playwright.config.ts                 # present
  grep -c "webServer" examples/demo/frontend/playwright.config.ts               # expect 0

D-04 (verify.sh):
  grep "playwright show-report" scripts/demo/verify.sh                          # present
  grep -c "DEMO_FRONTEND_URL" scripts/demo/verify.sh                            # >= 2
  grep "trap cleanup" scripts/demo/verify.sh                                    # present
  bash -n scripts/demo/verify.sh                                                # exits 0

D-05 (ci.yml):
  grep -A1 "Upload Playwright" .github/workflows/ci.yml | grep "failure()"     # present
  grep "examples/demo/frontend/playwright-report" .github/workflows/ci.yml     # present
  grep "if-no-files-found" .github/workflows/ci.yml                            # present: ignore
  python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"   # exits 0

D-06 artifact hygiene:
  git ls-files -- 'examples/demo/frontend/test-results' 'examples/demo/frontend/playwright-report'
  # must return zero lines (no committed artifacts)
  grep "forbiddenSourceTerms" examples/demo/frontend/tests/ui-matrix.spec.ts   # present
</verification>

<success_criteria>
Phase 122 is complete when all five ROADMAP success criteria are satisfied by the three file edits and the evidence record:

1. Playwright, demo, UI matrix, FleetDesk, and integration scripts are audited for fixed ports, sleeps, shared state, artifact leakage, and flaky readiness checks — satisfied by D-03 audit evidence recorded in SUMMARY (smoke.sh: real Docker health polling, zero waitForTimeout in specs, cleanup traps on all scripts) and D-06 spec inventory (15 specs, distinct assertion surfaces, no pixel baselines).

2. Known transient browser/test behavior is fixed at root cause — satisfied by D-01 (trace: "retain-on-failure" with retries: 0, not hidden behind blind retries).

3. Generated screenshots and browser artifacts remain ignored artifacts, not checked-in baselines — satisfied by D-06 evidence (gitignore covers both dirs, git ls-files returns empty, ui-matrix.spec.ts:401-411 anti-baseline guard intact).

4. Low-signal or redundant browser/demo checks are rewritten/demoted/removed only with explicit evidence — satisfied by D-06 KEEP determination (no CIDX-05 demotion bar met for any of the 15 specs).

5. Browser/demo failure output includes exact URLs, commands, and artifact paths needed for local reproduction — satisfied by D-04 (verify.sh failure block) and D-05 (CI artifact upload makes playwright-report/ accessible in GitHub Actions UI).
</success_criteria>

<output>
Create .planning/phases/122-browser-demo-integration-determinism-0-plans/122-01-SUMMARY.md when done, following the repo's standard SUMMARY template. Include the D-03/D-06 audit evidence section as described in Task 5.
</output>
