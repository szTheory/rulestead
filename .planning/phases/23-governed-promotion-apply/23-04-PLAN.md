---
phase: 23-governed-promotion-apply
plan: 04
type: execute
wave: 4
depends_on:
  - 23-03
files_modified:
  - rulestead/lib/rulestead.ex
  - rulestead/lib/rulestead/promotion/apply.ex
  - rulestead/lib/rulestead/audit_event.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/test/rulestead/audit_event_governance_test.exs
  - rulestead/test/rulestead/promotion/reapply_version_test.exs
autonomous: true
requirements:
  - PROM-03
  - PROM-04
must_haves:
  truths:
    - "Promotion audit metadata records source env, target env, compare token, selected flag scope, governance linkage, and immutable environment-version ids as one canonical backend truth."
    - "Operators have a backend-supported minimal revert path by re-applying a prior immutable environment version as a fresh promotion action."
    - "Re-apply-version follows the same compare/apply/governed contract as forward promotion rather than reusing `rollback_audit_event/1`."
  artifacts:
    - path: "rulestead/lib/rulestead/audit_event.ex"
      provides: "Promotion-specific audit metadata and governance linkage for later mounted admin rendering"
    - path: "rulestead/test/rulestead/promotion/reapply_version_test.exs"
      provides: "Executable proof that revert is modeled as re-apply-version through the same promotion bundle path"
    - path: "rulestead/lib/rulestead/promotion/apply.ex"
      provides: "Backend reapply-version entrypoint that loads immutable environment-version state as a fresh promotion source"
  key_links:
    - from: "rulestead/lib/rulestead/promotion/apply.ex"
      to: "rulestead/lib/rulestead/audit_event.ex"
      via: "promotion apply results and re-apply-version outcomes flow into canonical audit metadata with compare and environment-version linkage"
      pattern: "compare_token|source_environment_key|target_environment_key|environment_version_id"
    - from: "rulestead/lib/rulestead/store/ecto.ex"
      to: "rulestead/lib/rulestead/audit_event.ex"
      via: "Ecto execution persists promotion governance linkage and environment-version ids into audit metadata consumed later by mounted admin review screens"
      pattern: "change_request_id|scheduled_execution_id|environment_version_id"
    - from: "rulestead/lib/rulestead/audit_event.ex"
      to: "rulestead_admin/lib/rulestead_admin/live/change_request_live/show.ex"
      via: "promotion-specific audit linkage becomes the canonical backend truth that 23-05 review and detail screens render"
      pattern: "reapply_version|compare_token|environment_version_id"
---

<objective>
Add promotion-specific audit linkage and the backend re-apply-version path on top of the governed execution contract from 23-03.

Purpose: Phase 23 still needs a stable backend source of truth for promotion audit rendering and historical re-apply before the mounted admin slice is wired. This plan owns the audit linkage normalization and the backend-only re-apply seam.
Output: Promotion-specific audit metadata, backend re-apply-version entrypoints, and targeted backend audit/re-apply tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-governed-promotion-apply/23-CONTEXT.md
@.planning/phases/23-governed-promotion-apply/23-RESEARCH.md
@.planning/phases/23-governed-promotion-apply/23-PATTERNS.md
@.planning/phases/23-governed-promotion-apply/23-03-PLAN.md
@rulestead/lib/rulestead.ex
@rulestead/lib/rulestead/promotion/apply.ex
@rulestead/lib/rulestead/audit_event.ex
@rulestead/lib/rulestead/store/ecto.ex
@rulestead/test/rulestead/audit_event_governance_test.exs
@rulestead/test/rulestead/promotion/reapply_version_test.exs

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add promotion-specific audit linkage and the backend re-apply-version path</name>
  <files>rulestead/lib/rulestead.ex, rulestead/lib/rulestead/promotion/apply.ex, rulestead/lib/rulestead/audit_event.ex, rulestead/lib/rulestead/store/ecto.ex, rulestead/test/rulestead/audit_event_governance_test.exs, rulestead/test/rulestead/promotion/reapply_version_test.exs</files>
  <behavior>
    - Test 1: promotion audit metadata records source env, target env, compare token, selected flag set, change-request or schedule linkage, and immutable environment-version ids so later review screens have one canonical payload to render.
    - Test 2: re-apply-version builds a fresh promotion from an immutable environment version and records a new forward audit trail instead of using `rollback_audit_event/1`.
  </behavior>
  <action>Extend promotion backend truth per D-16 through D-22. Normalize `AuditEvent.metadata/1` for promotion so the canonical links include source env, target env, compare token, selected flag keys, change request id, scheduled execution id, and the environment-version ids needed for forward re-apply from history. Add the backend `reapply_version` entrypoint in the promotion module and public facade so it loads a prior immutable environment version as the source side of a fresh compare/apply/governed proposal per D-17 through D-22; explicitly keep `rollback_audit_event/1` untouched as the narrow inverse-write tool for other operational cases. This slice must stop at backend truth and test coverage. The mounted admin entrypoint for choosing and launching re-apply-version belongs to 23-05.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/audit_event_governance_test.exs test/rulestead/promotion/reapply_version_test.exs</automated>
  </verify>
  <done>Promotion-specific audit truth and backend re-apply-version support exist independently of any LiveView wiring, giving 23-05 a stable, test-backed source of truth for operator-visible re-apply flows.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/23-governed-promotion-apply/23-04-SUMMARY.md`
</output>
