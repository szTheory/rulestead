---
phase: 11-mounted-admin-governance-and-schedule-ui
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/schedule_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/schedule_live/show.ex
  - rulestead_admin/test/rulestead_admin/live/schedule_live/index_test.exs
  - rulestead_admin/test/rulestead_admin/live/schedule_live/show_test.exs
autonomous: true
requirements:
  - SCH-03
must_haves:
  truths:
    - "Operators can scan upcoming, completed, failed, quarantined, and cancelled scheduled changes from one mounted list-first screen."
    - "A scheduled-change detail page uses the scheduled execution record as the source of truth and shows status, timing, failure details, and linked change-request/flag context."
    - "Cancellation and requeue actions stay explicit, reason-bearing, and route-backed rather than hidden behind job-system affordances."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/schedule_live/index.ex"
      provides: "Dense, filterable schedule list grouped by operator-meaningful status"
    - path: "rulestead_admin/lib/rulestead_admin/live/schedule_live/show.ex"
      provides: "Scheduled execution detail with lifecycle context, cancel/requeue affordances, and linked resources"
    - path: "rulestead_admin/test/rulestead_admin/live/schedule_live/index_test.exs"
      provides: "Mounted list-state verification for schedule visibility"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/schedule_live/index.ex"
      to: "rulestead/lib/rulestead.ex"
      via: "list_scheduled_executions"
      pattern: "Rulestead\\.list_scheduled_executions"
    - from: "rulestead_admin/lib/rulestead_admin/live/schedule_live/show.ex"
      to: "rulestead/lib/rulestead.ex"
      via: "fetch_scheduled_execution, cancel_scheduled_execution, requeue_scheduled_execution"
      pattern: "Rulestead\\.(fetch_scheduled_execution|cancel_scheduled_execution|requeue_scheduled_execution)"
    - from: "rulestead_admin/lib/rulestead_admin/live/schedule_live/show.ex"
      to: "rulestead/lib/rulestead/governance/scheduled_execution.ex"
      via: "state/action/execution-mode projection"
      pattern: "ScheduledExecution"
---

<objective>
Implement the Phase 11 schedule list and scheduled-execution detail workflows.

Purpose: Satisfy SCH-03 per D-14 through D-17 by giving operators a list-first, mounted schedule surface that reflects durable scheduled-execution truth instead of Oban job rows or ad hoc UI state.
Output: Route-backed schedule index and detail pages with filterable state views, linked change-request/flag context, and explicit cancel/requeue controls.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mounted-admin-governance-and-schedule-ui/11-CONTEXT.md
@.planning/phases/10-scheduled-changes-and-durable-execution/10-CONTEXT.md
@rulestead_admin/lib/rulestead_admin/live/schedule_live/index.ex
@rulestead_admin/lib/rulestead_admin/live/schedule_live/show.ex
@rulestead/lib/rulestead.ex
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/governance/scheduled_execution.ex

<interfaces>
From `rulestead/lib/rulestead/store/command.ex`:

```elixir
defmodule ListScheduledExecutions do
  defstruct environment_key: nil, state: nil, action: nil,
            resource_type: nil, resource_key: nil, scheduled_by_id: nil,
            change_request_id: nil, limit: 50, after: nil, before: nil
end

defmodule CancelScheduledExecution do
  defstruct [:scheduled_execution_id, actor: nil, reason: nil, metadata: %{}]
end

defmodule RequeueScheduledExecution do
  defstruct [:scheduled_execution_id, actor: nil, reason: nil, metadata: %{}]
end
```

From `rulestead/lib/rulestead/governance/scheduled_execution.ex`:

```elixir
@states [:scheduled, :running, :completed, :failed, :quarantined, :cancelled]
@execution_modes [:change_request, :policy_bypass, :emergency_bypass]
```

From `rulestead/lib/rulestead.ex`:

```elixir
def fetch_scheduled_execution(%Command.FetchScheduledExecution{} = command)
def list_scheduled_executions(command_or_opts \\ Command.ListScheduledExecutions.new())
def cancel_scheduled_execution(%Command.CancelScheduledExecution{} = command)
def requeue_scheduled_execution(%Command.RequeueScheduledExecution{} = command)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the list-first schedule surface</name>
  <files>rulestead_admin/lib/rulestead_admin/live/schedule_live/index.ex, rulestead_admin/test/rulestead_admin/live/schedule_live/index_test.exs</files>
  <behavior>
    - Test 1: the schedule page defaults to a dense operator list filtered by the current environment, not a calendar-first interface.
    - Test 2: operators can scan status groups including `scheduled`, `running`, `completed`, `failed`, `quarantined`, and `cancelled`, with links back to both the flag and linked change request when present.
  </behavior>
  <action>Implement `/admin/flags/schedule` as the canonical Phase 11 schedule surface per D-14 through D-17. Use `Rulestead.list_scheduled_executions/1` against the durable scheduled execution record, keep filters URL-backed via `handle_params/3`, and make the primary presentation a dense list or grouped list with clear status chips, requested-vs-actual timing, and link-outs to the related flag and change request. Do not ship calendar-first UX, Oban-job inspection language, or client-only derived state.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/schedule_live/index_test.exs</automated>
  </verify>
  <done>The mounted schedule list is env-aware, list-first, and operator-readable, and it scans all required terminal and in-flight states from the scheduled execution record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the scheduled-execution detail page with explicit recovery actions</name>
  <files>rulestead_admin/lib/rulestead_admin/live/schedule_live/show.ex, rulestead_admin/test/rulestead_admin/live/schedule_live/show_test.exs</files>
  <behavior>
    - Test 1: the detail page renders status, action, execution mode, scheduled time, executed time, attempt count, failure reason, and actor chain from the scheduled execution record.
    - Test 2: cancel and requeue remain explicit route-backed actions with required reason capture and no hidden automation.
    - Test 3: quarantined records surface the explicit requeue recovery path, failed records surface read-only failure guidance and audit context, and completed/cancelled records remain readable history rather than editable workflow state.
  </behavior>
  <action>Implement `/admin/flags/schedule/:scheduled_execution_id` as the detail companion to the list page. The page must treat `Rulestead.Governance.ScheduledExecution` as product truth per D-17, render honest actor wording from Phase 10 (`scheduled by`, `approved by`, `executed by scheduler`), and expose only the actions supported by the public facade for the current state: cancel when still schedulable, requeue only when quarantined, and read-only history otherwise. For failed records, show operator-meaningful failure details, audit-linked guidance, and next-step copy without inventing a requeue affordance the current root/store contract does not permit. Keep all failure details redaction-safe; do not expose raw Oban internals or invent reschedule/edit workflows beyond Phase 11.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/schedule_live/show_test.exs</automated>
  </verify>
  <done>Operators can inspect any scheduled execution from one stable route, understand its lifecycle and linked governance context, and take only the explicit cancel/requeue actions supported by the Phase 10 public contract, with requeue limited to quarantined executions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mounted schedule views → scheduled execution facade | UI reads and writes must project durable scheduled execution state, not Oban internals |
| operator-triggered cancel/requeue → scheduled execution lifecycle | mutating recovery actions must remain explicit, auditable, and reason-bearing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | T | schedule status/detail rendering | mitigate | Read status, timing, and failure data from `Rulestead.fetch_scheduled_execution/1` so UI cannot drift into job-row truth or client-side inference. |
| T-11-07 | R | cancel/requeue controls | mitigate | Require explicit reason capture and call the existing audited public facade commands rather than local optimistic state changes. |
| T-11-08 | I | failure and recovery details | mitigate | Keep failure text operator-meaningful and redaction-safe; do not surface raw job args, session-shaped metadata, or webhook-era concepts deferred to Phase 12. |
</threat_model>

<verification>
Run the schedule LiveView test files from `rulestead_admin`. Confirm list filters remain URL-backed, schedule detail reflects scheduled-execution truth, and cancel/requeue are only offered in valid states.
</verification>

<success_criteria>
- SCH-03 is satisfied by mounted schedule list/detail screens covering upcoming, completed, failed, quarantined, and cancelled executions.
- The schedule list remains the primary workflow surface; any date view stays absent or secondary.
- Recovery actions are explicit, audited, and grounded in scheduled execution state rather than job system internals.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mounted-admin-governance-and-schedule-ui/11-mounted-admin-governance-and-schedule-ui-03-SUMMARY.md`
</output>
