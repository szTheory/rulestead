---
phase: 11-mounted-admin-governance-and-schedule-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead_admin/lib/rulestead_admin/router.ex
  - rulestead_admin/lib/rulestead_admin/components/shell.ex
  - rulestead_admin/lib/rulestead_admin/live/change_request_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/change_request_live/show.ex
  - rulestead_admin/lib/rulestead_admin/live/schedule_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/schedule_live/show.ex
  - rulestead_admin/test/rulestead_admin/router_test.exs
  - rulestead_admin/test/rulestead_admin/live/governance_route_contract_test.exs
autonomous: true
requirements:
  - GOV-05
  - SCH-03
must_haves:
  truths:
    - "Mounted admin navigation exposes dedicated change-request and schedule routes without breaking the existing `/admin/flags` seam."
    - "Every new governance route keeps `?env=` as canonical state and can derive environment links through the existing live session."
    - "The admin shell makes governance and schedule entrypoints visible without turning per-flag pages into heavy inline workspaces."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/router.ex"
      provides: "Mounted route contracts for change-request and schedule flows"
      exports: ["rulestead_admin/2"]
    - path: "rulestead_admin/lib/rulestead_admin/live/change_request_live/index.ex"
      provides: "Route-backed queue stub using Session-driven env handling"
    - path: "rulestead_admin/lib/rulestead_admin/live/schedule_live/index.ex"
      provides: "Route-backed schedule stub using Session-driven env handling"
    - path: "rulestead_admin/test/rulestead_admin/live/governance_route_contract_test.exs"
      provides: "Mounted route contract proof for new governance pages"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/router.ex"
      to: "rulestead_admin/lib/rulestead_admin/live/session.ex"
      via: "shared live_session with `mount_path` and `policy` session data"
      pattern: "on_mount: \\[\\{RulesteadAdmin.Live.Session, :default\\}\\]"
    - from: "rulestead_admin/lib/rulestead_admin/live/change_request_live/index.ex"
      to: "rulestead_admin/lib/rulestead_admin/live/session.ex"
      via: "Session.current_path/3 and Session.env_links/3"
      pattern: "Session\\.(current_path|env_links)"
    - from: "rulestead_admin/lib/rulestead_admin/live/schedule_live/index.ex"
      to: "rulestead_admin/lib/rulestead_admin/components/shell.ex"
      via: "shared calm shell with governance navigation"
      pattern: "Shell\\.page"
---

<objective>
Establish the mounted route, shell-navigation, and LiveView contracts for Phase 11 governance and schedule pages.

Purpose: Give later plans stable route-backed surfaces per D-01, D-02, D-14, and D-22 before workflow-specific data and actions land.
Output: Router entries, shell navigation additions, and four route-backed LiveView stubs that preserve the sibling-package mount seam and canonical `?env=` state.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mounted-admin-governance-and-schedule-ui/11-CONTEXT.md
@.planning/phases/11-mounted-admin-governance-and-schedule-ui/11-DISCUSSION-LOG.md
@rulestead_admin/lib/rulestead_admin/router.ex
@rulestead_admin/lib/rulestead_admin/live/session.ex
@rulestead_admin/lib/rulestead_admin/components/shell.ex
@rulestead_admin/test/support/conn_case.ex

<interfaces>
From `rulestead_admin/lib/rulestead_admin/router.ex`:

```elixir
live "/", RulesteadAdmin.Live.FlagLive.Index, :index
live "/audit", RulesteadAdmin.Live.AuditLive.Index, :index
live "/:key", RulesteadAdmin.Live.FlagLive.Show, :show
```

From `rulestead_admin/lib/rulestead_admin/live/session.ex`:

```elixir
def current_path(socket_or_assigns, base_path, params \\ %{})
def env_links(socket_or_assigns, base_path, params \\ %{})
def placeholder_assigns(socket_or_assigns, opts)
```

From `rulestead_admin/lib/rulestead_admin/components/shell.ex`:

```elixir
attr :page_title, :string, required: true
attr :current_environment, :map, required: true
slot :header_actions
slot :inner_block, required: true
def page(assigns)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mounted governance and schedule route contracts</name>
  <files>rulestead_admin/lib/rulestead_admin/router.ex, rulestead_admin/lib/rulestead_admin/live/change_request_live/index.ex, rulestead_admin/lib/rulestead_admin/live/change_request_live/show.ex, rulestead_admin/lib/rulestead_admin/live/schedule_live/index.ex, rulestead_admin/lib/rulestead_admin/live/schedule_live/show.ex, rulestead_admin/test/rulestead_admin/router_test.exs, rulestead_admin/test/rulestead_admin/live/governance_route_contract_test.exs</files>
  <behavior>
    - Test 1: mounted router exposes `/admin/flags/change-requests`, `/admin/flags/change-requests/:id`, `/admin/flags/schedule`, and `/admin/flags/schedule/:scheduled_execution_id` inside the existing `rulestead_admin "/admin/flags"` macro expansion.
    - Test 2: each new page redirects to a canonical `?env=` URL through the existing session model and renders calm route-owned placeholder copy rather than inline workbench behavior.
  </behavior>
  <action>Create the four Phase 11 LiveView stubs and wire them into `RulesteadAdmin.Router` per D-01, D-02, D-05, D-14, and D-22. The stubs must use `handle_params/3`, `Session.current_path/3`, and `Session.env_links/3` so env state stays URL-backed and host-session-driven; do not introduce path-based env routing, mount-path redesigns, or webhook routes (deferred ideas). Keep copy calm/read-oriented and make the placeholder pages explicitly route-backed workflow homes that later plans will fill in, not generic “coming soon” noise.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/router_test.exs test/rulestead_admin/live/governance_route_contract_test.exs</automated>
  </verify>
  <done>Mounted route expansion proves the four new URLs exist in-order under `/admin/flags`, and each new LiveView can mount with canonical `?env=` state using the shared live session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the shared shell with governance navigation without breaking calm operator posture</name>
  <files>rulestead_admin/lib/rulestead_admin/components/shell.ex, rulestead_admin/lib/rulestead_admin/live/change_request_live/index.ex, rulestead_admin/lib/rulestead_admin/live/schedule_live/index.ex, rulestead_admin/test/rulestead_admin/live/governance_route_contract_test.exs</files>
  <behavior>
    - Test 1: queue and schedule pages render inside the existing shell and expose stable cross-links between flag inventory, change requests, schedule, and audit.
    - Test 2: shell-level navigation stays mounted-package aware and preserves the current environment query string on every governance link.
  </behavior>
  <action>Update `Shell.page` so Phase 11 pages can render lightweight top-level governance navigation without changing the mounted-package boundary or inventing a standalone-admin chrome. Keep the implementation minimal: shared shell navigation or header affordances are acceptable, but they must preserve D-18 and D-21 by keeping per-flag pages calm and making heavy workflows route-backed. Route links must be computed from `rulestead_admin_mount_path` and `?env=` rather than hardcoded absolute paths.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/governance_route_contract_test.exs</automated>
  </verify>
  <done>The shell exposes mounted governance entrypoints that preserve `?env=`, avoid mount-path redesign, and keep the new pages visually aligned with the existing admin shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host session → mounted LiveView routes | untrusted URL params and host session data are normalized into the shared LiveView session |
| shell navigation → route links | navigation must not drop environment scope or escape the mounted admin path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T | `router.ex` route additions | mitigate | Build links from `mount_path` + `Session.current_path/3` so route params cannot silently reshape the mounted boundary. |
| T-11-02 | I | governance/schedule placeholder pages | mitigate | Normalize `env` through `RulesteadAdmin.Live.Session.resolve/3` and avoid rendering environment state from raw params. |
</threat_model>

<verification>
Run the targeted router and governance route contract suite from `rulestead_admin`. Confirm the resulting route contracts stay under `/admin/flags` and that every placeholder page redirects or links with `?env=` intact.
</verification>

<success_criteria>
- Mounted router exposes the four Phase 11 governance/schedule URLs under the existing admin mount seam.
- New LiveViews use URL-backed environment state via the existing session helper instead of ad hoc path or assign state.
- Shared shell navigation surfaces change-request and schedule entrypoints without turning the package into a standalone app.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mounted-admin-governance-and-schedule-ui/11-mounted-admin-governance-and-schedule-ui-01-SUMMARY.md`
</output>
