---
phase: 11-mounted-admin-governance-and-schedule-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - rulestead/lib/rulestead.ex
  - rulestead/test/rulestead/governance_facade_contract_test.exs
  - rulestead_admin/lib/rulestead_admin/live/change_request_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/change_request_live/show.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex
  - rulestead_admin/test/rulestead_admin/live/change_request_live/index_test.exs
  - rulestead_admin/test/rulestead_admin/live/change_request_live/show_test.exs
  - rulestead_admin/test/rulestead_admin/live/flag_live/show_test.exs
autonomous: true
requirements:
  - GOV-05
  - SCH-03
must_haves:
  truths:
    - "Operators can scan open change requests in a dedicated queue instead of hunting through audit history."
    - "A change-request detail page answers what changed, who requested it, what approvals are required, and what happens next before any execution action is taken."
    - "Per-flag detail stays calm/read-oriented while still surfacing compact change-request and schedule summary cards that deep-link into route-backed workflows."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/change_request_live/index.ex"
      provides: "Dedicated change-request queue with filterable list projection"
    - path: "rulestead_admin/lib/rulestead_admin/live/change_request_live/show.ex"
      provides: "Diff-first review page with approve/reject plus post-approval execute/schedule panel"
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex"
      provides: "Compact governance summary cards and deep links from flag detail"
    - path: "rulestead/lib/rulestead.ex"
      provides: "Public facade reads for change-request fetch/list needed by mounted admin"
      exports: ["fetch_change_request", "list_change_requests"]
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/change_request_live/index.ex"
      to: "rulestead/lib/rulestead.ex"
      via: "public facade list helper"
      pattern: "Rulestead\\.list_change_requests"
    - from: "rulestead_admin/lib/rulestead_admin/live/change_request_live/show.ex"
      to: "rulestead/lib/rulestead.ex"
      via: "fetch, approve, reject, execute, and schedule helpers"
      pattern: "Rulestead\\.(fetch_change_request|approve_change_request|reject_change_request|execute_change_request|schedule_change_request)"
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex"
      to: "rulestead_admin/lib/rulestead_admin/live/change_request_live/show.ex"
      via: "summary-card deep links"
      pattern: "change-requests"
---

<objective>
Implement the Phase 11 change-request queue, diff-first review page, and calm flag-detail governance summaries.

Purpose: Satisfy GOV-05 per D-05 through D-13 and D-18 through D-21 by turning governance review into a dedicated mounted workflow rather than overloading audit or flag detail.
Output: Public read helpers for change requests, a route-backed queue, a route-backed review/detail page, and compact per-flag summary cards with deep links.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mounted-admin-governance-and-schedule-ui/11-CONTEXT.md
@.planning/phases/09-governance-core-contracts-change-requests-and-approval-polic/09-VERIFICATION.md
@rulestead_admin/lib/rulestead_admin/live/change_request_live/index.ex
@rulestead_admin/lib/rulestead_admin/live/change_request_live/show.ex
@rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex
@rulestead/lib/rulestead.ex
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/governance/change_request.ex
@rulestead/lib/rulestead/governance/approval.ex

<interfaces>
From `rulestead/lib/rulestead/store/command.ex`:

```elixir
defmodule ListChangeRequests do
  defstruct environment_key: nil, action: nil, status: nil,
            resource_type: nil, resource_key: nil, submitted_by_id: nil,
            limit: 50, after: nil, before: nil
end

defmodule ApproveChangeRequest do
  defstruct [:change_request_id, actor: nil, reason: nil, metadata: %{}]
end

defmodule ScheduleChangeRequest do
  defstruct [:change_request_id, :scheduled_for, actor: nil, reason: nil, metadata: %{}]
end
```

From `rulestead/lib/rulestead/governance/change_request.ex`:

```elixir
@states [:submitted, :approved, :rejected, :cancelled, :executed]
@governed_actions [:publish_ruleset, :advance_rollout, :engage_kill_switch, :release_kill_switch]
```

From `rulestead/lib/rulestead/governance/approval.ex`:

```elixir
@enforce_keys [:change_request_id, :decision, :reviewed_by, :correlation_id]
defstruct [:change_request_id, :decision, :reviewed_by, :reason, :correlation_id]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public change-request read helpers and a mounted queue page</name>
  <files>rulestead/lib/rulestead.ex, rulestead/test/rulestead/governance_facade_contract_test.exs, rulestead_admin/lib/rulestead_admin/live/change_request_live/index.ex, rulestead_admin/test/rulestead_admin/live/change_request_live/index_test.exs</files>
  <behavior>
    - Test 1: `Rulestead.fetch_change_request/1` and `Rulestead.list_change_requests/1` expose the existing store behavior without leaking adapter internals into `rulestead_admin`.
    - Test 2: the queue page lists change requests by environment with calm filters/status badges and links to the canonical detail route while preserving `?env=`.
  </behavior>
  <action>Add the missing root-facade read helpers in `rulestead/lib/rulestead.ex` so the mounted admin stays on public package seams rather than calling store modules directly. Then implement the dedicated change-request queue in `change_request_live/index.ex` per D-01, D-04, and D-05 using the existing `ListChangeRequests` command shape: keep it list-first, route-backed, env-scoped, and filterable by status/action/resource without introducing audit-overloading, calendar UI, or mount-path changes. Keep the visual posture dense and operational, with explicit links into the detail page and back to the related flag when `resource_key` is present.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/governance_facade_contract_test.exs && cd ../rulestead_admin && mix test test/rulestead_admin/live/change_request_live/index_test.exs</automated>
  </verify>
  <done>`rulestead_admin` can read change requests through public facade helpers, and the queue screen renders a route-backed, env-aware inbox that complements audit instead of duplicating it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the diff-first review page and calm flag-detail governance summaries</name>
  <files>rulestead_admin/lib/rulestead_admin/live/change_request_live/show.ex, rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex, rulestead_admin/test/rulestead_admin/live/change_request_live/show_test.exs, rulestead_admin/test/rulestead_admin/live/flag_live/show_test.exs</files>
  <behavior>
    - Test 1: the review page shows proposed change diff first, with compact summary/status/context above the fold and secondary simulation/audit context below it.
    - Test 2: while a request is awaiting review, approve/reject are primary actions; once approved, a second-stage execute/schedule panel appears without auto-executing on approval.
    - Test 3: approve, reject, execute, and schedule each follow the locked `preview -> confirm -> audit` flow from D-12, including explicit reason capture before mutation and a visible resulting audit state or linked audit evidence after completion.
    - Test 4: flag detail gains compact `Open change requests` and `Scheduled changes` cards with 1-3 redaction-safe preview rows and deep links, while the page stays a calm read surface.
  </behavior>
  <action>Implement the change-request detail page per D-06 through D-13: diff-first layout, honest actor chain copy (`requested by`, `approved by`, `scheduled by`, `executed by scheduler`), and no combined approve-and-execute shortcut. Make D-12 explicit in the LiveView behavior and tests for every mutating review action on this route: `Approve`, `Reject`, `Execute now`, and `Schedule` must all preserve `preview -> confirm -> audit`, meaning the operator starts from the diff/summary preview, enters a confirmation step with required reason capture before the command is submitted, and lands back on a state that exposes the resulting audit evidence or audit-linked state transition after completion. Reuse the existing governance command surfaces and public schedule helper, and keep simulation context, approval history, and related audit references secondary rather than splitting them into separate required routes. In the same plan, extend `FlagLive.Show` with the two compact summary cards required by D-18 through D-21; previews must be read-only, truncated, and route-backed, not inline workbenches.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/change_request_live/show_test.exs test/rulestead_admin/live/flag_live/show_test.exs && cd ../rulestead && mix test test/rulestead/governance_facade_contract_test.exs</automated>
  </verify>
  <done>The review page presents governed intent clearly before mutation, every mutating action on the review route follows `preview -> confirm -> audit` with explicit reason capture and inspectable resulting audit state, approval and execution stay separate, and flag detail advertises open governance work through compact route-backed summaries without becoming a workflow hub.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mounted LiveView review actions → public governance facade | operator-triggered mutations must stay on the existing audited and authorized write path |
| change-request payload projection → rendered diff and actor chain | persisted governance state is rendered to operators and must remain honest, redacted, and action-specific |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | S | review actions on `change_request_live/show.ex` | mitigate | Use persisted change-request context plus current actor through `Rulestead.approve_change_request/1`, `reject_change_request/1`, `execute_change_request/1`, and `schedule_change_request/1`; do not trust client-supplied actor chain echoes. |
| T-11-04 | T | diff/review rendering | mitigate | Render diff and approval state from fetched change-request and linked records, keeping approve and execute separate per D-09 through D-11 so the UI cannot collapse two audited transitions into one hidden action. |
| T-11-05 | I | flag-detail summary previews | mitigate | Keep preview rows truncated and redaction-safe per D-21, and link to the full route-backed workflow for any sensitive or large diff context. |
</threat_model>

<verification>
Run the targeted governance facade and LiveView suites. Confirm the queue and detail flows preserve `?env=`, show explicit actor/state wording, and never auto-execute on approval.
</verification>

<success_criteria>
- GOV-05 is satisfied by a dedicated review surface that shows diff, simulation context, actor, environment, and approval state before execution.
- Change-request queue and detail routes stay mounted-package aware and do not overload audit history.
- Flag detail remains calm while exposing compact governance discoverability cards for open change requests and scheduled changes.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mounted-admin-governance-and-schedule-ui/11-mounted-admin-governance-and-schedule-ui-02-SUMMARY.md`
</output>
