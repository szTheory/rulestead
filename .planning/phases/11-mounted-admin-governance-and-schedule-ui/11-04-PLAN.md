---
phase: 11-mounted-admin-governance-and-schedule-ui
plan: 04
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
  - 11-03
files_modified:
  - rulestead_admin/test/rulestead_admin/live/change_request_live/accessibility_test.exs
  - rulestead_admin/test/rulestead_admin/live/schedule_live/accessibility_test.exs
  - rulestead_admin/test/rulestead_admin/integration/admin_mount_phase11_test.exs
  - scripts/ci/verify_phase11_admin_governance.sh
  - rulestead/doc/admin-ui.md
autonomous: true
requirements:
  - GOV-05
  - SCH-03
must_haves:
  truths:
    - "Phase 11 governance and schedule flows are provable from the real mounted sibling-package entrypoint, not just isolated component tests."
    - "New review and schedule screens preserve accessibility and keyboard-first operator readability."
    - "Public admin navigation docs describe the new mounted URLs and keep the sibling-package contract honest without broadening release posture."
  artifacts:
    - path: "scripts/ci/verify_phase11_admin_governance.sh"
      provides: "Scripts-first Phase 11 verification entrypoint"
    - path: "rulestead_admin/test/rulestead_admin/integration/admin_mount_phase11_test.exs"
      provides: "Sibling-package mount proof covering new governance routes"
    - path: "rulestead/doc/admin-ui.md"
      provides: "Updated stable mounted admin navigation contract"
  key_links:
    - from: "scripts/ci/verify_phase11_admin_governance.sh"
      to: "rulestead_admin/test/rulestead_admin/integration/admin_mount_phase11_test.exs"
      via: "scripts-first verification"
      pattern: "mix test .*admin_mount_phase11_test"
    - from: "rulestead/doc/admin-ui.md"
      to: "rulestead_admin/lib/rulestead_admin/router.ex"
      via: "documented stable operator navigation"
      pattern: "change-requests|schedule"
---

<objective>
Close Phase 11 with accessibility coverage, mounted sibling-package verification, and honest admin navigation docs.

Purpose: Prove the new governance UI is shippable inside the existing two-package release design and keep the host-facing route contract accurate without pulling in Phase 12 webhook surfaces or future admin redesign.
Output: Accessibility test coverage for new screens, a Phase 11 scripts-first verifier, a mounted-entrypoint integration test, and updated `rulestead/doc/admin-ui.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mounted-admin-governance-and-schedule-ui/11-CONTEXT.md
@rulestead_admin/test/support/axe_audit.ex
@rulestead_admin/test/support/conn_case.ex
@rulestead_admin/test/rulestead_admin/integration/admin_mount_test.exs
@rulestead/doc/admin-ui.md
@prompts/rulestead-security-privacy-and-threat-model.md
@prompts/rulestead-host-app-integration-seam.md

<interfaces>
From `rulestead_admin/test/support/axe_audit.ex`:

```elixir
RulesteadAdmin.TestSupport.AxeAudit.assert_page_accessible!(html, ...)
```

From `rulestead/doc/admin-ui.md`:

```markdown
The `env` query parameter is the canonical environment selector across the mounted UI.
Operators can treat these URL shapes as the stable v0.1.0 navigation layer:
```

From `prompts/rulestead-host-app-integration-seam.md`:

```elixir
rulestead_admin "/admin/flags", policy: MyApp.RulesteadPolicy
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add accessibility coverage for the new governance and schedule screens</name>
  <files>rulestead_admin/test/rulestead_admin/live/change_request_live/accessibility_test.exs, rulestead_admin/test/rulestead_admin/live/schedule_live/accessibility_test.exs</files>
  <behavior>
    - Test 1: the change-request queue and detail pages pass the existing axe-style audit and expose clear headings, labels, and action semantics.
    - Test 2: the schedule list and detail pages pass the same audit while keeping dense operator content keyboard-readable.
  </behavior>
  <action>Extend the existing accessibility posture to the new Phase 11 LiveViews using the repo’s current `axe_audit` helper style. Cover both list and detail surfaces for change requests and schedule pages, with attention to dense filter controls, summary bands, action buttons, and route-backed navigation. Keep this focused on shipped Phase 11 pages only; do not pull in calendar widgets, webhook surfaces, or future workflow chrome.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/change_request_live/accessibility_test.exs test/rulestead_admin/live/schedule_live/accessibility_test.exs</automated>
  </verify>
  <done>The new Phase 11 pages have automated accessibility checks aligned with the existing admin test style and no obvious heading/label/keyboard regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove sibling-package mount behavior and capture it in a scripts-first verifier</name>
  <files>rulestead_admin/test/rulestead_admin/integration/admin_mount_phase11_test.exs, scripts/ci/verify_phase11_admin_governance.sh</files>
  <behavior>
    - Test 1: a host-style mounted admin session can reach the new Phase 11 routes from the real `rulestead_admin` entrypoint while preserving `?env=` behavior.
    - Test 2: one scripts-first command proves the new governance queue, review page, schedule pages, and the core `rulestead` governance facade contract coverage those screens depend on, without claiming webhook or standalone-admin behavior.
  </behavior>
  <action>Create a focused Phase 11 integration test rooted in the real mounted entrypoint instead of reusing implementation-local helpers alone. Then add `scripts/ci/verify_phase11_admin_governance.sh` that runs the new Phase 11 LiveView, accessibility, mount, and `rulestead/test/rulestead/governance_facade_contract_test.exs` coverage in a readable order so the mounted UI proof also re-executes the public governance facade contract introduced in 11-02. Keep the verifier narrow to Phase 11 scope and sibling-package proof, following the scripts-first precedent from earlier phases; do not widen it into Phase 12 webhook verification or release-publish work.</action>
  <verify>
    <automated>bash scripts/ci/verify_phase11_admin_governance.sh</automated>
  </verify>
  <done>One repeatable verifier proves the Phase 11 governance routes through the mounted package entrypoint, reruns the core governance facade contract coverage those routes depend on, and keeps CI/readability aligned with the project’s scripts-first posture.</done>
</task>

<task type="auto">
  <name>Task 3: Update the mounted admin route contract documentation</name>
  <files>rulestead/doc/admin-ui.md</files>
  <action>Update `rulestead/doc/admin-ui.md` so the stable operator navigation section includes the Phase 11 mounted governance and schedule URLs, keeps `?env=` as canonical state per D-22, and describes the new review/schedule surfaces in calm operational language. Keep the docs honest about the sibling-package mount seam and do not add standalone-admin posture, calendar-first claims, webhook visibility, or any Phase 8-only docs.</action>
  <verify>
    <automated>rg -n \"change-requests|schedule|env query parameter\" rulestead/doc/admin-ui.md</automated>
  </verify>
  <done>The public admin UI doc reflects the shipped Phase 11 mounted routes and preserves the linked-version, two-package boundary without drifting into future-phase claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mounted host app → integration verification | the real mount seam must remain the proof point for new Phase 11 behavior |
| public docs → host integrators | documentation must not promise unsupported routes or standalone-admin behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | R | Phase 11 verification evidence | mitigate | Add a scripts-first verifier plus real mounted-entrypoint integration coverage so Phase 11 behavior is reproducible and auditable in CI. |
| T-11-10 | I | `rulestead/doc/admin-ui.md` route contract | mitigate | Document only shipped mounted routes and the canonical `?env=` contract; exclude webhook, calendar-first, and standalone-admin claims deferred to later phases. |
</threat_model>

<verification>
Run the new accessibility suites and the Phase 11 verifier script. Confirm the mounted-entrypoint test exercises the new route families and that `rulestead/doc/admin-ui.md` matches the shipped route contract.
</verification>

<success_criteria>
- Phase 11 has one scripts-first verification entrypoint plus mounted integration coverage.
- Accessibility checks cover the new change-request and schedule list/detail screens.
- The public admin navigation doc reflects shipped Phase 11 routes and remains honest about the mounted two-package boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mounted-admin-governance-and-schedule-ui/11-mounted-admin-governance-and-schedule-ui-04-SUMMARY.md`
</output>
