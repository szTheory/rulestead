---
phase: 51-mounted-guardrail-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/timeline.ex
  - rulestead_admin/lib/rulestead_admin/components/audit_components.ex
  - rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_test.exs
  - rulestead_admin/test/rulestead_admin/live/flag_live/timeline_test.exs
autonomous: true
requirements:
  - ADM-01
must_haves:
  truths:
    - "Operators can distinguish automatic guardrail hold, rollback, and evaluated events from manual rollout actions."
    - "Mounted rollout page includes a bounded guardrail intervention excerpt in the existing workflow."
    - "Per-flag timeline renders automatic guardrail rows with system/source wording and keeps raw metadata behind disclosure."
    - "Unauthorized or unavailable audit reads do not expose rollout history and do not break the rollout page."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/timeline.ex"
      provides: "Guardrail automatic event title and summary projection"
      contains: "Automatic guardrail hold"
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex"
      provides: "Rollout-page guardrail intervention excerpt"
      contains: "Rulestead.list_audit_events"
    - path: "rulestead_admin/lib/rulestead_admin/components/audit_components.ex"
      provides: "Visible Automatic label for guardrail automation entries"
      contains: "entry.automatic?"
    - path: "rulestead_admin/test/rulestead_admin/live/flag_live/timeline_test.exs"
      provides: "Automatic/manual guardrail timeline distinction tests"
      contains: "Automatic guardrail rollback"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex"
      to: "Rulestead.list_audit_events/1"
      via: "load_guardrail_interventions/3"
      pattern: "Rulestead\\.list_audit_events\\("
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/timeline.ex"
      to: "rulestead_admin/lib/rulestead_admin/components/audit_components.ex"
      via: "entry_view/1 automatic? field"
      pattern: "automatic\\?: guardrail_automation_event\\?\\(event\\)"
---

<objective>
Add automatic guardrail intervention wording to the existing timeline surfaces and include a compact intervention excerpt on the rollout page.

Purpose: Complete ADM-01's intervention-reason requirement without creating a standalone guardrail dashboard or global observability surface.
Output: Timeline projection, audit row label, rollout-page excerpt, and route-backed tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/51-mounted-guardrail-workflow/51-CONTEXT.md
@.planning/phases/51-mounted-guardrail-workflow/51-RESEARCH.md
@.planning/phases/51-mounted-guardrail-workflow/51-VALIDATION.md
@.planning/phases/51-mounted-guardrail-workflow/51-PATTERNS.md
@.planning/phases/51-mounted-guardrail-workflow/51-UI-SPEC.md
@.planning/phases/51-mounted-guardrail-workflow/51-01-SUMMARY.md
@.planning/phases/50-guarded-decision-engine-audit/50-CONTEXT.md
@.planning/phases/50-guarded-decision-engine-audit/50-01-SUMMARY.md

<interfaces>
From `rulestead_admin/lib/rulestead_admin/live/flag_live/timeline.ex`:
```elixir
defp load_page(socket, key, env) do
  with {:ok, detail} <- Rulestead.fetch_flag(key, env),
       {:ok, page} <- Rulestead.list_audit_events(flag_key: key, environment_key: env, actor: socket.assigns.current_actor) do
    assign(socket, :entries, build_entries(page.entries))
  end
end

defp entry_view(event) do
  metadata = redacted_metadata(event.metadata)
  %{
    title: title_for(event),
    meta: meta_for(event),
    summary: summary_for(event, before_state, after_state, diff_state),
    raw: %{event: Map.take(event, [:event_type, :result, :resource_key, :environment_key, :actor_display, :occurred_at]), metadata: metadata}
  }
end
```

Phase 50 event types from `rulestead/lib/rulestead/fake.ex` and `rulestead/lib/rulestead/store/ecto.ex`:
```elixir
"rollout.guardrail_held"
"rollout.guardrail_rollback"
"rollout.guardrail_evaluated"
metadata: %{source: :guardrail_automation, request_id: "..."}
links: %{"guardrail_decision_id" => decision.id}
guardrail: normalized_guardrail_evidence
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test automatic guardrail timeline distinction</name>
  <files>rulestead_admin/test/rulestead_admin/live/flag_live/timeline_test.exs, rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_test.exs</files>
  <read_first>
    rulestead_admin/test/rulestead_admin/live/flag_live/timeline_test.exs
    rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_test.exs
    rulestead/test/rulestead/guarded_rollout_test.exs
    rulestead/lib/rulestead/fake.ex
    .planning/phases/51-mounted-guardrail-workflow/51-CONTEXT.md
    .planning/phases/51-mounted-guardrail-workflow/51-UI-SPEC.md
  </read_first>
  <behavior>
    - Per-flag timeline renders `Automatic guardrail hold`, `Automatic guardrail rollback`, and `Guardrail evaluated` for Phase 50 event types.
    - Automatic rows render visible `Automatic` label and `source guardrail_automation` wording when metadata source is present.
    - Manual rollout/ruleset rows render `Manual rollout action` and do not render the `Automatic` label.
    - Rollout page renders `Guardrail interventions` excerpt with guardrail rows and `Open full timeline`.
  </behavior>
  <action>
    Add a timeline test named exactly `test "timeline distinguishes automatic guardrail events from manual rollout actions", %{conn: conn} do`.

    In that test, seed a guarded rollout path with the same pattern as `rulestead/test/rulestead/guarded_rollout_test.exs`: publish a rollout rule with guardrails, call `Rulestead.advance_rollout/4` with metadata `%{request_id: "req-manual-advance", source: :admin_ui}`, call `Rulestead.evaluate_guarded_rollout/4` with metadata `%{request_id: "req-guardrail-held", source: :guardrail_automation}` and a stale or insufficient sample fact to produce `"rollout.guardrail_held"`. If rollback coverage requires a stable predecessor, follow the Phase 50 healthy-then-breach sequence to produce `"rollout.guardrail_rollback"`; otherwise add the rollback event by using existing Fake-supported guarded rollout APIs only, not direct audit storage.

    Assert the timeline HTML includes `Automatic guardrail hold`, `Automatic`, `source guardrail_automation`, `Guardrail evaluated` when present, `Manual rollout action`, and `Show raw detail`. Assert it does not include `raw_provider_payload`.

    Add a rollout-page test named exactly `test "rollout page shows guardrail intervention excerpt with automatic labels", %{conn: conn} do`. Seed at least one guardrail held event, open `/admin/flags/checkout-redesign/rollouts?env=prod`, and assert `Guardrail interventions`, `Automatic guardrail hold`, `Automatic`, and `Open full timeline`.
  </action>
  <acceptance_criteria>
    - `rg -n "timeline distinguishes automatic guardrail events from manual rollout actions" rulestead_admin/test/rulestead_admin/live/flag_live/timeline_test.exs` returns one match.
    - `rg -n "rollout page shows guardrail intervention excerpt with automatic labels" rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_test.exs` returns one match.
    - `rg -n "Automatic guardrail hold|source guardrail_automation|Manual rollout action|Guardrail interventions|Open full timeline" rulestead_admin/test/rulestead_admin/live/flag_live/*_test.exs` shows all required strings.
    - `! rg -n "raw_provider_payload|dashboard|fleet|observability|automatic advance" rulestead_admin/test/rulestead_admin/live/flag_live/timeline_test.exs rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_test.exs`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/flag_live/rollouts_test.exs test/rulestead_admin/live/flag_live/timeline_test.exs</automated>
  </verify>
  <done>Tests fail before implementation and cover automatic/manual wording plus rollout-page intervention excerpt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project guardrail intervention events into mounted timeline surfaces</name>
  <files>rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex, rulestead_admin/lib/rulestead_admin/live/flag_live/timeline.ex, rulestead_admin/lib/rulestead_admin/components/audit_components.ex</files>
  <read_first>
    rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex
    rulestead_admin/lib/rulestead_admin/live/flag_live/timeline.ex
    rulestead_admin/lib/rulestead_admin/components/audit_components.ex
    rulestead/lib/rulestead/audit_event.ex
    rulestead/lib/rulestead/store/command.ex
    .planning/phases/51-mounted-guardrail-workflow/51-CONTEXT.md
    .planning/phases/51-mounted-guardrail-workflow/51-UI-SPEC.md
  </read_first>
  <behavior>
    - Timeline title clauses map `rollout.guardrail_held` to `Automatic guardrail hold`, `rollout.guardrail_rollback` to `Automatic guardrail rollback`, and `rollout.guardrail_evaluated` to `Guardrail evaluated`.
    - Timeline summaries use bounded copy: `Guardrail automation held this rollout fail-closed.`, `A confirmed threshold breach triggered rollback to the last stable rollout snapshot.`, and `Automation is waiting for valid guardrail evidence and will not assume the stage is healthy.` as appropriate.
    - `AuditComponents.timeline_row/1` renders `Automatic` only when `entry.automatic?` is true and keeps raw detail behind `<details>`.
    - Rollout page loads a bounded intervention excerpt using `Rulestead.list_audit_events/1` with `actor: socket.assigns.current_actor`.
  </behavior>
  <action>
    In `timeline.ex`, add `guardrail_automation_event?/1` returning true for event types `["rollout.guardrail_held", "rollout.guardrail_rollback", "rollout.guardrail_evaluated"]`.

    Extend `entry_view/1` with:
    - `automatic?: guardrail_automation_event?(event)`
    - `source_label: source_label(metadata)`

    Extend `redacted_metadata/1` allowlist to include `"guardrail"`, `"guardrail.evidence"`, `"guardrail.signal_key"`, `"guardrail.environment_key"`, `"links.guardrail_decision_id"`, `"links.stable_guardrail_decision_id"`, `"source"`, and `"request_id"`. Keep raw detail behind existing `<details>` only.

    Add title clauses exactly:
    - `defp title_for(%{event_type: "rollout.guardrail_held"}), do: "Automatic guardrail hold"`
    - `defp title_for(%{event_type: "rollout.guardrail_rollback"}), do: "Automatic guardrail rollback"`
    - `defp title_for(%{event_type: "rollout.guardrail_evaluated"}), do: "Guardrail evaluated"`

    Add summary clauses for guardrail events:
    - Held: `Guardrail automation held this rollout fail-closed. Review the missing or stale signal before advancing.`
    - Rollback: `A confirmed threshold breach triggered rollback to the last stable rollout snapshot.`
    - Evaluated: `Automation is waiting for valid guardrail evidence and will not assume the stage is healthy.`
    Append ` Reason: #{reason}` only when `event.reason` is present.

    In `AuditComponents.timeline_row/1`, render `<p :if={Map.get(@entry, :automatic?, false)} class="rs-audit-row__source">Automatic<span :if={Map.get(@entry, :source_label)}> source {@entry.source_label}</span></p>` before the summary. Render `<p :if={!Map.get(@entry, :automatic?, false) and String.starts_with?(to_string(@entry.raw.event.event_type), "rollout.")} class="rs-audit-row__source">Manual rollout action</p>` for manual rollout rows.

    In `rollouts.ex`, initialize `:guardrail_interventions` to `[]`. Add `load_guardrail_interventions(flag_key, env, actor)` that calls `Rulestead.list_audit_events(flag_key: flag_key, environment_key: env, actor: actor)`, sorts newest first, filters to `rollout.guardrail_held`, `rollout.guardrail_rollback`, `rollout.guardrail_evaluated`, and manual rollout/ruleset events, maps through a small local entry view or a shared helper, and takes at most 5 rows. On `{:error, _}`, return `[]`.

    Render a compact `<section class="rs-card" aria-label="Guardrail interventions">` in the rollout page after the guardrail status panel with heading `Guardrail interventions`, an empty copy `No guardrail intervention events are available for this rollout stage yet.`, rows using `AuditComponents.timeline_row`, and a link `Open full timeline`. Keep this inside the current rollout workflow; do not add routes or global filters.
  </action>
  <acceptance_criteria>
    - `rg -n "Automatic guardrail hold|Automatic guardrail rollback|Guardrail evaluated|guardrail_automation_event\\?" rulestead_admin/lib/rulestead_admin/live/flag_live/timeline.ex` shows all required clauses.
    - `rg -n "entry\\.automatic\\?|Manual rollout action|rs-audit-row__source" rulestead_admin/lib/rulestead_admin/components/audit_components.ex` shows all required rendering hooks.
    - `rg -n "Rulestead\\.list_audit_events|guardrail_interventions|aria-label=\"Guardrail interventions\"|No guardrail intervention events are available for this rollout stage yet" rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex` shows all required strings.
    - `! rg -n "dashboard|fleet|observability|insights|automatic advance|raw_provider_payload" rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex rulestead_admin/lib/rulestead_admin/live/flag_live/timeline.ex rulestead_admin/lib/rulestead_admin/components/audit_components.ex`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/flag_live/rollouts_test.exs test/rulestead_admin/live/flag_live/timeline_test.exs</automated>
  </verify>
  <done>Automatic guardrail interventions are visibly distinct in timeline surfaces, rollout page excerpt is bounded, and raw evidence remains behind disclosure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mounted LiveView -> core audit read | Mounted companion reads audit history through `Rulestead.list_audit_events/1` with current actor. |
| audit metadata -> rendered timeline row | Redacted audit metadata becomes operator-visible source, reason, and intervention copy. |
| rollout page -> per-flag timeline | Rollout page links to existing audit timeline instead of a new dashboard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-06 | Spoofing | `load_guardrail_interventions/3` in `rollouts.ex` | mitigate | Pass `actor: socket.assigns.current_actor` to `Rulestead.list_audit_events/1`; do not add direct storage reads. |
| T-51-07 | Information Disclosure | `Timeline.redacted_metadata/1` and `AuditComponents.timeline_row/1` | mitigate | Extend redaction allowlist only for bounded guardrail evidence, source, request id, and links; keep raw detail behind `<details>`. |
| T-51-08 | Repudiation | automatic/manual row projection | mitigate | Label guardrail automation rows with `Automatic` and `source guardrail_automation`; label manual rollout rows with `Manual rollout action`. |
| T-51-09 | Tampering | rollout-page intervention excerpt | accept | Excerpt is read-only and renders no mutation controls; full audit history remains on the existing per-flag timeline. |
| T-51-10 | Elevation of Privilege | unauthorized audit reads | mitigate | Use existing core audit read with current actor and render an empty excerpt on read error instead of bypassing policy. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Intervention reasons stay inside mounted rollout experience | Covered by rollout-page intervention excerpt in Task 2. |
| REQ | ADM-01 operators see intervention reasons without standalone admin or dashboard | Covered by Tasks 1-2. |
| RESEARCH | Reuse `Rulestead.list_audit_events/1` and existing audit components | Covered by Task 2. |
| RESEARCH | Automatic/manual distinction for `rollout.guardrail_*` events | Covered by Tasks 1-2. |
| CONTEXT | D-04, D-05, D-06 | Covered by rollout-page excerpt and no new route/global filter. |
| CONTEXT | D-11, D-12 | Covered by explicit automatic titles, source label, and summary copy. |
| CONTEXT | Deferred global audit expansion/dashboard/provider UI | Excluded explicitly in actions and acceptance criteria. |
</source_audit>

<verification>
Run:
```bash
cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/flag_live/rollouts_test.exs test/rulestead_admin/live/flag_live/timeline_test.exs
cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/guarded_rollout_test.exs test/rulestead/guardrails/decision_test.exs
```
</verification>

<success_criteria>
- Per-flag timeline uses `Automatic guardrail hold`, `Automatic guardrail rollback`, and `Guardrail evaluated` titles.
- Automatic guardrail rows display `Automatic` and `source guardrail_automation` when present.
- Manual rollout rows display `Manual rollout action` and do not use the automatic label.
- Rollout page shows `Guardrail interventions` with at most 5 bounded rows and a link to `Open full timeline`.
- No new `/guardrails` route, global dashboard, provider UI, raw payload panel, or standalone admin behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/51-mounted-guardrail-workflow/51-02-SUMMARY.md`
</output>
