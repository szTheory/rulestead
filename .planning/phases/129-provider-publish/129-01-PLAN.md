---
phase: 129-provider-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - open_feature_rulestead/mix.exs
  - open_feature_rulestead/lib/open_feature_rulestead.ex
  - open_feature_rulestead/lib/open_feature_rulestead/provider.ex
  - open_feature_rulestead/LICENSE
  - open_feature_rulestead/CHANGELOG.md
autonomous: true
requirements: [REL-05]

must_haves:
  truths:
    - "open_feature_rulestead/mix.exs declares @version \"1.0.0\""
    - "deps/0 calls rulestead_dep/0, which returns {:rulestead, \"~> 1.0\"} only when OPEN_FEATURE_RULESTEAD_HEX_RELEASE == \"1\", else {:rulestead, path: \"../rulestead\"}"
    - "mix.exs has a docs/0 block wired via docs: docs() with source_ref: \"open_feature_rulestead-v#{@version}\""
    - "package/0 lists explicit files: including LICENSE and CHANGELOG.md and a Changelog link ending /open_feature_rulestead/CHANGELOG.md"
    - "open_feature_rulestead/LICENSE exists (verbatim MIT copy)"
    - "open_feature_rulestead/CHANGELOG.md has a single 1.0.0 keep-a-changelog entry"
    - "OpenFeatureRulestead and OpenFeatureRulestead.Provider both carry non-stub @moduledoc"
    - "Default-env (no env var) `mix deps.get` + compile + companion tests still pass with the path dep"
  prohibitions:
    - "open_feature_rulestead does NOT appear in release-please-config.json or .release-please-manifest.json"
    - "The published rulestead constraint is NOT ~> #{@version} (no provider @version interpolated into the core dep)"
    - "docs/0 contains NO logo / favicon / assets / before_closing_head_tag theming and NO brandbook symlink"
    - "package/0 has NO maintainers: key"
  artifacts:
    - path: "open_feature_rulestead/mix.exs"
      provides: "version 1.0.0, env-gated rulestead_dep/0, ex_doc, package, docs"
      contains: "rulestead_dep"
    - path: "open_feature_rulestead/LICENSE"
      provides: "MIT license required by package files whitelist"
      contains: "MIT License"
    - path: "open_feature_rulestead/CHANGELOG.md"
      provides: "1.0.0 trust-parity changelog entry"
      contains: "1.0.0"
  key_links:
    - from: "open_feature_rulestead/mix.exs"
      to: "rulestead (Hex or path)"
      via: "rulestead_dep/0 env-gated swap on OPEN_FEATURE_RULESTEAD_HEX_RELEASE"
      pattern: "OPEN_FEATURE_RULESTEAD_HEX_RELEASE"
    - from: "open_feature_rulestead/mix.exs package/0"
      to: "open_feature_rulestead/CHANGELOG.md"
      via: "Changelog link with mandatory open_feature_rulestead/ path segment"
      pattern: "open_feature_rulestead/CHANGELOG.md"
---

<objective>
Make `open_feature_rulestead` publish-ready at `1.0.0` entirely through path-dep-safe, committable-to-main changes: bump the version, install the env-gated `rulestead` dep swap (D-01/D-02/D-03), add ex_doc + a lean `docs/0` block (D-04/D-05/D-06), fill the stub `@moduledoc`s (D-07), tighten `package/0` (D-08), and add the missing `LICENSE` (D-09) and a `1.0.0` `CHANGELOG.md` (D-10). README needs no edits (D-11).

Purpose: Every artifact `mix hex.publish` will upload must exist and be honest BEFORE the irreversible publish in plan 129-03. This plan is fully autonomous and committable to `main` because the default (no-env) branch keeps the path dep — nothing here changes dev/CI/test behavior.

Output: A `main`-commit-ready `open_feature_rulestead/` package satisfying success criteria 1 and 4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-provider-publish/129-CONTEXT.md
@CLAUDE.md
</context>

<artifacts_produced>
This plan creates/modifies these symbols and files:
- `open_feature_rulestead/mix.exs`: `@version "1.0.0"`, new private `rulestead_dep/0` helper, new `OPEN_FEATURE_RULESTEAD_HEX_RELEASE` env gate, `ex_doc` dep, expanded `package/0`, new `docs/0` + `docs: docs()` in `project/0`
- `open_feature_rulestead/lib/open_feature_rulestead.ex`: filled `@moduledoc`
- `open_feature_rulestead/lib/open_feature_rulestead/provider.ex`: new `@moduledoc` on `OpenFeatureRulestead.Provider`
- `open_feature_rulestead/LICENSE`: new file (verbatim MIT copy)
- `open_feature_rulestead/CHANGELOG.md`: new file (single `1.0.0` entry)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bump version + install env-gated rulestead dep swap + ex_doc (D-01/D-02/D-03/D-04)</name>
  <files>open_feature_rulestead/mix.exs</files>
  <read_first>
    - open_feature_rulestead/mix.exs (current state — change site; @version "0.1.0", hardcoded path dep on line 29)
    - rulestead_admin/mix.exs (analog / source of truth — `rulestead_dep/0` at lines 47-53, `ex_doc` dep line 39, the `<PACKAGE>_HEX_RELEASE` env-gate house pattern)
  </read_first>
  <action>
    Change `@version` from `"0.1.0"` to `"1.0.0"` (D-01).

    Replace the hardcoded `{:rulestead, path: "../rulestead"}` line in `deps/0` with a call to a new private `rulestead_dep/0` helper (D-02), structurally mirroring `rulestead_admin/mix.exs:47-53`. The helper gates on `System.get_env("OPEN_FEATURE_RULESTEAD_HEX_RELEASE") == "1"`: the gated (true) branch returns `{:rulestead, "~> 1.0"}`; the default (else) branch returns `{:rulestead, path: "../rulestead"}`. The env var name follows the `<PACKAGE_UPCASE>_HEX_RELEASE` convention.

    CRITICAL (D-03): the published constraint is the loose major pin `{:rulestead, "~> 1.0"}` — deliberately NOT admin's `~> #{@version}`. Never interpolate the provider's own `@version` into the core dep. The provider is an unlinked satellite floating against a stable post-1.0 core, so a loose major pin is idiomatic.

    Add `{:ex_doc, "~> 0.38", only: [:dev, :test], runtime: false}` to `deps/0` (D-04) — mandatory so `mix hex.publish` finds a generated `doc/` dir and HexDocs does not render blank.
  </action>
  <verify>
    <automated>cd open_feature_rulestead && grep -q '@version "1.0.0"' mix.exs && grep -q 'OPEN_FEATURE_RULESTEAD_HEX_RELEASE' mix.exs && grep -q '{:rulestead, "~> 1.0"}' mix.exs && grep -q 'ex_doc' mix.exs && ! grep -q 'rulestead, "~> #{@version}"' mix.exs && OPEN_FEATURE_RULESTEAD_HEX_RELEASE= mix deps.get && OPEN_FEATURE_RULESTEAD_HEX_RELEASE= mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` contains `@version "1.0.0"`.
    - A private `rulestead_dep/0` exists; gated branch is exactly `{:rulestead, "~> 1.0"}`, default branch is `{:rulestead, path: "../rulestead"}`, gate is `System.get_env("OPEN_FEATURE_RULESTEAD_HEX_RELEASE") == "1"`.
    - `deps/0` calls `rulestead_dep()` (no remaining hardcoded path line).
    - `mix.exs` contains the `ex_doc ~> 0.38` dev/test dep.
    - `mix.exs` does NOT contain `rulestead, "~> #{@version}"` (negative — no version interpolation into the core dep).
    - With no env var set, `mix deps.get` resolves the path dep and `mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>Version is 1.0.0; the env-gated swap defaults to path (dev/CI safe) and flips to `~> 1.0` Hex under the env var; ex_doc is available for doc generation.</done>
</task>

<task type="auto">
  <name>Task 2: Add lean docs/0 + tighten package/0 + fill @moduledocs (D-05/D-06/D-07/D-08)</name>
  <files>open_feature_rulestead/mix.exs, open_feature_rulestead/lib/open_feature_rulestead.ex, open_feature_rulestead/lib/open_feature_rulestead/provider.ex</files>
  <read_first>
    - open_feature_rulestead/mix.exs (current package/0; will add docs/0 and docs: docs())
    - rulestead_admin/mix.exs (analog docs/0 lines 72-107 and package/0 lines 55-70 — scale DOWN, drop all theming)
    - open_feature_rulestead/lib/open_feature_rulestead.ex (current stub @moduledoc to expand)
    - open_feature_rulestead/lib/open_feature_rulestead/provider.ex (no @moduledoc yet — add one)
    - open_feature_rulestead/README.md (install snippet + module summaries to echo in moduledocs)
  </read_first>
  <action>
    Add `docs: docs()` to `project/0` and a lean private `docs/0` (D-05/D-06):
    `main: "readme"`, `source_ref: "open_feature_rulestead-v#{@version}"` (D-06 — component-in-tag so [source] links resolve to the published provider source), `source_url: @source_url`, `homepage_url: @homepage_url`,
    `extras: ["README.md", "CHANGELOG.md"]`, ONE `groups_for_modules` group (label at your discretion, e.g. "OpenFeature Provider") containing `OpenFeatureRulestead.Provider` and `OpenFeatureRulestead.ContextMapper`, and a `skip_undefined_reference_warnings_on` guard returning true for refs starting with `"Rulestead."` or `"OpenFeature."` (cross-package refs).
    Deliberately include NO `logo`/`favicon`/`assets`/`before_closing_head_tag` theming and NO brandbook symlink (D-05) — match the upstream lean SDK docs.

    Tighten `package/0` (D-08) to the siblings' shape scaled down: explicit `files: ~w(lib .formatter.exs mix.exs README.md LICENSE CHANGELOG.md)`; add a `"Changelog"` link `"#{@source_url}/blob/main/open_feature_rulestead/CHANGELOG.md"` (the `open_feature_rulestead/` path segment is MANDATORY — copying rulestead's path 404s); keep the existing GitHub + HexDocs links; expand `description` slightly (wording at your discretion). Do NOT add a `maintainers:` key (both siblings omit it).

    Fill the stub `@moduledoc` in `lib/open_feature_rulestead.ex` (short purpose + install snippet) and add a `@moduledoc` to `OpenFeatureRulestead.Provider` (D-07) so the HexDocs landing page is not an empty stub. `OpenFeatureRulestead.ContextMapper` already has a `@moduledoc` — leave it.
  </action>
  <verify>
    <automated>cd open_feature_rulestead && grep -q 'docs: docs()' mix.exs && grep -q 'source_ref: "open_feature_rulestead-v#{@version}"' mix.exs && grep -q 'groups_for_modules' mix.exs && grep -q 'skip_undefined_reference_warnings_on' mix.exs && ! grep -qE 'logo:|favicon:|before_closing_head_tag' mix.exs && ! grep -q 'maintainers:' mix.exs && grep -q 'blob/main/open_feature_rulestead/CHANGELOG.md' mix.exs && grep -q 'LICENSE CHANGELOG.md' mix.exs && grep -vE '^\s*#' lib/open_feature_rulestead.ex | grep -q '@moduledoc' && grep -vE '^\s*#' lib/open_feature_rulestead/provider.ex | grep -q '@moduledoc'</automated>
  </verify>
  <acceptance_criteria>
    - `project/0` includes `docs: docs()`; `docs/0` sets `main: "readme"`, `source_ref: "open_feature_rulestead-v#{@version}"`, `extras: ["README.md", "CHANGELOG.md"]`, one `groups_for_modules` group with both public modules, and a `skip_undefined_reference_warnings_on` guard for `Rulestead.`/`OpenFeature.` refs.
    - `mix.exs` contains NO `logo:`, `favicon:`, `assets:`, or `before_closing_head_tag` (negative); NO `maintainers:` (negative).
    - `package/0` `files:` is the explicit `~w(lib .formatter.exs mix.exs README.md LICENSE CHANGELOG.md)` whitelist; a `Changelog` link ends in `/open_feature_rulestead/CHANGELOG.md`.
    - `OpenFeatureRulestead` and `OpenFeatureRulestead.Provider` each have a non-empty `@moduledoc`.
  </acceptance_criteria>
  <done>HexDocs will render a populated landing page (filled moduledocs, lean docs config, resolvable [source] links via the component tag), and the package whitelist matches the files plan 129-03 will publish.</done>
</task>

<task type="auto">
  <name>Task 3: Add LICENSE + 1.0.0 CHANGELOG, then prove the default-env package builds clean (D-09/D-10/D-11)</name>
  <files>open_feature_rulestead/LICENSE, open_feature_rulestead/CHANGELOG.md</files>
  <read_first>
    - LICENSE (repo root — verbatim MIT source to copy)
    - rulestead_admin/CHANGELOG.md (voice / shape to echo: "Promotion, not rewrite", version-truth, zero breaking changes — keep ONLY the hand-written 1.0.0 narrative section; do NOT clone the release-please machine-generated commit lists)
    - open_feature_rulestead/README.md (confirms README is already honest — D-11, no edits)
  </read_first>
  <action>
    Create `open_feature_rulestead/LICENSE` as a VERBATIM copy of the repo-root `LICENSE` (MIT) (D-09) — required because the `files:` whitelist now lists it.

    Create `open_feature_rulestead/CHANGELOG.md` (D-10): keep-a-changelog format, a SINGLE `1.0.0` entry in the siblings' version-truth voice (promotion not rewrite, zero breaking changes, adapter-scoped). Add an OpenFeature independent-versioning note: the provider versions independently of the `open_feature` SDK and depends on `open_feature ~> 0.1.3`; shipping `1.0.0` while depending on `open_feature ~> 0.1.x` is idiomatic, not a contradiction. This is NOT release-please managed — do NOT include any machine-generated conventional-commits section. Prose wording is at your discretion within this shape.

    Do NOT edit `open_feature_rulestead/README.md` (D-11) — it is already honest (`v1.0.0`, install `{:open_feature_rulestead, "~> 1.0"}`) and `scripts/check_version_truth.py` already passes for it.

    Final proof: run the companion contract tests in the DEFAULT env (path dep) to confirm nothing regressed, and run `scripts/check_version_truth.py` to confirm the README pin stays green.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead && diff -q LICENSE open_feature_rulestead/LICENSE && grep -q '1.0.0' open_feature_rulestead/CHANGELOG.md && grep -qi 'open_feature' open_feature_rulestead/CHANGELOG.md && ! grep -qiE 'Synchronize rulestead-monorepo|compare/open_feature_rulestead-v0' open_feature_rulestead/CHANGELOG.md && python3 scripts/check_version_truth.py && RULESTEAD_TEST_SCOPE=openfeature_companion bash scripts/ci/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `open_feature_rulestead/LICENSE` is byte-identical to the repo-root `LICENSE` (`diff -q` reports no difference).
    - `open_feature_rulestead/CHANGELOG.md` has a single `1.0.0` keep-a-changelog entry mentioning the OpenFeature independent-versioning note, with NO release-please machine-generated commit list (negative grep on `Synchronize rulestead-monorepo` / `compare/open_feature_rulestead-v0`).
    - `python3 scripts/check_version_truth.py` exits 0 (README pin stays green; D-11 untouched).
    - `RULESTEAD_TEST_SCOPE=openfeature_companion bash scripts/ci/test.sh` passes against the path-resolved core (default env), proving no regression.
    - `open_feature_rulestead/README.md` is unchanged (D-11).
  </acceptance_criteria>
  <done>LICENSE and CHANGELOG exist for the publish whitelist; the package compiles and the companion contract tests pass in the default (path-dep) env; version-truth stays green. The package is now commit-ready for `main` with zero publish-time side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev workspace → committed `main` source | These changes land on `main` before any publish; the env-gated default branch must keep the path dep so nothing here can leak a Hex-only or broken config into dev/CI. |
| `mix.exs` config → future published tarball | `package/0` `files:` and `rulestead_dep/0` shape this plan sets directly determine tarball correctness in plan 129-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Tampering | `rulestead_dep/0` env gate | mitigate | Default (no-env) branch MUST return the `path:` dep; Task 1 verify runs `mix deps.get` + `mix compile` with the env var unset to prove dev/CI is unaffected. |
| T-129-02 | Information Disclosure | `package/0` `files:` whitelist | mitigate | Explicit `~w(...)` whitelist (no glob over the package root) ensures only intended files (no `.env`, `_build`, secrets) ship in the future tarball. |
| T-129-03 | Repudiation | CHANGELOG version truth | accept | CHANGELOG is hand-written and reviewed at commit; low-value target, no automated machine section to forge. |
| T-129-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan; the only new dep (`ex_doc`) is a long-established, already-vendored-in-siblings Hex package — no new legitimacy surface. |
</threat_model>

<verification>
- `mix.exs` declares `@version "1.0.0"`, the env-gated `rulestead_dep/0`, `ex_doc`, expanded `package/0`, and lean `docs/0`.
- Default env (`OPEN_FEATURE_RULESTEAD_HEX_RELEASE` unset): `mix deps.get` resolves the path dep, `mix compile --warnings-as-errors` and the companion contract tests pass.
- `open_feature_rulestead/LICENSE` is a verbatim MIT copy; `CHANGELOG.md` holds a single hand-written `1.0.0` entry.
- Negatives hold: no `~> #{@version}` core pin, no docs theming, no `maintainers:`, no release-please machine changelog section, provider absent from release-please config.
- `scripts/check_version_truth.py` stays green.
</verification>

<success_criteria>
Success criteria 1 (version + env-gated dep flip committed to main) and 4 (minimal CHANGELOG for trust parity) are fully satisfied by this plan's committed artifacts. The package is publish-ready but NOT yet published (that is plan 129-03).
</success_criteria>

<output>
Create `.planning/phases/129-provider-publish/129-01-SUMMARY.md` when done.
</output>