---
phase: 59-mounted-governance-workflows
plan: 59-04
type: feature
wave: 3
depends_on:
  - 59-02
  - 59-03
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/change_request_live/show.ex
  - rulestead_admin/test/rulestead_admin/live/change_request_live/show_test.exs
  - rulestead_admin/test/rulestead_admin/live/governance_route_contract_test.exs
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - ADM-02
  - ADM-03
must_haves:
  truths:
    - CR show for apply_audience_mutation displays frozen blast-radius evidence between proposed change and review context
    - Approve action blocked when reviewer visibility tier is not full
    - No new standalone admin routes introduced
  artifacts:
    - rulestead_admin/lib/rulestead_admin/live/change_request_live/show.ex
---

# Plan 59-04: Change Request Show Evidence And Phase Proof

<objective>
Complete ADM-02 on the review hub with frozen governance metadata, ADM-03 approve gating, route contract check, and roadmap plan completion marker.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-59-07: Live re-assess on CR show mutates evidence | medium | Read only `metadata["blast_radius_assessment"]` — no `assess_audience_blast_radius` call |
| T-59-08: Approve without full dependency visibility | high | On show load, compute tier; disable approve with capability_explanation |
</threat_model>

<tasks>

<task id="59-04-01" type="execute">
  <name>Frozen blast-radius panel on CR show</name>
  <read_first>
    - rulestead_admin/lib/rulestead_admin/live/change_request_live/show.ex
    - rulestead_admin/lib/rulestead_admin/components/governance_components.ex
    - rulestead/lib/rulestead/governance/audience_mutation_change_request.ex (metadata keys)
    - rulestead_admin/test/rulestead_admin/live/change_request_live/show_test.exs
    - .planning/phases/59-mounted-governance-workflows/59-CONTEXT.md (D-07, D-08 CR show)
  </read_first>
  <action>
In `change_request_live/show.ex`:

1. After loading change_request, when `change_request.governed_action == "apply_audience_mutation"` (or action atom normalized):
   - Assign `@governance_metadata` from `change_request.metadata`
   - Assign `@blast_radius_assessment` = `metadata["blast_radius_assessment"]` (atomize keys helper if needed)

2. Between `<section>Proposed change</section>` and `<section>Review context</section>`, render:

```heex
<section :if={@blast_radius_assessment}>
  <GovernanceComponents.blast_radius_panel
    assessment={@blast_radius_assessment}
    variant={:reviewer}
    visibility={:full}
    frozen?={true}
  />
</section>
```

3. Enhance `diff_summary/1` or `diff_title/1` for audience mutations to show operation + audience_key from `change_request.command` (no predicate).

4. For ADM-03 approve gate: on mount, when governed_action is audience mutation, call dependency loader for `resource_key` audience — if `visibility_tier != :full`, set `@approve_blocked_reason` and hide Approve button, show `capability_explanation` with title **Broader flag read access required to approve this change.**

Add show_test:
- Seed audience mutation CR via `submit_change_request` with `action: :apply_audience_mutation` in prod (reuse Fake seed from 59-03 tests)
- Assert HTML contains `Evidence frozen at submission` or frozen fingerprint line
- Assert `blast_radius_assessment` verdict string present
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/change_request_live/show_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `show.ex` renders `GovernanceComponents.blast_radius_panel` for `apply_audience_mutation`
    - `show.ex` does not call `assess_audience_blast_radius`
    - show_test asserts frozen evidence copy
  </acceptance_criteria>
</task>

<task id="59-04-02" type="execute">
  <name>Route contract and roadmap</name>
  <read_first>
    - rulestead_admin/test/rulestead_admin/live/governance_route_contract_test.exs
    - rulestead_admin/lib/rulestead_admin/router.ex
    - .planning/ROADMAP.md (Phase 59 plans line)
  </read_first>
  <action>
1. Extend `governance_route_contract_test.exs` (or add assertion file) to document that audience governance uses existing routes only:
   - `/audiences/:key/edit/preview`, `/edit/confirm`, `/archive/preview`, `/archive/confirm`
   - No new `live_session` routes matching `governance` or `proposal`

2. Update `.planning/ROADMAP.md` Phase 59:
   - `**Plans**: 4 plans` with `4/4` placeholder or `0/4` → keep `0/4` until execute; set plan list: `59-01` … `59-04` descriptions one line each

3. Run full phase validation command from `59-VALIDATION.md`.
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/ test/rulestead_admin/live/change_request_live/show_test.exs test/rulestead_admin/live/governance_route_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `governance_route_contract_test.exs` exits 0
    - ROADMAP Phase 59 lists 4 plans (59-01 through 59-04)
    - Full automated command exits 0
  </acceptance_criteria>
</task>

</tasks>
