---
phase: 59-mounted-governance-workflows
plan: 59-02
type: feature
wave: 2
depends_on:
  - 59-01
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/audience_live/edit_preview.ex
  - rulestead_admin/lib/rulestead_admin/live/audience_live/archive_preview.ex
  - rulestead_admin/test/rulestead_admin/live/audience_live/edit_preview_test.exs
  - rulestead_admin/test/rulestead_admin/live/audience_live/archive_preview_test.exs
autonomous: true
requirements:
  - ADM-01
  - ADM-02
must_haves:
  truths:
    - Protected prod preview shows governance callout and blast_radius_panel above impact_preview
    - Governed preview CTA reads Continue to submit; direct-apply reads Continue to confirm
  artifacts:
    - rulestead_admin/lib/rulestead_admin/live/audience_live/edit_preview.ex
    - rulestead_admin/lib/rulestead_admin/live/audience_live/archive_preview.ex
---

# Plan 59-02: Audience Preview Governance UX

<objective>
Wire governance assessment into edit and archive preview LiveViews with operator copy and CTA branching (ADM-01 detection UX, ADM-02 evidence).
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-59-03: Operator believes direct apply available when above threshold | medium | Callout + panel on preview before confirm navigation |
</threat_model>

<tasks>

<task id="59-02-01" type="execute">
  <name>Edit preview governance panel and CTA</name>
  <read_first>
    - rulestead_admin/lib/rulestead_admin/live/audience_live/edit_preview.ex
    - rulestead_admin/lib/rulestead_admin/live/audience_live/governance.ex
    - rulestead_admin/lib/rulestead_admin/components/governance_components.ex
    - rulestead_admin/test/rulestead_admin/live/audience_live/edit_preview_test.exs
    - .planning/phases/59-mounted-governance-workflows/59-CONTEXT.md (D-03, D-08)
  </read_first>
  <action>
In `edit_preview.ex`:

1. After successful `preview_audience_impact`, pipe socket through `AudienceLive.Governance.load_governance_context(socket, preview, operation: :update)`.

2. In `render/1` **before** `AudienceComponents.impact_preview`:
   - When `@governance_mode == :change_request`, render `FlagComponents.callout` with title "Change request required" and body: `This change exceeds the direct-apply limit for **{@current_environment.name}**. You will submit a change request instead of applying immediately.`
   - Render `<GovernanceComponents.blast_radius_panel assessment={@blast_radius_assessment} variant={:operator} visibility={visibility_attr(@visibility_tier)} environment_label={@current_environment.name} />` where `visibility_attr` maps `:full` → `:full`, else `:redacted`

3. Update Continue section link text:
   - `:change_request` → link text **Continue to submit** (same `confirm_path` URL — no new route)
   - else → **Continue to confirm**

4. When `@governance_mode == :blocked`, still show panel + callout; disable or remove Continue link (only "Back to audience").

Add test `edit_preview_test.exs`:
- New describe `"governance in prod"`
- `init_session` with `env=prod` and environments `[%{key: "prod", name: "Production"}]`
- Seed ≥3 flag references to audience in **prod** (copy `seed_audience_flag!` pattern but `environment_keys: ["prod"]` and publish ruleset to prod)
- Assert HTML contains `Continue to submit` and `Governance required` (or callout title)
- Assert `Continue to confirm` absent in prod above-threshold case

Keep existing `env=test` tests green (unrestricted — no governed callout required).
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/edit_preview_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `edit_preview.ex` calls `load_governance_context`
    - `edit_preview.ex` renders `GovernanceComponents.blast_radius_panel`
    - Prod above-threshold test asserts `Continue to submit`
    - `mix test test/rulestead_admin/live/audience_live/edit_preview_test.exs` exits 0
  </acceptance_criteria>
</task>

<task id="59-02-02" type="execute">
  <name>Archive preview parity</name>
  <read_first>
    - rulestead_admin/lib/rulestead_admin/live/audience_live/archive_preview.ex
    - rulestead_admin/lib/rulestead_admin/live/audience_live/edit_preview.ex (after 59-02-01)
    - .planning/phases/59-mounted-governance-workflows/59-CONTEXT.md (D-03 archive wording)
  </read_first>
  <action>
Mirror 59-02-01 in `archive_preview.ex` with `operation: :archive` and copy using "archive" instead of "update".

Create `archive_preview_test.exs` with prod archive scenario (0 refs → below threshold may show direct apply; seed refs if testing above-threshold archive — archive limit is 0 so any refs → above).

Minimum test: prod with 1+ references shows governed callout and `Continue to submit`.
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/archive_preview_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `archive_preview.ex` contains `load_governance_context` and `blast_radius_panel`
    - `archive_preview_test.exs` exits 0 with governed CTA assertion
  </acceptance_criteria>
</task>

</tasks>
