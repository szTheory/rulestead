---
phase: 98-admin-re-skin-css-cascade
plan: 02
type: execute
wave: 2
depends_on:
  - 98-01
files_modified:
  - rulestead_admin/priv/static/css/rulestead_admin.css
autonomous: true
requirements:
  - SKIN-01
  - SKIN-02
must_haves:
  truths:
    - "Block 1 (.rs-shell, [data-rulestead]) has 7 brand/status hex declarations replaced with mineral targets from tokens.json admin_css_mapping.light"
    - "Block 4 (.rs-shell[data-theme=light]) is byte-identical to Block 1 — mirrored verbatim"
    - "Zero non-color property changes in the CSS diff (SC-1)"
    - "check_synced_pair.py exits 0 confirming Block 1≡4 identical (light pair)"
    - "check_brand_tokens.py exits 0 for the light block (7 tokens now match)"
    - "Implements CONTEXT decisions D-01 (Block 1/4 light line ranges + 1≡4 synced-pair invariant), D-02 (Block 1 source-of-truth mirrored verbatim to Block 4), D-03 (colors-only diff, :root invariants untouched, SC-1), D-04 (verbatim light hex from tokens.json admin_css_mapping.light — recompute nothing), and D-04b (Gap-2 per-surface canonicals encoded verbatim)"
  artifacts:
    - path: "rulestead_admin/priv/static/css/rulestead_admin.css"
      provides: "Block 1 and Block 4 re-skinned to mineral light palette"
      contains: "--rs-primary: #3A6F8F"
  key_links:
    - from: "brandbook/tokens.json admin_css_mapping.light"
      to: "rulestead_admin.css Block 1 (lines 225-303)"
      via: "verbatim hex copy — no recomputation"
      pattern: "--rs-primary.*#3A6F8F"
    - from: "rulestead_admin.css Block 1 (lines 225-303)"
      to: "rulestead_admin.css Block 4 (lines 469-549)"
      via: "verbatim mirror — byte-identical copy"
      pattern: "SYNCED PAIR.*light"
---

<objective>
Re-skin Block 1 (light default `.rs-shell, [data-rulestead]`, lines 225-303) with the 7 mineral light hex swaps from `tokens.json admin_css_mapping.light`, then mirror Block 1 verbatim into Block 4 (explicit light `.rs-shell[data-theme="light"]`, lines 469-549) to maintain the synced-pair invariant. Colors only — zero non-color changes.

Purpose: Land the light-theme mineral re-skin. Once this plan is complete, `check_brand_tokens.py` exits 0 for the light block, and `check_synced_pair.py` confirms the Block 1≡4 light pair is byte-identical.

Output: rulestead_admin.css modified (Blocks 1 and 4 only). No script changes, no new files, no non-color property changes.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/rulestead/.planning/PROJECT.md
@/Users/jon/projects/rulestead/.planning/ROADMAP.md
@/Users/jon/projects/rulestead/.planning/STATE.md
@/Users/jon/projects/rulestead/.planning/phases/98-admin-re-skin-css-cascade/98-CONTEXT.md
@/Users/jon/projects/rulestead/.planning/phases/98-admin-re-skin-css-cascade/98-RESEARCH.md
@/Users/jon/projects/rulestead/.planning/phases/98-admin-re-skin-css-cascade/98-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from direct file inspection. -->

Block 1 target swaps — 7 declarations change (from tokens.json admin_css_mapping.light — encode verbatim, recompute nothing):
  --rs-primary:       #2563eb  →  #3A6F8F
  --rs-primary-hover: #1d4ed8  →  #2d5f7c
  --rs-accent:        #9a3f12  →  #9b5931
  --rs-success:       #15803d  →  #2d7753
  --rs-warning:       #b45309  →  #8f601a
  --rs-error:         #b91c1c  →  #B44949
  --rs-critical:      #b91c1c  →  #B44949

All other hex-literal tokens in Block 1 stay unchanged (neutral ramp #ffffff/#f8fafc/etc., soft tints #dbeafe/#fde8dc/etc., hover/text/border variants). Do NOT touch rgba() composites or var() aliases.

Block 1 location: lines 225-303. Opens with ".rs-shell," (line 225) followed by "[data-rulestead] {" (line 226). Closes with "}" at line 303.

Block 4 location: lines 469-549. Opens with ".rs-shell[data-theme=\"light\"]," (line 470). After the edit to Block 1, Block 4 must be a byte-identical copy of the Block 1 interior (from the opening "{" to the closing "}"). The selector lines themselves differ between the two blocks — only the declaration content inside must be identical (this is what check_synced_pair.py's decls() function checks, not the selector lines).

check_brand_tokens.py oracle — post-edit verification:
  python3 scripts/check_brand_tokens.py must exit 0 with "BRAND TOKENS SYNCED (N tokens)"
  (still exits 1 for dark block mismatches until Plan 98-03 runs — this plan only fixes light)

check_synced_pair.py oracle — post-edit verification:
  python3 scripts/check_synced_pair.py must exit 0 confirming Block 1≡4 light pair identical
  (Block 2≡3 dark pair should still pass as before — this plan does not touch Blocks 2 or 3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin Block 1 — 7 light hex swaps from tokens.json admin_css_mapping.light</name>
  <files>rulestead_admin/priv/static/css/rulestead_admin.css</files>
  <read_first>
    - rulestead_admin/priv/static/css/rulestead_admin.css lines 225-303 (Block 1 current state — all declarations to be changed and all declarations to remain unchanged)
    - brandbook/tokens.json lines 302-346 (admin_css_mapping.light — the authoritative target values; read these, copy verbatim, compute nothing)
  </read_first>
  <action>
    Edit Block 1 only (lines 225-303). Apply exactly 7 color hex swaps to the `--rs-*` brand/status declarations per the `admin_css_mapping.light` table in tokens.json. Read tokens.json first and copy the target values verbatim — do not re-derive or adjust any hex.

    Swaps to apply:
    - Line with `--rs-primary:`:       replace `#2563eb` with `#3A6F8F`
    - Line with `--rs-primary-hover:`: replace `#1d4ed8` with `#2d5f7c`
    - Line with `--rs-accent:`:        replace `#9a3f12` with `#9b5931`
    - Line with `--rs-success:`:       replace `#15803d` with `#2d7753`
    - Line with `--rs-warning:`:       replace `#b45309` with `#8f601a`
    - Line with `--rs-error:`:         replace `#b91c1c` with `#B44949`
    - Line with `--rs-critical:`:      replace `#b91c1c` with `#B44949`

    Zero other changes. Do not touch: the selector lines (`.rs-shell,` / `[data-rulestead] {`), the `color-scheme: light;` declaration, any comment text, any neutral ramp token (--rs-neutral-*), any var() alias declaration, any rgba() composite (--rs-shadow-*, --rs-focus-ring, --rs-overlay-veil, --rs-scrim), any soft/subtle/hover/text/border variant that is NOT in the 7 list above (e.g., --rs-primary-soft, --rs-on-primary, --rs-accent-soft, --rs-success-hover, --rs-success-soft, --rs-success-bg-subtle, --rs-success-text, --rs-success-border, --rs-warning-hover, --rs-warning-soft, --rs-warning-text, --rs-warning-border, --rs-error-hover, --rs-error-soft, --rs-error-bg-subtle, --rs-error-text, --rs-error-text-strong, --rs-error-border, --rs-error-border-strong, --rs-primary-ring, --rs-disabled-bg, --rs-disabled-text).

    Do not touch Block 2 (lines 305-386), Block 3 (lines 388-467), or Block 4 (lines 469-549) in this task.

    After making the 7 edits, immediately run `python3 scripts/check_brand_tokens.py` to confirm the light block now matches — it should exit 0 (or show only [dark] mismatches if D-05b is also active, which is expected until Plan 98-03 runs the dark swap). If it still shows light mismatches, recheck the hex values against tokens.json.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead && python3 scripts/check_brand_tokens.py 2>&1 | grep -v '^\s*\[dark\]'</automated>
  </verify>
  <acceptance_criteria>
    - python3 scripts/check_brand_tokens.py output contains no light-block mismatches (lines without "[dark]" prefix show 0 mismatches)
    - git diff rulestead_admin/priv/static/css/rulestead_admin.css shows exactly 7 changed lines in Block 1, all containing hex color values — no spacing, no comment, no property name change
    - Block 4 (lines 469-549) is unchanged in this task
    - Block 2 and Block 3 are unchanged
  </acceptance_criteria>
  <done>Block 1 has exactly 7 swapped hex declarations matching tokens.json admin_css_mapping.light; check_brand_tokens.py light mismatches = 0</done>
</task>

<task type="auto">
  <name>Task 2: Mirror Block 1 → Block 4 verbatim (light synced-pair invariant)</name>
  <files>rulestead_admin/priv/static/css/rulestead_admin.css</files>
  <read_first>
    - rulestead_admin/priv/static/css/rulestead_admin.css lines 225-303 (Block 1 after Task 1 edits — this is the source to mirror)
    - rulestead_admin/priv/static/css/rulestead_admin.css lines 469-549 (Block 4 current state — the mirror target; its selector lines differ from Block 1's selectors but interior declarations must become byte-identical to Block 1's interior)
  </read_first>
  <action>
    Mirror the interior of Block 1 verbatim into Block 4. "Interior" means every line from (and including) the opening `{` through (and including) the closing `}` — everything except the selector lines themselves.

    Specifically: Block 1 opens with `.rs-shell,\n[data-rulestead] {` (lines 225-226); Block 4 opens with `.rs-shell[data-theme="light"],\n[data-rulestead][data-theme="light"] {` (lines 469-471). These selector lines DIFFER and must remain as-is in each block.

    The interior (from `{` to `}`) must be byte-identical between Block 1 and Block 4. Copy the interior of Block 1 as it stands after Task 1 (including `color-scheme: light;`, all comments, all declarations in exactly the same order with the same whitespace). Replace the interior of Block 4 with this copy.

    After the copy, run `python3 scripts/check_synced_pair.py`. The script's `decls()` function extracts `--rs-*` declarations from each block and compares them as sorted lists — the light pair assertion (D-05a, added in Plan 98-01) must pass.

    Also run `mix phx.digest` in the rulestead_admin/ directory to regenerate the fingerprint manifest for the updated CSS file.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead && python3 scripts/check_synced_pair.py</automated>
  </verify>
  <acceptance_criteria>
    - python3 scripts/check_synced_pair.py exits 0 with both pair lines: dark pair line unchanged ("SYNCED PAIR IDENTICAL (56 tokens)") AND light pair line present (confirms Block 1≡4)
    - git diff rulestead_admin/priv/static/css/rulestead_admin.css — Block 4 changes show only the 7 hex value substitutions (same ones as Block 1), no structural changes
    - Block 2 (lines 305-386) and Block 3 (lines 388-467) remain unchanged
    - mix phx.digest completed successfully in rulestead_admin/
  </acceptance_criteria>
  <done>Block 4 is byte-identical to Block 1 interior; check_synced_pair.py exits 0 confirming light pair; dark pair still confirmed; phx.digest regenerated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tokens.json → CSS file | Hex values copied verbatim from tokens.json into CSS; no recomputation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-04 | Tampering | rulestead_admin.css Block 1 edit | accept | No security-relevant surface. This is a CSS color token edit guarded by check_brand_tokens.py. Correctness guarded by check_brand_tokens.py / check_synced_pair.py. |
| T-98-05 | Tampering | rulestead_admin.css Block 4 mirror | accept | No security-relevant surface. Correctness guarded by check_synced_pair.py light-pair assertion (D-05a, added in Plan 98-01). |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | No package installs. Python stdlib + CSS edits only. |
</threat_model>

<verification>
Run from repo root after both tasks complete:

```
python3 scripts/check_synced_pair.py
python3 scripts/check_brand_tokens.py
python3 scripts/check_tokens_css.py
python3 scripts/check_contrast.py
git diff rulestead_admin/priv/static/css/rulestead_admin.css
```

Expected:
- check_synced_pair.py: exits 0, both dark pair + light pair lines present
- check_brand_tokens.py: exits 1 with only [dark] mismatches (8 dark tokens still wrong until Plan 98-03)
- check_tokens_css.py: exits 0 (unchanged)
- check_contrast.py: exits 0 (unchanged)
- git diff: Block 1 shows 7 hex substitutions, Block 4 shows matching 7 hex substitutions; no other lines changed
</verification>

<success_criteria>
- Block 1 and Block 4 each contain the 7 mineral light hex values from tokens.json admin_css_mapping.light
- check_synced_pair.py exits 0 (both pairs confirmed)
- check_brand_tokens.py light mismatches = 0 (dark still 8, normal until Plan 98-03)
- git diff shows zero non-color property changes in the CSS file
</success_criteria>

<output>
Create `.planning/phases/98-admin-re-skin-css-cascade/98-02-SUMMARY.md` when done
</output>
