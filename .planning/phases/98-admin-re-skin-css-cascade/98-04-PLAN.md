---
phase: 98-admin-re-skin-css-cascade
plan: 04
type: execute
wave: 4
depends_on:
  - 98-03
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/98-admin-re-skin-css-cascade/98-VALIDATION.md
autonomous: false
requirements:
  - SKIN-01
  - SKIN-02
  - SKIN-03
must_haves:
  truths:
    - "All four guard scripts pass: check_synced_pair.py exits 0, check_brand_tokens.py exits 0, check_tokens_css.py exits 0, check_contrast.py exits 0"
    - "design-system.html swatches reflect mineral palette (confirmed via var(--rs-*) transitive update — no manual edits needed)"
    - "CSS diff contains zero non-color property changes (SC-1 gate — manual human review)"
    - "WCAG-AA passes for all normal-weight text pairings in both light and dark themes (check_contrast.py green)"
    - "STATE.md, ROADMAP.md, and REQUIREMENTS.md updated to reflect Phase 98 complete"
    - "Implements CONTEXT decisions D-03 (final SC-1 zero-non-color-diff human gate) and D-06 (design-system.html swatches var-driven — auto-update, no manual hex edits)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Phase 98 complete status + decisions recorded"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 98 marked complete with completion date"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SKIN-01, SKIN-02, SKIN-03 marked complete"
    - path: ".planning/phases/98-admin-re-skin-css-cascade/98-VALIDATION.md"
      provides: "nyquist_compliant: true; all statuses marked green"
  key_links:
    - from: "scripts/check_contrast.py PALETTE_CHECKS"
      to: "rulestead_admin.css --rs-primary: #3A6F8F (light)"
      via: "hardcoded matrix confirms AA ≥4.5:1 on all light/dark surfaces"
      pattern: "CONTRAST CHECK PASS"
    - from: "rulestead_admin/priv/static/design-system.html"
      to: "rulestead_admin.css --rs-* custom properties"
      via: "var(--rs-*) CSS references — no manual edit needed (D-06)"
      pattern: "var\\(--rs-"
---

<objective>
Final verification sweep: run all four guard scripts, confirm design-system.html swatches auto-reflect the mineral palette, perform the manual SC-1 diff review (zero non-color changes gate), and update planning artifacts (STATE.md, ROADMAP.md, REQUIREMENTS.md, VALIDATION.md) to close Phase 98.

Purpose: Make the phase machine-verifiably complete. This plan is the phase close — all guards green, all planning docs current, SC-1 human-reviewed.

Output: Four updated planning files; human checkpoint for SC-1 diff review. No CSS or script changes (those landed in Plans 98-02 and 98-03).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/rulestead/.planning/PROJECT.md
@/Users/jon/projects/rulestead/.planning/ROADMAP.md
@/Users/jon/projects/rulestead/.planning/STATE.md
@/Users/jon/projects/rulestead/.planning/REQUIREMENTS.md
@/Users/jon/projects/rulestead/.planning/phases/98-admin-re-skin-css-cascade/98-CONTEXT.md
@/Users/jon/projects/rulestead/.planning/phases/98-admin-re-skin-css-cascade/98-VALIDATION.md
@/Users/jon/projects/rulestead/.planning/phases/98-admin-re-skin-css-cascade/98-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full guard suite sweep + automated verification</name>
  <files></files>
  <read_first>
    - .planning/phases/98-admin-re-skin-css-cascade/98-VALIDATION.md — the per-task verification map; use this as the completion checklist
    - .planning/ROADMAP.md — Phase 98 success criteria (4 items) to confirm each is met
    - scripts/ci/lint.sh — run the full suite to confirm all guards pass through the CI entry point
  </read_first>
  <action>
    Run the complete guard suite from repo root. All four must exit 0:

    1. `python3 scripts/check_synced_pair.py` — expects output with both dark pair (56 tokens) AND light pair lines; exits 0
    2. `python3 scripts/check_brand_tokens.py` — expects "BRAND TOKENS SYNCED (N tokens)"; exits 0
    3. `python3 scripts/check_tokens_css.py` — expects "TOKENS.CSS MIRROR SYNCED (68 tokens)"; exits 0
    4. `python3 scripts/check_contrast.py` — expects "CONTRAST CHECK PASS (18 checks)"; exits 0

    Then confirm design-system.html swatch behavior: the swatches are 100% var(--rs-*)-driven (D-06 per CONTEXT.md). Run `grep -c 'var(--rs-' rulestead_admin/priv/static/design-system.html` to confirm the swatch block uses var() references. Confirm the two scaffold-chrome literals remain: `grep -n '#333\|#888' rulestead_admin/priv/static/design-system.html` should show exactly line 57 (#333) and line 361 (#888) — and no other hex literals in the swatch section. No manual edits to design-system.html are made.

    Record the output of each command (exit code + success line) for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead && python3 scripts/check_synced_pair.py && python3 scripts/check_brand_tokens.py && python3 scripts/check_tokens_css.py && python3 scripts/check_contrast.py</automated>
  </verify>
  <acceptance_criteria>
    - All four guard scripts exit 0 (combined command exits 0)
    - check_synced_pair.py output contains both a dark-pair line and a light-pair line
    - check_brand_tokens.py output contains "BRAND TOKENS SYNCED" (no "DRIFT")
    - check_tokens_css.py output contains "TOKENS.CSS MIRROR SYNCED"
    - check_contrast.py output contains "CONTRAST CHECK PASS (18 checks)"
    - design-system.html has no manually added hex colors in the swatch region; scaffold chromes #333 and #888 remain at their original lines
  </acceptance_criteria>
  <done>Four guards green; design-system.html swatch behavior confirmed var-driven; SC-2 (SKIN-02) and SC-3 (SKIN-03) both machine-verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 98-02 and 98-03 re-skinned all 4 cascade blocks in rulestead_admin.css to the mineral palette (15 hex swaps total: 7 light + 8 dark). Task 1 of this plan confirmed all four guard scripts exit 0. The CSS diff is now ready for the SC-1 zero-non-color-changes review.
  </what-built>
  <how-to-verify>
    Run from repo root:

    1. `git diff rulestead_admin/priv/static/css/rulestead_admin.css`

    Review every changed line. The acceptance gate (SC-1) requires: every changed line in the diff is a `--rs-*: #xxxxxx;` color declaration line. Zero tolerance for any other change — no spacing normalization, no comment text change, no property rename, no structural line change.

    Expected: 30 changed lines total (7 in Block 1 + 7 in Block 4 + 8 in Block 3 + 8 in Block 2). Each changed line should look like:
      -  --rs-primary:       #2563eb;
      +  --rs-primary:       #3A6F8F;

    2. Open rulestead_admin/priv/static/design-system.html in a browser (or via the file:// harness). Confirm the color swatches display mineral tones — the Stead Blue family (steel-blue hue rather than bright Tailwind blue), mineral success green (muted, not emerald), mineral amber warning (not Tailwind yellow), mineral error red (muted, not bright red). Both light and dark theme views should show the mineral palette.

    3. Optionally: check both themes in the design-system.html viewer to confirm visual consistency.
  </how-to-verify>
  <resume-signal>Type "approved" if the diff contains only color hex swap lines and the design-system.html swatches look correct in both themes. Or describe any non-color diff lines found (SC-1 violation) or visual concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update planning artifacts — STATE.md, ROADMAP.md, REQUIREMENTS.md, VALIDATION.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/98-admin-re-skin-css-cascade/98-VALIDATION.md</files>
  <read_first>
    - .planning/STATE.md — full current state; identify the "Current Position" section and "Accumulated Context > Decisions" section
    - .planning/ROADMAP.md — Phase 98 entry and the Progress Table at the bottom
    - .planning/REQUIREMENTS.md — Traceability table; SKIN-01, SKIN-02, SKIN-03 rows
    - .planning/phases/98-admin-re-skin-css-cascade/98-VALIDATION.md — frontmatter (nyquist_compliant, wave_0_complete) and per-task status column
  </read_first>
  <action>
    Update four planning files after the human checkpoint approves:

    STATE.md:
    - Update "Current Position" section: Phase → 98 (admin-re-skin-css-cascade), Plan → 4 of 4 complete, Status → Phase complete — ready for verification, Last activity → today's date
    - Update "Current focus" line to indicate Phase 99 (Specimens) is next
    - Add decision entries under "Accumulated Context > Decisions" for the key Phase 98 outcomes:
      "[Phase 98]: All 4 cascade blocks re-skinned to mineral palette — colors only; 15 hex swaps (7 light Block 1+4, 8 dark Block 3+2)"
      "[Phase 98]: check_synced_pair.py extended to assert both Block 1≡4 (light) and Block 2≡3 (dark) pairs"
      "[Phase 98]: check_brand_tokens.py extended to diff Block 3 vs admin_css_mapping.dark"
      "[Phase 98]: lint.sh CWD bug fixed — cd RULESTEAD_REPO before guard invocations"
      "[Phase 98]: design-system.html swatches auto-reflected mineral palette via var(--rs-*) — zero manual edits"
    - Update progress bar: increment completed_phases to 4 and update the percent/progress bar accordingly (4/7 phases, ~57%)

    ROADMAP.md:
    - In the Phase 98 entry: change `- [ ] **Phase 98:` to `- [x] **Phase 98:`
    - Add completion note: "(completed YYYY-MM-DD)" at the end of the Phase 98 goal sentence
    - Update the Phase 98 Plans section: replace the four `- [ ] 98-0N-PLAN.md — (to be planned)` stubs with the actual plan objectives and mark each `[x]`
    - Update the Progress Table row for Phase 98: Plans Complete → "4/4", Status → "Complete", Completed → today's date

    REQUIREMENTS.md:
    - Update Traceability table: SKIN-01 row → Status: "Complete"; SKIN-02 row → Status: "Complete"; SKIN-03 row → Status: "Complete"
    - Update the requirement checkboxes: `- [ ] **SKIN-01**:` → `- [x] **SKIN-01**:` (and similarly for SKIN-02, SKIN-03)

    98-VALIDATION.md:
    - Set frontmatter `nyquist_compliant: true` and `wave_0_complete: true`
    - Update all ⬜ pending statuses in the per-task verification map to ✅ green
    - Set `**Approval:** ✅ Phase 98 complete — all guards green, SC-1 diff reviewed and approved`
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead && grep -c "SKIN-01.*Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md Current Position shows Phase 98 complete with all 4 decisions recorded
    - ROADMAP.md Phase 98 entry is checked `[x]` with completion date; all 4 plan stubs replaced with actual objectives
    - REQUIREMENTS.md SKIN-01, SKIN-02, SKIN-03 are checked `[x]` and show "Complete" in Traceability table
    - 98-VALIDATION.md shows nyquist_compliant: true and all statuses ✅ green
    - git diff of planning files shows only the expected updates — no other sections touched
  </acceptance_criteria>
  <done>All four planning artifacts updated; Phase 98 marked complete; SKIN-01/02/03 requirements closed; VALIDATION.md signed off</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI guard scripts → repo state | Guards confirm CSS and token sync invariants hold; this plan runs them as the phase gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-08 | Tampering | Planning doc updates | accept | No security-relevant surface. Planning files are docs, not runtime code. Human checkpoint (SC-1 review) gates the doc updates. |
| T-98-09 | Information Disclosure | design-system.html | accept | No security-relevant surface. Static HTML fixture with no auth/session/PII. The swatch review confirms var-driven rendering only — no new hex literals introduced. |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Guard script runs only. |
</threat_model>

<verification>
Phase 98 is complete when all of the following are true:

1. `python3 scripts/check_synced_pair.py` — exits 0, both pairs confirmed
2. `python3 scripts/check_brand_tokens.py` — exits 0, "BRAND TOKENS SYNCED"
3. `python3 scripts/check_tokens_css.py` — exits 0, "TOKENS.CSS MIRROR SYNCED"
4. `python3 scripts/check_contrast.py` — exits 0, "CONTRAST CHECK PASS (18 checks)"
5. `git diff rulestead_admin/priv/static/css/rulestead_admin.css` — reviewed by human, zero non-color lines changed
6. SKIN-01, SKIN-02, SKIN-03 marked complete in REQUIREMENTS.md
7. ROADMAP.md Phase 98 checked [x] with completion date

ROADMAP Success Criteria mapping:
- SC-1: mineral hex in all 4 blocks, zero non-color diff → verified by guards + human diff review
- SC-2: check_synced_pair.py exits 0 (both pairs) → verified in Task 1
- SC-3: check_brand_tokens.py exits 0 → verified in Task 1
- SC-4: design-system.html swatches reflect mineral (var-driven, transitive) + WCAG-AA (check_contrast.py green) → verified in Task 1 + human checkpoint
</verification>

<success_criteria>
- All four guard scripts exit 0
- SC-1 diff review approved by human (zero non-color lines changed)
- STATE.md, ROADMAP.md, REQUIREMENTS.md, VALIDATION.md all updated
- Phase 98 complete — ready for Phase 99 (Specimens)
</success_criteria>

<output>
Create `.planning/phases/98-admin-re-skin-css-cascade/98-04-SUMMARY.md` when done
</output>
