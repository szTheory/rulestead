---
phase: 98-admin-re-skin-css-cascade
plan: 03
type: execute
wave: 3
depends_on:
  - 98-02
files_modified:
  - rulestead_admin/priv/static/css/rulestead_admin.css
autonomous: true
requirements:
  - SKIN-01
  - SKIN-02
  - SKIN-03
must_haves:
  truths:
    - "Block 3 (.rs-shell[data-theme=dark]) has 8 brand/status hex declarations replaced with mineral targets from tokens.json admin_css_mapping.dark"
    - "The --rs-success-border change from #166534 to #166634 is applied correctly in Block 3"
    - "Block 2 (@media prefers-color-scheme dark) is byte-identical to Block 3 — mirrored verbatim"
    - "Zero non-color property changes in the CSS diff (SC-1)"
    - "check_synced_pair.py exits 0 confirming both dark pair (Block 2≡3) AND light pair (Block 1≡4) identical"
    - "check_brand_tokens.py exits 0 (all mismatches resolved — both light and dark blocks now match tokens.json)"
  artifacts:
    - path: "rulestead_admin/priv/static/css/rulestead_admin.css"
      provides: "All 4 cascade blocks re-skinned to mineral palette; SKIN-01 complete"
      contains: "--rs-primary: #5885a0"
  key_links:
    - from: "brandbook/tokens.json admin_css_mapping.dark"
      to: "rulestead_admin.css Block 3 (lines 388-467)"
      via: "verbatim hex copy — recompute nothing"
      pattern: "--rs-primary.*#5885a0"
    - from: "rulestead_admin.css Block 3 (lines 388-467)"
      to: "rulestead_admin.css Block 2 (lines 305-386)"
      via: "verbatim mirror — byte-identical copy"
      pattern: "SYNCED PAIR IDENTICAL.*56 tokens"
---

<objective>
Re-skin Block 3 (explicit dark `.rs-shell[data-theme="dark"]`, lines 388-467) with the 8 mineral dark hex swaps from `tokens.json admin_css_mapping.dark` — including the critical one-digit `--rs-success-border` fix (`#166534` → `#166634`) — then mirror Block 3 verbatim into Block 2 (system dark `@media (prefers-color-scheme: dark)`, lines 305-386) to maintain the dark synced-pair invariant. Colors only — zero non-color changes.

Purpose: Complete the full CSS re-skin. After this plan, check_brand_tokens.py exits 0 (both light and dark blocks match tokens.json), check_synced_pair.py exits 0 (both pairs confirmed), and design-system.html swatches auto-reflect the mineral palette via var(--rs-*) — SKIN-01 done.

Output: rulestead_admin.css modified (Blocks 2 and 3 only, plus phx.digest). No script changes, no new files, no non-color property changes.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/rulestead/.planning/PROJECT.md
@/Users/jon/projects/rulestead/.planning/ROADMAP.md
@/Users/jon/projects/rulestead/.planning/STATE.md
@/Users/jon/projects/rulestead/.planning/phases/98-admin-re-skin-css-cascade/98-CONTEXT.md
@/Users/jon/projects/rulestead/.planning/phases/98-admin-re-skin-css-cascade/98-RESEARCH.md
@/Users/jon/projects/rulestead/.planning/phases/98-admin-re-skin-css-cascade/98-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from direct file inspection. -->

Block 3 target swaps — 8 declarations change (from tokens.json admin_css_mapping.dark — encode verbatim, recompute nothing):
  --rs-primary:        #2563eb  →  #5885a0
  --rs-primary-hover:  #5a96f5  →  #4a7d9c
  --rs-accent:         #e8834a  →  #ba6b3c
  --rs-success:        #4ade80  →  #488d6b
  --rs-warning:        #fbbf24  →  #B57A21
  --rs-error:          #f87171  →  #bf6464
  --rs-critical:       #f87171  →  #bf6464
  --rs-success-border: #166534  →  #166634   ← ONE-DIGIT CHANGE (3→6 in third hex pair); highest-risk edit

The --rs-success-border change: current value is #166534, target is #166634. The difference is
in the second hex byte: "53" becomes "66". A character-by-character review of this specific line
in the final diff is required. This change is guarded by check_brand_tokens.py D-05b dark diff
(added in Plan 98-01) which will explicitly flag "[dark] --rs-success-border" if the wrong value
is written.

All other tokens in Block 3 stay unchanged: neutral ramp (--rs-neutral-0 through --rs-neutral-900),
var() aliases (--rs-bg through --rs-text-placeholder), on-primary (#ffffff), soft/rgba() composites
(--rs-primary-soft, --rs-accent-soft, --rs-success-soft, --rs-success-bg-subtle, --rs-success-text,
--rs-warning-soft, --rs-warning-text, --rs-warning-border, --rs-error-soft, --rs-error-bg-subtle,
--rs-error-text, --rs-error-text-strong, --rs-error-border, --rs-error-border-strong),
success-hover (#86efac — stays), shadow rgba() composites, focus-ring rgba(), primary-ring rgba(),
--rs-disabled-bg (#253243 — stays, matches tokens.json dark), --rs-disabled-text (#6b84a0 — stays).

Block 3 location: lines 388-467. Opens with ".rs-shell[data-theme=\"dark\"]," (line 389) and
"[data-rulestead][data-theme=\"dark\"] {" (line 390). Header comment says "SYNCED PAIR: keep
this block identical to @media dark above" (line 392).

Block 2 location: lines 305-386. Opens with "@media (prefers-color-scheme: dark) {" (line 305),
inside is ".rs-shell," block. After editing Block 3, Block 2's interior CSS declarations must be
byte-identical to Block 3's interior declarations (this is what check_synced_pair.py guards).

D-02 CRITICAL: Block 3 is the source-of-truth for dark. Mirror Block 3 → Block 2. Do NOT edit
Block 2 directly and mirror to Block 3 (Pitfall 4 in RESEARCH.md: Block 2 appears first in the
file at line 305, which can tempt editing it first — resist).

check_brand_tokens.py oracle — post-edit verification:
  python3 scripts/check_brand_tokens.py must exit 0 with "BRAND TOKENS SYNCED (N tokens)"
  (all 15 mismatches resolved — 7 light fixed in Plan 98-02, 8 dark fixed here)

check_synced_pair.py oracle — post-edit verification:
  python3 scripts/check_synced_pair.py must exit 0 confirming both dark pair (Block 2≡3) and
  light pair (Block 1≡4) identical — both lines present.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin Block 3 — 8 dark hex swaps from tokens.json admin_css_mapping.dark</name>
  <files>rulestead_admin/priv/static/css/rulestead_admin.css</files>
  <read_first>
    - rulestead_admin/priv/static/css/rulestead_admin.css lines 388-467 (Block 3 current state — all declarations; identify the 8 lines to change and all lines to leave unchanged)
    - brandbook/tokens.json lines 348-387 (admin_css_mapping.dark — the authoritative target values; read these, copy verbatim, compute nothing)
  </read_first>
  <action>
    Edit Block 3 only (lines 388-467). Apply exactly 8 color hex swaps to the `--rs-*` brand/status declarations per the `admin_css_mapping.dark` table in tokens.json. Read tokens.json first and copy target values verbatim.

    Swaps to apply:
    - Line with `--rs-primary:`:        replace `#2563eb` with `#5885a0`
    - Line with `--rs-primary-hover:`:  replace `#5a96f5` with `#4a7d9c`
    - Line with `--rs-accent:`:         replace `#e8834a` with `#ba6b3c`
    - Line with `--rs-success:`:        replace `#4ade80` with `#488d6b`
    - Line with `--rs-warning:`:        replace `#fbbf24` with `#B57A21`
    - Line with `--rs-error:`:          replace `#f87171` with `#bf6464`
    - Line with `--rs-critical:`:       replace `#f87171` with `#bf6464`
    - Line with `--rs-success-border:`: replace `#166534` with `#166634` ← ONE DIGIT CHANGE — verify character-by-character: current "166534", target "166634"; the second hex pair changes from "53" to "66"

    Zero other changes. Do not touch: selector lines, `color-scheme: dark;`, the "SYNCED PAIR" comment, any neutral ramp (--rs-neutral-*), any var() alias, any rgba() composite (shadows, focus-ring, primary-ring, overlay-veil, scrim, all soft/rgba tokens), --rs-on-primary, --rs-success-hover (#86efac — leave), --rs-success-text, --rs-warning-hover, --rs-warning-text, --rs-warning-border, all --rs-error-* variants beyond --rs-error itself and --rs-critical, --rs-disabled-bg (#253243 — leave), --rs-disabled-text (#6b84a0 — leave).

    Do not touch Block 1 (lines 225-303), Block 2 (lines 305-386), or Block 4 (lines 469-549) in this task.

    After making the 8 edits, run `python3 scripts/check_brand_tokens.py` to confirm all mismatches are now resolved. It should exit 0 with "BRAND TOKENS SYNCED". If any [dark] mismatch remains, it means a target value was mis-typed — recheck against tokens.json. The --rs-success-border line is the highest-risk: confirm the written value is `#166634` (not `#166534`).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead && python3 scripts/check_brand_tokens.py</automated>
  </verify>
  <acceptance_criteria>
    - python3 scripts/check_brand_tokens.py exits 0 with "BRAND TOKENS SYNCED" — zero mismatches (neither light nor dark)
    - The --rs-success-border line in Block 3 reads `#166634` (target) not `#166534` (old value)
    - git diff rulestead_admin/priv/static/css/rulestead_admin.css shows exactly 8 changed lines in Block 3, all containing hex color values — no spacing, no comment, no property name change
    - Block 1, Block 2, and Block 4 are unchanged in this task
  </acceptance_criteria>
  <done>Block 3 has exactly 8 swapped hex declarations matching tokens.json admin_css_mapping.dark; check_brand_tokens.py exits 0; --rs-success-border is #166634</done>
</task>

<task type="auto">
  <name>Task 2: Mirror Block 3 → Block 2 verbatim (dark synced-pair invariant)</name>
  <files>rulestead_admin/priv/static/css/rulestead_admin.css</files>
  <read_first>
    - rulestead_admin/priv/static/css/rulestead_admin.css lines 388-467 (Block 3 after Task 1 edits — this is the source to mirror)
    - rulestead_admin/priv/static/css/rulestead_admin.css lines 305-386 (Block 2 current state — the mirror target; its wrapper is @media (prefers-color-scheme: dark) { .rs-shell, { ... } } — the interior .rs-shell, { declarations must be byte-identical to Block 3's interior after the mirror)
  </read_first>
  <action>
    Mirror the interior declaration content of Block 3 verbatim into Block 2's inner block.

    Block 2 structure (lines 305-386): The outer wrapper is `@media (prefers-color-scheme: dark) {` ... `}`. Inside the media query is `.rs-shell,\n[data-rulestead] {` ... `}`. The interior declarations of this inner block must become byte-identical to Block 3's interior declarations (from `{` to `}`), including the `color-scheme: dark;` declaration, the "SYNCED PAIR" comment, and all other comments and declarations in their exact order and whitespace.

    Copy the interior of Block 3 (everything between and including the opening `{` at line 390 through the closing `}` of the inner block — i.e., the same content that decls() extracts) and replace Block 2's inner block interior with it.

    After the copy, run `python3 scripts/check_synced_pair.py`. The dark-pair check (Block 2 ≡ Block 3) must confirm identical. The light-pair check (Block 1 ≡ Block 4, preserved from Plan 98-02) must also still confirm identical. The script must exit 0 with both pair lines present.

    Also run `mix phx.digest` in the rulestead_admin/ directory to regenerate the fingerprint manifest for the final CSS state.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead && python3 scripts/check_synced_pair.py && python3 scripts/check_brand_tokens.py</automated>
  </verify>
  <acceptance_criteria>
    - python3 scripts/check_synced_pair.py exits 0 with both pair lines: dark pair "SYNCED PAIR IDENTICAL (56 tokens)" and light pair line
    - python3 scripts/check_brand_tokens.py exits 0 with "BRAND TOKENS SYNCED"
    - git diff rulestead_admin/priv/static/css/rulestead_admin.css — Block 2 changes show only the 8 hex substitutions matching Block 3; no structural changes; zero non-color lines changed across the entire diff
    - Block 1 and Block 4 remain unchanged (were completed in Plan 98-02)
    - mix phx.digest completed successfully in rulestead_admin/
  </acceptance_criteria>
  <done>Block 2 is byte-identical to Block 3; check_synced_pair.py exits 0 (both pairs); check_brand_tokens.py exits 0; all 4 cascade blocks re-skinned to mineral palette (SKIN-01 complete)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tokens.json → CSS file | Hex values copied verbatim from tokens.json into CSS; no recomputation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-06 | Tampering | rulestead_admin.css Block 3 edit | accept | No security-relevant surface. This is a CSS color token edit. Correctness guarded by check_brand_tokens.py dark diff (D-05b). The --rs-success-border one-digit change (#166534→#166634) is explicitly caught by this guard. |
| T-98-07 | Tampering | rulestead_admin.css Block 2 mirror | accept | No security-relevant surface. Correctness guarded by check_synced_pair.py dark-pair assertion. Mirror-not-generate policy prevents silent drift. |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | No package installs. CSS edit only. |
</threat_model>

<verification>
Run from repo root after both tasks complete:

```
python3 scripts/check_synced_pair.py
python3 scripts/check_brand_tokens.py
python3 scripts/check_tokens_css.py
python3 scripts/check_contrast.py
git diff rulestead_admin/priv/static/css/rulestead_admin.css
```

Expected results (full green — all 4 blocks re-skinned):
- check_synced_pair.py: exits 0, both dark pair + light pair lines present
- check_brand_tokens.py: exits 0, "BRAND TOKENS SYNCED" — all 15 mismatches resolved
- check_tokens_css.py: exits 0, "TOKENS.CSS MIRROR SYNCED (68 tokens)" — unchanged
- check_contrast.py: exits 0, "CONTRAST CHECK PASS (18 checks)" — unchanged
- git diff: exactly 15 changed lines total across 4 blocks (7 in Block 1, 7 in Block 4, 8 in Block 3, 8 in Block 2); every changed line is a --rs-* color declaration; zero non-color lines
</verification>

<success_criteria>
- All 4 cascade blocks contain mineral palette hex values
- check_synced_pair.py exits 0 (both pairs)
- check_brand_tokens.py exits 0 (all tokens synced)
- git diff shows zero non-color property changes in the CSS file
- SKIN-01 complete
</success_criteria>

<output>
Create `.planning/phases/98-admin-re-skin-css-cascade/98-03-SUMMARY.md` when done
</output>
