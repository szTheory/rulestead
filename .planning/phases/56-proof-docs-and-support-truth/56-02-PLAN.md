---
phase: 56-proof-docs-and-support-truth
plan: 02
type: execute
wave: 2
depends_on:
  - 56-01
files_modified:
  - rulestead/test/rulestead/release_contract_test.exs
  - README.md
  - MAINTAINING.md
  - rulestead/README.md
  - rulestead_admin/README.md
requirements:
  - VER-02
autonomous: true
must_haves:
  truths:
    - "A new `release_contract_test.exs` block mirrors `guarded_rollout_foundations` drift guards for reusable-targeting support truth across root README, MAINTAINING, and sibling package READMEs."
    - "Public and package docs cite `mix verify.phase56`, preview-basis limits, tenant/environment scope, preview → confirm → audit, fail-closed blockers, and host-owned identity/observability boundaries."
    - "Forbidden operator-doc phrases (standalone admin, built-in observability, authoritative population counts, segment library UI) are refuted by release-contract tests."
  artifacts:
    - path: "rulestead/test/rulestead/release_contract_test.exs"
      provides: "`reusable targeting deepening support truth` drift guard block"
    - path: "README.md"
      provides: "Proof today entry for v1.6 reusable targeting deepening"
    - path: "MAINTAINING.md"
      provides: "Reusable Targeting Deepening Proof maintainer section"
  key_links:
    - from: "rulestead/test/rulestead/release_contract_test.exs"
      to: "README.md, MAINTAINING.md, rulestead/README.md, rulestead_admin/README.md"
      via: "assert doc =~ required fragments; refute forbidden phrases"
      pattern: "reusable targeting deepening support truth"
    - from: "README.md"
      to: "rulestead/lib/mix/tasks/verify.phase56.ex"
      via: "mix verify.phase56 proof citation"
      pattern: "mix verify.phase56"
---

<threat_model>
## ASVS L1 Threat Register

| Threat ID | ASVS L1 Category | Component | Disposition | Mitigation Plan |
|-----------|-------------------|-----------|-------------|-----------------|
| T-56-02 | V7 Error Handling — misleading support docs | README/MAINTAINING | mitigate | Required phrase clusters for preview basis, uncertainty, fail-closed, scope semantics; forbidden observability/population claims. |
| T-56-03 | V1 Architecture — package boundary drift | Package READMEs | mitigate | Assert `rulestead` owns domain/validation/contracts; `rulestead_admin` is mounted companion only; refute `standalone rulestead_admin`. |
| T-56-05 | V7 Error Handling — terminology drift | Operator-facing docs | mitigate | Refute operator-facing `segment library` / `manage segments`; require **Audience** vocabulary in doc updates. |
| T-56-06 | V14 Configuration — proof command drift | Maintainer docs | mitigate | `assert` blocks require `mix verify.phase56` in root README and MAINTAINING; optional CI scope string reserved for 56-04. |
</threat_model>

<objective>
Lock support-truth drift guards and public/package doc alignment for v1.6 reusable targeting deepening (VER-02).

Purpose: Docs and release-contract checks must describe the same supported scope — preview-basis limits, tenant/environment semantics, host-owned boundaries — without manual spot-checks alone.
Output: Extended `release_contract_test.exs`, updated README/MAINTAINING/package READMEs with coupled required/forbidden phrase guards.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-proof-docs-and-support-truth/56-CONTEXT.md
@.planning/phases/56-proof-docs-and-support-truth/56-RESEARCH.md
@.planning/phases/54-dependency-truth-and-promotion-safety/54-HANDOFF-CHECKLIST.md
@.planning/phases/55-mounted-operator-workflows/55-HANDOFF-CHECKLIST.md
@rulestead/test/rulestead/release_contract_test.exs
@.planning/research/PITFALLS.md
</context>

<source_audit>
GOAL: public docs, package docs, release-contract checks describe same reusable-targeting scope — covered.
REQ VER-02: covered (this plan); flow guide prose in 56-03.
CONTEXT D-03, D-05, D-07, D-09: covered — guarded_rollout mirror; Audience vocabulary; no Phase 8 docs; telemetry as admin signals only.
Upstream: cite 54/55 handoff checklists in MAINTAINING section prose.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Update README and package README proof sections</name>
  <read_first>
    - README.md
    - rulestead/README.md
    - rulestead_admin/README.md
    - rulestead/test/rulestead/release_contract_test.exs
    - .planning/phases/56-proof-docs-and-support-truth/56-RESEARCH.md
  </read_first>
  <action>
    Edit public/package READMEs in one coherent wave. Use **Audience** (not "segment") in operator-facing copy.

    **Root `README.md` — extend `## Proof today` section with a v1.6 bullet block containing:**
    - Heading or lead phrase: `reusable targeting deepening` or `reusable audience deepening`
    - `mix verify.phase56` as primary maintainer command
    - `preview basis` (or `authored state`) and `explicit sample` language
    - `environment` scope + `tenant` scope semantics (e.g. `environment scope`, `tenant scope`, or `?env=` / `tenant_key`)
    - `preview` → `confirm` → `audit` workflow phrase
    - `fail closed` for missing/archived/incompatible/stale references
    - `host-owned` + (`identity` or `observability`) boundary
    - `audprev_` or `preview fingerprint` mention
    - `uncertainty` or `does not claim` language for population/affected-user counts
    - `mounted companion` for admin package (not standalone product)

    **`rulestead/README.md` — add bounded support-truth paragraph:**
    - `rulestead` owns `domain` / `validation` / `contracts`
    - dependency + promotion/manifest `fail closed` language
    - `no metrics ingestion`, `no dashboards`, or equivalent host-owned observability boundary
    - `mix verify.phase56` citation

    **`rulestead_admin/README.md` — add mounted companion paragraph:**
    - `mounted companion` / `mounted presentation`
    - reads core dependency/preview/audit truth; `not a standalone admin`
    - `/admin/audiences` path-level mention (no LiveView internals)
    - `preview` + `confirm` + `audit` for audience mutations

    Do **not** add Phase 8-only artifacts (`guides/cheatsheet.cheatmd`, extending-rulestead expansion).
  </action>
  <verify>
    <automated>rg -n "mix verify\.phase56|reusable targeting|preview basis|fail closed|mounted companion" README.md rulestead/README.md rulestead_admin/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "mix verify\\.phase56|reusable targeting|preview basis|fail closed|mounted companion" README.md rulestead/README.md rulestead_admin/README.md` finds required phrases in all three targets.
    - `rg -n "segment library|manage segments|standalone rulestead_admin|built-in observability" README.md rulestead/README.md rulestead_admin/README.md` finds no forbidden operator phrases.
    - `rg -n "Proof today" README.md` finds section with v1.6 deepening entry.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add MAINTAINING proof section and release_contract drift guard block</name>
  <read_first>
    - MAINTAINING.md
    - rulestead/test/rulestead/release_contract_test.exs
    - .planning/phases/56-proof-docs-and-support-truth/56-CONTEXT.md
    - .planning/phases/54-dependency-truth-and-promotion-safety/54-HANDOFF-CHECKLIST.md
    - .planning/phases/55-mounted-operator-workflows/55-HANDOFF-CHECKLIST.md
  </read_first>
  <action>
    **MAINTAINING.md** — add section `## Reusable Targeting Deepening Proof` (parallel to `## Guarded Rollout Foundations Proof` ~line 220) containing:
    - When to run: reusable audience/dependency/preview/compare/promotion docs or VER-01 v1.6 changes
    - Primary command: `cd rulestead && mix verify.phase56`
    - Bounded claims list: proves dependency inventory, preview determinism, stale-token rejection, fail-closed blockers, audit evidence, explain trace carry-through, promotion/manifest blockers
    - Explicit non-claims: no authoritative affected-user counts, no graph visualizer, no bulk automation, no Rulestead-owned metrics/dashboards
    - References to `54-HANDOFF-CHECKLIST.md` and `55-HANDOFF-CHECKLIST.md` as upstream boundary contracts
    - `VER-01` mapping for v1.6 milestone

    **`release_contract_test.exs`** — add sibling test after guarded_rollout block (~line 336):

    ```elixir
    test "reusable targeting deepening support truth stays bounded across root package and maintainer docs" do
    ```

    Mirror guarded_rollout structure: read `@root_readme_path`, `@runtime_readme_path`, `@admin_readme_path`, `@maintaining_path`.

    **Required asserts (use stable substrings matching edited docs):**
    - All four doc targets: `mix verify.phase56`
    - Root README + MAINTAINING: `reusable targeting` or `reusable audience`
    - Preview basis cluster: `preview basis` or `authored state`; `explicit sample` or `explicit samples`
    - Scope cluster: `environment` + scope language; `tenant` scope language
    - Workflow: `preview` + `confirm` + `audit` (or arrow variant)
    - Safety: `fail closed` or `fails closed`; `stale preview` or `preview fingerprint` or `audprev_`
    - Dependency/promotion: `dependency` + (`inventory` or `findings` or `visibility`); `promotion` or `manifest` + blocker/fail-closed language
    - Package truth: `mounted companion`; `host-owned`; `rulestead` owns domain/validation/contracts
    - MAINTAINING: section title `Reusable Targeting Deepening Proof`; cites `54-HANDOFF` and `55-HANDOFF` paths or checklist names

    **Forbidden phrases loop** (same four doc targets as guarded_rollout):

    ```elixir
    forbidden_phrases = [
      "standalone rulestead_admin",
      "standalone control plane",
      "graph visualizer",
      "bulk automation",
      "one-click bulk",
      "authoritative affected-user",
      "real user population",
      "built-in observability",
      "Rulestead dashboard",
      "metrics ingestion",
      "metrics warehouse",
      "automatic progressive delivery platform",
      "segment library",
      "manage segments"
    ]
    ```

    Reuse existing `"dependency truth for promotion and manifest stays core-owned without rulestead_admin leakage"` test — extend only if new core APIs added; do not duplicate `RulesteadAdmin` refute logic.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/release_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "reusable targeting deepening support truth stays bounded" rulestead/test/rulestead/release_contract_test.exs` finds new test name.
    - `rg -n "mix verify\\.phase56|Reusable Targeting Deepening Proof|54-HANDOFF|55-HANDOFF" MAINTAINING.md rulestead/test/rulestead/release_contract_test.exs` finds maintainer section and guard asserts.
    - `rg -n "segment library|standalone rulestead_admin|built-in observability" rulestead/test/rulestead/release_contract_test.exs` finds forbidden phrase list in new block.
    - `cd rulestead && mix test test/rulestead/release_contract_test.exs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd rulestead && mix test test/rulestead/release_contract_test.exs` green
- [ ] `cd rulestead && mix verify.phase56` green (includes release_contract in core union)
</verification>

<success_criteria>
VER-02 drift guards and README/MAINTAINING/package README proof sections stay coupled: doc edits and release_contract asserts land together and pass.
</success_criteria>

<output>
After completion, create `.planning/phases/56-proof-docs-and-support-truth/56-02-SUMMARY.md`.
</output>
