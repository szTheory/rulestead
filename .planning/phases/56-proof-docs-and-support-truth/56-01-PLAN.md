---
phase: 56-proof-docs-and-support-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/mix/tasks/verify.phase56.ex
  - rulestead/mix.exs
requirements:
  - VER-01
autonomous: true
must_haves:
  truths:
    - "`mix verify.phase56` is the single v1.6.0 merge gate: deduplicated union of Phase 54 core contracts, Phase 55 core/admin paths, Phase 53 gaps (`impact_preview_test.exs`, `audience_mutation_audit_test.exs`), and optional admin completion tests."
    - "Phase 54 and Phase 55 verify tasks remain unchanged; Phase 56 composes upward without replacing prior gates."
    - "Maintainer entrypoint `cd rulestead && mix verify.phase56` runs core tests via `Mix.Task.run(\"test\", paths)` then admin tests via the Phase 55 `mix cmd` pattern."
  artifacts:
    - path: "rulestead/lib/mix/tasks/verify.phase56.ex"
      provides: "v1.6.0 merge gate with flat deduplicated test union"
    - path: "rulestead/mix.exs"
      provides: "preferred_envs for verify.phase56 in test env"
  key_links:
    - from: "rulestead/lib/mix/tasks/verify.phase56.ex"
      to: "verify.phase54.ex and verify.phase55.ex path inventories"
      via: "commented union manifest — do not invoke prior tasks"
      pattern: "@phase56_core_tests"
    - from: "rulestead/lib/mix/tasks/verify.phase56.ex"
      to: "rulestead_admin test suite"
      via: "Mix.Task.run(\"cmd\", [\"sh\", \"-c\", \"cd #{admin_dir} && MIX_ENV=test mix test ...\"])"
      pattern: "rulestead_admin/live"
---

<threat_model>
## ASVS L1 Threat Register

| Threat ID | ASVS L1 Category | Component | Disposition | Mitigation Plan |
|-----------|-------------------|-----------|-------------|-----------------|
| T-56-01 | V1 Architecture — incomplete verification | `verify.phase56` gate | mitigate | Deduplicated union must include all Phase 54 paths, Phase 55-unique core paths, Phase 53 gaps, and admin paths from Phase 55 plus optional completion tests. |
| T-56-02 | V14 Configuration — wrong Mix env | `rulestead/mix.exs` | mitigate | Add `{:"verify.phase56", :test}` to `cli/0` `preferred_envs` alongside existing `verify.phase54`. |
| T-56-03 | V1 Architecture — gate drift | Prior verify tasks | mitigate | Do not edit `verify.phase54.ex` or `verify.phase55.ex` (D-02); document union source in Phase 56 task comments. |
| T-56-04 | V14 Configuration — admin package isolation | Admin test invocation | mitigate | Reuse Phase 55 `Path.expand("../../../../rulestead_admin", __DIR__)` + `mix cmd` shell pattern; never import `RulesteadAdmin` into core task module. |
</threat_model>

<objective>
Ship `mix verify.phase56` as the v1.6.0 merge gate (VER-01).

Purpose: Phases 53–55 delivered reusable targeting deepening; Phase 56 must prove that surface in one maintainer command before docs and drift guards land.
Output: `Mix.Tasks.Verify.Phase56` with flat deduplicated core union + admin shell tests, and `mix.exs` preferred-env wiring.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-proof-docs-and-support-truth/56-CONTEXT.md
@.planning/phases/56-proof-docs-and-support-truth/56-RESEARCH.md
@.planning/phases/56-proof-docs-and-support-truth/56-VALIDATION.md
@.planning/phases/54-dependency-truth-and-promotion-safety/54-HANDOFF-CHECKLIST.md
@.planning/phases/55-mounted-operator-workflows/55-HANDOFF-CHECKLIST.md
@rulestead/lib/mix/tasks/verify.phase54.ex
@rulestead/lib/mix/tasks/verify.phase55.ex
</context>

<source_audit>
GOAL: repo-local proof for dependency inventory, preview determinism, stale-token rejection, fail-closed blockers, audit evidence, explain trace carry-through, promotion/manifest blockers — covered by union gate.
REQ VER-01: covered by this plan (proof gate only; docs in 56-02/03).
CONTEXT D-01, D-02, D-06: covered — hybrid flat union; prior gates unchanged; primary entrypoint documented in task comments.
Deferred: README/MAINTAINING proof citations (56-02); CI scope (56-04).
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add Mix.Tasks.Verify.Phase56 with deduplicated core union</name>
  <read_first>
    - rulestead/lib/mix/tasks/verify.phase54.ex
    - rulestead/lib/mix/tasks/verify.phase55.ex
    - rulestead/test/rulestead/targeting/impact_preview_test.exs
    - rulestead/test/rulestead/audience_mutation_audit_test.exs
    - .planning/phases/56-proof-docs-and-support-truth/56-CONTEXT.md
  </read_first>
  <action>
    Create `rulestead/lib/mix/tasks/verify.phase56.ex` with `# credo:disable-for-this-file`, `@shortdoc "Runs Phase 56 reusable targeting deepening verification suite"`, and module comment: union of `verify.phase54` + `verify.phase55` + Phase 53 gaps — do **not** call those tasks (avoids duplicate runs).

    Define `@phase56_core_tests` as ordered deduplicated set-union:

    **From Phase 54 (13 paths — copy exactly from `verify.phase54.ex`):**
    - `test/rulestead/targeting/dependency_sort_property_test.exs`
    - `test/rulestead/store/audience_dependency_inventory_contract_test.exs`
    - `test/rulestead/store/compare_contract_test.exs`
    - `test/rulestead/store/publish_ruleset_dependency_contract_test.exs`
    - `test/rulestead/store/promotion_apply_contract_test.exs`
    - `test/rulestead/store/manifest_import_contract_test.exs`
    - `test/rulestead/store/audience_impact_contract_test.exs`
    - `test/rulestead/store/ecto_audience_impact_contract_test.exs`
    - `test/rulestead/manifest/export_test.exs`
    - `test/rulestead/manifest/import_test.exs`
    - `test/rulestead/manifest/validate_test.exs`
    - `test/rulestead/runtime/audience_snapshot_test.exs`
    - `test/rulestead/release_contract_test.exs`

    **Phase 55-unique core (append; skip overlaps already in Phase 54 list):**
    - `test/rulestead/admin/dependency_visibility_test.exs`
    - `test/rulestead/targeting/dependency_inventory_test.exs`

    **Phase 53 gaps (D-01 minimum):**
    - `test/rulestead/targeting/impact_preview_test.exs`
    - `test/rulestead/audience_mutation_audit_test.exs`

    In `run/1`: `Mix.Task.run("test", @phase56_core_tests)` only for core (17 paths total).
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/targeting/impact_preview_test.exs test/rulestead/audience_mutation_audit_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "defmodule Mix.Tasks.Verify.Phase56|@phase56_core_tests|impact_preview_test|audience_mutation_audit_test" rulestead/lib/mix/tasks/verify.phase56.ex` finds module and gap paths.
    - `rg -n "verify\\.phase54|verify\\.phase55" rulestead/lib/mix/tasks/verify.phase56.ex` finds union comment and does **not** find `Mix.Task.run("verify.phase`.
    - `rg -c "test/rulestead/" rulestead/lib/mix/tasks/verify.phase56.ex | head -1` — file lists exactly 17 core test path strings (13 phase54 + 2 phase55-unique + 2 gaps).
    - `cd rulestead && mix test test/rulestead/targeting/impact_preview_test.exs test/rulestead/audience_mutation_audit_test.exs` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire admin completion tests and mix.exs preferred_envs</name>
  <read_first>
    - rulestead/lib/mix/tasks/verify.phase55.ex
    - rulestead/mix.exs
    - rulestead_admin/test/rulestead_admin/live/environment_compare_live/show_test.exs
    - rulestead_admin/test/rulestead_admin/live/flag_live/rules_test.exs
    - rulestead_admin/test/rulestead_admin/live/flag_live/simulate_test.exs
  </read_first>
  <action>
    Extend `Mix.Tasks.Verify.Phase56` `run/1` after core tests:

    1. Define `@admin_test_paths` as Phase 55 admin paths **plus** optional completion paths:
       - `test/rulestead_admin/live/audience_live` (directory — same as phase55)
       - `test/rulestead_admin/live/flag_live/explain_test.exs`
       - `test/rulestead_admin/router_test.exs`
       - `test/rulestead_admin/live/environment_compare_live/index_test.exs`
       - `test/rulestead_admin/live/environment_compare_live/show_test.exs`
       - `test/rulestead_admin/live/flag_live/rules_test.exs`
       - `test/rulestead_admin/live/flag_live/simulate_test.exs`

    2. Invoke admin tests using Phase 55 pattern:
       `admin_dir = Path.expand("../../../../rulestead_admin", __DIR__)`
       `Mix.Task.run("cmd", ["sh", "-c", "cd #{admin_dir} && MIX_ENV=test mix test #{Enum.join(@admin_test_paths, " ")}"])`

    3. Update `rulestead/mix.exs` `cli/0`:
       `[preferred_envs: [{:"verify.phase54", :test}, {:"verify.phase56", :test}]]`

    Do **not** modify `verify.phase54.ex` or `verify.phase55.ex`.
  </action>
  <verify>
    <automated>cd rulestead && mix verify.phase56</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "verify\\.phase56" rulestead/mix.exs` finds preferred_env entry.
    - `rg -n "show_test\\.exs|rules_test\\.exs|simulate_test\\.exs|@admin_test_paths" rulestead/lib/mix/tasks/verify.phase56.ex` finds admin completion paths.
    - `rg -n "Mix\\.Task\\.run\\(\"cmd\"" rulestead/lib/mix/tasks/verify.phase56.ex` finds admin shell invocation.
    - `cd rulestead && mix verify.phase56` exits 0 with core + admin tests green.
    - `git diff --name-only rulestead/lib/mix/tasks/verify.phase54.ex rulestead/lib/mix/tasks/verify.phase55.ex` produces no output (files unchanged).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd rulestead && mix verify.phase56` green
- [ ] `cd rulestead && mix verify.phase54` still green (unchanged gate)
- [ ] `cd rulestead && mix verify.phase55` still green (unchanged gate)
</verification>

<success_criteria>
VER-01 proof gate exists: one command proves Phase 54 + 55 + Phase 53 gap coverage without modifying prior phase verify tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/56-proof-docs-and-support-truth/56-01-SUMMARY.md`.
</output>
