---
phase: 56-proof-docs-and-support-truth
plan: 03
type: execute
wave: 2
depends_on:
  - 56-01
files_modified:
  - guides/flows/rulesets.md
  - guides/flows/explainability.md
  - guides/flows/admin-ui.md
  - guides/flows/multi-env.md
requirements:
  - VER-02
autonomous: true
must_haves:
  truths:
    - "Four flow guides describe reusable Audience workflows, preview-basis limits, tenant/environment scope, dependency visibility, preview → confirm → audit, and read-only compare — in place, no new guide files."
    - "Operator copy uses **Audience** externally; `segment` appears only as internal implementation vocabulary if unavoidable."
    - "Guides state snapshot-local evaluation, host-owned identity/observability, and audit/explain escalation paths without observability over-claims."
  artifacts:
    - path: "guides/flows/rulesets.md"
      provides: "Reusable Audience impact preview and fail-closed publish/archive guidance"
    - path: "guides/flows/explainability.md"
      provides: "Audience trace steps and support-safe explain permalinks"
    - path: "guides/flows/admin-ui.md"
      provides: "Mounted `/admin/audiences` routes and preview→confirm→audit workflow"
    - path: "guides/flows/multi-env.md"
      provides: "Compare/promotion dependency findings with explicit env/tenant scope"
  key_links:
    - from: "guides/flows/admin-ui.md"
      to: "55-HANDOFF-CHECKLIST.md mounted presentation boundary"
      via: "mounted companion renders core truth"
      pattern: "/admin/audiences"
    - from: "guides/flows/explainability.md"
      to: "rulestead/runtime audience snapshot evaluation"
      via: "snapshot-local; no live DB/admin/identity lookup"
      pattern: "snapshot-local"
---

<threat_model>
## ASVS L1 Threat Register

| Threat ID | ASVS L1 Category | Component | Disposition | Mitigation Plan |
|-----------|-------------------|-----------|-------------|-----------------|
| T-56-07 | V7 Error Handling — false precision in operator docs | `guides/flows/rulesets.md` | mitigate | Document preview basis, uncertainty, explicit samples; forbid exact population impact language. |
| T-56-08 | V1 Architecture — scope collapse | `guides/flows/multi-env.md` | mitigate | Same-name audience ≠ equivalent across env/tenant; host owns tenant catalog. |
| T-56-09 | V7 Error Handling — observability over-claim | `guides/flows/explainability.md` | mitigate | Escalation via explain + dependency inventory + audit; not Rulestead dashboards/metrics. |
| T-56-10 | V1 Architecture — mounted vs core confusion | `guides/flows/admin-ui.md` | mitigate | Core validates; mounted companion presents; compare remains read-only without Apply/Publish. |
</threat_model>

<objective>
Align operator/support flow guides with v1.6 reusable targeting deepening truth (VER-02).

Purpose: Support and operators must read the same bounded scope in flow guides that release-contract tests guard in READMEs — audience traces, preview limits, dependency findings, promotion fail-closed behavior.
Output: In-place edits to four existing `guides/flows/*.md` files only (D-04, D-07).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-proof-docs-and-support-truth/56-CONTEXT.md
@.planning/phases/56-proof-docs-and-support-truth/56-RESEARCH.md
@.planning/phases/54-dependency-truth-and-promotion-safety/54-HANDOFF-CHECKLIST.md
@.planning/phases/55-mounted-operator-workflows/55-HANDOFF-CHECKLIST.md
@.planning/research/PITFALLS.md
@prompts/rulestead-admin-ux-and-operator-ia.md
</context>

<source_audit>
GOAL: docs describe same supported reusable-targeting scope as proof gate — covered for operator flow guides.
REQ VER-02: covered (guide alignment portion); drift guards in 56-02.
CONTEXT D-04, D-05, D-07: in-place edits only; Audience vocabulary; no Phase 8 docs.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Update rulesets.md and explainability.md for Audience preview and trace truth</name>
  <read_first>
    - guides/flows/rulesets.md
    - guides/flows/explainability.md
    - .planning/phases/56-proof-docs-and-support-truth/56-RESEARCH.md
    - .planning/research/PITFALLS.md
  </read_first>
  <action>
    **`guides/flows/rulesets.md`** — add section(s) covering:
    - Reusable **Audience** naming (operator term; note internal `segment_match` only in code citations if needed)
    - Before publish/archive of shared audiences: **impact preview** with **preview basis** (authored state ± explicit samples)
    - **Fail closed** when references are missing, archived, incompatible, stale, or tenant-mismatched
    - Explicit **environment scope** and **tenant scope** when same-name audiences differ
    - Pointer to mounted `/admin/audiences` workflow: **preview → confirm → audit** (path-level; no LiveView internals)
    - **Uncertainty** language: previews do not claim authoritative affected-user or population counts

    **`guides/flows/explainability.md`** — add section(s) covering:
    - Audience trace steps in explain/simulate output: `matched`, `missed`, `missing from snapshot`, `archived`
    - **Snapshot-local** evaluation — no live DB, mounted-admin, host identity, or observability lookup for audience resolution
    - Support-safe explain permalinks: flag, environment, tenant, targeting key — not raw traits
    - Escalation path: explain + dependency inventory + audit (not observability dashboards or Rulestead-owned metrics)
  </action>
  <verify>
    <automated>rg -n "Audience|preview basis|fail closed|snapshot-local|preview → confirm → audit|uncertainty" guides/flows/rulesets.md guides/flows/explainability.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "## .*Audience|preview basis|fail closed|environment scope|tenant scope" guides/flows/rulesets.md` finds reusable-audience guidance.
    - `rg -n "matched|missed|missing from snapshot|archived|snapshot-local" guides/flows/explainability.md` finds audience trace vocabulary.
    - `rg -n "segment library|manage segments|authoritative affected-user|built-in observability" guides/flows/rulesets.md guides/flows/explainability.md` finds no forbidden operator phrases.
    - `rg -n "Audience" guides/flows/rulesets.md guides/flows/explainability.md` finds operator-facing term (not "segment" as product name).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update admin-ui.md and multi-env.md for mounted workflows and compare/promotion scope</name>
  <read_first>
    - guides/flows/admin-ui.md
    - guides/flows/multi-env.md
    - .planning/phases/55-mounted-operator-workflows/55-HANDOFF-CHECKLIST.md
    - .planning/phases/54-dependency-truth-and-promotion-safety/54-HANDOFF-CHECKLIST.md
  </read_first>
  <action>
    **`guides/flows/admin-ui.md`** — extend stable mounted seam list with:
    - `/admin/audiences`, `/admin/audiences/:key`, edit/archive preview/confirm routes (path-level only)
    - Audience **used-by** tables with policy-aware redaction copy
    - **Preview → confirm → audit** for audience mutations (parallel to existing cleanup flow documentation)
    - Compare **dependency findings** as **read-only** — no Apply/Publish on compare surfaces
    - Reiterate: mounted companion renders core truth; core validates (cite 54/55 handoff boundary concept)

    **`guides/flows/multi-env.md`** — add section(s) covering:
    - Compare and promotion surfaces show **audience dependency findings** with explicit env/tenant scope on links (`?env=`, `tenant_key`, or equivalent)
    - Promotion/manifest **fail closed** on missing/incompatible reusable targeting assets
    - **Tenant scope** must not collapse across environments (same-name ≠ equivalent)
    - Host owns tenant catalog and authorization; Rulestead carries scope on payloads only
    - Compare/promotion read-only posture for dependency findings (no inline apply from compare)

    Do **not** create new guide files or Phase 8-only docs.
  </action>
  <verify>
    <automated>rg -n "/admin/audiences|read-only|dependency findings|tenant scope|fail closed" guides/flows/admin-ui.md guides/flows/multi-env.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "/admin/audiences|preview → confirm → audit|used-by|read-only" guides/flows/admin-ui.md` finds mounted audience workflow coverage.
    - `rg -n "dependency findings|fail closed|tenant scope|same-name" guides/flows/multi-env.md` finds compare/promotion scope guidance.
    - `rg -n "Apply|Publish" guides/flows/admin-ui.md guides/flows/multi-env.md` — compare sections must state read-only / no apply-from-compare (grep confirms negation prose like `no Apply` or `read-only`).
    - `rg -n "segment library|standalone admin|graph visualizer" guides/flows/admin-ui.md guides/flows/multi-env.md` finds no forbidden phrases.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Four flow guides updated in place
- [ ] `cd rulestead && mix verify.phase56` green (no code changes expected; confirms gate still passes)
</verification>

<success_criteria>
Operator flow guides match v1.6 support truth: Audience terminology, preview-basis limits, scoped dependency findings, mounted preview→confirm→audit, and host-owned boundaries — without new guide artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/56-proof-docs-and-support-truth/56-03-SUMMARY.md`.
</output>
