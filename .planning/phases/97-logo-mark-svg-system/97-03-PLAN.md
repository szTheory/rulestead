---
phase: 97-logo-mark-svg-system
plan: 03
type: execute
wave: 3
depends_on: ["97-02"]
files_modified:
  - rulestead_admin/priv/static/images/rs-mark.svg
  - rulestead_admin/priv/static/images/rs-mark-dark.svg
  - examples/demo/backend/priv/static/images/logo.svg
  - examples/demo/backend/priv/static/images/logo.svg.gz
  - examples/demo/backend/priv/static/cache_manifest.json
autonomous: false
requirements: [LOGO-04, LOGO-05]
user_setup: []

must_haves:
  truths:
    - "rulestead_admin/priv/static/images/ holds rs-mark.svg and rs-mark-dark.svg as committed package assets"
    - "The demo logo.svg is the new Rulestead mark — the phoenix-flame fill is gone"
    - "The old fingerprinted logo file and its .gz are deleted; new fingerprint + .gz sidecars are regenerated by mix phx.digest"
    - "cache_manifest.json points to the new fingerprinted logo"
    - "The new mark renders correctly in the demo at width=36 (human visual confirmation)"
  artifacts:
    - path: "rulestead_admin/priv/static/images/rs-mark.svg"
      provides: "Admin-embedded light mark"
      contains: "<title"
    - path: "rulestead_admin/priv/static/images/rs-mark-dark.svg"
      provides: "Admin-embedded dark mark"
      contains: "<title"
    - path: "examples/demo/backend/priv/static/images/logo.svg"
      provides: "New demo logo (replaces phoenix-flame)"
      contains: "<title"
  key_links:
    - from: "examples/demo/backend/priv/static/cache_manifest.json"
      to: "examples/demo/backend/priv/static/images/logo-<newhash>.svg"
      via: "mix phx.digest manifest entry"
      pattern: "images/logo-"
    - from: "examples/demo/backend/lib/rulestead_demo_web/components/layouts.ex line 41"
      to: "examples/demo/backend/priv/static/images/logo.svg"
      via: "<img src={~p\"/images/logo.svg\"} width=\"36\">"
      pattern: "images/logo.svg"
---

<objective>
Embed the new marks where consumers expect them: copy rs-mark.svg + rs-mark-dark.svg into the admin package's `priv/static/images/`, replace the phoenix-flame demo logo with the icon-only rs-mark.svg, delete the old fingerprinted sidecars, re-run `mix phx.digest` to regenerate the fingerprint + .gz + cache_manifest, and visually confirm the demo renders the new mark.

Purpose: LOGO-04 (admin copies exist) + LOGO-05 (phoenix-flame retired, sidecars regenerated). Sibling-package layout preserved — admin and demo each get their own static copy from the single brandbook source.
Output: 2 admin marks + replaced demo logo + regenerated digest artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-logo-mark-svg-system/97-RESEARCH.md
@.planning/phases/97-logo-mark-svg-system/97-VALIDATION.md
@examples/demo/backend/lib/rulestead_demo_web/components/layouts.ex

<interfaces>
<!-- Exact procedures from RESEARCH.md Pattern 8 (Phoenix digest) + Pattern 9 (admin embed). -->
Old fingerprinted demo file hash: 06a11be1f2cdde2c851763d00bdd2e80
Phoenix-flame fill to retire: #FD4F00 (grep target — must be 0 after replace)
Demo embed: layouts.ex line 41 — <img src={~p"/images/logo.svg"} width="36" /> (icon-only mark, NOT wordmark — too wide at 36px per RESEARCH.md Open Question 1)

Admin embed (Pattern 9): admin does NOT run phx.digest on its own assets; files committed as package
  assets (priv/ is in mix.exs files: list). Do NOT modify shell.ex / layouts — Phase 98 wires them in.

Demo digest (Pattern 8) — exact sequence, run from repo root:
  cp brandbook/assets/logo/rs-mark.svg examples/demo/backend/priv/static/images/logo.svg
  rm examples/demo/backend/priv/static/images/logo-06a11be1f2cdde2c851763d00bdd2e80.svg
  rm examples/demo/backend/priv/static/images/logo-06a11be1f2cdde2c851763d00bdd2e80.svg.gz
  rm examples/demo/backend/priv/static/images/logo.svg.gz
  (cd examples/demo/backend && mix phx.digest)
  Do NOT use `mix phx.digest.clean --all` (nukes CSS/JS too).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy marks to admin package + replace demo logo + re-digest</name>
  <files>rulestead_admin/priv/static/images/rs-mark.svg, rulestead_admin/priv/static/images/rs-mark-dark.svg, examples/demo/backend/priv/static/images/logo.svg, examples/demo/backend/priv/static/images/logo.svg.gz, examples/demo/backend/priv/static/cache_manifest.json</files>
  <read_first>
    - .planning/phases/97-logo-mark-svg-system/97-RESEARCH.md (Pattern 8 digest workflow + Pattern 9 admin embed + Pitfall 3 old fingerprint)
    - examples/demo/backend/lib/rulestead_demo_web/components/layouts.ex (line 41 — confirms the embed and width=36)
    - brandbook/assets/logo/rs-mark.svg and rs-mark-dark.svg (the source files to copy)
  </read_first>
  <action>
    LOGO-04 admin copies: `mkdir -p rulestead_admin/priv/static/images/` then copy `brandbook/assets/logo/rs-mark.svg` and `brandbook/assets/logo/rs-mark-dark.svg` into it (preserving exact bytes). Do NOT modify any admin .ex file — Phase 98 wires the mark into the shell.

    LOGO-05 demo replacement (RESEARCH.md Pattern 8, from repo root):
    1. `cp brandbook/assets/logo/rs-mark.svg examples/demo/backend/priv/static/images/logo.svg` (icon-only mark — the wordmark is too wide at width=36).
    2. Delete the old fingerprinted file and its gz, and the plain gz: `rm examples/demo/backend/priv/static/images/logo-06a11be1f2cdde2c851763d00bdd2e80.svg examples/demo/backend/priv/static/images/logo-06a11be1f2cdde2c851763d00bdd2e80.svg.gz examples/demo/backend/priv/static/images/logo.svg.gz`
    3. Re-digest: `(cd examples/demo/backend && mix phx.digest)`. This regenerates logo.svg.gz, a new logo-<newhash>.svg + its .gz, and updates cache_manifest.json.
    Do NOT run `mix phx.digest.clean --all`. After digest, confirm exactly one fingerprinted logo svg exists (the new hash), the old hash is gone, and two logo*.gz sidecars exist.
  </action>
  <acceptance_criteria>
    - `ls rulestead_admin/priv/static/images/rs-mark.svg rulestead_admin/priv/static/images/rs-mark-dark.svg | wc -l` returns 2
    - `grep -c 'FD4F00' examples/demo/backend/priv/static/images/logo.svg` returns 0 (phoenix-flame fill gone)
    - `ls examples/demo/backend/priv/static/images/logo-06a11be1f2cdde2c851763d00bdd2e80.svg 2>/dev/null | wc -l` returns 0 (old hash deleted)
    - `ls examples/demo/backend/priv/static/images/logo-*.svg | wc -l` returns 1 (one new fingerprint)
    - `ls examples/demo/backend/priv/static/images/logo*.gz | wc -l` returns 2 (plain + fingerprinted sidecars regenerated)
    - `grep -q 'images/logo-' examples/demo/backend/priv/static/cache_manifest.json` succeeds (manifest references a fingerprinted logo)
    - `grep -q '06a11be1f2cdde2c851763d00bdd2e80' examples/demo/backend/priv/static/cache_manifest.json && echo STALE || echo CLEAN` prints CLEAN
  </acceptance_criteria>
  <verify>
    <automated>ls rulestead_admin/priv/static/images/rs-mark.svg rulestead_admin/priv/static/images/rs-mark-dark.svg | wc -l | grep -q '^[[:space:]]*2$' && [ "$(grep -c 'FD4F00' examples/demo/backend/priv/static/images/logo.svg)" = "0" ] && [ "$(ls examples/demo/backend/priv/static/images/logo-*.svg | wc -l | tr -d ' ')" = "1" ] && [ "$(ls examples/demo/backend/priv/static/images/logo*.gz | wc -l | tr -d ' ')" = "2" ] && echo OK</automated>
  </verify>
  <done>Admin holds both marks; demo logo is the new mark; old fingerprint+gz gone; new fingerprint+gz regenerated; manifest clean of the old hash.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visually confirm the demo renders the new mark</name>
  <what-built>The phoenix-flame demo logo was replaced with the new Rulestead mark (rs-mark.svg) and `mix phx.digest` regenerated the fingerprint + gz sidecars + cache_manifest.</what-built>
  <read_first>
    - examples/demo/backend/lib/rulestead_demo_web/components/layouts.ex (line 41 — where the logo renders at width=36)
    - .planning/phases/97-logo-mark-svg-system/97-VALIDATION.md (Manual-Only Verifications — demo render row)
  </read_first>
  <how-to-verify>
    1. Boot the demo backend: `(cd examples/demo/backend && mix phx.server)`.
    2. Open the app in a browser; the header logo renders via `<img src="/images/logo.svg" width="36">` (layouts.ex line 41).
    3. Confirm: the new Rulestead mark displays in place of the orange phoenix-flame, reads clearly at 36px, and is not clipped/distorted.
    4. Separately, open `brandbook/assets/logo/rs-favicon.svg` rendered at a 16x16 canvas (e.g. `<img src=... width="16">` or a screenshot) and confirm the mark is legible at 16px (LOGO-03 manual gate).
  </how-to-verify>
  <acceptance_criteria>
    - Manual gate (RESEARCH.md A7 / VALIDATION Manual-Only): maintainer confirms (a) the new mark renders at 36px in the demo header replacing the phoenix-flame, and (b) rs-favicon.svg is legible at 16px. The automated preconditions (FD4F00 absent, new fingerprint present) are already asserted in Task 1; this gate is the human visual confirmation that has no automated equivalent.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the new mark renders correctly at 36px and the favicon reads at 16px, or describe what looks wrong.</resume-signal>
  <done>Maintainer confirms the demo header shows the new mark and the favicon is legible at 16px.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brandbook source → consumer packages | SVGs copied from canonical source into admin + demo priv/static |
| demo build → served static assets | mix phx.digest fingerprints + gzips assets served by Plug.Static |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-07 | Tampering | copied SVGs differ from canonical brandbook source | mitigate | Copy preserves exact bytes from brandbook/assets/logo; the canonical files already passed the script/raster/title gates in plan 97-02. No re-authoring here. |
| T-97-08 | Information Disclosure | stale fingerprinted file left in repo / manifest | mitigate | Old hash file + gz explicitly removed; verify cache_manifest.json no longer references 06a11be1f2cdde2c851763d00bdd2e80 (RESEARCH.md Pitfall 3). |
| T-97-09 | Denial of Service | mix phx.digest.clean --all wiping CSS/JS assets | mitigate | Use targeted `rm` of only logo sidecars + plain `mix phx.digest`; never `--all` (RESEARCH.md Anti-Pattern). |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installed. `mix phx.digest` uses the already-present Phoenix 1.8.7 in the demo. |
</threat_model>

<verification>
- All Task 1 acceptance_criteria assertions pass
- Task 2 human-verify gate approved (demo render + favicon legibility)
</verification>

<success_criteria>
Admin package holds rs-mark.svg + rs-mark-dark.svg; demo logo.svg is the new mark with the phoenix-flame fill gone; old fingerprint + gz deleted; new fingerprint + 2 gz sidecars regenerated; cache_manifest clean of the old hash; maintainer confirmed the demo renders the new mark at 36px and the favicon reads at 16px.
</success_criteria>

<output>
Create `.planning/phases/97-logo-mark-svg-system/97-03-SUMMARY.md` when done.
</output>
