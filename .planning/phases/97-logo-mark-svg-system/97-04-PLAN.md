---
phase: 97-logo-mark-svg-system
plan: 04
type: execute
wave: 4
depends_on: ["97-03"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LOGO-01, LOGO-02, LOGO-03, LOGO-04, LOGO-05]
user_setup: []

must_haves:
  truths:
    - "The full Nyquist assertion set from 97-VALIDATION.md passes (grep counts, file existence, lint exit)"
    - "scripts/ci/lint.sh exits 0 with SVG SIZE BUDGET OK"
    - "The demo backend boots and serves the new logo"
    - "STATE.md and ROADMAP.md reflect Phase 97 complete; REQUIREMENTS.md marks LOGO-01..05 done"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated project state — Phase 97 complete"
      contains: "97"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 97 row marked complete with plan list"
      contains: "Phase 97"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "LOGO-01..LOGO-05"
      via: "status flip to done"
      pattern: "LOGO-0[1-5]"
---

<objective>
Run the complete phase verification sweep — every Nyquist assertion in 97-VALIDATION.md plus a demo boot smoke — to prove all five LOGO requirements are satisfied, then update STATE.md, ROADMAP.md, and REQUIREMENTS.md to reflect Phase 97 complete.

Purpose: Goal-backward closure. The phase goal (full logo system committed, phoenix-flame retired) is only TRUE when every assertion passes together in one sweep.
Output: A verification record in the summary + updated planning docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/97-logo-mark-svg-system/97-VALIDATION.md
@.planning/phases/97-logo-mark-svg-system/97-RESEARCH.md

<interfaces>
<!-- Full assertion set from 97-VALIDATION.md Per-Task Verification Map. Run all in one sweep. -->
LOGO-01: ls brandbook/assets/logo/concepts/rs-mark-concept-{a,b,c}.svg  (3)
LOGO-02: ls brandbook/assets/logo/rs-{wordmark,wordmark-dark,mark,mark-dark,mark-mono,favicon,social-card}.svg | wc -l  (7)
LOGO-02: grep -c '<text' brandbook/assets/logo/*.svg  (all 0)
LOGO-03: grep -c 'base64' brandbook/assets/logo/*.svg  (all 0)  +  grep -l 'href=.http' brandbook/assets/logo/*.svg  (none)
LOGO-03: grep 'viewBox="0 0 1200 630"' brandbook/assets/logo/rs-social-card.svg
LOGO-04: per-file <title> present; grep -c 'currentColor' rs-mark-mono.svg  (>0); bash scripts/ci/lint.sh → SVG SIZE BUDGET OK
LOGO-04: ls rulestead_admin/priv/static/images/rs-mark.svg rulestead_admin/priv/static/images/rs-mark-dark.svg  (2)
LOGO-05: grep -c 'FD4F00' examples/demo/backend/priv/static/images/logo.svg  (0)
LOGO-05: ls examples/demo/backend/priv/static/images/logo-06a11be1f2cdde2c851763d00bdd2e80.svg 2>/dev/null | wc -l  (0)
LOGO-05: ls examples/demo/backend/priv/static/images/logo-*.svg | wc -l  (1)  +  logo*.gz | wc -l  (2)
Demo boot smoke: (cd examples/demo/backend && mix compile) succeeds; mix phx.server boots without logo asset error.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full Nyquist verification sweep + demo boot smoke</name>
  <files>.planning/phases/97-logo-mark-svg-system/97-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/97-logo-mark-svg-system/97-VALIDATION.md (Per-Task Verification Map — every row)
    - .planning/phases/97-logo-mark-svg-system/97-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map)
  </read_first>
  <action>
    Run every assertion from the interfaces block in a single sweep and record pass/fail for each LOGO-01..05 in the summary. Specifically:
    - Concept files (3), lockup set (7), no <text>, no base64, no external href=http, no <script>, social-card viewBox, mono currentColor, per-file <title>, admin marks (2), demo FD4F00 absent, old hash gone (0), one new fingerprint (1), two gz sidecars (2).
    - `bash scripts/ci/lint.sh` exits 0 with `SVG SIZE BUDGET OK`.
    - Demo boot smoke: `(cd examples/demo/backend && mix compile)` succeeds; optionally start `mix phx.server` long enough to confirm it boots and the `/images/logo.svg` route serves the new file (no missing-asset error), then stop it.
    If any assertion fails, STOP and report it (do not mark the phase complete) — a failure means a gap closure plan is needed.
  </action>
  <acceptance_criteria>
    - `bash scripts/ci/lint.sh` prints `SVG SIZE BUDGET OK` and exits 0
    - `ls brandbook/assets/logo/rs-{wordmark,wordmark-dark,mark,mark-dark,mark-mono,favicon,social-card}.svg | wc -l` returns 7
    - `[ "$(grep -ch '<text' brandbook/assets/logo/*.svg | paste -sd+ - | bc)" = "0" ]` true (zero live text across all logo SVGs)
    - `[ "$(grep -ch 'base64' brandbook/assets/logo/*.svg | paste -sd+ - | bc)" = "0" ]` true
    - `grep -q 'currentColor' brandbook/assets/logo/rs-mark-mono.svg` succeeds
    - `grep -q 'viewBox="0 0 1200 630"' brandbook/assets/logo/rs-social-card.svg` succeeds
    - `ls rulestead_admin/priv/static/images/rs-mark.svg rulestead_admin/priv/static/images/rs-mark-dark.svg | wc -l` returns 2
    - `[ "$(grep -c 'FD4F00' examples/demo/backend/priv/static/images/logo.svg)" = "0" ]` true
    - `[ "$(ls examples/demo/backend/priv/static/images/logo-*.svg | wc -l | tr -d ' ')" = "1" ]` true
    - `(cd examples/demo/backend && mix compile)` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/lint.sh | grep -q 'SVG SIZE BUDGET OK' && ls brandbook/assets/logo/rs-{wordmark,wordmark-dark,mark,mark-dark,mark-mono,favicon,social-card}.svg | wc -l | grep -q '^[[:space:]]*7$' && [ "$(grep -ch '<text' brandbook/assets/logo/*.svg | paste -sd+ - | bc)" = "0" ] && grep -q 'currentColor' brandbook/assets/logo/rs-mark-mono.svg && [ "$(grep -c 'FD4F00' examples/demo/backend/priv/static/images/logo.svg)" = "0" ] && echo SWEEP_OK</automated>
  </verify>
  <done>Every LOGO-01..05 assertion passes in one sweep; lint exits 0; demo compiles; pass/fail recorded per requirement in the summary.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md, ROADMAP.md, and REQUIREMENTS.md for Phase 97 complete</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md (Current Position, progress frontmatter, Phase Dependency Map)
    - .planning/ROADMAP.md (Phase 97 row + Progress Table)
    - .planning/REQUIREMENTS.md (LOGO-01..05 checklist + status table lines 34-38, 108-112)
  </read_first>
  <action>
    Only after Task 1 reports all assertions green:
    - STATE.md: advance Current Position to Phase 97 complete / ready for Phase 98; bump progress frontmatter (completed_phases, completed_plans, percent — Phase 97 adds 4 plans); update `stopped_at` and `last_updated`/`last_activity` (today 2026-06-05); add a Phase 97 line to Accumulated Context noting the lockup set committed, the selected concept letter (from 97-02 summary), and that the phoenix-flame demo logo was retired + re-digested. Update the progress bar.
    - ROADMAP.md: flip the Phase 97 checkbox to `[x]`, replace the four `(to be planned)` plan lines with the real one-line objectives, and update the Progress Table row (Plans Complete 4/4, Status Complete, Completed 2026-06-05).
    - REQUIREMENTS.md: check the LOGO-01..05 boxes (lines ~34-38) and flip their status rows (lines ~108-112) from Pending to Done.
    Do NOT touch any other phase's rows.
  </action>
  <acceptance_criteria>
    - `grep -c 'LOGO-0[1-5].*Done' .planning/REQUIREMENTS.md` returns 5 (or the status table shows all five LOGO rows Done)
    - `grep -q '97. Logo & Mark SVG System | 4/4' .planning/ROADMAP.md` succeeds (Progress Table updated) OR the Phase 97 Progress Table row shows `4/4` and `Complete`
    - `grep -q '\[x\] \*\*Phase 97' .planning/ROADMAP.md` succeeds (phase checkbox flipped)
    - `grep -q 'to be planned' .planning/ROADMAP.md | head` shows the four 97 placeholder lines are gone (no `97-0._-PLAN.md — (to be planned)` remains)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '\[x\] \*\*Phase 97' .planning/ROADMAP.md && ! grep -q '97-0._-PLAN.md — (to be planned)' .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <done>STATE.md, ROADMAP.md, and REQUIREMENTS.md all reflect Phase 97 complete with LOGO-01..05 marked done; only Phase 97 rows changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → source of truth | STATE/ROADMAP/REQUIREMENTS edits define downstream phase readiness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-10 | Repudiation | phase marked complete while an assertion failed | mitigate | Task 2 runs ONLY after Task 1 reports all-green; any red assertion stops the sweep and blocks the doc update (gap closure needed instead). |
| T-97-11 | Tampering | doc edit changes a non-97 phase row | mitigate | Edits scoped to Phase 97 rows + LOGO-01..05 status lines only; verify other phase rows unchanged in the diff. |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installed in this plan. |
</threat_model>

<verification>
- Task 1 sweep: every LOGO-01..05 assertion green; `SVG SIZE BUDGET OK`; demo compiles
- Task 2: ROADMAP Phase 97 checkbox `[x]`, Progress Table 4/4 Complete; REQUIREMENTS LOGO-01..05 Done; STATE advanced
</verification>

<success_criteria>
The full Nyquist assertion set passes in one sweep, lint exits 0, the demo compiles and serves the new logo, and STATE.md/ROADMAP.md/REQUIREMENTS.md all reflect Phase 97 complete with LOGO-01..05 marked done.
</success_criteria>

<output>
Create `.planning/phases/97-logo-mark-svg-system/97-04-SUMMARY.md` when done. Record the per-requirement pass/fail table in the summary.
</output>
