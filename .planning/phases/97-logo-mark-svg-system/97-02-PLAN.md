---
phase: 97-logo-mark-svg-system
plan: 02
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - brandbook/assets/logo/rs-wordmark.svg
  - brandbook/assets/logo/rs-wordmark-dark.svg
  - brandbook/assets/logo/rs-mark.svg
  - brandbook/assets/logo/rs-mark-dark.svg
  - brandbook/assets/logo/rs-mark-mono.svg
  - brandbook/assets/logo/rs-favicon.svg
  - brandbook/assets/logo/rs-social-card.svg
  - brandbook/assets/logo/svgo.config.mjs
  - scripts/gen_wordmark_paths.py
autonomous: false
requirements: [LOGO-01, LOGO-02, LOGO-03, LOGO-04]
user_setup: []

must_haves:
  truths:
    - "Maintainer has selected one concept (A, B, or C) before any lockup file is authored"
    - "The full lockup set (7 files) exists in brandbook/assets/logo/"
    - "Every wordmark glyph is outlined to a path — zero live <text> elements across all logo SVGs"
    - "No SVG contains embedded raster (base64) or external http references"
    - "rs-mark-mono.svg fills with currentColor; rs-social-card.svg uses viewBox 0 0 1200 630"
    - "Every logo SVG carries <title> + <desc> + role=img and is within the 20480-byte budget"
  artifacts:
    - path: "brandbook/assets/logo/rs-mark.svg"
      provides: "Icon-only mark, light"
      contains: "<title"
    - path: "brandbook/assets/logo/rs-mark-dark.svg"
      provides: "Icon-only mark, dark"
      contains: "<title"
    - path: "brandbook/assets/logo/rs-mark-mono.svg"
      provides: "Monochrome mark, currentColor"
      contains: "currentColor"
    - path: "brandbook/assets/logo/rs-wordmark.svg"
      provides: "Wordmark + icon lockup, light (outlined paths)"
      contains: "<title"
    - path: "brandbook/assets/logo/rs-wordmark-dark.svg"
      provides: "Wordmark + icon lockup, dark"
      contains: "<title"
    - path: "brandbook/assets/logo/rs-favicon.svg"
      provides: "16px-legible favicon"
      contains: "<title"
    - path: "brandbook/assets/logo/rs-social-card.svg"
      provides: "1200x630 social/OG card"
      contains: "viewBox=\"0 0 1200 630\""
  key_links:
    - from: "brandbook/assets/logo/rs-wordmark.svg"
      to: "scripts/gen_wordmark_paths.py output"
      via: "outlined Sora Bold glyph paths"
      pattern: "<path"
    - from: "brandbook/assets/logo/*.svg fills"
      to: "brandbook/tokens.json primitives"
      via: "hex values copied from primitives"
      pattern: "#3A6F8F|#5885a0|#183247|#e8edf3"
---

<objective>
Gate on the maintainer's A/B/C concept selection, then author the full Rulestead lockup set (7 SVGs) into `brandbook/assets/logo/`: wordmark (light/dark), mark (light/dark/mono), favicon, and social card. Outline the wordmark text to paths via the fontTools script, optimize with the a11y-preserving SVGO config, and verify zero live text, zero raster, and currentColor integrity.

Purpose: LOGO-02/03/04 — the selected concept graduates to a complete, accessible, source-controlled, text-outlined lockup. This is the heart of the phase.
Output: 7 lockup SVGs + svgo.config.mjs + the one-shot wordmark path generator script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-logo-mark-svg-system/97-RESEARCH.md
@.planning/phases/97-logo-mark-svg-system/97-VALIDATION.md
@.planning/phases/97-logo-mark-svg-system/97-CONCEPT-REVIEW.md
@brandbook/brand-book.md
@brandbook/tokens.json

<interfaces>
<!-- Fill hexes from tokens.json (RESEARCH.md Pattern 7 variant table). Use directly. -->
| File                | Background        | Mark fill | Text fill |
| rs-wordmark.svg     | transparent       | #3A6F8F   | #183247   |
| rs-wordmark-dark.svg| transparent       | #5885a0   | #e8edf3   |
| rs-mark.svg         | transparent       | #3A6F8F   | —         |
| rs-mark-dark.svg    | transparent       | #5885a0   | —         |
| rs-mark-mono.svg    | transparent       | currentColor | —      |
| rs-favicon.svg      | #3A6F8F (solid)   | #ffffff   | —         |
| rs-social-card.svg  | #183247 (Ink Blue)| #5885a0   | #e8edf3   |
FORBIDDEN as fill anywhere: #D2A94E (Signal Gold — decorative-only, PAL-04)

Wordmark text-to-path: scripts/gen_wordmark_paths.py (RESEARCH.md Pattern 6 + Code Examples).
  - fontTools 4.62.1 (user install). MUST prepend sys.path: ~/Library/Python/3.14/lib/python/site-packages
  - SORA_BOLD_TTF_URL = https://fonts.gstatic.com/s/sora/v17/xMQOuFFYT72X5wkB_18qmnndmSe1mX-K.ttf
  - Y-down flip required: scale(s, -s). TTF downloaded to temp, NOT committed.
  - Fallback if script fails (RESEARCH.md Pattern 6 alternatives): hand-trace geometric letterforms or online converter — committing only path data. Success criterion is grep '<text' = 0, not font fidelity.

SVGO config (RESEARCH.md Pattern 2): preset-default overrides removeTitle:false, removeDesc:false,
  removeViewBox:false, cleanupIds:false, convertColors:false (convertColors off protects currentColor).
Favicon constraints (RESEARCH.md Pattern 4): square viewBox 0 0 32 32 or 0 0 64 64, fills only (no strokes),
  2-3 shapes max, reads as the mark at 16px.
Social card (RESEARCH.md Pattern 5): viewBox="0 0 1200 630" width="1200" height="630", single bg rect,
  all text outlined to paths.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Maintainer selects concept A, B, or C</name>
  <decision>Which of the three mark concepts graduates to the full lockup set?</decision>
  <context>
    LOGO-01 / ROADMAP mid-phase human checkpoint. The three concepts from plan 97-01 are presented in `.planning/phases/97-logo-mark-svg-system/97-CONCEPT-REVIEW.md`. This is a brand/design decision and cannot be automated. No lockup file may be authored until a single letter is chosen. The selected mark's geometry becomes rs-mark.svg and is the basis for every other file in this plan.
  </context>
  <read_first>
    - .planning/phases/97-logo-mark-svg-system/97-CONCEPT-REVIEW.md
    - brandbook/assets/logo/concepts/rs-mark-concept-a.svg
    - brandbook/assets/logo/concepts/rs-mark-concept-b.svg
    - brandbook/assets/logo/concepts/rs-mark-concept-c.svg
  </read_first>
  <options>
    <option id="concept-a">
      <name>A — Structured path mark</name>
      <pros>Encodes rule evaluation / decision flow / controlled progression (brand book §14)</pros>
      <cons>Stepped-line motif may need care to stay legible at 16px favicon size</cons>
    </option>
    <option id="concept-b">
      <name>B — Stead frame</name>
      <pros>Stable operating ground / governed boundaries; enclosure reads strongly at small sizes</pros>
      <cons>Frame motif risks reading as a generic container if inner element is weak</cons>
    </option>
    <option id="concept-c">
      <name>C — Layered field</name>
      <pros>Environments / rule layers / topology of change; visually rich</pros>
      <cons>Layered detail is hardest to keep legible at 16px favicon</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Maintainer has replied with exactly one of: A, B, C (or concept-a / concept-b / concept-c). The chosen letter is recorded in the plan summary. This is a manual gate — no automated assertion; the gate is "a single concept letter is on record before Task 2 begins."
  </acceptance_criteria>
  <resume-signal>Reply with a single concept: A, B, or C</resume-signal>
  <done>One concept letter is selected and recorded; Task 2 may proceed using that concept's geometry.</done>
</task>

<task type="auto">
  <name>Task 2: Generate wordmark paths + author the 7-file lockup set + SVGO</name>
  <files>scripts/gen_wordmark_paths.py, brandbook/assets/logo/svgo.config.mjs, brandbook/assets/logo/rs-wordmark.svg, brandbook/assets/logo/rs-wordmark-dark.svg, brandbook/assets/logo/rs-mark.svg, brandbook/assets/logo/rs-mark-dark.svg, brandbook/assets/logo/rs-mark-mono.svg, brandbook/assets/logo/rs-favicon.svg, brandbook/assets/logo/rs-social-card.svg</files>
  <read_first>
    - .planning/phases/97-logo-mark-svg-system/97-RESEARCH.md (Pattern 1 a11y skeleton, Pattern 2 SVGO, Pattern 3 mono, Pattern 4 favicon, Pattern 5 social card, Pattern 6 text-to-path + fallbacks, Pattern 7 variant fills, all Pitfalls)
    - brandbook/assets/logo/concepts/rs-mark-concept-{selected}.svg (the chosen concept geometry)
    - brandbook/tokens.json (exact fill hexes)
    - brandbook/brand-book.md §14 (wordmark character — grounded, tightly spaced, architected)
  </read_first>
  <action>
    First write `scripts/gen_wordmark_paths.py` per RESEARCH.md Pattern 6 / Code Examples: prepend `~/Library/Python/3.14/lib/python/site-packages` to sys.path, download Sora Bold TTF over HTTPS from the fonts.gstatic.com URL in the interfaces block ONLY (no other host), extract glyph outlines for "Rulestead" via SVGPathPen + TTFont, apply the scale(s, -s) Y-flip, accumulate advance widths, and print/emit the `<path>` data. The TTF goes to a temp dir and is NOT committed. Run it to get the wordmark path data. If the script fails for any reason, use a Pattern 6 fallback (hand-traced geometric letterforms or one-time online converter) — commit only the resulting path data, never a font binary.

    Then write `brandbook/assets/logo/svgo.config.mjs` exactly per RESEARCH.md Pattern 2 (preset-default overrides removeTitle:false, removeDesc:false, removeViewBox:false, cleanupIds:false, convertColors:false; multipass:true).

    Then hand-author all 7 lockup SVGs using the selected concept's geometry for the mark and the generated paths for the wordmark text. Apply the exact background/mark/text fills from the interfaces table. Every file uses the accessible skeleton (role="img", aria-labelledby → unique <title id> + <desc id>, visual content wrapped in <g aria-hidden="true">):
      - rs-mark.svg / rs-mark-dark.svg — icon-only, viewBox 0 0 64 64 (or the concept's native square), the selected mark geometry.
      - rs-mark-mono.svg — same geometry, ALL paths fill="currentColor", no hex fills anywhere.
      - rs-wordmark.svg / rs-wordmark-dark.svg — mark beside the outlined "Rulestead" wordmark paths; wordmark text fill per table; tightly spaced per §14.
      - rs-favicon.svg — square viewBox (0 0 32 32 or 0 0 64 64), solid #3A6F8F background rect, #ffffff mark, fills only, 2-3 shapes max, must read at 16px.
      - rs-social-card.svg — viewBox="0 0 1200 630" width="1200" height="630", single #183247 background rect, #5885a0 mark + #e8edf3 outlined wordmark + outlined tagline "Runtime decisions, made clear." — ALL text outlined (zero <text>).

    Finally run SVGO over the 7 lockup files (NOT the concepts dir):
    `npx svgo --config brandbook/assets/logo/svgo.config.mjs brandbook/assets/logo/rs-wordmark.svg brandbook/assets/logo/rs-wordmark-dark.svg brandbook/assets/logo/rs-mark.svg brandbook/assets/logo/rs-mark-dark.svg brandbook/assets/logo/rs-mark-mono.svg brandbook/assets/logo/rs-favicon.svg brandbook/assets/logo/rs-social-card.svg`
    After SVGO, re-verify: every file keeps <title>; rs-mark-mono.svg still contains currentColor (if SVGO stripped it, the convertColors:false override should prevent this — re-run or fix); rs-social-card.svg still has viewBox="0 0 1200 630"; every file ≤20480 bytes (trim path precision / simplify social-card background if over). Do NOT add a package.json or run npm install — `npx svgo` is ad-hoc.
  </action>
  <acceptance_criteria>
    - `ls brandbook/assets/logo/rs-{wordmark,wordmark-dark,mark,mark-dark,mark-mono,favicon,social-card}.svg | wc -l` returns 7
    - `grep -c '<text' brandbook/assets/logo/*.svg` returns 0 for every file (glyphs outlined)
    - `grep -c 'base64' brandbook/assets/logo/*.svg` returns 0 for every file
    - `grep -rl 'http' brandbook/assets/logo/*.svg` prints nothing (no external refs; xmlns is `xmlns="http://www.w3.org/2000/svg"` — if SVGO leaves the xmlns and the grep matches, narrow to `grep -l 'href=.http' brandbook/assets/logo/*.svg` which must print nothing)
    - `grep -c '<script' brandbook/assets/logo/*.svg` returns 0 for every file
    - `grep 'viewBox="0 0 1200 630"' brandbook/assets/logo/rs-social-card.svg` matches
    - `grep -c 'currentColor' brandbook/assets/logo/rs-mark-mono.svg` returns > 0
    - `for f in brandbook/assets/logo/*.svg; do grep -q '<title' "$f" || echo "MISSING TITLE: $f"; done` prints nothing
    - `grep -l 'D2A94E' brandbook/assets/logo/*.svg` prints nothing (no Signal Gold)
    - `bash scripts/ci/lint.sh` prints `SVG SIZE BUDGET OK`
    - `ls scripts/gen_wordmark_paths.py` succeeds; `ls *.ttf brandbook/assets/logo/*.ttf 2>/dev/null | wc -l` returns 0 (no font binary committed)
  </acceptance_criteria>
  <verify>
    <automated>ls brandbook/assets/logo/rs-{wordmark,wordmark-dark,mark,mark-dark,mark-mono,favicon,social-card}.svg | wc -l | grep -q '^[[:space:]]*7$' && [ "$(grep -ch '<text' brandbook/assets/logo/*.svg | paste -sd+ - | bc)" = "0" ] && grep -q 'currentColor' brandbook/assets/logo/rs-mark-mono.svg && grep -q 'viewBox="0 0 1200 630"' brandbook/assets/logo/rs-social-card.svg && bash scripts/ci/lint.sh | grep -q 'SVG SIZE BUDGET OK' && echo OK</automated>
  </verify>
  <done>All 7 lockup SVGs exist, accessible, text-outlined, raster-free, script-free, within budget; mono uses currentColor; social card is 1200x630; wordmark generator script committed; no font binary committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Fonts CDN → build host | Sora Bold TTF downloaded over the network during wordmark outlining |
| repo → committed SVG | Hand-authored / script-generated SVGs enter the repo as brand source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03 | Tampering (MITM) / V9 Communications | Sora Bold TTF download in gen_wordmark_paths.py | mitigate | Download URL MUST be HTTPS from `fonts.gstatic.com` only (the exact URL in the interfaces block). No other host. urllib over https; TTF used in temp, never committed. |
| T-97-04 | Tampering / Elevation | committed SVG containing `<script>` | mitigate | SVGO preset-default `removeScripts` strips scripts; verify `grep -c '<script' brandbook/assets/logo/*.svg` = 0 post-SVGO. Served via `<img>` downstream — no inline execution. |
| T-97-05 | Information Disclosure | external http/href refs in committed SVG | mitigate | Verify no external refs: `grep -l 'href=.http' brandbook/assets/logo/*.svg` prints nothing (the only allowed `http` is the SVG namespace `xmlns="http://www.w3.org/2000/svg"`). |
| T-97-06 | Tampering | base64-embedded raster | mitigate | `grep -c 'base64' brandbook/assets/logo/*.svg` = 0 — all geometry is vector paths. |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installed. `npx svgo` runs ad-hoc (npm, approved in RESEARCH.md Package Legitimacy Audit). fontTools 4.62.1 already present (user install) — not installed by this plan. |
</threat_model>

<verification>
- All Task 2 acceptance_criteria assertions pass
- `bash scripts/ci/lint.sh` → `SVG SIZE BUDGET OK`
- A concept letter is recorded in the summary (Task 1 gate satisfied)
</verification>

<success_criteria>
The selected concept has graduated into a complete 7-file lockup set in `brandbook/assets/logo/` — accessible, text-outlined, raster-free, script-free, within the 20480-byte budget; rs-mark-mono.svg uses currentColor; rs-social-card.svg is viewBox 0 0 1200 630; the wordmark-path generator is committed with no font binary.
</success_criteria>

<output>
Create `.planning/phases/97-logo-mark-svg-system/97-02-SUMMARY.md` when done. Record the selected concept letter in the summary.
</output>
