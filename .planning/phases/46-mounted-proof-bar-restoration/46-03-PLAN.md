---
phase: 46-mounted-proof-bar-restoration
plan: 03
type: execute
wave: 3
depends_on:
  - 46-02
files_modified:
  - scripts/ci/test.sh
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - VER-01
must_haves:
  truths:
    - "Mounted proof failures distinguish setup/prerequisite drift from mounted-contract regressions without hiding raw test output."
    - "GitHub Actions exposes a stable named mounted-proof job and gates it by relevant paths at the job level."
    - "The mounted proof job feeds `release_gate` so branch protection can rely on one explicit merge-blocking result."
  artifacts:
    - path: "scripts/ci/test.sh"
      provides: "Categorized mounted-proof failure output and exact rerun/setup guidance"
      min_lines: 90
    - path: ".github/workflows/ci.yml"
      provides: "Named mounted proof lane, path-gated changes output, and release-gate wiring"
      min_lines: 140
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/test.sh"
      via: "the named workflow job must call the same repo-root verifier command maintainers and adopters use locally"
      pattern: "mounted_admin_contract|RULESTEAD_TEST_SCOPE"
    - from: ".github/workflows/ci.yml"
      to: "release_gate"
      via: "mounted proof must become merge-blocking through the existing aggregator rather than a separate protection model"
      pattern: "needs|changes|release_gate"
---

<objective>
Tighten shared scripts and CI output so mounted proof failures produce actionable remediation.

Purpose: Once the restored mounted contract bar is green locally, the last Phase 46 step is to make that bar honest and actionable in automation. The script should explain what likely broke and how to rerun it, while CI should surface a stable named lane and thread it into `release_gate`.
Output: categorized script output, a named mounted-proof workflow job, relevant path gating, and release-gate aggregation for the restored mounted verifier.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-mounted-proof-bar-restoration/46-RESEARCH.md
@.planning/phases/46-mounted-proof-bar-restoration/46-VALIDATION.md
@AGENTS.md
@prompts/rulestead-release-engineering-and-ci.md
@prompts/rulestead-testing-and-e2e-strategy.md
@prompts/rulestead-host-app-integration-seam.md
@prompts/rulestead-security-privacy-and-threat-model.md
@scripts/ci/test.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add named CI wiring and structured remediation around the restored mounted proof bar</name>
  <read_first>
    - scripts/ci/test.sh
    - .github/workflows/ci.yml
    - prompts/rulestead-release-engineering-and-ci.md
    - .planning/phases/46-mounted-proof-bar-restoration/46-RESEARCH.md
  </read_first>
  <files>scripts/ci/test.sh, .github/workflows/ci.yml</files>
  <behavior>
    - Test 1: mounted verifier failures print category, rerun command, setup expectation, and fallback guidance while preserving raw Mix/ExUnit output.
    - Test 2: workflow `changes` output includes a mounted-proof path bucket covering script, workflow, `rulestead_admin`, and relevant `rulestead` lifecycle/admin files.
    - Test 3: a stable named mounted-proof job runs only when needed and is included in `release_gate`.
  </behavior>
  <action>Extend `scripts/ci/test.sh` so the mounted verifier wrapper classifies likely failures as setup/prerequisite drift, mounted-contract regression, or unknown, and prints exact rerun/setup guidance without mutating local state or swallowing the raw failing command output. Then update `.github/workflows/ci.yml` to add a mounted-proof path bucket under `changes`, create a dedicated mounted-proof job patterned after `openfeature-companion`, and thread its result into `release_gate`. Keep job-id stability and job-level gating explicit. Do not use workflow-level path filtering, auto-setup hidden prerequisites, or fold the mounted proof into the main matrix in a way that obscures its identity.</action>
  <acceptance_criteria>
    - `RULESTEAD_TEST_SCOPE=mounted_admin_contract bash /Users/jon/projects/rulestead/scripts/ci/test.sh` still passes after the wrapper changes.
    - `rg -n "mounted|mounted-proof|mounted_admin_contract|release_gate|changes" /Users/jon/projects/rulestead/.github/workflows/ci.yml /Users/jon/projects/rulestead/scripts/ci/test.sh` finds the new wiring and remediation labels.
    - `bash -n /Users/jon/projects/rulestead/scripts/ci/test.sh` passes.
  </acceptance_criteria>
  <verify>
    <automated>bash -n /Users/jon/projects/rulestead/scripts/ci/test.sh && RULESTEAD_TEST_SCOPE=mounted_admin_contract bash /Users/jon/projects/rulestead/scripts/ci/test.sh</automated>
  </verify>
  <done>The restored mounted proof bar is named, actionable, and merge-blocking through the repo’s existing release gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script wrapper -> adopter remediation | If failure output is vague, users cannot tell whether they need setup help or whether the mounted contract actually regressed |
| workflow gating -> branch protection | If CI does not expose a stable mounted-proof lane or does not gate the right paths, required-check semantics drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-21 | R | failure output | mitigate | Print category plus exact rerun/setup guidance while leaving raw failures visible. |
| T-46-22 | T | path gating | mitigate | Add mounted-proof-specific `changes` output for all relevant mounted contract paths. |
| T-46-23 | D | branch-protection contract | mitigate | Thread the mounted-proof job into `release_gate` instead of relying on an advisory or hidden lane. |
</threat_model>

<verification>
- `bash -n /Users/jon/projects/rulestead/scripts/ci/test.sh`
- `RULESTEAD_TEST_SCOPE=mounted_admin_contract bash /Users/jon/projects/rulestead/scripts/ci/test.sh`
- `rg -n "mounted|mounted-proof|mounted_admin_contract|release_gate|changes" /Users/jon/projects/rulestead/.github/workflows/ci.yml /Users/jon/projects/rulestead/scripts/ci/test.sh`
</verification>

<success_criteria>
- Mounted proof failures are categorized and actionable.
- CI exposes a stable mounted-proof lane by name.
- `release_gate` becomes the single branch-protection path for the restored mounted contract bar.
</success_criteria>

<output>
After completion, create `.planning/phases/46-mounted-proof-bar-restoration/46-03-SUMMARY.md`
</output>
