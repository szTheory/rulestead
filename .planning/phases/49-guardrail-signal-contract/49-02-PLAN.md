---
phase: 49-guardrail-signal-contract
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - rulestead/lib/rulestead/ruleset/guardrail.ex
  - rulestead/lib/rulestead/ruleset/rollout.ex
  - rulestead/lib/rulestead/ruleset/rule.ex
  - rulestead/lib/rulestead/ruleset.ex
  - rulestead/test/rulestead/ruleset_validation_test.exs
autonomous: true
requirements: [ROL-01]
must_haves:
  truths:
    - "Authored rollout state can carry one or more explicit guardrail definitions as stable configuration, not as transient runtime metadata."
    - "Each guardrail definition freezes signal identity, threshold semantics, freshness requirements, sample-size requirements, and explicit scope expectations inside the same draft/publish validation path as the existing ruleset."
    - "Invalid or partial guardrail definitions fail authored-state validation before publish instead of leaking weak semantics into later rollout automation."
  artifacts:
    - path: "rulestead/lib/rulestead/ruleset/guardrail.ex"
      provides: "Typed embedded guardrail schema and changeset rules for rollout-authored state"
      min_lines: 70
    - path: "rulestead/lib/rulestead/ruleset/rollout.ex"
      provides: "Rollout embed extended to own explicit guardrail configuration"
      min_lines: 35
    - path: "rulestead/test/rulestead/ruleset_validation_test.exs"
      provides: "Ruleset validation proof for valid and invalid guardrail authored payloads"
      min_lines: 80
  key_links:
    - from: "rulestead/lib/rulestead/ruleset/rollout.ex"
      to: "rulestead/lib/rulestead/ruleset/guardrail.ex"
      via: "rollout authored-state should compose the new embed directly rather than hiding guardrails in generic metadata"
      pattern: "embeds_many|guardrails"
    - from: "rulestead/lib/rulestead/ruleset/rule.ex"
      to: "rulestead/lib/rulestead/ruleset/rollout.ex"
      via: "existing rule strategy validation must continue to own when rollout configuration is legal"
      pattern: "cast_embed\\(:rollout|validate_rollout_requirements"
    - from: "rulestead/test/rulestead/ruleset_validation_test.exs"
      to: "rulestead/lib/rulestead/ruleset.ex"
      via: "tests must prove guardrail validation runs through the standard ruleset draft/publish changeset path"
      pattern: "Ruleset\\.changeset|guardrail"
---

<objective>
Attach typed guardrail definitions to authored rollout state and validate them through the existing ruleset discipline.

Purpose: `ROL-01` is not satisfied until operators can author guardrails on the rollout itself. This plan turns the seam defined in Plan 01 into stable authored-state that survives draft, publish, compare, and later decision-engine work without inventing side-channel rollout safety storage.
Output: a new embedded guardrail schema composed into rollout-authored state plus ruleset validation coverage for valid and invalid guardrail payloads.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-guardrail-signal-contract/49-CONTEXT.md
@.planning/phases/49-guardrail-signal-contract/49-RESEARCH.md
@.planning/phases/49-guardrail-signal-contract/49-PATTERNS.md
@.planning/phases/49-guardrail-signal-contract/49-01-SUMMARY.md
@rulestead/lib/rulestead/ruleset.ex
@rulestead/lib/rulestead/ruleset/rule.ex
@rulestead/lib/rulestead/ruleset/rollout.ex
@rulestead/test/rulestead/ruleset_validation_test.exs

<interfaces>
From `rulestead/lib/rulestead/ruleset.ex`:
```elixir
schema "rulesets" do
  field(:version, :integer)
  field(:status, Ecto.Enum, values: @statuses, default: :draft)
  embeds_many(:rules, Rule, on_replace: :delete)
end
```

From `rulestead/lib/rulestead/ruleset/rule.ex`:
```elixir
embedded_schema do
  field(:strategy, Ecto.Enum, values: @strategies)
  embeds_one(:rollout, Rollout, on_replace: :update)
end
```

From `rulestead/lib/rulestead/ruleset/rollout.ex`:
```elixir
embedded_schema do
  field(:bucket_by, Ecto.Enum, values: @bucket_by_values)
  field(:percentage, :integer)
  field(:salt, :string)
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a typed guardrail embed to rollout authored-state</name>
  <files>rulestead/lib/rulestead/ruleset/guardrail.ex, rulestead/lib/rulestead/ruleset/rollout.ex, rulestead/lib/rulestead/ruleset/rule.ex, rulestead/lib/rulestead/ruleset.ex</files>
  <action>
    Extend authored rollout state per D-04, D-05, D-06, and D-10 by introducing a dedicated `Rulestead.Ruleset.Guardrail` embedded schema and composing it into the existing rollout embed. The guardrail schema must carry stable authored fields for signal identity, comparator/threshold semantics, freshness requirements, minimum sample size, explicit environment expectations, optional tenant expectations, and any bounded scope-source/provenance fields that later phases will need. Use Ecto embeds and enums in the style already used across rules, rollouts, and experiments; do not hide guardrails in `metadata`, do not add mutable health state, and do not widen the rule strategy matrix.

    Keep rule legality explicit: rollout-enabled strategies may own guardrails, unsupported strategies must still reject incompatible rollout payloads, and the parent ruleset changeset remains the only validation path. This slice must not implement monitoring windows, stage outcomes, or UI-specific labels per D-13 and D-15.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/ruleset_validation_test.exs</automated>
  </verify>
  <done>Guardrails are first-class authored rollout configuration validated through the existing ruleset embed chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ruleset validation proof for valid and invalid guardrail payloads</name>
  <files>rulestead/test/rulestead/ruleset_validation_test.exs</files>
  <behavior>
    - Test 1: a valid rollout rule accepts one or more guardrail definitions with explicit comparator, threshold, freshness, sample-size, environment, and optional tenant fields.
    - Test 2: missing threshold semantics, blank signal identity, or missing environment scope make the authored ruleset invalid.
    - Test 3: invalid enum values or partial guardrail scope/provenance fields fail closed in the ruleset changeset.
  </behavior>
  <action>
    Add focused authored-state tests that prove D-04 through D-12 survive the existing draft/publish changeset path. Reuse `valid_ruleset_attrs/0` style fixtures where possible and extend them only enough to encode staged rollout guardrails. Assert on the nested ruleset errors so future regressions cannot silently demote guardrails back into loose maps. Do not add runtime evaluation assertions here; this slice owns authored-state validity only.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/ruleset_validation_test.exs</automated>
    <automated>rg -n "guardrail|threshold|freshness|min_sample|environment|tenant" /Users/jon/projects/rulestead/rulestead/test/rulestead/ruleset_validation_test.exs</automated>
  </verify>
  <done>Ruleset validation now rejects malformed guardrail authored-state and accepts the bounded contract Phase 49 intends to ship.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-authored rollout payload -> ruleset changeset | Invalid or weak guardrail config could enter durable authored state unless changeset rules fail closed |
| ruleset embed chain -> later rollout consumers | If guardrails hide in metadata or partial maps, later automation and export surfaces will inherit ambiguous semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | T | `Rulestead.Ruleset.Guardrail` | mitigate | Use typed embedded schema fields and bounded enums instead of freeform maps so authored threshold and scope semantics cannot drift. |
| T-49-05 | R | `Rulestead.Ruleset` changeset chain | mitigate | Validate guardrails through the existing ruleset draft/publish path so authored changes remain reviewable and replayable. |
| T-49-06 | I | rollout authored-state | mitigate | Reject missing environment scope, blank signal identifiers, and partial sample/freshness requirements before publish. |
</threat_model>

<verification>
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/ruleset_validation_test.exs`
- `rg -n "guardrail|threshold|freshness|min_sample|environment|tenant" /Users/jon/projects/rulestead/rulestead/test/rulestead/ruleset_validation_test.exs`
</verification>

<success_criteria>
- Rollout authored-state owns guardrail definitions directly.
- Guardrail fields are explicit, typed, and validated through the normal ruleset changeset flow.
- Invalid or partial guardrail definitions fail before publish.
- The slice stays out of automatic decision execution and mounted UI behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/49-guardrail-signal-contract/49-02-SUMMARY.md`
</output>
