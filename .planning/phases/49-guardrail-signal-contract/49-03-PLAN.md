---
phase: 49-guardrail-signal-contract
plan: 03
type: execute
wave: 3
depends_on: [49-01, 49-02]
files_modified:
  - rulestead/lib/rulestead/store/command.ex
  - rulestead/lib/rulestead/audit_event.ex
  - rulestead/lib/rulestead/telemetry.ex
  - rulestead/lib/rulestead/promotion/compare.ex
  - rulestead/lib/rulestead/manifest/export.ex
  - rulestead/test/rulestead/store/manifest_export_contract_test.exs
  - rulestead/test/rulestead/audit_event_governance_test.exs
autonomous: true
requirements: [ROL-01]
must_haves:
  truths:
    - "The new guardrail contract survives serialization and comparison surfaces without dropping scope, threshold, freshness, or sample-size semantics."
    - "Guardrail scope and provenance reuse the existing bounded command and audit normalization style instead of inventing a second dialect."
    - "Phase 49 closes with executable proof that the contract is exportable, comparable, and audit-compatible without implementing hold/rollback decisions or mounted UI."
  artifacts:
    - path: "rulestead/lib/rulestead/store/command.ex"
      provides: "Guardrail scope/provenance normalization aligned with existing governance metadata helpers"
      min_lines: 20
    - path: "rulestead/lib/rulestead/manifest/export.ex"
      provides: "Manifest/export projection that keeps authored guardrail definitions intact"
      min_lines: 20
    - path: "rulestead/test/rulestead/store/manifest_export_contract_test.exs"
      provides: "Contract proof that export surfaces preserve the new authored guardrail shape"
      min_lines: 40
  key_links:
    - from: "rulestead/lib/rulestead/store/command.ex"
      to: "rulestead/lib/rulestead/audit_event.ex"
      via: "guardrail scope and provenance must flow through one bounded normalization path for future automatic interventions"
      pattern: "tenant|provenance|guardrail"
    - from: "rulestead/lib/rulestead/manifest/export.ex"
      to: "rulestead/test/rulestead/store/manifest_export_contract_test.exs"
      via: "export proof must assert the authored guardrail contract survives manifest rendering"
      pattern: "guardrail|threshold|freshness|min_sample"
    - from: "rulestead/lib/rulestead/promotion/compare.ex"
      to: "rulestead/lib/rulestead/ruleset/guardrail.ex"
      via: "compare canonicalization should treat guardrails as authored rollout state, not opaque metadata"
      pattern: "guardrail|rollout"
---

<objective>
Reclose scope, audit, compare, and export proof for the new guardrail contract without leaking into later phases.

Purpose: The Phase 49 contract is incomplete if it only validates in memory. This plan threads the new authored-state through the repo’s bounded normalization and projection surfaces so later decision-engine and mounted work inherit one stable contract instead of rebuilding it piecemeal.
Output: guardrail-aware command/audit/export normalization plus focused proof that authored guardrail state survives manifest and audit-facing projections unchanged in meaning.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-guardrail-signal-contract/49-CONTEXT.md
@.planning/phases/49-guardrail-signal-contract/49-RESEARCH.md
@.planning/phases/49-guardrail-signal-contract/49-PATTERNS.md
@.planning/phases/49-guardrail-signal-contract/49-01-SUMMARY.md
@.planning/phases/49-guardrail-signal-contract/49-02-SUMMARY.md
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/audit_event.ex
@rulestead/lib/rulestead/telemetry.ex
@rulestead/lib/rulestead/promotion/compare.ex
@rulestead/lib/rulestead/manifest/export.ex
@rulestead/test/rulestead/store/manifest_export_contract_test.exs

<interfaces>
From `rulestead/lib/rulestead/store/command.ex`:
```elixir
def normalize_tenant_provenance(value) when is_list(value) or is_map(value) do
  ...
end

def with_tenant_provenance(payload, source \\ nil, opts \\ []) do
  ...
end
```

From `rulestead/lib/rulestead/audit_event.ex`:
```elixir
def metadata(attrs \\ %{}) when is_list(attrs) or is_map(attrs) do
  ...
  |> maybe_put("tenant", tenant)
end
```

From `rulestead/lib/rulestead/manifest/export.ex`:
```elixir
"active_ruleset" => active_ruleset
...
|> maybe_put("rollout", ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reuse bounded scope and audit normalization for guardrail contract metadata</name>
  <files>rulestead/lib/rulestead/store/command.ex, rulestead/lib/rulestead/audit_event.ex, rulestead/lib/rulestead/telemetry.ex</files>
  <action>
    Thread the new contract through existing normalization seams per D-11 and D-14. Extend `Rulestead.Store.Command.GovernanceSupport` with a bounded helper for guardrail scope/provenance that reuses the existing tenant/environment vocabulary instead of inventing a new one. Teach `Rulestead.AuditEvent.metadata/1` and any telemetry metadata helpers touched in this slice to preserve only shallow, bounded guardrail contract fields that later automatic hold/rollback events will need: signal identity, scope, comparator, threshold value, freshness requirement, sample-size requirement, and normalized reason/status. Do not emit or persist raw provider payloads, freeform upstream error strings, or Phase 50 action outcomes.

    This is a compatibility slice only: no new audit event types, no automatic mutations, no monitoring windows, and no `rulestead_admin` work.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/audit_event_governance_test.exs</automated>
  </verify>
  <done>The guardrail contract can already flow through bounded governance/audit normalization without creating a second metadata dialect.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve authored guardrails in compare and manifest export proof</name>
  <files>rulestead/lib/rulestead/promotion/compare.ex, rulestead/lib/rulestead/manifest/export.ex, rulestead/test/rulestead/store/manifest_export_contract_test.exs</files>
  <action>
    Extend compare/export projections so the authored guardrail contract survives the same proof surfaces operators already use to inspect authored state. In `Rulestead.Promotion.Compare`, treat guardrails as part of canonical rollout-authored state so compare drift can reason over them later without opaque metadata blobs. In `Rulestead.Manifest.Export`, include guardrails in the projected active ruleset payload in a normalized shape consistent with the new embed. Update `manifest_export_contract_test.exs` to assert that the expected manifest contains authored guardrails with explicit threshold, freshness, minimum sample size, environment scope, and optional tenant scope.

    Keep the proof limited to authored-state transport per D-13: do not implement manifest import decision logic, staged monitoring windows, hold/rollback state, or UI-facing copy.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/store/manifest_export_contract_test.exs</automated>
    <automated>rg -n "guardrail|threshold|freshness|min_sample|tenant|environment" /Users/jon/projects/rulestead/rulestead/test/rulestead/store/manifest_export_contract_test.exs</automated>
  </verify>
  <done>Export and compare surfaces preserve the authored guardrail contract so later phases inherit one stable representation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored-state -> manifest/compare projection | Guardrail semantics can be lost or reshaped if projection code treats them as opaque rollout metadata |
| normalized contract -> audit/telemetry metadata | Overbroad persistence could leak provider-specific or sensitive observability detail into durable or emitted surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-07 | I | `Rulestead.AuditEvent.metadata/1` and telemetry helpers | mitigate | Persist and emit only bounded guardrail contract fields; reject raw provider blobs and freeform upstream errors. |
| T-49-08 | T | `Rulestead.Manifest.Export` and `Rulestead.Promotion.Compare` | mitigate | Canonicalize guardrails as first-class rollout-authored state so projection/export cannot silently drop threshold or scope semantics. |
| T-49-09 | R | governance metadata path | mitigate | Reuse one normalization helper in `Command.GovernanceSupport` so future automatic actions can cite the exact same scope and provenance contract. |
</threat_model>

<verification>
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/audit_event_governance_test.exs`
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/store/manifest_export_contract_test.exs`
- `rg -n "guardrail|threshold|freshness|min_sample|tenant|environment" /Users/jon/projects/rulestead/rulestead/test/rulestead/store/manifest_export_contract_test.exs`
</verification>

<success_criteria>
- The guardrail contract reuses the existing bounded command/audit normalization style.
- Compare and manifest export treat guardrails as authored rollout state, not opaque metadata.
- Proof covers transport of explicit threshold and scope semantics without implementing Phase 50 or 51 behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/49-guardrail-signal-contract/49-03-SUMMARY.md`
</output>
