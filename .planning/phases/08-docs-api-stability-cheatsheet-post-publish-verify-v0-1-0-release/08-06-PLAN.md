---
phase: 08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release
plan: 06
type: execute
wave: 4
depends_on:
  - 08-05
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release-please.yml
  - .github/workflows/publish-hex.yml
  - scripts/ci/release_gate.sh
  - scripts/ci/release_please_dry_run.sh
  - scripts/ci/check_package_whitelist.sh
  - scripts/ci/admin_publish_guard.sh
  - MAINTAINING.md
autonomous: true
requirements:
  - REL-03
user_setup:
  - service: hex
    why: "GitHub Actions must publish both sibling packages after one explicit maintainer approval"
    env_vars:
      - name: HEX_API_KEY
        source: "Hex.pm -> Account settings -> API Keys"
    dashboard_config:
      - task: "Create a protected GitHub Actions environment for Hex publish approval (for example `hex-publish`) with one required reviewer"
        location: "GitHub repo Settings -> Environments"
must_haves:
  truths:
    - "Release automation requires one explicit maintainer approval before irreversible Hex publication, then publishes `rulestead` before `rulestead_admin`."
    - "Manual recovery remains available if the gated publish path fails after tagging."
    - "Release readiness treats the Phase 7 sibling-package admin slice as a hard prerequisite instead of assuming the train is green."
    - "Publish choreography hands off cleanly to a later post-publish verification wave instead of claiming live artifact proof before Hex publish finishes."
  artifacts:
    - path: ".github/workflows/release-please.yml"
      provides: "Gated publish choreography with one explicit approval and ordered sibling-package release flow"
    - path: ".github/workflows/publish-hex.yml"
      provides: "Publish workflow that executes preflight, waits for approval, then publishes core before admin"
    - path: "MAINTAINING.md"
      provides: "Maintainer-facing publish, recovery, and handoff runbook for v0.1.0"
  key_links:
    - from: ".github/workflows/release-please.yml"
      to: ".github/workflows/publish-hex.yml"
      via: "release tags prepare the publish run, but protected-environment approval gates irreversible publish per D-16 through D-19"
      pattern: "workflow_dispatch|environment|tag"
    - from: "scripts/ci/release_gate.sh"
      to: "rulestead_admin/test/rulestead_admin/live/flag_live/simulate_test.exs"
      via: "release readiness re-runs the Phase 7 sibling-package admin slice before publish"
      pattern: "rulestead_admin|simulate_test|phase 7"
    - from: ".github/workflows/publish-hex.yml"
      to: "mix verify.release_publish"
      via: "workflow stops at publish completion and hands the live verification trio to the next wave once Hex artifacts exist"
      pattern: "verify.release_publish|post-publish|version"
---

<objective>
Finish the release choreography: gated publish automation, release preflight, and the maintainer recovery path.

Purpose: Phase 8 must ship a release process maintainers can trust. This plan covers the irreversible choreography up to and including ordered Hex publish, but leaves live post-publish artifact verification to the next wave once the artifacts actually exist.
Output: Updated release workflows/scripts and a maintainer runbook reflecting the v0.1.0 publish path and handoff into post-publish verification.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-CONTEXT.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-VERIFICATION.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-11-SUMMARY.md
@.planning/phases/01-repo-bootstrap/01-05-SUMMARY.md
@.github/workflows/ci.yml
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml
@scripts/ci/release_gate.sh
@scripts/ci/release_please_dry_run.sh
@prompts/rulestead-release-engineering-and-ci.md
@MAINTAINING.md

<interfaces>
Existing release surfaces:
```text
scripts/ci/release_gate.sh
scripts/ci/release_please_dry_run.sh
.github/workflows/release-please.yml
.github/workflows/publish-hex.yml
```

Required new automation from roadmap:
- ordered publish: `rulestead` first, then `rulestead_admin`
- final handoff into a post-publish verification wave once live Hex artifacts exist
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the gated release and preflight choreography into workflows and scripts</name>
  <files>.github/workflows/ci.yml, .github/workflows/release-please.yml, .github/workflows/publish-hex.yml, scripts/ci/release_gate.sh, scripts/ci/release_please_dry_run.sh, scripts/ci/check_package_whitelist.sh, scripts/ci/admin_publish_guard.sh, MAINTAINING.md</files>
  <behavior>
    - Test 1: the release path requires one explicit approval before Hex publish and publishes `rulestead` before `rulestead_admin`.
    - Test 2: release readiness re-runs the Phase 7 sibling-package admin slice before publish instead of assuming older verification is still valid.
    - Test 3: maintainers have a documented manual recovery path if publish fails partway through, plus an explicit handoff into the live post-publish verification wave.
  </behavior>
  <action>Update the existing release scripts/workflows rather than replacing them. Implement D-16 through D-19 by keeping release-please as the tag/PR generator, then using a protected-environment or equivalent explicit approval gate before the actual publish credentials are exposed. Preserve ordered lockstep publish: `rulestead` first, `rulestead_admin` second. Evolve the existing shell scripts so the preflight gate includes the still-relevant Phase 7 sibling-package admin slice noted in the 2026-04-24 verification report, using 07-11 as the contract-closing fix but not assuming the release train is green until that slice is re-run. Update `MAINTAINING.md` with the exact release, fallback, recovery, and post-publish handoff choreography. Keep the admin publish guard structural and maintain the linked-version two-package design. Do not add the daily drift workflow in this plan; that moves to the final post-publish verification plan to keep this publish choreography reviewable.</action>
  <verify>
    <automated>bash -lc 'scripts/ci/release_please_dry_run.sh && scripts/ci/release_gate.sh changes=success lint=success test=success integration-placeholder=success'</automated>
  </verify>
  <done>The release workflow is gated, ordered, phase-7-aware at preflight time, and documented for maintainers with a real fallback path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub workflow triggers -> Hex publish credentials | CI automation crosses into irreversible package publication here |
| release preflight -> sibling-package admin slice | Stale verification evidence must not be trusted for release readiness |
| publish completion -> post-publish verification wave | Live artifact verification must not be claimed before Hex serves the new packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | E | publish workflows | mitigate | Require one explicit approval via a protected environment or equivalent gate before exposing `HEX_API_KEY`, then publish core before admin per D-16 through D-18. |
| T-08-06-02 | R | release preflight | mitigate | Re-run the Phase 7 sibling-package admin slice during release readiness so old verification reports cannot stand in for a fresh green preflight. |
| T-08-06-03 | T | publish handoff | mitigate | Keep live `mix verify.release_publish` and parity execution out of this plan and hand off explicitly to the final post-publish plan once the Hex artifacts exist. |
</threat_model>

<verification>
Dry-run the release-please helper and validate the updated gate script. Confirm that the maintainer runbook names the approval step, ordered publish sequence, manual recovery path, and the follow-on post-publish verification handoff explicitly.
</verification>

<success_criteria>
Phase 8 has a trustworthy publish machine: one approval before publish, ordered sibling-package publish, a fresh Phase 7 preflight, and a documented recovery path plus explicit handoff into the post-publish verification wave.
</success_criteria>

<output>
After completion, create `.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-06-SUMMARY.md`
</output>
