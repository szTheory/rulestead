---
phase: 08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - rulestead/README.md
  - rulestead_admin/README.md
  - guides/introduction/installation.md
  - guides/introduction/getting-started.md
  - guides/introduction/upgrading.md
  - CONVENTIONS.md
autonomous: true
requirements:
  - DOC-04
must_haves:
  truths:
    - "A first-time reader lands on an Alex-first root README that gets to a working install quickly, then routes clearly into build, operate, and extend paths."
    - "The core package README stays runtime-first and minimal, while the admin package README stays narrowly focused on the mount seam, `policy:` requirement, and host session/query contract."
    - "Installation, getting-started, and upgrading guides describe the shipped v0.1.0 path without Phase 9+ promises or stale pre-release caveats."
    - "A top-level `CONVENTIONS.md` states the discipline layer for determinism, first-match precedence, tenancy boundaries, testing, and PII-redaction expectations."
  artifacts:
    - path: "README.md"
      provides: "Alex-first docs front door with role-based path split per D-11 through D-13"
    - path: "rulestead/README.md"
      provides: "Core package-local runtime README that points to shared guides without duplicating the root story"
    - path: "rulestead_admin/README.md"
      provides: "Narrow admin host-facing contract doc matching D-02 and D-13"
    - path: "guides/introduction/getting-started.md"
      provides: "Published-package quickstart that mirrors the supported install and mount path"
    - path: "CONVENTIONS.md"
      provides: "Phase 8 discipline layer for DOC-04"
  key_links:
    - from: "README.md"
      to: "guides/introduction/getting-started.md"
      via: "15-minute quickstart hands readers into the full onboarding path"
      pattern: "getting-started"
    - from: "rulestead_admin/README.md"
      to: "rulestead_admin/lib/rulestead_admin/router.ex"
      via: "documented router macro, `policy:` requirement, and `?env=` convention"
      pattern: "rulestead_admin|policy|env"
    - from: "CONVENTIONS.md"
      to: "guides/flows/telemetry.md"
      via: "PII-redaction and public-contract rules align with the existing telemetry contract"
      pattern: "PII|redaction|telemetry"
---

<objective>
Author the docs front door and discipline layer for the first real release: root/package READMEs, introduction guides, and the new `CONVENTIONS.md`.

Purpose: Phase 8 ships the first public release. The repo front door must become trustworthy release documentation rather than a Phase 1 promise, and the discipline layer must be explicit before API-stability and release verification docs lock.
Output: Release-ready root/package READMEs, three introduction guides, and a concrete `CONVENTIONS.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-CONTEXT.md
@.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-PATTERNS.md
@README.md
@rulestead/README.md
@rulestead_admin/README.md
@prompts/rulestead-personas-jtbd-and-onboarding.md
@prompts/rulestead-engineering-dna-from-prior-libs.md

<interfaces>
From `rulestead_admin/lib/rulestead_admin/router.ex`:
```elixir
defmacro rulestead_admin(path, opts \\ []) do
  policy = Keyword.fetch!(opts, :policy)
```

Documented host session/query contract already established in `rulestead_admin/README.md`:
```markdown
- "current_actor" for policy checks
- "rulestead_admin_environments" as the environment picker source
- "rulestead_admin_last_env" as the remembered fallback
The URL query param `env` is the canonical environment selector.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the root and package READMEs for the real v0.1.0 audience split</name>
  <files>README.md, rulestead/README.md, rulestead_admin/README.md</files>
  <action>Replace Phase 1/pre-release framing with release-ready copy. In `README.md`, implement the Alex-first front door per D-11 and D-12: lead with the 15-minute quickstart, then route immediately into three explicit paths (`build with Rulestead`, `operate via Admin UI`, `extend Rulestead`). In `rulestead/README.md`, keep the runtime package minimal and runtime-first per D-13. In `rulestead_admin/README.md`, keep the contract narrow per D-02, D-03, and D-13: mount seam, required `policy:` behavior, host session inputs, and explicit `?env=` URL convention only. Do not stabilize internal LiveView module names, CSS/DOM structure, or socket assigns in any README.</action>
  <verify>
    <automated>cd rulestead && mix docs --warnings-as-errors</automated>
  </verify>
  <done>The root README becomes the real public front door, the core README stays package-local and runtime-first, and the admin README promises only the intended narrow host-facing contract.</done>
</task>

<task type="auto">
  <name>Task 2: Author release-ready introduction docs and the discipline layer</name>
  <files>guides/introduction/installation.md, guides/introduction/getting-started.md, guides/introduction/upgrading.md, CONVENTIONS.md</files>
  <action>Write the three introduction guides against shipped behavior only, reflecting the published-package install path and the two-package release design. `installation.md` should explain when apps need only `rulestead` vs both packages. `getting-started.md` should mirror the quickstart and point to the follow-on guides without becoming an architecture essay. `upgrading.md` should set the pre-1.0 compatibility posture and point readers to `guides/api_stability.md` once Plan 04 lands. Add `CONVENTIONS.md` for DOC-04, codifying determinism, first-match-wins precedence, explicit tenancy scoping, fake-first testing, and no-PII-in-telemetry-or-audit defaults. Reference existing Credo/redaction enforcement as the mechanical guardrail; do not invent future governance features or future-phase rules.</action>
  <verify>
    <automated>cd rulestead && mix docs --warnings-as-errors</automated>
  </verify>
  <done>The introductory docs are release-accurate, and `CONVENTIONS.md` states the non-negotiable discipline layer the rest of Phase 8 can cite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs -> adopters | External readers will treat documented behavior as a support promise |
| docs claims -> package/runtime behavior | Any overstatement of internal or future behavior becomes an accidental public contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | S | `rulestead_admin/README.md` | mitigate | Document only the router seam, required policy behavior, and explicit session/query conventions per D-02 and D-03; exclude internal LiveView modules and DOM structure. |
| T-08-01-02 | T | `CONVENTIONS.md` | mitigate | Tie discipline rules to shipped behavior and existing enforcement, not speculative future seams. |
| T-08-01-03 | R | root onboarding docs | mitigate | Mirror the actual install/mount flow and linked-version packaging so release verification can test the same promised path later in Phase 8. |
</threat_model>

<verification>
Build HexDocs after the README and introduction/conventions rewrite, then sanity-check that the package-local READMEs keep the sibling-package boundary intact and do not expose admin internals as public API.
</verification>

<success_criteria>
The public docs front door is ready for v0.1.0: first-time readers can install quickly, package-local READMEs stay honest about their boundaries, and the discipline layer is explicit enough for the API-stability and release plans to anchor on.
</success_criteria>

<output>
After completion, create `.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-01-SUMMARY.md`
</output>
