---
phase: 08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - guides/api_stability.md
  - rulestead/mix.exs
  - rulestead_admin/mix.exs
  - rulestead/CHANGELOG.md
  - rulestead_admin/CHANGELOG.md
  - rulestead/test/rulestead/release_contract_test.exs
  - rulestead_admin/test/rulestead_admin/integration/admin_mount_test.exs
autonomous: true
requirements:
  - REL-06
must_haves:
  truths:
    - "The locked v0.1.0 public surface is enumerated in one explicit `guides/api_stability.md` contract."
    - "The admin package contract remains narrow: router mount seam, required `policy:` behavior, and explicitly documented host-facing URL/session conventions only."
    - "Automated contract tests fail if the documented public surface drifts or if internal admin modules start masquerading as public API."
    - "Both package docs configs include the final Phase 8 docs set and changelogs reference the API-stability contract."
  artifacts:
    - path: "guides/api_stability.md"
      provides: "REL-06 locked public surface inventory with non-public exclusions"
    - path: "rulestead/test/rulestead/release_contract_test.exs"
      provides: "Public-surface lock tests for root APIs, structs, error types, telemetry, and config keys"
    - path: "rulestead_admin/test/rulestead_admin/integration/admin_mount_test.exs"
      provides: "Admin mount contract proof without exposing internal LiveView module names"
    - path: "rulestead/mix.exs"
      provides: "Final Phase 8 ExDoc extras wiring for the shared docs set"
  key_links:
    - from: "guides/api_stability.md"
      to: "rulestead/test/rulestead/release_contract_test.exs"
      via: "documented public surface is enforced by imports and reflection-based contract checks"
      pattern: "Rulestead|Context|Result|Error|telemetry"
    - from: "guides/api_stability.md"
      to: "rulestead_admin/test/rulestead_admin/integration/admin_mount_test.exs"
      via: "admin contract proof is limited to the router seam and host-facing conventions"
      pattern: "rulestead_admin|policy|env"
    - from: "rulestead/mix.exs"
      to: "guides/api_stability.md"
      via: "HexDocs extras include the locked public contract in the final docs nav"
      pattern: "api_stability|cheatsheet|extending-rulestead"
---

<objective>
Lock the public contract: author `guides/api_stability.md`, wire the final docs set into HexDocs, update changelog references, and add public-surface lock tests.

Purpose: Phase 8 must turn the codebase's intended public boundary into an explicit, test-backed release contract. This is the line between "documented behavior" and "internal implementation detail."
Output: `guides/api_stability.md`, final docs extras wiring, changelog references, and public-surface contract tests for both sibling packages.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-CONTEXT.md
@.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-PATTERNS.md
@rulestead/mix.exs
@rulestead_admin/mix.exs
@rulestead/lib/rulestead.ex
@rulestead/lib/rulestead/context.ex
@rulestead/lib/rulestead/result.ex
@rulestead/lib/rulestead/error.ex
@rulestead/lib/rulestead/store.ex
@rulestead/lib/rulestead/admin/policy.ex
@rulestead_admin/lib/rulestead_admin/router.ex

<interfaces>
Current root public modules/functions:
```elixir
Rulestead.evaluate/3
Rulestead.evaluate!/3
Rulestead.enabled?/2
Rulestead.get_value/3
Rulestead.get_variant/2
Rulestead.explain/2
Rulestead.explain_flag/4
```

Current concrete public behaviors/macros:
```elixir
Rulestead.Store
Rulestead.Admin.Policy
RulesteadAdmin.Router.rulestead_admin/2
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write `api_stability.md` with an explicit public/private boundary</name>
  <files>guides/api_stability.md, rulestead/CHANGELOG.md, rulestead_admin/CHANGELOG.md</files>
  <behavior>
    - Test 1: the contract doc enumerates the locked `Rulestead` root surface, public structs/fields, closed error-type set, telemetry event names/metadata keys, and supported config keys.
    - Test 2: the contract doc states clearly that the admin package boundary is the router mount seam plus documented host-facing conventions, not internal LiveView modules or DOM structure.
    - Test 3: any mention of planned seams is excluded from the stability contract.
  </behavior>
  <action>Author `guides/api_stability.md` per D-01 through D-04. Enumerate the stable `rulestead` surface as explicit closed catalogs: public modules, public function arities, public struct fields, error `:type` atoms, telemetry events/metadata keys, and NimbleOptions/config schema keys. For `rulestead_admin`, document only the narrow host-facing contract from D-02: router mount macro, required `policy:` behavior, and explicitly documented session/query conventions. State bluntly that `RulesteadAdmin.Live.*`, `RulesteadAdmin.Components.*`, socket assigns, DOM/CSS, and helper modules remain non-public per D-03. Update both changelogs to point readers at `guides/api_stability.md` as the release contract reference.</action>
  <verify>
    <automated>cd rulestead && mix docs --warnings-as-errors</automated>
  </verify>
  <done>`guides/api_stability.md` exists as the authoritative v0.1.0 public contract, and the changelogs point to it explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add public-surface lock tests and final docs wiring</name>
  <files>rulestead/mix.exs, rulestead_admin/mix.exs, rulestead/test/rulestead/release_contract_test.exs, rulestead_admin/test/rulestead_admin/integration/admin_mount_test.exs</files>
  <behavior>
    - Test 1: root contract tests import and assert the locked public modules/functions/struct fields named in `guides/api_stability.md`.
    - Test 2: admin contract tests prove the mount seam and host-facing conventions without importing internal LiveView modules as public API.
    - Test 3: the final HexDocs extras include `CONVENTIONS.md`, the cheatsheet, `api_stability.md`, and the extending guide.
  </behavior>
  <action>Add `rulestead/test/rulestead/release_contract_test.exs` as the API-stability lock test suite. Use imports/reflection/assertions to pin the documented public surface and to fail fast if new undocumented public functions or contract drift appear. Update the existing admin mount integration test so it proves only the stable admin package boundary, not internal module names. Wire the final Phase 8 docs set into `rulestead/mix.exs` and keep `rulestead_admin/mix.exs` package-local and narrow; do not create a second shared docs pipeline.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/release_contract_test.exs && cd ../rulestead_admin && mix test test/rulestead_admin/integration/admin_mount_test.exs'</automated>
  </verify>
  <done>The docs nav includes the full Phase 8 surface, and automated contract tests lock the intended public boundary for both packages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code surface -> public API contract | Any module/function/field named here becomes a compatibility promise |
| admin package internals -> host apps | Host apps must not accidentally bind to internal LiveView implementation details |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | T | `guides/api_stability.md` | mitigate | Use explicit closed catalogs for the stable surface and a blunt non-public section excluding internal runtime/admin modules per D-01 through D-04. |
| T-08-04-02 | E | `rulestead_admin` contract | mitigate | Limit admin lock tests to the router seam, `policy:` requirement, and documented host-facing conventions so internal modules cannot become implicit public API. |
| T-08-04-03 | R | docs extras wiring | mitigate | Add the final docs set to the existing ExDoc pipeline and verify it builds, preventing undocumented or unreachable contract docs. |
</threat_model>

<verification>
Run the new contract tests and rebuild docs. Confirm that the admin contract proof stops at the router seam and that every catalog listed in `guides/api_stability.md` has a corresponding automated check.
</verification>

<success_criteria>
Phase 8 has a locked, test-backed public contract, the docs nav exposes it, and the admin package remains intentionally narrow at the boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-04-SUMMARY.md`
</output>
