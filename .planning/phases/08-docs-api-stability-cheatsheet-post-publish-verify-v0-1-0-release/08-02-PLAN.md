---
phase: 08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/flows/evaluation.md
  - guides/flows/rulesets.md
  - guides/flows/rollout.md
  - guides/flows/admin-ui.md
  - guides/flows/explainability.md
  - guides/flows/multi-env.md
  - guides/cheatsheet.cheatmd
autonomous: true
requirements:
  - DOC-05
must_haves:
  truths:
    - "Runtime and operator guides explain the shipped evaluation, ruleset, rollout, explainability, admin UI, and multi-environment flows without future-phase detours."
    - "Operator/admin docs keep their host-facing contract aligned with the mounted `rulestead_admin` package instead of promising internal LiveView APIs."
    - "The cheatsheet is a one-page quick reference that covers the common public API and release-supported recipes."
  artifacts:
    - path: "guides/flows/evaluation.md"
      provides: "Runtime evaluation guide for the root public API"
    - path: "guides/flows/admin-ui.md"
      provides: "Operator/admin guide aligned to the mounted package contract"
    - path: "guides/flows/explainability.md"
      provides: "Support/operator explanation workflow for simulation and trace reading"
    - path: "guides/cheatsheet.cheatmd"
      provides: "DOC-05 one-page quick reference"
  key_links:
    - from: "guides/flows/evaluation.md"
      to: "README.md"
      via: "quickstart hands into the deeper evaluation path"
      pattern: "enabled\\?|get_value|get_variant|explain"
    - from: "guides/flows/admin-ui.md"
      to: "rulestead_admin/README.md"
      via: "mount seam, `policy:` requirement, and env query convention stay aligned"
      pattern: "policy|env|rulestead_admin"
    - from: "guides/cheatsheet.cheatmd"
      to: "guides/api_stability.md"
      via: "quick reference stays inside the locked public surface"
      pattern: "Rulestead\\.|RulesteadAdmin"
---

<objective>
Finish the high-signal flow docs and the one-page cheatsheet for v0.1.0.

Purpose: These are the narrative guides actual adopters will use after the quickstart. They need to be calm, task-oriented, and constrained to the supported package boundary so the release docs read as trustworthy, not aspirational.
Output: Six authored flow guides plus `guides/cheatsheet.cheatmd`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-CONTEXT.md
@.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-PATTERNS.md
@guides/flows/telemetry.md
@rulestead_admin/README.md
@prompts/rulestead-personas-jtbd-and-onboarding.md
@prompts/rulestead-release-engineering-and-ci.md

<interfaces>
Public root APIs already present in `rulestead/lib/rulestead.ex`:
```elixir
def evaluate(flag_payload, context, opts \\ [])
def enabled?(flag_payload, context)
def get_value(flag_payload, context, default)
def get_variant(flag_payload, context)
def explain(flag_payload, context)
def explain_flag(flag_key, environment_key, context, opts \\ [])
```

Admin host seam already present in `rulestead_admin/lib/rulestead_admin/router.ex`:
```elixir
defmacro rulestead_admin(path, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the runtime and operator flow guides around shipped behavior</name>
  <files>guides/flows/evaluation.md, guides/flows/rulesets.md, guides/flows/rollout.md, guides/flows/admin-ui.md, guides/flows/explainability.md, guides/flows/multi-env.md</files>
  <action>Write the six Phase 8 flow guides against the real shipped surfaces. Keep runtime guidance rooted in the current public `Rulestead` APIs and ordered-rules model. Keep operator guidance aligned with D-14 and the mounted-package posture: `admin-ui.md`, `rollout.md`, `explainability.md`, and `multi-env.md` should describe how host apps mount and operate the admin package, but they must not stabilize `RulesteadAdmin.Live.*`, DOM/CSS structure, or internal assign names per D-02 and D-03. Reuse the public-API phrasing pattern from `guides/flows/telemetry.md`: concise promise first, explicit catalog or bounded workflow second.</action>
  <verify>
    <automated>cd rulestead && mix docs --warnings-as-errors</automated>
  </verify>
  <done>The shipped flow guides cover runtime usage and operator workflows in a way that matches the actual package boundary and does not promise hidden internals.</done>
</task>

<task type="auto">
  <name>Task 2: Add the release-ready one-page cheatsheet</name>
  <files>guides/cheatsheet.cheatmd</files>
  <action>Create `guides/cheatsheet.cheatmd` for DOC-05 as a terse, command-heavy quick reference. Cover the common public evaluation APIs, install/mount commands, test-helper entrypoints, and the operator path at a glance. Keep the cheatsheet inside the locked v0.1.0 public surface and align examples with the published linked-version packages. Do not let the cheatsheet become a second narrative guide or a dumping ground for internal admin module names.</action>
  <verify>
    <automated>cd rulestead && mix docs --warnings-as-errors</automated>
  </verify>
  <done>The repo has a concise cheatsheet that complements the root README and detailed guides without extending the public contract beyond what Phase 8 locks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flow guides -> operator/app-developer behavior | Readers will use these guides as normative instructions for runtime and admin workflows |
| cheatsheet -> public API expectations | Every snippet on the cheatsheet becomes a de facto stability promise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | T | flow guides | mitigate | Keep the guides anchored to shipped `Rulestead` root APIs and the mounted admin seam; exclude internal `RulesteadAdmin.Live.*` and DOM/CSS details per D-02 and D-03. |
| T-08-02-02 | I | explainability/admin guides | mitigate | Reuse redacted, bounded terminology from the telemetry and security work; do not instruct readers to surface raw traits or PII. |
| T-08-02-03 | R | cheatsheet | mitigate | Limit cheatsheet snippets to the locked public surface and examples the release verification plans will exercise from published packages. |
</threat_model>

<verification>
Build docs after authoring the six flow guides and cheatsheet, then scan the admin/operator docs to confirm they document the mount seam and URL/session conventions rather than internal LiveView implementation details.
</verification>

<success_criteria>
The runtime/operator guide set is complete and the cheatsheet exists as a fast reference, all within the intended v0.1.0 public boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-02-SUMMARY.md`
</output>
