---
phase: 08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release
plan: 05
type: execute
wave: 3
depends_on:
  - 08-04
files_modified:
  - rulestead/lib/mix/tasks/verify.workspace_clean.ex
  - rulestead/lib/mix/tasks/verify.release_publish.ex
  - rulestead/lib/mix/tasks/verify.release_parity.ex
  - rulestead/test/rulestead/mix/tasks/verify_workspace_clean_test.exs
  - rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs
  - rulestead/test/rulestead/mix/tasks/verify_release_parity_test.exs
  - rulestead/test/support/release_publish_fixture.ex
autonomous: true
requirements:
  - REL-03
must_haves:
  truths:
    - "Maintainers have the required verification trio available from the core package with deterministic success/failure semantics before the first real release."
    - "The published-release verifier is implemented against published-artifact consumer fixtures and cannot silently fall back to local path dependencies."
    - "Release parity detects drift between the git tag and Hex tarballs with stable exit semantics, even before the first live `v0.1.0` publish."
  artifacts:
    - path: "rulestead/lib/mix/tasks/verify.workspace_clean.ex"
      provides: "Workspace cleanliness verifier scoped to publishable/package-test surfaces"
    - path: "rulestead/lib/mix/tasks/verify.release_publish.ex"
      provides: "Published-Hex verifier implementation that can exercise `rulestead` and `rulestead_admin` consumer fixtures once artifacts exist"
    - path: "rulestead/lib/mix/tasks/verify.release_parity.ex"
      provides: "Git-tag vs Hex-tarball parity verifier with stable exit codes"
    - path: "rulestead/test/support/release_publish_fixture.ex"
      provides: "Shared fixture helpers that force published-version consumer setup for the later live release proof"
  key_links:
    - from: "rulestead/lib/mix/tasks/verify.release_publish.ex"
      to: "rulestead/test/support/release_publish_fixture.ex"
      via: "the verifier shares explicit published-version consumer setup with the later post-publish execution plan"
      pattern: "mix new|phx.new|Hex"
    - from: "rulestead/lib/mix/tasks/verify.release_parity.ex"
      to: "rulestead/test/rulestead/mix/tasks/verify_release_parity_test.exs"
      via: "pure compute step and exit-code contract stay testable"
      pattern: "compute|exit"
    - from: "rulestead/lib/mix/tasks/verify.workspace_clean.ex"
      to: "scripts/ci/check_package_whitelist.sh"
      via: "publishable-surface cleanliness stays aligned with package whitelist enforcement"
      pattern: "package\\.files|test"
---

<objective>
Implement the Phase 8 verification trio and the published-release harness logic that the final live release proof will execute after publish.

Purpose: Release is not complete when tags exist, but the trio must exist and be testable before publish. This plan builds the verification entrypoints and harnesses without falsely claiming real `v0.1.0` artifact proof before the publish choreography runs.
Output: `mix verify.workspace_clean`, `mix verify.release_publish`, `mix verify.release_parity`, focused unit tests, and shared published-release fixture helpers ready for the final post-publish execution plan.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-CONTEXT.md
@.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-PATTERNS.md
@.planning/phases/05-host-app-seams-plug-liveview-oban-installer-test-helpers/05-04-SUMMARY.md
@rulestead/lib/mix/tasks/rulestead.install.ex
@scripts/ci/check_package_whitelist.sh
@prompts/rulestead-release-engineering-and-ci.md

<interfaces>
Required Phase 8 verification contract from roadmap:
```text
mix verify.workspace_clean
mix verify.release_publish <version>
mix verify.release_parity <version>
```

Published-release proof must eventually cover:
- fresh `mix new` consumer proof for `rulestead`
- fresh `mix phx.new` consumer proof for `rulestead_admin`
- HexDocs versioned URL reachability
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the three Mix verification tasks with unit-level contract tests</name>
  <files>rulestead/lib/mix/tasks/verify.workspace_clean.ex, rulestead/lib/mix/tasks/verify.release_publish.ex, rulestead/lib/mix/tasks/verify.release_parity.ex, rulestead/test/rulestead/mix/tasks/verify_workspace_clean_test.exs, rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs, rulestead/test/rulestead/mix/tasks/verify_release_parity_test.exs</files>
  <behavior>
    - Test 1: `verify.workspace_clean` fails when publishable/package-test surfaces are dirty and has no hidden escape hatch.
    - Test 2: `verify.release_publish <version>` enforces published-version inputs, uses explicit consumer-fixture setup, verifies versioned HexDocs reachability, and is ready to prove both sibling packages from published artifacts per D-08 and D-09 once the release exists.
    - Test 3: `verify.release_parity <version>` exposes a pure `compute/2`-style diff step with exit codes `0` parity, `2` drift, `1` runtime failure.
  </behavior>
  <action>Implement the verification trio per REL-03 and the roadmap. `verify.workspace_clean` should scope cleanliness to `package.files ++ ["test"]` and not offer a bypass flag. `verify.release_publish` must reject local path-dependency fallbacks, use published Hex package inputs only, and encode the full D-08/D-09 consumer-proof workflow so Wave 5 can run it against the actual `v0.1.0` release. `verify.release_parity` should diff tag contents against the Hex tarball with a pure compute step for testability and stable exit semantics. Keep the implementation scripts-first where helpful, but the public entrypoints must be these Mix tasks.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/mix/tasks/verify_workspace_clean_test.exs test/rulestead/mix/tasks/verify_release_publish_test.exs test/rulestead/mix/tasks/verify_release_parity_test.exs</automated>
  </verify>
  <done>The verification trio exists behind stable Mix entrypoints and has focused tests pinning the behavior and exit-code contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shared published-release fixture helpers instead of live artifact execution</name>
  <files>rulestead/test/support/release_publish_fixture.ex</files>
  <behavior>
    - Test 1: the shared fixture helpers generate fresh `mix new` and `mix phx.new` consumer apps configured with explicit package versions, never path dependencies.
    - Test 2: the helpers encode the documented admin mount/session/query contract needed for the later live `rulestead_admin` proof.
    - Test 3: the helpers are callable from `verify.release_publish` so the final release execution path uses the same setup logic tested here.
  </behavior>
  <action>Add shared fixture helpers for the published-release consumer setup using the Phase 5 fresh-app patterns, but swap path deps for explicit versioned Hex deps and force failures if a local sibling path is attempted. Encode both the `mix new` core-consumer path and the `mix phx.new` admin mount/boot path from D-08 through D-10 so `verify.release_publish` and the final post-publish execution plan can reuse the exact same harness. Do not claim live published-artifact success in this plan; that belongs in the later real-release verification plan.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/mix/tasks/verify_release_publish_test.exs</automated>
  </verify>
  <done>The repo has shared published-release fixture helpers that enforce versioned Hex-consumer setup and are ready for the real post-publish smoke execution after publish.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git tag/worktree -> published Hex artifacts | Release verification must detect drift between what was tagged and what Hex serves |
| published packages -> fresh consumer apps | External consumers rely on Hex artifacts, not local path dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | T | `verify.release_parity` | mitigate | Diff the git tag against the Hex tarball with stable exit codes so drift is detectable and automatable. |
| T-08-05-02 | R | `verify.release_publish` | mitigate | Prove both sibling packages from published artifacts and check versioned HexDocs reachability instead of trusting local builds. |
| T-08-05-03 | D | `verify.workspace_clean` | mitigate | Fail fast on dirty publishable surfaces with no bypass flag so release verification cannot proceed from an unclean workspace. |
</threat_model>

<verification>
Run the verification-task test suite and confirm the published-release harness rejects local path fallbacks. Parity drift should return exit code `2` instead of a generic crash, and the live release proof should still be deferred to the final post-publish plan.
</verification>

<success_criteria>
Phase 8 has the required verification trio implemented and test-backed, with the live published-artifact execution intentionally deferred to the final post-publish verification plan after publish.
</success_criteria>

<output>
After completion, create `.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-05-SUMMARY.md`
</output>
