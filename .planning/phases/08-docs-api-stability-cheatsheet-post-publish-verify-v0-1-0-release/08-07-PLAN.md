---
phase: 08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release
plan: 07
type: execute
wave: 5
depends_on:
  - 08-06
files_modified:
  - .github/workflows/verify-published-release.yml
  - scripts/ci/verify_published_release.sh
  - MAINTAINING.md
autonomous: false
requirements:
  - REL-03
  - REL-04
must_haves:
  truths:
    - "The real `v0.1.0` release is verified only after both sibling packages are live on Hex."
    - "The required post-publish trio runs against the actual published release: `mix verify.workspace_clean`, `mix verify.release_publish <version>`, and `mix verify.release_parity <version>`."
    - "Daily drift automation re-runs published-release verification and opens or updates one rolling GitHub issue only when drift is real."
    - "Maintainers capture durable evidence that the published `rulestead` and `rulestead_admin` artifacts match the documented install and mount paths."
  artifacts:
    - path: "scripts/ci/verify_published_release.sh"
      provides: "Locally runnable post-publish entrypoint that executes the verification trio against a supplied released version"
    - path: ".github/workflows/verify-published-release.yml"
      provides: "Daily/manual drift workflow that reuses the same post-publish verification entrypoint and rolling issue mode"
    - path: "MAINTAINING.md"
      provides: "Maintainer runbook section covering live post-publish verification evidence capture and daily drift expectations"
  key_links:
    - from: "scripts/ci/verify_published_release.sh"
      to: "mix verify.release_publish"
      via: "the local post-publish entrypoint runs the published-artifact consumer proof after Hex serves both packages"
      pattern: "verify\\.workspace_clean|verify\\.release_publish|verify\\.release_parity"
    - from: ".github/workflows/verify-published-release.yml"
      to: "scripts/ci/verify_published_release.sh"
      via: "daily drift cron and manual dispatch reuse the same local verification logic"
      pattern: "schedule|workflow_dispatch|update_existing"
    - from: "MAINTAINING.md"
      to: ".github/workflows/publish-hex.yml"
      via: "the runbook tells maintainers exactly when to transition from publish completion into the real post-publish trio"
      pattern: "post-publish|evidence|Hex"
---

<objective>
Run and operationalize the real post-publish verification wave for `v0.1.0`, then keep that proof alive with daily drift monitoring.

Purpose: REL-03 is only fully satisfied once the trio runs against the actual published sibling packages. This final wave executes the real release proof after publish and wires the same logic into the recurring drift monitor.
Output: A local post-publish verification entrypoint, a daily/manual drift workflow, updated maintainer instructions, and a blocking checkpoint for live release evidence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-CONTEXT.md
@.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-05-PLAN.md
@.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-06-PLAN.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-VERIFICATION.md
@.github/workflows/publish-hex.yml
@MAINTAINING.md

<interfaces>
Required live verification trio:
```text
mix verify.workspace_clean
mix verify.release_publish <version>
mix verify.release_parity <version>
```

Blocking live release bar from D-08 and D-09:
- confirm both `rulestead` and `rulestead_admin` are live on Hex for `v0.1.0`
- prove fresh `mix new` install for `rulestead`
- prove fresh `mix phx.new` mount/boot for `rulestead_admin`
- capture versioned HexDocs reachability and parity evidence
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the real post-publish entrypoint and recurring drift workflow</name>
  <files>.github/workflows/verify-published-release.yml, scripts/ci/verify_published_release.sh, MAINTAINING.md</files>
  <behavior>
    - Test 1: the local entrypoint runs `mix verify.workspace_clean`, `mix verify.release_publish <version>`, and `mix verify.release_parity <version>` in order against a supplied released version.
    - Test 2: the daily workflow reuses that same entrypoint, resolves the latest released version, and updates one existing open issue instead of creating a new issue every day.
    - Test 3: maintainer docs explain what evidence to capture from the live run and how to interpret verification or drift failures.
  </behavior>
  <action>Create `scripts/ci/verify_published_release.sh` as the locally runnable post-publish entrypoint for REL-03 and REL-04, and wire `.github/workflows/verify-published-release.yml` to run it daily and on manual dispatch. The script must execute the trio in order against a supplied release version, and the workflow must reuse the same logic while resolving the latest published version for drift monitoring. Use the rolling-issue pattern from the Phase 8 release-engineering guidance: one issue, `update_existing: true`, `search_existing: open`, labels `area:release` and `severity:drift`, and only open/update on actual verification failure or parity drift. Update `MAINTAINING.md` so the handoff from publish completion to live verification is explicit and evidence-oriented.</action>
  <verify>
    <automated>bash scripts/ci/verify_published_release.sh 0.1.0</automated>
  </verify>
  <done>The repo has one canonical post-publish verification entrypoint, the daily drift workflow reuses it, and the maintainer runbook explains live verification evidence and failure handling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the live `v0.1.0` post-publish verification and capture evidence</name>
  <files>.github/workflows/verify-published-release.yml, scripts/ci/verify_published_release.sh, MAINTAINING.md, .planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-07-SUMMARY.md</files>
  <what-built>The publish choreography from 08-06 and the verification trio/harness from 08-05 are now wired into a real post-publish command path that targets live Hex artifacts.</what-built>
  <action>After the gated publish flow completes and both sibling packages are visible on Hex, run the real `v0.1.0` post-publish verification trio and capture the resulting evidence bundle in the phase summary. Treat failures here as release blockers, not informational drift, because this task is the first real execution of REL-03 against live artifacts.</action>
  <how-to-verify>
    1. Complete the gated publish flow from `08-06` so both `rulestead` and `rulestead_admin` `v0.1.0` are visible on Hex.
    2. From the repo root, run `bash scripts/ci/verify_published_release.sh 0.1.0`.
    3. Confirm the run executes `mix verify.workspace_clean`, `mix verify.release_publish 0.1.0`, and `mix verify.release_parity 0.1.0` successfully.
    4. Confirm the evidence shows both sibling packages are live on Hex, the fresh `mix new` core-consumer proof passed, the fresh `mix phx.new` admin mount/boot proof passed, and versioned HexDocs is reachable.
    5. Record the command output and live package URLs in the phase summary as the REL-03/REL-04 evidence bundle.
  </how-to-verify>
  <verify>
    <automated>MISSING — execute after publish by running `bash scripts/ci/verify_published_release.sh 0.1.0` and attach the output to `08-07-SUMMARY.md`</automated>
  </verify>
  <done>The live `0.1.0` run has durable evidence showing all three verification commands succeeded against the published sibling packages, or the summary records the blocking failure with enough detail to recover.</done>
  <resume-signal>Type "approved" once the live `0.1.0` trio succeeds, or reply with the failing step and output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Hex release -> verification tooling | The final release proof must operate against real published artifacts, not local builds |
| drift cron -> GitHub issues | Automated issue creation must stay bounded to real failures and real drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | R | live post-publish evidence | mitigate | Run the trio only after both packages are live on Hex and capture durable output/URLs in the phase summary. |
| T-08-07-02 | T | `scripts/ci/verify_published_release.sh` | mitigate | Force the script to use released version inputs and the published-artifact verifier from 08-05 so local path fallbacks cannot satisfy the release bar. |
| T-08-07-03 | D | daily drift automation | mitigate | Use one rolling issue with `update_existing: true` and open/update only on real verification failure or parity drift. |
</threat_model>

<verification>
Run the local post-publish entrypoint against `0.1.0` after publish, then confirm the daily workflow reuses the same script and rolling-issue mode rather than a separate codepath.
</verification>

<success_criteria>
Phase 8 fully satisfies REL-03 and REL-04 only after the live `0.1.0` post-publish trio succeeds against both published sibling packages and the recurring drift monitor is in place.
</success_criteria>

<output>
After completion, create `.planning/phases/08-docs-api-stability-cheatsheet-post-publish-verify-v0-1-0-release/08-07-SUMMARY.md`
</output>
