---
phase: 13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead_admin/test/rulestead_admin/live/flag_live/simulate_test.exs
  - rulestead_admin/test/rulestead_admin/live/flag_live/simulate_accessibility_test.exs
  - rulestead_admin/test/rulestead_admin/live/flag_live/show_test.exs
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "Simulation test helpers create and publish rulesets using actor metadata."
  artifacts:
    - path: "rulestead_admin/test/rulestead_admin/live/flag_live/simulate_test.exs"
      provides: "Updated test helpers passing actor metadata"
  key_links: []
---

<objective>
Close the remaining Phase 7 simulation helper verification gap from the real `rulestead_admin` entrypoint by ensuring rulesets are seeded with actor metadata.

Purpose: To ensure the test helpers reflect real-world requirements for actor presence in ruleset creation and publication, closing OPS-01.
Output: Updated test files in `rulestead_admin` that successfully inject `@admin_actor`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-operational-carryover-closure-and-milestone-verification/13-PATTERNS.md
@.planning/phases/13-operational-carryover-closure-and-milestone-verification/13-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Update Test Helpers with Actor Metadata</name>
  <files>
    rulestead_admin/test/rulestead_admin/live/flag_live/simulate_test.exs,
    rulestead_admin/test/rulestead_admin/live/flag_live/simulate_accessibility_test.exs,
    rulestead_admin/test/rulestead_admin/live/flag_live/show_test.exs
  </files>
  <action>
    Find where `Command.SaveDraftRuleset.new` and `Command.PublishRuleset.new` are called in the test helpers (often within a `publish_ruleset!` private function or setup block). Update the calls to pass the `actor: @admin_actor` keyword list, matching the pattern identified in `13-PATTERNS.md`. Ensure `@admin_actor` is defined in the module attributes if it isn't already.
  </action>
  <verify>
    <automated>cd rulestead_admin && MIX_ENV=test mix test test/rulestead_admin/live/flag_live/</automated>
  </verify>
  <done>The tests pass and the gaps where actor metadata was missing from ruleset operations are closed.</done>
</task>
</tasks>

<success_criteria>
- The `mix test` suite in `rulestead_admin/test/rulestead_admin/live/flag_live/` passes.
- No `SaveDraftRuleset` or `PublishRuleset` commands in these tests omit the `actor:` metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/13-operational-carryover-closure-and-milestone-verification/13-01-SUMMARY.md`
</output>
