---
phase: 44-openfeature-bridge-proof-final-support-audit
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/ci/test.sh
  - .github/workflows/ci.yml
  - MAINTAINING.md
autonomous: true
requirements:
  - OFE-01
  - VER-01
must_haves:
  truths:
    - "The repo exposes one named OpenFeature companion proof surface that maintainers and CI can cite verbatim."
    - "That proof surface stays bounded to the package contract and does not redefine the repo as a three-package release machine."
    - "CI visibility improves without making the OpenFeature companion an equal always-on blocker for unrelated work."
  artifacts:
    - path: "scripts/ci/test.sh"
      provides: "Named OpenFeature companion test scope for local and CI reruns"
      min_lines: 45
    - path: ".github/workflows/ci.yml"
      provides: "Visible CI lane or path-gated job for the named OpenFeature companion proof"
      min_lines: 120
    - path: "MAINTAINING.md"
      provides: "Maintainer guidance tying the same proof command to Phase 44 support truth"
      min_lines: 180
  key_links:
    - from: "scripts/ci/test.sh"
      to: ".github/workflows/ci.yml"
      via: "CI should invoke the same named proof scope maintainers run locally"
      pattern: "openfeature_companion|TEST_SCOPE"
    - from: "MAINTAINING.md"
      to: "scripts/ci/test.sh"
      via: "maintainer truth should cite the exact companion proof command"
      pattern: "openfeature_companion|OpenFeature|proof"
---

<objective>
Expose the OpenFeature companion proof path as a first-class named repo verification surface without widening the release model.

Purpose: Phase 44 needs more than package tests on disk; it needs a command and CI surface the repo can point to by name. The proof bar must be visible and rerunnable, but it must remain companion-scoped and compatible with the two-package release design.
Output: a named `openfeature_companion` proof scope in scripts plus matching CI and maintainer wiring.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-openfeature-bridge-proof-final-support-audit/44-CONTEXT.md
@.planning/phases/44-openfeature-bridge-proof-final-support-audit/44-RESEARCH.md
@scripts/ci/test.sh
@.github/workflows/ci.yml
@MAINTAINING.md
@open_feature_rulestead/test/open_feature_rulestead/provider_test.exs
@open_feature_rulestead/test/open_feature_rulestead/context_mapper_test.exs
@prompts/rulestead-release-engineering-and-ci.md
@prompts/rulestead-testing-and-e2e-strategy.md
@prompts/rulestead-engineering-dna-from-prior-libs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a named OpenFeature companion scope to the repo test script</name>
  <read_first>
    - scripts/ci/test.sh
    - open_feature_rulestead/test/open_feature_rulestead/provider_test.exs
    - open_feature_rulestead/test/open_feature_rulestead/context_mapper_test.exs
    - .planning/phases/44-openfeature-bridge-proof-final-support-audit/44-CONTEXT.md
  </read_first>
  <files>scripts/ci/test.sh</files>
  <behavior>
    - Test 1: the repo has one explicit named scope for the OpenFeature companion proof.
    - Test 2: the scope stays bounded to the package-local provider/context proof and does not absorb demo/browser verification.
    - Test 3: the scope is rerunnable locally with one environment variable or argument pattern consistent with existing CI scripts.
  </behavior>
  <action>Extend `scripts/ci/test.sh` with a companion-named scope such as `openfeature_companion`. Keep the implementation small and scripts-first: install package deps in `open_feature_rulestead`, run only the package-local proof files, and print a readable lane description. Do not widen the default `all` scope and do not add browser/demo steps here. The scope should be suitable for both local reruns and CI job wiring.</action>
  <acceptance_criteria>
    - `rg -n "openfeature_companion|OpenFeature companion" /Users/jon/projects/rulestead/scripts/ci/test.sh` finds the named scope.
    - `RULESTEAD_TEST_SCOPE=openfeature_companion bash /Users/jon/projects/rulestead/scripts/ci/test.sh` passes.
  </acceptance_criteria>
  <verify>
    <automated>RULESTEAD_TEST_SCOPE=openfeature_companion bash /Users/jon/projects/rulestead/scripts/ci/test.sh</automated>
  </verify>
  <done>The repo now has one explicit named command for the OpenFeature companion proof surface.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the named proof scope in CI and maintainer guidance without changing the release model</name>
  <read_first>
    - .github/workflows/ci.yml
    - MAINTAINING.md
    - scripts/ci/test.sh
    - .planning/phases/44-openfeature-bridge-proof-final-support-audit/44-RESEARCH.md
    - prompts/rulestead-release-engineering-and-ci.md
  </read_first>
  <files>.github/workflows/ci.yml, MAINTAINING.md</files>
  <behavior>
    - Test 1: CI exposes the OpenFeature proof by name or job identity.
    - Test 2: the CI shape keeps the proof bounded and does not imply `open_feature_rulestead` is now part of the default two-package publish choreography.
    - Test 3: maintainer docs cite the exact same proof command and explain when it is the right bar.
  </behavior>
  <action>Update `.github/workflows/ci.yml` so the OpenFeature companion proof is visible in CI, ideally through a path-gated or otherwise bounded job that runs the new named scope without redefining the default core release posture. Then update `MAINTAINING.md` so maintainers know which command closes the Phase 44 OpenFeature truth surface, how it relates to the mounted-admin and release-publish bars, and why it is intentionally companion-scoped. Do not thread this scope into linked-package publish steps or imply a third primary release package.</action>
  <acceptance_criteria>
    - `rg -n "openfeature|openfeature_companion|TEST_SCOPE" /Users/jon/projects/rulestead/.github/workflows/ci.yml /Users/jon/projects/rulestead/MAINTAINING.md` finds the named lane/guidance.
    - `! rg -n "publish-openfeature|open_feature_rulestead-v|third package" /Users/jon/projects/rulestead/.github/workflows/ci.yml /Users/jon/projects/rulestead/MAINTAINING.md` succeeds unless the text explicitly rejects that posture.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "openfeature|openfeature_companion|TEST_SCOPE" /Users/jon/projects/rulestead/.github/workflows/ci.yml /Users/jon/projects/rulestead/MAINTAINING.md</automated>
  </verify>
  <done>The OpenFeature companion proof is visible in CI and maintainer docs while staying outside the core release-model boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo script names -> support truth | If there is no stable command name, docs and maintainers will drift quickly |
| CI visibility -> release expectations | A new CI lane can accidentally imply a third primary package or an always-on blocker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04 | R | `scripts/ci/test.sh` | mitigate | Add one explicit named companion scope so proof is reproducible and citeable. |
| T-44-05 | I | `.github/workflows/ci.yml`, `MAINTAINING.md` | mitigate | Keep CI and maintainer wording clear that this is a companion proof bar, not a release-model expansion. |
| T-44-06 | T | workflow wiring | mitigate | Path-gate or otherwise bound the job so unrelated PRs do not inherit a misleading always-on OpenFeature contract. |
</threat_model>

<verification>
- `RULESTEAD_TEST_SCOPE=openfeature_companion bash /Users/jon/projects/rulestead/scripts/ci/test.sh`
- `rg -n "openfeature|openfeature_companion|TEST_SCOPE" /Users/jon/projects/rulestead/.github/workflows/ci.yml /Users/jon/projects/rulestead/MAINTAINING.md`
</verification>

<success_criteria>
- The repo exposes a stable named OpenFeature companion proof command.
- CI and maintainer docs point at the same command.
- The new proof surface is visible without changing the linked two-package release design.
</success_criteria>

<output>
After completion, create `.planning/phases/44-openfeature-bridge-proof-final-support-audit/44-02-SUMMARY.md`
</output>
