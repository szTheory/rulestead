---
phase: 77-evaluation-and-lifecycle-doc-alignment
plan: 77-01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/flows/evaluation.md
  - guides/introduction/getting-started.md
  - guides/introduction/installation.md
  - rulestead/README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
must_haves:
  truths:
    - evaluation.md documents Rulestead.Runtime keyed lookup with examples; payload-first remains canonical
    - getting-started and installation include lifecycle-required-fields callout linking flag-lifecycle
    - rulestead/README.md API ordering matches footguns (Runtime keyed lookup before payload-first root APIs)
  artifacts:
    - guides/flows/evaluation.md
    - guides/introduction/getting-started.md
    - guides/introduction/installation.md
    - rulestead/README.md
---

# Plan 77-01: Evaluation And Lifecycle Doc Alignment

<objective>
Ship DOC-01–DOC-03: expand evaluation.md Runtime APIs, add intro lifecycle callouts, reorder rulestead/README.md entrypoints. Docs only — no lib/, release_contract_test, or verify task changes.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-77-01: Docs imply `Rulestead.enabled?("key", conn)` | high | Examples use `(flag_payload, context)` OR `Runtime.enabled?(env, key, context)` only |
| T-77-02: Runtime overshadows payload-first contract | medium | Keep Core Calls + opening on `Rulestead.evaluate/3`; Runtime is subsection |
| T-77-03: Wrong lifecycle field names | medium | Use `owner_ref` + `expected_expiration` per spine and store |
| T-77-04: Scope creep into Phase 78 guards | low | No edits to `release_contract_test.exs` or `mix verify.*` |
</threat_model>

<tasks>

<task id="77-01-01" type="execute">
  <name>Expand evaluation.md Runtime keyed lookup (DOC-01)</name>
  <read_first>
    - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-CONTEXT.md (D-01–D-04)
    - guides/flows/evaluation.md (section "Pure Evaluation Versus Runtime Lookup")
    - guides/recipes/footguns.md (payload-first vs keyed runtime table)
    - rulestead/lib/rulestead/runtime.ex (@spec lines for Runtime functions)
    - guides/introduction/phoenix-integration-spine.md (§5 First runtime evaluation)
  </read_first>
  <action>
In `guides/flows/evaluation.md`:

1. Leave **Core Calls** and opening paragraphs unchanged (payload-first canonical).

2. Replace/expand **"Pure Evaluation Versus Runtime Lookup"** with:
   - Short "when to use payload-first" list (keep existing bullets).
   - New subsection **"Runtime keyed lookup (`Rulestead.Runtime`)"** documenting:
     - `Rulestead.Runtime.evaluate/3`, `enabled?/3`, `get_value/4`, `get_variant/3`, `explain/3`
     - Signature pattern: `(environment_key, flag_key, context)` where `context` is `%Rulestead.Context{}` or normalizable map/keyword.
   - One complete example:

```elixir
context =
  Rulestead.Context.new(
    environment: "production",
    targeting_key: "user-123",
    attributes: %{plan: :pro}
  )

{:ok, enabled?} =
  Rulestead.Runtime.enabled?("production", "checkout_v2", context)
```

   - One sentence: projection helpers on root `Rulestead` take **flag payload + context** — link [Footguns](../recipes/footguns.md).
   - Link [Phoenix Integration Spine](../introduction/phoenix-integration-spine.md) for Plug → assigns → Runtime path.

3. Do not remove or demote existing payload-first examples elsewhere in the file.
  </action>
  <verify>
    <automated>grep -q 'Rulestead.Runtime.enabled?' guides/flows/evaluation.md && grep -q 'Rulestead.Runtime.evaluate/3' guides/flows/evaluation.md && grep -q 'Rulestead.evaluate/3' guides/flows/evaluation.md</automated>
  </verify>
  <acceptance_criteria>
    - evaluation.md contains `Rulestead.Runtime.enabled?` in a fenced example
    - evaluation.md lists at least three Runtime function names with `/3` or `/4` arity notation
    - File still contains `Rulestead.evaluate/3` in Core Calls section
    - Contains link to footguns.md or phoenix-integration-spine.md
  </acceptance_criteria>
</task>

<task id="77-01-02" type="execute">
  <name>Intro lifecycle-required-fields callouts (DOC-02)</name>
  <read_first>
    - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-CONTEXT.md (D-05–D-07)
    - guides/introduction/getting-started.md
    - guides/introduction/installation.md
    - guides/flows/flag-lifecycle.md (What To Record Up Front / birth)
    - guides/introduction/phoenix-integration-spine.md (§6 lifecycle)
  </read_first>
  <action>
**getting-started.md:** After §2 (Install and migrate) or before §4 (admin UI), add a callout block:

> **Lifecycle required at flag create:** Every new flag must include **`owner_ref`** (host-owned team/service reference) and **`expected_expiration`** (review date). Rulestead does not maintain a team directory. See [Flag Lifecycle](../flows/flag-lifecycle.md) and the [Phoenix Integration Spine](phoenix-integration-spine.md#6-create-your-first-flag-lifecycle-required) for the first-hour create path.

**installation.md:** In **What happens next**, after the Phoenix spine bullet, add a parallel callout with the same two field names and link to `../flows/flag-lifecycle.md`.

Keep edits minimal — do not rewrite install steps.
  </action>
  <verify>
    <automated>grep -q owner_ref guides/introduction/getting-started.md && grep -q expected_expiration guides/introduction/getting-started.md guides/introduction/installation.md && grep -q flag-lifecycle guides/introduction/getting-started.md guides/introduction/installation.md</automated>
  </verify>
  <acceptance_criteria>
    - getting-started.md contains both `owner_ref` and `expected_expiration`
    - installation.md contains both field names
    - Both files link to flag-lifecycle.md
  </acceptance_criteria>
</task>

<task id="77-01-03" type="execute">
  <name>Reorder rulestead/README.md API entrypoints (DOC-03)</name>
  <read_first>
    - rulestead/README.md (## Runtime entrypoints)
    - guides/recipes/footguns.md (table)
    - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-CONTEXT.md (D-08–D-10)
  </read_first>
  <action>
Replace `## Runtime entrypoints` in `rulestead/README.md` with two subsections:

**### Keyed snapshot lookup (`Rulestead.Runtime`)** — Phoenix apps with local snapshot cache:
- `Rulestead.Runtime.enabled?/3` — `(environment_key, flag_key, context)`
- `Rulestead.Runtime.get_variant/3`
- `Rulestead.Runtime.evaluate/3`
- `Rulestead.Runtime.get_value/4`
- `Rulestead.Runtime.explain/3`

One line: see [evaluation.md](../guides/flows/evaluation.md) and [Phoenix Integration Spine](../guides/introduction/phoenix-integration-spine.md).

**### Payload-first evaluation (`Rulestead`)** — tests, simulations, authored payload in hand:
- `Rulestead.evaluate/3`
- `Rulestead.enabled?/2`
- `Rulestead.get_value/3`
- `Rulestead.get_variant/2`
- `Rulestead.explain/2`

Do not alter sections below (guarded rollout, targeting, etc.).
  </action>
  <verify>
    <automated>grep -q 'Rulestead.Runtime.enabled?/3' rulestead/README.md && grep -q 'Payload-first' rulestead/README.md && grep -q 'Rulestead.evaluate/3' rulestead/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README lists `Rulestead.Runtime.enabled?/3` before root `Rulestead.evaluate/3` in document order
    - README contains substring `Payload-first` or `payload-first`
    - Milestone contract sections after entrypoints remain present (grep `verify.phase` still matches)
  </acceptance_criteria>
</task>

</tasks>

<verification>
## Plan-level proof

```bash
grep -q 'Rulestead.Runtime.enabled?' guides/flows/evaluation.md
grep -q owner_ref guides/introduction/getting-started.md
grep -q expected_expiration guides/introduction/installation.md
grep -q 'Rulestead.Runtime' rulestead/README.md
! grep -q 'traits: %{' guides/flows/evaluation.md guides/introduction/getting-started.md
```
</verification>
