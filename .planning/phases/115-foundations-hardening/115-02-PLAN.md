---
phase: "115-foundations-hardening"
plan: 02
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - "rulestead_admin/priv/static/css/rulestead_admin.css"
autonomous: true
requirements: [FND-01, FND-03, FND-04, FND-05]
requirements_addressed: [FND-01, FND-03, FND-04, FND-05]
decision_coverage:
  - "D-02"
  - "D-03"
  - "D-04"
  - "D-05"
  - "D-06"
  - "D-08"
  - "D-09"
  - "D-10"
  - "D-15"
user_setup: []
must_haves:
  truths:
    - "D-02: CSS edits stay inside existing mounted-admin foundation behavior and do not alter primitive/composite scope unless foundation verification is blocked."
    - "D-03: CSS breakpoint edits either use the canonical set or remain listed in the foundation exception ledger."
    - "D-04: Responsive behavior preserves desktop-oriented operator workflows while preventing mobile/narrow overflow."
    - "D-05: CSS hardening does not redesign token values, palette, typography families, or logo treatment."
    - "D-06: Radius, pill, elevation, and emphasis usage follows the foundation contract rather than component-by-component drift."
    - "D-08: Focus uses the unified `--rs-focus-ring` by default across shell controls, buttons, links, forms, command palette, env/tenant controls, subnav, task links, and route widgets."
    - "D-09: Known focus exceptions have explicit visible alternatives or source markers."
    - "D-10: Reduced-motion users do not receive nonessential scale, translate, blur, or staged animation effects."
    - "D-15: CSS choices follow recommendation-first defaults within the locked foundation scope."
---

<objective>
Apply targeted admin CSS foundation hardening.

Purpose: Satisfy FND-03, FND-04, and FND-05 while preserving the existing visual system and contract from Plan 01.
Output: Narrow changes in `rulestead_admin/priv/static/css/rulestead_admin.css`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/115-foundations-hardening/115-CONTEXT.md
@.planning/phases/115-foundations-hardening/115-FOUNDATIONS-CONTRACT.md
@.planning/phases/115-foundations-hardening/115-UI-SPEC.md
@.planning/phases/115-foundations-hardening/115-PATTERNS.md
@rulestead_admin/priv/static/css/rulestead_admin.css
@rulestead_admin/lib/rulestead_admin/components/shell.ex
@examples/demo/backend/lib/rulestead_demo_web/live/ui_matrix_live.ex
@examples/demo/frontend/tests/ui-matrix.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a reduced-motion floor for nonessential transforms</name>
  <files>rulestead_admin/priv/static/css/rulestead_admin.css</files>
  <read_first>
    @rulestead_admin/priv/static/css/rulestead_admin.css
    @.planning/phases/115-foundations-hardening/115-FOUNDATIONS-CONTRACT.md
    @brandbook/brand-book.md
  </read_first>
  <action>Add a `@media (prefers-reduced-motion: reduce)` block near the existing motion section in `rulestead_admin.css`. Inside it, set `.rs-shell *, .rs-shell *::before, .rs-shell *::after` transition duration and animation duration to `0ms`, animation iteration count to `1`, and scroll behavior to `auto`. Add targeted transform neutralization for active/hover selectors that can scale or translate outside the no-preference animation block: `.rs-shell button:active:not(:disabled)`, `.rs-shell .rs-button:active:not(:disabled)`, `a.rs-shell__brand:active`, `a.rs-shell__env-link:active`, `a.rs-env-state:active`, `.rs-attention__card:hover`, `.rs-task-link:hover`, `.rs-task-link:active`, `.rs-badge:hover`, `.rs-signal:hover`, and `.rs-cmdk:not([hidden]) .rs-cmdk__panel`. Use `transform: none` and avoid changing colors or layout. Keep the existing `prefers-reduced-motion: no-preference` animation block intact.</action>
  <verify>
    <automated>python3 scripts/check_admin_foundations.py</automated>
  </verify>
  <acceptance_criteria>
    - `rulestead_admin.css` contains `@media (prefers-reduced-motion: reduce)`.
    - The reduced-motion block contains `transition-duration: 0ms`.
    - The reduced-motion block contains `animation-duration: 0ms`.
    - The reduced-motion block contains `transform: none`.
    - `python3 scripts/check_admin_foundations.py` exits 0.
  </acceptance_criteria>
  <done>Reduced-motion CSS floor neutralizes nonessential transform/animation behavior while preserving state visibility.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Normalize or document foundation-sensitive CSS selectors</name>
  <files>rulestead_admin/priv/static/css/rulestead_admin.css</files>
  <read_first>
    @rulestead_admin/priv/static/css/rulestead_admin.css
    @.planning/phases/115-foundations-hardening/115-FOUNDATIONS-CONTRACT.md
    @brandbook/tokens.css
  </read_first>
  <action>Review current noncanonical media thresholds in `rulestead_admin.css` against `115-FOUNDATIONS-CONTRACT.md`. Migrate only selector groups that are exact or safe canonical equivalents after reading the surrounding CSS. Leave content-specific thresholds in place if the contract records them. Confirm CSS still uses `--rs-radius-sm`, `--rs-radius-md`, `--rs-radius-lg`, `--rs-radius-full`, `--rs-shadow-sm`, `--rs-shadow`, and `--rs-shadow-panel` for foundation surfaces instead of new hardcoded shape/elevation values. Do not touch palette variables, logo variables, component modules, routes, static fixtures, package metadata, or release files.</action>
  <verify>
    <automated>python3 scripts/check_admin_foundations.py && git diff --check -- rulestead_admin/priv/static/css/rulestead_admin.css</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/check_admin_foundations.py` exits 0.
    - `git diff --check -- rulestead_admin/priv/static/css/rulestead_admin.css` exits 0.
    - `rulestead_admin.css` contains no new hex color literals outside existing token/cascade contexts.
    - `rulestead_admin.css` still contains `.rs-shell :where(a, button, input, select, textarea,`.
  </acceptance_criteria>
  <done>CSS media/shape/elevation edits remain within the foundation contract and pass the source guard.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- |
| T-115-05 | Denial of Service | reduced-motion CSS | mitigate | Source guard verifies reduced-motion floor without broad runtime loops. |
| T-115-06 | Tampering | admin visual system | mitigate | Contract and guard constrain CSS edits to existing foundation tokens. |
| T-115-07 | Spoofing | focus affordance | mitigate | Focus exceptions must be named; default ring remains source-asserted. |
</threat_model>

<verification>
1. `python3 scripts/check_admin_foundations.py`
2. `git diff --check -- rulestead_admin/priv/static/css/rulestead_admin.css`
</verification>

<success_criteria>
- FND-03 focus contract remains visible and source-backed.
- FND-04 reduced-motion users receive no nonessential scale/translate/staged effects.
- FND-05 shape/elevation/emphasis rules stay token-driven.
</success_criteria>
