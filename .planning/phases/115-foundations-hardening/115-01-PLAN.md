---
phase: "115-foundations-hardening"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/115-foundations-hardening/115-FOUNDATIONS-CONTRACT.md"
  - "scripts/check_admin_foundations.py"
  - "scripts/ci/lint.sh"
autonomous: true
requirements: [FND-01, FND-02, FND-05]
requirements_addressed: [FND-01, FND-02, FND-05]
decision_coverage:
  - "D-01"
  - "D-02"
  - "D-03"
  - "D-04"
  - "D-05"
  - "D-06"
  - "D-07"
  - "D-15"
user_setup: []
must_haves:
  truths:
    - "D-01: Phase 115 remains foundation-only and does not widen product, package, route, schema, release, Storybook, pixel-baseline, FleetDesk, or publish-prep scope."
    - "D-02: Phase 115 implementation stays scoped to FND-01 through FND-06; primitive/composite API changes route to Phase 116 unless foundation verification is blocked."
    - "D-03: The canonical breakpoint set is `40rem`, `48rem`, `60rem`, and `75rem`; every other admin CSS media threshold is documented with selector and reason."
    - "D-04: The admin stays responsive but not mobile-first; mobile work prevents overflow and preserves critical operator paths."
    - "D-05: Typography, spacing, radius, motion, shadow, focus, logo, and token source-of-truth rules are captured in a contract that downstream phases can cite."
    - "D-06: Radius, pill usage, elevation, and colored emphasis rules follow the existing softened-rectangle and low-contrast brand direction."
    - "D-07: A repo-local guard fails if new repeatable foundation drift appears without contract coverage."
    - "D-15: Planning uses recommendation-first defaults because this phase does not change public API, package, release, security, governance, or product scope."
  artifacts:
    - path: ".planning/phases/115-foundations-hardening/115-FOUNDATIONS-CONTRACT.md"
      provides: "Foundation rules and explicit exception ledger."
      contains: "## Breakpoint Contract"
    - path: "scripts/check_admin_foundations.py"
      provides: "Deterministic source guard for foundation drift."
      contains: "ADMIN FOUNDATIONS OK"
    - path: "scripts/ci/lint.sh"
      provides: "Normal CI entry point for the new foundation guard."
      contains: "check_admin_foundations.py"
---

<objective>
Create the Phase 115 foundation contract and source guard.

Purpose: Make FND-01, FND-02, and FND-05 auditable before CSS changes begin.
Output: `115-FOUNDATIONS-CONTRACT.md`, `scripts/check_admin_foundations.py`, and CI wiring.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/115-foundations-hardening/115-CONTEXT.md
@.planning/phases/115-foundations-hardening/115-RESEARCH.md
@.planning/phases/115-foundations-hardening/115-UI-SPEC.md
@.planning/phases/115-foundations-hardening/115-PATTERNS.md
@.planning/phases/113-design-system-inventory-ui-matrix-contract/113-DESIGN-SYSTEM-INVENTORY.md
@.planning/phases/113-design-system-inventory-ui-matrix-contract/113-UI-MATRIX-CONTRACT.md
@.planning/phases/114-repo-native-component-matrix-harness/114-02-SUMMARY.md
@brandbook/brand-book.md
@brandbook/tokens.json
@brandbook/tokens.css
@rulestead_admin/priv/static/css/rulestead_admin.css
@scripts/check_brand_tokens.py
@scripts/check_tokens_css.py
@scripts/check_logo_assets.py
@scripts/ci/lint.sh
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write the foundation contract and exception ledger</name>
  <files>.planning/phases/115-foundations-hardening/115-FOUNDATIONS-CONTRACT.md</files>
  <read_first>
    @.planning/phases/115-foundations-hardening/115-CONTEXT.md
    @.planning/phases/115-foundations-hardening/115-UI-SPEC.md
    @brandbook/brand-book.md
    @brandbook/tokens.css
    @rulestead_admin/priv/static/css/rulestead_admin.css
  </read_first>
  <action>Create `115-FOUNDATIONS-CONTRACT.md` with sections `## Scope`, `## Breakpoint Contract`, `## Noncanonical Breakpoint Exceptions`, `## Scalar Token Contract`, `## Focus Contract`, `## Reduced Motion Contract`, `## Radius, Pill, Elevation, And Emphasis Rules`, `## Dense Technical Content Rules`, and `## Verification Commands`. Include canonical breakpoints `40rem`, `48rem`, `60rem`, and `75rem`. List every current noncanonical media threshold in `rulestead_admin.css` with a selector/purpose row. Include the command `python3 scripts/check_admin_foundations.py` and the browser commands from `115-VALIDATION.md`. Do not introduce new token values, palette colors, logo changes, component APIs, routes, or future-phase work.</action>
  <verify>
    <automated>test -f .planning/phases/115-foundations-hardening/115-FOUNDATIONS-CONTRACT.md && rg -q '## Breakpoint Contract|## Focus Contract|## Reduced Motion Contract|40rem|48rem|60rem|75rem' .planning/phases/115-foundations-hardening/115-FOUNDATIONS-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - `115-FOUNDATIONS-CONTRACT.md` contains `## Breakpoint Contract`.
    - `115-FOUNDATIONS-CONTRACT.md` contains `40rem`, `48rem`, `60rem`, and `75rem`.
    - `115-FOUNDATIONS-CONTRACT.md` contains `## Noncanonical Breakpoint Exceptions`.
    - `115-FOUNDATIONS-CONTRACT.md` contains `## Focus Contract`.
    - `115-FOUNDATIONS-CONTRACT.md` contains `## Reduced Motion Contract`.
    - `115-FOUNDATIONS-CONTRACT.md` contains `python3 scripts/check_admin_foundations.py`.
  </acceptance_criteria>
  <done>Foundation contract records the canonical set, exceptions, focus/motion rules, shape/elevation rules, and verification commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the deterministic foundation guard</name>
  <files>scripts/check_admin_foundations.py, scripts/ci/lint.sh</files>
  <read_first>
    @scripts/check_brand_tokens.py
    @scripts/check_tokens_css.py
    @scripts/check_logo_assets.py
    @scripts/ci/lint.sh
    @.planning/phases/115-foundations-hardening/115-FOUNDATIONS-CONTRACT.md
    @rulestead_admin/priv/static/css/rulestead_admin.css
  </read_first>
  <action>Create `scripts/check_admin_foundations.py` as a stdlib-only Python guard. It must read `rulestead_admin/priv/static/css/rulestead_admin.css` and `.planning/phases/115-foundations-hardening/115-FOUNDATIONS-CONTRACT.md`. It must parse all `@media (...)` thresholds, allow canonical thresholds `40rem`, `48rem`, `60rem`, `75rem`, `47.99rem`, `prefers-color-scheme: dark`, `prefers-reduced-motion: no-preference`, and `prefers-reduced-motion: reduce`, and require every other threshold literal to appear in the contract. It must also assert the contract contains the required section headings, CSS contains `@media (prefers-reduced-motion: reduce)`, CSS contains the known command-palette focus exception marker `cmdk: inside modal`, and CSS contains `--rs-focus-ring`. Print `ADMIN FOUNDATIONS OK` on success. Wire the guard into `scripts/ci/lint.sh` using the existing `python3 "${RULESTEAD_REPO}/scripts/...py"` pattern. Do not add dependencies or change existing guard semantics.</action>
  <verify>
    <automated>python3 scripts/check_admin_foundations.py && rg -q 'check_admin_foundations.py' scripts/ci/lint.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check_admin_foundations.py` contains `ADMIN FOUNDATIONS OK`.
    - `scripts/check_admin_foundations.py` contains `CANONICAL_MEDIA`.
    - `scripts/check_admin_foundations.py` reads `115-FOUNDATIONS-CONTRACT.md`.
    - `scripts/check_admin_foundations.py` reads `rulestead_admin/priv/static/css/rulestead_admin.css`.
    - `scripts/ci/lint.sh` contains `check_admin_foundations.py`.
    - `python3 scripts/check_admin_foundations.py` exits 0.
  </acceptance_criteria>
  <done>Foundation guard is wired into CI and proves documented media, reduced-motion, and focus invariants.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- |
| T-115-01 | Tampering | foundation contract | mitigate | Guard requires documented thresholds and required sections. |
| T-115-02 | Repudiation | CI proof | mitigate | `scripts/ci/lint.sh` runs the guard and prints a deterministic pass line. |
| T-115-03 | Information Disclosure | source guard | accept | Guard reads only repo source files. |
| T-115-04 | Denial of Service | CI runtime | mitigate | Guard is stdlib-only and bounded to two files. |
</threat_model>

<verification>
1. `python3 scripts/check_admin_foundations.py`
2. `rg -q 'check_admin_foundations.py' scripts/ci/lint.sh`
3. `git diff --check`
</verification>

<success_criteria>
- FND-01 has a canonical breakpoint set plus documented exceptions.
- FND-02 has a source-backed foundation contract aligned with brandbook/admin CSS.
- FND-05 has explicit shape, pill, elevation, and emphasis rules.
- CI includes a readable foundation drift guard.
</success_criteria>
