---
phase: 75-proof-umbrella-and-milestone-closure
plan: 75-01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/mix/tasks/verify.phase73.ex
  - rulestead/lib/mix/tasks/verify.adopter.ex
  - rulestead/mix.exs
  - scripts/ci/test.sh
autonomous: true
requirements:
  - VER-01
  - VER-02
must_haves:
  truths:
    - mix verify.phase73 runs phase72 flat union plus context_test.exs without delegating to verify.phase72
    - mix verify.adopter delegates only to verify.phase73
    - post_ga_band_closure CI scope invokes verify.phase73
  artifacts:
    - rulestead/lib/mix/tasks/verify.phase73.ex
    - rulestead/lib/mix/tasks/verify.adopter.ex
---

# Plan 75-01: verify.phase73 And Adopter Delegate

<objective>
Ship VER-01 and VER-02: flat `mix verify.phase73` merge gate (phase72 union + v1.10.1 context contract tests) and retarget `mix verify.adopter` plus CI `post_ga_band_closure` to that gate. No new product APIs.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-75-01: phase73 delegates to phase72 causing double admin runs | medium | Copy test path lists; call `Mix.Task.run("test", paths)` only — never `Mix.Task.run("verify.phase72")` |
| T-75-02: Drift between phase72 and phase73 unions | low | Comment in phase73.ex that union = phase72 core + v1.10.1 additions; single append for context_test |
| T-75-03: CI still runs phase72 after doc bump | medium | Update `scripts/ci/test.sh` `run_post_ga_band_closure` remediation strings and runner |
</threat_model>

<tasks>

<task id="75-01-01" type="execute">
  <name>Add mix verify.phase73 flat union (VER-01)</name>
  <read_first>
    - .planning/phases/75-proof-umbrella-and-milestone-closure/75-CONTEXT.md (D-01–D-06 locked decisions)
    - rulestead/lib/mix/tasks/verify.phase72.ex (full @phase72_core_tests and @admin_test_paths)
    - rulestead/lib/mix/tasks/verify.adopter.ex
    - .planning/phases/75-proof-umbrella-and-milestone-closure/75-RESEARCH.md
    - prompts/rulestead-release-engineering-and-ci.md (per-phase verify pattern)
  </read_first>
  <action>
Create `rulestead/lib/mix/tasks/verify.phase73.ex`:

- `use Mix.Task`, `@moduledoc false`, `@shortdoc "Runs Phase 75 v1.10.1 support-truth verification suite"`
- Copy `@phase72_core_tests` from `verify.phase72.ex` **verbatim** into `@phase73_core_tests`
- Append **one** path at end of core list: `"test/rulestead/context_test.exs"`
- Copy `@admin_test_paths` unchanged from phase72
- `@impl run/1`: `Mix.Task.run("test", @phase73_core_tests)` then admin subprocess identical to phase72 (`Path.expand("../../../../rulestead_admin", __DIR__)` + `mix test` joined paths)
- Module comment: flat union of phase72 + v1.10.1 context guards; **do not** delegate to `verify.phase72`

Update `rulestead/mix.exs` `cli/0` `preferred_envs` — add `{:"verify.phase73", :test}` after `verify.phase72` entry.

**Do not** remove or edit `verify.phase72.ex`.
  </action>
  <verify>
    <automated>cd rulestead && mix help verify.phase73 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `rulestead/lib/mix/tasks/verify.phase73.ex`
    - `grep -q 'context_test.exs' rulestead/lib/mix/tasks/verify.phase73.ex`
    - `grep -q 'verify.phase72' rulestead/lib/mix/tasks/verify.phase73.ex` returns exit 1 (no delegation)
    - `grep -q 'verify.phase73' rulestead/mix.exs`
    - `cd rulestead && mix verify.phase73` exits 0
  </acceptance_criteria>
</task>

<task id="75-01-02" type="execute">
  <name>Retarget verify.adopter and CI post_ga_band_closure (VER-02)</name>
  <read_first>
    - rulestead/lib/mix/tasks/verify.adopter.ex
    - scripts/ci/test.sh (functions `run_post_ga_band_closure`, `print_post_ga_band_closure_failure_guidance` ~L423-472)
    - scripts/demo/proof.sh (should already call verify.adopter — confirm only)
  </read_first>
  <action>
**verify.adopter.ex:**

- Change `Mix.Task.run("verify.phase72", args)` → `Mix.Task.run("verify.phase73", args)`
- Update `@moduledoc` first paragraph: integrator entrypoint for **v1.10.1** support-truth; delegates to `mix verify.phase73`
- Update `@shortdoc`: mention phase73

**scripts/ci/test.sh:**

- In `run_post_ga_band_closure`, replace `verify.phase72` with `verify.phase73` in `run_mix_logged` call
- In `print_post_ga_band_closure_failure_guidance`, change remediation line to `cd rulestead && mix verify.phase73`

Leave `scripts/demo/proof.sh` unchanged if it already runs `mix verify.adopter`.
  </action>
  <verify>
    <automated>cd rulestead && mix verify.adopter 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'verify.phase73' rulestead/lib/mix/tasks/verify.adopter.ex`
    - `grep 'verify.phase72' rulestead/lib/mix/tasks/verify.adopter.ex` returns exit 1
    - `grep 'verify.phase73' scripts/ci/test.sh`
    - `cd rulestead && mix verify.adopter` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead && mix verify.phase73
cd rulestead && mix verify.adopter
RULESTEAD_TEST_SCOPE=post_ga_band_closure bash scripts/ci/test.sh
```
</verification>

<success_criteria>
- VER-01 and VER-02 satisfied
- phase73 is strict superset of phase72 (one additional test file)
- CI post_ga_band_closure and adopter share the same gate
</success_criteria>
