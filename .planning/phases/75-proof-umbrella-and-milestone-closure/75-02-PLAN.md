---
phase: 75-proof-umbrella-and-milestone-closure
plan: 75-02
type: docs
wave: 2
depends_on:
  - 75-01
files_modified:
  - MAINTAINING.md
  - README.md
  - rulestead/README.md
  - guides/introduction/product-boundary.md
  - rulestead/test/rulestead/release_contract_test.exs
  - .planning/threads/2026-05-28-path-to-done-milestones.md
autonomous: true
requirements:
  - DOC-02
must_haves:
  truths:
    - Maintainer and root docs name mix verify.phase73 as the current post-GA band merge gate
    - release_contract_test post-GA block asserts phase73 (not phase72-only) in README and MAINTAINING
    - path-to-done thread reflects v1.10.1 exit criteria with phase73 proof spine
  artifacts:
    - MAINTAINING.md
    - rulestead/test/rulestead/release_contract_test.exs
---

# Plan 75-02: Maintainer Proof Matrix And Doc Contract Bump

<objective>
DOC-02: Align maintainer proof matrix, root/package README post-GA bullets, product-boundary band gate, path-to-done thread, and release-contract doc honesty with `mix verify.phase73` as the v1.10.1 adopter bar. Depends on 75-01 shipping the task.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-75-02: Docs claim phase73 but CI still says phase72 | high | 75-01 must land first; this plan updates release_contract asserts to match |
| T-75-03: Removing historical phase72 references from milestone archives | low | Only update "current bar" sections; keep v1.10.0 historical mentions in RETROSPECTIVE/MILESTONES |
</threat_model>

<tasks>

<task id="75-02-01" type="execute">
  <name>Update MAINTAINING Post-GA section and proof matrix (DOC-02)</name>
  <read_first>
    - MAINTAINING.md (sections "Post-GA Band Closure Proof" ~L413-447 and "Proof Matrix" ~L449-458)
    - rulestead/lib/mix/tasks/verify.phase73.ex (confirm task name)
    - .planning/phases/75-proof-umbrella-and-milestone-closure/75-RESEARCH.md
  </read_first>
  <action>
In **MAINTAINING.md**:

**Post-GA Band Closure Proof** code blocks and prose:

- Replace primary command `mix verify.phase72` with `mix verify.phase73`
- Keep integrator alias note: `mix verify.adopter` (delegates to phase73)
- Add one bullet under "Proves:" — `context_test.exs` — traits→attributes promotion (v1.10.1)
- Add bullet — bidirectional api_stability catalog guards via `release_contract_test.exs` (v1.10.1 / Phase 74)

**Proof Matrix** table row for post-GA band:

- Change command column from `mix verify.phase72` / `mix verify.adopter` to `mix verify.phase73` / `mix verify.adopter`
- "Proves" column: mention v1.10.1 context + api_stability contract guards
- Add optional row: `mix verify.phase72` | Historical v1.10.0 gate | Superseded by phase73 for v1.10.1+

Remove stale copy implying api_stability catalog sync is still open in MAINTAINING if any "Phase 74" pending language remains in Post-GA section (catalog completeness is done).
  </action>
  <verify>
    <automated>rg 'mix verify\.phase73' MAINTAINING.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `MAINTAINING.md` contains `mix verify.phase73` at least twice (proof section + matrix)
    - `MAINTAINING.md` Post-GA section mentions `context_test` or traits→attributes
    - `rg 'mix verify\.phase72' MAINTAINING.md` only in historical/superseded context (≤2 matches acceptable in matrix footnote)
  </acceptance_criteria>
</task>

<task id="75-02-02" type="execute">
  <name>Bump README, product-boundary, release_contract, path-to-done (DOC-02)</name>
  <read_first>
    - README.md (Post-GA band closure bullet ~L243-246)
    - rulestead/README.md (Post-GA section ~L106)
    - guides/introduction/product-boundary.md (band closure gate ~L69-70)
    - rulestead/test/rulestead/release_contract_test.exs (test `"post-GA band closure support truth stays bounded"` ~L634-659)
    - .planning/threads/2026-05-28-path-to-done-milestones.md
  </read_first>
  <action>
**README.md** — Post-GA band closure bullet:

- Change `mix verify.phase72` to `mix verify.phase73` (keep alias sentence for `mix verify.adopter`)

**rulestead/README.md** — Post-GA band closure line:

- Same phase72 → phase73 substitution

**guides/introduction/product-boundary.md** — band closure maintainer gate lines:

- `mix verify.phase72` → `mix verify.phase73`
- Integrator shortcut line: note adopter delegates to phase73

**release_contract_test.exs** — in `"post-GA band closure support truth stays bounded across root package and maintainer docs"`:

- `assert root_readme =~ "mix verify.phase72"` → `"mix verify.phase73"`
- `assert maintaining =~ "mix verify.phase72"` (both occurrences) → `"mix verify.phase73"`
- `assert runtime_readme =~ "mix verify.phase72"` → `"mix verify.phase73"`
- Keep `assert root_readme =~ "mix verify.adopter"` and phase68 historical reference unchanged

**path-to-done thread** (`.planning/threads/2026-05-28-path-to-done-milestones.md`):

- Milestone 1 row: mark investigations INV-API-01 / INV-MAINT-01 **Closed** in exit criteria column
- Proof spine section: `mix verify.adopter` / band closure — note delegates to phase73
- Open investigations table: set INV-API-01 and INV-MAINT-01 status to Closed (Phase 75)

Do **not** edit `guides/api_stability.md` content in this task.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/release_contract_test.exs --only test:"post-GA band closure support truth stays bounded" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `mix verify.phase73`
    - `rulestead/README.md` contains `mix verify.phase73`
    - `guides/introduction/product-boundary.md` contains `mix verify.phase73`
    - `release_contract_test.exs` contains `mix verify.phase73` in post-GA band closure test
    - `cd rulestead && mix test test/rulestead/release_contract_test.exs` exits 0
    - path-to-done thread lists INV-API-01 and INV-MAINT-01 as Closed
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead && mix test test/rulestead/release_contract_test.exs
rg 'mix verify\.phase73' README.md MAINTAINING.md rulestead/README.md guides/introduction/product-boundary.md
```
</verification>

<success_criteria>
- DOC-02 complete: proof matrix and path-to-done reference v1.10.1 exit criteria
- No stale "open gap" copy for api_stability deferral or Phase 8 artifacts in files touched
</success_criteria>
