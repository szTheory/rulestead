---
phase: 02-data-model-error-model-ecto-store-fake-adapter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead.ex
  - rulestead/lib/rulestead/error.ex
  - rulestead/lib/rulestead/evaluation_error.ex
  - rulestead/lib/rulestead/ruleset_error.ex
  - rulestead/lib/rulestead/kill_switch_error.ex
  - rulestead/lib/rulestead/config_error.ex
  - rulestead/lib/rulestead/store_error.ex
  - rulestead/lib/rulestead/auth_error.ex
  - rulestead/lib/rulestead/store.ex
  - rulestead/lib/rulestead/store/command.ex
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03
  - ERR-04
must_haves:
  truths:
    - "Phase 2 locks one stable public `%Rulestead.Error{}` envelope before adapter-specific failures are implemented."
    - "Store-facing code can target a key-first `Rulestead.Store` behavior instead of ad hoc Repo calls."
    - "The public bang/non-bang convention is reserved explicitly, including `evaluate/3` and `evaluate!/3`, without implementing Phase 3 evaluation."
  artifacts:
    - path: "rulestead/lib/rulestead/error.ex"
      provides: "Stable `%Rulestead.Error{}` contract with JSON-safe encoding"
    - path: "rulestead/lib/rulestead/store.ex"
      provides: "Key-first store behavior and callback types"
    - path: "rulestead/lib/rulestead.ex"
      provides: "Root bang/non-bang public API convention and evaluator stub reservation"
  key_links:
    - from: "rulestead/lib/rulestead.ex"
      to: "rulestead/lib/rulestead/store.ex"
      via: "public bang/non-bang wrappers delegate through configured store adapter"
      pattern: "def .*!"
    - from: "rulestead/lib/rulestead.ex"
      to: "rulestead/lib/rulestead/error.ex"
      via: "`evaluate/3` and `evaluate!/3` reserve the typed error convention"
      pattern: "def evaluate"
---

<objective>
Lock the Phase 2 public API contract: one stable error envelope, the domain-command store behavior, and the reserved bang/non-bang entrypoint pattern.

Purpose: Schemas and adapters need fixed public semantics before implementation, and the roadmap explicitly requires the `evaluate/3` and `evaluate!/3` stub convention to be reserved in Phase 2.
Output: Error modules, store behavior/commands, root public wrappers, and evaluator stub reservation.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-CONTEXT.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-PATTERNS.md
@rulestead/lib/rulestead.ex
@prompts/rulestead-engineering-dna-from-prior-libs.md
@prompts/elixir-best-practices-deep-research.md
@prompts/rulestead-domain-language-field-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the public error envelope and typed helper namespaces</name>
  <files>rulestead/lib/rulestead/error.ex, rulestead/lib/rulestead/evaluation_error.ex, rulestead/lib/rulestead/ruleset_error.ex, rulestead/lib/rulestead/kill_switch_error.ex, rulestead/lib/rulestead/config_error.ex, rulestead/lib/rulestead/store_error.ex, rulestead/lib/rulestead/auth_error.ex</files>
  <action>Implement the locked Phase 2 error contract per D-14 through D-18. Create one concrete `%Rulestead.Error{}` struct with `:domain`, closed-atom `:type`, `:message`, safe `:metadata`, flat `:details`, raw `:cause`, and optional `:plug_status`, and make it the raised exception as well as the return-envelope payload. Exclude `:cause` from `Jason.Encoder` per D-16. Add typed helper namespace modules as constructors/normalizers that all return the same root struct, not competing public structs. Document the exact closed leaf-atom posture so later plans map validation and repo failures into stable atoms such as `:flag_not_found`, `:environment_not_found`, `:repo_not_configured`, `:repo_ambiguous`, `:variant_weights_invalid`, and `:store_unavailable`.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/error_test.exs'</automated>
  </verify>
  <done>Phase 2 exposes one stable public error shape with typed constructor namespaces and no raw-cause leakage through JSON encoding.</done>
</task>

<task type="auto">
  <name>Task 2: Define the key-first store behavior and shared command structs</name>
  <files>rulestead/lib/rulestead/store.ex, rulestead/lib/rulestead/store/command.ex</files>
  <action>Create the `Rulestead.Store` behavior per D-10 through D-13 as a domain-command, key-first contract. Define typed command/request structs in `Rulestead.Store.Command` for the Phase 2 operations downstream adapters must share: fetch by `flag_key` + `environment_key`, save draft ruleset, publish ruleset version, archive flag, and list/search surfaces needed in this phase. Public selectors stay on `flag_key` and `environment_key`; internal UUIDs remain private. No callback or public contract path may return `nil` for a miss.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/store_contract_shape_test.exs'</automated>
  </verify>
  <done>Downstream plans can implement adapters and tests against one explicit behavior and one explicit set of domain command structs.</done>
</task>

<task type="auto">
  <name>Task 3: Reserve public bang/non-bang conventions, including evaluator stubs</name>
  <files>rulestead/lib/rulestead.ex</files>
  <action>Extend `Rulestead` with the Phase 2 public bang/non-bang pattern. Add the store-facing public wrappers that route through the configured store adapter and raise the same `%Rulestead.Error{}` on bang variants. Also explicitly reserve `evaluate/3` and `evaluate!/3` as Phase 2 stubs per the roadmap and D-14, documenting that they only lock the tuple/raise convention now and do not implement the Phase 3 evaluator. If executable stubs are used, they must fail with a typed evaluation/config error that clearly indicates the runtime evaluator is not yet implemented.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/public_api_convention_test.exs'</automated>
  </verify>
  <done>The public root module now reserves both the store API shape and the `evaluate/3` + `evaluate!/3` Phase 2 stub convention without leaking evaluator behavior into this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public caller→store API | Untrusted app code passes keys and command payloads into the public Phase 2 authoring surface |
| public caller→error JSON | Serialized error values may cross process or HTTP boundaries later and must remain safe now |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | I | `Rulestead.Error` JSON encoding | mitigate | Exclude `:cause` from `Jason.Encoder` per D-16 so raw exceptions, changesets, and payloads cannot leak into serialized audit or API data. |
| T-02-05 | T | `Rulestead.Store` public contract | mitigate | Make selectors key-first and reject `nil`/UUID-based public misses per D-10 through D-13, forcing adapters to normalize failures into typed errors. |
| T-02-06 | S | reserved public evaluator entrypoints | mitigate | Reserve `evaluate/3` and `evaluate!/3` explicitly with typed not-yet-implemented behavior so callers cannot infer ad hoc semantics before Phase 3 lands. |
</threat_model>

<verification>
Run the error, store-contract-shape, and public API convention tests, then confirm the resulting public contract is still confined to `rulestead/` with no Phase 3 runtime logic and no Phase 5 host-app integration files.
</verification>

<success_criteria>
Phase 2 has a fixed public error/store contract and an explicit reserved bang/non-bang convention, including evaluator stubs, that later plans can build on without reopening API shape decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-02-SUMMARY.md`
</output>
