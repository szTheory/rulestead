---
phase: 02-data-model-error-model-ecto-store-fake-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/mix.exs
  - rulestead/config/config.exs
  - rulestead/config/test.exs
  - rulestead/lib/rulestead/repo.ex
  - rulestead/test/test_helper.exs
  - rulestead/test/support/repo_case.ex
autonomous: true
requirements:
  - STORE-01
must_haves:
  truths:
    - "Phase 2 tests can boot a local Ecto repo and run with sandbox `mode: :manual` before any adapter work starts."
    - "Later Phase 2 plans inherit one shared repo/test harness instead of re-creating database bootstrapping piecemeal."
    - "No Phase 5 host-app seams are introduced while setting up Ecto support."
  artifacts:
    - path: "rulestead/lib/rulestead/repo.ex"
      provides: "Internal Ecto repo for Phase 2 persistence work"
    - path: "rulestead/test/support/repo_case.ex"
      provides: "Shared Ecto sandbox harness for later adapter tests"
    - path: "rulestead/test/test_helper.exs"
      provides: "Manual sandbox bootstrap"
  key_links:
    - from: "rulestead/test/test_helper.exs"
      to: "rulestead/lib/rulestead/repo.ex"
      via: "sandbox manual mode bootstrap"
      pattern: "Ecto.Adapters.SQL.Sandbox.mode"
    - from: "rulestead/test/support/repo_case.ex"
      to: "rulestead/lib/rulestead/repo.ex"
      via: "shared case template checkout/setup"
      pattern: "use ExUnit.CaseTemplate"
---

<objective>
Create the repo and test harness foundation for Phase 2 Ecto work.

Purpose: Later plans need a working internal repo and one shared sandbox setup before schemas, adapters, or installer smoke tests can land.
Output: Repo config, test bootstrap, and reusable `RepoCase`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-CONTEXT.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-PATTERNS.md
@.planning/phases/01-repo-bootstrap/01-CONTEXT.md
@rulestead/mix.exs
@rulestead/test/test_helper.exs
@prompts/rulestead-testing-and-e2e-strategy.md
@prompts/ecto-best-practices-deep-research.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the minimal Ecto repo and dependency foundation inside `rulestead/`</name>
  <files>rulestead/mix.exs, rulestead/config/config.exs, rulestead/config/test.exs, rulestead/lib/rulestead/repo.ex</files>
  <action>Update the core package to support Ecto-backed Phase 2 work inside `rulestead/` only. Add the minimal dependencies and repo configuration needed for `Ecto.Repo`, Postgrex, and JSON encoding used by the Phase 2 error contract. Create `Rulestead.Repo` as an internal repo module. Keep the setup narrow: no endpoint, router, PubSub, Oban, application child wiring, or host-app integration surfaces per D-23 through D-26 and the Phase 2 boundary.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix deps.get && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>`rulestead/` can compile with an internal repo available for Phase 2 schema and adapter work, without pulling Phase 5 seams forward.</done>
</task>

<task type="auto">
  <name>Task 2: Establish shared sandbox bootstrap and reusable repo case</name>
  <files>rulestead/test/test_helper.exs, rulestead/test/support/repo_case.ex</files>
  <action>Configure `test/test_helper.exs` and `RepoCase` for ExUnit SQL sandbox usage in manual mode, matching the explicit Phase 2 scope and test DNA. `RepoCase` should own checkout/setup so later store and migration tests reuse one pattern instead of inlining DB setup. This task is the Phase 2 test prerequisite behind STORE-01 and the roadmap’s sandbox expectation; do not add fake-only helpers or installer logic here.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead_test.exs'</automated>
  </verify>
  <done>Later Phase 2 plans can run DB-backed tests through one shared sandbox harness with `mode: :manual` already locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runtime→repo | Local tests open SQL sandbox connections against a developer-controlled Postgres instance |
| package config→future installer work | Repo wiring added now becomes the base that later installer tests rely on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | D | `Rulestead.Repo` test setup | mitigate | Use explicit test-only repo config and sandbox `:manual` mode so adapter tests cannot interfere through shared implicit connections. |
| T-02-02 | T | package/runtime boundary | mitigate | Limit this plan to repo/test plumbing only; do not touch router, endpoint, application, Oban, or admin wiring per D-23 through D-26. |
| T-02-03 | E | shared test helpers | mitigate | Centralize DB setup in `RepoCase` so later tests do not invent privileged ad hoc repo access patterns. |
</threat_model>

<verification>
Compile the package and run the existing root test to prove the repo/test harness boots cleanly without introducing host-app integration files.
</verification>

<success_criteria>
Phase 2 has a working internal repo and shared Ecto test harness that later plans can depend on without reopening setup decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-01-SUMMARY.md`
</output>
