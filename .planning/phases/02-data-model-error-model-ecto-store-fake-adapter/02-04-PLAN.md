---
phase: 02-data-model-error-model-ecto-store-fake-adapter
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - rulestead/lib/rulestead/fake.ex
  - rulestead/lib/rulestead/fake/control.ex
  - rulestead/test/support/store_contract_case.ex
  - rulestead/test/support/store_fixtures.ex
  - rulestead/test/rulestead/store/fake_contract_test.exs
autonomous: true
requirements:
  - STORE-07
must_haves:
  truths:
    - "Phase 2 has a real in-memory store adapter that exercises the same semantic contract as the Ecto adapter."
    - "Adapter parity is enforced by a shared contract suite rather than by duplicate bespoke tests."
    - "Fake-only controls stay outside the shared `Rulestead.Store` behavior."
  artifacts:
    - path: "rulestead/lib/rulestead/fake.ex"
      provides: "Contract-faithful in-memory store adapter"
    - path: "rulestead/lib/rulestead/fake/control.ex"
      provides: "Test-only fake controls without widening the public store behavior"
    - path: "rulestead/test/support/store_contract_case.ex"
      provides: "Shared adapter contract suite reusable by fake and Ecto tests"
  key_links:
    - from: "rulestead/test/support/store_contract_case.ex"
      to: "rulestead/lib/rulestead/store.ex"
      via: "shared callback expectations and error normalization"
      pattern: "use ExUnit.CaseTemplate"
    - from: "rulestead/lib/rulestead/fake.ex"
      to: "rulestead/lib/rulestead/ruleset.ex"
      via: "shared ruleset validation and publish semantics from Plan 02-03"
      pattern: "Ruleset"
---

<objective>
Implement the release-gate fake adapter and the shared store contract suite that the Ecto adapter will later have to satisfy.

Purpose: Phase 2 requires a fast deterministic adapter for tests, but it must stay contract-faithful to the same ruleset validations and publish boundaries established by the data model.
Output: `Rulestead.Fake`, fake controls, and adapter-neutral contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-CONTEXT.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-PATTERNS.md
@rulestead/lib/rulestead/store.ex
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/error.ex
@rulestead/lib/rulestead/ruleset.ex
@prompts/rulestead-testing-and-e2e-strategy.md
@prompts/rulestead-engineering-dna-from-prior-libs.md

<interfaces>
This plan must implement the `Rulestead.Store` callbacks and shared command structs created in Plan 02-02, using the ruleset/schema validations and publish boundaries created in Plan 02-03. The contract suite written here is the same suite Plan 02-05 will reuse for the Ecto adapter.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a contract-faithful in-memory fake adapter with isolated test controls</name>
  <files>rulestead/lib/rulestead/fake.ex, rulestead/lib/rulestead/fake/control.ex</files>
  <action>Implement `Rulestead.Fake` per D-19 through D-22 as an in-memory adapter that satisfies `Rulestead.Store` with the same selectors, ruleset validations, publish/version boundaries, archived/read-only behavior, and typed error mapping that the Ecto adapter will enforce. Reuse the schema and changeset semantics from Plan 02-03 rather than inventing fake-only shortcuts for invalid rulesets or weight-sum failures. Put test-only affordances such as state reset, clock control, and inspection hooks in `Rulestead.Fake.Control`, not in `Rulestead.Store`.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/store/fake_contract_test.exs'</automated>
  </verify>
  <done>The repo has a deterministic in-memory adapter that is useful for tests because it is faithful to the shared store semantics and validation rules, not because it cuts corners.</done>
</task>

<task type="auto">
  <name>Task 2: Create the shared adapter contract suite and run it against Fake</name>
  <files>rulestead/test/support/store_contract_case.ex, rulestead/test/support/store_fixtures.ex, rulestead/test/rulestead/store/fake_contract_test.exs</files>
  <action>Create one shared contract suite per D-21 that any store adapter test module can `use`. The suite must cover the happy and unhappy paths called out in the locked context: duplicate keys, invalid rulesets, weight-sum failures, publish/version conflicts, not-found behavior, and archived/read-only behavior. Use shared fixture builders so Fake and Ecto consume the exact same payloads and expected errors, and make the fixtures derive invalid rulesets from the real schema semantics introduced in Plan 02-03.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/store/fake_contract_test.exs --seed 0'</automated>
  </verify>
  <done>Phase 2 has an explicit parity harness that later adapters must satisfy rather than a loosely implied contract, and that harness is sequenced after the schema validations it depends on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test helpers→adapter contract | Test code must not quietly widen the production store API |
| fake state→shared validation semantics | In-memory state can drift from real-store semantics unless parity is enforced mechanically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | T | `Rulestead.Fake` semantics | mitigate | Reuse the same `Rulestead.Store` callbacks, `%Rulestead.Error{}` mapping, and Plan 02-03 ruleset validation semantics as the real adapter per D-19 and D-21. |
| T-02-11 | E | fake control surface | mitigate | Keep reset/time/control APIs in `Rulestead.Fake.Control` per D-20 and D-22 so callers cannot rely on fake-only behavior through the public store contract. |
| T-02-12 | R | adapter parity claims | mitigate | Encode happy and unhappy path parity in one shared contract suite per D-21 and require both adapters to run it. |
</threat_model>

<verification>
Run the fake contract test module and confirm the shared contract suite covers every locked unhappy path from the context file, using the same validation semantics the Ecto adapter will inherit from the schema layer.
</verification>

<success_criteria>
The fake adapter is useful as the Phase 2 release-gate target because its semantics are mechanically tied to both the shared store contract and the schema validation rules it claims parity with.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-04-SUMMARY.md`
</output>
