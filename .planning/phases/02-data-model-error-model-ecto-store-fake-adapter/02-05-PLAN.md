---
phase: 02-data-model-error-model-ecto-store-fake-adapter
plan: 05
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/install.ex
  - rulestead/lib/rulestead/install/repo_locator.ex
  - rulestead/lib/rulestead/install/migration_writer.ex
  - rulestead/lib/rulestead/install/config_writer.ex
  - rulestead/lib/mix/tasks/rulestead.install.ex
  - rulestead/priv/templates/rulestead.install/config/rulestead.exs
  - rulestead/test/rulestead/store/ecto_contract_test.exs
  - rulestead/test/rulestead/mix/tasks/rulestead_install_test.exs
  - rulestead/test/rulestead/integration/install_smoke_test.exs
autonomous: true
requirements:
  - STORE-01
must_haves:
  truths:
    - "A real Ecto/Postgres adapter exists behind the same `Rulestead.Store` behavior the fake already satisfies."
    - "The same shared contract suite validates both fake and Ecto adapters."
    - "Phase 2 ships only the narrow installer slice: migrations plus config, and a fresh Phoenix app can run install plus migrate to produce a clean schema."
  artifacts:
    - path: "rulestead/lib/rulestead/store/ecto.ex"
      provides: "Transactional Ecto-backed authoring adapter"
    - path: "rulestead/lib/mix/tasks/rulestead.install.ex"
      provides: "Minimal Phase 2 installer entrypoint"
    - path: "rulestead/test/rulestead/integration/install_smoke_test.exs"
      provides: "Fresh Phoenix app install+migrate smoke proof"
  key_links:
    - from: "rulestead/lib/rulestead/store/ecto.ex"
      to: "rulestead/test/support/store_contract_case.ex"
      via: "shared adapter parity suite"
      pattern: "use Rulestead.StoreContractCase"
    - from: "rulestead/lib/mix/tasks/rulestead.install.ex"
      to: "rulestead/priv/templates/rulestead.install/config/rulestead.exs"
      via: "config file generation"
      pattern: "rulestead.exs"
    - from: "rulestead/test/rulestead/integration/install_smoke_test.exs"
      to: "rulestead/priv/repo/migrations"
      via: "fresh host app copies migrations then runs ecto.migrate"
      pattern: "mix ecto.migrate"
---

<objective>
Close Phase 2 with the real Ecto adapter and the minimal installer, then prove both adapter parity and the fresh-host install+migrate smoke path.

Purpose: This is the smallest end-to-end slice that satisfies the real-store requirement and the roadmap’s installer success criterion without pulling Phase 5 host-app integration forward.
Output: `Rulestead.Store.Ecto`, installer helpers, parity tests, installer idempotency tests, and a fresh Phoenix app smoke test.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-CONTEXT.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-PATTERNS.md
@rulestead/lib/rulestead/store.ex
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/error.ex
@rulestead/lib/rulestead/flag.ex
@rulestead/lib/rulestead/flag_environment.ex
@rulestead/lib/rulestead/ruleset.ex
@rulestead/test/support/store_contract_case.ex
@prompts/ecto-best-practices-deep-research.md
@prompts/rulestead-testing-and-e2e-strategy.md

<interfaces>
The Ecto adapter must satisfy the same command structs and shared contract suite already exercised by `Rulestead.Fake`. Installer scope is locked to D-23 through D-26 only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Ecto store adapter and prove parity with the shared contract suite</name>
  <files>rulestead/lib/rulestead/store/ecto.ex, rulestead/test/rulestead/store/ecto_contract_test.exs</files>
  <action>Implement `Rulestead.Store.Ecto` against the schemas and migrations from Plan 02-03, using the behavior and command structs from Plan 02-02. Map all not-found, constraint, validation, and config failures to the locked `%Rulestead.Error{}` taxonomy per D-10 through D-18. Use `Repo.transact/1` or `Ecto.Multi` where it clarifies multi-row writes, but keep publish/version pointer flips transactional and immutable per D-07 and D-13. Wire `test/rulestead/store/ecto_contract_test.exs` to the shared contract suite from Plan 02-04 so the same adapter-neutral tests execute against the real adapter.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix ecto.drop --quiet && mix ecto.create && mix ecto.migrate && mix test test/rulestead/store/ecto_contract_test.exs'</automated>
  </verify>
  <done>Phase 2 has a real Ecto-backed authoring adapter that is provably contract-compatible with the fake adapter.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the minimal migration-and-config installer with idempotency tests</name>
  <files>rulestead/lib/rulestead/install.ex, rulestead/lib/rulestead/install/repo_locator.ex, rulestead/lib/rulestead/install/migration_writer.ex, rulestead/lib/rulestead/install/config_writer.ex, rulestead/lib/mix/tasks/rulestead.install.ex, rulestead/priv/templates/rulestead.install/config/rulestead.exs, rulestead/test/rulestead/mix/tasks/rulestead_install_test.exs</files>
  <action>Implement the narrow Phase 2 installer slice per D-23 through D-26. The task must resolve a repo when exactly one exists, require `--repo MyApp.Repo` when multiple repos are configured, fail with a typed config error when no repo is configured, copy the library migrations into the host app, write `config/rulestead.exs`, and inject `import_config \"rulestead.exs\"` into `config/config.exs` only when absent. Make reruns idempotent and emit only skip/already-present output per D-25. Explicitly forbid and test against router, endpoint, application, Oban, PubSub, or admin-mount edits so no Phase 5 wiring leaks into this plan.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/mix/tasks/rulestead_install_test.exs'</automated>
  </verify>
  <done>The repo ships a minimal Phase 2 installer that is intentionally limited to migration/config setup and mechanically prevents Phase 5 integration leakage.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the fresh Phoenix app install plus migrate smoke path</name>
  <files>rulestead/test/rulestead/integration/install_smoke_test.exs</files>
  <action>Create an automated smoke test for Roadmap Success Criterion 1. The test must generate a temporary fresh Phoenix app, add `{:rulestead, path: ...}` as a local dependency, run `mix deps.get`, run `mix rulestead.install` with the right repo selection behavior, then run `mix ecto.migrate` and assert the resulting host schema contains the Phase 2 tables and default environments. Keep this test scoped to migrations plus config only: no router, endpoint, admin mount, Oban, PubSub, or LiveView wiring assertions belong here.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/integration/install_smoke_test.exs --timeout 300000'</automated>
  </verify>
  <done>Phase 2 explicitly proves that a fresh Phoenix host can install the library’s migrations/config and migrate to a clean schema without any manual schema repair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store adapter→database | Authoring commands mutate persistent state and must preserve invariants under concurrent writes |
| installer→host app files | Library code writes into a host project and must stay inside the narrow Phase 2 contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | T | `Rulestead.Store.Ecto` publish path | mitigate | Keep ruleset publish and active-pointer flip transactional per D-07 and D-13 so partial writes cannot leave environment state inconsistent. |
| T-02-14 | I | installer config generation | mitigate | Emit only `config/rulestead.exs` and `config/config.exs` import changes per D-23 through D-26; do not touch router, endpoint, application, or auth/admin files. |
| T-02-15 | D | repo discovery in installer and host smoke test | mitigate | Fail fast with typed config errors for zero/multiple repo cases per D-24 so install runs do not hang or write to the wrong host repo. |
</threat_model>

<verification>
Run the Ecto contract suite, installer tests, and the fresh Phoenix app smoke test, then confirm the installer helper/template paths are the only host-writing surfaces added by this plan.
</verification>

<success_criteria>
Phase 2 ends with both store adapters implemented behind one behavior, parity proven by one shared contract suite, and the installer limited to migrations plus config while the fresh-host install+migrate path is explicitly verified.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-05-SUMMARY.md`
</output>
