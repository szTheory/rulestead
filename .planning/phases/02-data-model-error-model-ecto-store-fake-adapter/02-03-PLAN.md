---
phase: 02-data-model-error-model-ecto-store-fake-adapter
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - rulestead/lib/rulestead/flag.ex
  - rulestead/lib/rulestead/environment.ex
  - rulestead/lib/rulestead/flag_environment.ex
  - rulestead/lib/rulestead/ruleset.ex
  - rulestead/lib/rulestead/audience.ex
  - rulestead/lib/rulestead/audit_event.ex
  - rulestead/lib/rulestead/ruleset/rule.ex
  - rulestead/lib/rulestead/ruleset/condition.ex
  - rulestead/lib/rulestead/ruleset/variant.ex
  - rulestead/lib/rulestead/ruleset/rollout.ex
  - rulestead/priv/repo/migrations/20260423020100_create_rulestead_authoring_tables.exs
  - rulestead/priv/repo/migrations/20260423020200_seed_default_environments.exs
autonomous: true
requirements:
  - STORE-01
  - ADMIN-08
must_haves:
  truths:
    - "A single flag identity can carry different active behavior per environment without duplicating the flag."
    - "Rulesets persist as immutable versioned publishing units with ordered embedded rule data."
    - "Environment groundwork for later admin UI exists in the core data model, but no UI ships in Phase 2."
  artifacts:
    - path: "rulestead/lib/rulestead/flag_environment.ex"
      provides: "Per-environment behavior anchor joining one flag identity to one environment"
    - path: "rulestead/lib/rulestead/ruleset.ex"
      provides: "Versioned ruleset schema with embedded rule graph"
    - path: "rulestead/priv/repo/migrations/20260423020100_create_rulestead_authoring_tables.exs"
      provides: "Authoring-store relational schema and constraints"
  key_links:
    - from: "rulestead/lib/rulestead/flag_environment.ex"
      to: "rulestead/lib/rulestead/ruleset.ex"
      via: "active_ruleset_id pointer"
      pattern: "belongs_to :active_ruleset"
    - from: "rulestead/lib/rulestead/ruleset.ex"
      to: "rulestead/lib/rulestead/ruleset/rule.ex"
      via: "embedded ordered rules graph"
      pattern: "embeds_many :rules"
---

<objective>
Implement the Phase 2 authoring data model: relational entities for reusable identities and lifecycle, plus embedded ordered rule documents inside immutable rulesets.

Purpose: The fake and Ecto adapters need a shared domain model that preserves one-flag-many-environments semantics and the validation rules they must both honor.
Output: Schema modules and migrations for the complete Phase 2 authoring model.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-CONTEXT.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-PATTERNS.md
@rulestead/lib/rulestead/store.ex
@rulestead/lib/rulestead/store/command.ex
@prompts/rulestead-domain-language-field-guide.md
@prompts/ecto-best-practices-deep-research.md
@prompts/elixir-plug-ecto-phoenix-system-design-best-practices-deep-research.md

<interfaces>
From `rulestead/lib/rulestead/store/command.ex` created in Plan 02-02:

```elixir
defmodule Rulestead.Store.Command.FetchFlag do
  defstruct [:flag_key, :environment_key]
end

defmodule Rulestead.Store.Command.SaveRulesetDraft do
  defstruct [:flag_key, :environment_key, :ruleset, :actor]
end

defmodule Rulestead.Store.Command.PublishRuleset do
  defstruct [:flag_key, :environment_key, :ruleset_version, :actor]
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the relational authoring schemas around flag identity and environment scope</name>
  <files>rulestead/lib/rulestead/flag.ex, rulestead/lib/rulestead/environment.ex, rulestead/lib/rulestead/flag_environment.ex, rulestead/lib/rulestead/audience.ex, rulestead/lib/rulestead/audit_event.ex</files>
  <action>Implement the relational boundary exactly per D-01 through D-04 and D-08 through D-09. `Flag` owns the canonical identity and lifecycle fields. `Environment` is a first-class schema with host-defined keys supported. `FlagEnvironment` joins one flag to one environment and carries the active ruleset pointer plus env-scoped status placeholders. `Audience` stays relational and reusable. `AuditEvent` lands as the append-only authoring history row that later phases can extend. Keep these schemas thin: fields, embeds, associations, and changesets only. Do not normalize rule nodes into standalone tables and do not add admin UI behavior or Phase 7 kill-switch workflows beyond schema affordances.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/schema/flag_environment_test.exs test/rulestead/schema/environment_test.exs'</automated>
  </verify>
  <done>The relational authoring model expresses one canonical flag identity, reusable audiences, and explicit per-environment behavior anchors.</done>
</task>

<task type="auto">
  <name>Task 2: Create immutable ruleset schemas with embedded ordered rule graphs</name>
  <files>rulestead/lib/rulestead/ruleset.ex, rulestead/lib/rulestead/ruleset/rule.ex, rulestead/lib/rulestead/ruleset/condition.ex, rulestead/lib/rulestead/ruleset/variant.ex, rulestead/lib/rulestead/ruleset/rollout.ex</files>
  <action>Implement the hybrid persistence shape from D-05 through D-09. `Ruleset` is the versioned publishing unit with embedded JSONB-backed rule data; the ordered owned graph is expressed via embedded schemas for `Rule`, `Condition`, `Variant`, and `Rollout`. Publishing produces a new immutable ruleset version rather than mutating embedded data in place per D-07. Embedded rules may reference reusable `Audience` records by stable key/ID, but audience definitions themselves stay relational. Keep variant-weight validation and rollout/bucketing config validation in the changeset layer so both adapters can reuse the same ruleset semantics later.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/schema/ruleset_test.exs'</automated>
  </verify>
  <done>The data model preserves document-style ordered rule evaluation while keeping reusable entities relational and future-friendly.</done>
</task>

<task type="auto">
  <name>Task 3: Add migrations for authoring tables, constraints, and default environments</name>
  <files>rulestead/priv/repo/migrations/20260423020100_create_rulestead_authoring_tables.exs, rulestead/priv/repo/migrations/20260423020200_seed_default_environments.exs</files>
  <action>Create the Phase 2 migrations that back the schemas and enforce the real database invariants. Use UUID primary keys with `gen_random_uuid()` defaults, relational tables for `flags`, `environments`, `flag_environments`, `rulesets`, `audiences`, and `audit_events`, JSONB columns for embedded ruleset documents, and real indexes/constraints for uniqueness and immutability. The first migration should own the table structure and partial-unique/index rules; the second should seed the conventional environments idempotently for library tests and installer-generated hosts while preserving the ability to add host-defined environment keys later. Do not add snapshot, cache, router, endpoint, or admin-mount wiring here.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix ecto.drop --quiet && mix ecto.create && mix ecto.migrate && mix test test/rulestead/schema/migration_smoke_test.exs'</automated>
  </verify>
  <done>This plan leaves the repo with an executable authoring schema and migrations that encode the locked Phase 2 relational/document boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter writes→database constraints | Untrusted application/store payloads cross into persisted authoring data |
| ruleset JSON→future evaluator/runtime | Stored embedded rule documents will later be compiled and evaluated by downstream phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | T | `rulesets` embedded document | mitigate | Validate rule/condition/variant/rollout shape in embedded changesets and back it with JSONB + immutable-version modeling per D-05 through D-07. |
| T-02-08 | E | `flag_environments` join table | mitigate | Enforce one `{flag_id, environment_id}` row with a real unique index so callers cannot create shadow environment state. |
| T-02-09 | R | `audit_events` seed/model groundwork | mitigate | Keep audit rows append-oriented and separate from mutable flag/ruleset tables so later phases can rely on stable change history. |
</threat_model>

<verification>
Run the migration smoke test on a clean local database, then confirm only schema/migration files changed and no admin UI or runtime cache files appeared.
</verification>

<success_criteria>
The Phase 2 data model fully encodes the locked one-flag-many-environments design, the hybrid relational/embedded ruleset boundary, and the environment schema groundwork needed later by admin UI phases.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-03-SUMMARY.md`
</output>
