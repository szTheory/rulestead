---
phase: 17
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/*_create_analytics_events.exs
  - lib/rulestead/analytics/event.ex
  - lib/rulestead/analytics/event_mapper.ex
  - test/rulestead/analytics/event_mapper_test.exs
autonomous: true
requirements:
  - ANA-01
must_haves:
  truths:
    - "Events are stored with Ecto UUIDv4 and accurate occurred_at timestamps."
    - "Raw event maps can be deterministically transformed into Ecto insertable maps."
  artifacts:
    - path: "lib/rulestead/analytics/event.ex"
      provides: "Ecto schema for analytics events"
    - path: "lib/rulestead/analytics/event_mapper.ex"
      provides: "Pure mapping functions for insert_all"
  key_links:
    - from: "lib/rulestead/analytics/event_mapper.ex"
      to: "Ecto.UUID.generate/0"
      via: "UUIDv4 generation"
---

<objective>
Build the persistence layer and mapping functions for metrics ingestion.
Purpose: We need an optimized database structure and pure functional mappers to format raw incoming metrics before they hit the database.
Output: Ecto schema and test-driven event mapper.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-analytics-and-metrics-ingestion/RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API to Mapper | Mapper receives untrusted map payload from host application |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | Analytics.EventMapper | mitigate | Ensure strict type casting and validation boundaries on data before returning the insert map. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Analytics Event Schema and Migration</name>
  <files>lib/rulestead/analytics/event.ex</files>
  <action>
    Run `mix ecto.gen.migration create_analytics_events` to create the `rulestead_analytics_events` table (id binary_id primary key, kind string, actor_id string, event_name string, env string, metadata map, occurred_at utc_datetime_usec, inserted_at/updated_at). Ensure `occurred_at` is indexed. Create the `Rulestead.Analytics.Event` schema mapping to this table, explicitly referencing the analog `rulestead/lib/rulestead/audit_event.ex` from `17-PATTERNS.md` to match established Ecto schema conventions in the project. Use Ecto's native UUIDv4 (`Ecto.UUID`) for id without `autogenerate: true` for timestamps, since we'll use `insert_all`.
  </action>
  <verify>
    <automated>mix ecto.migrate</automated>
  </verify>
  <done>Migration runs successfully and schema matches database table</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Event Mapper</name>
  <files>lib/rulestead/analytics/event_mapper.ex, test/rulestead/analytics/event_mapper_test.exs</files>
  <action>
    Implement `to_insert_map(raw_event)` as pure functional mapping logic. Ensure it safely extracts required values, handles both "exposure" and "custom" kinds, and returns a map with explicit `:id` (via `Ecto.UUID.generate/0`), `:occurred_at` (now or provided), `:inserted_at`, and `:updated_at`. Write tests asserting that the returned map contains all explicit fields necessary for `Repo.insert_all`.
  </action>
  <verify>
    <automated>mix test test/rulestead/analytics/event_mapper_test.exs</automated>
  </verify>
  <done>Event mapper correctly transforms maps with explicitly set ID and timestamps and all tests pass</done>
</task>

</tasks>

<verification>
mix test test/rulestead/analytics/event_mapper_test.exs
</verification>

<success_criteria>
- Migration exists and applies.
- Event mapper successfully transforms raw maps into `insert_all` compatible maps with explicit IDs and timestamps.
</success_criteria>

<output>
After completion, create `.planning/phases/17-analytics-and-metrics-ingestion/17-01-SUMMARY.md`
</output>