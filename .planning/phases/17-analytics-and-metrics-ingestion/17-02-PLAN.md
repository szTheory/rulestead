---
phase: 17
plan: 02
type: tdd
wave: 2
depends_on: ["17-01"]
files_modified:
  - lib/rulestead/analytics/batcher.ex
  - test/rulestead/analytics/batcher_test.exs
  - lib/rulestead/analytics/telemetry_handler.ex
  - test/rulestead/analytics/telemetry_handler_test.exs
autonomous: true
requirements:
  - ANA-01
must_haves:
  truths:
    - "Evaluation impressions are captured natively without coupling to core logic."
    - "Events are buffered in a non-blocking ETS table."
  artifacts:
    - path: "lib/rulestead/analytics/batcher.ex"
      provides: "ETS Buffer and GenServer flusher"
    - path: "lib/rulestead/analytics/telemetry_handler.ex"
      provides: "Telemetry hook for Rulestead evaluations"
  key_links:
    - from: "lib/rulestead/analytics/telemetry_handler.ex"
      to: "lib/rulestead/analytics/batcher.ex"
      via: "Non-blocking ETS insert"
    - from: "lib/rulestead/analytics/batcher.ex"
      to: "Ecto.Repo"
      via: "Repo.insert_all"
---

<objective>
Implement the high-throughput, non-blocking ingestion buffer and telemetry hook.
Purpose: To allow high-volume flag impressions to be captured asynchronously without exhausting the database connection pool.
Output: Batcher (GenServer/ETS) and Telemetry handler.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-analytics-and-metrics-ingestion/RESEARCH.md
@.planning/phases/17-analytics-and-metrics-ingestion/17-PATTERNS.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Buffer Limits | ETS table must not exhaust BEAM memory under heavy load or DB failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Denial of Service | Analytics.Batcher | mitigate | Enforce maximum `ets.info(:size)` limit; drop events if buffer is full to prevent memory exhaustion. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Analytics Batcher</name>
  <files>lib/rulestead/analytics/batcher.ex, test/rulestead/analytics/batcher_test.exs</files>
  <action>
    Implement a GenServer with an ETS table buffer. `init/1` creates a `:public, :set, :write_concurrency` ETS table. Expose a non-blocking `insert(event)` interface using a monotonic unique integer as key. Run a periodic flusher via `handle_info(:flush, state)` (e.g., every 5s) that atomically selects up to 2000 items, extracts them, formats via `EventMapper`, and calls `Repo.insert_all`. Drop events if the ETS table size exceeds the configured max (e.g., 50,000). Explicitly reference the analog `rulestead/lib/rulestead/telemetry/cache.ex` in action logic for how to handle ETS correctly. Write tests asserting that `insert/1` populates ETS and `flush` drains and writes to DB.
  </action>
  <verify>
    <automated>mix test test/rulestead/analytics/batcher_test.exs</automated>
  </verify>
  <done>Batcher GenServer buffers to ETS without blocking and correctly flushes payloads via EventMapper</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Telemetry Handler</name>
  <files>lib/rulestead/analytics/telemetry_handler.ex, test/rulestead/analytics/telemetry_handler_test.exs</files>
  <action>
    Create a module that exposes `attach/0` to attach to `[:rulestead, :eval, :decide, :stop]`. Implement `handle_event/4` to pluck only specific targeting metadata (targeting_key, flag_key, environment, variant, experiment_bucket) to prevent PII leakage. Map this to an exposure event map and call `Rulestead.Analytics.Batcher.insert/1`. Explicitly reference the analog `rulestead/lib/rulestead/telemetry/telemetry.ex` to align with the framework's existing telemetry hook conventions. Test by providing mock telemetry payload to `handle_event/4` and asserting it forms the correct map and inserts it.
  </action>
  <verify>
    <automated>mix test test/rulestead/analytics/telemetry_handler_test.exs</automated>
  </verify>
  <done>Telemetry handler correctly hooks and pushes exposure events to Batcher without PII leakage</done>
</task>

</tasks>

<verification>
mix test test/rulestead/analytics/batcher_test.exs test/rulestead/analytics/telemetry_handler_test.exs
</verification>

<success_criteria>
- Batcher can process events asynchronously into the database.
- Memory protection exists via ETS size limit.
- Telemetry captures impressions correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/17-analytics-and-metrics-ingestion/17-02-SUMMARY.md`
</output>