---
phase: 17
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - lib/rulestead/analytics.ex
  - lib/rulestead.ex
  - lib/rulestead/application.ex
  - test/rulestead/analytics_test.exs
  - test/rulestead_test.exs
autonomous: true
requirements:
  - ANA-02
must_haves:
  truths:
    - "Host apps can submit custom analytics events using Rulestead.track/3."
    - "The Batcher starts gracefully with the main supervision tree."
    - "The TelemetryHandler automatically attaches on application start."
  artifacts:
    - path: "lib/rulestead.ex"
      provides: "Public track/3 API"
    - path: "lib/rulestead/application.ex"
      provides: "Startup wiring"
  key_links:
    - from: "lib/rulestead.ex"
      to: "lib/rulestead/analytics.ex"
      via: "Delegation of track/3"
    - from: "lib/rulestead/application.ex"
      to: "lib/rulestead/analytics/batcher.ex"
      via: "Supervisor child spec"
---

<objective>
Expose the public `track/3` API boundary for host applications and wire the components into the application's supervision tree.
Purpose: Finalize integration of analytics by connecting the external seams and bootstrapping the services.
Output: Integrated `track/3` API and wired supervision tree.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-analytics-and-metrics-ingestion/RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Surface | Host apps interact with the public track/3 API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Information Disclosure | track/3 | accept | Assume host apps control metadata in track/3. Rely on host to avoid sending PII. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Public Analytics Interface</name>
  <files>lib/rulestead/analytics.ex, lib/rulestead.ex, test/rulestead/analytics_test.exs</files>
  <action>
    Create `lib/rulestead/analytics.ex` implementing `track(context_or_actor_id, event_name, metadata \\ %{})`. It extracts the `actor_id` from the context, constructs an event map `%{kind: "custom", actor_id: actor_id, event_name: event_name, metadata: metadata}`, and passes it to `Rulestead.Analytics.Batcher.insert/1`. Update `lib/rulestead.ex` to expose `@spec track(...)` delegating to `Rulestead.Analytics.track/3`. Explicitly reference the analog `rulestead/lib/rulestead.ex` when modifying `analytics.ex` to align with the core module's delegation and domain API patterns. Test that tracking events successfully buffers them in the Batcher.
  </action>
  <verify>
    <automated>mix test test/rulestead/analytics_test.exs</automated>
  </verify>
  <done>track/3 is exposed on Rulestead and correctly pushes to Batcher, tested</done>
</task>

<task type="auto">
  <name>Task 2: Supervision and Wiring</name>
  <files>lib/rulestead/application.ex</files>
  <action>
    Modify `lib/rulestead/application.ex`. Add `Rulestead.Analytics.Batcher` to the children list to start on boot. After starting the supervisor tree, ensure `Rulestead.Analytics.TelemetryHandler.attach()` is called (either in `start/2` or during component init) so the hook is active.
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>Supervision tree starts correctly and full test suite passes</done>
</task>

</tasks>

<verification>
mix test
</verification>

<success_criteria>
- `Rulestead.track/3` delegates properly.
- Background worker is supervised.
- Full test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/17-analytics-and-metrics-ingestion/17-03-SUMMARY.md`
</output>