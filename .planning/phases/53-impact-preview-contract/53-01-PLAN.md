---
phase: 53-impact-preview-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/targeting/impact_preview.ex
  - rulestead/lib/rulestead/targeting/audience_dependencies.ex
  - rulestead/test/rulestead/targeting/impact_preview_test.exs
requirements:
  - IMP-01
  - IMP-02
  - IMP-04
autonomous: true
must_haves:
  truths:
    - "Impact preview payloads expose environment scope, tenant scope, affected references, preview basis, uncertainty, redacted sample evidence, and stable preview fingerprints."
    - "Preview fingerprints are stale-resistant and change when audience definition, scope, affected references, or explicit sample evidence changes."
    - "Affected reference summaries are stable-sorted and support-safe without authoritative population counts."
  artifacts:
    - path: "rulestead/lib/rulestead/targeting/impact_preview.ex"
      provides: "Pure audience impact preview contract and fingerprint/token helpers"
      exports: ["schema_version/0", "preview_fingerprint/1", "build/1", "finding/4"]
    - path: "rulestead/lib/rulestead/targeting/audience_dependencies.ex"
      provides: "Pure affected-reference summary helpers over authored flag/ruleset state"
      exports: ["summarize/2", "reference_keys/1"]
    - path: "rulestead/test/rulestead/targeting/impact_preview_test.exs"
      provides: "Unit proof for preview shape, redaction, determinism, scope binding, and stable ordering"
  key_links:
    - from: "rulestead/lib/rulestead/targeting/impact_preview.ex"
      to: "rulestead/lib/rulestead/promotion/compare.ex"
      via: "same deterministic normalize/hash/finding pattern"
      pattern: "audprev_"
    - from: "rulestead/lib/rulestead/targeting/audience_dependencies.ex"
      to: "rules with strategy :segment_match and audience_key"
      via: "authored-state scan only"
      pattern: "segment_match"
---

<objective>
Create the pure Phase 53 audience impact-preview contract and affected-reference summary helpers.

Purpose: Operators need truthful, stale-resistant preview payloads before any audience mutation is allowed, and downstream store adapters need one deterministic contract to implement.
Output: New pure targeting modules plus focused ExUnit coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-impact-preview-contract/53-RESEARCH.md
@.planning/phases/53-impact-preview-contract/53-PATTERNS.md
@prompts/rulestead-security-privacy-and-threat-model.md
@prompts/rulestead-telemetry-observability-and-audit.md
@prompts/rulestead-domain-language-field-guide.md

<interfaces>
From `rulestead/lib/rulestead/promotion/compare.ex`:
```elixir
def fingerprint(term), do: "sha256:" <> hash_term(term)
def compare_token(attrs) when is_map(attrs), do: "cmp_" <> hash_term(token_payload)
def finding(severity, class, code, attrs \\ %{}), do: %{severity: severity, class: class, code: code}
```

From `rulestead/lib/rulestead/ruleset/rule.ex`:
```elixir
@strategies [:forced_value, :percentage_rollout, :variant_split, :segment_match, :experiment]
field(:audience_key, :string)
```

Phase boundary: do not add mounted admin screens, graph visualization, normalized dependency truth tables, host identity/observability lookups, or authoritative affected-user counts.
</interfaces>
</context>

<source_audit>
GOAL: covered by pure preview payloads that make audience mutations understandable before apply.
REQ IMP-01: covered by preview payload fields and redacted evidence.
REQ IMP-02: covered by preview fingerprint/token basis and blocker finding shape.
REQ IMP-03: not in this plan; covered by Plan 02 and Plan 04.
REQ IMP-04: covered by support-safe preview evidence shape consumed by audit in Plan 04.
RESEARCH: covers pure impact preview module, dependency summary helpers, Compare-style fingerprinting, stable sorting, redaction, and false-precision avoidance.
CONTEXT decisions: no CONTEXT.md exists; STATE/PROJECT decisions honored: authored-state and explicit-sample previews only, no identity/observability ownership, no Phase 55 UI.
Deferred/out-of-scope: no IMP-05 richer host-supplied cohort preview, no graph visualizer, no Phase 54 normalized inventory.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic audience impact preview contract</name>
  <files>rulestead/lib/rulestead/targeting/impact_preview.ex, rulestead/test/rulestead/targeting/impact_preview_test.exs</files>
  <read_first>
    - rulestead/lib/rulestead/promotion/compare.ex
    - rulestead/lib/rulestead/admin/redaction.ex
    - rulestead/lib/rulestead/audit_event.ex
    - rulestead/test/rulestead/promotion/compare_test.exs
    - rulestead/lib/rulestead/targeting/impact_preview.ex
    - rulestead/test/rulestead/targeting/impact_preview_test.exs
  </read_first>
  <behavior>
    - Test `preview_fingerprint/1` returns a string beginning with `audprev_` and is stable when map key order changes.
    - Test the fingerprint changes when `environment_key`, `tenant_key`, `audience_key`, `before_definition`, `after_definition`, `affected_references`, or redacted sample evidence changes.
    - Test `build/1` returns `preview_schema_version`, `preview_fingerprint`, `environment_scope`, `tenant_scope`, `audience_key`, `preview_basis`, `uncertainty`, `sample_evidence`, `affected_references`, and `findings`.
    - Test sample evidence drops sensitive keys such as `email`, `phone`, `session`, `session_token`, and `socket_session`.
  </behavior>
  <action>
    Create `Rulestead.Targeting.ImpactPreview` with `@schema_version 1`, `schema_version/0`, `fingerprint/1`, `preview_fingerprint/1`, `build/1`, and `finding/4`. Use the `Rulestead.Promotion.Compare` normalization/hash style, but prefix preview tokens with `audprev_`. `preview_fingerprint/1` must hash a map containing exactly `schema_version`, `environment_key`, `tenant_key`, `audience_key`, `operation`, `before_fingerprint`, `after_fingerprint`, `affected_reference_keys`, `sample_fingerprint`, and `preview_basis`. `build/1` must accept atom/string keys for `:environment_key`, `:tenant_key`, `:audience_key`, `:operation`, `:before_definition`, `:after_definition`, `:affected_references`, `:samples`, `:preview_basis`, and `:findings`, normalize blank strings to `nil`, normalize missing samples to `[]`, and set `uncertainty` to `%{basis: preview_basis, authoritative_population_count?: false, message: "authored-state and explicit-sample preview only"}`. Redact samples using the existing `Rulestead.Admin.Redaction.redact_metadata/2` helper and/or the `Rulestead.AuditEvent.metadata/1` sensitive-key scrubbing pattern; if a wrapper is needed, make it an explicit allowlist wrapper around those existing helpers for support-safe preview fields. Do not add an independent sample-scrubbing implementation, do not store raw samples, and do not claim population counts. Use canonical audience vocabulary from the field guide.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/targeting/impact_preview_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "defmodule Rulestead.Targeting.ImpactPreview|@schema_version 1|def schema_version|def preview_fingerprint|audprev_|authoritative_population_count\\?: false" rulestead/lib/rulestead/targeting/impact_preview.ex` finds all listed strings.
    - `rg -n "Rulestead.Admin.Redaction|AuditEvent.metadata|redact_metadata" rulestead/lib/rulestead/targeting/impact_preview.ex` proves preview redaction uses the existing redaction/audit scrub pattern.
    - `rg -n "email|phone|session_token|socket_session" rulestead/test/rulestead/targeting/impact_preview_test.exs` finds explicit assertions that PII keys are removed from `sample_evidence`.
    - `cd rulestead && mix test test/rulestead/targeting/impact_preview_test.exs` passes.
  </acceptance_criteria>
  <done>Pure preview contract is deterministic, scope-bound, redacted, and proves IMP-01/IMP-02 payload behavior without store or UI dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authored-state affected-reference summaries</name>
  <files>rulestead/lib/rulestead/targeting/audience_dependencies.ex, rulestead/test/rulestead/targeting/impact_preview_test.exs</files>
  <read_first>
    - rulestead/lib/rulestead/targeting/audience_dependencies.ex
    - rulestead/lib/rulestead/targeting/impact_preview.ex
    - rulestead/lib/rulestead/promotion/compare.ex
    - rulestead/lib/rulestead/manifest/import.ex
    - rulestead/lib/rulestead/ruleset/rule.ex
    - rulestead/test/rulestead/store/compare_contract_test.exs
  </read_first>
  <behavior>
    - Test `summarize/2` finds authored rules where `strategy` is `:segment_match` or `"segment_match"` and `audience_key` equals the target audience.
    - Test summaries include stable `reference_key`, `flag_key`, `ruleset_version`, `ruleset_status`, `rule_key`, `rollout_context`, `lifecycle_context`, `environment_key`, and `tenant_key`.
    - Test summaries sort by `{environment_key, tenant_key, flag_key, ruleset_version, rule_key}` and never include raw actor/sample evidence.
  </behavior>
  <action>
    Create `Rulestead.Targeting.AudienceDependencies` with `summarize(audience_key, authored_flags_or_payloads)` and `reference_keys(references)`. Accept authored flag payload maps shaped like existing compare payloads: `%{flag: %{key: ...}, active_ruleset: %{version: ..., status: ..., rules: [...]}, environment_key: ..., tenant_key: ...}`. Scan only the passed authored state; do not query `Rulestead`, `Store`, `Repo`, `rulestead_admin`, host identity, telemetry, observability, or audit. Include blocker-friendly metadata fields `reference_key: "flag:<flag_key>:ruleset:<version>:rule:<rule_key>"`, `resource_type: "flag"`, `rule_strategy: "segment_match"`, `rollout_context`, and `lifecycle_context`, using `%{available?: false}` if context is absent. Add tests in `impact_preview_test.exs` that call this helper and then pass its output to `ImpactPreview.build/1`.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/targeting/impact_preview_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "defmodule Rulestead.Targeting.AudienceDependencies|def summarize|def reference_keys|segment_match|reference_key" rulestead/lib/rulestead/targeting/audience_dependencies.ex` finds all listed strings.
    - `rg -n "Rulestead\\.(Store|Repo|Admin)|Repo\\.|Store\\.|Telemetry|Observability|rulestead_admin" rulestead/lib/rulestead/targeting/audience_dependencies.ex` returns no forbidden live lookup dependency; the module namespace `Rulestead.Targeting.AudienceDependencies` itself is allowed.
    - `cd rulestead && mix test test/rulestead/targeting/impact_preview_test.exs` passes.
  </acceptance_criteria>
  <done>Affected-reference summaries are authored-state only, stable-sorted, support-safe, and ready for store adapters to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator/Core caller -> pure preview contract | Untrusted command/sample evidence enters preview payload construction. |
| Authored state -> affected-reference summary | Stored flag/ruleset data is summarized for operator/audit surfaces. |
| Preview payload -> downstream mutation apply | Fingerprints become security-sensitive confirmation evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | `ImpactPreview.preview_fingerprint/1` | mitigate | Bind token to schema version, environment, tenant, audience key, operation, before/after definition fingerprints, affected references, sample fingerprint, and preview basis. |
| T-53-02 | Elevation of privilege | Preview confirmation scope | mitigate | Include explicit `environment_key` and `tenant_key` in preview payload and fingerprint; Plan 03/04 reject mismatches. |
| T-53-03 | Information Disclosure | `sample_evidence` | mitigate | Redact sensitive keys recursively and expose only support-safe evidence; never include raw sample evidence. |
| T-53-04 | Tampering | Runtime lookup drift | mitigate | This plan uses pure authored-state helpers only; Plan 02 proves evaluator does not perform live lookups. |
| T-53-05 | Repudiation | Audit preview evidence | mitigate | Provide stable preview fingerprint and affected-reference summary fields consumed by Plan 04 audit writes. |
| T-53-06 | Elevation of privilege | Protected shared-targeting mutation bypass | mitigate | Emit blocker finding shape and preview token basis required by apply commands in Plan 03/04. |
</threat_model>

<verification>
Run `cd rulestead && mix test test/rulestead/targeting/impact_preview_test.exs`.
</verification>

<success_criteria>
Phase 53 has a pure, deterministic preview contract and dependency summary foundation that covers IMP-01, the token basis for IMP-02, and audit evidence inputs for IMP-04 without schema changes or runtime lookups.
</success_criteria>

<output>
After completion, create `.planning/phases/53-impact-preview-contract/53-01-SUMMARY.md`.
</output>
