---
phase: 29-tenancy-helpers-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - rulestead/lib/rulestead/manifest/import.ex
  - rulestead/lib/rulestead/manifest/plan.ex
  - rulestead/lib/rulestead/promotion/compare.ex
  - rulestead/lib/rulestead/promotion/apply.ex
  - rulestead/lib/rulestead/audit_event.ex
  - rulestead/lib/rulestead/store/command.ex
  - rulestead/lib/rulestead/admin/authorizer.ex
  - rulestead/test/rulestead/manifest/import_test.exs
  - rulestead/test/rulestead/promotion/compare_test.exs
  - rulestead/test/rulestead/promotion/apply_test.exs
  - rulestead/test/rulestead/store/manifest_import_contract_test.exs
  - rulestead/test/rulestead/store/promotion_apply_contract_test.exs
  - rulestead/test/rulestead/audit_event_governance_test.exs
  - rulestead/test/rulestead/release_contract_test.exs
  - rulestead_admin/lib/rulestead_admin/live/session.ex
  - rulestead_admin/lib/rulestead_admin/components/shell.ex
  - rulestead_admin/test/rulestead_admin/live/session_test.exs
autonomous: true
requirements:
  - TEN-01
  - TEN-02
  - TEN-03
must_haves:
  truths:
    - "Compare, import, and promotion flows classify tenant-sensitive invalid states with one stable vocabulary and reject drift before mutation."
    - "Saved plans and audit events persist only bounded tenant provenance, not tenant catalogs, labels, or session baggage."
    - "Mounted admin resolves tenant scope from host-provided allowed values, keeps tenant separate from environment, and fails closed on invalid scope."
    - "The public mounted-admin policy seam remains `Rulestead.Admin.Policy.can?/4` with tenant awareness flowing through normalized resource/context inputs instead of signature churn."
  artifacts:
    - path: "rulestead/lib/rulestead/manifest/import.ex"
      provides: "Tenant-aware import preview and apply revalidation using the canonical finding vocabulary"
    - path: "rulestead/lib/rulestead/promotion/compare.ex"
      provides: "Tenant-aware compare findings and reviewed-artifact scope semantics shared with import/promotion apply"
    - path: "rulestead/lib/rulestead/manifest/plan.ex"
      provides: "Bounded tenant provenance persisted in saved plans"
    - path: "rulestead/lib/rulestead/audit_event.ex"
      provides: "Bounded tenant provenance mirrored into audit metadata while preserving redaction guarantees"
    - path: "rulestead_admin/lib/rulestead_admin/live/session.ex"
      provides: "Mounted admin tenant resolution that mirrors the existing environment selector posture"
    - path: "rulestead_admin/lib/rulestead_admin/components/shell.ex"
      provides: "Visible current tenant scope via chip or picker without introducing all-tenant UX"
  key_links:
    - from: "rulestead/lib/rulestead/manifest/import.ex"
      to: "rulestead/lib/rulestead/manifest/plan.ex"
      via: "preview writes bounded reviewed tenant scope into the plan and apply revalidates exact live scope before mutation"
      pattern: "tenant|plan|drift|scope"
    - from: "rulestead/lib/rulestead/promotion/compare.ex"
      to: "rulestead/lib/rulestead/promotion/apply.ex"
      via: "compare and apply share one reviewed-artifact tenant finding and drift vocabulary"
      pattern: "tenant|compare|stale|finding"
    - from: "rulestead/lib/rulestead/store/command.ex"
      to: "rulestead/lib/rulestead/audit_event.ex"
      via: "normalized tenant provenance survives command metadata shaping and remains bounded in the audit envelope"
      pattern: "tenant|metadata|scope_source"
    - from: "rulestead_admin/lib/rulestead_admin/live/session.ex"
      to: "rulestead/lib/rulestead/admin/authorizer.ex"
      via: "mounted admin resolves explicit tenant scope and passes it through normalized authorization inputs without callback-shape changes"
      pattern: "tenant|policy|authorizer"
---

<objective>
Complete the reviewed-artifact and operator-facing side of the bounded tenancy contract by unifying tenant validation, persisting bounded provenance metadata, and adding the minimum mounted admin tenant scope seam.

Purpose: Phase 29 must let operators review and apply exact tenant-scoped intent safely, then see that bounded scope reflected in plans, audit trails, and mounted admin state without broadening into a future tenancy control plane.
Output: shared tenant validation vocabulary, exact-scope revalidation at apply time, bounded tenant provenance in plans/audit metadata, mounted admin tenant resolution and shell display, and targeted regression coverage across both packages.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-tenancy-helpers-validation/29-CONTEXT.md
@.planning/phases/29-tenancy-helpers-validation/29-RESEARCH.md
@.planning/phases/29-tenancy-helpers-validation/29-PATTERNS.md
@.planning/phases/25-tenancy-helpers-validation/25-02-PLAN.md
@.planning/phases/22-environment-compare-conflict-model/22-CONTEXT.md
@.planning/phases/23-governed-promotion-apply/23-CONTEXT.md
@.planning/phases/24-gitops-manifests-cli-surface/24-CONTEXT.md
@.planning/phases/27-comprehensive-rbac-security-hardening/27-CONTEXT.md
@rulestead/lib/rulestead/manifest/import.ex
@rulestead/lib/rulestead/manifest/plan.ex
@rulestead/lib/rulestead/promotion/compare.ex
@rulestead/lib/rulestead/promotion/apply.ex
@rulestead/lib/rulestead/audit_event.ex
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/admin/authorizer.ex
@rulestead_admin/lib/rulestead_admin/live/session.ex
@rulestead_admin/lib/rulestead_admin/components/shell.ex
@rulestead/test/rulestead/manifest/import_test.exs
@rulestead/test/rulestead/promotion/apply_test.exs
@rulestead/test/rulestead/store/promotion_apply_contract_test.exs
@rulestead_admin/test/rulestead_admin/live/session_test.exs

<interfaces>
Current saved-plan persistence:
```elixir
|> maybe_put("tenant_key", fetch_optional_string(plan, "tenant_key"))
```

Current import findings:
```elixir
Result.finding("widened_tenant_scope", "blocker", ...)
Result.finding("mismatched_tenant_scope", "blocker", ...)
```

Current mounted admin route shape:
```elixir
params |> Map.put("env", env_key) |> URI.encode_query()
```

Locked Phase 29 contract:
```text
reviewed tenant scope must be exact at apply time, tenant provenance must stay bounded, and mounted admin tenant scope must come from host-authorized allowed values with no implicit all-tenant mode
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify tenant validation and reviewed-scope revalidation across compare, import, and apply</name>
  <files>rulestead/lib/rulestead/manifest/import.ex, rulestead/lib/rulestead/manifest/plan.ex, rulestead/lib/rulestead/promotion/compare.ex, rulestead/lib/rulestead/promotion/apply.ex, rulestead/test/rulestead/manifest/import_test.exs, rulestead/test/rulestead/promotion/compare_test.exs, rulestead/test/rulestead/promotion/apply_test.exs, rulestead/test/rulestead/store/manifest_import_contract_test.exs, rulestead/test/rulestead/store/promotion_apply_contract_test.exs</files>
  <behavior>
    - Test 1: compare and import preview surfaces emit the canonical tenant findings `widened_tenant_scope`, `mismatched_tenant_scope`, and `tenant_scope_drifted` where appropriate.
    - Test 2: apply rejects or returns stale for exact reviewed-scope drift before mutation, preserving Fake and Ecto parity.
    - Test 3: saved plans persist only bounded tenant provenance fields needed to explain and revalidate reviewed tenant scope.
  </behavior>
  <action>Factor tenant-sensitive validation into one shared classifier model reused by import preview, promotion compare, and apply-time revalidation. Preserve the existing preview-first reviewed-artifact posture: operators and CI can inspect blocked or stale results, but mutation still fails closed when live scope diverges from reviewed scope. Extend `Rulestead.Manifest.Plan` only with bounded provenance fields such as reviewed `tenant_key`, `scope_source`, and bounded validation evidence. Do not persist tenant labels, allowed tenant catalogs, or freeform UI confirmation text.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/manifest/import_test.exs test/rulestead/promotion/compare_test.exs test/rulestead/promotion/apply_test.exs test/rulestead/store/manifest_import_contract_test.exs test/rulestead/store/promotion_apply_contract_test.exs</automated>
  </verify>
  <done>Tenant-sensitive invalid states are classified and revalidated consistently across compare, import, and apply, with bounded reviewed-scope metadata persisted in plans only where necessary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bounded tenant provenance to audit metadata and the minimum mounted admin tenant scope seam</name>
  <files>rulestead/lib/rulestead/audit_event.ex, rulestead/lib/rulestead/store/command.ex, rulestead/lib/rulestead/admin/authorizer.ex, rulestead/test/rulestead/audit_event_governance_test.exs, rulestead/test/rulestead/release_contract_test.exs, rulestead_admin/lib/rulestead_admin/live/session.ex, rulestead_admin/lib/rulestead_admin/components/shell.ex, rulestead_admin/test/rulestead_admin/live/session_test.exs</files>
  <behavior>
    - Test 1: normalized command and audit metadata carry bounded tenant provenance while still scrubbing session/socket-sensitive state.
    - Test 2: mounted admin resolves tenant scope from URL, remembered state, host defaults, and allowed tenant values in the same precedence pattern used for environments.
    - Test 3: route helpers and shell rendering keep tenant and environment separate and visible, using a read-only tenant chip when exactly one tenant is allowed.
    - Test 4: invalid or cross-tenant scope fails closed through authorization inputs without changing the public `Rulestead.Admin.Policy.can?/4` shape.
  </behavior>
  <action>Mirror the existing environment-resolution posture in mounted admin for tenant scope, but keep the host as the source of truth for allowed tenants and default tenant. Add only the minimum mounted-session and shell work needed to make tenant scope visible, deterministic, and separate from environment scope. On the backend side, extend normalized command and audit metadata so plans, apply flows, and audit events all speak the same bounded tenant provenance dialect. Keep authorization signature stability: tenant awareness must flow through normalized resource/context payloads, not a policy API redesign.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/audit_event_governance_test.exs test/rulestead/release_contract_test.exs && cd ../rulestead_admin && mix test test/rulestead_admin/live/session_test.exs</automated>
  </verify>
  <done>Operators can see and carry explicit tenant scope safely through mounted admin, and audit/plan artifacts expose the same bounded tenant provenance needed to explain reviewed actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewed preview -> saved plan artifact | Tenant scope leaves preview-time memory and becomes durable apply input here |
| saved plan -> apply path | Reviewed tenant scope re-enters a mutating path here |
| command metadata -> audit metadata | Operator-visible tenant provenance crosses into durable audit storage here |
| mounted admin params/session -> authorization | Explicit tenant scope chosen in the UI affects read/write guards here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | T | reviewed tenant scope | mitigate | Persist bounded reviewed scope fields and revalidate exact live/apply scope before mutation. |
| T-29-02-02 | I | audit and plan metadata | mitigate | Persist tenant IDs and bounded provenance enums only; keep labels, catalogs, and session baggage out. |
| T-29-02-03 | E | mounted admin scope resolution | mitigate | Require host-authorized allowed tenant values and fail closed on invalid scope instead of broadening access. |
| T-29-02-04 | R | explainability and review fidelity | mitigate | Use one stable tenant findings vocabulary and one bounded provenance dialect across compare, plans, apply, and audit. |
| T-29-02-05 | S | cross-tenant mutation intent | mitigate | Reuse preview-first apply discipline so wrong-tenant or widened-scope requests never reach mutation paths unchecked. |
</threat_model>

<verification>
Run the targeted import, promotion, contract, audit, release-contract, and mounted-session test suites. Confirm tenant findings are consistent across preview/apply flows, bounded provenance fields are persisted and scrubbed correctly, and mounted admin tenant handling stays explicit, host-bounded, and fail-closed.
</verification>

<success_criteria>
Phase 29 now carries explicit tenant scope safely through review, apply, audit, and mounted admin seams while preserving the current linked-version two-package release shape and refusing implicit all-tenant behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/29-tenancy-helpers-validation/29-02-SUMMARY.md`
</output>
