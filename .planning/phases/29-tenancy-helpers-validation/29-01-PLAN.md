---
phase: 29-tenancy-helpers-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/tenancy.ex
  - rulestead/lib/rulestead/tenancy/single_tenant.ex
  - rulestead/lib/rulestead/phoenix.ex
  - rulestead/lib/rulestead/live_view.ex
  - rulestead/lib/rulestead/oban.ex
  - rulestead/lib/rulestead/evaluator.ex
  - rulestead/test/rulestead/tenancy_test.exs
  - rulestead/test/rulestead/tenancy_property_test.exs
  - rulestead/test/rulestead/evaluator_test.exs
  - rulestead/test/rulestead/evaluator_property_test.exs
  - rulestead/test/rulestead/plug_test.exs
  - rulestead/test/rulestead/live_view_test.exs
  - rulestead/test/rulestead/oban_test.exs
  - rulestead/test/rulestead/release_contract_test.exs
autonomous: true
requirements:
  - TEN-01
  - TEN-03
must_haves:
  truths:
    - "The existing bounded tenancy seam remains the only place tenant scope is normalized, compared, and composed for runtime identity decisions."
    - "Single-tenant hosts keep the current nil-safe ergonomic default path with no new topology or setup burden."
    - "Tenant-aware runtime propagation and bucketing stay explicit, deterministic, and additive rather than ambient or schema-rewriting."
    - "Any tenant-local subject rebucketing is opt-in and documented, never a silent behavior change for legacy `bucket_by: :subject` rules."
  artifacts:
    - path: "rulestead/lib/rulestead/tenancy.ex"
      provides: "The completed runtime seam for normalized tenant scope, same-tenant guard helpers, and explicit bucket identity composition"
    - path: "rulestead/lib/rulestead/tenancy/single_tenant.ex"
      provides: "The safe default behavior that keeps single-tenant callers unchanged"
    - path: "rulestead/lib/rulestead/evaluator.ex"
      provides: "Explicit tenant and tenant-scoped-subject identity behavior without hidden rebucketing"
    - path: "rulestead/test/rulestead/tenancy_property_test.exs"
      provides: "Regression proof that tenant-aware identity composition stays deterministic and bounded"
  key_links:
    - from: "rulestead/lib/rulestead/phoenix.ex"
      to: "rulestead/lib/rulestead/tenancy.ex"
      via: "request-derived tenant scope is normalized through one explicit seam before entering `%Rulestead.Context{}`"
      pattern: "tenant|resolve|normalize"
    - from: "rulestead/lib/rulestead/live_view.ex"
      to: "rulestead/lib/rulestead/tenancy.ex"
      via: "socket/session tenant scope follows the same bounded seam as request context"
      pattern: "tenant|resolve|assign"
    - from: "rulestead/lib/rulestead/oban.ex"
      to: "rulestead/lib/rulestead/tenancy.ex"
      via: "job-bound context propagation preserves only bounded tenant scope fields"
      pattern: "tenant_key|context|serialize"
    - from: "rulestead/lib/rulestead/evaluator.ex"
      to: "rulestead/lib/rulestead/tenancy.ex"
      via: "bucket identity composition remains explicit and deterministic through the tenancy seam"
      pattern: "bucket|tenant|identity"
---

<objective>
Complete the runtime side of the bounded tenancy contract by tightening the existing seam, preserving the `SingleTenant` default, and making tenant-aware identity composition explicit.

Purpose: the repo already has real tenancy primitives, but they need one coherent runtime contract so request/socket/job propagation and evaluator identity semantics stay explicit, fail-closed, and aligned with the shipped two-package product shape.
Output: a completed runtime tenancy seam, explicit helper wiring, opt-in tenant-scoped subject composition, and targeted regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-tenancy-helpers-validation/29-CONTEXT.md
@.planning/phases/29-tenancy-helpers-validation/29-RESEARCH.md
@.planning/phases/29-tenancy-helpers-validation/29-PATTERNS.md
@.planning/phases/25-tenancy-helpers-validation/25-01-PLAN.md
@.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md
@rulestead/lib/rulestead/context.ex
@rulestead/lib/rulestead/tenancy.ex
@rulestead/lib/rulestead/tenancy/single_tenant.ex
@rulestead/lib/rulestead/phoenix.ex
@rulestead/lib/rulestead/live_view.ex
@rulestead/lib/rulestead/oban.ex
@rulestead/lib/rulestead/evaluator.ex
@rulestead/test/rulestead/tenancy_test.exs
@rulestead/test/rulestead/tenancy_property_test.exs
@rulestead/test/rulestead/evaluator_property_test.exs

<interfaces>
From `rulestead/lib/rulestead/context.ex`:
```elixir
defstruct actor: nil,
          targeting_key: nil,
          tenant_key: nil,
          environment: nil,
          attributes: %{},
          request_id: nil,
          session_id: nil,
          strict?: false
```

Current tenancy seam:
```elixir
@callback resolve_tenant(term()) :: tenant_scope()
@callback same_tenant?(tenant_scope(), tenant_scope()) :: boolean()
@callback tenant_topic(String.t(), tenant_scope()) :: String.t()
@callback compose_bucket_identity(Rulestead.Context.t(), atom() | String.t(), String.t() | nil) :: String.t() | nil
```

Locked Phase 29 contract:
```text
tenant_key stays canonical, SingleTenant stays the default, bucket_by: :tenant remains stable, and tenant-scoped subject rebucketing is explicit host opt-in only
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten the tenancy seam around normalized runtime scope and explicit propagation</name>
  <files>rulestead/lib/rulestead/tenancy.ex, rulestead/lib/rulestead/tenancy/single_tenant.ex, rulestead/lib/rulestead/phoenix.ex, rulestead/lib/rulestead/live_view.ex, rulestead/lib/rulestead/oban.ex, rulestead/test/rulestead/tenancy_test.exs, rulestead/test/rulestead/plug_test.exs, rulestead/test/rulestead/live_view_test.exs, rulestead/test/rulestead/oban_test.exs, rulestead/test/rulestead/release_contract_test.exs</files>
  <behavior>
    - Test 1: Plug, LiveView, and Oban helpers normalize tenant scope through `Rulestead.Tenancy` instead of ad hoc field copies.
    - Test 2: `SingleTenant` remains the zero-config default and does not force multi-tenant setup into existing callers.
    - Test 3: tenant normalization rejects blank or malformed scope and stays bounded across runtime context handoffs.
  </behavior>
  <action>Refine the existing runtime seam instead of rebuilding it. Keep `tenant_key` as the canonical runtime field while making sure all request/socket/job propagation routes through one explicit normalized seam for tenant scope, same-tenant comparisons, and any bounded helper metadata the runtime path needs. Preserve nil-safe single-tenant behavior and do not add tenant catalogs, topology state, or standalone admin concerns here. Reuse the current Phase 25 surfaces where they already fit the contract; only add helper surface area that is necessary to make runtime propagation coherent and testable.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/tenancy_test.exs test/rulestead/plug_test.exs test/rulestead/live_view_test.exs test/rulestead/oban_test.exs test/rulestead/release_contract_test.exs</automated>
  </verify>
  <done>Runtime helpers share one bounded tenancy seam, and existing single-tenant hosts keep working without new configuration burden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make tenant-aware bucket identity composition explicit and deterministic</name>
  <files>rulestead/lib/rulestead/tenancy.ex, rulestead/lib/rulestead/tenancy/single_tenant.ex, rulestead/lib/rulestead/evaluator.ex, rulestead/test/rulestead/tenancy_property_test.exs, rulestead/test/rulestead/evaluator_test.exs, rulestead/test/rulestead/evaluator_property_test.exs</files>
  <behavior>
    - Test 1: `bucket_by: :tenant` continues to hash by canonical `tenant_key` with the current deterministic behavior and missing-identity posture.
    - Test 2: explicit tenant-scoped subject composition only changes bucket identity when a host opts into that path through the seam.
    - Test 3: property coverage proves no silent rebucketing for existing subject-only behavior.
  </behavior>
  <action>Use the existing evaluator seam to encode the locked bucketing rules from Phase 29 context. Preserve current authored rule topology and `bucket_by` vocabulary while making tenant-wide bucketing and tenant-scoped subject composition explicit, additive behaviors. The key constraint is that legacy `bucket_by: :subject` semantics must not silently change. If tenant-local subject identity is supported, make it a clear host opt-in through the tenancy seam and document/test that enabling it rebuckets subject cohorts by design.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/tenancy_property_test.exs test/rulestead/evaluator_test.exs test/rulestead/evaluator_property_test.exs</automated>
  </verify>
  <done>Tenant-aware identity composition is explicit, deterministic, and bounded, with no ambient rebucketing or topology widening.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host request/socket/session -> `%Rulestead.Context{}` | Untrusted tenant scope first enters runtime evaluation here |
| `%Rulestead.Context{}` -> Oban payload | Tenant scope crosses a durable job boundary here |
| runtime context -> evaluator identity | Tenant-derived identity affects deterministic rollout and experiment assignment here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | S | tenancy normalization | mitigate | Normalize tenant scope through one seam and reject blank or malformed values before downstream use. |
| T-29-01-02 | I | runtime helper propagation | mitigate | Keep bounded tenant fields explicit and avoid serializing raw request/socket/session baggage. |
| T-29-01-03 | T | bucket identity composition | mitigate | Keep tenant-scoped rebucketing opt-in only and protect deterministic behavior with property tests. |
| T-29-01-04 | E | scope widening via helper drift | mitigate | Reuse one seam for same-tenant checks and avoid new ambient fallback behavior. |
| T-29-01-05 | D | single-tenant regression | mitigate | Preserve `SingleTenant` as the default path and cover no-config callers with targeted tests. |
</threat_model>

<verification>
Run the targeted tenancy, runtime-helper, and evaluator suites in `rulestead`. Confirm that tenant scope stays explicit through Plug, LiveView, and Oban helpers, `SingleTenant` still behaves as the nil-safe default, and bucket identity behavior remains deterministic unless a host explicitly opts into tenant-scoped subject composition.
</verification>

<success_criteria>
Phase 29 has a coherent runtime tenancy contract that preserves the existing product shape, keeps tenant scope explicit where it matters, and adds no hidden topology or rebucketing surprises.
</success_criteria>

<output>
After completion, create `.planning/phases/29-tenancy-helpers-validation/29-01-SUMMARY.md`
</output>
