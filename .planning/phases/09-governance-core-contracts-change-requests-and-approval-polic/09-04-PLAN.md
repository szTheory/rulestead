---
phase: 09-governance-core-contracts-change-requests-and-approval-polic
plan: 04
type: execute
wave: 4
depends_on:
  - 09-02
  - 09-03
files_modified:
  - rulestead/lib/rulestead.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/fake.ex
  - rulestead/lib/rulestead/telemetry.ex
  - rulestead/test/rulestead/governance_facade_contract_test.exs
  - rulestead/test/rulestead/store/governance_adapter_contract_test.exs
autonomous: true
requirements:
  - GOV-01
  - GOV-02
  - GOV-03
  - GOV-04
must_haves:
  truths:
    - "The public `Rulestead` facade exposes governed-change verbs for submit, approve, reject, cancel, and execute without requiring `rulestead_admin`."
    - "Both Ecto and Fake adapters implement the same governance operations so contract tests and host apps can exercise governed flows through either backend."
    - "Every governance transition emits immutable correlated audit metadata and the canonical `change_request.*` admin telemetry events."
  artifacts:
    - path: "rulestead/lib/rulestead.ex"
      provides: "Public governance facade and authorization entrypoints"
    - path: "rulestead/lib/rulestead/store/ecto.ex"
      provides: "Durable governance state transitions with correlated audit writes"
    - path: "rulestead/lib/rulestead/fake.ex"
      provides: "In-memory governance parity for tests and host smoke paths"
    - path: "rulestead/lib/rulestead/telemetry.ex"
      provides: "Canonical governance telemetry event metadata helpers"
    - path: "rulestead/test/rulestead/store/governance_adapter_contract_test.exs"
      provides: "Parity proof across Fake and Ecto adapters"
  key_links:
    - from: "rulestead/lib/rulestead.ex"
      to: "rulestead/lib/rulestead/store/ecto.ex"
      via: "root facade routes governed commands through the store and existing authorization/redaction envelope"
      pattern: "submit_change_request|approve_change_request|execute_change_request"
    - from: "rulestead/lib/rulestead/store/ecto.ex"
      to: "rulestead/lib/rulestead/audit_event.ex"
      via: "each governance mutation appends immutable audit rows with shared correlation ids"
      pattern: "change_request\\.|approval|correlation_id"
    - from: "rulestead/lib/rulestead/telemetry.ex"
      to: "prompts/rulestead-telemetry-observability-and-audit.md"
      via: "event names and metadata match the locked `change_request.*` contract"
      pattern: "change_request\\.submitted|change_request\\.approved|change_request\\.rejected|change_request\\.merged"
---

<objective>
Wire the root facade and both store adapters for governed change-request operations.

Purpose: Phase 9 must expose governance as a package-level contract that host apps and later phases can consume directly. This plan completes the actual submit/approve/reject/cancel/execute path and keeps Fake/Ecto behavior aligned.
Output: Public governance APIs, Ecto/Fake implementations, telemetry wiring, and adapter parity tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@rulestead/lib/rulestead.ex
@rulestead/lib/rulestead/store/ecto.ex
@rulestead/lib/rulestead/fake.ex
@rulestead/lib/rulestead/telemetry.ex
@rulestead/test/rulestead/admin_security_contract_test.exs
@prompts/rulestead-telemetry-observability-and-audit.md
@prompts/rulestead-security-privacy-and-threat-model.md

<interfaces>
From `rulestead/lib/rulestead.ex`:
```elixir
defp admin_write(operation, command) do
  redacted_command = redact_command(command)
  resource = command_resource(redacted_command)
  action = command_action(operation)
end
```

From `rulestead/lib/rulestead/store/ecto.ex`:
```elixir
defp audit_event_type(%Command.SaveDraftRuleset{}), do: "ruleset.save_draft"
defp audit_event_type(%Command.PublishRuleset{}), do: "ruleset.publish"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public governance verbs and canonical telemetry wiring at the root facade</name>
  <files>rulestead/lib/rulestead.ex, rulestead/lib/rulestead/telemetry.ex, rulestead/test/rulestead/governance_facade_contract_test.exs</files>
  <read_first>
    - rulestead/lib/rulestead.ex
    - rulestead/lib/rulestead/telemetry.ex
    - rulestead/lib/rulestead/admin/authorizer.ex
    - rulestead/lib/rulestead/store/command.ex
    - prompts/rulestead-telemetry-observability-and-audit.md
  </read_first>
  <acceptance_criteria>
    - `Rulestead` exports `submit_change_request`, `approve_change_request`, `reject_change_request`, `cancel_change_request`, and `execute_change_request` in command-first public forms.
    - The public governance verbs route through the existing authorization/redaction envelope rather than bypassing it.
    - Telemetry metadata supports the canonical admin events `change_request.submitted`, `change_request.approved`, `change_request.rejected`, and `change_request.merged` with `audit_event_id` or equivalent correlation metadata.
  </acceptance_criteria>
  <behavior>
    - Test 1: the new public verbs are exported and accept the new governance command structs.
    - Test 2: the root facade returns typed errors for change-request-required and self-approval-denied cases.
  </behavior>
  <action>Extend `Rulestead` with public governance verbs that accept the new command structs and execute through the existing `admin_write` authorization/redaction path rather than inventing a parallel mutation surface. Update the command-to-action/resource mapping so submit/approve/reject/cancel/execute use governed resource metadata and keep denied actions auditable per Phase 7 D-28 and D-32. Expand `Rulestead.Telemetry` so governance transitions emit the canonical `change_request.*` event names from the telemetry prompt and carry `change_request_id`, `correlation_id`, `audit_event_id`, governed action, and environment key in metadata. Do not add `rulestead_admin` dependencies or schedule/webhook execution semantics here.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/governance_facade_contract_test.exs test/rulestead/admin_security_contract_test.exs</automated>
  </verify>
  <done>The public package surface exposes governed-change operations through the same auth/redaction rules as other admin mutations, and telemetry names align with the locked event catalog.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Ecto and Fake adapter parity for governance state transitions</name>
  <files>rulestead/lib/rulestead/store/ecto.ex, rulestead/lib/rulestead/fake.ex, rulestead/test/rulestead/store/governance_adapter_contract_test.exs</files>
  <read_first>
    - rulestead/lib/rulestead/store/ecto.ex
    - rulestead/lib/rulestead/fake.ex
    - rulestead/lib/rulestead/store.ex
    - rulestead/lib/rulestead/store/command.ex
    - rulestead/lib/rulestead/audit_event.ex
  </read_first>
  <acceptance_criteria>
    - Both adapters implement submit, approve, reject, cancel, execute, and fetch/list operations for change requests.
    - Submit, approve/reject, cancel, and execute each append immutable audit data carrying the same `correlation_id` plus the relevant `change_request_id`.
    - The adapter contract test passes against both Fake and Ecto without special-case assertions for one backend.
  </acceptance_criteria>
  <behavior>
    - Test 1: submit -> approve -> execute produces correlated request, approval, execution, and audit artifacts in both adapters.
    - Test 2: reject and cancel are terminal and block later execute calls.
  </behavior>
  <action>Implement the governance store callbacks in both adapters. In `store/ecto.ex`, create transactional state transitions that persist the request/approval rows and append immutable audit rows with shared correlation ids, preserving explicit `preview -> confirm -> audit` semantics from the admin UX prompt and keeping writes inside one explicit transactional path per Phase 7 specifics. In `fake.ex`, mirror the same state machine and audit payload shape for test determinism. Keep `approve_change_request` separate from `execute_change_request` so Phase 10 scheduling can later call execute on already-approved work without changing this contract. Do not mutate runtime evaluation behavior directly in this phase; execution here means governed authoring-store mutation only.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/store/governance_adapter_contract_test.exs</automated>
  </verify>
  <done>Governance transitions behave the same in Fake and Ecto, and every transition produces the correlated durable/audit state later phases depend on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public governance facade -> store adapters | Privileged governed mutations enter the durable store path here |
| governance state transition -> audit/telemetry emission | Durable and ephemeral observability surfaces must stay correlated but distinct here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | T | governance execution path | mitigate | Route all public governance verbs through the existing `admin_write` authorization/redaction envelope; do not create a bypass surface. |
| T-09-04-02 | R | correlated audit trail | mitigate | Append immutable audit rows with shared `change_request_id` and `correlation_id` on submit/approve/reject/cancel/execute in both adapters. |
| T-09-04-03 | I | telemetry metadata | mitigate | Emit the canonical `change_request.*` events with redacted metadata only; keep audit and telemetry separate per the telemetry prompt. |
| T-09-04-04 | D | adapter divergence | mitigate | Enforce one parity contract suite across Ecto and Fake so host tests and later phases do not depend on backend-specific governance behavior. |
</threat_model>

<verification>
Run the new facade and adapter contract suites, then inspect that `Rulestead` exports the new governance verbs without any dependency on `rulestead_admin` or future schedule/webhook modules.
</verification>

<success_criteria>
Governed changes are now a real package contract: host apps can submit, review, and execute them through `Rulestead`, and both adapters preserve the same correlated audit/telemetry behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/09-governance-core-contracts-change-requests-and-approval-polic/09-04-SUMMARY.md`
</output>
