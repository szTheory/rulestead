---
phase: 09-governance-core-contracts-change-requests-and-approval-polic
plan: 05
type: execute
wave: 5
depends_on:
  - 09-04
files_modified:
  - rulestead/test/rulestead/governance_safety_contract_test.exs
  - rulestead/test/rulestead/governance_threat_model_test.exs
  - scripts/ci/verify_phase09_governance.sh
autonomous: true
requirements:
  - GOV-01
  - GOV-02
  - GOV-03
  - GOV-04
must_haves:
  truths:
    - "Phase 9 safety rules are provable with one repeatable verification entrypoint instead of tribal knowledge."
    - "Contract tests prove change-request submission, peer approval, self-approval denial, cancellation, rejection, and correlated execution behavior."
    - "Governance verification keeps sibling-package CI readability intact by using a scripts-first entrypoint that can include package-local smoke checks without folding in future UI work."
  artifacts:
    - path: "rulestead/test/rulestead/governance_safety_contract_test.exs"
      provides: "End-to-end contract tests for governed change lifecycle and requirement coverage"
    - path: "rulestead/test/rulestead/governance_threat_model_test.exs"
      provides: "Executable coverage for self-approval, denied direct execution, and immutable audit correlation"
    - path: "scripts/ci/verify_phase09_governance.sh"
      provides: "Scripts-first verification entrypoint preserving CI readability and sibling-package awareness"
  key_links:
    - from: "scripts/ci/verify_phase09_governance.sh"
      to: "rulestead/test/rulestead/governance_safety_contract_test.exs"
      via: "one named Phase 9 verification entrypoint runs the governance contract suite"
      pattern: "governance_safety_contract_test"
    - from: "scripts/ci/verify_phase09_governance.sh"
      to: "rulestead_admin/test/rulestead_admin/router_test.exs"
      via: "package-local smoke remains visible in the verification script without pulling Phase 11 UI work forward"
      pattern: "router_test|session_test"
    - from: "rulestead/test/rulestead/governance_threat_model_test.exs"
      to: "rulestead/lib/rulestead/admin/authorizer.ex"
      via: "tests prove the high-severity safety rules from the threat model"
      pattern: "unauthorized|change_request_required|self_approval"
---

<objective>
Finish Phase 9 with executable governance safety verification and a scripts-first CI entrypoint.

Purpose: The governance core is only shippable if the safety rules are easy to re-run and hard to regress. This plan packages the requirement coverage and threat-model checks into one clear verification surface.
Output: Governance safety tests, threat-model tests, and a `scripts/ci` verifier for Phase 9.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-VERIFICATION.md
@rulestead/test/rulestead/admin_security_contract_test.exs
@prompts/rulestead-security-privacy-and-threat-model.md
@prompts/rulestead-testing-and-e2e-strategy.md
@prompts/rulestead-engineering-dna-from-prior-libs.md

<interfaces>
From `rulestead/test/rulestead/admin_security_contract_test.exs`:
```elixir
assert {:error, %Error{domain: :auth, type: :unauthorized}} = ...
assert {"ruleset.publish", :denied, "viewer-1", "req-publish"} in denied
```

Scripts-first CI pattern from prior phases:
```bash
scripts/ci/<named-check>.sh
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add governance contract tests that prove the full safety lifecycle</name>
  <files>rulestead/test/rulestead/governance_safety_contract_test.exs, rulestead/test/rulestead/governance_threat_model_test.exs</files>
  <read_first>
    - rulestead/test/rulestead/admin_security_contract_test.exs
    - rulestead/test/rulestead/governance_facade_contract_test.exs
    - rulestead/test/rulestead/store/governance_adapter_contract_test.exs
    - prompts/rulestead-security-privacy-and-threat-model.md
    - prompts/rulestead-testing-and-e2e-strategy.md
  </read_first>
  <acceptance_criteria>
    - The safety contract test covers submit, peer approve, reject, cancel, and execute flows through the public facade.
    - The threat-model test explicitly proves: direct execution can be blocked in production, production self-approval is denied by default, and audit rows stay correlated across lifecycle events.
    - Both tests run from the `rulestead` package only and do not depend on Phase 11 UI surfaces.
  </acceptance_criteria>
  <behavior>
    - Test 1: a production publish request requires governance submission, then a different reviewer can approve and execute it successfully.
    - Test 2: the submitter cannot self-approve the same production request unless the host policy override is turned on.
    - Test 3: rejected or cancelled requests cannot execute.
    - Test 4: correlated immutable audit rows exist for submit, approve/reject, cancel, and execute transitions.
  </behavior>
  <action>Write a focused governance safety suite in `rulestead/test/rulestead/` that exercises the public facade and adapter behavior end to end. Use the fake-backed test posture from the testing prompt where possible, and add Ecto-backed assertions only where immutable audit persistence needs proof. Cover the GOV-01..04 matrix explicitly: submission instead of direct mutation, environment-sensitive change-request requirement, default production self-approval denial, and correlated audit events for approval/rejection/execution/cancellation. Keep the suite phase-scoped; do not add schedule or webhook cases from later phases.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/governance_safety_contract_test.exs test/rulestead/governance_threat_model_test.exs</automated>
  </verify>
  <done>The governance core has high-signal safety tests that map directly to the Phase 9 requirements and threat model.</done>
</task>

<task type="auto">
  <name>Task 2: Package Phase 9 verification into a scripts-first CI entrypoint with sibling-package smoke visibility</name>
  <files>scripts/ci/verify_phase09_governance.sh</files>
  <read_first>
    - .planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-VERIFICATION.md
    - prompts/rulestead-testing-and-e2e-strategy.md
    - prompts/rulestead-engineering-dna-from-prior-libs.md
  </read_first>
  <acceptance_criteria>
    - `scripts/ci/verify_phase09_governance.sh` runs the new `rulestead` governance tests and a package-local `rulestead_admin` smoke slice that is already expected to stay green.
    - The script names each sub-step plainly so CI output stays readable.
    - The script does not claim to close the carried Phase 7 `simulate_test.exs` gap or add future Phase 11 governance UI assertions.
  </acceptance_criteria>
  <action>Create `scripts/ci/verify_phase09_governance.sh` as the single scripts-first verification entrypoint for this phase. It should run the new governance safety tests inside `rulestead`, then run a narrow mounted-package smoke slice inside `rulestead_admin` that keeps sibling-package entrypoints visible without pretending to solve the open Phase 7 `simulate_test.exs` gap. Use explicit step labels and exit-on-failure semantics so the script is readable in CI logs and local runs. Do not fold in browser checks, scheduled execution, or mounted governance review screens from later phases.</action>
  <verify>
    <automated>bash scripts/ci/verify_phase09_governance.sh</automated>
  </verify>
  <done>Phase 9 verification is reproducible from one named script, and the monorepo still surfaces sibling-package regression signal alongside the new core governance tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy + facade + store interactions -> verification suite | High-severity governance rules must be provable across module boundaries here |
| monorepo core tests -> sibling-package smoke checks | Core governance changes can still regress mounted package entrypoints here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | R | governance lifecycle verification | mitigate | Add explicit requirement-mapped tests for submit/approve/reject/cancel/execute and correlated audit rows. |
| T-09-05-02 | E | self-approval and direct-execution guards | mitigate | Add threat-model tests that prove production self-approval denial and change-request-required enforcement at the public facade. |
| T-09-05-03 | D | CI readability / regression detection | mitigate | Use one scripts-first verifier that labels each sub-step and includes a mounted-package smoke slice instead of hiding sibling-package signal. |
| T-09-05-04 | T | false closure of carried Phase 7 gap | accept | Keep the verifier honest about the outstanding `simulate_test.exs` gap; include only the already-green admin smoke slice in this phase. |
</threat_model>

<verification>
Run `bash scripts/ci/verify_phase09_governance.sh` and confirm it executes the new governance tests plus the intended sibling-package smoke slice with readable step output.
</verification>

<success_criteria>
Phase 9 closes with one repeatable verification entrypoint and explicit tests for every governance safety rule, without blurring the line into later UI or scheduling work.
</success_criteria>

<output>
After completion, create `.planning/phases/09-governance-core-contracts-change-requests-and-approval-polic/09-05-SUMMARY.md`
</output>
