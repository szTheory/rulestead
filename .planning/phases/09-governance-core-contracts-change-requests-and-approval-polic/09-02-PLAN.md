---
phase: 09-governance-core-contracts-change-requests-and-approval-polic
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - rulestead/priv/repo/migrations/TIMESTAMP_create_rulestead_change_requests_and_approvals.exs
  - rulestead/lib/rulestead/audit_event.ex
  - rulestead/lib/rulestead/store.ex
  - rulestead/lib/rulestead/store/command.ex
  - rulestead/test/rulestead/store/command_governance_test.exs
  - rulestead/test/rulestead/audit_event_governance_test.exs
autonomous: true
requirements:
  - GOV-01
  - GOV-04
must_haves:
  truths:
    - "Governed mutations have durable storage tables and exact store command shapes before any root facade starts calling them."
    - "Audit events can carry immutable governance correlation fields linking submission, approval, rejection, cancellation, and execution."
    - "Store-level commands for change requests and approvals stay key-first and actor-bearing, matching the existing sibling-package/public-API design."
  artifacts:
    - path: "rulestead/priv/repo/migrations/TIMESTAMP_create_rulestead_change_requests_and_approvals.exs"
      provides: "Durable persistence for change requests and approval rows with correlation-safe indexes"
    - path: "rulestead/lib/rulestead/store.ex"
      provides: "Governance callback surface for submit, approve, reject, cancel, execute, and lookup operations"
    - path: "rulestead/lib/rulestead/store/command.ex"
      provides: "Key-first command structs for governance operations"
    - path: "rulestead/lib/rulestead/audit_event.ex"
      provides: "Normalized governance correlation metadata fields for immutable audit rows"
  key_links:
    - from: "rulestead/lib/rulestead/store/command.ex"
      to: "rulestead/lib/rulestead/store.ex"
      via: "new governance callbacks consume explicit command structs rather than loose maps"
      pattern: "SubmitChangeRequest|ApproveChangeRequest|RejectChangeRequest|CancelChangeRequest|ExecuteChangeRequest"
    - from: "rulestead/lib/rulestead/audit_event.ex"
      to: "rulestead/priv/repo/migrations/TIMESTAMP_create_rulestead_change_requests_and_approvals.exs"
      via: "audit metadata and durable tables share change_request_id / approval_id / correlation_id semantics"
      pattern: "change_request_id|approval_id|correlation_id"
    - from: "rulestead/test/rulestead/store/command_governance_test.exs"
      to: "rulestead/lib/rulestead/store/command.ex"
      via: "tests freeze actor-bearing governance command shapes"
      pattern: "actor|reason|metadata"
---

<objective>
Add the persistence and store-contract substrate for governed mutations.

Purpose: Phase 9 needs durable change-request and approval state plus exact command/store contracts before policy and root-facade wiring can be implemented safely.
Output: Governance migration, expanded store behavior, governance command structs, and audit-correlation metadata helpers.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-governance-core-contracts-change-requests-and-approval-polic/09-01-PLAN.md
@rulestead/lib/rulestead/store.ex
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/audit_event.ex
@prompts/rulestead-telemetry-observability-and-audit.md
@prompts/rulestead-security-privacy-and-threat-model.md
@prompts/rulestead-engineering-dna-from-prior-libs.md

<interfaces>
From `rulestead/lib/rulestead/store.ex`:
```elixir
@callback create_flag(Command.CreateFlag.t()) :: result(map())
@callback save_draft_ruleset(Command.SaveDraftRuleset.t()) :: result(map())
@callback list_audit_events(Command.ListAuditEvents.t()) :: result(Command.Page.t(map()))
```

From `rulestead/lib/rulestead/store/command.ex`:
```elixir
defmodule SaveDraftRuleset do
  defstruct [:flag_key, :environment_key, :ruleset, actor: nil, metadata: %{}]
end

defmodule PublishRuleset do
  defstruct [:flag_key, :environment_key, version: nil, actor: nil, metadata: %{}]
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the governance persistence model and immutable correlation fields</name>
  <files>rulestead/priv/repo/migrations/TIMESTAMP_create_rulestead_change_requests_and_approvals.exs, rulestead/lib/rulestead/audit_event.ex, rulestead/test/rulestead/audit_event_governance_test.exs</files>
  <read_first>
    - rulestead/lib/rulestead/audit_event.ex
    - prompts/rulestead-telemetry-observability-and-audit.md
    - prompts/rulestead-security-privacy-and-threat-model.md
    - prompts/rulestead-domain-language-field-guide.md
  </read_first>
  <acceptance_criteria>
    - The migration creates durable `change_requests` and `approvals` tables plus indexes/constraints that tie approval rows to one request and preserve correlation identifiers.
    - `AuditEvent.metadata/1` normalizes `change_request_id`, `approval_id`, `governance_action`, and `execution_stage` keys in addition to the existing request/source fields.
    - The audit-event governance test proves those keys survive normalization and serialization without raw session data.
  </acceptance_criteria>
  <behavior>
    - Test 1: governance-specific audit metadata fields serialize deterministically.
    - Test 2: missing governance metadata remains optional, but when present it is normalized into string-keyed maps.
  </behavior>
  <action>Create a single governance migration that adds durable tables for change requests and approvals with UUID primary keys, `correlation_id`, governed action, environment key, resource key, submitter/reviewer identity snapshots, status fields, and JSON command snapshots. Add constraints so an approval belongs to one change request and duplicate reviewer decisions for the same request are rejected. Extend `Rulestead.AuditEvent.metadata/1` so immutable audit rows can carry `change_request_id`, `approval_id`, `governance_action`, and `execution_stage` inside normalized metadata while preserving existing request/source/rollback fields. Keep the ledger append-only per Phase 7 D-24 and do not introduce mutable audit updates or webhook/scheduler tables.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/audit_event_governance_test.exs</automated>
  </verify>
  <done>The schema and audit metadata model can represent every Phase 9 governance transition with a shared immutable correlation trail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the store behavior and command layer for governance operations</name>
  <files>rulestead/lib/rulestead/store.ex, rulestead/lib/rulestead/store/command.ex, rulestead/test/rulestead/store/command_governance_test.exs</files>
  <read_first>
    - rulestead/lib/rulestead/store.ex
    - rulestead/lib/rulestead/store/command.ex
    - rulestead/lib/rulestead/governance/change_request.ex
    - rulestead/lib/rulestead/governance/approval.ex
    - rulestead/lib/rulestead/governance/approval_requirement.ex
  </read_first>
  <acceptance_criteria>
    - `Rulestead.Store` gains explicit callbacks for `submit_change_request`, `approve_change_request`, `reject_change_request`, `cancel_change_request`, `execute_change_request`, and at least one read path for fetch/list.
    - `Rulestead.Store.Command` adds one struct per governance operation and each struct includes actor-bearing metadata plus exact keys for request id, reason, and environment scope where applicable.
    - The command test asserts those structs normalize metadata maps and never require `rulestead_admin` inputs.
  </acceptance_criteria>
  <behavior>
    - Test 1: governance command constructors produce stable structs from map/keyword input.
    - Test 2: every write-path governance command carries `actor` and `metadata` fields matching the Phase 7 admin mutation envelope.
  </behavior>
  <action>Extend `Rulestead.Store` with governance callbacks that later adapters will implement: submit, approve, reject, cancel, execute, fetch by id, and list change requests. Add corresponding command structs to `Rulestead.Store.Command` using the existing key-first, actor-bearing style: request identifiers stay explicit, governed action/resource/environment remain top-level fields, and write-path commands accept `actor`, `reason`, and `metadata` maps carrying request/source correlation. Mirror the existing command ergonomics from `SaveDraftRuleset` and `PublishRuleset`; do not add loose map-based callbacks, UI-only flags, or scheduled/webhook fields.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/store/command_governance_test.exs</automated>
  </verify>
  <done>The store layer now has exact governance commands and callback signatures for later adapter and facade work, with immutable audit-correlation fields built in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/root command constructors -> store adapters | Untrusted caller input becomes durable governance state here |
| store writes -> immutable audit ledger | Durable change-request state must stay correlated with append-only audit events here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | T | governance tables | mitigate | Add durable request/approval schema with explicit foreign keys, correlation identifiers, and reviewer uniqueness so approvals cannot drift from requests. |
| T-09-02-02 | R | audit correlation model | mitigate | Normalize `change_request_id`, `approval_id`, `governance_action`, and `execution_stage` in `AuditEvent.metadata/1` so every transition is reconstructable from immutable rows. |
| T-09-02-03 | I | command metadata | mitigate | Keep command structs actor-bearing but session-free; only request/source/reason/correlation metadata may cross into store calls per Phase 7 D-33 and D-34. |
| T-09-02-04 | D | store callback surface | mitigate | Freeze explicit command structs and callbacks now so later adapters do not invent incompatible map payloads or implicit defaults. |
</threat_model>

<verification>
Run the governance command and audit-event tests, then inspect the migration to confirm it adds only governance persistence and not future scheduling or webhook artifacts.
</verification>

<success_criteria>
Phase 9 has a durable storage contract and exact store interface for governed mutations, with immutable audit-correlation fields that later plans can wire end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/09-governance-core-contracts-change-requests-and-approval-polic/09-02-SUMMARY.md`
</output>
