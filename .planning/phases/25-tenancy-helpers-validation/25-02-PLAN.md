---
phase: 25-tenancy-helpers-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - rulestead/lib/rulestead/environment_version.ex
  - rulestead/lib/rulestead/manifest/import.ex
  - rulestead/lib/rulestead/manifest/plan.ex
  - rulestead/lib/rulestead/promotion/compare.ex
  - rulestead/lib/rulestead/audit_event.ex
  - rulestead/lib/rulestead/store/command.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/fake.ex
  - rulestead/lib/rulestead/admin/policy.ex
  - rulestead/lib/rulestead/admin/authorizer.ex
  - rulestead/test/rulestead/manifest/import_test.exs
  - rulestead/test/rulestead/store/manifest_import_contract_test.exs
  - rulestead/test/rulestead/store/promotion_apply_contract_test.exs
  - rulestead/test/rulestead/promotion/compare_test.exs
  - rulestead/test/rulestead/audit_event_governance_test.exs
  - rulestead/test/rulestead/release_contract_test.exs
  - rulestead_admin/lib/rulestead_admin/live/session.ex
  - rulestead_admin/test/rulestead_admin/live/session_test.exs
autonomous: true
requirements:
  - TEN-01
  - TEN-02
  - TEN-03
must_haves:
  truths:
    - "Import and promotion preview paths surface tenant-sensitive blockers before any apply happens."
    - "Saved plan and audit metadata carry bounded tenant scope so operators can see what tenant a reviewed action targets."
    - "Mounted admin scope resolution can carry explicit tenant scope without implying cross-tenant or all-tenant mutation defaults."
    - "Tenant-aware safety checks fail closed on scope mismatch or widening instead of leaking across tenants."
  artifacts:
    - path: "rulestead/lib/rulestead/manifest/import.ex"
      provides: "Tenant-aware import preview and apply validation over the existing saved-plan contract"
    - path: "rulestead/lib/rulestead/manifest/plan.ex"
      provides: "Saved plan artifact fields for bounded tenant scope metadata"
    - path: "rulestead/lib/rulestead/promotion/compare.ex"
      provides: "Tenant-sensitive compare findings that reuse the existing finding taxonomy"
    - path: "rulestead/lib/rulestead/store/ecto.ex"
      provides: "Adapter-backed parity for tenant-scope persistence or rejection where apply metadata crosses store boundaries"
    - path: "rulestead/lib/rulestead/fake.ex"
      provides: "Fake adapter parity for tenant-sensitive preview/apply revalidation"
    - path: "rulestead/lib/rulestead/audit_event.ex"
      provides: "Bounded tenant-aware audit metadata with sensitive session data still scrubbed"
    - path: "rulestead_admin/lib/rulestead_admin/live/session.ex"
      provides: "Minimum mounted-admin tenant scope seam aligned with the existing environment resolver"
  key_links:
    - from: "rulestead/lib/rulestead/manifest/import.ex"
      to: "rulestead/lib/rulestead/manifest/plan.ex"
      via: "preview stores bounded tenant scope in the saved plan and apply revalidates that scope before mutation"
      pattern: "tenant|scope|plan_token"
    - from: "rulestead/lib/rulestead/promotion/compare.ex"
      to: "rulestead/lib/rulestead/manifest/import.ex"
      via: "promotion/import preview share one tenant-sensitive finding vocabulary instead of divergent validators"
      pattern: "finding|tenant|blocked|warning"
    - from: "rulestead/lib/rulestead/store/command.ex"
      to: "rulestead/lib/rulestead/audit_event.ex"
      via: "bounded tenant metadata survives command normalization and becomes auditable without session leakage"
      pattern: "metadata|tenant|scope"
    - from: "rulestead/lib/rulestead/admin/policy.ex"
      to: "rulestead/lib/rulestead/admin/authorizer.ex"
      via: "tenant scope reaches authorization through normalized resource/context metadata while the public `can?/4` callback shape stays unchanged"
      pattern: "can\\?/4|resource|tenant|scope"
    - from: "rulestead_admin/lib/rulestead_admin/live/session.ex"
      to: "rulestead/lib/rulestead/admin/authorizer.ex"
      via: "mounted admin scope resolution and authorization stay explicit about environment plus tenant scope"
      pattern: "tenant|scope|access_admin"
---

<objective>
Extend preview validation, saved-plan metadata, audit metadata, and mounted admin scope seams with explicit tenant awareness.

Purpose: Phase 25 needs tenant-sensitive safety checks and operator-visible metadata on top of the new tenancy seam so import, promotion, audit, and mounted admin flows can reject wrong-tenant or widened-tenant intent before mutation without widening into future tenancy topology work.
Output: Tenant-aware preview/apply validation, bounded tenant scope in saved plans and audit metadata, minimal mounted admin tenant scope resolution, and targeted regression tests across `rulestead` and `rulestead_admin`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-tenancy-helpers-validation/25-CONTEXT.md
@.planning/phases/25-tenancy-helpers-validation/25-RESEARCH.md
@.planning/phases/25-tenancy-helpers-validation/25-PATTERNS.md
@.planning/phases/24-gitops-manifests-cli-surface/24-03-PLAN.md
@.planning/phases/24-gitops-manifests-cli-surface/24-04-PLAN.md
@rulestead/lib/rulestead/manifest/import.ex
@rulestead/lib/rulestead/manifest/plan.ex
@rulestead/lib/rulestead/manifest/result.ex
@rulestead/lib/rulestead/promotion/compare.ex
@rulestead/lib/rulestead/audit_event.ex
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/admin/authorizer.ex
@rulestead_admin/lib/rulestead_admin/live/session.ex
@rulestead/test/rulestead/manifest/import_test.exs
@rulestead/test/rulestead/store/manifest_import_contract_test.exs
@rulestead/test/rulestead/promotion/compare_test.exs
@rulestead/test/rulestead/audit_event_governance_test.exs
@rulestead_admin/test/rulestead_admin/live/session_test.exs

<interfaces>
From rulestead/lib/rulestead/manifest/plan.ex:
```elixir
%{
  "schema_version" => 1,
  "kind" => "rulestead_apply_plan",
  "mode" => "import" | "promote",
  "target_environment_key" => "...",
  "plan_token" => "...",
  "target_fingerprint" => "...",
  "dependency_closure_keys" => [...],
  "proposed_target_bundle" => %{...}
}
```

From rulestead/lib/rulestead/manifest/import.ex:
```elixir
findings =
  diff_result["findings"] ++
    additive_findings(...) ++
    dependency_findings(...) ++
    governance_findings(...)
```

From rulestead/lib/rulestead/audit_event.ex:
```elixir
%{
  "before" => ...,
  "after" => ...,
  "diff" => ...,
  "links" => ...,
  "context" => context
}
```

From rulestead_admin/lib/rulestead_admin/live/session.ex:
```elixir
%{
  actor: actor,
  environment: environment,
  environments: environments,
  env_source: env_source,
  mount_path: mount_path,
  policy: policy
}
```

Locked Phase 25 contract:
```text
reuse preview-first compare/import/promote flows, persist only bounded tenant scope metadata, and add only the minimum mounted admin seam needed for explicit tenant scope safety
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tenant-aware preview blockers and saved-plan scope metadata</name>
  <files>rulestead/lib/rulestead/environment_version.ex, rulestead/lib/rulestead/manifest/import.ex, rulestead/lib/rulestead/manifest/plan.ex, rulestead/lib/rulestead/promotion/compare.ex, rulestead/lib/rulestead/store/ecto.ex, rulestead/lib/rulestead/fake.ex, rulestead/test/rulestead/manifest/import_test.exs, rulestead/test/rulestead/store/manifest_import_contract_test.exs, rulestead/test/rulestead/store/promotion_apply_contract_test.exs, rulestead/test/rulestead/promotion/compare_test.exs</files>
  <behavior>
    - Test 1: import preview emits tenant-sensitive findings when a reviewed operation widens or mismatches tenant scope, and apply rejects the saved plan when live scope no longer matches the reviewed tenant scope.
    - Test 2: promote compare can project tenant-sensitive warnings or blockers through the existing finding taxonomy and stable ordering without creating a second validator shape.
    - Test 3: saved import or promote plans persist only bounded tenant scope metadata and revalidate it before mutation, with Fake and Ecto promotion-apply parity proving the same tenant mismatch is rejected across adapters.
  </behavior>
  <action>Extend preview/apply validation and saved-plan fields per D-09 and D-10. Add bounded tenant scope fields to `Rulestead.Manifest.Plan` and any environment-version persistence touched by saved apply metadata so import and promote plans can persist explicit environment-plus-tenant scope metadata without serializing tenant-owned state, session baggage, or future tenant topology. Then extend `Rulestead.Manifest.Import`, `Rulestead.Promotion.Compare`, and the relevant apply parity surfaces (`Store.Ecto`, `Rulestead.Fake`, and `promotion_apply_contract_test.exs`) to reuse the current preview-first finding contract for tenant-sensitive blockers and warnings: detect same-tenant violations, scope widening, or plan/apply tenant mismatch before apply; keep findings in the existing result envelope; and preserve the Phase 24 saved-plan, stale, and governance posture. Do not invent tenant-specific manifest trees, inheritance, destructive prune, or a second validation engine.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/manifest/import_test.exs test/rulestead/store/manifest_import_contract_test.exs test/rulestead/store/promotion_apply_contract_test.exs test/rulestead/promotion/compare_test.exs</automated>
  </verify>
  <done>Import and promotion preview/apply flows now surface tenant-sensitive problems before mutation and carry only bounded tenant scope metadata in saved plan artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bounded tenant-aware audit metadata and the minimum mounted admin scope seam</name>
  <files>rulestead/lib/rulestead/audit_event.ex, rulestead/lib/rulestead/store/command.ex, rulestead/lib/rulestead/admin/policy.ex, rulestead/lib/rulestead/admin/authorizer.ex, rulestead/test/rulestead/audit_event_governance_test.exs, rulestead/test/rulestead/release_contract_test.exs, rulestead_admin/lib/rulestead_admin/live/session.ex, rulestead_admin/test/rulestead_admin/live/session_test.exs</files>
  <behavior>
    - Test 1: command normalization and audit metadata persist tenant scope in bounded, string-normalized form while continuing to drop sensitive session and socket data.
    - Test 2: mounted admin session resolution can accept explicit tenant scope from params or remembered session state, produce tenant-aware links, and fail closed when tenant scope is invalid or cross-tenant.
    - Test 3: admin authorization checks can consume environment plus tenant scope metadata through normalized resource/context payloads without changing the public `Rulestead.Admin.Policy.can?/4` callback shape or creating implicit all-tenant mutation defaults.
  </behavior>
  <action>Add the operator-facing metadata and mounted seam per D-11, D-12, and D-13. Extend `Rulestead.Store.Command` and `Rulestead.AuditEvent` so normalized command metadata and audit envelopes can carry bounded tenant scope fields such as `tenant_key` or a normalized tenant scope token while preserving the current sensitive-key scrubbing rules. Keep `Rulestead.Admin.Policy.can?/4` as the stable public callback: tenant scope should reach policy decisions through normalized resource/context metadata assembled by `Rulestead.Admin.Authorizer` and mounted session helpers, not by changing callback arity or signature. Then add the minimum mounted admin seam in `RulesteadAdmin.Live.Session`, plus any supporting authorizer normalization needed, so the mounted UI can resolve explicit tenant scope alongside environment scope, remember it deterministically, and reject cross-tenant leakage with fail-closed behavior. Keep the admin work scoped to mounted-session helpers and authorization inputs only; do not build standalone tenant dashboards, cross-tenant fleet views, or hidden all-tenant mutation flows.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/audit_event_governance_test.exs test/rulestead/release_contract_test.exs && cd ../rulestead_admin && mix test test/rulestead_admin/live/session_test.exs</automated>
  </verify>
  <done>Audit trails and mounted admin flows now expose explicit tenant scope where operators need it, while cross-tenant leakage remains fail-closed and single-tenant hosts keep the ergonomic default path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewed preview -> saved plan artifact | Tenant scope leaves preview-time memory and becomes durable apply input here |
| saved plan -> apply path | Stored tenant scope re-enters mutating import/promotion execution here |
| command metadata -> audit event | Operator-facing tenant scope crosses into append-only audit storage here |
| mounted admin params/session -> authorization | Tenant scope chosen in the mounted UI influences read/write guards here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | T | import/promotion saved plans | mitigate | Persist bounded tenant scope in the reviewed plan and reject apply when requested or live scope diverges from that reviewed scope. |
| T-25-02-02 | I | audit metadata | mitigate | Normalize tenant metadata to bounded strings/maps and keep existing session/socket-sensitive key scrubbing intact. |
| T-25-02-03 | E | mounted admin scope resolution | mitigate | Require explicit tenant scope where used, preserve same-tenant guards, and fail closed on mismatched or invalid tenant scope. |
| T-25-02-04 | R | operator review and auditability | mitigate | Carry tenant scope through findings, saved plans, and audit metadata so operators can trace exactly which tenant a preview or mutation targeted. |
| T-25-02-05 | S | cross-tenant mutation intent | mitigate | Reuse the Phase 24 preview-first posture to block tenant-sensitive widening and wrong-tenant requests before any apply path reaches the store. |
</threat_model>

<verification>
Run the targeted import, manifest-import contract, promotion-apply parity, promotion compare, audit-event, release-contract, and mounted-session tests. Confirm tenant-sensitive blockers appear during preview, saved plans and audits carry bounded tenant scope, promotion apply parity holds across Fake and Ecto, and mounted admin scope stays explicit without implying future tenant topology work.
</verification>

<success_criteria>
Phase 25 now rejects tenant-sensitive invalid states before apply and exposes bounded tenant scope metadata in saved plan, audit, and mounted admin seams without widening beyond the current linked-version runtime-plus-mounted-admin model.
</success_criteria>

<output>
After completion, create `.planning/phases/25-tenancy-helpers-validation/25-02-SUMMARY.md`
</output>
