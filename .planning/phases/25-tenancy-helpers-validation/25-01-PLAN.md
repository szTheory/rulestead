---
phase: 25-tenancy-helpers-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/config.ex
  - rulestead/lib/rulestead/tenancy.ex
  - rulestead/lib/rulestead/tenancy/single_tenant.ex
  - rulestead/lib/rulestead/phoenix.ex
  - rulestead/lib/rulestead/live_view.ex
  - rulestead/lib/rulestead/oban.ex
  - rulestead/lib/rulestead/oban/middleware.ex
  - rulestead/lib/rulestead/evaluator.ex
  - rulestead/test/rulestead/config_test.exs
  - rulestead/test/rulestead/tenancy_test.exs
  - rulestead/test/rulestead/tenancy_property_test.exs
  - rulestead/test/rulestead/oban_test.exs
  - rulestead/test/rulestead/evaluator_test.exs
  - rulestead/test/rulestead/evaluator_property_test.exs
  - rulestead/test/rulestead/release_contract_test.exs
autonomous: true
requirements:
  - TEN-01
  - TEN-03
must_haves:
  truths:
    - "Single-tenant hosts can keep using runtime, request, LiveView, and Oban helpers without configuring multi-tenant topology."
    - "Tenant scope is resolved through one explicit tenancy seam instead of ad hoc hidden defaults inside framework helpers."
    - "Rollout and experiment bucketing can use tenant-aware identity composition without changing authored rule topology or breaking deterministic behavior."
    - "Tenant-aware helper behavior stays bounded to explicit resolution, scope, same-tenant checks, and bucketing hooks."
  artifacts:
    - path: "rulestead/lib/rulestead/tenancy.ex"
      provides: "Host-facing tenancy seam for explicit tenant resolution, scope normalization, same-tenant guards, topic helpers, and bucketing identity composition"
    - path: "rulestead/lib/rulestead/tenancy/single_tenant.ex"
      provides: "Nil-safe default tenancy implementation that preserves ergonomic single-tenant behavior"
    - path: "rulestead/lib/rulestead/config.ex"
      provides: "Closed host-config schema for registering the tenancy seam explicitly under `:tenancy`"
    - path: "rulestead/lib/rulestead/evaluator.ex"
      provides: "Deterministic tenant-aware bucketing hook without changing existing bucket_by schema"
    - path: "rulestead/test/rulestead/tenancy_test.exs"
      provides: "Direct seam coverage for config wiring, `SingleTenant`, and same-tenant normalization"
    - path: "rulestead/test/rulestead/evaluator_property_test.exs"
      provides: "Regression proof that tenant-aware bucketing remains deterministic"
  key_links:
    - from: "rulestead/lib/rulestead/config.ex"
      to: "rulestead/lib/rulestead/tenancy.ex"
      via: "host tenancy registration is validated through one explicit top-level `:tenancy` config block"
      pattern: "tenancy|module|validate"
    - from: "rulestead/lib/rulestead/phoenix.ex"
      to: "rulestead/lib/rulestead/tenancy.ex"
      via: "conn-derived tenant scope is normalized through the shared seam before entering `%Rulestead.Context{}`"
      pattern: "tenant_key|tenant_scope|same_tenant"
    - from: "rulestead/lib/rulestead/live_view.ex"
      to: "rulestead/lib/rulestead/tenancy.ex"
      via: "socket/session tenant scope is resolved explicitly through the shared seam instead of ambient defaults"
      pattern: "tenant_key|tenant_scope|same_tenant"
    - from: "rulestead/lib/rulestead/oban.ex"
      to: "rulestead/lib/rulestead/tenancy.ex"
      via: "job-bound context serialization keeps tenant scope bounded and normalized"
      pattern: "tenant_key|tenant_scope"
    - from: "rulestead/lib/rulestead/evaluator.ex"
      to: "rulestead/lib/rulestead/tenancy.ex"
      via: "tenant bucketing identity flows through one additive hook while salts and strict/permissive semantics stay intact"
      pattern: "resolve_bucket_identity|bucketing_key|tenant"
---

<objective>
Ship the minimal tenancy seam, the safe `SingleTenant` default, and the additive bucketing hooks for runtime helper flows.

Purpose: Phase 25 needs one explicit tenancy contract that keeps tenant scope visible and composable across runtime seams without inventing tenant-partitioned storage, environment-per-tenant topology, or hidden ambient resolution.
Output: `Rulestead.Tenancy`, `Rulestead.Tenancy.SingleTenant`, explicit runtime/helper seam wiring, additive evaluator bucketing hooks, and targeted regression tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-tenancy-helpers-validation/25-CONTEXT.md
@.planning/phases/25-tenancy-helpers-validation/25-RESEARCH.md
@.planning/phases/25-tenancy-helpers-validation/25-PATTERNS.md
@.planning/phases/24-gitops-manifests-cli-surface/24-CONTEXT.md
@.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md
@rulestead/lib/rulestead/context.ex
@rulestead/lib/rulestead/phoenix.ex
@rulestead/lib/rulestead/live_view.ex
@rulestead/lib/rulestead/oban.ex
@rulestead/lib/rulestead/oban/middleware.ex
@rulestead/lib/rulestead/evaluator.ex
@rulestead/lib/rulestead/ruleset/rollout.ex
@rulestead/lib/rulestead/ruleset/experiment.ex
@rulestead/test/rulestead/evaluator_test.exs
@rulestead/test/rulestead/evaluator_property_test.exs
@rulestead/test/rulestead/oban_test.exs

<interfaces>
From rulestead/lib/rulestead/context.ex:
```elixir
defstruct actor: nil,
          targeting_key: nil,
          tenant_key: nil,
          environment: nil,
          attributes: %{},
          request_id: nil,
          session_id: nil,
          strict?: false
```

From rulestead/lib/rulestead/phoenix.ex and rulestead/lib/rulestead/live_view.ex:
```elixir
%{}
|> maybe_put(:targeting_key, ...)
|> maybe_put(:tenant_key, ...)
|> maybe_put(:environment, ...)
```

From rulestead/lib/rulestead/oban.ex:
```elixir
@bounded_fields ~w(actor targeting_key tenant_key environment attributes request_id session_id strict?)a
```

From rulestead/lib/rulestead/evaluator.ex:
```elixir
defp resolve_bucket_identity(context, :tenant), do: present(context.tenant_key)
defp resolve_bucket_identity(context, "tenant"), do: present(context.tenant_key)
```

Locked Phase 25 contract:
```text
tenant_key stays the canonical runtime field; the new seam adds explicit helper composition around it and preserves current single-tenant defaults
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the tenancy seam and explicit request/socket resolution helpers</name>
  <files>rulestead/lib/rulestead/config.ex, rulestead/lib/rulestead/tenancy.ex, rulestead/lib/rulestead/tenancy/single_tenant.ex, rulestead/lib/rulestead/phoenix.ex, rulestead/lib/rulestead/live_view.ex, rulestead/test/rulestead/config_test.exs, rulestead/test/rulestead/tenancy_test.exs, rulestead/test/rulestead/release_contract_test.exs</files>
  <behavior>
    - Test 1: the host config schema grows a top-level `:tenancy` block that validates the configured tenancy module and defaults cleanly to `Rulestead.Tenancy.SingleTenant`.
    - Test 2: explicit tenant inputs from conn and socket/session helpers are normalized through the shared seam and merged into `%Rulestead.Context{}` without hidden mutating defaults.
    - Test 3: dedicated tenancy seam tests cover `SingleTenant`, `same_tenant?/2`, normalized scope helpers, and fail-closed mismatched or missing tenant scope behavior.
  </behavior>
  <action>Create the bounded `Rulestead.Tenancy` seam and the `Rulestead.Tenancy.SingleTenant` default per D-01, D-02, D-03, D-04, D-05, D-06, and D-13. Register the seam explicitly through a new top-level `:tenancy` block in `Rulestead.Config` so host selection is validated and release-contract-visible rather than hidden under `:runtime` or ad hoc application keys. Keep helper entrypoints explicit and caller-visible, and include only the small helper surface Phase 25 needs such as tenant resolution, normalized tenant scope, `same_tenant?/2`, and tenant-topic shaping. Then route `Rulestead.Phoenix.context_from_conn/2` and `Rulestead.LiveView.context_from_socket/2` through that seam so `tenant_key` remains the canonical runtime field while resolution and guard behavior stop living as ad hoc field copies. Do not introduce tenant lifecycle management, ambient process-state lookup, environment-per-tenant topology, tenant inheritance, or admin-product surface expansion.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/config_test.exs test/rulestead/tenancy_test.exs test/rulestead/release_contract_test.exs</automated>
  </verify>
  <done>The repo has one explicit tenancy seam with a safe single-tenant default, and request/socket helpers consume it without changing the canonical `%Rulestead.Context{}` contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bounded job propagation and deterministic tenant-aware bucketing hooks</name>
  <files>rulestead/lib/rulestead/tenancy.ex, rulestead/lib/rulestead/oban.ex, rulestead/lib/rulestead/oban/middleware.ex, rulestead/lib/rulestead/evaluator.ex, rulestead/test/rulestead/tenancy_property_test.exs, rulestead/test/rulestead/oban_test.exs, rulestead/test/rulestead/evaluator_test.exs, rulestead/test/rulestead/evaluator_property_test.exs</files>
  <behavior>
    - Test 1: Oban context serialization and attach helpers preserve only bounded tenant-aware scope fields and rebuild them deterministically on the worker side.
    - Test 2: rollout and experiment rules using `bucket_by: :tenant` keep Phase 3 strict/permissive error posture and deterministic hashing behavior.
    - Test 3: dedicated tenancy property coverage proves additive bucketing helpers can compose tenant scope with existing identity inputs when explicitly requested, while subject/account/session behavior remains unchanged and cross-tenant collisions do not widen silently.
  </behavior>
  <action>Wire the seam into job propagation and evaluator bucketing per D-07 and D-08. Extend `Rulestead.Oban` and `Rulestead.Oban.Middleware` so tenant-aware scope stays bounded and normalized when contexts cross job boundaries, matching the existing explicit propagation posture. Then route `Rulestead.Evaluator.resolve_bucket_identity/2` through a tenancy helper that keeps `context.tenant_key` canonical for `bucket_by: :tenant` while allowing additive tenant-aware identity composition behind one explicit hook. Preserve Phase 3 salts, `bucket_by` enums, ruleset topology, and the current strict-versus-permissive missing-identity semantics. Do not rewrite rollout or experiment schemas, do not inspect hidden actor payloads for ambient tenant inference, and do not introduce random fallback chains.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/tenancy_property_test.exs test/rulestead/oban_test.exs test/rulestead/evaluator_test.exs test/rulestead/evaluator_property_test.exs</automated>
  </verify>
  <done>Tenant-aware bucketing and bounded job propagation exist as additive hooks over the current runtime contract, with deterministic regression coverage and unchanged single-tenant defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host request/socket input -> `%Rulestead.Context{}` | Untrusted tenant scope enters runtime evaluation and admin helper flows here |
| `%Rulestead.Context{}` -> Oban payload | Tenant scope crosses a durable serialization boundary here |
| runtime context -> evaluator bucketing | Tenant-derived identity influences rollout and experiment assignment here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | S | `Rulestead.Tenancy` resolution helpers | mitigate | Normalize all tenant identifiers through one seam, reject blank or mismatched same-tenant comparisons, and avoid hidden ambient scope resolution. |
| T-25-01-02 | I | Phoenix/LiveView/Oban helper flows | mitigate | Keep tenant scope explicit, bounded, and whitelisted just like existing context propagation; do not serialize raw conn/socket/job baggage. |
| T-25-01-03 | T | evaluator bucketing identity | mitigate | Funnel tenant-aware identity through one helper while preserving existing salt layering, `bucket_by` enums, and strict/permissive missing-identity semantics. |
| T-25-01-04 | E | tenancy seam scope expansion | accept | Phase 25 intentionally ships helper seams only; rejecting tenant-partitioned storage and env-per-tenant topology keeps privilege surface small. |
| T-25-01-05 | D | single-tenant default regressions | mitigate | Ship `SingleTenant` as the default module and add regression tests proving current callers still work without extra configuration. |
</threat_model>

<verification>
Run the targeted config, tenancy seam, release-contract, Oban, and evaluator suites in `rulestead`. Confirm the top-level `:tenancy` config block validates the host seam, single-tenant callers remain config-free, tenant scope stays explicit through request/socket/job helpers, and tenant bucketing remains deterministic without widening rule topology.
</verification>

<success_criteria>
Phase 25 now has one explicit tenancy seam and one safe default path that make tenant scope visible where needed while keeping runtime evaluation, helper ergonomics, and deterministic bucketing aligned with the current two-package product shape.
</success_criteria>

<output>
After completion, create `.planning/phases/25-tenancy-helpers-validation/25-01-SUMMARY.md`
</output>
