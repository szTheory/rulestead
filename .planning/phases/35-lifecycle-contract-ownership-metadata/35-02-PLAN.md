---
phase: 35-lifecycle-contract-ownership-metadata
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - rulestead/lib/rulestead/audit_event.ex
  - rulestead/lib/rulestead/admin/lifecycle.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/fake.ex
  - rulestead/test/rulestead/admin_lifecycle_test.exs
  - rulestead/test/rulestead/store_ecto_admin_test.exs
  - rulestead/test/rulestead/audit_event_governance_test.exs
  - rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex
  - rulestead_admin/test/rulestead_admin/live/flag_live/show_test.exs
autonomous: true
requirements:
  - LIF-01
must_haves:
  truths:
    - "Ownership and lifecycle mutations emit bounded audit summaries that stay readable and queryable without parsing raw diffs."
    - "Mounted-admin detail reads show authored ownership and lifecycle intent through the shared projector, not through persisted machine status."
    - "Audit, detail, and adapter read models agree on the same ownership/lifecycle vocabulary and stay off the runtime hot path."
  artifacts:
    - path: "rulestead/lib/rulestead/audit_event.ex"
      provides: "Central ownership/lifecycle transition summary generation inside the existing audit envelope"
      min_lines: 40
    - path: "rulestead/lib/rulestead/admin/lifecycle.ex"
      provides: "Derived lifecycle projector aligned to authored ownership and lifecycle metadata"
      min_lines: 45
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex"
      provides: "Mounted detail rendering for authored ownership and lifecycle contract fields"
      min_lines: 40
  key_links:
    - from: "rulestead/lib/rulestead/store/ecto.ex"
      to: "rulestead/lib/rulestead/audit_event.ex"
      via: "write paths emit normalized ownership/lifecycle transition summaries through the shared metadata builder"
      pattern: "ownership_transition|lifecycle_transition"
    - from: "rulestead/lib/rulestead/admin/lifecycle.ex"
      to: "rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex"
      via: "mounted detail consumes authored facts through the projector instead of persisting machine statuses"
      pattern: "owner_ref|owner_kind|default_overridden|review_by"
    - from: "rulestead/lib/rulestead/fake.ex"
      to: "rulestead/lib/rulestead/store/ecto.ex"
      via: "adapter parity holds for ownership/lifecycle payload decoration and audit summaries"
      pattern: "ownership_transition|lifecycle_transition|lifecycle"
---

<objective>
Add bounded audit transition summaries and align the mounted-admin detail/projection surfaces to the new authored ownership and lifecycle contract.

Purpose: After the authored contract exists, Phase 35 still needs readable audit continuity and projection/detail alignment so operators see one auditable story without introducing archive-readiness scoring or lifecycle workbench UX.
Output: centralized audit summaries, projector alignment, and mounted-admin detail rendering/tests that stay inside the Phase 35 contract boundary.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-lifecycle-contract-ownership-metadata/35-CONTEXT.md
@.planning/phases/35-lifecycle-contract-ownership-metadata/35-RESEARCH.md
@.planning/phases/35-lifecycle-contract-ownership-metadata/35-PATTERNS.md
@.planning/phases/35-lifecycle-contract-ownership-metadata/35-01-PLAN.md
@rulestead/lib/rulestead/audit_event.ex
@rulestead/lib/rulestead/admin/lifecycle.ex
@rulestead/lib/rulestead/store/ecto.ex
@rulestead/lib/rulestead/fake.ex
@rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex

<interfaces>
From `rulestead/lib/rulestead/audit_event.ex`:
```elixir
@spec metadata(map() | keyword()) :: map()
def metadata(attrs \\ %{}) when is_list(attrs) or is_map(attrs)
```

From `rulestead/lib/rulestead/admin/lifecycle.ex`:
```elixir
@spec classify(map() | struct(), map() | struct(), keyword()) :: %{
  state: state(),
  mode: :permanent | :expiring,
  owner: term(),
  expected_expiration: Date.t() | nil,
  permanent: boolean(),
  last_evaluated_at: DateTime.t() | nil
}
```

From `rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex`:
```elixir
<FlagComponents.stat title="Lifecycle" value={humanize(@detail.lifecycle.state)} />
<FlagComponents.stat title="Owner" value={@detail.lifecycle.owner} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit bounded ownership and lifecycle audit summaries through the shared envelope</name>
  <files>rulestead/lib/rulestead/audit_event.ex, rulestead/lib/rulestead/store/ecto.ex, rulestead/lib/rulestead/fake.ex, rulestead/test/rulestead/audit_event_governance_test.exs, rulestead/test/rulestead/store_ecto_admin_test.exs</files>
  <behavior>
    - Test 1: ownership and lifecycle mutations produce compact `ownership_transition` and `lifecycle_transition` blocks alongside canonical `before`/`after`/`diff`.
    - Test 2: the summary shape is normalized, bounded, and identical across Ecto and fake-backed mutation paths.
    - Test 3: summary blocks stay hints only and do not replace the canonical diff or create bespoke lifecycle audit tables.
  </behavior>
  <action>Per D-19 through D-23 and D-25, extend the existing `AuditEvent.metadata/1` normalization seam so ownership and lifecycle mutation summaries are generated centrally from normalized before/after data, using bounded enums and snapshot-safe fields. Then thread that helper through Ecto and fake mutation paths that change authored ownership or lifecycle metadata, including the existing governed/scheduled pathways that already share the audit envelope. Keep the generic envelope intact, avoid per-event schema sprawl, and do not add archive-readiness or auto-cleanup semantics.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/audit_event_governance_test.exs test/rulestead/store_ecto_admin_test.exs</automated>
  </verify>
  <done>Audit rows now carry stable ownership/lifecycle transition summaries through the existing envelope with adapter parity and bounded metadata vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align the lifecycle projector and mounted detail surface to authored metadata</name>
  <files>rulestead/lib/rulestead/admin/lifecycle.ex, rulestead/lib/rulestead/store/ecto.ex, rulestead/lib/rulestead/fake.ex, rulestead/test/rulestead/admin_lifecycle_test.exs, rulestead/test/rulestead/store_ecto_admin_test.exs, rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex, rulestead_admin/test/rulestead_admin/live/flag_live/show_test.exs</files>
  <behavior>
    - Test 1: the shared projector exposes authored ownership and lifecycle facts plus advisory default provenance without persisting computed machine status.
    - Test 2: list/detail payloads and mounted detail rendering show owner ref/kind/display and lifecycle intent/override state consistently.
    - Test 3: the detail screen remains Phase 35-bounded and does not introduce Phase 36 readiness scoring or Phase 37 workbench actions.
  </behavior>
  <action>Per D-01, D-08, D-10, D-15 through D-18, and D-25, extend `Rulestead.Admin.Lifecycle` and the detail payload decorators so the mounted-admin read model exposes authored ownership and lifecycle facts as the basis for operator guidance, while keeping stale/archive-readiness opinion logic derived and off the runtime path. Update the mounted detail screen to render the new ownership object, authored lifecycle intent, default rationale, and override state in the existing calm detail layout, but defer code-reference signals, cleanup recommendations, filters, and archive actions to Phases 36 and 37.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/admin_lifecycle_test.exs test/rulestead/store_ecto_admin_test.exs && cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/flag_live/show_test.exs</automated>
  </verify>
  <done>The projector, adapter read models, and mounted detail UI all speak the same authored ownership/lifecycle contract and remain within the Phase 35 boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored mutation payloads -> audit metadata | Mutation summaries are derived from user-authored data before entering the append-only audit ledger |
| shared lifecycle projector -> mounted-admin detail surface | Read models present operator guidance and authored facts without leaking runtime or future-phase semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | R | `rulestead/lib/rulestead/audit_event.ex` | mitigate | Generate ownership/lifecycle summaries centrally from normalized before/after state so mutation history remains attributable and adapter-parity-safe. |
| T-35-06 | I | `rulestead/lib/rulestead/audit_event.ex` | mitigate | Keep summaries bounded to refs, kinds, authored lifecycle fields, and override provenance; do not store host-directory lookups or extra sensitive context. |
| T-35-07 | T | `rulestead/lib/rulestead/admin/lifecycle.ex` | mitigate | Preserve authored facts as the source for projections and avoid persisting computed lifecycle opinions that could drift from canonical authored truth. |
| T-35-08 | E | `rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex` | accept | The detail screen remains read-oriented in this phase and does not introduce new mutation actions, so residual privilege risk stays within the existing mounted-admin policy boundary. |
</threat_model>

<verification>
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/audit_event_governance_test.exs test/rulestead/store_ecto_admin_test.exs`
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/admin_lifecycle_test.exs test/rulestead/store_ecto_admin_test.exs`
- `cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/flag_live/show_test.exs`
</verification>

<success_criteria>
- Ownership/lifecycle changes are auditable through bounded summary metadata without replacing the canonical diff.
- Mounted-admin detail reads explain authored ownership and lifecycle intent through the shared projector.
- No Phase 36 readiness scoring, Phase 37 workbench UX, or runtime-path coupling is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/35-lifecycle-contract-ownership-metadata/35-02-SUMMARY.md`
</output>
