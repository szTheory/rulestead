---
phase: 35-lifecycle-contract-ownership-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/flag.ex
  - rulestead/lib/rulestead/flag/ownership.ex
  - rulestead/lib/rulestead/flag/lifecycle_metadata.ex
  - rulestead/lib/rulestead/admin/lifecycle_defaults.ex
  - rulestead/lib/rulestead/store/command.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/fake.ex
  - rulestead/priv/repo/migrations/20260523120000_add_phase35_ownership_lifecycle_metadata.exs
  - rulestead/test/rulestead/admin_lifecycle_test.exs
  - rulestead/test/rulestead/admin_contract_test.exs
  - rulestead/test/rulestead/store_ecto_admin_test.exs
  - rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex
  - rulestead_admin/test/rulestead_admin/live/flag_live/form_test.exs
autonomous: true
requirements:
  - LIF-01
must_haves:
  truths:
    - "Operators can author stable ownership metadata with an opaque owner reference, bounded kind, and optional display snapshot."
    - "Operators can author explicit lifecycle intent from the mounted admin without hidden runtime behavior or silent archive semantics."
    - "Create, update, fetch, and list flows preserve the new authored contract across Ecto and fake adapters while remaining backward-compatible with existing owner strings."
  artifacts:
    - path: "rulestead/lib/rulestead/flag/ownership.ex"
      provides: "Bounded authored ownership embed with `owner_ref`, `owner_kind`, and `owner_display` validation"
      min_lines: 30
    - path: "rulestead/lib/rulestead/flag/lifecycle_metadata.ex"
      provides: "Authored lifecycle metadata embed for explicit intent, review horizon, and override tracking"
      min_lines: 35
    - path: "rulestead/lib/rulestead/admin/lifecycle_defaults.ex"
      provides: "Admin-only lifecycle suggestion seam for create/edit flows"
      min_lines: 25
    - path: "rulestead/priv/repo/migrations/20260523120000_add_phase35_ownership_lifecycle_metadata.exs"
      provides: "Compatibility migration that introduces authored ownership/lifecycle metadata without a host-owned identity table"
      min_lines: 40
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex"
      provides: "Mounted admin authoring UI for owner contract and lifecycle suggestion/override controls"
      min_lines: 60
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex"
      to: "rulestead/lib/rulestead/admin/lifecycle_defaults.ex"
      via: "form initialization and validation use advisory lifecycle suggestions"
      pattern: "LifecycleDefaults|suggest"
    - from: "rulestead/lib/rulestead/store/command.ex"
      to: "rulestead/lib/rulestead/flag/ownership.ex"
      via: "command normalization emits the canonical authored ownership payload"
      pattern: "owner_ref|owner_kind|owner_display"
    - from: "rulestead/lib/rulestead/store/ecto.ex"
      to: "rulestead/lib/rulestead/flag.ex"
      via: "create/update flows persist the new embeds through the flag changeset"
      pattern: "cast_embed|ownership|lifecycle"
---

<objective>
Establish the authored ownership and lifecycle contract as durable flag metadata across schema, migration, command normalization, adapters, and mounted-admin authoring.

Purpose: Phase 35 must deliver explicit, host-friendly authored truth for lifecycle and ownership without introducing runtime coupling, owner-directory drift, or lifecycle automation.
Output: authored ownership/lifecycle embeds, a compatibility migration, normalized command/store flows, and mounted-admin authoring controls that follow one coherent contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/35-lifecycle-contract-ownership-metadata/35-CONTEXT.md
@.planning/phases/35-lifecycle-contract-ownership-metadata/35-RESEARCH.md
@.planning/phases/35-lifecycle-contract-ownership-metadata/35-PATTERNS.md
@rulestead/lib/rulestead/flag.ex
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/store/ecto.ex
@rulestead/lib/rulestead/fake.ex
@rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex

<interfaces>
From `rulestead/lib/rulestead/flag.ex`:
```elixir
schema "flags" do
  field(:owner, :string)
  field(:expected_expiration, :date)
  field(:permanent, :boolean, default: false)
end

@spec changeset(t(), map()) :: Ecto.Changeset.t()
```

From `rulestead/lib/rulestead/store/command.ex`:
```elixir
defmodule GovernanceSupport do
  def normalize_string(value)
  def normalize_map(value)
  def maybe_put(map, key, value)
end
```

From `rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex`:
```elixir
defp persist(:new, _flag_key, form_data) do
  Rulestead.create_flag(%{...})
end

defp persist(:edit, flag_key, form_data) do
  Rulestead.update_flag(flag_key, %{...})
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the authored ownership and lifecycle contract foundation</name>
  <files>rulestead/lib/rulestead/flag.ex, rulestead/lib/rulestead/flag/ownership.ex, rulestead/lib/rulestead/flag/lifecycle_metadata.ex, rulestead/lib/rulestead/admin/lifecycle_defaults.ex, rulestead/priv/repo/migrations/20260523120000_add_phase35_ownership_lifecycle_metadata.exs, rulestead/test/rulestead/admin_lifecycle_test.exs, rulestead/test/rulestead/admin_contract_test.exs</files>
  <behavior>
    - Test 1: a flag changeset accepts authored ownership metadata with `owner_ref`, bounded `owner_kind`, and optional `owner_display`, while rejecting blank refs or invalid kinds.
    - Test 2: lifecycle metadata stores authored intent plus advisory default provenance and requires explicit operator posture for `remote_config`.
    - Test 3: the lifecycle default seam returns suggestion, rationale, and override state without persisting computed stale/archive-ready statuses.
  </behavior>
  <action>Per D-01 through D-06 and D-09 through D-18, introduce one bounded ownership embed and one authored lifecycle embed on `Rulestead.Flag`, backed by a compatibility migration that backfills the current `owner`, `expected_expiration`, and `permanent` fields into the new shape without creating host-owned tables or runtime dependencies. Add a dedicated admin-only lifecycle default suggester module per D-11 through D-14, including the explicit `remote_config` exception. Keep legacy `owner` string input as migration/normalization compatibility only per D-24, and do not add computed lifecycle status columns, projection-refresh infrastructure, or archive-readiness scoring.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/admin_lifecycle_test.exs test/rulestead/admin_contract_test.exs test/rulestead/store_ecto_admin_test.exs</automated>
  </verify>
  <done>Schema, embeds, migration, and lifecycle default policy exist with contract tests proving explicit authored ownership/lifecycle truth and no persisted machine lifecycle opinions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire command normalization, adapter persistence, and mounted-admin authoring to the new contract</name>
  <files>rulestead/lib/rulestead/store/command.ex, rulestead/lib/rulestead/store/ecto.ex, rulestead/lib/rulestead/fake.ex, rulestead/test/rulestead/store_ecto_admin_test.exs, rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex, rulestead_admin/test/rulestead_admin/live/flag_live/form_test.exs</files>
  <behavior>
    - Test 1: create/update commands normalize manual owner entry and optional host-picker payload into a canonical ownership object keyed by `owner_ref`.
    - Test 2: when a host validation seam is configured, create/update flows honor both accepted and rejected validation outcomes for picker-supplied or manually entered owner references without inventing an owner directory.
    - Test 3: Ecto and fake adapters persist and read back the authored ownership/lifecycle contract with parity across create, update, list, and fetch paths.
    - Test 4: the mounted admin form shows lifecycle suggestions with explicit override controls, consumes the optional host picker/validation seam when present, and preserves manual opaque `owner_ref` fallback when it is absent.
  </behavior>
  <action>Per D-03 through D-08, D-11 through D-17, and D-24, add shared normalization helpers in `Rulestead.Store.Command` that accept manual entry and host-supplied picker data while always producing one canonical ownership payload. Explicitly include an optional host validation seam for owner references in mounted-admin create/update flows: when the host provides it, picker-selected or manually entered owner values must be validated before persistence, with rejected validation outcomes surfaced back through the authoring flow; when the host does not provide it, manual opaque `owner_ref` entry remains the supported fallback. Update Ecto and fake create/update/list/fetch flows to persist and expose the authored embeds, preserving backward compatibility for older owner strings and the linked-version sibling-package boundary. Extend the mounted form to author `owner_ref`, `owner_kind`, optional `owner_display`, and lifecycle suggestion/override state from `Rulestead.Admin.LifecycleDefaults`, while keeping all normalization logic out of the template and avoiding any Phase 36/37 readiness or workbench UX.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/store_ecto_admin_test.exs test/rulestead/admin_contract_test.exs && cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/flag_live/form_test.exs</automated>
  </verify>
  <done>Public command/store flows and mounted-admin authoring all use the same authored ownership/lifecycle contract, with Ecto/fake parity and automated form coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mounted admin form -> command normalization | Untrusted operator input crosses from the UI into the durable ownership/lifecycle contract |
| migration backfill -> authored flag rows | Existing freeform owner/lifecycle data is transformed into the new bounded contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | T | `rulestead/lib/rulestead/flag.ex` and new embeds | mitigate | Enforce bounded enums, required fields, and compatibility normalization in changesets so malformed ownership/lifecycle payloads cannot become durable truth. |
| T-35-02 | S | `rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex` | mitigate | Accept host-picker data only as opaque references per D-07 and validate the same manual-entry contract, preventing the UI from impersonating a host-owned directory. |
| T-35-03 | E | `rulestead/lib/rulestead/store/command.ex` | mitigate | Keep normalization centralized and public-facade-backed so write paths cannot bypass the bounded ownership/lifecycle contract. |
| T-35-04 | I | `rulestead/priv/repo/migrations/20260523120000_add_phase35_ownership_lifecycle_metadata.exs` | mitigate | Backfill only the bounded owner snapshot and authored lifecycle facts; do not store host-linked identity data or computed lifecycle opinions. |
</threat_model>

<verification>
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/admin_lifecycle_test.exs test/rulestead/admin_contract_test.exs`
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/store_ecto_admin_test.exs test/rulestead/admin_contract_test.exs`
- `cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/flag_live/form_test.exs`
</verification>

<success_criteria>
- Authored flags now have one explicit ownership contract and one explicit lifecycle-intent contract, both host-friendly and backward-compatible.
- Mounted-admin create/edit flows expose lifecycle suggestions as advisory scaffolding with explicit operator override.
- Ecto and fake adapters persist the new authored truth without widening runtime evaluation or Phase 36/37 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/35-lifecycle-contract-ownership-metadata/35-01-SUMMARY.md`
</output>
