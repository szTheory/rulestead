---
phase: 125-version-truth-sweep-release-docs
plan: 03
type: execute
wave: 2
depends_on:
  - 125-01
files_modified:
  - guides/introduction/upgrading.md
  - MAINTAINING.md
  - brandbook/CHANGELOG-PREAMBLE-1.0.md
autonomous: false
requirements:
  - REL-03
must_haves:
  truths:
    - "upgrading.md has an 'Upgrading 0.1.x → 1.0' section stating zero code changes / dep-pin bump only / 'promotion, not rewrite' (per ROADMAP SC-4, D-07)"
    - "MAINTAINING.md has a 'Cutting a major (X.0.0)' runbook covering Release-As, package sequence, deprecation-window checklist, mandatory post-cut Release-As removal (per ROADMAP SC-5, D-07)"
    - "The runbook states open_feature_rulestead is NOT release-please managed (manual publish, Phase 129) — only rulestead + rulestead_admin are linked-versions (D-08)"
    - "brandbook/CHANGELOG-PREAMBLE-1.0.md exists as a staged two-package 'promotion, not rewrite' preamble, NOT committed into the bot-managed CHANGELOGs (per ROADMAP SC-6, D-09)"
  artifacts:
    - path: "guides/introduction/upgrading.md"
      provides: "Upgrading 0.1.x → 1.0 section"
      contains: "Upgrading 0.1.x"
    - path: "MAINTAINING.md"
      provides: "Cutting a major (X.0.0) runbook"
      contains: "Cutting a major"
    - path: "brandbook/CHANGELOG-PREAMBLE-1.0.md"
      provides: "Staged two-package 1.0.0 CHANGELOG preamble"
      contains: "Promotion, not rewrite"
  key_links:
    - from: "MAINTAINING.md"
      to: ".github/workflows/release-please.yml + release-please-config.json"
      via: "Runbook documents Release-As mechanism + mandatory post-cut removal (does not execute it — Phase 128)"
      pattern: "Release-As"
---

<objective>
Author the three release docs adopters and maintainers need BEFORE the Phase 128 cut:
the "Upgrading 0.1.x → 1.0" section in `upgrading.md` (D-07), the "Cutting a major (X.0.0)"
runbook in `MAINTAINING.md` (D-07/D-08), and the staged `brandbook/CHANGELOG-PREAMBLE-1.0.md`
artifact (D-09). All three carry the "promotion, not rewrite" through-line: a `1.0.0` that is
the same battle-tested code, just honestly versioned — explicit zero breaking changes, only a
dep-pin bump.

This phase only DOCUMENTS the runbook and STAGES the preamble. It does NOT cut the release,
edit `.release-please-manifest.json`, or write into the bot-managed CHANGELOGs (Phase 128).

Purpose: REL-03 / ROADMAP SC-4, SC-5, SC-6.
Output: additive section in upgrading.md + runbook in MAINTAINING.md + NEW brandbook preamble.

## Artifacts this phase produces (this plan's slice)
- NEW section "Upgrading 0.1.x → 1.0" in `guides/introduction/upgrading.md`
- NEW runbook section "Cutting a major (X.0.0)" in `MAINTAINING.md`
- NEW `brandbook/CHANGELOG-PREAMBLE-1.0.md` (staged two-package release preamble)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-version-truth-sweep-release-docs/125-CONTEXT.md
@.planning/phases/125-version-truth-sweep-release-docs/125-RESEARCH.md
@.planning/phases/125-version-truth-sweep-release-docs/125-PATTERNS.md
@brandbook/RELEASE-TEMPLATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the "Upgrading 0.1.x → 1.0" section to upgrading.md</name>
  <files>guides/introduction/upgrading.md</files>
  <read_first>
    - CONTEXT D-07 (the upgrading.md additive section: zero code changes, dep-pin bump, "promotion, not rewrite")
    - RESEARCH §"Gap 4 — upgrading.md ... Current Structure" (insertion point: new H2 immediately after the L5 intro, BEFORE the reframed callout)
    - PATTERNS §"guides/introduction/upgrading.md" (analog = the file's own existing H2 sections; coupling note to the L262 contract assert)
    - The live `guides/introduction/upgrading.md` AS SWEPT BY PLAN 01 (this plan runs in Wave 2 after 125-01 — the L3-8 callout is already reframed; do not re-sweep)
  </read_first>
  <action>
    Add a new H2 section titled exactly `## Upgrading 0.1.x → 1.0` immediately after the L5 intro prose,
    BEFORE the (already-reframed-by-Plan-01) callout — so it is the first section a reader hits. Content:
    - State explicitly there are ZERO code changes — the `1.0.0` is the same battle-tested code, honestly versioned.
    - The upgrade is a dependency-pin bump ONLY: `~> 0.1` → `~> 1.0` (this is upgrade-instruction context;
      the `~> 0.1` here is the FROM side of an upgrade arrow and is allowed — but note this file IS scanned
      by `check_version_truth.py`, so anchor the pin reference so it does not trip the guard: write the pin
      transition using the heading's `0.1.x → 1.0` arrow framing and `~> 1.0` for the install line. Confirm
      `python3 scripts/check_version_truth.py` stays green after this edit — see acceptance criteria).
    - Use explicit "promotion, not rewrite" framing.
    Keep the section idiomatic to the file's existing H2 prose style. Do NOT restructure the rest of the file.

    Guard-compatibility note: because `guides/introduction/upgrading.md` is in the guard's scan set, the
    literal `~> 0.1` (without a closing `.N`) anywhere in this section would trip `~> 0\.1(?![.\d])`. Phrase
    the dep-pin transition so the only forward-version pin token written is `~> 1.0`, and express the FROM
    side as part of the `0.1.x → 1.0` heading arrow / prose rather than a bare `~> 0.1` pin line. Verify the
    guard is green at task end.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead &amp;&amp; grep -q "Upgrading 0.1.x" guides/introduction/upgrading.md &amp;&amp; grep -qi "promotion" guides/introduction/upgrading.md &amp;&amp; python3 scripts/check_version_truth.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Upgrading 0.1.x → 1.0" guides/introduction/upgrading.md` returns 1.
    - `grep -qi "promotion" guides/introduction/upgrading.md` succeeds (exit 0).
    - `grep -qi "zero" guides/introduction/upgrading.md` OR `grep -qi "no code change" guides/introduction/upgrading.md` succeeds (zero-breaking-changes language present).
    - `python3 scripts/check_version_truth.py` exits 0 (the new section did not reintroduce a guard-tripping `~> 0.1`).
  </acceptance_criteria>
  <done>upgrading.md has the "Upgrading 0.1.x → 1.0" H2 stating zero code changes / dep-pin-bump / "promotion, not rewrite"; the drift guard stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the "Cutting a major (X.0.0)" runbook to MAINTAINING.md</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - CONTEXT D-07 (runbook contents), D-08 (open_feature is NOT release-please managed — manual, Phase 129; only rulestead + rulestead_admin are linked-versions)
    - RESEARCH §"Gap 3 — release-please Mechanics for the MAINTAINING Runbook" (verified facts: linked packages, Release-As config-block path, mandatory post-cut removal, bump-minor-pre-major no-op post-1.0, publish order, manifest is 0.1.7 generically referenced)
    - RESEARCH §"Gap 4 — ... MAINTAINING.md" (insertion point: after `## Gated publish choreography` ~L188, before `## Manual recovery path` L189)
    - PATTERNS §"MAINTAINING.md" (analog = the file's own `## Release Please flow` / `## Gated publish choreography` heading + prose + checklist style; coupling to the L285 contract assert)
    - The live `MAINTAINING.md` AS SWEPT BY PLAN 01 (Wave 2 — L11-12/L167/L592 already reframed; do not re-sweep)
  </read_first>
  <action>
    Add a new section titled exactly `## Cutting a major (X.0.0)` after `## Gated publish choreography`
    (ends ~L188) and before `## Manual recovery path` (L189), mirroring the existing release sections'
    heading + numbered/checklist style. The runbook MUST cover (D-07/D-08):
    - The `Release-As` mechanism: add `"release-as": "1.0.0"` into the `rulestead` block of
      `release-please-config.json` as the major-cut path (note the `release-please.yml` bootstrap echo of
      `Release-As: 0.1.0` is historical, not the major-cut path). Reference the manifest generically — do
      not hard-code a fixed current version.
    - The package publish sequence: `rulestead` first, then `rulestead_admin` (linked-versions move together).
    - The deprecation-window checklist, tied to the Phase-124 `api_stability.md` soft→hard→remove-on-major policy.
    - The MANDATORY post-cut removal step: after the `1.0.0` PR merges, `"release-as": "1.0.0"` MUST be removed
      from `release-please-config.json`, or release-please re-proposes `1.0.0` forever.
    - D-08 accuracy (explicit): `open_feature_rulestead` is NOT release-please managed — it is a separate
      MANUAL publish (Phase 129), strictly after `rulestead@1.0.0` is live. Only `rulestead` +
      `rulestead_admin` are linked-versions. The runbook must say this explicitly rather than imply
      release-please cuts all three.
    - Optionally note `bump-minor-pre-major` / `bump-patch-for-minor-pre-major` become no-ops post-1.0.

    Do NOT edit `release-please-config.json`, `.release-please-manifest.json`, or the workflow — the runbook
    DOCUMENTS these; the actual cut is Phase 128.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead &amp;&amp; grep -q "## Cutting a major" MAINTAINING.md &amp;&amp; grep -q "Release-As" MAINTAINING.md &amp;&amp; grep -qi "open_feature" MAINTAINING.md &amp;&amp; python3 scripts/check_version_truth.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Cutting a major (X.0.0)" MAINTAINING.md` returns 1.
    - `grep -q "Release-As" MAINTAINING.md` succeeds (the mechanism is named).
    - `grep -qi "remov" MAINTAINING.md` near the runbook AND `grep -q "release-as" MAINTAINING.md` — the mandatory post-cut removal step is present.
    - `grep -qi "open_feature" MAINTAINING.md` AND `grep -qi "manual" MAINTAINING.md` — D-08 manual-publish note present.
    - `grep -qi "deprecat" MAINTAINING.md` — deprecation-window checklist present.
    - `python3 scripts/check_version_truth.py` exits 0 (runbook did not reintroduce a guard-tripping `~> 0.1`).
    - `git diff --name-only` does NOT include `release-please-config.json`, `.release-please-manifest.json`, or `.github/workflows/release-please.yml`.
  </acceptance_criteria>
  <done>MAINTAINING.md has the "Cutting a major (X.0.0)" runbook covering Release-As, the rulestead→rulestead_admin sequence, the deprecation-window checklist, the mandatory post-cut Release-As removal, and the explicit D-08 manual-provider note; the drift guard stays green and no release-please config files were edited.</done>
</task>

<task type="auto">
  <name>Task 3: Create the staged brandbook/CHANGELOG-PREAMBLE-1.0.md artifact</name>
  <files>brandbook/CHANGELOG-PREAMBLE-1.0.md</files>
  <read_first>
    - CONTEXT D-09 (staged artifact in brandbook/, NOT committed into the bot-managed CHANGELOGs)
    - RESEARCH §"Gap 5 — CHANGELOG Preamble Artifact" (recommended shape + the bot-managed-CHANGELOG collision rationale)
    - PATTERNS §"brandbook/CHANGELOG-PREAMBLE-1.0.md" (analog = brandbook/RELEASE-TEMPLATE.md; microcopy: operator-consequence-first, state what changed and what did not, no hype)
    - `brandbook/RELEASE-TEMPLATE.md` (the established sibling staging artifact + microcopy rules)
  </read_first>
  <action>
    Create `brandbook/CHANGELOG-PREAMBLE-1.0.md`: a short two-package Markdown preamble meant to be pasted
    ABOVE release-please's generated `1.0.0` bullets during the Phase 128 release PR. Inherit the
    `RELEASE-TEMPLATE.md` tone (operator-consequence-first, state what changed and what did NOT, no hype).
    Content (D-09 / "promotion, not rewrite"):
    - A heading framing `1.0.0` as "Promotion, not rewrite".
    - State `rulestead` and `rulestead_admin` graduate to `1.0.0` together (linked versions) — same
      battle-tested code, honestly versioned, EXPLICIT zero breaking changes.
    - No public API changes (point at `guides/api_stability.md`).
    - Upgrade is a dependency-pin bump only (point at `guides/introduction/upgrading.md`).
    Note: this file legitimately contains `0.1` / `~> 0.1` in upgrade-instruction context, but it lives in
    `brandbook/` which the guard does NOT scan — no conflict with `check_version_truth.py`.
    Do NOT write into `rulestead/CHANGELOG.md` or `rulestead_admin/CHANGELOG.md` (bot-managed, D-09).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead &amp;&amp; test -f brandbook/CHANGELOG-PREAMBLE-1.0.md &amp;&amp; grep -qi "promotion" brandbook/CHANGELOG-PREAMBLE-1.0.md &amp;&amp; grep -qi "rulestead_admin" brandbook/CHANGELOG-PREAMBLE-1.0.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f brandbook/CHANGELOG-PREAMBLE-1.0.md` succeeds.
    - `grep -qi "promotion" brandbook/CHANGELOG-PREAMBLE-1.0.md` succeeds.
    - `grep -qi "zero breaking" brandbook/CHANGELOG-PREAMBLE-1.0.md` OR `grep -qi "no public api" brandbook/CHANGELOG-PREAMBLE-1.0.md` succeeds (zero-breaking-changes framing).
    - `grep -qi "rulestead_admin" brandbook/CHANGELOG-PREAMBLE-1.0.md` succeeds (two-package framing).
    - `git diff --name-only` does NOT include `rulestead/CHANGELOG.md` or `rulestead_admin/CHANGELOG.md` (bot-managed, untouched).
  </acceptance_criteria>
  <done>brandbook/CHANGELOG-PREAMBLE-1.0.md exists as a two-package "promotion, not rewrite" preamble with explicit zero-breaking-changes framing; the bot-managed CHANGELOGs are untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — confirm release-doc clarity + green drift guard</name>
  <action>Pause for human verification of the release-doc clarity and the green drift guard (see how-to-verify). Do not proceed until approved.</action>
  <what-built>The "Upgrading 0.1.x → 1.0" section in upgrading.md, the "Cutting a major (X.0.0)" runbook in MAINTAINING.md, and the staged brandbook/CHANGELOG-PREAMBLE-1.0.md artifact.</what-built>
  <how-to-verify>
    1. Read the new upgrading.md section — confirm the "promotion, not rewrite" framing is genuinely CLEAR (not just keyword-present): explicit zero breaking changes + dep-pin-only bump.
    2. Read the MAINTAINING.md runbook — confirm it names the `Release-As` mechanism, the rulestead→rulestead_admin sequence, the deprecation-window checklist, the mandatory post-cut `release-as` removal, and explicitly states `open_feature_rulestead` is a MANUAL publish (Phase 129), not release-please managed.
    3. Read brandbook/CHANGELOG-PREAMBLE-1.0.md — confirm it is two-package, "promotion not rewrite", explicit zero breaking changes, and reads as ready-to-paste release-PR text.
    4. Confirm `python3 scripts/check_version_truth.py` is green (no doc edit reintroduced stale version language).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. runbook missing the post-cut removal step, preamble not clearly two-package, weak promotion framing).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook ↔ release pipeline | The MAINTAINING runbook documents (does not execute) the `Release-As` cut; the staged preamble lives outside the bot-managed CHANGELOGs. No runtime input crosses any boundary. |

## STRIDE Threat Register

This phase authors release docs and a staged text artifact — no runtime attack surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-04 | Information Disclosure | runbook accuracy (implying release-please cuts open_feature) | mitigate | D-08: the runbook explicitly states open_feature_rulestead is a MANUAL publish (Phase 129); acceptance criteria grep-assert the manual note. A wrong runbook would mislead a future maintainer into a broken cut. |
| T-125-05 | Tampering | accidental edit of bot-managed CHANGELOGs / release-please config | mitigate | The preamble is staged in `brandbook/` (D-09); acceptance criteria assert `git diff` excludes the CHANGELOGs and release-please config files. |
| T-125-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- SC-4: `grep -q "Upgrading 0.1.x" guides/introduction/upgrading.md` AND `grep -qi "promotion" ...` succeed.
- SC-5: `grep -q "## Cutting a major" MAINTAINING.md` AND the Release-As / post-cut-removal / manual-provider / deprecation-window greps succeed.
- SC-6: `test -f brandbook/CHANGELOG-PREAMBLE-1.0.md` AND `grep -qi "promotion" ...` succeed.
- The drift guard (`python3 scripts/check_version_truth.py`) stays green after all doc edits.
- No release-please config / bot CHANGELOG files were edited.
</verification>

<success_criteria>
- upgrading.md has a clear "Upgrading 0.1.x → 1.0" section (zero code changes / dep-pin bump / promotion-not-rewrite).
- MAINTAINING.md has a "Cutting a major (X.0.0)" runbook (Release-As, package sequence, deprecation-window, mandatory post-cut removal, explicit D-08 manual-provider note).
- brandbook/CHANGELOG-PREAMBLE-1.0.md is staged as a two-package "promotion, not rewrite" preamble.
- The drift guard stays green; bot-managed CHANGELOGs and release-please config are untouched.
</success_criteria>

<output>
Create `.planning/phases/125-version-truth-sweep-release-docs/125-03-SUMMARY.md` when done.
</output>