---
phase: 125-version-truth-sweep-release-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - rulestead/README.md
  - rulestead_admin/README.md
  - open_feature_rulestead/README.md
  - guides/cheatsheet.cheatmd
  - guides/introduction/getting-started.md
  - guides/introduction/phoenix-integration-spine.md
  - guides/introduction/product-boundary.md
  - guides/introduction/installation.md
  - guides/introduction/upgrading.md
  - guides/flows/telemetry.md
  - MAINTAINING.md
  - rulestead/test/rulestead/release_contract_test.exs
autonomous: false
requirements:
  - REL-02
must_haves:
  truths:
    - "Criterion-1 grep over the shipped surface returns zero hits (per ROADMAP SC-1)"
    - "The README 'Two version lines' callout is deleted entirely (per ROADMAP SC-2, D-01)"
    - "Every shipped install snippet reads ~> 1.0 (not ~> 0.1)"
    - "release_contract_test.exs is green after re-anchor — the bidirectional guard still enforces version truth (D-10)"
    - "The legitimate third-party pin {:open_feature, \"~> 0.1.3\"} is preserved verbatim (D-04)"
    - "The demo README 0.1.x assert (L265) is unchanged — demo README is out of sweep scope (D-03)"
  artifacts:
    - path: "rulestead/test/rulestead/release_contract_test.exs"
      provides: "Re-anchored bidirectional doc-to-code version-truth guard"
      contains: "~> 1.0"
    - path: "README.md"
      provides: "Root README swept to 1.0 truth, callout deleted"
      contains: "~> 1.0"
  key_links:
    - from: "rulestead/test/rulestead/release_contract_test.exs"
      to: "README.md, rulestead/README.md, rulestead_admin/README.md, guides/introduction/upgrading.md, MAINTAINING.md"
      via: "File.read! + =~ asserts read the real swept files; re-anchored asserts must match swept prose"
      pattern: "=~ \"~> 1.0\""
---

<objective>
Atomically sweep the 13 shipped doc files from `0.1.x` release language to `1.0` truth
AND re-anchor the 6 collision asserts in `release_contract_test.exs` in lockstep — in the
SAME change — so the bidirectional doc↔code guard never goes red mid-phase (D-10).

This is the landmine plan: the contract test reads the real files via `File.read!` and
asserts strings the sweep removes. Editing docs without the test re-anchor reds the suite;
editing the test without the sweep leaves a hole. Both land together.

Purpose: Make every shipped file tell the truth about the `1.x` line (REL-02, ROADMAP
SC-1/SC-2) while keeping the existing release-contract guard green and still enforcing.
Output: 13 swept doc files (callout deleted) + 6 re-anchored test asserts.

## Artifacts this phase produces (this plan's slice)
- MODIFIED: 13 shipped doc files (string reframes + "Two version lines" callout deletion)
- MODIFIED: `rulestead/test/rulestead/release_contract_test.exs` (6 assert re-anchors; L265 survivor untouched)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-version-truth-sweep-release-docs/125-CONTEXT.md
@.planning/phases/125-version-truth-sweep-release-docs/125-RESEARCH.md
@.planning/phases/125-version-truth-sweep-release-docs/125-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic version-truth sweep of the 13 shipped doc files</name>
  <files>README.md, rulestead/README.md, rulestead_admin/README.md, open_feature_rulestead/README.md, guides/cheatsheet.cheatmd, guides/introduction/getting-started.md, guides/introduction/phoenix-integration-spine.md, guides/introduction/product-boundary.md, guides/introduction/installation.md, guides/introduction/upgrading.md, guides/flows/telemetry.md, MAINTAINING.md</files>
  <read_first>
    - RESEARCH §"Gap 6 — Full File-by-File Sweep Map" (authoritative per-line action table — every hit, every file, verified live)
    - CONTEXT D-01 (the 13-file inventory), D-02 (CONTRIBUTING zero hits), D-03 (demo README NOT swept), D-04 (three exclusion classes preserved verbatim)
    - PATTERNS §"11 remaining swept docs" and §"guides/introduction/upgrading.md" (callout syntax + insertion notes)
    - The live files themselves before editing each (line numbers in the sweep map were verified 2026-06-18; re-confirm with grep if drift suspected)
  </read_first>
  <action>
    Apply the authoritative sweep map in RESEARCH §Gap 6 to all 13 files. Replace `~> 0.1`→`~> 1.0`,
    `0.1.x`/`v0.1.x`→`1.0`/`1.x` (reframe idiomatically per sentence, do not mechanically string-swap),
    and DELETE the "Two version lines" callout blocks entirely where the map says DELETE (root README
    L6-10 is a full-block delete per criterion 2; package READMEs and guides reframe or delete per the map).

    Reframe — do not just delete — version-line callouts in `rulestead/README.md`, `rulestead_admin/README.md`,
    `guides/introduction/getting-started.md`, `guides/introduction/installation.md`, and
    `guides/introduction/upgrading.md` so the surrounding install/hero prose stays coherent and shows `~> 1.0`.
    For `guides/introduction/upgrading.md` and `MAINTAINING.md` in THIS task perform ONLY the in-place string
    reframe of the existing lines (upgrading L3-4/L8/L10/L24/L30; MAINTAINING L11-12/L167/L592) — the NEW
    additive sections (the "Upgrading 0.1.x → 1.0" section and the "Cutting a major" runbook) are Plan 03, not here.

    HARD EXCLUSIONS (preserve verbatim, D-04):
    - `open_feature_rulestead/README.md:28` `{:open_feature, "~> 0.1.3"}` — NEVER touch (real upstream pin;
      editing it bricks adopter `mix deps.get`). Only L29 (`{:open_feature_rulestead, "~> 0.1"}`→`~> 1.0`)
      and L14-15 prose are swept in that file.
    - Do NOT sweep `v0.1.0` historical strings (RESEARCH Pitfall 3) — they are NOT criterion-1 patterns and
      pervade `guides/`. Touching them is scope creep and risks breaking unrelated `release_contract_test.exs` text.
    - Do NOT touch `examples/demo/README.md` (D-03), `.planning/`, `prompts/`, or generated `rulestead/doc/` (D-04).
    - `CONTRIBUTING.md` has zero hits (D-02) — do not edit it.

    Leave the `v1.0.0` GA facts (e.g. the 2026-05-21 GA date) intact wherever a reframed callout mentions them —
    those are true and several contract-test asserts depend on them (L232/261/283/284).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead &amp;&amp; grep -rn '0\.1\.x\|~> 0\.1[^.0-9]\|0\.1\.7\|future.*1\.0\|1\.0 API freeze\|Two version lines' README.md rulestead/README.md rulestead_admin/README.md open_feature_rulestead/README.md guides/ MAINTAINING.md CONTRIBUTING.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - The criterion-1 grep (above) returns ZERO matching lines (grep exit code 1 = no match) across the shipped surface.
    - `grep -n 'open_feature, "~> 0.1.3"' open_feature_rulestead/README.md` still returns the line (third-party pin preserved, D-04).
    - `grep -c "Two version lines" README.md` returns 0 (root README callout deleted, SC-2).
    - `grep -c '~> 1\.0' README.md` returns at least 1 (install snippet reframed).
    - `git diff --name-only` does NOT include `examples/demo/README.md`, `CONTRIBUTING.md`, `.planning/`, `prompts/`, or `rulestead/doc/`.
  </acceptance_criteria>
  <done>Criterion-1 grep returns zero hits in the shipped surface; the root "Two version lines" callout is gone; install snippets show `~> 1.0`; the `~> 0.1.3` third-party pin and all out-of-scope paths are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Re-anchor the 6 contract-test asserts in lockstep</name>
  <files>rulestead/test/rulestead/release_contract_test.exs</files>
  <read_first>
    - RESEARCH §"Gap 1 — Exact Contract-Test Surgery Map" (the verified 7-row flip table)
    - RESEARCH Pitfall 2 (CONTEXT D-10 over-counts: there is exactly ONE `maintaining =~ "0.1.x"` at L285, NOT two)
    - PATTERNS §"release_contract_test.exs" (re-anchor rule + the keep-list)
    - The live test file L224-308 (path consts L8-18; both target tests)
  </read_first>
  <action>
    Re-anchor exactly 6 asserts to positive `1.0`-truth substrings that the Task-1 sweep actually introduced
    in the same files (verify by running the test — the asserted substring MUST literally appear in the swept output):
    - L233 `root_readme =~ "0.1.x"` → flip to a forward string present in the swept root README (e.g. `root_readme =~ "~> 1.0"`).
    - L234 `root_readme =~ "Two version lines"` → DELETE this assert and REPLACE it with a POSITIVE guard
      (e.g. `assert root_readme =~ "~> 1.0"`) so version truth stays enforced — do NOT leave a hole.
    - L249 `runtime_readme =~ "0.1.x"` → flip to a string present in swept `rulestead/README.md` (e.g. `=~ "~> 1.0"`).
    - L254 `admin_readme =~ "0.1.x"` → flip to a string present in swept `rulestead_admin/README.md` (e.g. `=~ "~> 1.0"`).
    - L262 `upgrading =~ "0.1.x"` → flip DELIBERATELY to `upgrading =~ "~> 1.0"` (do NOT leave a bare `=~ "0.1.x"`
      that passes by coincidence on the future "Upgrading 0.1.x → 1.0" heading — Pitfall 2 / Open Question 2).
    - L285 `maintaining =~ "0.1.x"` → flip to a string present in swept `MAINTAINING.md` (e.g. `=~ "1.0"` or
      `=~ "Cutting a major"` once Plan 03 lands; for THIS plan anchor to a swept-line string like `=~ "1.0"` that
      already exists after Task 1's MAINTAINING reframe).

    DO NOT touch (they already assert `v1.0.0`/GA-date truth and stay green): L232, L261, L283, L284.
    SURVIVOR — leave UNCHANGED: L265 `demo_readme =~ "0.1.x"` (demo README is out of sweep scope, D-03).
    There is NO second `maintaining =~ "0.1.x"` assert — do not hunt for a phantom one (Pitfall 2).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead &amp;&amp; mix test test/rulestead/release_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `cd rulestead && mix test test/rulestead/release_contract_test.exs` reports 0 failures.
    - `grep -c 'demo_readme =~ "0.1.x"' rulestead/test/rulestead/release_contract_test.exs` returns 1 (survivor intact).
    - `grep -c 'Two version lines' rulestead/test/rulestead/release_contract_test.exs` returns 0 (assert deleted).
    - `grep -c '=~ "0.1.x"' rulestead/test/rulestead/release_contract_test.exs` returns exactly 1 (only the demo survivor remains; all other 0.1.x asserts flipped).
    - `grep -c 'maintaining =~ "0.1.x"' rulestead/test/rulestead/release_contract_test.exs` returns 0.
  </acceptance_criteria>
  <done>The contract test is green; exactly one `=~ "0.1.x"` assert remains (the demo survivor L265); the "Two version lines" assert is replaced by a positive `~> 1.0` guard; L262 anchors deliberately to `~> 1.0`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — confirm sweep coherence + green contract test</name>
  <action>Pause for human verification of the sweep coherence and the green contract test (see how-to-verify). Do not proceed until approved.</action>
  <what-built>The 13-file version-truth sweep (root README "Two version lines" callout deleted entirely; all install snippets reframed to `~> 1.0`) plus the 6 re-anchored `release_contract_test.exs` asserts.</what-built>
  <how-to-verify>
    1. Read the root `README.md` and the 3 package READMEs after the sweep. Confirm the "Two version lines"
       callout block is GONE (not merely reworded) and no orphaned admonition syntax (`> [!NOTE]` fragments,
       dangling blockquote lines) is left behind.
    2. Confirm every install snippet now shows `{:rulestead, "~> 1.0"}` / `{:rulestead_admin, "~> 1.0"}`.
    3. Confirm `open_feature_rulestead/README.md:28` still shows `{:open_feature, "~> 0.1.3"}` (the upstream pin — must NOT change).
    4. Spot-read the reframed install/hero prose in `rulestead/README.md` and `guides/introduction/installation.md`
       — it should read coherently with the callout removed.
    5. Confirm `cd rulestead && mix test test/rulestead/release_contract_test.exs` is green.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. orphaned callout syntax, incoherent prose, accidental v0.1.0 sweep).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs ↔ CI guard | The contract test and (later) the drift guard read on-disk doc strings; a sweep that flips a string the test asserts is a coherence boundary, not an attack surface. |

## STRIDE Threat Register

This phase adds NO runtime attack surface — it is a docs reframe plus test-assertion edits.
The honest threat model is "no new attack surface."

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Tampering | `open_feature_rulestead/README.md:28` third-party pin | mitigate | Hard exclusion (D-04); editing `~> 0.1.3` would brick adopter `mix deps.get`. Acceptance criteria grep-asserts the line is preserved. |
| T-125-02 | Denial of Service | `release_contract_test.exs` red-after-sweep | mitigate | Lockstep re-anchor in the SAME plan/wave (D-10); the human checkpoint confirms the suite is green before the wave merges. |
| T-125-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (zero new dependencies); nothing to verify. |
</threat_model>

<verification>
- SC-1: `grep -rn '0\.1\.x\|~> 0\.1[^.0-9]\|0\.1\.7\|future.*1\.0\|1\.0 API freeze\|Two version lines' README.md rulestead/README.md rulestead_admin/README.md open_feature_rulestead/README.md guides/ MAINTAINING.md CONTRIBUTING.md` returns zero hits.
- SC-2: `grep -c "Two version lines" README.md` returns 0.
- Lockstep: `cd rulestead && mix test test/rulestead/release_contract_test.exs` is green.
- D-04: `{:open_feature, "~> 0.1.3"}` still present in `open_feature_rulestead/README.md`.
- D-03: `demo_readme =~ "0.1.x"` (L265) unchanged.
</verification>

<success_criteria>
- Criterion-1 grep returns zero hits across the shipped surface (ROADMAP SC-1).
- The README "Two version lines" callout is deleted entirely (ROADMAP SC-2).
- `release_contract_test.exs` is green with exactly one `=~ "0.1.x"` assert remaining (the demo survivor).
- The third-party `~> 0.1.3` pin and all out-of-scope paths (demo README, CONTRIBUTING, `.planning/`, `prompts/`, generated `doc/`, `v0.1.0` strings) are untouched.
</success_criteria>

<output>
Create `.planning/phases/125-version-truth-sweep-release-docs/125-01-SUMMARY.md` when done.
</output>
