---
phase: 125-version-truth-sweep-release-docs
plan: 02
type: execute
wave: 2
depends_on:
  - 125-01
files_modified:
  - scripts/check_version_truth.py
  - scripts/ci/lint.sh
autonomous: true
requirements:
  - REL-02
must_haves:
  truths:
    - "scripts/check_version_truth.py exits 0 on the now-clean tree (per ROADMAP SC-3)"
    - "The guard exits 1 when ~> 0.1 (or any criterion-1 pattern) reappears in the shipped surface"
    - "The guard does NOT flag the legitimate third-party pin ~> 0.1.3 (the (?![.\\d]) lookahead landmine)"
    - "The guard is wired into scripts/ci/lint.sh and bash scripts/ci/lint.sh exits 0"
  artifacts:
    - path: "scripts/check_version_truth.py"
      provides: "Fail-closed CI drift guard scanning the criterion-1 shipped surface"
      contains: "(?![.\\d])"
    - path: "scripts/ci/lint.sh"
      provides: "Guard wired into the Python-guard block"
      contains: "check_version_truth.py"
  key_links:
    - from: "scripts/ci/lint.sh"
      to: "scripts/check_version_truth.py"
      via: "python3 invocation in the L43-69 guard block under set -euo pipefail (fail-closed)"
      pattern: "check_version_truth.py"
---

<objective>
Ship the fail-closed CI drift guard `scripts/check_version_truth.py` and wire it into
`scripts/ci/lint.sh` so stale `0.1.x` release language can never silently reappear in the
shipped doc surface — while never false-positiving on the legitimate third-party
`{:open_feature, "~> 0.1.3"}` pin (the `(?![.\d])` negative-lookahead landmine, D-06).

Wave 2: the guard must run AFTER Plan 01's sweep makes the tree clean, else it fails on its
first run.

Purpose: REL-02 / ROADMAP SC-3 — a CI drift guard, same posture as the existing 8
`check_*.py` brand-token guards.
Output: NEW `scripts/check_version_truth.py` + one-line wiring in `scripts/ci/lint.sh`.

## Artifacts this phase produces (this plan's slice)
- NEW `scripts/check_version_truth.py` (fail-closed CI drift guard)
- MODIFIED `scripts/ci/lint.sh` (guard wiring)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-version-truth-sweep-release-docs/125-CONTEXT.md
@.planning/phases/125-version-truth-sweep-release-docs/125-RESEARCH.md
@.planning/phases/125-version-truth-sweep-release-docs/125-PATTERNS.md
@scripts/check_brand_tokens.py
@scripts/check_logo_assets.py
@scripts/ci/lint.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/check_version_truth.py (fail-closed drift guard)</name>
  <files>scripts/check_version_truth.py</files>
  <read_first>
    - PATTERNS §"scripts/check_version_truth.py (NEW...)" (the copy-ready skeleton: imports, ROOT resolution, main()->int, iter_target_files, PATTERNS set, messaging convention)
    - RESEARCH §"Gap 2 — Drift-Guard Regex Design" (verified pattern table + the (?![.\d]) lookahead proof against the live ~> 0.1.3 line)
    - CONTEXT D-05 (ship as new scripts/check_version_truth.py), D-06 (scan criterion-1 surface only; anchored ~> 0.1; exclude .planning/, prompts/, rulestead/doc/, examples/, the open_feature third-party line)
    - `scripts/check_brand_tokens.py` (convention analog: main()->int, sys.exit(main()), "Usage (from repo root)" docstring, UPPERCASE OK / UPPERCASE DRIFT DETECTED messaging)
    - `scripts/check_logo_assets.py` (scan-shape analog: ROOT = Path(__file__).resolve().parents[1], collect-then-report)
  </read_first>
  <action>
    Create `scripts/check_version_truth.py` mirroring the `check_brand_tokens.py` / `check_logo_assets.py`
    convention exactly (so it is reviewable alongside the 8 siblings):
    - Module docstring including the literal line `Usage (from repo root):` and the success/failure contract.
    - `ROOT = Path(__file__).resolve().parents[1]` for CWD-independent repo-root resolution.
    - `main()` returns `int`; `if __name__ == "__main__": sys.exit(main())`.
    - Collect ALL hits (do not bail on first), then report: print a success token and return 0 when clean,
      or print a drift-detected header + an indented `path:line: <stripped line>` list and return 1 on any hit.
      Use a `VERSION TRUTH OK` / `VERSION TRUTH DRIFT DETECTED` style pair matching the sibling messaging.

    File include set (criterion-1 surface, D-06): the fixed list `README.md`, `rulestead/README.md`,
    `rulestead_admin/README.md`, `open_feature_rulestead/README.md`, `MAINTAINING.md`, `CONTRIBUTING.md`
    (D-02: zero hits today but guard must cover it for future drift), PLUS a recursive glob of `guides/`
    via `ROOT.joinpath("guides").rglob("*.md")` and `rglob("*.cheatmd")`. Do NOT scan `.planning/`,
    `prompts/`, `rulestead/doc/`, `examples/`.

    Pattern set (the load-bearing part, D-06): compile these six patterns — `0\.1\.x`, `0\.1\.7`,
    `future[^\n]*1\.0` (case-insensitive), `1\.0 API freeze`, `Two version lines`, and the anchored
    `~> 0\.1(?![.\d])`. The negative lookahead `(?![.\d])` is mandatory: it matches `~> 0.1"` / `~> 0.1,` /
    `~> 0.1` + space but does NOT match `~> 0.1.3` (the legitimate upstream pin at
    `open_feature_rulestead/README.md:28`). No per-file carve-out is needed — the lookahead handles it.

    Do NOT pattern on `v0.1.0` (RESEARCH Pitfall 3 — out of scope; pervades guides). Make the script
    executable (`chmod +x`) to match the sibling convention.

    Comment-text discipline: do NOT write the bare string `0.1.x` or `~> 0.1` as illustrative comment
    prose anywhere except inside a compiled regex literal — the script scans itself (it is under no
    excluded path, but it lives in `scripts/`, which is also not scanned, so this is belt-and-suspenders).
    Keep any explanatory comments referring to the patterns by concept, not by reproducing the literal token.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead &amp;&amp; python3 scripts/check_version_truth.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/check_version_truth.py` exits 0 on the post-Plan-01 clean tree (prints the success token).
    - `grep -c '(?!\[.\\d\])\|(?\!\[.\\d\])' scripts/check_version_truth.py` is non-zero OR `python3 -c "import re; assert not re.search(r'~> 0\.1(?![.\d])', '{:open_feature, \"~> 0.1.3\"}')"` exits 0 — proving the lookahead does NOT match `~> 0.1.3`.
    - `python3 -c "import re; assert re.search(r'~> 0\.1(?![.\d])', 'use ~> 0.1\"')"` exits 0 — proving it DOES match a bare `~> 0.1`.
    - `test -x scripts/check_version_truth.py` (executable bit set).
  </acceptance_criteria>
  <done>The guard exits 0 on the clean tree, exits 1 on a seeded `~> 0.1` reappearance, never flags `~> 0.1.3`, and matches the sibling `check_*.py` convention (docstring, `main()->int`, messaging).</done>
</task>

<task type="auto">
  <name>Task 2: Wire the guard into scripts/ci/lint.sh and adversarially verify</name>
  <files>scripts/ci/lint.sh</files>
  <read_first>
    - PATTERNS §"scripts/ci/lint.sh (MODIFY...)" (the comment-then-invoke insertion shape)
    - RESEARCH §"Gap 2 — lint.sh wiring" (insert in the L43-69 Python-guard block, after the check_logo_assets.py line at L62; cd "${RULESTEAD_REPO}" at L40 sets repo root; set -euo pipefail at L2 makes it fail-closed)
    - The live `scripts/ci/lint.sh` L40-69
  </read_first>
  <action>
    Insert ONE guard invocation into the existing Python-guard block of `scripts/ci/lint.sh`
    (the L43-69 block, after the `check_logo_assets.py` invocation at ~L62), copying the exact
    comment-then-invoke shape of the 8 sibling guards:
    a `#` comment line describing the guard, then `python3 "${RULESTEAD_REPO}/scripts/check_version_truth.py"`.
    The block already runs after `cd "${RULESTEAD_REPO}"` (L40), so the script's relative paths resolve;
    `set -euo pipefail` (L2) makes any non-zero exit fail the lane (fail-closed). No other lint.sh change.

    After wiring, perform the adversarial self-test (RESEARCH Wave 0 gap): temporarily seed a `~> 0.1`
    line into a scanned guide, confirm the guard exits 1; then seed a `~> 0.1.3` line, confirm the guard
    STAYS green (lookahead proof); then revert both seeds and confirm a clean `bash scripts/ci/lint.sh` exit 0.
    Leave the tree clean (no seeded lines) at task end.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead &amp;&amp; bash scripts/ci/lint.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'check_version_truth.py' scripts/ci/lint.sh` returns 1 (guard wired exactly once).
    - `bash scripts/ci/lint.sh` exits 0 on the clean tree.
    - Adversarial proof (run during task, reverted before commit): seeding `~> 0.1"` into a `guides/` file makes `python3 scripts/check_version_truth.py` exit 1; seeding `~> 0.1.3` does NOT (guard stays 0).
    - `git status --porcelain guides/` shows no seeded leftovers at task end.
  </acceptance_criteria>
  <done>The guard is wired into the lint.sh Python-guard block, `bash scripts/ci/lint.sh` exits 0 on the clean tree, the adversarial lookahead proof passed, and no seed lines remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI guard ↔ doc tree | The guard reads (never writes) the shipped doc surface; its only failure mode is a faulty regex, not an external input. |

## STRIDE Threat Register

This phase adds a read-only CI grep guard — no runtime attack surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-03 | Denial of Service | `check_version_truth.py` false-positive on `~> 0.1.3` | mitigate | The `(?![.\d])` negative lookahead excludes the third-party pin; acceptance criteria include an adversarial proof that `~> 0.1.3` does NOT trip the guard. A faulty regex would brick CI permanently — the self-test prevents shipping it. |
| T-125-SC | Tampering | npm/pip/cargo installs | accept | No package installs (guard uses only stdlib `re`/`sys`/`pathlib`); nothing to verify. |
</threat_model>

<verification>
- SC-3: `python3 scripts/check_version_truth.py` exits 0 on the clean tree; seeding `~> 0.1` makes it exit 1; `~> 0.1.3` does NOT trip it; `bash scripts/ci/lint.sh` exits 0.
- Guard wired exactly once: `grep -c 'check_version_truth.py' scripts/ci/lint.sh` == 1.
</verification>

<success_criteria>
- `scripts/check_version_truth.py` is a fail-closed guard matching the sibling `check_*.py` convention.
- It exits 0 on the clean tree, 1 on a `~> 0.1` reappearance, and never flags `~> 0.1.3`.
- It is wired into `scripts/ci/lint.sh` and `bash scripts/ci/lint.sh` exits 0.
</success_criteria>

<output>
Create `.planning/phases/125-version-truth-sweep-release-docs/125-02-SUMMARY.md` when done.
</output>
