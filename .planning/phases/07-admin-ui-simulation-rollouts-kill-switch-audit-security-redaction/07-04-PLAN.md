---
phase: 07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex
  - rulestead_admin/lib/rulestead_admin/components/rollout_components.ex
  - rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_test.exs
  - rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_accessibility_test.exs
autonomous: true
requirements:
  - ADMIN-05
  - ADMIN-09
must_haves:
  truths:
    - "Operators can edit a rollout on a dedicated page without blurring the Phase 6 rules workspace or bypassing draft/publish."
    - "The rollout UI widens exposure only, keeps variant composition stable, and shows the rollout rule’s place in first-match order."
    - "Risky jumps require an explicit confirm flow and bounded sample preview before publish."
    - "Suggested ladders are recommendations, not automation."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex"
      provides: "Rollout controls LiveView"
    - path: "rulestead_admin/lib/rulestead_admin/components/rollout_components.ex"
      provides: "Rollout ladder, sample-preview, and risky-jump confirmation components"
    - path: "rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_test.exs"
      provides: "Behavioral proof for rollout editing, preview, and publish controls"
    - path: "rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_accessibility_test.exs"
      provides: "Automated accessibility proof for the rollout page"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex"
      to: "rulestead/lib/rulestead.ex"
      via: "rollout page saves drafts and publishes through existing core verbs rather than hidden persistence"
      pattern: "save_draft_ruleset|publish_ruleset"
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex"
      to: "rulestead_admin/lib/rulestead_admin/components/rollout_components.ex"
      via: "ladder, preview, and warning flows stay in shared operator components"
      pattern: "ladder|preview|confirm"
---

<objective>
Ship the dedicated rollout controls page with explicit draft/publish semantics, bounded preview sampling, and risk-aware confirmation flows.

Purpose: Rollouts are the highest-risk Phase 7 editing workflow. The UI must make safe widening legible and explicit without mutating live state behind the operator’s back.
Output: Rollout LiveView, ladder/preview/confirm components, and behavior+a11y proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-CONTEXT.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-UI-SPEC.md
@prompts/rulestead-admin-ux-and-operator-ia.md
@rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex
@rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex
@rulestead/lib/rulestead/ruleset/rule.ex

<interfaces>
Phase 7 rollout decisions to implement:
- `D-09` and `D-15`: explicit draft/publish, no hidden persistence.
- `D-11`: widen exposure percentage only; do not change variant composition here.
- `D-12`: show where the rollout rule sits in ordered first-match evaluation.
- `D-13` and `D-14`: recommended ladder plus bounded preview sample.
- `D-16`: no scheduled rollout automation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the rollout editing page with rule-order context and bounded preview sampling</name>
  <read_first>@rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex @rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex @.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-CONTEXT.md</read_first>
  <files>rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex, rulestead_admin/lib/rulestead_admin/components/rollout_components.ex, rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_test.exs</files>
  <acceptance_criteria>The rollout page edits rollout percentage only, shows surrounding rule order, surfaces ladder recommendations, and renders a bounded sample preview that compares intended exposure to observed assignment distribution before publish.</acceptance_criteria>
  <behavior>
    - Test 1: the page loads the current rollout rule in environment context, keeps variant weights read-only/stable, and updates only percentage exposure on draft edits.
    - Test 2: the preview panel samples a bounded targeting-key set and reports observed distribution without publishing or persisting hidden changes.
    - Test 3: the UI makes the rollout rule’s ordered position obvious relative to surrounding rules so first-match semantics remain visible.
  </behavior>
  <action>Implement `ADMIN-05` on the dedicated rollout route while preserving the Phase 6 rules workspace boundary per `D-03`. Reuse the current ruleset loading/saving model from the rules workspace, but restrict this page to rollout-specific editing per `D-11`: percentage widening only, stable variant composition, explicit draft save, explicit publish. Build the preview as a bounded sample of targeting keys per `D-14`; it should be deterministic enough for operator comparison and cheap enough for a LiveView request, not an unbounded background job. Surface lifecycle/owner/current environment context near the header so `ADMIN-09` stays visible on this operational page.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/flag_live/rollouts_test.exs</automated>
  </verify>
  <done>The rollout page is explicit, safe, and legible: edit a draft, preview a bounded sample, and see exactly where the rollout sits in evaluation order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add risky-jump confirmation and accessibility proof for rollout controls</name>
  <read_first>@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-UI-SPEC.md @rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_test.exs</read_first>
  <files>rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_accessibility_test.exs</files>
  <acceptance_criteria>Skipping the recommended ladder or jumping more than one ladder step triggers the elevated confirmation flow, and the page passes automated accessibility checks.</acceptance_criteria>
  <behavior>
    - Test 1: a safe next-step publish does not require the elevated warning, but a risky jump does and blocks publish until confirmed with a reason.
    - Test 2: the rollout page passes the project’s automated accessibility scan with ladder controls, preview panel, and confirmation affordances present.
  </behavior>
  <action>Implement the risk model from `D-10` and the UI spec: ladder suggestions like `5 -> 25 -> 50 -> 100` are recommendations, not automation, but skipping the ladder or jumping more than one step must trigger a stronger confirmation. Keep the confirm flow route-local and do not turn it into a background scheduler or governance system. Add route-specific accessibility proof once the page is fully interactive.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/flag_live/rollouts_accessibility_test.exs</automated>
  </verify>
  <done>The rollout workflow remains explicit, accessible, and resistant to risky jumps without adding automation or hidden persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rollout form edits -> published runtime state | Exposure changes affect live traffic once published |
| preview sampling -> operator decision-making | A misleading preview could cause unsafe publish decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | T | rollout draft edits | mitigate | Save through existing draft/publish verbs only; never persist on every interaction. |
| T-07-12 | I | rollout preview | mitigate | Keep preview bounded and deterministic enough for comparison, and label it as a preview rather than observed production telemetry. |
| T-07-13 | E | production rollout publish | mitigate | Require the elevated confirmation flow for risky jumps and keep environment-sensitive warnings visible in the page frame. |
</threat_model>

<verification>
Run the rollout behavior and accessibility tests, then inspect that no scheduler/change-request automation or variant-weight editing slipped into this page.
</verification>

<success_criteria>
Operators can widen exposure safely on a dedicated rollout page, preview the effect before publish, and hit a stronger warning when they try to move too fast.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-04-SUMMARY.md`
</output>
