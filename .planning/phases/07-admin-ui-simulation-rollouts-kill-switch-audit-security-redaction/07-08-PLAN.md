---
phase: 07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction
plan: 08
type: execute
gap_closure: true
wave: 2
depends_on:
  - 07-07
files_modified:
  - .credo.exs
  - rulestead/mix.exs
  - rulestead/lib/rulestead/credo/no_raw_traits_in_telemetry_meta.ex
  - rulestead/lib/rulestead/credo/no_raw_traits_in_logger.ex
  - rulestead/lib/rulestead/credo/no_mutation_outside_multi.ex
  - rulestead/lib/rulestead/credo/no_socket_captured_in_async.ex
  - rulestead/lib/rulestead/credo/no_eval_outside_context.ex
autonomous: true
requirements:
  - SEC-03
  - SEC-04
  - TEL-03
must_haves:
  truths:
    - "The sibling-package command path `cd rulestead_admin && mix test ...` compiles and runs with the local Credo checks present."
    - "The five Phase 7 custom Credo checks still load under `mix credo --strict` and under the dedicated fixture-backed test harness."
    - "Dev-only static-analysis code does not leak into the runtime dependency surface for sibling-package builds."
  artifacts:
    - path: .credo.exs
      provides: Dev-only load wiring for the five Phase 7 custom Credo checks
    - path: rulestead/mix.exs
      provides: Compile-path-safe dependency/layout contract for sibling-package builds
    - path: rulestead/lib/rulestead/credo/no_raw_traits_in_telemetry_meta.ex
      provides: TEL-03 custom check preserved behind compile-safe loading
  key_links:
    - from: .credo.exs
      to: rulestead/lib/rulestead/credo/no_raw_traits_in_telemetry_meta.ex
      via: strict Credo loads the local Phase 7 checks only when Credo is available
      pattern: "requires|Code\\.ensure_loaded"
    - from: rulestead/mix.exs
      to: rulestead_admin/mix.exs
      via: sibling package compiles path dependency without runtime Credo requirement leakage
      pattern: "only:|runtime:"
---

<objective>
Close the packaging and compile-path gap introduced by the Phase 7 custom Credo checks so the sibling-package entrypoint works again without weakening the static-enforcement contract.

Purpose: Verification found that `cd rulestead_admin && mix test ...` is broken because Phase 7 moved Credo check modules into the runtime compile path. Fix this first so the remaining admin-package gap-closure plans can verify from the real sibling-package entrypoint.
Output: Compile-safe custom Credo check loading, preserved core-package lint/test proof, and restored `rulestead_admin` path-dependency compilation.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-CONTEXT.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-PATTERNS.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-VERIFICATION.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-REVIEW.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-06-SUMMARY.md

<interfaces>
From rulestead/mix.exs:
```elixir
defp deps do
  [
    {:credo, ...}
  ]
end
```

From .credo.exs:
```elixir
requires: [...]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the custom Credo checks off the sibling-package runtime failure path</name>
  <files>.credo.exs, rulestead/mix.exs, rulestead/lib/rulestead/credo/no_raw_traits_in_telemetry_meta.ex, rulestead/lib/rulestead/credo/no_raw_traits_in_logger.ex, rulestead/lib/rulestead/credo/no_mutation_outside_multi.ex, rulestead/lib/rulestead/credo/no_socket_captured_in_async.ex, rulestead/lib/rulestead/credo/no_eval_outside_context.ex</files>
  <behavior>
    - Test 1: `cd rulestead_admin && mix test test/rulestead_admin/live/audit_live/index_test.exs` compiles the path dependency without requiring runtime `Credo.Check`.
    - Test 2: `cd rulestead && mix test test/rulestead/credo_checks_test.exs` still loads and proves all five Phase 7 checks.
    - Test 3: strict Credo still reaches the local check registration path from `.credo.exs`, even if unrelated repo-wide findings remain non-zero.
  </behavior>
  <action>Restructure the Phase 7 custom Credo check loading so sibling-package dependency compilation no longer needs `Credo.Check` at runtime, addressing verification blocker 4 and review CR-04. The fix may be a guarded module definition or a dev/test-only compile/load arrangement, but it must preserve the real lint contract inside `rulestead/` and restore plain `mix test` compilation inside `rulestead_admin/`. Do not add a shipped runtime Credo dependency and do not weaken the checks into no-ops during core-package lint/test.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/audit_live/index_test.exs && cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/credo_checks_test.exs && (mix credo --strict test/support/credo_fixtures/raw_traits_in_telemetry.ex >/tmp/07-08-credo.out 2>&1 || true) && rg "raw traits|NoRawTraitsInTelemetryMeta" /tmp/07-08-credo.out</automated>
  </verify>
  <done>The admin package compiles cleanly through the sibling path dependency, and the core package still exercises the five Phase 7 custom Credo checks under dedicated test coverage while leaving repo-wide strict Credo status to the existing global lint debt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core package test/lint compile path -> sibling package dependency compile | Dev-only static-analysis code must not leak into the runtime dependency surface here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | D | sibling package compilation | mitigate | Keep custom Credo checks off the runtime compile path while preserving core-package lint/test coverage. |
| T-07-08-02 | T | static-analysis loading contract | mitigate | Load the custom checks only where Credo is available so strict lint remains real and runtime builds stay unaffected. |
</threat_model>

<verification>
Run the compile-safety gate first, then the core Credo proof:

```bash
cd rulestead_admin && mix test test/rulestead_admin/live/audit_live/index_test.exs
cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/credo_checks_test.exs
(mix credo --strict test/support/credo_fixtures/raw_traits_in_telemetry.ex >/tmp/07-08-credo.out 2>&1 || true) && rg "raw traits|NoRawTraitsInTelemetryMeta" /tmp/07-08-credo.out
```
</verification>

<success_criteria>
- `cd rulestead_admin && mix test ...` compiles and runs with the local `../rulestead` dependency.
- The five custom Phase 7 Credo checks still prove out under `test/rulestead/credo_checks_test.exs`.
- The compile-path fix does not weaken the local check registration contract described in `.credo.exs`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-08-SUMMARY.md`
</output>
