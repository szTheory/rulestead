---
phase: 07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction
plan: 07
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead.ex
  - rulestead/lib/rulestead/admin/authorizer.ex
  - rulestead/lib/rulestead/store/command.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/fake.ex
  - rulestead/test/rulestead/admin_security_contract_test.exs
  - rulestead/test/rulestead/admin_audit_kill_switch_test.exs
  - rulestead/test/rulestead/integration/admin_lifecycle_runtime_test.exs
autonomous: true
requirements:
  - ADMIN-05
  - ADMIN-06
  - ADMIN-07
  - SEC-01
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "Every public admin mutation write is authorized through the Phase 7 host-policy envelope before adapter writes run."
    - "Denied admin writes are audit-visible for rollout, rule-edit, archive, rollback, and kill-switch operations."
    - "Kill-switch engage and release publish fresh runtime snapshot state and change Runtime evaluation after refresh."
    - "Ruleset publish audit rows capture structured reorder diff metadata with exact before/after rule positions and actor linkage."
    - "Global audit queries apply actor, mutation, environment, and date filters before row limiting."
  artifacts:
    - path: rulestead/lib/rulestead.ex
      provides: Authorized public mutation facade with denied-audit handling
    - path: rulestead/lib/rulestead/admin/authorizer.ex
      provides: Host-policy-final authorization contract
    - path: rulestead/lib/rulestead/store/ecto.ex
      provides: Runtime snapshot propagation, ruleset diff metadata, and filtered audit queries
    - path: rulestead/lib/rulestead/fake.ex
      provides: Fake adapter parity for denied audit and diff/query behavior
    - path: rulestead/test/rulestead/integration/admin_lifecycle_runtime_test.exs
      provides: Runtime proof for kill-switch propagation
  key_links:
    - from: rulestead/lib/rulestead.ex
      to: rulestead/lib/rulestead/admin/authorizer.ex
      via: admin_write/2 authorization for save/publish/archive and denied-audit persistence
      pattern: "admin_write|Authorizer.authorize"
    - from: rulestead/lib/rulestead/store/ecto.ex
      to: rulestead/lib/rulestead/runtime
      via: runtime snapshot insert/publication on kill-switch engage/release
      pattern: "insert_runtime_snapshot|publish"
    - from: rulestead/lib/rulestead/store/ecto.ex
      to: rulestead_admin/lib/rulestead_admin/live/audit_live/index.ex
      via: pre-limit filtered list_audit_events query and diff metadata projection
      pattern: "list_audit_events|audit_metadata"
---

<objective>
Close the Phase 7 core contract gaps that make the shipped UI unsafe or misleading: host-policy bypass, missing denied-audit coverage, kill-switch/runtime divergence, hollow ruleset diff rows, and post-limit audit filtering.

Purpose: Finish the backend guarantees the verification report marked as blockers so the admin package can safely rely on the Phase 7 security and audit envelope.
Output: Authorized mutation facade updates, adapter parity for denied audit + ruleset diffs + kill-switch snapshot propagation, and regression/integration proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-CONTEXT.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-VERIFICATION.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-REVIEW.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-01-SUMMARY.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-05-SUMMARY.md

<interfaces>
From rulestead/lib/rulestead/admin/authorizer.ex:
```elixir
@spec authorize(term(), atom(), term(), String.t() | atom() | nil) ::
        :ok | {:error, Rulestead.Error.t(), audit_payload()}
```

From rulestead/lib/rulestead.ex:
```elixir
def save_draft_ruleset(%Command.SaveDraftRuleset{} = command)
def publish_ruleset(%Command.PublishRuleset{} = command)
def archive_flag(%Command.ArchiveFlag{} = command)
def engage_kill_switch(%Command.EngageKillSwitch{} = command)
def release_kill_switch(%Command.ReleaseKillSwitch{} = command)
def list_audit_events(%Command.ListAuditEvents{} = command)
```

From rulestead/lib/rulestead/store/ecto.ex:
```elixir
def save_draft_ruleset(%Command.SaveDraftRuleset{} = command)
def publish_ruleset(%Command.PublishRuleset{} = command)
def engage_kill_switch(%Command.EngageKillSwitch{} = command)
def release_kill_switch(%Command.ReleaseKillSwitch{} = command)
def list_audit_events(%Command.ListAuditEvents{} = command)
defp insert_runtime_snapshot(repo, environment, published_at)
defp audit_metadata(command, ruleset)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seal the host-policy authorization envelope for all public Phase 7 writes</name>
  <files>rulestead/lib/rulestead.ex, rulestead/lib/rulestead/admin/authorizer.ex, rulestead/test/rulestead/admin_security_contract_test.exs</files>
  <behavior>
    - Test 1: A configured host `Rulestead.Admin.Policy` denial blocks `save_draft_ruleset`, `publish_ruleset`, and `archive_flag` even when the actor carries fallback roles.
    - Test 2: The same denied writes return typed unauthorized errors and produce denied audit persistence hooks instead of direct store writes.
    - Test 3: When no host policy module is configured, fallback roles still preserve the existing package-default behavior.
  </behavior>
  <action>Refactor `Rulestead.Admin.Authorizer.allowed?/4` so host policy is final when configured, per D-30 and review CR-01. Route `save_draft_ruleset/1`, `publish_ruleset/1`, and `archive_flag/1` through the same `admin_write/2` path already used by kill-switch and rollback verbs, per D-28/D-30/D-32. Expand `maybe_persist_denied_mutation/3` so every public admin mutation denied by policy becomes audit-visible, not only kill-switch operations. Keep redaction-before-persist intact per D-33/D-34; do not add bundled-auth assumptions or bypass the host seam.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/admin_security_contract_test.exs -x</automated>
  </verify>
  <done>Host-policy denials are no longer bypassable, all public mutation verbs authorize through the same root envelope, and denied writes are observable in regression tests for draft/publish/archive alongside existing kill-switch coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish runtime and audit truth on kill-switch and ruleset writes</name>
  <files>rulestead/lib/rulestead/store/command.ex, rulestead/lib/rulestead/store/ecto.ex, rulestead/lib/rulestead/fake.ex, rulestead/test/rulestead/admin_audit_kill_switch_test.exs, rulestead/test/rulestead/integration/admin_lifecycle_runtime_test.exs</files>
  <behavior>
    - Test 1: Engaging a kill switch inserts/publishes fresh runtime snapshot state, and `Runtime.evaluate/3` returns the default value after refresh.
    - Test 2: Releasing the kill switch restores authored runtime behavior after refresh.
    - Test 3: Publishing a reordered ruleset records structured `before`, `after`, and `diff` metadata with exact rule positions and actor identity.
    - Test 4: Audit list queries honor actor/mutation/date filters before limit truncation.
  </behavior>
  <action>Extend the store command/query contract and both adapters so kill-switch engage/release rebuild runtime snapshots and drive the existing refresh publication path, per D-18/D-19/D-23 and the verification blocker. Enrich ruleset audit metadata for draft/publish writes with structured reorder diffs that retain exact positions and linked actor context, per D-26/D-27/D-29. Push actor, mutation, environment, and date filtering into `list_audit_events/1` before limit application so the global audit surface stops dropping older matches after the first 50 rows, addressing review WR-03. Maintain fake/Ecto parity; do not introduce a new history store or scheduled automation.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/admin_audit_kill_switch_test.exs test/rulestead/integration/admin_lifecycle_runtime_test.exs -x</automated>
  </verify>
  <done>Kill-switch writes change runtime snapshot state and prove refresh-driven evaluation changes, ruleset audit rows expose structured reorder diffs with actor linkage, and filtered audit queries behave correctly before row limiting in both adapters.</done>
</task>

<task type="auto">
  <name>Task 3: Regress the full Phase 7 backend closure set together</name>
  <files>rulestead/test/rulestead/admin_security_contract_test.exs, rulestead/test/rulestead/admin_audit_kill_switch_test.exs, rulestead/test/rulestead/integration/admin_lifecycle_runtime_test.exs</files>
  <action>Run the focused backend suite that covers the gap-closure contract end to end: policy-final authorization, denied audit visibility, kill-switch runtime propagation, ruleset diff metadata, and pre-limit audit filtering. If assertions overlap awkwardly, tighten the existing tests instead of adding new broad integration scaffolding. Keep the closure narrow to Phase 7 behaviors only.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/admin_security_contract_test.exs test/rulestead/admin_audit_kill_switch_test.exs test/rulestead/integration/admin_lifecycle_runtime_test.exs</automated>
  </verify>
  <done>The focused backend regression suite passes and directly proves every core/store blocker called out in 07-VERIFICATION.md and 07-REVIEW.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin LiveView -> public mutation facade | Untrusted operator actions cross into policy-gated writes here |
| public facade -> store adapter | Authorized commands and denied-audit payloads cross into persistence/runtime publication here |
| store adapter -> runtime snapshot refresh | Persisted authoring state becomes live evaluation state here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | E | `Rulestead.Admin.Authorizer` | mitigate | Make host policy final when configured; fallback roles apply only when no host policy exists. |
| T-07-07-02 | R | `Rulestead.admin_write/2` denied paths | mitigate | Persist denied audit rows for all public Phase 7 mutation verbs per D-28/D-32. |
| T-07-07-03 | T | kill-switch adapter writes | mitigate | Rebuild/publish runtime snapshots on engage/release so authored state and runtime state cannot diverge. |
| T-07-07-04 | I | audit query/filter path | mitigate | Apply filters server-side before limit and redact metadata before persistence/query projection. |
</threat_model>

<verification>
Run the focused backend regression suite from `rulestead/`:

```bash
cd rulestead && mix test test/rulestead/admin_security_contract_test.exs test/rulestead/admin_audit_kill_switch_test.exs test/rulestead/integration/admin_lifecycle_runtime_test.exs
```
</verification>

<success_criteria>
- Host-policy denials are final for configured policies and no longer bypassable by built-in fallback roles.
- `save_draft_ruleset`, `publish_ruleset`, and `archive_flag` use the same authorized Phase 7 mutation envelope as kill-switch and rollback writes.
- Denied writes are audit-visible across the full public Phase 7 mutation surface.
- Kill-switch engage/release publish runtime snapshot updates and runtime evaluation proof passes.
- Ruleset reorder audit rows expose exact position-aware diff metadata and actor linkage.
- Audit filtering happens before limit truncation and is covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-07-SUMMARY.md`
</output>
