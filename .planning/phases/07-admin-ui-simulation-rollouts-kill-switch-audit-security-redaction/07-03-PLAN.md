---
phase: 07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/flag_live/simulate.ex
  - rulestead_admin/lib/rulestead_admin/components/simulate_components.ex
  - rulestead_admin/test/rulestead_admin/live/flag_live/simulate_test.exs
  - rulestead_admin/test/rulestead_admin/live/flag_live/simulate_accessibility_test.exs
autonomous: true
requirements:
  - ADMIN-04
  - ADMIN-09
  - SEC-03
must_haves:
  truths:
    - "Operators can open a dedicated simulation screen for one flag and one environment, submit one actor/context payload, and get a summary-first explain result."
    - "Saved archetypes stay scoped to the simulation page and can be applied without becoming a broader profile-management system."
    - "The screen can export an Elixir-friendly test fixture literal instead of raw JSON or an opaque blob."
    - "Raw trait values remain redacted by default in visible/persisted metadata pathways."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/simulate.ex"
      provides: "Simulation/explain LiveView"
    - path: "rulestead_admin/lib/rulestead_admin/components/simulate_components.ex"
      provides: "Trace panel, archetype chips, and fixture export presentation components"
    - path: "rulestead_admin/test/rulestead_admin/live/flag_live/simulate_test.exs"
      provides: "Behavioral proof for simulation result rendering, archetypes, and fixture export"
    - path: "rulestead_admin/test/rulestead_admin/live/flag_live/simulate_accessibility_test.exs"
      provides: "Automated accessibility proof for the simulation screen"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/simulate.ex"
      to: "rulestead/lib/rulestead.ex"
      via: "screen calls the explicit Phase 7 admin simulation/explain facade instead of embedding a second evaluator or reaching into runtime internals"
      pattern: "simulate_flag|simulate_explain|admin_simulation"
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/simulate.ex"
      to: "rulestead_admin/lib/rulestead_admin/components/simulate_components.ex"
      via: "summary-first trace rendering and fixture export stay in shared UI primitives"
      pattern: "trace|fixture|archetype"
---

<objective>
Ship the dedicated simulation/explain screen with the exact Phase 7 operator contract: one context at a time, summary first, saved archetypes, and copy-as-test-fixture output.

Purpose: This is the main support/debug workflow for Sam and app engineers, and it must use the existing evaluator/explain substrate rather than inventing a UI-local decision engine.
Output: Simulation LiveView, trace/fixture components, behavior+a11y tests, and redaction-safe operator flows.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-CONTEXT.md
@.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-UI-SPEC.md
@prompts/rulestead-admin-ux-and-operator-ia.md
@prompts/rulestead-domain-language-field-guide.md
@rulestead_admin/lib/rulestead_admin/live/flag_live/simulate.ex
@rulestead_admin/lib/rulestead_admin/components/simulate_components.ex
@rulestead_admin/lib/rulestead_admin/live/session.ex

<interfaces>
Phase 7 simulation decisions to implement:
- `D-01` and `D-03`: dedicated `/simulate` route, not detail or rules.
- `D-02`, `D-04`, and `D-05`: one submit gives a summary-first result with progressive trace disclosure.
- `D-06` and `D-07`: saved archetypes and an Elixir-ready fixture export belong on this page.
- `D-08`: no persisted run history.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the simulation/explain LiveView with archetypes and fixture export</name>
  <read_first>@rulestead_admin/lib/rulestead_admin/live/flag_live/simulate.ex @.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-CONTEXT.md @.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-UI-SPEC.md</read_first>
  <files>rulestead_admin/lib/rulestead_admin/live/flag_live/simulate.ex, rulestead_admin/lib/rulestead_admin/components/simulate_components.ex, rulestead_admin/test/rulestead_admin/live/flag_live/simulate_test.exs</files>
  <acceptance_criteria>The simulation page accepts targeting key plus traits, renders the summary-first result fields from `D-04`, supports saved archetype apply/reset flow scoped to this screen, and exports a usable ExUnit-ready canonical context literal separate from any redacted UI metadata.</acceptance_criteria>
  <behavior>
    - Test 1: running a simulation shows matched rule or default path, value/variant, reason, bucket result, snapshot version, and cache age before any raw trace detail.
    - Test 2: per-rule pass/fail and bucket math details remain collapsed behind explicit disclosure controls.
    - Test 3: applying an archetype fills the form, and the copy-as-test-fixture action returns an Elixir map/struct literal suitable for direct ExUnit use.
  </behavior>
  <action>Implement `ADMIN-04` on the dedicated `/simulate` route per `D-01` and `D-03`. Keep the operator interaction simple: one actor/context payload at a time, one submit, summary-first results, expandable trace detail, and no simulation-history persistence. Archetypes may live in URL/session/component state so long as they remain page-scoped per `D-06`; do not evolve them into a broader profile system. The fixture export must be Elixir-friendly per `D-07`, using the project’s canonical `Rulestead.Context` shape and stable key ordering so support and app engineers can paste it into tests directly. Treat reproducibility and redaction as two separate outputs: export the canonical context literal unchanged for test-fixture use, and use the Phase 7 redaction helper from `07-01` only for result-adjacent metadata echoed in the UI or copied into non-fixture summary affordances.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/flag_live/simulate_test.exs</automated>
  </verify>
  <done>The simulation page is usable end-to-end for real operator debugging and produces immediately actionable test-fixture output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add accessibility and redaction proof for the simulation screen</name>
  <read_first>@rulestead_admin/test/rulestead_admin/live/flag_live/simulate_test.exs @prompts/rulestead-security-privacy-and-threat-model.md</read_first>
  <files>rulestead_admin/test/rulestead_admin/live/flag_live/simulate_accessibility_test.exs</files>
  <acceptance_criteria>The simulation page passes automated accessibility checks and the tests prove redacted-by-default behavior for non-allowlisted trait keys in visible metadata flows while leaving the canonical fixture export reproducible.</acceptance_criteria>
  <behavior>
    - Test 1: the simulation page passes the project’s automated accessibility scan with the summary grid, disclosures, and fixture export controls rendered.
    - Test 2: a trait like `email` or `ip` is not echoed back as raw visible metadata in result-summary or non-fixture copied metadata unless explicitly allowlisted, while the canonical fixture export remains the true context literal used for reproduction.
  </behavior>
  <action>Add focused a11y proof for the simulation route and bake the redaction posture into the screen-level tests. This plan is not where telemetry/logging enforcement is implemented; it is where the operator workflow proves it does not casually surface raw traits in the UI while still remaining useful. Keep the test coverage route-specific and do not fold in rollout, kill-switch, or audit assertions here.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/flag_live/simulate_accessibility_test.exs</automated>
  </verify>
  <done>The simulation workflow is accessible and visibly aligned with the project’s redact-by-default security posture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-entered simulation traits -> visible result UI | Potentially sensitive input is echoed back into the admin screen |
| simulation screen -> core explain facade | UI must not bypass redaction or invent a second evaluation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | I | simulation result rendering | mitigate | Render summary-first results and keep raw-ish trace data behind disclosures; route metadata through the redaction helper before display. |
| T-07-09 | T | fixture export | mitigate | Export the canonical Elixir context literal only, not a UI-local or partially redacted blob that cannot reproduce the decision. |
| T-07-10 | E | evaluation path | mitigate | Call the core simulation/explain facade from `07-01`; do not embed evaluator logic inside LiveView. |
</threat_model>

<verification>
Run the simulation behavior and accessibility tests and confirm the page has no persisted-history feature and no cross-flag playground behavior.
</verification>

<success_criteria>
Operators can explain one decision at a time on a dedicated page, save/apply archetypes locally to that workflow, and export a real ExUnit fixture without leaking raw traits.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-ui-simulation-rollouts-kill-switch-audit-security-redaction/07-03-SUMMARY.md`
</output>
