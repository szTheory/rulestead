---
phase: 36-archive-readiness-signals-cleanup-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/code_refs/scan_receipt.ex
  - rulestead/lib/rulestead/webhooks/code_refs_plug.ex
  - rulestead/lib/rulestead/admin/lifecycle.ex
  - rulestead/lib/rulestead/store/command.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/fake.ex
  - rulestead/priv/repo/migrations/20260523130000_create_rulestead_code_reference_scans.exs
  - rulestead/test/rulestead/webhooks/code_refs_plug_test.exs
  - rulestead/test/rulestead/admin_lifecycle_test.exs
  - rulestead/test/rulestead/store_ecto_admin_test.exs
  - rulestead/test/rulestead/store/fake_contract_test.exs
autonomous: true
requirements:
  - LIF-02
must_haves:
  truths:
    - "Operators can inspect lifecycle posture, freshness evidence, and archive-readiness guidance as separate fields instead of one overloaded stale state."
    - "Fresh no-code-reference evidence only appears when a bounded recent scan receipt exists; zero references from no scan or stale scans stay explicit uncertainty per D-17."
    - "Archive-readiness guidance explains itself with bounded reasons, blockers, unknowns, and next actions, while missing evidence lowers confidence instead of boosting readiness."
    - "Ecto and Fake list/detail payloads expose the same archive-readiness contract and support the same advisory filters."
  artifacts:
    - path: "rulestead/lib/rulestead/code_refs/scan_receipt.ex"
      provides: "Persisted code-reference scan receipt that records when the latest successful scan dataset arrived, including zero-reference scans, per D-17 and D-37"
      min_lines: 30
    - path: "rulestead/lib/rulestead/webhooks/code_refs_plug.ex"
      provides: "Ingress logic that writes a bounded scan receipt alongside code-reference replacement so scan freshness has a source of truth independent of reference rows"
      min_lines: 90
    - path: "rulestead/lib/rulestead/admin/lifecycle.ex"
      provides: "Pure archive-readiness classifier that separates authored lifecycle posture, freshness evidence, and advisory readiness per D-03 through D-24 and D-36 through D-39"
      min_lines: 150
    - path: "rulestead/lib/rulestead/store/command.ex"
      provides: "Shared list-flags command contract for readiness and evidence-quality advisory filters"
      min_lines: 30
    - path: "rulestead/lib/rulestead/store/ecto.ex"
      provides: "Decorated list/detail payloads and in-memory advisory filters backed by the shared lifecycle projector"
      min_lines: 160
    - path: "rulestead/lib/rulestead/fake.ex"
      provides: "Fake adapter parity for the same payload and filter contract"
      min_lines: 120
  key_links:
    - from: "rulestead/lib/rulestead/webhooks/code_refs_plug.ex"
      to: "rulestead/lib/rulestead/code_refs/scan_receipt.ex"
      via: "accepted webhook payloads replace reference rows and persist a last-successful-scan receipt even when `references` is empty"
      pattern: "scan_receipt|last_successful_scan|references"
    - from: "rulestead/lib/rulestead/store/ecto.ex"
      to: "rulestead/lib/rulestead/code_refs/scan_receipt.ex"
      via: "list/detail decoration reads the latest scan receipt so D-17 freshness semantics are based on persisted receipt time, not inferred from reference-row presence"
      pattern: "scan_receipt|last_scan|fresh_scan"
    - from: "rulestead/lib/rulestead/store/ecto.ex"
      to: "rulestead/lib/rulestead/admin/lifecycle.ex"
      via: "decorate_payload/4 and environment_cards/1 attach the shared lifecycle/archive-readiness payload"
      pattern: "Lifecycle.classify|archive_readiness|evidence_quality|code_refs_scan"
    - from: "rulestead/lib/rulestead/fake.ex"
      to: "rulestead/lib/rulestead/admin/lifecycle.ex"
      via: "fake adapter mirrors the same lifecycle/archive-readiness payload"
      pattern: "Lifecycle.classify|archive_readiness|evidence_quality|code_refs_scan"
    - from: "rulestead/lib/rulestead/store/command.ex"
      to: "rulestead/lib/rulestead/store/ecto.ex"
      via: "ListFlags carries readiness/evidence-quality filter inputs into store filtering"
      pattern: "readiness|evidence_quality"
---

<objective>
Build the shared archive-readiness projection and store/filter contract for Phase 36.

Purpose: LIF-02 needs one read-only, explainable classifier that combines authored lifecycle facts with bounded evidence signals without persisting computed readiness truth or widening into Phase 37 mutation work.
Output: a bounded scan-freshness source of truth plus a pure lifecycle/archive-readiness projector and Ecto/Fake payload/filter parity that later admin and CLI surfaces can render directly.
Execution note: this plan intentionally keeps the scan-receipt ingress seam, classifier work, and Ecto/Fake parity in one slice because D-17 cannot be implemented or verified unless the persisted scan-freshness source of truth and the shared advisory payload land together.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-archive-readiness-signals-cleanup-analysis/36-CONTEXT.md
@.planning/phases/36-archive-readiness-signals-cleanup-analysis/36-RESEARCH.md
@.planning/phases/36-archive-readiness-signals-cleanup-analysis/36-PATTERNS.md
@.planning/phases/36-archive-readiness-signals-cleanup-analysis/36-VALIDATION.md
@.planning/phases/35-lifecycle-contract-ownership-metadata/35-01-SUMMARY.md
@.planning/phases/35-lifecycle-contract-ownership-metadata/35-02-SUMMARY.md
@.planning/phases/15-lifecycle-hygiene-and-code-references/15-CONTEXT.md
@rulestead/lib/rulestead/code_refs/scan_receipt.ex
@rulestead/lib/rulestead/webhooks/code_refs_plug.ex
@rulestead/lib/rulestead/admin/lifecycle.ex
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/store/ecto.ex
@rulestead/lib/rulestead/fake.ex

<interfaces>
From `rulestead/lib/rulestead/webhooks/code_refs_plug.ex`:
```elixir
defp handle_payload(conn, %{"references" => references}) when is_list(references) do
  Ecto.Multi.new()
  |> Ecto.Multi.delete_all(:delete_old, CodeReference)
  |> Ecto.Multi.insert_all(:insert_new, CodeReference, valid_references)
  |> Repo.transact()
end
```

From `rulestead/lib/rulestead/admin/lifecycle.ex`:
```elixir
@spec classify(map() | struct(), map() | struct(), keyword()) :: %{
  state: state(),
  mode: :permanent | :expiring,
  ownership: map(),
  review_by: Date.t() | nil,
  suggestion: map(),
  last_evaluated_at: DateTime.t() | nil
}
```

From `rulestead/lib/rulestead/store/command.ex`:
```elixir
defmodule ListFlags do
  defstruct environment_key: nil,
            query: nil,
            owner: nil,
            tags: [],
            lifecycle: nil,
            stale: nil,
            flag_type: nil,
            include_archived?: false,
            limit: 50,
            after: nil,
            before: nil,
            offset: 0,
            sort: :flag_key,
            page: nil
end
```

From `rulestead/lib/rulestead/store/ecto.ex` and `rulestead/lib/rulestead/fake.ex`:
```elixir
payload
|> Map.put(:lifecycle, lifecycle(flag, flag_environment))

entries
|> maybe_filter_owner(command.owner)
|> maybe_filter_tags(command.tags)
|> maybe_filter_lifecycle(command.lifecycle)
|> maybe_filter_stale(command.stale)
|> maybe_filter_flag_type(command.flag_type)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist a bounded code-reference scan receipt that survives zero-reference scans</name>
  <read_first>
    - rulestead/lib/rulestead/webhooks/code_refs_plug.ex
    - rulestead/lib/rulestead/code_refs/code_reference.ex
    - .planning/phases/15-lifecycle-hygiene-and-code-references/15-CONTEXT.md
    - .planning/phases/36-archive-readiness-signals-cleanup-analysis/36-CONTEXT.md
    - rulestead/test/rulestead/webhooks/code_refs_plug_test.exs
  </read_first>
  <files>rulestead/lib/rulestead/code_refs/scan_receipt.ex, rulestead/lib/rulestead/webhooks/code_refs_plug.ex, rulestead/priv/repo/migrations/20260523130000_create_rulestead_code_reference_scans.exs, rulestead/test/rulestead/webhooks/code_refs_plug_test.exs</files>
  <behavior>
    - Test 1: an accepted code-reference webhook persists one last-successful-scan receipt even when `references` is an empty list, so “fresh no refs” is representable per D-17.
    - Test 2: malformed or unauthorized webhook payloads do not advance the stored scan receipt.
    - Test 3: replacing reference rows and writing the scan receipt happen in one bounded transaction without adding crawler/background subsystems or persisted readiness truth per D-06 and D-37.
  </behavior>
  <action>Create a lightweight persisted scan-receipt seam for code-reference ingestion so Phase 36 has a real source of truth for scan freshness. Add a migration and schema dedicated to the latest successful code-reference scan receipt, then update `Rulestead.Webhooks.CodeRefsPlug` so every accepted payload records that receipt in the same transaction that replaces `code_references`, including zero-reference uploads. Base freshness on the persisted receipt time rather than inferring it from reference-row timestamps or row presence, because empty scans must still prove “fresh no refs” while `no scan` and `stale scan` remain uncertainty per D-17. Do not add a crawler, background subsystem, host-side mutation flow, or persisted readiness score.</action>
  <acceptance_criteria>
    - `rg -n "scan_receipt|last_successful_scan|fresh scan" rulestead/lib/rulestead/webhooks/code_refs_plug.ex rulestead/lib/rulestead/code_refs/scan_receipt.ex rulestead/test/rulestead/webhooks/code_refs_plug_test.exs` finds the new bounded freshness contract.
    - `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/webhooks/code_refs_plug_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/webhooks/code_refs_plug_test.exs</automated>
  </verify>
  <done>The repo can now prove when the latest successful code-reference scan landed, even if that scan found zero references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor the lifecycle projector into a split readiness model</name>
  <read_first>
    - rulestead/lib/rulestead/admin/lifecycle.ex
    - rulestead/lib/rulestead/code_refs/scan_receipt.ex
    - rulestead/lib/rulestead/webhooks/code_refs_plug.ex
    - .planning/phases/36-archive-readiness-signals-cleanup-analysis/36-CONTEXT.md
    - .planning/phases/36-archive-readiness-signals-cleanup-analysis/36-RESEARCH.md
    - .planning/phases/15-lifecycle-hygiene-and-code-references/15-CONTEXT.md
    - rulestead/test/rulestead/admin_lifecycle_test.exs
  </read_first>
  <files>rulestead/lib/rulestead/admin/lifecycle.ex, rulestead/test/rulestead/admin_lifecycle_test.exs</files>
  <behavior>
    - Test 1: `classify/3` returns separate `lifecycle`, `freshness`, and `archive_readiness` branches instead of treating `lifecycle.state` as the single stale/readiness truth.
    - Test 2: permanent posture and protected types (`kill_switch`, `operational`, `permission`) resist `archive_candidate` unless explicit authored posture and strong evidence say otherwise per D-20 through D-23.
    - Test 3: fresh empty scans count as positive no-code-ref evidence only when the persisted scan receipt is recent; missing or stale scan receipts become `unknowns` / weaker `evidence_quality`, never positive evidence toward archive readiness per D-16 through D-19.
    - Test 4: conflicting or weak evidence withholds the primary recommendation and yields a review-oriented response per D-25 through D-27.
  </behavior>
  <action>Rewrite `Rulestead.Admin.Lifecycle.classify/3` so it keeps Phase 35 authored truth intact but adds one pure, bounded advisory payload per D-03 through D-24 and D-36 through D-39: separate authored lifecycle posture from freshness evidence, add `archive_readiness` with `readiness`, `evidence_quality`, `reasons`, `unknowns`, `blockers`, `recommended_next_action`, and bounded secondary actions, and keep any internal sort tuple hidden. Use rule-ladder plus evidence-matrix semantics per D-09 through D-15, model `stale` as one evidence signal per D-08, preserve `archived_at` as the only hard terminal fact per D-05, and interpret code-reference evidence from the new persisted scan-receipt seam: a recent successful empty scan is positive “no refs found” evidence, while missing or stale scan receipts remain uncertainty per D-17. Never persist or expose a numeric readiness score per D-06 and D-10.</action>
  <acceptance_criteria>
    - `rg -n "archive_readiness|evidence_quality|recommended_next_action|unknowns|blockers" rulestead/lib/rulestead/admin/lifecycle.ex` finds the new shared vocabulary.
    - `rg -n "code_refs_scan|scan_receipt|no_code_refs" rulestead/lib/rulestead/admin/lifecycle.ex rulestead/test/rulestead/admin_lifecycle_test.exs` shows explicit D-17 coverage instead of inferred freshness semantics.
    - `rg -n "keep_active|needs_review|archive_candidate" rulestead/test/rulestead/admin_lifecycle_test.exs` proves all bounded readiness categories are covered.
    - `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/admin_lifecycle_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/admin_lifecycle_test.exs</automated>
  </verify>
  <done>The lifecycle seam exposes one pure archive-readiness payload that is explainable, uncertainty-aware, and still advisory-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire advisory filters and payload parity through Ecto and Fake stores</name>
  <read_first>
    - rulestead/lib/rulestead/store/command.ex
    - rulestead/lib/rulestead/store/ecto.ex
    - rulestead/lib/rulestead/fake.ex
    - rulestead/lib/rulestead/admin/lifecycle.ex
    - rulestead/lib/rulestead/code_refs/scan_receipt.ex
    - rulestead/test/rulestead/store_ecto_admin_test.exs
    - rulestead/test/rulestead/store/fake_contract_test.exs
  </read_first>
  <files>rulestead/lib/rulestead/store/command.ex, rulestead/lib/rulestead/store/ecto.ex, rulestead/lib/rulestead/fake.ex, rulestead/test/rulestead/store_ecto_admin_test.exs, rulestead/test/rulestead/store/fake_contract_test.exs</files>
  <behavior>
    - Test 1: `list_flags` and `fetch_flag` expose the same `archive_readiness` payload in Ecto and Fake paths.
    - Test 2: list filters can independently narrow by authored lifecycle posture, freshness/stale evidence, readiness, and evidence quality without collapsing them into one state.
    - Test 3: sort/filter behavior continues to treat missing or stale scan-receipt evidence as uncertainty rather than matching archive-positive filters, while fresh empty scans can support D-17 “no refs found” evidence.
  </behavior>
  <action>Add advisory filter fields to `Rulestead.Store.Command.ListFlags` first, then update Ecto and Fake decoration/filtering so both stores consume the Phase 36 payload from `Rulestead.Admin.Lifecycle` and preserve parity per D-03, D-04, D-07, D-08, D-17, and D-31 through D-39. Extend the shared payload returned by list/detail/environment-card paths with the new split lifecycle/freshness/readiness shape, feed the persisted scan-receipt freshness signal into the classifier, update in-memory filter helpers for readiness and evidence quality, keep lifecycle and stale filters working as separate axes, and do not introduce database-persisted readiness columns, generated values, background recompute jobs, or LiveView-only duplicate heuristics.</action>
  <acceptance_criteria>
    - `rg -n "readiness|evidence_quality" rulestead/lib/rulestead/store/command.ex rulestead/lib/rulestead/store/ecto.ex rulestead/lib/rulestead/fake.ex` shows the shared advisory filter contract in all three files.
    - `rg -n "archive_readiness|scan_receipt|code_refs_scan" rulestead/lib/rulestead/store/ecto.ex rulestead/lib/rulestead/fake.ex` shows payload decoration and D-17 freshness sourcing in both adapters.
    - `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/store_ecto_admin_test.exs test/rulestead/store/fake_contract_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/store_ecto_admin_test.exs test/rulestead/store/fake_contract_test.exs</automated>
  </verify>
  <done>Ecto and Fake list/detail flows expose the same archive-readiness contract and the same advisory filter semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored authored data -> archive-readiness classifier | Durable flag metadata and evidence records are interpreted into operator guidance |
| list/detail store payloads -> admin and CLI consumers | Shared read-model data crosses into multiple presentation surfaces that must not invent their own heuristics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-00 | T | `rulestead/lib/rulestead/webhooks/code_refs_plug.ex` and `scan_receipt.ex` | mitigate | Persist scan freshness only on authorized, valid webhook replacement transactions so stale or malformed payloads cannot forge D-17 “fresh no refs” evidence. |
| T-36-01 | T | `rulestead/lib/rulestead/admin/lifecycle.ex` | mitigate | Keep the classifier pure and enum-bounded so weak/missing evidence cannot be coerced into archive-positive truth through ad hoc template logic. |
| T-36-02 | I | `rulestead/lib/rulestead/admin/lifecycle.ex` | mitigate | Return explicit `unknowns` and `blockers` per D-16 through D-24 so the system does not hide uncertainty behind false precision. |
| T-36-03 | E | `rulestead/lib/rulestead/store/ecto.ex` and `rulestead/lib/rulestead/fake.ex` | mitigate | Centralize readiness filtering in the shared store seams so downstream surfaces cannot bypass the advisory-only contract or diverge across adapters. |
</threat_model>

<verification>
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/webhooks/code_refs_plug_test.exs`
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/admin_lifecycle_test.exs`
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/store_ecto_admin_test.exs test/rulestead/store/fake_contract_test.exs`
</verification>

<success_criteria>
- A successful code-reference upload now leaves a bounded scan-freshness receipt even when the scan finds zero references.
- One shared lifecycle classifier now exposes authored posture, freshness evidence, and archive-readiness guidance as separate concepts.
- Ecto and Fake payloads reuse that classifier and preserve advisory filter parity.
- No persisted computed readiness truth, background recompute, CLI mutation, or Phase 37 action flow is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/36-archive-readiness-signals-cleanup-analysis/36-01-SUMMARY.md`
</output>
