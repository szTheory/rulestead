---
phase: 89-focus-interaction-state-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead_admin/priv/static/theme-harness.html
autonomous: true
requirements: [A11Y-02, A11Y-03]

must_haves:
  truths:
    - "Keyboard-tabbing in the harness reaches: a text input, a select, a [role='tab'] strip (3 tabs), a primary button, a secondary button, and a danger button — all in .rs-shell"
    - "Each interactive element type is on a visually distinct surface: plain page-bg, card surface (--rs-surface), and colored-fill (primary button uses --rs-primary as its own surface)"
    - "The harness continues to work as a file:// fixture — no server, no build step"
  artifacts:
    - path: "rulestead_admin/priv/static/theme-harness.html"
      provides: "Interactive focus targets for screenshot-verifiable ring audit"
      contains: "role=\"tab\""
    - path: "rulestead_admin/priv/static/theme-harness.html"
      contains: "rs-button--primary"
    - path: "rulestead_admin/priv/static/theme-harness.html"
      contains: "rs-button--danger"
  key_links:
    - from: "theme-harness.html"
      to: "rulestead_admin.css"
      via: "<link rel='stylesheet' href='./css/rulestead_admin.css'>"
      pattern: "already present; extends section within .rs-shell__body"
---

<objective>
Extend the static theme harness with a dedicated "Focus Ring Targets" section containing every interactive element type used in the admin, so Plan 02's unified ring is screenshot-verifiable on all three surface types (page-bg, card, colored-fill) in both themes.

Purpose: Plan 02 adds the ring; Plan 01 adds the targets. Both are wave-1 so they can be reviewed together. Without representative targets, there is no way to screenshot-verify the ring on a primary button (colored fill) or on a card surface.

Output: An extended theme-harness.html that exercises text input, select, [role="tab"] strip, and primary/secondary/danger buttons — all inside .rs-shell, all keyboard-reachable.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/rulestead/.planning/PROJECT.md
@/Users/jon/projects/rulestead/.planning/ROADMAP.md
@/Users/jon/projects/rulestead/.planning/STATE.md
@/Users/jon/projects/rulestead/.planning/phases/89-focus-interaction-state-unification/89-CONTEXT.md
@/Users/jon/projects/rulestead/.planning/phases/89-focus-interaction-state-unification/89-VALIDATION.md

<interfaces>
<!-- Current harness structure (from direct read). -->
<!-- File: rulestead_admin/priv/static/theme-harness.html -->
<!-- Body structure: single .rs-shell > .rs-shell__header + .rs-shell__layout -->
<!--   .rs-shell__layout > .rs-shell__rail (nav links) + .rs-shell__main -->
<!--     .rs-shell__main > .rs-shell__body (grid, gap: --rs-page-gap) -->
<!--       sections: Badges, Card surface, Flash variants, Disabled state, Surface swatches -->
<!-- New section slots in at the END of .rs-shell__body, before the closing </div></main> -->

<!-- Relevant CSS classes available: -->
<!--   .rs-button                 — secondary/default button -->
<!--   .rs-button.rs-button--primary  — colored fill (#2563eb light, #2563eb dark) -->
<!--   .rs-button.rs-button--danger   — error-soft bg, error border -->
<!--   .rs-card                   — card surface (--rs-surface) -->
<!--   .rs-flag-subnav__tab (or a div with role="tab" + tabindex) — tab strip -->

<!-- Theme toggle helpers already in harness: -->
<!--   window.setTheme('dark') / window.clearTheme() via console -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Focus Ring Targets" section to harness</name>
  <files>rulestead_admin/priv/static/theme-harness.html</files>
  <read_first>
    Read the full harness file to get current line count and locate the insertion point (closing </div> of .rs-shell__body at line ~111). Read .planning/phases/89-focus-interaction-state-unification/89-CONTEXT.md decisions block. Read lines 512–590 of rulestead_admin/priv/static/css/rulestead_admin.css for .rs-button variant classes; lines 1880–1910 for .rs-flag-subnav__tab shape.
  </read_first>
  <action>
    Insert a new section inside .rs-shell__body immediately before its closing tag. The section heading style must match the existing section headings exactly (font-size: var(--rs-text-sm), font-weight: var(--rs-weight-semibold), color: var(--rs-text-muted), text-transform: uppercase, letter-spacing: var(--rs-tracking-wide), margin: 0 0 0.5rem).

    The section contains four sub-groups, each clearly labeled and visually separated:

    GROUP 1 — "Text input + select (page-bg surface)":
      A label + text input (type="text", placeholder="Type here…") and a label + select with 3 option elements. These are bare inputs on the page background so the focus ring is verifiable against --rs-bg.

    GROUP 2 — "Buttons on page-bg surface":
      Three buttons in a flex row (gap: 0.5rem):
        - class="rs-button" — label "Secondary"
        - class="rs-button rs-button--primary" — label "Primary"
        - class="rs-button rs-button--danger" — label "Danger"
      The primary button is colored fill (--rs-primary background); this is the CRITICAL surface for the two-stop ring test (the inner-gap stop separates the ring from the blue fill).

    GROUP 3 — "Primary button on card surface":
      Wrap a class="rs-button rs-button--primary" (label "Primary on card") inside a .rs-card div with a note span "card surface (--rs-surface)". This makes it screenshot-verifiable that the ring reads on a card.

    GROUP 4 — "Tab strip [role=tab]":
      A div with role="tablist" aria-label="Demo tabs" containing three children each with role="tab" tabindex="0" (first) or tabindex="-1" (remaining, so only one is in natural tab order; a real tab-key implementation is not required — the point is a keyboard-focusable [role=tab] element). Style the strip with inline styles matching the .rs-flag-subnav feel: display flex, gap 0, border-bottom 1px solid var(--rs-border). Each tab: padding 0.4rem 0.9rem; border: none; background: transparent; color: var(--rs-text-muted); cursor: pointer; border-bottom: 2px solid transparent. The first tab gets color: var(--rs-text); border-bottom-color: var(--rs-primary) (active look). Use <button> elements (not <a>) with the role="tab" attribute so they are natively focusable without extra tabindex.

    Do NOT introduce any new CSS classes or JavaScript. Inline styles only (matching existing harness patterns). Do NOT alter the outside-shell probe div or the script block at the bottom.
  </action>
  <verify>
    Open the harness in a browser (file://) and press Tab repeatedly. Confirm focus lands on: text input, select, Secondary button, Primary button, Danger button, Primary-on-card button, and the active tab button. Confirm the harness still shows the outside-shell scope probe at the bottom. No regressions to existing sections.

    Automated grep gate (run from repo root):
    grep -c 'role="tab"' rulestead_admin/priv/static/theme-harness.html
    Expected: >= 3

    grep -c 'rs-button--primary' rulestead_admin/priv/static/theme-harness.html
    Expected: >= 2

    grep -c 'rs-button--danger' rulestead_admin/priv/static/theme-harness.html
    Expected: >= 1
  </verify>
  <done>
    theme-harness.html contains a "Focus Ring Targets" section with at least: one text input, one select, three [role="tab"] buttons, a secondary button, two primary buttons (page-bg + card surfaces), and a danger button. All are keyboard-reachable. No new CSS classes or JS added. File:// opens without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file:// harness | Static HTML fixture, no server, no user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01-SC | Tampering | theme-harness.html | accept | Static devtools-only fixture; no user data, no routes, no network surface introduced |
</threat_model>

<verification>
grep -c 'role="tab"' rulestead_admin/priv/static/theme-harness.html   # >= 3
grep -c 'rs-button--primary' rulestead_admin/priv/static/theme-harness.html   # >= 2
grep -c 'rs-button--danger' rulestead_admin/priv/static/theme-harness.html    # >= 1
grep -c 'type="text"' rulestead_admin/priv/static/theme-harness.html          # >= 1
grep -c '<select' rulestead_admin/priv/static/theme-harness.html               # >= 1
grep -c 'outside-shell' rulestead_admin/priv/static/theme-harness.html        # still 1 (probe intact)
</verification>

<success_criteria>
theme-harness.html opens via file:// and presents keyboard-reachable interactive elements covering all three surface types required for focus ring verification: page-bg (inputs + buttons), card (primary button inside .rs-card), and colored-fill (primary buttons). Tab strip with [role=tab] present for the [role="tab"] `:where()` member verification in Plan 02. Existing harness sections and scope probe intact.
</success_criteria>

<output>
Create .planning/phases/89-focus-interaction-state-unification/89-01-SUMMARY.md when done.
</output>
