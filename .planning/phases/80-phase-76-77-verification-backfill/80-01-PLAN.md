---
phase: 80-phase-76-77-verification-backfill
plan: 80-01
type: docs
wave: 1
depends_on:
  - 79-01
files_modified:
  - .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md
  - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md
  - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md
autonomous: true
requirements:
  - INT-01
  - INT-03
  - DOC-01
  - DOC-03
must_haves:
  truths:
    - Phase 76 INT-01–INT-03 proof is recorded in 76-VERIFICATION.md with grep + contract-test commands
    - Phase 77 DOC-01–DOC-03 proof is recorded in 77-VERIFICATION.md; DOC-02 anchor cites Phase 79 (no re-fix)
    - 77-VALIDATION.md per-task rows show ✅ done and frontmatter status complete
    - mix verify.phase76 exits 0 and is documented as merge gate in both VERIFICATION files
  artifacts:
    - .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md
    - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md
    - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md
---

# Plan 80-01: Phase 76–77 Verification Backfill

<objective>
Close the v1.11 audit unverified-phase blocker for Phases 76–77 by backfilling formal `VERIFICATION.md` artifacts and refreshing stale `77-VALIDATION.md` task status. Docs-only — no edits to `guides/`, `rulestead/lib/`, or test files. Substance already shipped; this phase adds traceability only.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-80-01: Scope creep into guide or contract-test edits | high | D-01: touch only `.planning/phases/76-*` and `77-*` artifacts |
| T-80-02: DOC-02 anchor re-opened in Phase 80 | medium | Cross-reference `79-VERIFICATION.md`; grep anchor only, do not edit getting-started |
| T-80-03: 77-VALIDATION claims done but greps fail | medium | Run each per-task automated command before marking ✅ done |
| T-80-04: phase76 regression undetected | medium | Plan verification runs full `mix verify.phase76` |
</threat_model>

<tasks>

<task id="80-01-01" type="execute">
  <name>Create 76-VERIFICATION.md (INT-01–INT-03)</name>
  <read_first>
    - .planning/phases/80-phase-76-77-verification-backfill/80-CONTEXT.md (D-03–D-05)
    - .planning/phases/76-phoenix-integration-spine-doc/76-01-SUMMARY.md
    - .planning/phases/76-phoenix-integration-spine-doc/76-01-PLAN.md (verification block)
    - .planning/phases/78-doc-contract-guards-and-milestone-closure/78-VERIFICATION.md (template)
    - .planning/phases/79-lifecycle-deep-link-anchor-fix/79-VERIFICATION.md (template)
    - rulestead/test/rulestead/intro_integration_spine_contract_test.exs
  </read_first>
  <action>
Create `.planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md` with this structure:

**Frontmatter:**
```yaml
---
phase: 76-phoenix-integration-spine-doc
verified: YYYY-MM-DD
status: passed
---
```

**Title:** `# Phase 76 Verification`

**Proof checklist** table (columns: Check | Command | Result) — run each command; set Result to `PASS`:

| Check | Command |
|-------|---------|
| Spine file exists | `test -f guides/introduction/phoenix-integration-spine.md` |
| Spine first-hour path content | `grep -q 'Rulestead.Runtime' guides/introduction/phoenix-integration-spine.md && grep -q 'Rulestead.Plug' guides/introduction/phoenix-integration-spine.md && grep -q owner_ref guides/introduction/phoenix-integration-spine.md && grep -q expected_expiration guides/introduction/phoenix-integration-spine.md` |
| Hub cross-links (INT-03) | `grep -q phoenix-integration-spine guides/introduction/getting-started.md guides/introduction/installation.md README.md` |
| Intro contract test | `cd rulestead && mix test test/rulestead/intro_integration_spine_contract_test.exs` |
| Phase76 merge gate | `cd rulestead && mix verify.phase76` |

**Requirements** section — map each requirement to proof sources:

- **INT-01:** Spine documents supervision → config → Plug → Runtime eval path (`Rulestead.Runtime`, `Rulestead.Plug` in spine grep + contract test `"phoenix integration spine documents first-hour Phoenix path"`)
- **INT-02:** Spine §6 lifecycle-required `owner_ref` + `expected_expiration` (spine grep + contract test hub lifecycle assertions); **deep-link anchor regression owned by Phase 79** — cite `.planning/phases/79-lifecycle-deep-link-anchor-fix/79-VERIFICATION.md` for `#6-create-your-first-flag-lifecycle-required`
- **INT-03:** README, getting-started, installation cross-link spine (hub grep + contract tests `"intro hubs link spine and lifecycle-required fields"` and `"root readme routes Phoenix integrators to the spine"`)

Set `verified:` date to execution date. Do not edit any file outside `76-VERIFICATION.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md && grep -q 'INT-01' .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md && grep -q 'INT-02' .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md && grep -q 'INT-03' .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md && grep -q 'mix verify.phase76' .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md`
    - Frontmatter contains `phase: 76-phoenix-integration-spine-doc` and `status: passed`
    - Proof checklist includes all five rows from action (spine exists, spine content, hub cross-links, intro contract test, phase76 merge gate)
    - Requirements section lists **INT-01**, **INT-02**, and **INT-03** with proof pointers
    - INT-02 row references Phase 79 for anchor regression (does not claim Phase 80 fixed anchor)
    - `grep -q 'Proof checklist' .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md` succeeds
  </acceptance_criteria>
</task>

<task id="80-01-02" type="execute">
  <name>Create 77-VERIFICATION.md (DOC-01–DOC-03)</name>
  <read_first>
    - .planning/phases/80-phase-76-77-verification-backfill/80-CONTEXT.md (D-06–D-08)
    - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-01-SUMMARY.md
    - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-01-PLAN.md (verification block + per-task verify commands)
    - .planning/phases/79-lifecycle-deep-link-anchor-fix/79-VERIFICATION.md (DOC-02 anchor proof — cite, do not duplicate fix)
    - .planning/phases/78-doc-contract-guards-and-milestone-closure/78-VERIFICATION.md (template)
  </read_first>
  <action>
Create `.planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md` with this structure:

**Frontmatter:**
```yaml
---
phase: 77-evaluation-and-lifecycle-doc-alignment
verified: YYYY-MM-DD
status: passed
---
```

**Title:** `# Phase 77 Verification`

**Proof checklist** table (columns: Check | Command | Result) — run each command; set Result to `PASS`:

| Check | Command |
|-------|---------|
| evaluation.md Runtime APIs (DOC-01) | `grep -q 'Rulestead.Runtime.enabled?' guides/flows/evaluation.md && grep -q 'Rulestead.Runtime.evaluate/3' guides/flows/evaluation.md && grep -q 'Rulestead.evaluate/3' guides/flows/evaluation.md` |
| Intro lifecycle callouts (DOC-02) | `grep -q owner_ref guides/introduction/getting-started.md && grep -q expected_expiration guides/introduction/getting-started.md guides/introduction/installation.md && grep -q flag-lifecycle guides/introduction/getting-started.md guides/introduction/installation.md` |
| DOC-02 anchor (Phase 79) | `grep -q '#6-create-your-first-flag-lifecycle-required' guides/introduction/getting-started.md` |
| README Runtime ordering (DOC-03) | `grep -q 'Rulestead.Runtime.enabled?/3' rulestead/README.md && grep -q 'Payload-first' rulestead/README.md && grep -q 'Rulestead.evaluate/3' rulestead/README.md` |
| Intro contract test | `cd rulestead && mix test test/rulestead/intro_integration_spine_contract_test.exs` |
| Phase76 merge gate | `cd rulestead && mix verify.phase76` |

**Requirements** section:

- **DOC-01:** `evaluation.md` names `Rulestead.Runtime` keyed lookup with examples; payload-first `Rulestead.evaluate/3` remains in Core Calls (grep proof above). **Note:** no automated contract guard for evaluation.md Runtime strings yet — deferred to Phase 81 (grep proof only in Phase 80).
- **DOC-02:** getting-started and installation include lifecycle-required-fields callout (`owner_ref`, `expected_expiration`, `flag-lifecycle` grep). **Anchor slug regression:** verified in Phase 79 — see `.planning/phases/79-lifecycle-deep-link-anchor-fix/79-VERIFICATION.md` (do not re-fix anchor here).
- **DOC-03:** `rulestead/README.md` lists `Rulestead.Runtime.enabled?/3` before payload-first `Rulestead.evaluate/3` (grep ordering proof above).

Set `verified:` date to execution date. Do not edit guides, README, or test files.
  </action>
  <verify>
    <automated>test -f .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md && grep -q 'DOC-01' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md && grep -q 'DOC-02' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md && grep -q 'DOC-03' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md && grep -q 'Phase 81' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md && grep -q '79-VERIFICATION' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md`
    - Frontmatter contains `phase: 77-evaluation-and-lifecycle-doc-alignment` and `status: passed`
    - Proof checklist includes all six rows from action (DOC-01 grep, DOC-02 callout grep, DOC-02 anchor grep, DOC-03 README grep, intro contract test, phase76 merge gate)
    - DOC-01 requirement row explicitly notes contract guard deferred to Phase 81
    - DOC-02 requirement row cites Phase 79 VERIFICATION for anchor (contains `79-VERIFICATION` or `79-lifecycle-deep-link-anchor-fix`)
    - Requirements section lists **DOC-01**, **DOC-02**, and **DOC-03**
  </acceptance_criteria>
</task>

<task id="80-01-03" type="execute">
  <name>Refresh 77-VALIDATION.md task status</name>
  <read_first>
    - .planning/phases/80-phase-76-77-verification-backfill/80-CONTEXT.md (D-09–D-11)
    - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md (current stale rows)
    - .planning/phases/79-lifecycle-deep-link-anchor-fix/79-VALIDATION.md (sign-off template)
    - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-01-PLAN.md (per-task automated commands)
  </read_first>
  <action>
Edit `.planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md` only:

1. **Frontmatter:** change `status: draft` → `status: complete`. Preserve `nyquist_compliant: true` and all other fields.

2. **Test Infrastructure table:** update **Full suite command** from `See plan 77-01 verify block` to `cd rulestead && mix verify.phase76`.

3. **Per-Task Verification Map:** for rows `77-01-01`, `77-01-02`, `77-01-03`:
   - Change Status column from `⬜ pending` → `✅ done`
   - Add **File Exists** column (match Phase 79 table shape) with `✅` for each row if not present
   - Keep existing **Automated Command** strings unchanged:
     - 77-01-01: `grep -q 'Rulestead.Runtime.enabled?' guides/flows/evaluation.md`
     - 77-01-02: `grep -q expected_expiration guides/introduction/getting-started.md guides/introduction/installation.md`
     - 77-01-03: `grep -q 'Rulestead.Runtime' rulestead/README.md`

4. Run each automated command; confirm all exit 0 before marking done.

5. **Append Validation Sign-Off** section (after Manual-Only Verifications), matching Phase 79 pattern:
```markdown
## Validation Sign-Off

- [x] All tasks have automated verify
- [x] Sampling continuity: every task has automated verify
- [x] Wave 0: existing infrastructure covers requirements
- [x] No watch-mode flags
- [x] Feedback latency < 300s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** verified YYYY-MM-DD (phase 80 backfill — tasks shipped in phase 77)
```

Do not change automated grep command strings or add new test files.
  </action>
  <verify>
    <automated>grep -q 'status: complete' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md && ! grep -q '⬜ pending' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md && grep -c '✅ done' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md | grep -q '^3$' && grep -q 'Validation Sign-Off' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md && grep -q 'mix verify.phase76' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `77-VALIDATION.md` frontmatter `status: complete` (not `draft`)
    - No row contains `⬜ pending`
    - Exactly three rows show `✅ done` in Per-Task Verification Map (77-01-01, 77-01-02, 77-01-03)
    - `Validation Sign-Off` section present with all six checkboxes marked `[x]`
    - Full suite command references `cd rulestead && mix verify.phase76`
    - Per-task automated commands unchanged from original 77-VALIDATION.md
    - `grep -q 'Rulestead.Runtime.enabled?' guides/flows/evaluation.md` exits 0
    - `grep -q expected_expiration guides/introduction/getting-started.md guides/introduction/installation.md` exits 0
    - `grep -q 'Rulestead.Runtime' rulestead/README.md` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Artifact presence
test -f .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md
test -f .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md

# Requirement traceability
grep -q 'INT-01' .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md
grep -q 'INT-02' .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md
grep -q 'INT-03' .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md
grep -q 'DOC-01' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md
grep -q 'DOC-02' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md
grep -q 'DOC-03' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VERIFICATION.md

# 77-VALIDATION refresh
grep -q 'status: complete' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md
! grep -q '⬜ pending' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md
grep -q 'Validation Sign-Off' .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md

# Shipped content still green (unchanged by this phase)
test -f guides/introduction/phoenix-integration-spine.md
grep -q 'Rulestead.Runtime.enabled?' guides/flows/evaluation.md
grep -q '#6-create-your-first-flag-lifecycle-required' guides/introduction/getting-started.md

# Merge gate
cd rulestead && mix verify.phase76
```
</verification>

<success_criteria>
1. ROADMAP success criterion 1: `76-VERIFICATION.md` records INT-01–INT-03 proof checklist with commands.
2. ROADMAP success criterion 2: `77-VERIFICATION.md` records DOC-01–DOC-03 proof checklist with commands.
3. ROADMAP success criterion 3: `77-VALIDATION.md` task rows reflect shipped work (✅ done, not ⬜ pending).
4. ROADMAP success criterion 4: `mix verify.phase76` green with proof documented in both VERIFICATION files.
5. No edits outside `.planning/phases/76-phoenix-integration-spine-doc/` and `.planning/phases/77-evaluation-and-lifecycle-doc-alignment/`.
</success_criteria>

## PLANNING COMPLETE
