---
phase: 26-api-lockdown-and-documentation-perfection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/recipes/migrating-from-funwithflags.md
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "A developer migrating from FunWithFlags can follow a step-by-step guide to transition to Rulestead."
  artifacts:
    - path: "guides/recipes/migrating-from-funwithflags.md"
      provides: "Migration guide from FunWithFlags"
  key_links: []
---

<objective>
Provide a comprehensive migration guide for users transitioning from FunWithFlags to Rulestead.

Purpose: FunWithFlags is a popular prior-art library. A clear migration path reduces friction for adoption and clarifies conceptual shifts (like "Gates" becoming "Rules").
Output: A new recipe guide mapping FunWithFlags concepts and operations to Rulestead.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FunWithFlags Migration Guide</name>
  <files>guides/recipes/migrating-from-funwithflags.md</files>
  <action>
    Create a new markdown file `guides/recipes/migrating-from-funwithflags.md`.
    Include an introductory section explaining the conceptual shift: "Gates" are now "Rules", and "Priority" is "Ordered Evaluation".
    Provide a table or bulleted list mapping concepts:
    - `FunWithFlags.enabled?(:flag)` -> `Rulestead.enabled?(flag_payload, context)` (Rulestead requires explicit context).
    - `FunWithFlags.enable(:flag)` -> `Rulestead.publish_ruleset(...)` (Mutations use ChangeRequests/Publish commands for auditability).
    - Boolean Gate -> Rule without conditions.
    - Actor Gate -> Condition checking an ID (e.g. `{"user_id", :in, ["123"]}`).
    - Group Gate -> Condition checking a role explicitly passed in context.
    - Percentage Gate -> Rollout Rule (Deterministic bucketing based on an attribute).
    Follow the styling and tone of `guides/introduction/upgrading.md` (use appropriate Markdown headings and code blocks).
  </action>
  <verify>
    <automated>test -f guides/recipes/migrating-from-funwithflags.md && grep "FunWithFlags" guides/recipes/migrating-from-funwithflags.md > /dev/null</automated>
  </verify>
  <done>The migration guide exists and clearly maps FunWithFlags concepts to Rulestead concepts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | Static files; no runtime impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02   | Information Disclosure | Documentation | accept | Documentation is public by design; no sensitive information is included. |
</threat_model>

<verification>
- `guides/recipes/migrating-from-funwithflags.md` exists and contains required mappings.
</verification>

<success_criteria>
The markdown file is properly formatted and accurately reflects the transition of API and concepts from FunWithFlags.
</success_criteria>

<output>
After completion, create `.planning/phases/26-api-lockdown-and-documentation-perfection/26-02-SUMMARY.md`
</output>
