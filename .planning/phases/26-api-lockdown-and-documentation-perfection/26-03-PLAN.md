---
phase: 26-api-lockdown-and-documentation-perfection
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - rulestead/mix.exs
  - rulestead/lib/**/*.ex
autonomous: true
requirements:
  - API-01
  - DOC-01

must_haves:
  truths:
    - "A developer can view clear public Hexdocs without internal implementation details cluttering the index."
    - "The public API boundary is explicitly enforced via HexDocs groups."
  artifacts:
    - path: "rulestead/mix.exs"
      provides: "Hexdocs grouping configuration"
  key_links:
    - from: "rulestead/mix.exs"
      to: "guides/recipes/migrating-from-funwithflags.md"
      via: "groups_for_extras and extras list"
---

<objective>
Establish the strict public API boundary in Hexdocs and code by hiding internal modules and configuring ExDoc groups.

Purpose: As a stable v1.0.0 release, Rulestead must not expose its internal implementation details (e.g., Manifests, Engine internals, Store Commands) to end-users. 
Output: All internal modules marked with `@moduledoc false` and `mix.exs` configured with explicit groups for the public API and guides. Public modules explicitly stripped of `@moduledoc false`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-api-lockdown-and-documentation-perfection/26-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide internal modules and expose public API with @moduledoc</name>
  <files>rulestead/lib/**/*.ex</files>
  <action>
    Scan through all Elixir files in `rulestead/lib/`. 
    Identify internal modules vs public API modules. 
    The Public API consists ONLY of: `Rulestead`, `Rulestead.Ruleset`, `Rulestead.Rule`, `Rulestead.Flag`, `Rulestead.Result`, `Rulestead.Error`, `Rulestead.Store`, `Rulestead.Store.Ecto`, `Rulestead.Store.Redis`, `Rulestead.Runtime.Snapshot`, and `Rulestead.Tenancy`.
    
    For any module IN this Public API list, explicitly REMOVE `@moduledoc false` if it exists (e.g., it may incorrectly exist on `Rulestead.Flag`).
    For any module OUTSIDE this list (e.g., `Rulestead.Manifest.*`, `Rulestead.Store.Command.*`, `Rulestead.Engine.*`, internal utils), ensure they have `@moduledoc false` declared immediately after the `defmodule` line.
    Reference the Internal Modules Boundary analog from `26-PATTERNS.md` for proper placement.
    *Hint*: You can use shell scripting (`find`, `grep`, `sed`) or targeted edits to accomplish this en masse.
  </action>
  <verify>
    <automated>grep -r "defmodule Rulestead.Manifest" rulestead/lib/rulestead/manifest/ | grep -v false || true</automated>
  </verify>
  <done>No internal modules leak their documentation into Hexdocs and public APIs correctly omit @moduledoc false</done>
</task>

<task type="auto">
  <name>Task 2: Configure Hexdocs Groups in mix.exs</name>
  <files>rulestead/mix.exs</files>
  <action>
    Update the `docs/0` private function in `rulestead/mix.exs`.
    Add the new `guides/recipes/migrating-from-funwithflags.md` to the `extras` list.
    Add `groups_for_modules` to categorize the public API, Store Adapters, and Extensibility as per Phase 26 RESEARCH.md:
    - "Public API": `[Rulestead, Rulestead.Ruleset, Rulestead.Rule, Rulestead.Flag, Rulestead.Result, Rulestead.Error]`
    - "Store Adapters": `[Rulestead.Store.Ecto, Rulestead.Store.Redis]`
    - "Extensibility": `[Rulestead.Store, Rulestead.Runtime.Snapshot, Rulestead.Tenancy]`
    Add `groups_for_extras` to map the documentation folders:
    - "Introduction": `~r"guides/introduction/"`
    - "Flows": `~r"guides/flows/"`
    - "Recipes": `~r"guides/recipes/"`
  </action>
  <verify>
    <automated>cd rulestead && mix docs</automated>
  </verify>
  <done>Mix docs runs successfully and correctly categorizes modules and extras</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal -> Hexdocs | Internal code structure exported to static documentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03   | Information Disclosure | Internal Modules | mitigate | Use `@moduledoc false` to prevent internal module details from appearing in public API documentation. |
</threat_model>

<verification>
- All internal modules have `@moduledoc false`.
- All public modules do not have `@moduledoc false`.
- `mix docs` groups the Public API, Store Adapters, and Extensibility correctly.
</verification>

<success_criteria>
`mix docs` succeeds and a developer can verify that the generated Hexdocs HTML explicitly omits internal implementation details while displaying all public API endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/26-api-lockdown-and-documentation-perfection/26-03-SUMMARY.md`
</output>
