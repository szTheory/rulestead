---
phase: 26-api-lockdown-and-documentation-perfection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/**/*.ex
  - rulestead/mix.exs
  - rulestead/.dialyzer_ignore.exs
  - rulestead_admin/mix.exs
  - rulestead_admin/.dialyzer_ignore.exs
autonomous: true
requirements:
  - API-02

must_haves:
  truths:
    - "Dialyzer runs cleanly on the rulestead project with no warnings, proving type stability."
    - "External or unfixable dialyzer warnings are explicitly ignored."
    - "All public API modules and internal modules have comprehensive @spec annotations."
  artifacts:
    - path: "rulestead/.dialyzer_ignore.exs"
      provides: "Dialyzer warning suppression"
    - path: "rulestead_admin/.dialyzer_ignore.exs"
      provides: "Dialyzer warning suppression for admin app"
  key_links:
    - from: "rulestead/mix.exs"
      to: "rulestead/.dialyzer_ignore.exs"
      via: "dialyzer configuration"
---

<objective>
Fix Dialyzer warnings and ensure complete @spec annotations across the codebase for 100% type stability.

Purpose: Elixir projects transitioning to v1.0.0 require comprehensive typing and clean Dialyzer runs. This builds trust with consumers of the API that their host apps will not break due to internal type mismatch.
Output: Comprehensive typespecs in `rulestead/lib`, ignored external warnings via `.dialyzer_ignore.exs`, and clean Dialyzer output.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-api-lockdown-and-documentation-perfection/26-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and apply @spec annotations across all modules</name>
  <files>rulestead/lib/**/*.ex</files>
  <action>
    Conduct a full audit of all modules in `rulestead/lib/`.
    Ensure all public structs clearly document their `@type t()`.
    Ensure all public functions have a `@doc` string followed immediately by an `@spec` defining argument types and return shapes.
    Ensure all exported functions in internal modules (even with `@moduledoc false`) have comprehensive `@spec`s.
    Reference the Public Function, Type Definition, and Internal Module Function Typespecs patterns from `26-PATTERNS.md`.
    Fix existing missing specs and `when _ :: map() === nil` in `Rulestead.ex`.
  </action>
  <verify>
    <automated>cd rulestead && mix compile</automated>
  </verify>
  <done>All exported functions and structs have @spec and @type annotations</done>
</task>

<task type="auto">
  <name>Task 2: Resolve core Rulestead Dialyzer warnings</name>
  <files>rulestead/mix.exs, rulestead/.dialyzer_ignore.exs</files>
  <action>
    Create `rulestead/.dialyzer_ignore.exs` to suppress warnings related to external apps that cannot be easily typed (like missing `:inets` in mix tasks). 
    Update the `dialyzer` keyword in `rulestead/mix.exs` `project/0` function to include `ignore_warnings: ".dialyzer_ignore.exs"`.
    Run dialyzer and fix any remaining typespecs, such as `validate!/1` in `Rulestead.Config` or `Ecto.Multi` operations in `Rulestead.Store.Ecto`.
  </action>
  <verify>
    <automated>cd rulestead && mix dialyzer</automated>
  </verify>
  <done>mix dialyzer exits with 0 warnings in the rulestead application</done>
</task>

<task type="auto">
  <name>Task 3: Resolve Rulestead Admin Dialyzer warnings</name>
  <files>rulestead_admin/mix.exs, rulestead_admin/.dialyzer_ignore.exs</files>
  <action>
    Run dialyzer in `rulestead_admin/`. 
    Create `rulestead_admin/.dialyzer_ignore.exs` for any warnings related to external admin dependencies (like Phoenix or LiveView internals) that cannot be fixed via typespecs.
    Update `rulestead_admin/mix.exs` dialyzer config to use the ignore file.
    Fix any easily fixable missing specs in the admin code.
  </action>
  <verify>
    <automated>cd rulestead_admin && mix dialyzer</automated>
  </verify>
  <done>mix dialyzer exits with 0 warnings in the rulestead_admin application</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal -> Dialyzer | Static analysis over typed code; no runtime security impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01   | Tampering | Typespecs | accept | Types are strictly checked at compile time by Dialyzer; no runtime tamper vector. |
</threat_model>

<verification>
- Dialyzer passes cleanly in `rulestead`
- Dialyzer passes cleanly in `rulestead_admin`
</verification>

<success_criteria>
`mix dialyzer` outputs success with zero warnings in both core and admin applications, and all public/internal APIs are strictly spec'd.
</success_criteria>

<output>
After completion, create `.planning/phases/26-api-lockdown-and-documentation-perfection/26-01-SUMMARY.md`
</output>
