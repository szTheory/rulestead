---
phase: 19-redis-storage-and-caching-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [rulestead/mix.exs, rulestead/lib/rulestead/store/redis.ex, rulestead/lib/rulestead/application.ex, rulestead/test/rulestead/store/redis_test.exs]
autonomous: true
requirements: [STO-01]

must_haves:
  truths:
    - "Redis adapter correctly fetches serialized snapshots from Redis"
    - "Redis adapter returns standardized Rulestead.Error on misses or connection errors"
    - "Redis connection is managed as part of the Rulestead supervision tree"
  artifacts:
    - path: "rulestead/lib/rulestead/store/redis.ex"
      provides: "Read-only Rulestead.Store implementation for Redis"
    - path: "rulestead/test/rulestead/store/redis_test.exs"
      provides: "Unit tests for Redis adapter logic"
  key_links:
    - from: "rulestead/lib/rulestead/store/redis.ex"
      to: "Redix"
      via: "Redix.command/2"
---

<objective>
Implement the Redis storage adapter to enable high-throughput snapshot reads.

Purpose: Offload evaluation-plane reads from Postgres to a distributed Redis cache.
Output: Working Rulestead.Store.Redis adapter and connection management.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v0.5.0-ROADMAP.md
@.planning/milestones/v0.5.0-REQUIREMENTS.md
@.planning/phases/19-redis-storage-and-caching-adapter/19-CONTEXT.md
@.planning/phases/19-redis-storage-and-caching-adapter/19-PATTERNS.md
@.planning/phases/19-redis-storage-and-caching-adapter/RESEARCH.md
@rulestead/mix.exs
@rulestead/lib/rulestead/store.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Redis dependencies and configure connection</name>
  <files>rulestead/mix.exs, rulestead/lib/rulestead/application.ex</files>
  <behavior>
    - Rulestead app starts Redix worker under supervision
    - Redix worker is named :rulestead_redis (per RESEARCH.md pattern)
  </behavior>
  <action>
    - Add `{:redix, "~> 1.5"}` and `{:telemetry, "~> 1.2"}` to `rulestead/mix.exs` deps.
    - Run `mix deps.get`.
    - Update `rulestead/lib/rulestead/application.ex` to start `Redix` under the supervision tree.
    - Use application configuration (defaulting to "redis://localhost:6379") for the connection.
  </action>
  <verify>
    <automated>mix test --only nothing # Ensure project compiles</automated>
  </verify>
  <done>Dependencies installed, Redix supervised and named :rulestead_redis</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Read-Only Redis Store Adapter</name>
  <files>rulestead/lib/rulestead/store/redis.ex, rulestead/test/rulestead/store/redis_test.exs</files>
  <behavior>
    - fetch_snapshot(command) returns {:ok, snapshot} if key exists in Redis
    - fetch_snapshot(command) returns {:error, %Rulestead.Error{type: :not_found}} if key missing
    - fetch_snapshot(command) uses :erlang.binary_to_term(bin, [:safe]) for security (per RESEARCH.md)
    - All mutation methods (create_flag, update_flag, etc.) return {:error, %Rulestead.Error{type: :invalid_command}}
  </behavior>
  <action>
    - Create `rulestead/lib/rulestead/store/redis.ex` implementing `Rulestead.Store` behavior.
    - Implement `fetch_snapshot/1` using `Redix.command(:rulestead_redis, ["GET", "rulestead:snapshot:#{command.environment_key}"])`.
    - Use `Rulestead.Error` to wrap any Redix transport errors.
    - Stub all other Store callbacks to return invalid_command errors per 19-PATTERNS.md.
    - Create `rulestead/test/rulestead/store/redis_test.exs` with tests for fetch_snapshot success/miss/error and mutation stubs.
  </action>
  <verify>
    <automated>mix test rulestead/test/rulestead/store/redis_test.exs</automated>
  </verify>
  <done>Redis adapter implemented with TDD and passing tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis → Store.Redis | External data from Redis enters the system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Elevation of Privilege | Store.Redis | mitigate | Use `:erlang.binary_to_term(binary, [:safe])` to prevent atom exhaustion/RCE (per RESEARCH.md Pitfall 1). |
| T-19-02 | Information Disclosure | Redis | accept | Snapshots are already distributed to edge nodes; Redis access should be secured by network/password but payload is trusted internal state. |
</threat_model>

<success_criteria>
- `Rulestead.Store.Redis` passes unit tests for read operations.
- All write operations return standardized errors.
- Redis connection is established successfully in development.
</success_criteria>

<output>
After completion, create `.planning/phases/19-redis-storage-and-caching-adapter/19-01-SUMMARY.md`
</output>
