---
phase: 19-redis-storage-and-caching-adapter
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified: [rulestead/lib/rulestead/redis/publisher.ex, rulestead/lib/rulestead/application.ex, rulestead/lib/mix/tasks/rulestead.redis.sync.ex, rulestead/test/rulestead/redis/publisher_test.exs, rulestead/test/rulestead/redis/integration_test.exs]
autonomous: true
requirements: [STO-01, STO-02]

must_haves:
  truths:
    - "A mutation in Ecto triggers an automatic snapshot push to Redis"
    - "The evaluation engine (Refresh) can pull these snapshots from Redis"
    - "Evaluations continue seamlessly if Redis becomes unavailable (graceful degradation)"
    - "Operators can manually sync all snapshots to Redis via Mix task"
  artifacts:
    - path: "rulestead/lib/rulestead/redis/publisher.ex"
      provides: "Telemetry handler that syncs Ecto snapshots to Redis"
    - path: "rulestead/lib/mix/tasks/rulestead.redis.sync.ex"
      provides: "Mix task for initial/manual Redis population"
    - path: "rulestead/test/rulestead/redis/integration_test.exs"
      provides: "E2E verification of Ecto -> Redis -> Runtime flow"
  key_links:
    - from: "[:rulestead, :runtime, :snapshot, :published]"
      to: "Rulestead.Redis.Publisher"
      via: ":telemetry.attach/4"
    - from: "Rulestead.Redis.Publisher"
      to: "Redis"
      via: "Redix SET command"
---

<objective>
Integrate the Redis adapter with the control plane and ensure graceful degradation.

Purpose: Automate the distribution of flag state from the database to the cache.
Output: Publisher service and integration tests for distributed state flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v0.5.0-ROADMAP.md
@.planning/milestones/v0.5.0-REQUIREMENTS.md
@.planning/phases/19-redis-storage-and-caching-adapter/19-CONTEXT.md
@.planning/phases/19-redis-storage-and-caching-adapter/19-PATTERNS.md
@.planning/phases/19-redis-storage-and-caching-adapter/RESEARCH.md
@.planning/phases/19-redis-storage-and-caching-adapter/19-01-PLAN.md
@rulestead/lib/rulestead/store/ecto.ex
@rulestead/lib/rulestead/runtime/refresh.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Redis Publisher</name>
  <files>rulestead/lib/rulestead/redis/publisher.ex, rulestead/lib/rulestead/application.ex, rulestead/test/rulestead/redis/publisher_test.exs</files>
  <behavior>
    - Publisher attaches to [:rulestead, :runtime, :snapshot, :published] telemetry event
    - When event triggers, publisher fetches latest snapshot from Rulestead.Store.Ecto
    - Publisher pushes fetched snapshot to Redis using SET "rulestead:snapshot:#{env_key}"
  </behavior>
  <action>
    - Create `rulestead/lib/rulestead/redis/publisher.ex` following the pattern in 19-PATTERNS.md.
    - Implement `init/1` with `:telemetry.attach/4`.
    - Implement `handle_event/4` to fetch the snapshot from Ecto (using `Rulestead.Store.Ecto.fetch_snapshot/1`) and push it to Redis (using `:erlang.term_to_binary/1`).
    - Update `rulestead/lib/rulestead/application.ex` to start `Rulestead.Redis.Publisher` in the supervision tree.
    - Create `rulestead/test/rulestead/redis/publisher_test.exs` and use a mock/spy to verify telemetry attachment and Redis push calls.
  </action>
  <verify>
    <automated>mix test rulestead/test/rulestead/redis/publisher_test.exs</automated>
  </verify>
  <done>Publisher implemented and verified via unit tests</done>
</task>

<task type="auto">
  <name>Task 2: End-to-End Integration and Graceful Degradation</name>
  <files>rulestead/test/rulestead/redis/integration_test.exs</files>
  <action>
    - Create `rulestead/test/rulestead/redis/integration_test.exs`.
    - Scenario 1: Create a flag via `Rulestead` API (using Ecto), verify snapshot appears in Redis.
    - Scenario 2: Configure `Rulestead.Runtime.Refresh` to use `Rulestead.Store.Redis`, verify it pulls from Redis.
    - Scenario 3: Simulate Redis failure (e.g. stop Redix or use a faulty mock), verify `Refresh` keeps the previous cache and emits "refresh failed" telemetry instead of crashing (Graceful Degradation per STO-02).
  </action>
  <verify>
    <automated>mix test rulestead/test/rulestead/redis/integration_test.exs</automated>
  </verify>
  <done>End-to-end flow verified including graceful degradation</done>
</task>

<task type="auto">
  <name>Task 3: Implement Redis Seeding Mix Task</name>
  <files>rulestead/lib/mix/tasks/rulestead.redis.sync.ex</files>
  <behavior>
    - Mix task fetches all environment snapshots from Ecto
    - Mix task pushes each snapshot to Redis
  </behavior>
  <action>
    - Create `rulestead/lib/mix/tasks/rulestead.redis.sync.ex`.
    - Implementation: Iterate over all `Rulestead.Environment` records, call `Rulestead.Store.Ecto.fetch_snapshot/1` for each, and push the resulting binary to Redis.
  </action>
  <verify>
    <manual>Run `mix rulestead.redis.sync` and verify Redis is populated with snapshots for all existing environments.</manual>
  </verify>
  <done>Redis seeding task implemented and verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Publisher → Redis | Internal state pushed to external cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Denial of Service | Redis | mitigate | Ensure Redis push failures in `Publisher` do not crash the calling process or the supervision tree (use async cast or handle errors gracefully). |
</threat_model>

<success_criteria>
- Snapshot sync from Ecto to Redis is fully automated.
- Evaluation plane successfully hydrates from Redis.
- System remains operational during Redis outages.
</success_criteria>

<output>
After completion, create `.planning/phases/19-redis-storage-and-caching-adapter/19-02-SUMMARY.md`
</output>
