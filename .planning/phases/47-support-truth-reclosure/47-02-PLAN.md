---
phase: 47-support-truth-reclosure
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - rulestead_admin/README.md
  - README.md
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "The mounted companion package docs state that host-owned prerequisites are required and missing prerequisites fail closed."
    - "The `?env=` query parameter is documented as canonical, while remembered env/session state is documented as fallback-only convenience."
    - "Package wording stays narrowly focused on the mounted host seam rather than turning into a second root README or lifecycle guide."
  artifacts:
    - path: "rulestead_admin/README.md"
      provides: "The canonical fail-closed mounted companion contract wording"
  key_links:
    - from: "rulestead_admin/README.md"
      to: "README.md"
      via: "root-level support posture reference"
      pattern: "broader release and proof posture"
---

<objective>
Publish the exact mounted prerequisite, fail-closed, and fallback semantics the repo currently supports.

Purpose: the mounted companion package README should tell host-app teams what inputs are required, what happens when those inputs are missing, and how explicit URL/env selection interacts with remembered session fallback so support truth does not rely on inference.
Output: sharper mounted contract wording in `rulestead_admin/README.md`, with only minimal supporting touch-up in `README.md` if needed.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-support-truth-reclosure/47-CONTEXT.md
@.planning/phases/47-support-truth-reclosure/47-RESEARCH.md
@README.md
@rulestead_admin/README.md
@rulestead_admin/lib/rulestead_admin/live/session.ex
@rulestead_admin/test/rulestead_admin/live/session_test.exs
@rulestead_admin/test/rulestead_admin/integration/admin_mount_test.exs
@scripts/ci/test.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make missing-prerequisite behavior explicit in the package README</name>
  <files>rulestead_admin/README.md</files>
  <action>
    Revise `rulestead_admin/README.md` so it explicitly states:
    - the host must provide the documented router macro, `policy:`, actor/session inputs, and environment list
    - missing or unsupported prerequisites fail closed for the mounted companion surface
    - `rulestead_admin` consumes host-owned auth/identity/policy/session truth and does not supply its own auth model

    Ground the wording in the current mounted proof surface and keep it focused on the host seam instead of implementation internals.
  </action>
  <verify>
    <automated>rg -n "fail closed|policy:|current_actor|rulestead_admin_environments|rulestead_admin_last_env|host.*owns|host-owned" /Users/jon/projects/rulestead/rulestead_admin/README.md</automated>
  </verify>
  <done>The package README states the host-owned prerequisites and fail-closed mounted behavior explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Clarify canonical env selection versus fallback-only remembered state</name>
  <files>rulestead_admin/README.md README.md</files>
  <action>
    Update the environment-selection wording so:
    - `?env=` remains the canonical selector
    - remembered env/session values are described as fallback-only convenience when URL scope is absent
    - the root README, if it mentions fallback semantics at all, stays short and points to the package README for the full contract

    Do not introduce a new support guide or a second lifecycle narrative.
  </action>
  <verify>
    <automated>rg -n "\\?env=|canonical environment selector|fallback|remembered" /Users/jon/projects/rulestead/README.md /Users/jon/projects/rulestead/rulestead_admin/README.md</automated>
  </verify>
  <done>The docs make URL/env precedence and fallback-only remembered state unambiguous.</done>
</task>

</tasks>

<verification>
- `RULESTEAD_TEST_SCOPE=mounted_admin_contract bash /Users/jon/projects/rulestead/scripts/ci/test.sh`
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/release_contract_test.exs`
</verification>

<success_criteria>
- `rulestead_admin/README.md` explicitly documents fail-closed mounted prerequisite behavior.
- The package README makes `?env=` canonical and remembered env/session values fallback-only.
- Root/package wording remains narrow and does not create a standalone-admin or second-root narrative.
</success_criteria>

<output>
After completion, create `.planning/phases/47-support-truth-reclosure/47-02-SUMMARY.md`
</output>
