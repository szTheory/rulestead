---
phase: 118-evidence-idempotence-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check_design_system_evidence.py
  - scripts/ci/lint.sh
autonomous: true
requirements:
  - VER-02
  - VER-03
must_haves:
  truths:
    - "A normal lint run blocks removal of Phase 118 browser evidence contracts before design-system drift reaches review."
    - "The guard preserves generated-artifact screenshot posture from D-02 and D-12 without adding pixel baselines or external AI review."
    - "The guard verifies matrix, workflow, static contrast, fixture-health, and route-isolation evidence hooks required by VER-02 and VER-03."
  artifacts:
    - path: scripts/check_design_system_evidence.py
      provides: "Stdlib source guard for Phase 118 evidence and idempotence invariants"
      contains: "DESIGN SYSTEM EVIDENCE OK"
    - path: scripts/ci/lint.sh
      provides: "Durable CI guard-chain wiring for design-system evidence drift"
      contains: "check_design_system_evidence.py"
  key_links:
    - from: scripts/ci/lint.sh
      to: scripts/check_design_system_evidence.py
      via: "python3 guard invocation"
      pattern: "check_design_system_evidence\\.py"
    - from: scripts/check_design_system_evidence.py
      to: examples/demo/frontend/tests/ui-matrix.spec.ts
      via: "source assertions"
      pattern: "ui-matrix-overview-shell"
    - from: scripts/check_design_system_evidence.py
      to: examples/demo/frontend/tests/admin-flow-ia.spec.ts
      via: "route set and artifact source assertions"
      pattern: "overview.*inventory.*rules.*kill.*audience.*audit.*explain.*simulate"
---

<objective>
Create the durable source guard that keeps Phase 118 evidence rerunnable and idempotent.

Purpose: VER-03 requires the existing guard chain to stay green and extend only where it prevents real design-system drift. This plan adds one narrow stdlib guard for the concrete drift class exposed by the current design-system milestone: losing matrix/workflow evidence coverage, generated screenshot artifact posture, fixture-health proof, or selected contrast proof.
Output: `scripts/check_design_system_evidence.py` and `scripts/ci/lint.sh` wiring.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/118-evidence-idempotence-guardrails/118-CONTEXT.md
@.planning/phases/118-evidence-idempotence-guardrails/118-RESEARCH.md
@.planning/phases/118-evidence-idempotence-guardrails/118-VALIDATION.md
@.planning/phases/118-evidence-idempotence-guardrails/118-PATTERNS.md
@.planning/phases/118-evidence-idempotence-guardrails/118-UI-SPEC.md
@examples/demo/frontend/tests/ui-matrix.spec.ts
@examples/demo/frontend/tests/admin-flow-ia.spec.ts
@examples/demo/frontend/tests/design-system.spec.ts
@examples/demo/backend/test/rulestead_demo_web/live/ui_matrix_live_test.exs
@scripts/check_admin_foundations.py
@scripts/ci/lint.sh

<interfaces>
Existing evidence contracts the guard must preserve:
- `ui-matrix.spec.ts` uses `matrixPath = "/dev/rulestead-admin/ui-matrix"`, `testInfo.outputPath(...)`, and `ui-matrix-${sectionName}-${theme.name}-${viewport.name}-${motion.name}.png`.
- `admin-flow-ia.spec.ts` uses route names `overview`, `inventory`, `rules`, `kill`, `audience`, `audit`, `explain`, and `simulate`, and writes `flow-${route.name}-${theme.name}-${viewport.name}.png`.
- `design-system.spec.ts` owns selected contrast checks including `--rs-text on --rs-surface`, `--rs-on-primary`, and the documented input-hint contrast exception.
- `ui_matrix_live_test.exs` owns fixture health, rare-state coverage, real-component source checks, and `refute admin_router_source =~ "ui-matrix"`.
- `scripts/ci/lint.sh` is the normal guard spine from D-10.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the design-system evidence source guard</name>
  <files>scripts/check_design_system_evidence.py</files>
  <read_first>
    <file>scripts/check_design_system_evidence.py</file>
    <file>.planning/phases/118-evidence-idempotence-guardrails/118-CONTEXT.md</file>
    <file>.planning/phases/118-evidence-idempotence-guardrails/118-RESEARCH.md</file>
    <file>.planning/phases/118-evidence-idempotence-guardrails/118-VALIDATION.md</file>
    <file>.planning/phases/118-evidence-idempotence-guardrails/118-PATTERNS.md</file>
    <file>.planning/phases/118-evidence-idempotence-guardrails/118-UI-SPEC.md</file>
    <file>examples/demo/frontend/tests/ui-matrix.spec.ts</file>
    <file>examples/demo/frontend/tests/admin-flow-ia.spec.ts</file>
    <file>examples/demo/frontend/tests/design-system.spec.ts</file>
    <file>examples/demo/backend/test/rulestead_demo_web/live/ui_matrix_live_test.exs</file>
    <file>scripts/check_admin_foundations.py</file>
  </read_first>
  <action>Create executable `scripts/check_design_system_evidence.py` using only Python stdlib, per D-10, D-11, D-12, D-13, and D-19. Follow the failure aggregation style from `scripts/check_admin_foundations.py`: collect all failures, print `DESIGN SYSTEM EVIDENCE DRIFT DETECTED` plus one indented failure per line on error, and print exactly `DESIGN SYSTEM EVIDENCE OK` on success. Validate these exact evidence invariants: `ui-matrix.spec.ts` contains `/dev/rulestead-admin/ui-matrix`, `desktop`, `mobile`, `light`, `dark`, `system-dark`, `reducedMotion`, `expectNoHorizontalOverflow`, `#rs-cmdk`, `.rs-task-link`, all 13 `data-matrix-section` values from `overview-shell` through `static-fixtures`, `testInfo.outputPath`, and `ui-matrix-${sectionName}-${theme.name}-${viewport.name}-${motion.name}.png`; `admin-flow-ia.spec.ts` contains the exact eight route names in order `overview`, `inventory`, `rules`, `kill`, `audience`, `audit`, `explain`, `simulate`, plus `expectNoHorizontalOverflow`, `page.keyboard.press("Tab")`, `hiddenPaletteControl`, `getByRole("textbox", { name: "Reason" })`, `testInfo.outputPath`, and `flow-${route.name}-${theme.name}-${viewport.name}.png`; `design-system.spec.ts` contains selected contrast labels for `--rs-text on --rs-surface`, `--rs-on-primary`, badge text on soft surfaces, and the documented input-hint contrast exception; `ui_matrix_live_test.exs` contains every route label from the eight-route set, rare states `:empty`, `:permission_denied`, `:read_only`, `:unavailable`, `:destructive`, `:loading`, `:error`, and `refute admin_router_source =~ "ui-matrix"`. Also scan project manifests and the two Playwright evidence specs for forbidden adoption forms `toHaveScreenshot(`, `.toHaveScreenshot`, `matchSnapshot(`, `pixelmatch(`, `testInfo.snapshotPath`, `__screenshots__`, `-snapshots`, `@storybook`, and `phoenix_storybook`; do not fail on the existing composed guard strings such as `"toHave" + "Screenshot"` because those are source-posture guards, not tooling adoption. Do not add package installs, network access, runtime routes, schemas, release workflow edits, FleetDesk branding, or `rulestead_admin` publish-prep work per D-14, D-15, and D-16.</action>
  <verify>
    <automated>python3 scripts/check_design_system_evidence.py</automated>
    <automated>rg -n "DESIGN SYSTEM EVIDENCE OK|DESIGN SYSTEM EVIDENCE DRIFT DETECTED|ui-matrix-\\$\\{sectionName\\}|flow-\\$\\{route.name\\}" scripts/check_design_system_evidence.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Running `python3 scripts/check_design_system_evidence.py` exits 0 and prints exactly `DESIGN SYSTEM EVIDENCE OK` on the current repo state.</criterion>
    <criterion>The guard source contains explicit checks for all eight workflow route names: `overview`, `inventory`, `rules`, `kill`, `audience`, `audit`, `explain`, and `simulate`.</criterion>
    <criterion>The guard source contains explicit checks for the matrix artifact pattern `ui-matrix-${sectionName}-${theme.name}-${viewport.name}-${motion.name}.png` and workflow artifact pattern `flow-${route.name}-${theme.name}-${viewport.name}.png`.</criterion>
    <criterion>The guard source rejects forbidden adoption strings without matching the existing composed guard strings in the Playwright specs.</criterion>
  </acceptance_criteria>
  <done>Phase 118 has a deterministic source guard for evidence posture, route/matrix coverage, selected contrast proof, fixture health, and visual-tooling exclusions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the guard into the normal lint spine</name>
  <files>scripts/ci/lint.sh</files>
  <read_first>
    <file>scripts/ci/lint.sh</file>
    <file>scripts/check_design_system_evidence.py</file>
    <file>.planning/phases/118-evidence-idempotence-guardrails/118-CONTEXT.md</file>
    <file>.planning/phases/118-evidence-idempotence-guardrails/118-RESEARCH.md</file>
    <file>.planning/phases/118-evidence-idempotence-guardrails/118-VALIDATION.md</file>
  </read_first>
  <action>Add one CI-readable comment and one command to `scripts/ci/lint.sh` after `python3 "${RULESTEAD_REPO}/scripts/check_admin_foundations.py"` and before the SVG size budget loop. The command must be exactly `python3 "${RULESTEAD_REPO}/scripts/check_design_system_evidence.py"`. The comment must name design-system evidence posture, generated screenshots, and forbidden visual-baseline tooling. Preserve `set -euo pipefail`, the `RULESTEAD_REPO` root reset, existing guard order, package whitelist, Mix checks, Dialyzer, and SVG budgets. This implements D-10 and D-11 by extending the guard chain only for a concrete repeatable drift class, and it preserves D-12 by blocking visual-baseline tooling without adding broad pixel maintenance.</action>
  <verify>
    <automated>bash -n scripts/ci/lint.sh</automated>
    <automated>python3 scripts/check_design_system_evidence.py</automated>
    <automated>rg -n "check_design_system_evidence.py|generated screenshots|visual-baseline" scripts/ci/lint.sh</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`scripts/ci/lint.sh` contains exactly one invocation of `scripts/check_design_system_evidence.py`.</criterion>
    <criterion>`bash -n scripts/ci/lint.sh` exits 0.</criterion>
    <criterion>The new lint comment includes `generated screenshots` and `visual-baseline`.</criterion>
    <criterion>No package manifest, lockfile, schema, migration, release workflow, or `rulestead_admin/mix.exs` file is modified.</criterion>
  </acceptance_criteria>
  <done>The normal CI lint path now runs the Phase 118 evidence guard without widening release, package, schema, route, or visual-regression scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree to CI guard | Guard script turns repository text into pass/fail trust for design-system evidence posture. |
| CI guard to planning evidence | Later plans rely on this guard output as part of VER-03 proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Tampering | `scripts/check_design_system_evidence.py` | mitigate | Use explicit positive source assertions for exact route names, artifact patterns, section names, contrast labels, and matrix isolation markers; aggregate failures and exit nonzero on drift. |
| T-118-02 | Repudiation | `scripts/ci/lint.sh` guard output | mitigate | Print deterministic success string `DESIGN SYSTEM EVIDENCE OK` and wire the command into lint so future runs can cite a durable output. |
| T-118-03 | Denial of Service | CI lint guard | mitigate | Keep the new guard stdlib-only, file-read-only, and network-free; do not run browser tests from lint. |
| T-118-04 | Information Disclosure | source guard | accept | The guard reads source files already in the repo and does not write screenshots, secrets, environment values, or browser artifacts. |
| T-118-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are planned; if an executor proposes an install, stop and require package legitimacy review before changing manifests. |
</threat_model>

<verification>
Run the narrow guard verification for this plan:
- `python3 scripts/check_design_system_evidence.py`
- `bash -n scripts/ci/lint.sh`
- `rg -n "check_design_system_evidence.py|DESIGN SYSTEM EVIDENCE OK" scripts/check_design_system_evidence.py scripts/ci/lint.sh`
</verification>

<success_criteria>
VER-03 has a durable guard-chain extension, VER-02 evidence hooks are source-guarded, D-10 through D-16 and D-19 are explicitly preserved, and no out-of-scope package, schema, route, release, FleetDesk, or visual-baseline work appears in the diff.
</success_criteria>

<output>
Create `.planning/phases/118-evidence-idempotence-guardrails/118-01-SUMMARY.md` when done.
</output>
