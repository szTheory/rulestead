---
phase: "87"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead_admin/priv/static/css/rulestead_admin.css
autonomous: true
requirements:
  - THM-05

must_haves:
  truths:
    - "The :root block retains ONLY theme-invariant tokens: typography families and scale, radius, spacing/layout, control sizing, z-index ladder, motion easing, --rs-focus-ring-offset, --rs-disabled-opacity."
    - "All neutral ramp tokens (--rs-neutral-0 through --rs-neutral-900), semantic surface/border/text aliases, brand/status hard-hex tokens, shadow tokens, --rs-focus-ring, --rs-disabled-bg, --rs-disabled-text are declared on .rs-shell,[data-rulestead] with light values and color-scheme:light."
    - "No color token remains on :root after the refactor — a computed-styles inspection of <html> shows no --rs-neutral-*, --rs-bg, --rs-surface, --rs-primary, or --rs-shadow* tokens."
    - "The light default block on .rs-shell includes the three new tokens: --rs-focus-ring-color, --rs-overlay-veil, --rs-scrim."
    - "--rs-focus-ring references var(--rs-focus-ring-color) instead of a raw rgba() value."
    - "The CSS section comment header is updated to describe the new four-block THEME LAYER structure."
  artifacts:
    - path: "rulestead_admin/priv/static/css/rulestead_admin.css"
      provides: "Refactored token block: :root invariant-only + .rs-shell light default"
      contains: ".rs-shell,"
  key_links:
    - from: ":root block"
      to: ".rs-shell light block"
      via: "CSS cascade — :root invariant tokens inherited; variant tokens overridden at .rs-shell scope"
      pattern: ".rs-shell,"
---

<objective>
Refactor the CSS token block: strip all color/surface/border/text/shadow/focus tokens from `:root` and place them in a new `.rs-shell, [data-rulestead]` light-default block with `color-scheme: light`. The `:root` block is left with only theme-invariant tokens. Introduce the three new tokens (`--rs-focus-ring-color`, `--rs-overlay-veil`, `--rs-scrim`) in the light default block. Update `--rs-focus-ring` to reference `var(--rs-focus-ring-color)`.

This plan does NOT author the dark blocks — that is Plan 03. After Plan 02, the UI will render identically to before (light values are unchanged), but the token architecture now supports the dark cascade. Plan 02 is safe to land without dark mode because no dark blocks exist yet — the light default is the only block that fires.

Purpose: Establishes scope containment (THM-05) and the invariant/variant split. Every component rule that consumes `var(--rs-*)` continues to work unchanged because the light values are identical — they are merely redeclared one CSS selector scope deeper.

Output: Modified `rulestead_admin.css` with a split token block.
</objective>

<execution_context>
@/Users/jon/projects/rulestead/.planning/phases/87-token-theme-foundation/87-RESEARCH.md
@/Users/jon/projects/rulestead/.planning/phases/87-token-theme-foundation/87-CONTEXT.md
</execution_context>

<context>
@/Users/jon/projects/rulestead/.planning/PROJECT.md
@/Users/jon/projects/rulestead/.planning/ROADMAP.md

<interfaces>
<!-- Current :root block: lines 38-178 of rulestead_admin.css -->
<!--
Theme-invariant (STAY in :root — lines verified in RESEARCH.md §1):
  - Typography families:    --rs-font-display, --rs-font-sans, --rs-font-mono (lines 40-42)
  - Typography scale:       --rs-text-2xs … --rs-text-2xl, --rs-leading-*, --rs-weight-*, --rs-tracking-* (lines 97-115)
  - Radius:                 --rs-radius-sm … --rs-radius-full (lines 117-122)
  - Layout + spacing:       --rs-shell-max, --rs-space-1 … --rs-space-10, --rs-section-gap, --rs-page-gap (lines 130-142)
  - Control sizing:         --rs-control-h, --rs-control-h-sm, --rs-control-h-lg, --rs-control-px, --rs-touch-target-min (lines 144-149)
  - Focus structural:       --rs-focus-ring-offset: 2px, --rs-disabled-opacity: 0.55 (lines 153-154) ← STAY
  - Z-index:                --rs-z-base … --rs-z-toast (lines 159-165)
  - Motion:                 --rs-motion-fast … --rs-ease-emphasis (lines 169-177)

Theme-variant (MOVE to .rs-shell light block — lines verified in RESEARCH.md §1):
  - Neutral ramp:           --rs-neutral-0 … --rs-neutral-900 (lines 44-55)
  - Surface/border/text:    --rs-bg, --rs-surface, --rs-surface-muted, --rs-surface-faint, --rs-border,
                            --rs-border-subtle, --rs-border-strong, --rs-text, --rs-text-muted,
                            --rs-text-placeholder (lines 58-67)
  - Brand:                  --rs-primary, --rs-primary-hover, --rs-primary-soft, --rs-on-primary,
                            --rs-accent, --rs-accent-soft (lines 70-74)
  - Status success:         --rs-success, --rs-success-hover, --rs-success-soft, --rs-success-bg-subtle,
                            --rs-success-text, --rs-success-border (lines 76-81)
  - Status warning:         --rs-warning, --rs-warning-soft, --rs-warning-text, --rs-warning-border
                            ← ADD --rs-warning-hover: #92400e here in light (lines 83-85)
  - Status error/critical:  --rs-error, --rs-error-hover, --rs-critical, --rs-error-soft,
                            --rs-error-bg-subtle, --rs-error-text, --rs-error-text-strong,
                            --rs-error-border, --rs-error-border-strong (lines 86-94)
  - Shadows:                --rs-shadow-sm, --rs-shadow, --rs-shadow-panel (lines 125-127)
  - Focus + disabled:       --rs-focus-ring (update to reference var(--rs-focus-ring-color)),
                            --rs-disabled-bg, --rs-disabled-text (lines 152-156 partial)
  - NEW tokens (light values):
      --rs-focus-ring-color: rgba(37, 99, 235, 0.55)
      --rs-overlay-veil: rgba(238, 241, 245, 0.9)
      --rs-scrim: rgba(15, 23, 35, 0.45)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor :root — invariant-only; author light default block</name>
  <files>rulestead_admin/priv/static/css/rulestead_admin.css</files>

  <read_first>
    - /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css lines 1-220 (token block + first component rules — read before editing)
    - /Users/jon/projects/rulestead/.planning/phases/87-token-theme-foundation/87-RESEARCH.md §1 (exact token inventory), §2 (cascade mechanics + four-block structure), §7 (file section order after refactor), §8 pitfall 2 (color-scheme on :root)
    - /Users/jon/projects/rulestead/.planning/phases/87-token-theme-foundation/87-CONTEXT.md decisions section (token split + scope rules)
  </read_first>

  <action>
    Perform a surgical rewrite of the token block in `rulestead_admin/priv/static/css/rulestead_admin.css` (lines 38–178 approximately). All other lines in the file (component rules, media queries, etc.) must remain byte-for-byte identical. Do not reformat, re-order, or touch anything outside the token block.

    STEP 1 — Strip the :root block to invariant-only:

    The `:root { ... }` block keeps ONLY:
    - Typography families (--rs-font-display, --rs-font-sans, --rs-font-mono)
    - Typography scale (--rs-text-2xs through --rs-text-2xl, --rs-leading-*, --rs-weight-*, --rs-tracking-*)
    - Radius (--rs-radius-sm through --rs-radius-full)
    - Layout + spacing (--rs-shell-max, --rs-space-1 through --rs-space-10, --rs-section-gap, --rs-page-gap)
    - Control sizing (--rs-control-h, --rs-control-h-sm, --rs-control-h-lg, --rs-control-px, --rs-touch-target-min)
    - Focus structural scalars: --rs-focus-ring-offset: 2px and --rs-disabled-opacity: 0.55 (these are non-color, safe on :root)
    - Z-index ladder (--rs-z-base through --rs-z-toast)
    - Motion (--rs-motion-fast, --rs-motion-base, --rs-motion-slow, --rs-motion-slower, all --rs-ease-*)

    Remove from :root (they will be re-declared below in the variant block):
    - All --rs-neutral-* tokens
    - All --rs-bg, --rs-surface*, --rs-border*, --rs-text* tokens
    - All --rs-primary*, --rs-accent*, --rs-on-primary tokens
    - All status tokens (--rs-success*, --rs-warning*, --rs-error*, --rs-critical)
    - All --rs-shadow* tokens
    - --rs-focus-ring (will be redeclared with updated value)
    - --rs-disabled-bg, --rs-disabled-text (variant — color tokens)

    Update the :root section header comment to read:
    `/* ─── INVARIANT TOKENS (declared once; not theme-sensitive) ──────────── */`

    STEP 2 — Insert the THEME LAYER section after the :root block:

    Immediately after the closing `}` of `:root`, insert:

    ```
    /* ─── THEME LAYER ──────────────────────────────────────────────────────────
       All color, surface, border, text, shadow, and focus tokens are declared
       here — once per theme. Re-theming = redeclaring these tokens on the
       correct scope selector. Theme scope: .rs-shell / [data-rulestead] ONLY.
       NEVER declare theme tokens on :root or <html> (mounted-package discipline).
    ─────────────────────────────────────────────────────────────────────────── */

    /* ── 1. Light default ─────────────────────────────────────────────────── */
    .rs-shell,
    [data-rulestead] {
      color-scheme: light;

      /* Neutral ramp — mineral grays. Foundation for surfaces, borders, text. */
      --rs-neutral-0: #ffffff;
      --rs-neutral-25: #f8fafc;
      --rs-neutral-50: #f4f6f8;
      --rs-neutral-100: #eef1f5;
      --rs-neutral-200: #e7ebf0;
      --rs-neutral-300: #d8dee6;
      --rs-neutral-400: #b8c2cf;
      --rs-neutral-500: #99a3af;
      --rs-neutral-600: #5c6b7a;
      --rs-neutral-700: #263241;
      --rs-neutral-900: #1a2332;

      /* Surface / border / text aliases */
      --rs-bg:               var(--rs-neutral-50);
      --rs-surface:          var(--rs-neutral-0);
      --rs-surface-muted:    var(--rs-neutral-100);
      --rs-surface-faint:    var(--rs-neutral-25);
      --rs-border:           var(--rs-neutral-300);
      --rs-border-subtle:    var(--rs-neutral-200);
      --rs-border-strong:    var(--rs-neutral-400);
      --rs-text:             var(--rs-neutral-900);
      --rs-text-muted:       var(--rs-neutral-600);
      --rs-text-placeholder: var(--rs-neutral-500);

      /* Brand */
      --rs-primary:       #2563eb;
      --rs-primary-hover: #1d4ed8;
      --rs-primary-soft:  #dbeafe;
      --rs-on-primary:    #ffffff;
      --rs-accent:        #c45c26;
      --rs-accent-soft:   #fde8dc;

      /* Status — success */
      --rs-success:           #15803d;
      --rs-success-hover:     #166534;
      --rs-success-soft:      #dcfce7;
      --rs-success-bg-subtle: #f7fff9;
      --rs-success-text:      #047857;
      --rs-success-border:    #86efac;

      /* Status — warning */
      --rs-warning:        #b45309;
      --rs-warning-hover:  #92400e;
      --rs-warning-soft:   #fef3c7;
      --rs-warning-text:   #a16207;
      --rs-warning-border: #fcd34d;

      /* Status — error / critical */
      --rs-error:              #b91c1c;
      --rs-error-hover:        #991b1b;
      --rs-critical:           #b91c1c;
      --rs-error-soft:         #fee2e2;
      --rs-error-bg-subtle:    #fff7f7;
      --rs-error-text:         #be123c;
      --rs-error-text-strong:  #7f1d1d;
      --rs-error-border:       #fca5a5;
      --rs-error-border-strong:#fecaca;

      /* Shadows */
      --rs-shadow-sm:    0 1px 2px rgba(26, 35, 50, 0.05);
      --rs-shadow:       0 1px 2px rgba(26, 35, 50, 0.06), 0 8px 24px rgba(26, 35, 50, 0.06);
      --rs-shadow-panel: 0 1px 2px rgba(26, 35, 50, 0.04), 0 12px 32px rgba(26, 35, 50, 0.07);

      /* Focus + disabled */
      --rs-focus-ring-color: rgba(37, 99, 235, 0.55);
      --rs-focus-ring:       0 0 0 3px var(--rs-focus-ring-color);
      --rs-disabled-bg:      var(--rs-neutral-100);
      --rs-disabled-text:    var(--rs-neutral-500);

      /* Overlay / scrim (consumed by Phases 88 + 89) */
      --rs-overlay-veil: rgba(238, 241, 245, 0.9);
      --rs-scrim:        rgba(15, 23, 35, 0.45);
    }
    ```

    STEP 3 — Leave a clear placeholder comment for Plans 03 blocks (system dark, explicit dark, explicit light) that will be added by Plan 03. Insert after the closing `}` of the light block:

    ```
    /* ── 2–4. System dark, explicit dark, explicit light ─ added by Plan 03 ── */

    /* ─── END THEME LAYER ─────────────────────────────────────────────────── */
    ```

    IMPORTANT CONSTRAINTS:
    - Do NOT change anything in component rules (after line ~186 `.rs-shell { ... }`). The first component rule is `.rs-shell { font-family: ... }` and it must remain unchanged.
    - The `.rs-form-help` and `.rs-form-error` rules at the very top of the file (lines 1–20) must remain unchanged. These appear before the `:root` block and are untouched.
    - Preserve the existing breakpoints documentation comment inside the `:root` section header — it documents the breakpoints (--bp-sm through --bp-xl). Move it into the `:root` block header if it was there, or keep it in the invariant section comment.
    - The neutral ramp gap at 800 is intentional — do NOT add --rs-neutral-800. The gap matches the existing light ramp.
    - Add --rs-warning-hover: #92400e (light value) which was missing from the original token block.
  </action>

  <verify>
    <automated>
      # Scope check: no color tokens on :root
      grep -v '^[[:space:]]*//' /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css | grep -A 200 '^:root' | grep -B 1 '^}' | grep -c 'rs-neutral\|rs-bg\|rs-surface\|rs-primary\|rs-shadow\|rs-focus-ring[^-off]'
    </automated>
  </verify>

  <acceptance_criteria>
    - After the edit, `:root` block contains NO `--rs-neutral-*`, `--rs-bg`, `--rs-surface`, `--rs-primary`, `--rs-shadow*`, or `--rs-focus-ring:` declarations. Verify with: `awk '/^:root/,/^}/' rulestead_admin/priv/static/css/rulestead_admin.css | grep -c 'rs-neutral\|rs-bg\|rs-surface\|rs-primary\|rs-shadow\|--rs-focus-ring:'` returns 0.
    - `:root` STILL contains `--rs-focus-ring-offset` and `--rs-disabled-opacity` (the structural non-color scalars). Verify: `awk '/^:root/,/^}/' rulestead_admin/priv/static/css/rulestead_admin.css | grep -c 'focus-ring-offset\|disabled-opacity'` returns 2.
    - `.rs-shell, [data-rulestead]` block exists and contains `color-scheme: light`. Verify: `grep -c 'color-scheme: light' rulestead_admin/priv/static/css/rulestead_admin.css` returns at least 1.
    - `--rs-warning-hover` exists in the light block. Verify: `grep -c 'rs-warning-hover' rulestead_admin/priv/static/css/rulestead_admin.css` returns at least 1.
    - `--rs-focus-ring:` references `var(--rs-focus-ring-color)` (not a raw rgba value). Verify: `grep 'rs-focus-ring:' rulestead_admin/priv/static/css/rulestead_admin.css | grep -v 'offset'` shows `var(--rs-focus-ring-color)`.
    - New tokens present in light block: `grep -c 'rs-focus-ring-color\|rs-overlay-veil\|rs-scrim' rulestead_admin/priv/static/css/rulestead_admin.css` returns at least 3.
    - `[data-rulestead]` appears as a co-selector in the light block. Verify: `grep -c 'data-rulestead' rulestead_admin/priv/static/css/rulestead_admin.css` returns at least 1.
    - Scope fidelity — `color-scheme` does NOT appear in `:root`. Verify: `awk '/^:root/,/^}/' rulestead_admin/priv/static/css/rulestead_admin.css | grep -c 'color-scheme'` returns 0.
    - Surface direction: `--rs-surface-faint` is declared as `var(--rs-neutral-25)` (light ramp, lighter than surface — correct for light mode). Verify: `grep 'rs-surface-faint' rulestead_admin/priv/static/css/rulestead_admin.css | head -1` shows `neutral-25`.
    - WCAG: Light token values are identical to the original (no accidental changes). Key smoke-check: `grep 'rs-primary:' rulestead_admin/priv/static/css/rulestead_admin.css | head -1` shows `#2563eb`; `grep 'rs-text:' rulestead_admin/priv/static/css/rulestead_admin.css | grep 'neutral-900'` exists.
    - Component rules unchanged: `.rs-shell {` still exists at approximately the same position with `font-family: var(--rs-font-sans)`. Verify: `grep -c 'font-family: var(--rs-font-sans)' rulestead_admin/priv/static/css/rulestead_admin.css` returns 1.
  </acceptance_criteria>

  <done>The :root block is invariant-only (typography/spacing/radius/sizing/z-index/motion), the .rs-shell light block contains all variant tokens with unchanged light values plus three new tokens, --rs-focus-ring references var(--rs-focus-ring-color), --rs-warning-hover is present, and no component rules were modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token scope boundary | .rs-shell / [data-rulestead] selector contains variant tokens; :root contains only non-color invariants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-02 | Information Disclosure | color-scheme on :root | mitigate | Explicitly confirmed NOT set on :root (per pitfall 2 in RESEARCH.md); acceptance criteria verify this with grep |
| T-87-03 | Tampering | Host app token inheritance | mitigate | Moving color tokens off :root means host components cannot inherit --rs-primary etc. unintentionally; acceptance criteria verify this; RESEARCH.md §4 confirms no known adopters consume var(--rs-*) in host templates |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan |
</threat_model>

<verification>
After Plan 02 completes, verify that the admin UI renders identically to before (light values are unchanged):

```bash
# 1. :root has no color tokens
awk '/^:root/,/^}/' /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css | grep -v '^[[:space:]]*//' | grep -c 'rs-neutral\|rs-bg\|rs-surface\|rs-primary\|rs-shadow\|--rs-focus-ring:'

# 2. :root retains invariant structural scalars
awk '/^:root/,/^}/' /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css | grep -c 'focus-ring-offset\|disabled-opacity'

# 3. Light default block exists with color-scheme
grep -n 'color-scheme: light' /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css

# 4. New tokens declared in light block
grep -n 'rs-focus-ring-color\|rs-overlay-veil\|rs-scrim\|rs-warning-hover' /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css

# 5. focus-ring references var() not raw rgba
grep 'rs-focus-ring:' /Users/jon/projects/rulestead/rulestead_admin/priv/static/css/rulestead_admin.css | grep -v 'offset'
```

Expected: command 1 returns 0, command 2 returns 2, command 3 shows at least 1 result, command 4 shows at least 4 results (3 new + warning-hover), command 5 shows `var(--rs-focus-ring-color)`.
</verification>

<success_criteria>
- :root contains only theme-invariant (non-color) tokens.
- All color/surface/border/text/shadow/focus tokens are declared on .rs-shell,[data-rulestead] with identical light values to before.
- Three new tokens (--rs-focus-ring-color, --rs-overlay-veil, --rs-scrim) declared in the light block with correct light values.
- --rs-warning-hover added to the warning family.
- --rs-focus-ring updated to reference var(--rs-focus-ring-color).
- color-scheme NOT on :root; color-scheme:light on .rs-shell.
- No component rules modified; visual rendering identical to before this plan.
</success_criteria>

<output>
Create `.planning/phases/87-token-theme-foundation/87-02-SUMMARY.md` when done.
</output>
