---
phase: "123"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/123-dx-closeout-proof-0-plans/123-CI-CD-CLOSEOUT.md
  - .planning/phases/119-baseline-expert-audit-0-plans/119-CI-CD-AUDIT.md
autonomous: true
requirements:
  - CIDX-10

must_haves:
  truths:
    - "123-CI-CD-CLOSEOUT.md exists and contains all seven CIDX-10 required fields as section headers"
    - "Every metric line in 123-CI-CD-CLOSEOUT.md carries a [VERIFIED], [CITED], or [ASSUMED] evidence tag"
    - "p95 is recorded as unavailable from current sample with the verbatim reason from 119-CI-CD-AUDIT.md:109"
    - "Cache hit rate is recorded qualitatively (exact-hit vs miss/partial) — no synthesized percentage"
    - "Rollback notes are per-decision git-revert-granular with revert handle + trust boundary + footguns"
    - "119-CI-CD-AUDIT.md:213 fast-contributor-loop row names cd rulestead && mix ci as the canonical alias"
  artifacts:
    - path: ".planning/phases/123-dx-closeout-proof-0-plans/123-CI-CD-CLOSEOUT.md"
      provides: "CIDX-10 milestone closeout ledger (milestone counterpart to 119-CI-CD-AUDIT.md)"
      contains: "PR wall-clock, p95, cache hit rate, top slow tests, flake notes, residual risks, rollback notes"
    - path: ".planning/phases/119-baseline-expert-audit-0-plans/119-CI-CD-AUDIT.md"
      provides: "Rerun catalog with reconciled fast-loop canonical name"
      contains: "cd rulestead && mix ci"
  key_links:
    - from: "123-CI-CD-CLOSEOUT.md"
      to: "121-MEASUREMENT.md:136-154"
      via: "[CITED] tag on every before/after metric"
      pattern: "\\[CITED: 121-MEASUREMENT\\.md"
    - from: "123-CI-CD-CLOSEOUT.md"
      to: "119-CI-CD-AUDIT.md:109"
      via: "[CITED] tag on p95-unavailable statement"
      pattern: "unavailable from current sample"
---

<objective>
Author the `123-CI-CD-CLOSEOUT.md` milestone ledger (D-01..D-06) and reconcile the rerun-catalog fast-loop row in `119-CI-CD-AUDIT.md:213` (D-09).

Purpose: Establish the audit-trail counterpart to `119-CI-CD-AUDIT.md` so a maintainer can diff baseline against closeout; correct the catalog so `mix ci` is the single named fast-loop entry point.

Output: `123-CI-CD-CLOSEOUT.md` (new file, seven CIDX-10 sections, all metrics tagged), `119-CI-CD-AUDIT.md` (one-line edit at :213).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-dx-closeout-proof-0-plans/123-CONTEXT.md
@.planning/phases/123-dx-closeout-proof-0-plans/123-RESEARCH.md

<interfaces>
<!-- Key line/section references the executor must treat as authoritative. -->
<!-- Read these files before writing 123-CI-CD-CLOSEOUT.md. -->

From .planning/phases/119-baseline-expert-audit-0-plans/119-CI-CD-AUDIT.md:
- Evidence convention (lines 7-12): [VERIFIED: path-or-command] | [CITED: url] | [ASSUMED: reason]
- Critical-path sample (lines 103-107): three representative runs with wall-clock / longest-job / branch
- p95-unavailable statement (line 109): "The sample is enough to identify the current critical path,
  but not enough to claim a defensible p95 across event types and branch classes, so: p95 target
  unavailable from current sample. [VERIFIED: gh run list ...]"
- Rerun catalog (lines 211-224): table of Surface / Exact local rerun command / Boundary protected
- Fast-loop row (line 213): "| Fast contributor loop | `bash scripts/ci/contributor.sh` | ..."
  — D-09 reconciliation target: change to `cd rulestead && mix ci` (alias for `bash scripts/ci/contributor.sh`)
- Rollback guardrails model (lines 284-309): per-guardrail list; model the closeout rollback section on this shape

From .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-MEASUREMENT.md:
- Methodology statement (line 12): "Measurements use the EXACT locked Phase 119 commands from
  119-CI-CD-AUDIT.md:153-154 for direct comparability."
- Reversible-by-design (line 13): env var `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE` (tag: `:published_hex_smoke`)
- Before/after table (lines 136-154): wall-clock ~42s → ~4.6s default (-37s / -88%); "with dominant" ~22s
- Relocation framing (line 154): "The speedup is relocation (proof still runs on `guarded_rollout_foundations`
  scope via `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1`) — not a deletion or hidden cost."
- FUT-01 rejection (line 176): "Deferred to FUT-01; revisit only if the suite grows materially…"
- Phase 123 Feed summary (lines 222-229): decision / outcome / Phase 123 Feed summary table

From scripts/ci/report_cache_hit.sh:
- Emits qualitative exact-hit vs miss/partial signal; GitHub Actions surfaces no numeric rate.
  Use this as the cache hit rate methodology citation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 123-CI-CD-CLOSEOUT.md (D-01..D-06)</name>
  <files>.planning/phases/123-dx-closeout-proof-0-plans/123-CI-CD-CLOSEOUT.md</files>
  <read_first>
    - .planning/phases/123-dx-closeout-proof-0-plans/123-CONTEXT.md (decisions D-01..D-06 in full)
    - .planning/phases/119-baseline-expert-audit-0-plans/119-CI-CD-AUDIT.md (lines 1-12, 103-109, 211-224, 284-309, 332-336 — evidence convention, critical-path table, p95-unavailable statement, rerun catalog, rollback guardrails, Phase 123 handoff)
    - .planning/phases/121-mix-exunit-performance-test-value-cleanup-0-plans/121-MEASUREMENT.md (lines 12-17, 136-154, 176, 222-229 — methodology, before/after table, relocation framing, FUT-01 rejection, Phase 123 Feed)
    - scripts/ci/report_cache_hit.sh (qualitative cache-hit provenance)
    - .planning/phases/122-browser-demo-integration-determinism-0-plans/122-VERIFICATION.md (lines 89-91 — honest-non-execution precedent)
  </read_first>
  <action>
    Create `.planning/phases/123-dx-closeout-proof-0-plans/123-CI-CD-CLOSEOUT.md` as the milestone counterpart to `119-CI-CD-AUDIT.md`.

    Document header: "Phase 123 CI/CD Closeout — v1.18 CI/CD Reliability Milestone" with produced date, purpose (milestone counterpart to `119-CI-CD-AUDIT.md`; diff baseline against closeout), and reference to `121-MEASUREMENT.md` as the source of committed before/after deltas.

    Mirror the 119 audit evidence convention (D-02): tag every metric line with `[VERIFIED: path]`, `[CITED: path:lines]`, or `[ASSUMED: reason]`. Zero untagged metric rows.

    Structure as a field-by-field CIDX-10 ledger (D-02) with one section per field in this order:

    1. "## PR Wall-Clock (Before/After)" — cite `121-MEASUREMENT.md:136-154` for the committed deltas. Use Oban-grade rigor (D-04): state both endpoints (~42s baseline / ~4.6s default post-121), measurement corpus (18 schedulers, local machine, 4 runs, same locked Phase 119 commands per `121-MEASUREMENT.md:12`), factor delta (-37s / -88%), and methodology caveat (hex.pm latency variance per `121-MEASUREMENT.md:150`). Frame as relocation-not-deletion (D-04): the dominant test moved to the `guarded_rollout_foundations` scope; proof still runs on opt-in via `RULESTEAD_RUN_PUBLISHED_HEX_SMOKE=1` (per `121-MEASUREMENT.md:154`). Tag all metric rows `[CITED: 121-MEASUREMENT.md:136-154]`.

    2. "## p95 Target" — record as "unavailable from current sample" with the verbatim reason from `119-CI-CD-AUDIT.md:109`: the 20-run sample spans pull_request, push, and workflow_dispatch runs across dependabot, main, release branch, and feature branch contexts — insufficient for a defensible p95 across event types and branch classes (D-05). Show the verified representative critical-path wall-clocks instead (lines 103-107: 5m18s, 5m04s, 4m46s). Tag `[CITED: 119-CI-CD-AUDIT.md:109]` and `[CITED: 119-CI-CD-AUDIT.md:103-107]`. Do NOT synthesize a percentage.

    3. "## Cache Hit Rate" — record qualitatively (D-05): exact-hit vs miss/partial as `scripts/ci/report_cache_hit.sh` emits it. GitHub Actions surfaces no numeric rate; do NOT synthesize a percentage. State the post-Phase-120 posture: cross-lane `${{ runner.os }}-mix-` restore-key fallback removed (D-05/Phase 120); keys are now matrix-scoped per lane. Tag `[CITED: scripts/ci/report_cache_hit.sh]` and `[CITED: .planning/phases/120-workflow-topology-cache-hygiene-0-plans/]`.

    4. "## Top Slow Tests" — cite `121-MEASUREMENT.md:136-148` and the Phase 123 Feed table at `121-MEASUREMENT.md:222-229`. State: default lane slowest is now `Rulestead.Runtime.ClusterRefreshTest` at 309.6ms (was `VerifyReleasePublishTest` at ~27.95s). Next-slowest module 303ms (`Rulestead.Promotion.ApplyTest`). The dominant test is preserved and reachable on the opt-in lane at ~17090ms depending on hex.pm latency. Tag `[CITED: 121-MEASUREMENT.md:136-154]`.

    5. "## Flake Notes" — cite Phase 122 determinism work: Playwright `trace: on-first-retry` + `retries: 0` mismatch fixed at root cause (retain-on-failure, retries unchanged). No remaining known flakes. Compile-connected xref cycle length 47 noted as architectural evidence only (`121-MEASUREMENT.md:210-216`); not a flake. Tag `[CITED: .planning/phases/122-browser-demo-integration-determinism-0-plans/122-VERIFICATION.md]`.

    6. "## Residual Risks" — record the following items from the milestone scope:
       - D-14 anti-drift guard for the triage table: included in Wave 2 (plan 123-02) via `release_contract_test.exs` extension; if deferred, record as residual risk here.
       - FUT-01 partitioning: deferred per `121-MEASUREMENT.md:176`; revisit if suite grows materially.
       - Compile-connected xref cycle (length 47, centered on `lib/rulestead.ex`): architectural evidence, not refactored in v1.18.
       - Phase 122 honest-non-execution: mounted/openfeature companion proofs and DB-backed product suites not re-run for this docs diff (D-16); `publish-hex` / `verify-published-release` / `gh api` branch-protection writes not executed (D-17).
       Tag all claims with their source citation.

    7. "## Rollback Notes" — model on `119-CI-CD-AUDIT.md:284-309` but per-decision git-revert-granular (D-06). One row per Phase 120/121/122 change with: (a) what changed, (b) exact revert handle (commit/PR reference; for cache changes, state that a cache-key bump is required to invalidate poisoned entries), (c) trust boundary that must not be silently weakened on rollback (release_gate, protected Hex publish, linked-version order). Call out footguns:
       - Cache fallback removal (Phase 120): a cache change reverted without a key bump leaves poisoned OTP-incompatible `_build` entries — always bump the key prefix after reverting cache scope changes.
       - `openfeature-companion` wired into `release_gate.needs` (Phase 120): reverting this removes the companion from the aggregate gate; confirm required-check posture after any rollback.
       - Path-filter rollback (Phase 120): reverting workflow path filters can leave a required check stuck pending (per `119-CI-CD-AUDIT.md:88,302`).
       - Dominant-test relocation (Phase 121): revert = remove `@tag :published_hex_smoke` from the test and remove the exclude from `test_helper.exs` (reversible-by-design per `121-MEASUREMENT.md:13`). No proof loss on rollback.
       - FUT-01 partitioning rejection (Phase 121): recorded as a no-action decision (`121-MEASUREMENT.md:176`); no artifact to revert.
       - Playwright config fix (Phase 122): revert = restore `trace: on-first-retry` in `playwright.config.ts` (but traces will again not be produced with `retries: 0`; the fix is safe to preserve).
       Tag all rollback handles `[CITED: .planning/phases/NNN-*/NNN-*-SUMMARY.md]` or `[CITED: 121-MEASUREMENT.md:...]` as appropriate.

    NEVER fabricate a numeric percentage not present in the cited source documents.
  </action>
  <verify>
    <automated>grep -E "wall.clock|p95|cache hit rate|slow test|flake|residual risk|rollback" .planning/phases/123-dx-closeout-proof-0-plans/123-CI-CD-CLOSEOUT.md | wc -l</automated>
    Result must be >= 7 (one match per CIDX-10 field).
    <automated>grep -c "\[VERIFIED\]\|\[CITED\]\|\[ASSUMED\]" .planning/phases/123-dx-closeout-proof-0-plans/123-CI-CD-CLOSEOUT.md</automated>
    Result must be >= 10 (every metric line is tagged; zero untagged metric rows).
    <automated>grep "unavailable from current sample" .planning/phases/123-dx-closeout-proof-0-plans/123-CI-CD-CLOSEOUT.md</automated>
    Must match (p95 honest-gap statement present verbatim per D-05).
    <automated>grep "relocation" .planning/phases/123-dx-closeout-proof-0-plans/123-CI-CD-CLOSEOUT.md</automated>
    Must match (relocation-not-deletion framing per D-04).
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/123-dx-closeout-proof-0-plans/123-CI-CD-CLOSEOUT.md` exists.
    - Contains exactly seven top-level sections covering: PR wall-clock, p95, cache hit rate, top slow tests, flake notes, residual risks, rollback notes.
    - Every metric row carries a `[VERIFIED]`, `[CITED]`, or `[ASSUMED]` evidence tag; grep for untagged numeric claims returns zero.
    - p95 section contains the phrase "unavailable from current sample" and cites `119-CI-CD-AUDIT.md:109`.
    - Cache hit rate section contains no synthesized percentage; cites `scripts/ci/report_cache_hit.sh` and states qualitative (exact-hit vs miss/partial) posture.
    - PR wall-clock section states both endpoints (baseline ~42s, default post-121 ~4.6s), corpus (18 schedulers, 4 runs), factor delta (-37s / ~88%), and hex.pm latency caveat.
    - Rollback notes section contains one entry per Phase 120/121/122 change with a revert handle and footgun callout.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reconcile rerun-catalog fast-loop row (D-09)</name>
  <files>.planning/phases/119-baseline-expert-audit-0-plans/119-CI-CD-AUDIT.md</files>
  <read_first>
    - .planning/phases/119-baseline-expert-audit-0-plans/119-CI-CD-AUDIT.md (lines 211-224 — full rerun catalog; line 213 is the single edit target)
    - rulestead/mix.exs (line 172 — `ci: ["cmd bash ../scripts/ci/contributor.sh"]` confirms alias; verify before editing the catalog)
  </read_first>
  <action>
    Edit `119-CI-CD-AUDIT.md` line 213 (and only line 213) to change the fast-contributor-loop rerun command from `bash scripts/ci/contributor.sh` to `cd rulestead && mix ci` (alias for `bash scripts/ci/contributor.sh`) per D-09.

    Before edit, the row reads:
      `| Fast contributor loop | \`bash scripts/ci/contributor.sh\` | Common pre-push checks without slow proof scopes |`

    After edit, the row must read:
      `| Fast contributor loop | \`cd rulestead && mix ci\` (alias for \`bash scripts/ci/contributor.sh\`) | Common pre-push checks without slow proof scopes |`

    Do NOT modify any other line in the rerun catalog or in the audit document. This is a one-line targeted edit. The audit document is not in the Hex package and is not test-pinned — safe to edit.
  </action>
  <verify>
    <automated>grep "mix ci.*alias for.*contributor.sh" .planning/phases/119-baseline-expert-audit-0-plans/119-CI-CD-AUDIT.md</automated>
    Must match (canonical alias named in catalog per D-09).
    <automated>grep "bash scripts/ci/contributor.sh" .planning/phases/119-baseline-expert-audit-0-plans/119-CI-CD-AUDIT.md | grep -v "alias for"</automated>
    Must return zero lines (bare reference to `bash scripts/ci/contributor.sh` without alias annotation is gone from the fast-loop row; other rows like the Full local gate row are separate and may retain their own wording).
  </verify>
  <acceptance_criteria>
    - `119-CI-CD-AUDIT.md:213` (fast contributor loop row) now names `cd rulestead && mix ci` as the rerun command with the alias annotation.
    - No other line in the rerun catalog (lines 211-224) was modified.
    - `rulestead/mix.exs:172` still reads `ci: ["cmd bash ../scripts/ci/contributor.sh"]` confirming zero behavioral change (D-07).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs → repo | Edits to planning artifacts and `119-CI-CD-AUDIT.md` do not touch Hex package code, workflow YAML, or release trust surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Tampering | 123-CI-CD-CLOSEOUT.md (fabricated metrics) | mitigate | D-02 evidence-tag requirement: every metric line must carry [VERIFIED]/[CITED]/[ASSUMED]; a bare percentage without a citation source is an audit defect detectable by grep |
| T-123-02 | Repudiation | 119-CI-CD-AUDIT.md catalog edit at :213 | mitigate | One-line targeted edit; diff review confirms only the fast-loop row changed; the audit file is version-controlled |
| T-123-03 | Information Disclosure | rollback notes exposing secret values | accept | Rollback section references commit/PR handles and env var names only — no secret values; HEX_API_KEY is named as an identifier, not printed |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; all work is document authoring only |

**Release-trust boundary affirmed (D-16/D-17):** No workflow YAML, cache keys, action pins, permissions, or publish steps are touched in this wave. The rollback notes section actively guards against cache-key omission footguns and path-filter required-check traps rather than weakening them.
</threat_model>

<verification>
After both tasks complete:

1. `grep -E "wall.clock|p95|cache hit rate|slow test|flake|residual risk|rollback" .planning/phases/123-dx-closeout-proof-0-plans/123-CI-CD-CLOSEOUT.md | wc -l` — must be >= 7
2. `grep -c "\[VERIFIED\]\|\[CITED\]\|\[ASSUMED\]" .planning/phases/123-dx-closeout-proof-0-plans/123-CI-CD-CLOSEOUT.md` — must be >= 10
3. `grep "unavailable from current sample" .planning/phases/123-dx-closeout-proof-0-plans/123-CI-CD-CLOSEOUT.md` — must match
4. `grep "mix ci.*alias for.*contributor.sh" .planning/phases/119-baseline-expert-audit-0-plans/119-CI-CD-AUDIT.md` — must match
</verification>

<success_criteria>
- `123-CI-CD-CLOSEOUT.md` exists with all seven CIDX-10 field sections, every metric tagged, p95 honest-gapped, cache hit rate qualitative, rollback notes per-decision.
- `119-CI-CD-AUDIT.md:213` names `cd rulestead && mix ci` as the fast-loop canonical alias.
- No metrics fabricated; no protected MAINTAINING.md sections touched; no behavioral changes to any script or workflow.
</success_criteria>

<output>
Create `.planning/phases/123-dx-closeout-proof-0-plans/123-01-SUMMARY.md` when done.
</output>
