---
phase: 03-context-rules-deterministic-bucketing-pure-evaluator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/ruleset/condition.ex
  - rulestead/lib/rulestead/ruleset/rule.ex
  - rulestead/lib/rulestead/ruleset/rollout.ex
  - rulestead/lib/rulestead/ruleset/variant.ex
  - rulestead/test/support/store_fixtures.ex
  - rulestead/test/rulestead/ruleset_validation_test.exs
autonomous: true
requirements:
  - RULE-01
  - RULE-02
  - RULE-03
  - RULE-04
must_haves:
  truths:
    - "Invalid rule authoring payloads are rejected before runtime evaluation."
    - "Conditions use stable dot-path attributes and operator-specific payload validation."
    - "Variant weights and rollout bucketing metadata are validated in the authoring shape the evaluator will consume."
  artifacts:
    - path: "rulestead/lib/rulestead/ruleset/condition.ex"
      provides: "Operator-specific condition validation and normalization"
    - path: "rulestead/lib/rulestead/ruleset/rule.ex"
      provides: "Rule-level segment, rollout, and variant validation"
    - path: "rulestead/test/rulestead/ruleset_validation_test.exs"
      provides: "Executable proof that Phase 3 authoring semantics reject invalid payloads"
  key_links:
    - from: "rulestead/lib/rulestead/ruleset/rule.ex"
      to: "rulestead/lib/rulestead/ruleset/condition.ex"
      via: "cast_embed and rule-shape validation"
      pattern: "cast_embed\\(:conditions"
    - from: "rulestead/test/support/store_fixtures.ex"
      to: "rulestead/test/rulestead/ruleset_validation_test.exs"
      via: "realistic authored ruleset payloads"
      pattern: "valid_ruleset_attrs|invalid_variant_weight_ruleset_attrs"
---

<objective>
Tighten the persisted ruleset document so Phase 3 runtime code can evaluate a deterministic, validated authoring shape instead of loose Phase 2 maps.

Purpose: Move as much invalid-state rejection as possible into changesets/embeds, leaving the evaluator to handle only valid or explicitly malformed persisted data.
Output: Stronger embedded rule validators plus focused contract tests and fixtures.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md
@.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-PATTERNS.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-CONTEXT.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-04-SUMMARY.md

<interfaces>
From `rulestead/lib/rulestead/ruleset/condition.ex`:
```elixir
@operators [:equals, :in, :not_in, :gt, :lt, :gte, :lte, :regex, :exists]
field(:attribute, :string)
field(:operator, Ecto.Enum, values: @operators)
field(:value, :map, default: %{})
```

From `rulestead/lib/rulestead/ruleset/rollout.ex`:
```elixir
@bucket_by_values [:subject, :account, :tenant, :session]
field(:bucket_by, Ecto.Enum, values: @bucket_by_values)
field(:percentage, :integer)
field(:salt, :string)
```

From `rulestead/test/support/store_fixtures.ex`:
```elixir
%{
  key: "force-enabled",
  strategy: :forced_value,
  value: %{value: true},
  conditions: [%{attribute: "tenant_key", operator: :equals, value: %{equals: "acme"}}]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten condition, rollout, variant, and rule validation semantics</name>
  <files>rulestead/lib/rulestead/ruleset/condition.ex, rulestead/lib/rulestead/ruleset/rule.ex, rulestead/lib/rulestead/ruleset/rollout.ex, rulestead/lib/rulestead/ruleset/variant.ex</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md, .planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-PATTERNS.md, rulestead/lib/rulestead/ruleset/condition.ex, rulestead/lib/rulestead/ruleset/rule.ex, rulestead/lib/rulestead/ruleset/rollout.ex, rulestead/lib/rulestead/ruleset/variant.ex</read_first>
  <behavior>
    - Test 1: `Condition.changeset/2` accepts dot-path attributes, validates operator-specific payload shapes, and rejects array indexing or wildcard paths.
    - Test 2: `Rule.changeset/2` enforces reusable audience/segment references, weight-sum validation, and rollout presence rules without loose coercion.
    - Test 3: `Rollout.changeset/2` keeps authored percentages at `0..100` while preserving closed `bucket_by` values `:subject | :account | :tenant | :session`.
  </behavior>
  <action>Update the existing embedded ruleset modules in place per D-11 through D-16, D-19, and D-22. In `condition.ex`, replace the loose `%{}` payload assumption with operator-specific validation: scalar payloads for `equals`/numeric comparisons, homogeneous lists for `in` and `not_in`, `%{pattern: binary, options: binary}` for `regex`, and no payload for `exists`. Validate dot-separated map paths only; reject array indexing, wildcards, and boolean-expression syntax per D-12. In `rule.ex`, preserve ordered rules and existing strategies, but validate segment references for `:segment_match`, keep variant weights summing to 100, and make rollout metadata explicit rather than runtime-magic. In `rollout.ex` and `variant.ex`, preserve the author-facing `0..100` contract, stable variant keys, and closed `bucket_by` enum; do not introduce Phase 4 cache/snapshot fields.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/ruleset_validation_test.exs</automated>
  </verify>
  <done>The persisted ruleset embed shape rejects invalid Phase 3 authoring states before runtime evaluation, while preserving the single-document ruleset boundary from Phase 2.</done>
  <acceptance_criteria>`rulestead/lib/rulestead/ruleset/condition.ex` contains validation branches for `:equals`, `:in`, `:not_in`, `:gt`, `:lt`, `:gte`, `:lte`, `:regex`, and `:exists`. `rulestead/lib/rulestead/ruleset/rollout.ex` still contains `@bucket_by_values [:subject, :account, :tenant, :session]`. `rulestead/lib/rulestead/ruleset/rule.ex` still validates variant weights to exactly `100` and segment-match audience references.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add authored-shape regression tests and fixtures for Phase 3 semantics</name>
  <files>rulestead/test/support/store_fixtures.ex, rulestead/test/rulestead/ruleset_validation_test.exs</files>
  <read_first>.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md, .planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-PATTERNS.md, rulestead/test/support/store_fixtures.ex, rulestead/test/support/store_contract_case.ex, rulestead/test/rulestead/ruleset_validation_test.exs, rulestead/lib/rulestead/ruleset/condition.ex, rulestead/lib/rulestead/ruleset/rule.ex, rulestead/lib/rulestead/ruleset/rollout.ex, rulestead/lib/rulestead/ruleset/variant.ex</read_first>
  <behavior>
    - Test 1: valid fixtures cover nested attribute paths, regex payload maps, and segment-match rules the evaluator will later consume.
    - Test 2: invalid fixtures prove malformed payloads fail at validation time rather than at runtime.
    - Test 3: contract fixtures remain compatible with the Phase 2 fake/store shared test posture.
  </behavior>
  <action>Expand `test/support/store_fixtures.ex` with Phase 3-authored examples that reflect the locked persisted syntax: nested dot-path conditions, regex payload maps, explicit rollout salts, and segment references. Add `test/rulestead/ruleset_validation_test.exs` as the focused regression file for invalid payload shape, invalid regex options/patterns, unsupported path syntax, and variant/percentage guardrails. Reuse `Ruleset.changeset/2` and the embedded schema changesets directly; do not create fake-only validators or Phase 4 compile-time helpers in this plan.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/ruleset_validation_test.exs</automated>
  </verify>
  <done>Phase 3 authoring semantics are enforced by executable tests and reusable fixtures that later evaluator/property tests can build on.</done>
  <acceptance_criteria>`rulestead/test/support/store_fixtures.ex` contains at least one nested attribute path string with a dot, one regex payload map with `pattern`, and one rule using `strategy: :segment_match`. `rulestead/test/rulestead/ruleset_validation_test.exs` asserts that unsupported path syntax and malformed operator payloads are invalid.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored ruleset input -> Ecto changesets | Untrusted admin/store payloads become persisted runtime rules |
| persisted embeds -> future evaluator | Validated authoring shape becomes hot-path evaluation input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | `Rulestead.Ruleset.Condition` | mitigate | Validate operator-specific payload shapes and forbid unsupported path syntax per D-11..D-16 so runtime logic does not interpret ambiguous data. |
| T-03-05 | D | `Rulestead.Ruleset.Rule` | mitigate | Reject invalid weight sums, missing rollout configs, and malformed regex payloads before runtime evaluation. |
| T-03-06 | E | `Rulestead.Ruleset.Rollout` | mitigate | Keep `bucket_by` on a closed enum and explicit segment references so authored data cannot trigger hidden fallback identities. |
</threat_model>

<verification>
Run `cd rulestead && mix test test/rulestead/ruleset_validation_test.exs`.
</verification>

<success_criteria>
The Phase 2 ruleset document remains the single evaluator input boundary, but invalid Phase 3 condition/variant/rollout states are rejected by changesets before the pure runtime executes them.
</success_criteria>

<output>
After completion, create `.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-02-SUMMARY.md`
</output>
