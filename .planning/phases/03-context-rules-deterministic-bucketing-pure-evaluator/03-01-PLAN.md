---
phase: 03-context-rules-deterministic-bucketing-pure-evaluator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/context.ex
  - rulestead/lib/rulestead/result.ex
  - rulestead/lib/rulestead/evaluation_error.ex
  - rulestead/test/rulestead/context_test.exs
  - rulestead/test/rulestead/result_test.exs
autonomous: true
requirements:
  - CTX-01
  - EVAL-04
must_haves:
  truths:
    - "Callers can normalize map/keyword input into one authoritative `%Rulestead.Context{}` shape."
    - "Evaluation outcomes are pattern-matchable as a stable `%Rulestead.Result{}` with compact reason atoms and optional `debug_trace`."
    - "Strict-mode evaluator failures can be constructed through `Rulestead.EvaluationError` without ad hoc `%Rulestead.Error{}` literals."
  artifacts:
    - path: "rulestead/lib/rulestead/context.ex"
      provides: "Canonical runtime context struct and constructor"
    - path: "rulestead/lib/rulestead/result.ex"
      provides: "Stable evaluation result struct and constructor"
    - path: "rulestead/lib/rulestead/evaluation_error.ex"
      provides: "Typed evaluation-domain error constructors used by Phase 3 runtime paths"
  key_links:
    - from: "rulestead/lib/rulestead/context.ex"
      to: "rulestead/test/rulestead/context_test.exs"
      via: "normalization and alias handling coverage"
      pattern: "Context\\.new|Context\\.normalize"
    - from: "rulestead/lib/rulestead/result.ex"
      to: "rulestead/test/rulestead/result_test.exs"
      via: "stable field defaults and debug trace boundary"
      pattern: "Result\\.new|debug_trace"
---

<objective>
Establish the public runtime contracts the rest of Phase 3 builds against: a normalized `%Rulestead.Context{}` and a stable `%Rulestead.Result{}`.

Purpose: Lock the evaluator input/output shape before rule walking and bucketing land, so later plans can implement pure runtime logic without re-deciding public structs.
Output: Context/result modules, evaluation error constructors, and focused contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md
@.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-PATTERNS.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-CONTEXT.md

<interfaces>
From `rulestead/lib/rulestead.ex`:
```elixir
@spec evaluate(String.t() | atom(), term(), keyword()) :: {:error, Error.t()}
def evaluate(flag_key, context, opts \\ [])
```

From `rulestead/lib/rulestead/evaluation_error.ex`:
```elixir
@spec new(Error.type(), String.t(), keyword()) :: Error.t()
def new(type, message, opts \\ [])
```

Pattern guidance from `03-PATTERNS.md`:
- `Context.new/1` should immediately normalize keyword/map input into one canonical struct.
- `%Rulestead.Result{}` should expose a closed field set with stable defaults and optional `debug_trace`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the canonical runtime context contract</name>
  <files>rulestead/lib/rulestead/context.ex, rulestead/test/rulestead/context_test.exs</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md, .planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-PATTERNS.md, rulestead/lib/rulestead/evaluation_error.ex, rulestead/lib/rulestead/context.ex, rulestead/test/rulestead/context_test.exs</read_first>
  <behavior>
    - Test 1: `Context.new/1` accepts map and keyword input and normalizes to `%Rulestead.Context{}` with top-level `actor`, `targeting_key`, `tenant_key`, `environment`, `attributes`, `request_id`, `session_id`, and `strict?`.
    - Test 2: `Context.new(subject: ...)` normalizes immediately to `actor` and does not persist a public `subject` field.
    - Test 3: when `targeting_key` is absent but `actor.key` exists, the builder fills `targeting_key`; evaluator code will treat the normalized struct as authoritative.
  </behavior>
  <action>Create `rulestead/lib/rulestead/context.ex` as the Phase 3 canonical runtime input per D-01 through D-05. Use a typed `defstruct`/`@type t` plus `new/1` and `normalize/1` helpers following the public-struct normalization pattern from `Rulestead.Error`, but do not make it an exception. Canonical public field names must be exactly `actor`, `targeting_key`, `tenant_key`, `environment`, `attributes`, `request_id`, `session_id`, and `strict?`; do not expose `subject` as a struct field. Accept `subject` only as a temporary input alias per D-05, normalize it immediately to `actor`, and support defaulting `targeting_key` from `actor.key` per D-03. Keep scalar identity fields top-level per D-04 and treat missing optional fields as explicit `nil`/empty map defaults instead of hidden process state.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/context_test.exs</automated>
  </verify>
  <done>`Rulestead.Context` exists as a public struct module, `Context.new/1` and `Context.normalize/1` return the canonical shape, and tests prove the `subject` alias is normalized away.</done>
  <acceptance_criteria>`rulestead/lib/rulestead/context.ex` contains `defmodule Rulestead.Context`, `defstruct [` with `:actor` and `:targeting_key`, and a `def new(` function. `rulestead/test/rulestead/context_test.exs` contains assertions for `subject:` normalization and `actor.key` -> `targeting_key` defaulting.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the result struct and evaluation error constructors</name>
  <files>rulestead/lib/rulestead/result.ex, rulestead/lib/rulestead/evaluation_error.ex, rulestead/test/rulestead/result_test.exs</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md, .planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-PATTERNS.md, rulestead/lib/rulestead/error.ex, rulestead/lib/rulestead/evaluation_error.ex, rulestead/lib/rulestead/result.ex, rulestead/test/rulestead/result_test.exs</read_first>
  <behavior>
    - Test 1: `Result.new/1` produces a stable `%Rulestead.Result{}` with `value`, `enabled?`, `variant`, `reason`, `matched_rule`, `flag_key`, `flag_version`, `cache_age_ms`, and `debug_trace`.
    - Test 2: result reasons stay compact atoms such as `:rule_match`, `:default`, `:targeting_key_missing`, and `:error`.
    - Test 3: `Rulestead.EvaluationError` exposes constructors for strict-mode and malformed-runtime-data failures used later by the evaluator.
  </behavior>
  <action>Create `rulestead/lib/rulestead/result.ex` and extend `rulestead/lib/rulestead/evaluation_error.ex` per D-26 through D-30 and D-31 through D-32. `Result.new/1` must normalize input into a stable struct with the exact Phase 3 field set from the roadmap, but keep `debug_trace` optional and limited to factual evaluation data only; do not add Phase 4 diagnostics such as snapshot version or cache age semantics beyond the placeholder field. Add compact `EvaluationError` helpers for missing targeting identity, strict fail-closed behavior, invalid value/type projections, and malformed persisted ruleset/runtime data so later plans call constructors instead of inlining `%Rulestead.Error{}` values.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/result_test.exs</automated>
  </verify>
  <done>`Rulestead.Result` is available as a stable public struct with constructor coverage, and `Rulestead.EvaluationError` exposes the typed constructors the evaluator plan will call.</done>
  <acceptance_criteria>`rulestead/lib/rulestead/result.ex` contains `defmodule Rulestead.Result`, `defstruct`, and `def new(`. `rulestead/lib/rulestead/evaluation_error.ex` contains constructor functions for missing-targeting and malformed-runtime-data paths. `rulestead/test/rulestead/result_test.exs` asserts exact `reason` atoms and `debug_trace` normalization.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> `Rulestead.Context.new/1` | Untrusted runtime input becomes evaluator context |
| evaluator internals -> `%Rulestead.Result{}` | Runtime facts become public API output consumed by apps/tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `Rulestead.Context` | mitigate | Normalize aliases and top-level keys once per D-01..D-05 so later code does not read ambiguous fields like `subject` and `actor` interchangeably. |
| T-03-02 | I | `Rulestead.Result` | mitigate | Keep `debug_trace` factual and exclude Phase 4 diagnostics or raw payload leakage per D-27..D-30. |
| T-03-03 | D | `Rulestead.EvaluationError` | mitigate | Route evaluator failures through typed constructors so strict-mode paths fail closed instead of crashing on ad hoc term handling. |
</threat_model>

<verification>
Run `cd rulestead && mix test test/rulestead/context_test.exs test/rulestead/result_test.exs`.
</verification>

<success_criteria>
`Rulestead.Context` and `Rulestead.Result` exist as stable public contracts, `subject` is normalized away, and evaluator-domain errors can be constructed through dedicated helpers instead of inline literals.
</success_criteria>

<output>
After completion, create `.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-01-SUMMARY.md`
</output>
