---
phase: 03-context-rules-deterministic-bucketing-pure-evaluator
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - rulestead/lib/rulestead/bucket.ex
  - rulestead/lib/rulestead/evaluator.ex
  - rulestead/lib/rulestead/explainer.ex
  - rulestead/lib/rulestead.ex
  - rulestead/test/rulestead/evaluator_test.exs
  - rulestead/test/rulestead_test.exs
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-05
  - EVAL-07
  - EVAL-08
  - EVAL-09
must_haves:
  truths:
    - "A pure evaluator can resolve value, enabled state, variant, and explain output from an explicit in-memory authored flag payload carrying the active ruleset using first-match-wins semantics."
    - "Missing sticky identity behaves deterministically: non-applicable in normal mode, typed error in strict mode."
    - "Permissive sticky-identity misses emit one sanitized telemetry warning without introducing the Phase 4 telemetry wrapper/catalog."
    - "`Rulestead.explain/2` describes the same evaluation facts recorded in `Result.debug_trace`."
  artifacts:
    - path: "rulestead/lib/rulestead/bucket.ex"
      provides: "Deterministic 0..9999 bucketing with layered salts and explicit namespaces"
    - path: "rulestead/lib/rulestead/evaluator.ex"
      provides: "Pure first-match-wins evaluator over the authored in-memory flag payload, returning `{:ok, %Rulestead.Result{}} | {:error, %Rulestead.Error{}}`"
    - path: "rulestead/lib/rulestead.ex"
      provides: "Thin public facade for `evaluate/3`, `enabled?/2`, `get_value/3`, `get_variant/2`, `evaluate!/3`, and `explain/2` over the authored in-memory flag payload contract"
  key_links:
    - from: "rulestead/lib/rulestead.ex"
      to: "rulestead/lib/rulestead/evaluator.ex"
      via: "thin public delegation"
      pattern: "Rulestead\\.Evaluator"
    - from: "rulestead/lib/rulestead/evaluator.ex"
      to: "rulestead/lib/rulestead/bucket.ex"
      via: "rollout inclusion and variant assignment"
      pattern: "Rulestead\\.Bucket"
    - from: "rulestead/lib/rulestead/explainer.ex"
      to: "rulestead/lib/rulestead/result.ex"
      via: "render from factual debug trace"
      pattern: "debug_trace"
---

<objective>
Implement the pure evaluator, deterministic bucketing engine, human explainer, and root facade projections without crossing into Phase 4 snapshot/runtime lookup work.

Purpose: Deliver the runtime heart of the library now, while keeping caching, runtime lookup, and general telemetry/host-framework extraction deferred to later phases.
Output: Bucket/evaluator/explainer modules, wired public API, and focused behavior tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md
@.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-PATTERNS.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-04-SUMMARY.md

<interfaces>
From `rulestead/lib/rulestead.ex`:
```elixir
@spec evaluate(flag_payload, context, keyword()) :: {:ok, Rulestead.Result.t()} | {:error, Error.t()}
def evaluate(flag_payload, context, opts \\ [])
```

From `rulestead/test/support/store_contract_case.ex`:
```elixir
assert {:ok, payload} = Rulestead.fetch_flag("checkout-redesign", "test")
assert payload.active_ruleset.version == 1
```

Phase 3 contract guidance:
- The first argument to `evaluate/3`, `enabled?/2`, `get_value/3`, `get_variant/2`, and `explain/2` is the existing authored flag payload / active ruleset document already loaded in memory.
- Do not add hidden store lookup, `flag_key` fetch semantics, extra lookup opts, or alternate arities in Phase 3.
- Keep the public root module thin; put orchestration in `Rulestead.Evaluator` and prose rendering in `Rulestead.Explainer`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic bucketing and first-match evaluator orchestration</name>
  <files>rulestead/lib/rulestead/bucket.ex, rulestead/lib/rulestead/evaluator.ex, rulestead/test/rulestead/evaluator_test.exs</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md, .planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-PATTERNS.md, rulestead/lib/rulestead/result.ex, rulestead/lib/rulestead/context.ex, rulestead/lib/rulestead/evaluation_error.ex, rulestead/lib/rulestead/ruleset/rule.ex, rulestead/lib/rulestead/ruleset/condition.ex, rulestead/lib/rulestead/ruleset/rollout.ex, rulestead/lib/rulestead/ruleset/variant.ex, rulestead/lib/rulestead/bucket.ex, rulestead/lib/rulestead/evaluator.ex, rulestead/test/rulestead/evaluator_test.exs</read_first>
  <behavior>
    - Test 1: the evaluator walks rules in order and returns the first matching result, otherwise default.
    - Test 2: rollout inclusion and variant assignment use SHA-256, 10_000 buckets, layered salts, and explicit `bucket_by` identities without hidden fallback.
    - Test 3: missing sticky identity yields a non-applicable rule in normal mode and `{:error, %Rulestead.Error{type: :missing_targeting_key}}` in strict mode, while recording a factual warning/debug marker the public API can emit later in permissive mode.
  </behavior>
  <action>Create `rulestead/lib/rulestead/bucket.ex` and `rulestead/lib/rulestead/evaluator.ex` per D-06 through D-25 and D-24a. `Bucket` must hash canonical iodata with `hash_version: 1`, namespaces `rollout` and `variant`, and `(flag_key, rule_key, effective_salt, resolved targeting value)` using `:sha256`; convert the first 8 digest bytes into `0..9999` buckets per D-17 through D-21. `Evaluator` must stay pure and consume the existing in-memory authored flag payload / active ruleset document as its first argument, not a `flag_key`, store adapter, or DB lookup. Implement first-match-wins rule walking, nested map-path attribute resolution, same-type comparison semantics, segment hits, rollout inclusion, variant basis-point selection, default fallback, and strict/non-strict sticky-identity handling exactly per D-14 through D-25. When a permissive sticky rule cannot resolve its required identity, return a deterministic non-match plus structured warning/debug facts that the wrapper can emit; in strict mode return `{:error, %Rulestead.Error{type: :missing_targeting_key}}` immediately from the non-bang evaluator path. Do not call `:telemetry` from the evaluator itself. Keep the main orchestration as small helper-driven functions returning `{:ok, %Rulestead.Result{}} | {:error, %Rulestead.Error{}}`; do not add Phase 4 cache lookups, ETS, Plug/conn handling, process-dictionary behavior, or hidden runtime lookup.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/evaluator_test.exs</automated>
  </verify>
  <done>The pure evaluator and bucket engine resolve authored rules deterministically from the explicit in-memory flag payload contract and return the typed strict-mode missing-targeting-key error without hidden lookup.</done>
  <acceptance_criteria>`rulestead/lib/rulestead/bucket.ex` contains `:sha256`, `10_000`, and both `"rollout"` and `"variant"` namespaces. `rulestead/lib/rulestead/evaluator.ex` consumes an authored flag payload / active ruleset document as its first input and contains code paths for `:rule_match`, `:default`, and `{:error, %Rulestead.Error{type: :missing_targeting_key}}`. `rulestead/test/rulestead/evaluator_test.exs` asserts first-match-wins ordering and strict/non-strict sticky identity behavior.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the root facade projections and human explainer to the shared result/debug facts</name>
  <files>rulestead/lib/rulestead/explainer.ex, rulestead/lib/rulestead.ex, rulestead/test/rulestead_test.exs, rulestead/test/rulestead/evaluator_test.exs</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md, rulestead/lib/rulestead.ex, rulestead/lib/rulestead/result.ex, rulestead/lib/rulestead/evaluator.ex, rulestead/lib/rulestead/explainer.ex, rulestead/test/rulestead_test.exs, rulestead/test/rulestead/evaluator_test.exs</read_first>
  <behavior>
    - Test 1: `enabled?/2`, `get_value/3`, `get_variant/2`, and `explain/2` all take the authored in-memory flag payload as their first argument and are thin projections over one canonical `evaluate/3` result path, accepting explicit `Rulestead.Context` input or raw map/keyword data normalized through `Context.new/1`.
    - Test 2: `enabled?/2`, `get_value/3`, `get_variant/2`, and `explain/2` return `{:ok, projection}` on successful evaluation and propagate `{:error, %Rulestead.Error{type: :missing_targeting_key}}` unchanged in strict mode rather than fail-closing.
    - Test 3: permissive sticky-identity misses emit exactly one direct sanitized telemetry warning event, and `explain/2` renders human-readable text from the same `debug_trace` facts without Phase 4 snapshot/cache diagnostics.
  </behavior>
  <action>Update `rulestead/lib/rulestead.ex` to replace the Phase 2 evaluator stub with the real Phase 3 facade while keeping the root module thin per D-06 through D-10, D-24a, and the pattern map. Add `enabled?/2`, `get_value/3`, `get_variant/2`, `evaluate!/3`, and `explain/2` heads that all delegate to `Rulestead.Evaluator` and then project from `%Rulestead.Result{}` instead of re-implementing semantics. The first argument for every public evaluation helper in this plan is the authored in-memory flag payload / active ruleset document; do not add `flag_key` lookup, store fetches, lookup opts, or alternate arities. Normalize raw map/keyword inputs through `Rulestead.Context.new/1`; do not accept `Plug.Conn` in Phase 3 because host-app builders ship in Phase 5. Non-bang helper APIs must return `{:ok, projection}` on success and propagate `{:error, %Rulestead.Error{type: :missing_targeting_key}}` unchanged for the strict-mode sticky-identity failure path; `evaluate!/3` raises the same typed error. Emit one direct `:telemetry.execute/3` warning event only when a permissive sticky-identity miss is reported by the evaluator, with sanitized metadata limited to evaluation facts already present in the result/trace (for example `flag_key`, `environment`, `bucket_by`, `reason`, `strict?`) and without introducing `Rulestead.Telemetry`, a public event catalog, or broader Phase 4 spans. Implement `rulestead/lib/rulestead/explainer.ex` as a renderer over `debug_trace` facts per D-27 through D-30; it must produce human-readable prose but not become the source of truth. Preserve purity by accepting only already-in-memory evaluator inputs in Phase 3; do not add runtime cache lookups, snapshot-version diagnostics, application-env global flag registries, or hidden runtime lookup in this plan.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead_test.exs test/rulestead/evaluator_test.exs</automated>
  </verify>
  <done>The root module exposes real Phase 3 evaluator APIs as thin projections over the explicit in-memory flag payload contract, emits the narrow permissive-mode sticky-identity telemetry warning, and keeps `explain/2` consistent with the machine-readable trace/output while propagating the strict-mode typed error unchanged.</done>
  <acceptance_criteria>`rulestead/lib/rulestead.ex` contains public functions `enabled?/2`, `get_value/3`, `get_variant/2`, `evaluate/3`, `evaluate!/3`, and `explain/2` whose first argument is the authored in-memory flag payload, plus a direct `:telemetry.execute/3` call guarded to the permissive missing-sticky-identity path. `rulestead/lib/rulestead/explainer.ex` contains `defmodule Rulestead.Explainer` and reads `debug_trace` without snapshot/cache fields. `rulestead/test/rulestead_test.exs` asserts tuple-based projection behavior, strict-mode `{:error, %Rulestead.Error{type: :missing_targeting_key}}` propagation, and the single warning event emission for permissive sticky-identity misses.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored flag payload + context -> `Rulestead.Evaluator` | Untrusted runtime input drives feature decisions |
| `Rulestead.Evaluator` -> `Rulestead.Explainer` / root facade | Internal evaluation facts become user-visible output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | S | `Rulestead.Bucket` | mitigate | Resolve stickiness only from explicit normalized scalar fields per D-22 through D-25; do not inspect arbitrary actor payloads or fall back implicitly. |
| T-03-08 | T | `Rulestead.Evaluator` | mitigate | Enforce first-match-wins ordering and deterministic non-match behavior for missing/type-mismatched attributes so rule order cannot be bypassed by coercion. |
| T-03-09 | I | `Rulestead.Explainer` | mitigate | Render only factual `debug_trace` data and exclude Phase 4 diagnostics or raw attribute dumps beyond the locked trace surface. |
| T-03-10 | I | direct warning telemetry emission | mitigate | Emit only sanitized metadata already present in the result/trace for the narrow permissive missing-sticky-identity event; do not include raw actor attributes or introduce a broader event catalog in Phase 3. |
| T-03-11 | D | Root public facade | mitigate | Keep the hot path store-free and limit Phase 3 side effects to the single guarded warning event so evaluation does not block on runtime lookup or hidden state. |
</threat_model>

<verification>
Run `cd rulestead && mix test test/rulestead/evaluator_test.exs test/rulestead_test.exs`.
</verification>

<success_criteria>
The library can evaluate authored in-memory rules deterministically through one canonical result path, all public convenience APIs are projections over that path, and `explain/2` describes the same facts without introducing I/O or Phase 4 runtime behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-03-SUMMARY.md`
</output>
