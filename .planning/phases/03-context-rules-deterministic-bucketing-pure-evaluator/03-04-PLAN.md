---
phase: 03-context-rules-deterministic-bucketing-pure-evaluator
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - rulestead/mix.exs
  - rulestead/test/rulestead/bucket_property_test.exs
  - rulestead/test/rulestead/evaluator_property_test.exs
  - rulestead/test/rulestead/evaluator_test.exs
autonomous: true
requirements:
  - EVAL-05
  - EVAL-06
  - EVAL-08
  - TEST-04
must_haves:
  truths:
    - "The same bucketing inputs always produce the same bucket across 10k generated runs."
    - "Rule-order and explain/evaluate invariants are locked by tests instead of hand inspection."
    - "The Phase 3 evaluator contract is regression-protected before Phase 4 runtime wiring builds on it."
  artifacts:
    - path: "rulestead/test/rulestead/bucket_property_test.exs"
      provides: "Deterministic bucketing property coverage"
    - path: "rulestead/test/rulestead/evaluator_property_test.exs"
      provides: "First-match-wins and explain/evaluate invariant coverage"
    - path: "rulestead/mix.exs"
      provides: "StreamData dependency needed for Phase 3 property tests"
  key_links:
    - from: "rulestead/mix.exs"
      to: "rulestead/test/rulestead/bucket_property_test.exs"
      via: "StreamData dependency"
      pattern: "stream_data|StreamData"
    - from: "rulestead/test/rulestead/evaluator_property_test.exs"
      to: "rulestead/lib/rulestead/evaluator.ex"
      via: "generated invariant checks"
      pattern: "property|check all"
---

<objective>
Lock the determinism and explainability invariants with property tests and focused regression coverage.

Purpose: Prevent subtle drift in bucketing, rule ordering, and explain/evaluate consistency before later phases add caching, telemetry, and host-app seams on top.
Output: StreamData dependency, 10k-run bucket properties, evaluator invariants, and any small regression assertions needed in the existing evaluator test file.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md
@.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-PATTERNS.md
@.planning/phases/02-data-model-error-model-ecto-store-fake-adapter/02-04-SUMMARY.md

<interfaces>
From `prompts/rulestead-testing-and-e2e-strategy.md`:
- `test/rulestead_test.exs` covers public runtime happy paths and errors.
- `explain` is a superset of `evaluate`: both must agree on the resolved outcome.

From Phase 3 decisions:
- D-31: property tests own determinism, precedence, and explain/evaluate consistency.
- D-32: split by contract between `debug_trace` properties and `explain/2` renderer checks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add StreamData and the 10k-run deterministic bucketing property suite</name>
  <files>rulestead/mix.exs, rulestead/test/rulestead/bucket_property_test.exs</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md, prompts/rulestead-testing-and-e2e-strategy.md, rulestead/mix.exs, rulestead/test/rulestead/bucket_property_test.exs, rulestead/lib/rulestead/bucket.ex</read_first>
  <behavior>
    - Test 1: identical `(flag_key, rule_key, salt, targeting identity, namespace)` inputs always produce the same bucket across 10_000 generated runs.
    - Test 2: rollout and variant namespaces stay distinct so increasing rollout exposure does not reshuffle variant assignment inputs.
    - Test 3: bucket values are always integers in `0..9999`.
  </behavior>
  <action>Add `{:stream_data, ...}` as a test-only dependency in `rulestead/mix.exs` and create `test/rulestead/bucket_property_test.exs`. Use StreamData generators to prove D-17 through D-21: stable SHA-256 bucketing, `0..9999` range, and namespace separation between rollout inclusion and variant assignment. Configure the determinism property to run 10_000 cases as required by EVAL-06 and TEST-04. Keep this suite fast and pure; do not shell out, hit the fake store, or add randomness outside StreamData generators.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/bucket_property_test.exs</automated>
  </verify>
  <done>The repo has the property-test dependency it needs, and deterministic bucketing is locked with an explicit 10k-run property suite.</done>
  <acceptance_criteria>`rulestead/mix.exs` contains `:stream_data` with `only: :test`. `rulestead/test/rulestead/bucket_property_test.exs` contains `use ExUnitProperties`, a property with `max_runs: 10_000` or equivalent explicit 10k coverage, and assertions that bucket outputs are in `0..9999`.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add evaluator invariants for precedence, projections, and explain consistency</name>
  <files>rulestead/test/rulestead/evaluator_property_test.exs, rulestead/test/rulestead/evaluator_test.exs</files>
  <read_first>.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-CONTEXT.md, prompts/rulestead-testing-and-e2e-strategy.md, rulestead/test/rulestead/evaluator_test.exs, rulestead/test/rulestead/evaluator_property_test.exs, rulestead/lib/rulestead/evaluator.ex, rulestead/lib/rulestead/explainer.ex, rulestead/lib/rulestead.ex</read_first>
  <behavior>
    - Test 1: reordering two matching rules changes the chosen result only by rule order, proving first-match-wins.
    - Test 2: `enabled?/2`, `get_value/3`, and `get_variant/2` stay projection-consistent with `evaluate/3`.
    - Test 3: `explain/2` names the same matched rule/default path represented in the result/debug trace.
  </behavior>
  <action>Create `test/rulestead/evaluator_property_test.exs` for generated precedence and projection invariants, and keep `test/rulestead/evaluator_test.exs` for crisp example-based checks on human-readable explanation text. Encode D-31 and D-32 directly: property-test `debug_trace`/ordering invariants, then use example tests to assert the renderer includes the matched rule key or explicit default reason. Do not broaden the tests into Phase 4 diagnostics, telemetry warnings, cache age, or host-app builders.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/evaluator_property_test.exs test/rulestead/evaluator_test.exs</automated>
  </verify>
  <done>Rule precedence, public projections, and explain/evaluate consistency are protected by generated and example-based tests.</done>
  <acceptance_criteria>`rulestead/test/rulestead/evaluator_property_test.exs` contains `use ExUnitProperties` and at least one property covering rule ordering or projection consistency. `rulestead/test/rulestead/evaluator_test.exs` contains assertions that `explain/2` output includes a matched rule key or default reason string that agrees with the evaluation result.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated test input -> evaluator/bucket functions | Synthetic untrusted cases exercise determinism and precedence boundaries |
| evaluator facts -> human-readable explain output | Regression tests verify no semantic drift between machine and human surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | T | `Rulestead.Bucket` tests | mitigate | Use 10k generated cases to catch silent algorithm drift or namespace collisions before Phase 4 depends on the hash contract. |
| T-03-12 | R | evaluator invariants | mitigate | Encode first-match-wins and projection consistency as properties so future refactors cannot dispute semantics without failing CI. |
| T-03-13 | I | explain renderer tests | mitigate | Assert `explain/2` names the same matched rule/default path as `debug_trace`, preventing misleading human output. |
</threat_model>

<verification>
Run `cd rulestead && mix test test/rulestead/bucket_property_test.exs test/rulestead/evaluator_property_test.exs test/rulestead/evaluator_test.exs`.
</verification>

<success_criteria>
Phase 3 determinism, precedence, and explainability invariants are enforced by automated tests, including the required 10k-run bucketing property suite.
</success_criteria>

<output>
After completion, create `.planning/phases/03-context-rules-deterministic-bucketing-pure-evaluator/03-04-SUMMARY.md`
</output>
