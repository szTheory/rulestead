---
phase: 128-the-release-cut
plan: 03
type: execute
wave: 3
depends_on:
  - 128-02
files_modified:
  - release-please-config.json
  - MAINTAINING.md
autonomous: false
requirements:
  - REL-06

must_haves:
  truths:
    - "release-as key is completely absent from release-please-config.json"
    - "release-pr-automerge workflow is re-enabled in GitHub Actions UI"
    - "MAINTAINING.md confirms bump-minor-pre-major and bump-patch-for-minor-pre-major are now no-ops post-1.0"
  artifacts:
    - path: "release-please-config.json"
      provides: "Clean post-cut config — no release-as key in either package block"
  key_links:
    - from: "release-please-config.json (release-as removed)"
      to: "release-please future runs (no spurious 1.0.0 re-proposal)"
      via: "Absence of release-as key means release-please reverts to commit-convention version calculation"
      pattern: "release-as"
---

<objective>
Post-cut cleanup: remove release-as from config, re-enable auto-merge, and confirm MAINTAINING.md
accurately documents the now-no-op bump-*-pre-major flags.

Purpose: The release-as key is a trap — if left in, every subsequent push to main causes
release-please to re-propose 1.0.0 forever (Landmine 1). This plan closes that trap immediately
after the publish confirm. Re-enabling auto-merge restores the normal patch/minor release flow
for post-1.0 work.

Output:
- release-please-config.json with release-as key completely removed from the rulestead block
- MAINTAINING.md has an accurate no-op note for bump-*-pre-major (already present; confirmed)
- release-pr-automerge workflow re-enabled (human gate)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-the-release-cut/128-RESEARCH.md
@.planning/phases/128-the-release-cut/128-VALIDATION.md
@release-please-config.json
@MAINTAINING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove release-as from release-please-config.json and push to main</name>
  <files>release-please-config.json</files>
  <read_first>
    - release-please-config.json — confirm current state (release-as: "1.0.0" still present from 128-01)
    - .planning/phases/128-the-release-cut/128-RESEARCH.md — Step 13a and Landmine 1
    - .planning/phases/128-the-release-cut/128-RESEARCH.md — "Exact release-please-config.json After Post-Cut Cleanup"
  </read_first>
  <action>
    PRECONDITION: Plan 128-02 must be complete — both packages live at 1.0.0, verify-trio green.

    This is the MOST CRITICAL post-cut step. Remove "release-as": "1.0.0" from the rulestead
    package block in release-please-config.json. The rulestead block after removal must be:

    "rulestead": {
      "component": "rulestead",
      "release-type": "elixir",
      "package-name": "rulestead",
      "changelog-path": "rulestead/CHANGELOG.md",
      "include-component-in-tag": true
    }

    No other changes to release-please-config.json. The bump-minor-pre-major and
    bump-patch-for-minor-pre-major flags remain in the root config — they are now no-ops
    (only affect < 1.0.0 versioning) but are harmless to leave in place.

    Verify the key is gone immediately after editing:
      grep '"release-as"' release-please-config.json && echo "STILL PRESENT — MUST REMOVE" || echo "clean"
    The grep must return non-zero (no output / "clean").

    Commit with:
      chore: remove release-as after 1.0.0 promotion cut

    Push to main. This triggers release-please.yml again; without release-as, it will
    see no accumulated conventional commits since the last release and produce no new PR
    (or update the existing released state). Confirm in the Actions tab that release-please.yml
    runs without opening a new 1.0.0 PR.
  </action>
  <verify>
    <automated>grep '"release-as"' release-please-config.json && echo "STILL PRESENT" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - grep '"release-as"' release-please-config.json returns non-zero exit code (key is absent)
    - The rulestead block in release-please-config.json has exactly the five expected keys (component, release-type, package-name, changelog-path, include-component-in-tag)
    - git log --oneline -1 shows the chore: remove release-as commit
    - release-please.yml runs on main after push and does NOT open a new 1.0.0 PR
  </acceptance_criteria>
  <done>
    release-please-config.json has no release-as key; commit pushed to main; release-please.yml
    ran without proposing a duplicate 1.0.0 PR.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm MAINTAINING.md no-op note is accurate for bump-*-pre-major flags</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md lines 228–260 — the "Mandatory post-cut removal" and "Sequence summary" sections
    - .planning/phases/128-the-release-cut/128-RESEARCH.md — Step 13c and Open Question 3
  </read_first>
  <action>
    RESEARCH.md Open Question 3 confirms: MAINTAINING.md line 234 already states:
    "After the major lands, bump-minor-pre-major / bump-patch-for-minor-pre-major become
    no-ops — they only affect < 1.0.0 versioning. From 1.0.0 onward, ordinary semver applies
    and feat!: correctly drives the next major."

    This text is sufficient for REL-06. No additional documentation commit is required.

    Verify the no-op note is present and accurate:
      grep -i 'no-op' MAINTAINING.md
    This must return at least one line referencing bump-minor-pre-major or bump-patch-for-minor-pre-major.

    Verify it also correctly states the post-1.0 semver behavior (feat!: drives next major):
      grep -i 'feat!' MAINTAINING.md

    If either grep returns empty output, add the missing text to the "Mandatory post-cut removal"
    subsection under "## Cutting a major (X.0.0)" in MAINTAINING.md. The text to add (only if
    the grep above returns nothing):

    After the major lands, `bump-minor-pre-major` / `bump-patch-for-minor-pre-major` become
    **no-ops** — they only affect `< 1.0.0` versioning. From `1.0.0` onward, ordinary semver
    applies and `feat!:` correctly drives the next major.

    Based on the RESEARCH.md findings, the text is already present at line 234 — confirm with
    grep and treat this task as a verification step, not a write step.
  </action>
  <verify>
    <automated>grep -i 'no-op' MAINTAINING.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -i 'no-op' MAINTAINING.md returns at least one hit in the "Cutting a major" section
    - The text accurately reflects that bump-*-pre-major flags are now no-ops post-1.0.0
  </acceptance_criteria>
  <done>
    MAINTAINING.md confirms bump-minor-pre-major and bump-patch-for-minor-pre-major are no-ops
    post-1.0.0; no new commit required (existing text is accurate).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [HUMAN GATE] Re-enable release-pr-automerge workflow</name>
  <files>N/A — GitHub Actions UI toggle, no file edit</files>
  <read_first>
    - .planning/phases/128-the-release-cut/128-RESEARCH.md — Step 13b
  </read_first>
  <what-built>
    Tasks 1 and 2 complete: release-as is removed from config, MAINTAINING.md is confirmed
    accurate. The release-pr-automerge workflow is still disabled from Plan 128-01 Task 2.
    Re-enabling it restores the normal flow for future patch/minor releases.
  </what-built>
  <how-to-verify>
    1. Go to GitHub Actions → in the left sidebar, find the workflow named "release-pr-automerge".
    2. The workflow should currently show as disabled. Click the ⋯ (three-dot) menu → "Enable workflow".
    3. Confirm the workflow now shows as enabled (the disable option reappears in the menu).
    4. Final state check — run the full post-cut verification set:

       a) Confirm release-as is gone from config:
            grep '"release-as"' release-please-config.json && echo "STILL PRESENT" || echo "clean"
          Must return "clean".

       b) Confirm both packages are live (cached result from 128-02, spot-check):
            curl -fsS https://hex.pm/api/packages/rulestead/releases/1.0.0 | jq .version
            curl -fsS https://hex.pm/api/packages/rulestead_admin/releases/1.0.0 | jq .version
          Both must return "1.0.0".

       c) Confirm MAINTAINING.md no-op note:
            grep -i 'no-op' MAINTAINING.md
          Must return at least one hit.
  </how-to-verify>
  <action>
    Human operator action required — re-enable the release-pr-automerge workflow in the
    GitHub Actions UI. This restores the normal automatic-merge flow for future patch and
    minor releases post-1.0. See how-to-verify for navigation steps and final state checks.
  </action>
  <verify>
    <automated>grep '"release-as"' release-please-config.json && echo "STILL PRESENT" || echo "clean"</automated>
  </verify>
  <done>
    release-pr-automerge workflow re-enabled; all final state checks pass (release-as absent,
    both packages live at 1.0.0, MAINTAINING.md no-op note confirmed).
  </done>
  <resume-signal>
    Type "cleanup complete" after confirming: auto-merge re-enabled, release-as grep returns
    "clean", both curl checks return "1.0.0", MAINTAINING.md no-op grep hits.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release-please-config.json (post-cut) → future release-please runs | A stale release-as key would cause infinite spurious 1.0.0 PR re-proposals |
| re-enabled auto-merge → future release PRs | Auto-merge is safe again now that the 1.0.0 cut is complete and verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-09 | Tampering | release-as left in config after cut (Landmine 1) | mitigate | Task 1 verifies removal with grep gate before commit; grep verify is the acceptance criterion |
| T-128-10 | Denial of Service | Auto-merge stays disabled blocking future patch/minor releases | mitigate | Task 3 human gate explicitly re-enables; blocking checkpoint prevents the plan completing without the re-enable step |
| T-128-11 | Tampering | release-as accidentally added to rulestead_admin block during cleanup (Landmine 2) | mitigate | Task 1 action explicitly states only the rulestead block is touched; grep acceptance criterion counts matches |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this phase |
</threat_model>

<verification>
Post-cut config clean:
  grep '"release-as"' release-please-config.json || echo "clean"   # must echo "clean"

MAINTAINING.md no-op note:
  grep -i 'no-op' MAINTAINING.md   # must return at least one hit

Both packages still live (sanity):
  curl -fsS https://hex.pm/api/packages/rulestead/releases/1.0.0 | jq .version
  curl -fsS https://hex.pm/api/packages/rulestead_admin/releases/1.0.0 | jq .version
</verification>

<success_criteria>
- grep '"release-as"' release-please-config.json exits non-zero (key is completely absent)
- release-please.yml ran on main after removal commit and did NOT open a spurious 1.0.0 PR
- grep -i 'no-op' MAINTAINING.md returns at least one hit confirming bump-*-pre-major are no-ops post-1.0
- release-pr-automerge workflow is confirmed re-enabled in GitHub Actions UI
- Phase 128 verify-trio from Plan 128-02 remains green (belt-and-suspenders spot-check)
</success_criteria>

<output>
Create `.planning/phases/128-the-release-cut/128-03-SUMMARY.md` when done.

## Artifacts this plan produces

| Artifact | Change | Notes |
|----------|--------|-------|
| release-please-config.json | "release-as": "1.0.0" removed from rulestead block | Returns config to post-cut clean state |
| MAINTAINING.md | Verified accurate (no edit required — text already present at line 234) | Confirms no-op status of bump-*-pre-major flags post-1.0 |
</output>
