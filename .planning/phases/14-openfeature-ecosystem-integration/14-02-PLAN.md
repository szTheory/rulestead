---
phase: 14-openfeature-ecosystem-integration
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - open_feature_rulestead/lib/open_feature_rulestead/provider.ex
  - open_feature_rulestead/test/open_feature_rulestead/provider_test.exs
autonomous: true
requirements:
  - ECO-01
  - ECO-03
must_haves:
  truths:
    - "Provider implements OpenFeature.Provider behaviour"
    - "Provider delegates resolution to Rulestead.Runtime"
    - "Explainability trace is surfaced in ResolutionDetails"
  artifacts:
    - path: "open_feature_rulestead/lib/open_feature_rulestead/provider.ex"
      provides: "OpenFeature Provider implementation"
      contains: "@behaviour OpenFeature.Provider"
  key_links:
    - from: "open_feature_rulestead/lib/open_feature_rulestead/provider.ex"
      to: "rulestead/lib/rulestead/runtime.ex"
      via: "evaluate/3"
      pattern: "Rulestead.Runtime.evaluate"
---

<objective>
Implement the `OpenFeature.Provider` behaviour to bridge standard flag evaluation requests into Rulestead's engine.

Purpose: Allow operators to use the vendor-neutral OpenFeature SDK while retaining Rulestead's evaluation determinism and explainability.
Output: `OpenFeatureRulestead.Provider` module and unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-openfeature-ecosystem-integration/14-RESEARCH.md
@.planning/phases/14-openfeature-ecosystem-integration/14-PATTERNS.md
@rulestead/lib/rulestead/runtime.ex
@rulestead/lib/rulestead/result.ex

<interfaces>
From open_feature_rulestead/lib/open_feature_rulestead/context_mapper.ex:
```elixir
defmodule OpenFeatureRulestead.ContextMapper do
  def translate(of_context) when is_map(of_context)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provider Implementation</name>
  <files>
    open_feature_rulestead/lib/open_feature_rulestead/provider.ex,
    open_feature_rulestead/test/open_feature_rulestead/provider_test.exs
  </files>
  <behavior>
    - Test 1: `initialize` accepts `environment_key` and returns ready state.
    - Test 2: `resolve_boolean_value` (and others) maps context, calls `Rulestead.Runtime.evaluate`, and returns `ResolutionDetails`.
    - Test 3: Metadata in `ResolutionDetails` includes `matched_rule`, `flag_version`, and `cache_age_ms` from `Rulestead.Result`.
  </behavior>
  <action>
    Implement `OpenFeatureRulestead.Provider` adopting `@behaviour OpenFeature.Provider`.
    - Define a struct with `name: "Rulestead", state: :not_ready, environment_key: nil`.
    - Implement `initialize/3` per D-04 to require and store `environment_key`.
    - Implement `resolve_boolean_value`, `resolve_string_value`, `resolve_number_value`, `resolve_object_value`.
    - Delegate all resolves to a private `do_resolve/4` which:
      1. Translates context using `OpenFeatureRulestead.ContextMapper.translate/1`.
      2. Calls `Rulestead.Runtime.evaluate(key, translated_context, [environment: provider.environment_key])`.
      3. Maps the resulting `Rulestead.Result` to `OpenFeature.ResolutionDetails`.
    - Construct `flag_metadata` in `ResolutionDetails` by extracting scalar values from Rulestead's result (`matched_rule`, `flag_version`, `cache_age_ms`) per ECO-03 and D-03.
    - Explicitly reference the analog `rulestead/lib/rulestead/store/ecto.ex` and the 'Error Normalization' shared pattern from `14-PATTERNS.md` when handling errors. 
      Pattern excerpt:
      ```elixir
      {:error, error} -> {:error, StoreError.unavailable(cause: error)}
      ```
    - Handle errors by returning mapped error codes corresponding to `OpenFeature.Types.error_code()`, similar to the pattern above.
  </action>
  <verify>
    <automated>cd open_feature_rulestead && mix test test/open_feature_rulestead/provider_test.exs</automated>
  </verify>
  <done>All `resolve_*` callbacks correctly delegate to Rulestead and return standard ResolutionDetails</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenFeature Client → Rulestead Runtime | Provider executes flag evaluation logic passing through client context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Information Disclosure | `Provider` | mitigate | Strip nested/complex `debug_trace` objects; only map specific scalar telemetry (rule, cache_age, version) to OpenFeature metadata boundary |
</threat_model>

<verification>
Provider implements standard OpenFeature specs without crashing on telemetry boundaries.
</verification>

<success_criteria>
- Host applications can evaluate Rulestead flags via the generic OpenFeature client.
- Rulestead's unique explainability trace is safely translated and surfaced.
</success_criteria>

<output>
After completion, create `.planning/phases/14-openfeature-ecosystem-integration/14-02-SUMMARY.md`
</output>