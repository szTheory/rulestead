---
phase: 14-openfeature-ecosystem-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - open_feature_rulestead/mix.exs
  - open_feature_rulestead/.formatter.exs
  - open_feature_rulestead/README.md
  - open_feature_rulestead/lib/open_feature_rulestead.ex
  - open_feature_rulestead/lib/open_feature_rulestead/context_mapper.ex
  - open_feature_rulestead/test/open_feature_rulestead/context_mapper_test.exs
  - open_feature_rulestead/test/test_helper.exs
autonomous: true
requirements:
  - ECO-01
  - ECO-02
must_haves:
  truths:
    - "open_feature_rulestead package exists and can be compiled"
    - "Context Mapper correctly maps OpenFeature map to Rulestead Context struct"
  artifacts:
    - path: "open_feature_rulestead/mix.exs"
      provides: "Package definition"
      contains: ":open_feature"
    - path: "open_feature_rulestead/lib/open_feature_rulestead/context_mapper.ex"
      provides: "Context translation utility"
      exports: ["translate/1"]
  key_links:
    - from: "open_feature_rulestead/lib/open_feature_rulestead/context_mapper.ex"
      to: "rulestead/lib/rulestead/context.ex"
      via: "struct creation"
      pattern: "Rulestead.Context.new"
---

<objective>
Scaffold the `open_feature_rulestead` package and implement the `ContextMapper` utility.

Purpose: Provide the foundational structure for the OpenFeature integration and handle the translation of OpenFeature's weakly-typed context into Rulestead's strict `Context` struct.
Output: Scaffolding files and the `ContextMapper` module.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-openfeature-ecosystem-integration/14-RESEARCH.md
@.planning/phases/14-openfeature-ecosystem-integration/14-PATTERNS.md
@rulestead_admin/mix.exs
@rulestead/lib/rulestead/context.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package Scaffolding</name>
  <files>
    open_feature_rulestead/mix.exs,
    open_feature_rulestead/.formatter.exs,
    open_feature_rulestead/README.md,
    open_feature_rulestead/lib/open_feature_rulestead.ex,
    open_feature_rulestead/test/test_helper.exs
  </files>
  <action>
    Create a new Elixir package `open_feature_rulestead` as a sibling to `rulestead` and `rulestead_admin` per D-01.
    Define `mix.exs` with `app: :open_feature_rulestead`. Explicitly reference the analog `rulestead/mix.exs` from `14-PATTERNS.md` to ensure project structure aligns. 
    Pattern excerpt:
    ```elixir
      def project do
        [
          app: :rulestead, # adapt to :open_feature_rulestead
          version: @version,
          elixir: "~> 1.17",
          start_permanent: Mix.env() == :prod,
          elixirc_paths: elixirc_paths(Mix.env()),
          deps: deps(),
          package: package(),
          docs: docs(),
          dialyzer: dialyzer()
        ]
      end
    ```
    Add dependencies: `{:open_feature, "~> 0.1.3"}` and `{:rulestead, path: "../rulestead"}` (or hex version if `RULESTEAD_OPENFEATURE_HEX_RELEASE == "1"`, similar to `rulestead_admin`).
    Create standard boilerplate files: `.formatter.exs`, `README.md`, `lib/open_feature_rulestead.ex`, and `test/test_helper.exs` containing `ExUnit.start()`.
  </action>
  <verify>
    <automated>cd open_feature_rulestead && mix deps.get && mix compile</automated>
  </verify>
  <done>Package compiles successfully with dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Context Mapper</name>
  <files>
    open_feature_rulestead/lib/open_feature_rulestead/context_mapper.ex,
    open_feature_rulestead/test/open_feature_rulestead/context_mapper_test.exs
  </files>
  <behavior>
    - Test 1: Given a string-key map with "targetingKey", "tenantKey", "environment", "sessionId", "requestId", "actor", returns a valid `Rulestead.Context` struct.
    - Test 2: Unrecognized keys in the OpenFeature map are moved into the `attributes` map in the `Rulestead.Context`.
    - Test 3: Handles missing optional keys gracefully.
  </behavior>
  <action>
    Implement `OpenFeatureRulestead.ContextMapper.translate/1` per D-02.
    Explicitly reference the analog `rulestead/lib/rulestead/context.ex` from `14-PATTERNS.md` for transformation patterns, and `rulestead/test/rulestead/context_test.exs` for testing structure.
    Pattern excerpt for normalization:
    ```elixir
      defp normalize_scalar(nil), do: nil
      defp normalize_scalar(value) when is_binary(value) do
        value
        |> String.trim()
        |> case do
          "" -> nil
          normalized -> normalized
        end
      end
    ```
    Extract "targetingKey", "tenantKey", "environment", "sessionId", "requestId", "actor" as known keys.
    Pass all remaining keys into the `attributes` map.
    Use `Rulestead.Context.new/1` to build the struct.
  </action>
  <verify>
    <automated>cd open_feature_rulestead && mix test test/open_feature_rulestead/context_mapper_test.exs</automated>
  </verify>
  <done>Tests pass and context attributes are properly translated or assigned to custom attributes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenFeature Client → Provider | Untrusted data from generic client context is processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `ContextMapper` | mitigate | Strict map extraction filters out unexpected root-level properties by moving them safely into the nested `attributes` map |
</threat_model>

<verification>
Package `open_feature_rulestead` compiles and unit tests for `ContextMapper` pass locally.
</verification>

<success_criteria>
- Basic project structure exists for the OpenFeature provider.
- Context mapper translates OpenFeature weakly-typed variables into strictly typed domain Contexts.
</success_criteria>

<output>
After completion, create `.planning/phases/14-openfeature-ecosystem-integration/14-01-SUMMARY.md`
</output>