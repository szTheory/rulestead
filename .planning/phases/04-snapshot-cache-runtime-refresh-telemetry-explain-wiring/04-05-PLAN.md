---
phase: 04-snapshot-cache-runtime-refresh-telemetry-explain-wiring
plan: 05
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - rulestead/lib/rulestead/telemetry.ex
  - rulestead/lib/rulestead.ex
  - rulestead/lib/rulestead/runtime.ex
  - rulestead/lib/rulestead/runtime/refresh.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/store.ex
  - rulestead/mix.exs
  - guides/flows/telemetry.md
  - rulestead/test/rulestead/telemetry_test.exs
  - rulestead/test/rulestead/integration/runtime_hot_path_test.exs
autonomous: true
requirements:
  - TEL-01
  - TEL-02
  - TEL-04
must_haves:
  truths:
    - "Telemetry events are a versioned public API and use one bounded metadata spine across eval, runtime, store, and admin-coarse operations."
    - "Telemetry handlers are isolated so unknown reason atoms or additive metadata never crash the caller."
    - "The event catalog and automated tests describe the same public contract."
  artifacts:
    - path: "rulestead/lib/rulestead/telemetry.ex"
      provides: "Public telemetry wrapper and safe handler utilities"
    - path: "guides/flows/telemetry.md"
      provides: "Versioned Phase 4 event catalog and metadata contract"
    - path: "rulestead/test/rulestead/telemetry_test.exs"
      provides: "Executable event catalog conformance coverage"
  key_links:
    - from: "rulestead/lib/rulestead/telemetry.ex"
      to: "guides/flows/telemetry.md"
      via: "one event catalog source of truth"
      pattern: "span|attach|safe"
    - from: "rulestead/test/rulestead/integration/runtime_hot_path_test.exs"
      to: "rulestead/lib/rulestead/runtime.ex"
      via: "zero-DB-query hot-path proof"
      pattern: "cache_hit|eval|store"
---

<objective>
Freeze the Phase 4 telemetry contract, document it, and prove the runtime/store/public-operation event surface matches the roadmap and never crashes handlers.

Purpose: Close Phase 4 by turning the runtime behavior from the earlier plans into a documented, testable observability API with explicit handler safety guarantees.
Output: `Rulestead.Telemetry`, event instrumentation across the runtime/store/public surfaces, a Phase 4 telemetry guide, and telemetry-focused tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-snapshot-cache-runtime-refresh-telemetry-explain-wiring/04-CONTEXT.md
@rulestead/lib/rulestead.ex
@rulestead/lib/rulestead/runtime.ex
@rulestead/lib/rulestead/runtime/refresh.ex
@rulestead/lib/rulestead/store/ecto.ex
@guides/recipes/telemetry.md
@prompts/rulestead-telemetry-observability-and-audit.md
@prompts/rulestead-security-privacy-and-threat-model.md

<interfaces>
Phase 4 decision D-32 locks the event families and D-33 through D-35 lock the metadata spine and no-raw-payload discipline. The roadmap success criteria require both the event catalog guide and `mix test --include telemetry` proof.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instrument the public runtime and store surfaces through `Rulestead.Telemetry`</name>
  <files>rulestead/lib/rulestead/telemetry.ex, rulestead/lib/rulestead.ex, rulestead/lib/rulestead/runtime.ex, rulestead/lib/rulestead/runtime/refresh.ex, rulestead/lib/rulestead/store/ecto.ex, rulestead/lib/rulestead/store.ex, rulestead/test/rulestead/telemetry_test.exs</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/04-snapshot-cache-runtime-refresh-telemetry-explain-wiring/04-CONTEXT.md, prompts/rulestead-telemetry-observability-and-audit.md, prompts/rulestead-security-privacy-and-threat-model.md</read_first>
  <behavior>
    - Test 1: public eval/runtime/store operations emit the Phase 4 event families with the documented shared metadata spine.
    - Test 2: handler exceptions, unknown reason atoms, or additive metadata do not raise back into the instrumented operation.
    - Test 3: stale cache usage and snapshot apply/publish events emit without raw attributes, raw values, or host-framework structs.
  </behavior>
  <action>Add `Rulestead.Telemetry.span/3` and any safe handler-attachment helpers needed for TEL-01 and TEL-04. Instrument the public runtime façade, store read/write boundaries, refresh/apply paths, and the narrow admin-coarse mutation surfaces already present in the store layer so they emit the locked Phase 4 event families from D-32. Use one shared metadata builder for `flag_key`, `flag_type`, `environment`, `snapshot_version`, `cache_age_ms`, `reason`, `has_targeting_key?`, and `matched_rule_count`, with domain-specific additions only where bounded and documented. Keep handler safety explicit: instrumented operations must succeed even when a telemetry handler raises.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/telemetry_test.exs --include telemetry</automated>
  </verify>
  <done>The Phase 4 event families exist as a bounded public API and handler failures cannot crash the runtime or store operations they observe.</done>
  <acceptance_criteria>`rulestead/lib/rulestead/telemetry.ex` exposes `span/3`. `rulestead/test/rulestead/telemetry_test.exs` asserts event names and metadata keys for eval, runtime cache, snapshot, and store operations and covers a handler-raises case.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the event catalog guide and prove the hot path stays DB-free</name>
  <files>guides/flows/telemetry.md, rulestead/mix.exs, rulestead/test/rulestead/integration/runtime_hot_path_test.exs</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/04-snapshot-cache-runtime-refresh-telemetry-explain-wiring/04-CONTEXT.md, guides/recipes/telemetry.md, rulestead/mix.exs, prompts/rulestead-telemetry-observability-and-audit.md</read_first>
  <behavior>
    - Test 1: the telemetry guide documents exactly the event families, metadata spine, and handler-safety rules implemented in code.
    - Test 2: the ExDoc extras surface points at the Phase 4 telemetry guide location.
    - Test 3: an integration test proves keyed runtime evaluation hits ETS only and performs zero DB reads on the hot path after the cache is warm.
  </behavior>
  <action>Create the Phase 4 public event catalog guide at `guides/flows/telemetry.md`, folding or replacing the earlier scaffold in `guides/recipes/telemetry.md` as needed so the docs surface matches the roadmap. Update `mix.exs` ExDoc extras accordingly. Add a focused integration test that warms a runtime snapshot, evaluates through `Rulestead.Runtime`, and proves via telemetry/event capture or an adapter-visible query counter that the hot path performs no authoring-store DB reads once cache state is applied.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/telemetry_test.exs test/rulestead/integration/runtime_hot_path_test.exs --include telemetry'</automated>
  </verify>
  <done>The telemetry contract is documented where the roadmap says it lives, and the runtime hot path is proven DB-free after cache warmup.</done>
  <acceptance_criteria>`guides/flows/telemetry.md` enumerates the Phase 4 event families and metadata spine. `rulestead/mix.exs` references the flow guide in ExDoc extras. `rulestead/test/rulestead/integration/runtime_hot_path_test.exs` asserts zero DB reads on warm-cache keyed evaluation.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime/store code -> telemetry handlers | External handlers observe internal events and may misbehave |
| code instrumentation -> published docs | The documented event contract must match what the runtime actually emits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | I | telemetry metadata | mitigate | Centralize metadata shaping and omit raw values, raw attributes, actor payloads, and framework structs at emission time. |
| T-04-14 | D | handler failures | mitigate | Wrap handler execution so unknown reason atoms or handler exceptions cannot crash instrumented operations. |
| T-04-15 | R | public event catalog drift | mitigate | Lock guide coverage with executable telemetry tests so docs and event emission evolve together. |
</threat_model>

<verification>
Run the telemetry-focused test suite with `--include telemetry`, confirm the flow guide is wired into ExDoc extras, and execute the warm-cache integration proof that keyed runtime evaluation performs zero DB reads.
</verification>

<success_criteria>
Phase 4 ends with a documented, versioned telemetry API whose handlers are safe, whose metadata is bounded and redacted, and whose runtime hot path is observably DB-free.
</success_criteria>

<output>
After completion, create `.planning/phases/04-snapshot-cache-runtime-refresh-telemetry-explain-wiring/04-05-SUMMARY.md`
</output>
