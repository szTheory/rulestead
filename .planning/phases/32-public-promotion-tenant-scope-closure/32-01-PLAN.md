---
phase: 32-public-promotion-tenant-scope-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead.ex
  - rulestead/test/rulestead/promotion/compare_test.exs
  - rulestead/test/rulestead/promotion/apply_test.exs
autonomous: true
requirements:
  - TEN-01
  - TEN-03
must_haves:
  truths:
    - "Public `Rulestead.plan_promotion/3` preserves explicit `tenant_key` when callers already provide it."
    - "Saved promote plans generated from the public facade retain reviewed tenant scope in the same top-level `tenant_key` contract already used by compare and apply."
    - "Phase 32 fixes only the public promotion-plan tenant seam and does not pull the Phase 33 `compare_token` drill-in work into scope."
  artifacts:
    - path: "rulestead/lib/rulestead.ex"
      provides: "Bounded `plan_promotion/3` option forwarding into the existing tenant-aware compare seam"
      min_lines: 120
    - path: "rulestead/test/rulestead/promotion/compare_test.exs"
      provides: "Public compare and plan-generation regressions for tenant-aware facade forwarding"
      min_lines: 120
    - path: "rulestead/test/rulestead/promotion/apply_test.exs"
      provides: "Saved-plan replay regressions proving the public plan artifact keeps `tenant_key` after normalization"
      min_lines: 220
  key_links:
    - from: "rulestead/lib/rulestead.ex"
      to: "rulestead/lib/rulestead/store/command.ex"
      via: "`plan_promotion/3` forwards only `flag_keys` and `tenant_key` into `Command.CompareEnvironments.new/3` through `compare_environments/3`"
      pattern: "Keyword.take|flag_keys|tenant_key|compare_environments"
    - from: "rulestead/lib/rulestead.ex"
      to: "rulestead/lib/rulestead/manifest/plan.ex"
      via: "Plan generation copies `compare.tenant_key` into `Plan.build_promote/1` instead of inventing a new tenant metadata shape"
      pattern: "tenant_key|Plan.build_promote"
    - from: "rulestead/test/rulestead/promotion/apply_test.exs"
      to: "rulestead/lib/rulestead.ex"
      via: "Saved-plan apply assertions prove the generated plan round-trips through `Plan.load/1` without tenant drift"
      pattern: "apply_promotion_plan|tenant_key"
---

<objective>
Close the public promotion-plan seam where `tenant_key` is dropped before compare, and lock the saved-plan artifact contract with narrow regressions.

Purpose: Phase 32 exists because the lower compare/apply layers are already tenant-aware, but the public façade still discards explicit tenant scope before the saved plan is built.
Output: bounded `tenant_key` forwarding in `Rulestead.plan_promotion/3` plus focused public/saved-plan regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.1.0-MILESTONE-AUDIT.md
@.planning/phases/32-public-promotion-tenant-scope-closure/32-RESEARCH.md
@.planning/phases/32-public-promotion-tenant-scope-closure/32-PATTERNS.md
@.planning/phases/29-tenancy-helpers-validation/29-02-PLAN.md
@.planning/phases/30-mounted-admin-tenant-scope-closure/30-02-PLAN.md
@.planning/phases/31-audit-tenant-provenance-enforcement/31-01-PLAN.md
@rulestead/lib/rulestead.ex
@rulestead/lib/rulestead/manifest/plan.ex
@rulestead/lib/rulestead/promotion/compare.ex
@rulestead/lib/rulestead/store/command.ex
@rulestead/test/rulestead/promotion/compare_test.exs
@rulestead/test/rulestead/promotion/apply_test.exs

<interfaces>
From `rulestead/lib/rulestead.ex`:
```elixir
def plan_promotion(source_environment_key, target_environment_key, opts \\ []) do
  compare_opts =
    case Keyword.get(opts, :flag_keys) do
      nil -> []
      flag_keys -> [flag_keys: flag_keys]
    end

  with {:ok, compare} <-
         compare_environments(source_environment_key, target_environment_key, compare_opts) do
```

From `rulestead/lib/rulestead/store/command.ex`:
```elixir
defmodule CompareEnvironments do
  defstruct [
    :source_environment_key,
    :target_environment_key,
    tenant_key: nil,
    flag_keys: nil,
    compare_token: nil
  ]
end
```

From `rulestead/lib/rulestead/manifest/plan.ex`:
```elixir
|> maybe_put("tenant_key", fetch_optional_string(plan, "tenant_key"))
```

From `rulestead/lib/rulestead/promotion/compare.ex`:
```elixir
compare_token(%{..., tenant_key: attrs[:tenant_key] || attrs["tenant_key"], ...})
new_result(%{..., tenant_key: attrs[:tenant_key] || attrs["tenant_key"], ...})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forward explicit tenant scope through the public promotion-plan facade</name>
  <files>rulestead/lib/rulestead.ex, rulestead/test/rulestead/promotion/compare_test.exs</files>
  <behavior>
    - Test 1: `Rulestead.plan_promotion/3` forwards caller-provided `tenant_key` into the existing `compare_environments/3` seam together with any requested `flag_keys`.
    - Test 2: the public plan result copies `compare.tenant_key` into `details.plan.tenant_key` and keeps the existing top-level compare/apply token fields unchanged.
    - Test 3: omitted `tenant_key` still stays explicit-by-contract by forwarding nothing, not a fabricated scope.
  </behavior>
  <action>Replace the current `flag_keys`-only compare option shaping in `Rulestead.plan_promotion/3` with a bounded whitelist that forwards only `:flag_keys` and `:tenant_key`, dropping nil values and nothing else. Keep the public function signature unchanged, keep the compare/apply provenance contract from Phases 29-31, and do not widen this into CLI flag work or Phase 33 `compare_token` navigation fixes. Extend `compare_test.exs` so it proves the public facade reaches the store command with `tenant_key` and that the generated plan envelope surfaces the saved top-level `tenant_key` from the compare payload.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/promotion/compare_test.exs</automated>
  </verify>
  <done>`Rulestead.plan_promotion/3` forwards explicit tenant scope into compare using the existing bounded option vocabulary, and public compare/plan regressions prove the saved plan now carries `tenant_key` instead of dropping it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock saved promote-plan normalization around the tenant-scoped public artifact</name>
  <files>rulestead/test/rulestead/promotion/apply_test.exs</files>
  <behavior>
    - Test 1: a tenant-scoped plan produced from the public promotion flow survives `Plan.load/1` and `apply_promotion_plan/2` with the same `tenant_key`.
    - Test 2: tenant drift still fails closed as `stale` when a caller provides a mismatched live `tenant_key`.
    - Test 3: the regression stays focused on the saved plan handoff and does not introduce a new tenant metadata shape.
  </behavior>
  <action>Extend `apply_test.exs` with public-plan based coverage instead of hand-built synthetic scope semantics. Build or serialize a promote plan through the public API path, then assert the loaded plan still carries the reviewed top-level `tenant_key` into `apply_promotion_plan/2` and through stale-path reporting. Keep this task test-only and focused on proving the existing saved-plan handoff rather than widening ownership into additional production helpers.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/promotion/compare_test.exs test/rulestead/promotion/apply_test.exs</automated>
  </verify>
  <done>The saved promote-plan artifact generated from the public API round-trips through apply-time normalization with the same explicit `tenant_key`, and tenant drift still fails closed on replay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller opts -> `plan_promotion/3` compare opts | Explicit tenant scope enters the public facade here and must not be dropped or widened |
| compare result -> saved plan artifact | Reviewed tenant scope becomes durable apply input here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | T | `rulestead/lib/rulestead.ex` | mitigate | Whitelist only `flag_keys` and `tenant_key` when building compare opts so public plan generation cannot silently discard or over-forward tenant scope. |
| T-32-02 | R | saved promote plan artifact | mitigate | Copy `compare.tenant_key` into the existing top-level plan field and prove it in regressions so reviewed tenant scope stays auditable through serialization. |
</threat_model>

<verification>
Run the focused public promotion suites:
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/promotion/compare_test.exs`
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/promotion/compare_test.exs test/rulestead/promotion/apply_test.exs`
</verification>

<success_criteria>
`Rulestead.plan_promotion/3` no longer drops explicit `tenant_key`, the saved plan built from that path keeps the existing top-level tenant contract, and the regression suite proves the fix without touching Phase 33 compare drill-in behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/32-public-promotion-tenant-scope-closure/32-01-SUMMARY.md`
</output>
