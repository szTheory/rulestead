---
phase: 32-public-promotion-tenant-scope-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - rulestead/lib/rulestead.ex
  - rulestead/lib/mix/tasks/rulestead.promote.ex
  - rulestead/test/rulestead/promotion/apply_test.exs
  - rulestead/test/rulestead/store/promotion_apply_contract_test.exs
  - rulestead/test/rulestead/store/promotion_governed_apply_contract_test.exs
  - rulestead/test/rulestead/release_contract_test.exs
  - rulestead/test/rulestead/mix/tasks/rulestead_promote_test.exs
autonomous: true
requirements:
  - TEN-01
  - TEN-03
must_haves:
  truths:
    - "Saved promotion plans generated from the public API replay with the same explicit tenant scope through direct apply and governed protected-target handoff."
    - "Adapter parity proves the existing compare/apply/provenance contract remains intact for both Ecto and Fake stores after the Phase 32 seam fix."
    - "Programmatic Mix-task and release-contract regressions lock the public two-package runtime surface without introducing a new tenant CLI UX."
  artifacts:
    - path: "rulestead/lib/rulestead.ex"
      provides: "Promotion replay helpers that reconstruct direct and governed commands from the saved plan's existing `tenant_key` field"
      min_lines: 180
    - path: "rulestead/test/rulestead/store/promotion_apply_contract_test.exs"
      provides: "Direct apply adapter parity for tenant-scoped saved promotion plans"
      min_lines: 220
    - path: "rulestead/test/rulestead/store/promotion_governed_apply_contract_test.exs"
      provides: "Governed replay parity proving protected-target change requests keep tenant scope"
      min_lines: 80
    - path: "rulestead/test/rulestead/mix/tasks/rulestead_promote_test.exs"
      provides: "Programmatic promote task regressions for tenant-scoped plan/apply flows"
      min_lines: 120
    - path: "rulestead/test/rulestead/release_contract_test.exs"
      provides: "Release-surface lock proving Phase 32 did not widen the documented public API"
      min_lines: 220
  key_links:
    - from: "rulestead/lib/rulestead.ex"
      to: "rulestead/test/rulestead/store/promotion_apply_contract_test.exs"
      via: "`promotion_apply_command/3` rebuilds `tenant_key` from `plan[\"tenant_key\"]` for direct replay"
      pattern: "tenant_key|promotion_apply_command"
    - from: "rulestead/lib/rulestead.ex"
      to: "rulestead/test/rulestead/store/promotion_governed_apply_contract_test.exs"
      via: "`promotion_governance_command_payload/1` reuses the same top-level plan `tenant_key` in the protected-target change-request payload"
      pattern: "tenant_key|promotion_governance_command_payload|change_request"
    - from: "rulestead/lib/mix/tasks/rulestead.promote.ex"
      to: "rulestead/test/rulestead/mix/tasks/rulestead_promote_test.exs"
      via: "`compute_plan/3` and `compute_apply/2` stay thin wrappers around the fixed public `Rulestead` helpers"
      pattern: "compute_plan|compute_apply|tenant_key"
---

<objective>
Prove the fixed public saved plan replays with tenant scope intact across direct apply, governed replay, adapter parity, and programmatic release surfaces.

Purpose: Phase 32 is not complete when plan generation is fixed; the saved artifact must also remain authoritative through every shipped replay path that already depends on `plan["tenant_key"]`.
Output: replay/governance parity coverage, Mix-task regressions, and release-surface checks for the tenant-scoped public promotion flow.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.1.0-MILESTONE-AUDIT.md
@.planning/phases/32-public-promotion-tenant-scope-closure/32-RESEARCH.md
@.planning/phases/32-public-promotion-tenant-scope-closure/32-PATTERNS.md
@.planning/phases/31-audit-tenant-provenance-enforcement/31-01-PLAN.md
@rulestead/lib/rulestead.ex
@rulestead/lib/rulestead/promotion/apply.ex
@rulestead/lib/rulestead/store/ecto.ex
@rulestead/lib/rulestead/fake.ex
@rulestead/lib/mix/tasks/rulestead.promote.ex
@rulestead/test/rulestead/promotion/apply_test.exs
@rulestead/test/rulestead/store/promotion_apply_contract_test.exs
@rulestead/test/rulestead/store/promotion_governed_apply_contract_test.exs
@rulestead/test/rulestead/release_contract_test.exs
@rulestead/test/rulestead/mix/tasks/rulestead_promote_test.exs

<interfaces>
From `rulestead/lib/rulestead.ex`:
```elixir
Command.ApplyPromotion.new(%{
  source_environment_key: plan["source_environment_key"],
  target_environment_key: plan["target_environment_key"],
  tenant_key: plan["tenant_key"],
  flag_keys: plan["flag_keys"],
  compare_token: plan["compare_token"],
  ...
})
```

From `rulestead/lib/rulestead.ex`:
```elixir
%{
  "source_environment_key" => plan["source_environment_key"],
  "target_environment_key" => plan["target_environment_key"],
  "tenant_key" => plan["tenant_key"],
  "flag_keys" => plan["flag_keys"],
  "compare_token" => plan["compare_token"],
  ...
}
```

From `rulestead/lib/mix/tasks/rulestead.promote.ex`:
```elixir
def compute_plan(source_environment_key, target_environment_key, opts \\ []) do
  Rulestead.plan_promotion(source_environment_key, target_environment_key, opts)
end

def compute_apply(content, opts \\ []) do
  Rulestead.apply_promotion_plan(content, opts)
end
```

From `rulestead/lib/rulestead/store/ecto.ex`:
```elixir
command_snapshot: Command.GovernanceSupport.with_tenant_provenance(command.command)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove tenant-scoped saved plans replay identically through direct and governed apply</name>
  <files>rulestead/lib/rulestead.ex, rulestead/test/rulestead/promotion/apply_test.exs, rulestead/test/rulestead/store/promotion_apply_contract_test.exs, rulestead/test/rulestead/store/promotion_governed_apply_contract_test.exs</files>
  <behavior>
    - Test 1: direct saved-plan replay rebuilds `Command.ApplyPromotion` with the same `tenant_key` that was saved by `plan_promotion/3`.
    - Test 2: protected-target replay queues a change request whose `command["tenant_key"]` and `command["compare_token"]` match the saved plan.
    - Test 3: Fake and Ecto adapters persist the same tenant-scoped promotion contract and environment-version metadata when replaying a saved plan.
  </behavior>
  <action>Use the fixed public planning flow from `32-01` as the source artifact for replay tests. Extend `apply_test.exs`, `promotion_apply_contract_test.exs`, and `promotion_governed_apply_contract_test.exs` so they generate or consume a tenant-scoped public plan, then assert direct apply, governed apply, and adapter persistence all reuse the existing top-level `tenant_key` contract. Keep production changes in `rulestead/lib/rulestead.ex` limited to concrete replay gaps exposed by these regressions in `promotion_apply_command/3`, `dispatch_promotion_plan/4`, or `promotion_governance_command_payload/1`; do not invent a second tenant provenance block or widen the replay API.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/promotion/apply_test.exs test/rulestead/store/promotion_apply_contract_test.exs test/rulestead/store/promotion_governed_apply_contract_test.exs</automated>
  </verify>
  <done>Tenant-scoped saved plans replay identically through direct and governed paths, and contract suites prove Fake/Ecto parity on the existing plan/apply/provenance boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock Mix-task and release-surface regressions around the tenant-scoped public flow</name>
  <files>rulestead/lib/mix/tasks/rulestead.promote.ex, rulestead/test/rulestead/mix/tasks/rulestead_promote_test.exs, rulestead/test/rulestead/release_contract_test.exs</files>
  <behavior>
    - Test 1: `Mix.Tasks.Rulestead.Promote.compute_plan/3` returns a saved plan whose `tenant_key` matches the explicit public plan input.
    - Test 2: `compute_apply/2` preserves the same tenant scope through direct or governed replay without adding a new CLI switch.
    - Test 3: release-contract assertions prove Phase 32 did not widen the locked public API or mutate the bounded runtime surface unexpectedly.
  </behavior>
  <action>Keep the Mix task as a thin programmatic wrapper over `Rulestead.plan_promotion/3` and `Rulestead.apply_promotion_plan/2`. Extend `rulestead_promote_test.exs` to pass `tenant_key:` through `compute_plan/3` and verify the saved plan and queued change request keep that scope. Use `release_contract_test.exs` only to lock the existing public catalog and any explicitly documented metadata keys affected by this replay path; do not add human-facing CLI tenant UX or broader release-prep work. Production edits to `rulestead.promote.ex` are only allowed if the test exposes a concrete wrapper mismatch.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/mix/tasks/rulestead_promote_test.exs test/rulestead/release_contract_test.exs</automated>
  </verify>
  <done>Programmatic promote task coverage and release-contract checks prove the tenant-scoped public flow is locked on the shipped `rulestead` surface without adding new CLI or admin scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved plan artifact -> replay command | Reviewed tenant scope becomes a new direct or governed mutation command here |
| replay command -> adapter persistence | Tenant scope must remain intact across Fake/Ecto environment-version and change-request persistence |
| public `Rulestead` helpers -> Mix wrapper | Programmatic task entrypoints must not drift from the fixed public contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | T | `rulestead/lib/rulestead.ex` replay helpers | mitigate | Rebuild direct and governed commands only from the saved plan’s existing top-level `tenant_key` and compare fields, then verify parity in direct/governed tests. |
| T-32-04 | R | adapter and Mix-task replay surfaces | mitigate | Add contract and programmatic wrapper regressions proving Fake/Ecto persistence and Mix wrappers preserve the same tenant-scoped reviewed artifact without surface drift. |
</threat_model>

<verification>
Run the replay and release-targeted suites:
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/promotion/apply_test.exs test/rulestead/store/promotion_apply_contract_test.exs test/rulestead/store/promotion_governed_apply_contract_test.exs`
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/mix/tasks/rulestead_promote_test.exs test/rulestead/release_contract_test.exs`
</verification>

<success_criteria>
Saved promotion plans generated from the public API carry tenant scope all the way through direct apply, governed replay, adapter persistence, and programmatic Mix entrypoints, while the public runtime surface remains bounded and Phase 33 compare-drill-in work stays excluded.
</success_criteria>

<output>
After completion, create `.planning/phases/32-public-promotion-tenant-scope-closure/32-02-SUMMARY.md`
</output>
