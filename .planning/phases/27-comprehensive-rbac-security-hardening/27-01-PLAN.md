---
phase: 27-comprehensive-rbac-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/admin/policy.ex
  - rulestead/lib/rulestead/admin/authorizer.ex
  - rulestead/lib/rulestead/admin/**/*.ex
  - rulestead/test/rulestead/admin_security_contract_test.exs
  - rulestead/test/rulestead/admin_governance_policy_test.exs
  - rulestead/test/rulestead/release_contract_test.exs
autonomous: true
requirements:
  - SEC-01
  - SEC-02

must_haves:
  truths:
    - "The only canonical product roles are Viewer, Editor, and Admin."
    - "Authorization stays centered on the existing host-owned `Rulestead.Admin.Policy.can?/4` seam."
    - "Legacy fallback roles become temporary compatibility aliases instead of permanent public truth."
    - "Backend authorization can project a closed capability vocabulary that downstream code and UI can reuse."
  artifacts:
    - path: "rulestead/lib/rulestead/admin/policy.ex"
      provides: "Stable policy seam plus bounded RBAC vocabulary"
    - path: "rulestead/lib/rulestead/admin/authorizer.ex"
      provides: "Canonical role normalization, compatibility aliasing, capability projection, and fail-closed enforcement helpers"
  key_links:
    - from: "rulestead/lib/rulestead/admin/policy.ex"
      to: "rulestead/lib/rulestead/admin/authorizer.ex"
      via: "stable `can?/4` contract plus bounded action/resource/environment normalization"
    - from: "rulestead/lib/rulestead/admin/authorizer.ex"
      to: "rulestead/test/rulestead/admin_security_contract_test.exs"
      via: "canonical role mapping, deny payloads, and compatibility fallback coverage"
---

<objective>
Establish the Phase 27 RBAC contract: one closed authorization vocabulary, one canonical three-role model, and one reusable capability projection layered on the existing host-owned policy seam.

Purpose: The current code still treats legacy fallback role names as implied product truth. Phase 27 must make Viewer / Editor / Admin the only public vocabulary while preserving the current pure-Elixir host integration seam.
Output: a bounded RBAC vocabulary in the admin seam, authorizer support for canonical role/capability normalization, temporary compatibility aliases for legacy role inputs, and regression tests that lock the contract before broader enforcement and UI work lands.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-comprehensive-rbac-security-hardening/27-CONTEXT.md
@.planning/phases/27-comprehensive-rbac-security-hardening/27-RESEARCH.md
@.planning/phases/27-comprehensive-rbac-security-hardening/27-PATTERNS.md
@rulestead/lib/rulestead/admin/policy.ex
@rulestead/lib/rulestead/admin/authorizer.ex
@rulestead/test/rulestead/admin_security_contract_test.exs
@rulestead/test/rulestead/admin_governance_policy_test.exs

<interfaces>
From `rulestead/lib/rulestead/admin/policy.ex`:
```elixir
@callback can?(actor :: term(), action :: atom(), resource :: term(), environment_key :: String.t() | atom() | nil) :: boolean()
```

From `rulestead/lib/rulestead/admin/authorizer.ex`:
```elixir
@viewer_roles ~w(viewer auditor operator engineer admin incident_commander prod_operator)a
@editor_roles ~w(operator engineer admin incident_commander prod_operator)a
@production_roles ~w(admin incident_commander prod_operator)a
```

Locked Phase 27 contract:
```text
Viewer / Editor / Admin are the only canonical public roles; legacy names may survive only as temporary compatibility mapping inside the backend seam.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce the closed RBAC vocabulary on the stable policy seam</name>
  <files>rulestead/lib/rulestead/admin/policy.ex, rulestead/lib/rulestead/admin/authorizer.ex</files>
  <action>
    Extend the existing admin seam so the codebase has one explicit action/resource/environment vocabulary for admin authorization decisions. Keep `Rulestead.Admin.Policy.can?/4` as the stable host callback and keep any new helpers additive or internal. Add any bounded types, helper functions, or internal modules needed so downstream code can ask for normalized capability truth without inventing per-screen enums. Do not introduce a DSL, macro policy framework, custom role builder, or any dependency beyond the current pure Elixir seam.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/admin_security_contract_test.exs test/rulestead/admin_governance_policy_test.exs</automated>
  </verify>
  <done>The repo has a single bounded authorization vocabulary that can be shared across backend enforcement and mounted-admin capability projection while preserving `can?/4` as the stable host contract.</done>
</task>

<task type="auto">
  <name>Task 2: Canonicalize Viewer / Editor / Admin and demote legacy role names to compatibility aliases</name>
  <files>rulestead/lib/rulestead/admin/authorizer.ex, rulestead/test/rulestead/admin_security_contract_test.exs, rulestead/test/rulestead/admin_governance_policy_test.exs, rulestead/test/rulestead/release_contract_test.exs</files>
  <action>
    Refactor fallback authorization so canonical truth is exactly Viewer, Editor, and Admin. Legacy names like `auditor`, `operator`, `engineer`, `incident_commander`, and `prod_operator` may remain accepted as compatibility inputs during transition, but only inside normalization logic and regression tests. Add or extend contract tests to prove that the canonical role model is public truth, compatibility aliases stay bounded and temporary, and missing or malformed policy input fails closed instead of widening access.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/admin_security_contract_test.exs test/rulestead/admin_governance_policy_test.exs test/rulestead/release_contract_test.exs</automated>
  </verify>
  <done>Fallback authorization now teaches one canonical three-role model while preserving backward-compatible host inputs only through explicit normalization.</done>
</task>

<task type="auto">
  <name>Task 3: Add reusable capability projection helpers for later enforcement and UI slices</name>
  <files>rulestead/lib/rulestead/admin/**/*.ex, rulestead/test/rulestead/admin_security_contract_test.exs</files>
  <action>
    Add a small, reusable backend capability projection that can answer questions such as read allowed, execute allowed, proposal-only, admin-only, and environment/protected-route posture from the same normalized actor/resource/environment inputs used by authorization. Keep the projection internal-to-core or additive; it exists to prevent UI/backend drift in later slices, not to widen the public API. Preserve fail-closed behavior and keep deny reason codes bounded and machine-usable for later UI copy mapping.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/admin_security_contract_test.exs</automated>
  </verify>
  <done>Core authorization can project stable capability truth that later plans can consume without duplicating role semantics in LiveViews or docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host actor/session data -> authorizer normalization | Untrusted actor attributes become authorization input here |
| policy callback -> capability projection | Host policy decisions are translated into reusable backend/UI truth here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | E | legacy fallback role handling | mitigate | Move canonical truth to Viewer / Editor / Admin and keep legacy names as explicit compatibility aliases only. |
| T-27-01-02 | S | malformed actor input | mitigate | Normalize actor/resource/environment centrally and fail closed on missing or invalid data. |
| T-27-01-03 | T | per-screen vocabulary drift | mitigate | Introduce one closed capability vocabulary in the backend seam before UI code consumes it. |
</threat_model>

<verification>
Run the focused admin security, governance policy, and release-contract suites in `rulestead`. Confirm the repo now exposes one bounded RBAC vocabulary, treats Viewer / Editor / Admin as the only canonical public roles, preserves the existing host-owned `can?/4` seam, and keeps malformed or missing policy data fail-closed.
</verification>

<success_criteria>
Phase 27 has a stable RBAC contract layer that downstream enforcement, mounted-admin UX, and docs can all reuse without reintroducing legacy role names as product truth or adding any third-party authorization framework.
</success_criteria>

<output>
After completion, create `.planning/phases/27-comprehensive-rbac-security-hardening/27-01-SUMMARY.md`
</output>
