---
phase: 60-proof-docs-and-support-truth
plan: 60-02
type: docs
wave: 2
depends_on:
  - 60-01
files_modified:
  - rulestead/test/rulestead/release_contract_test.exs
  - README.md
  - rulestead/README.md
  - rulestead_admin/README.md
  - MAINTAINING.md
autonomous: true
requirements:
  - VER-02
must_haves:
  truths:
    - release_contract_test.exs enforces blast radius governance support truth across all maintainer docs
    - Root README Proof today adds v1.7 entry alongside v1.6 verify.phase56
    - Package READMEs describe core vs admin boundary and mounted companion posture
  artifacts:
    - release_contract_test.exs (new test block)
    - README.md, rulestead/README.md, rulestead_admin/README.md, MAINTAINING.md
---

# Plan 60-02: Release Contract Drift Guards And Support Truth READMEs

<objective>
Extend `release_contract_test.exs` with a blast-radius governance support-truth block and sync root/package READMEs plus MAINTAINING.md so public docs match bounded v1.7 scope.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-60-04: Docs claim observability-backed population counts | high | Forbidden phrases + preview basis / explicit samples language |
| T-60-05: Docs imply standalone admin product | high | Assert "mounted companion" / "not a standalone"; forbid standalone phrases |
| T-60-06: Removing verify.phase56 breaks v1.6 story | medium | Add v1.7 entry alongside phase56, never replace |
</threat_model>

<tasks>

<task id="60-02-01" type="execute">
  <name>Add blast radius governance support truth test block</name>
  <read_first>
    - rulestead/test/rulestead/release_contract_test.exs (reusable targeting block ~L338)
    - .planning/phases/60-proof-docs-and-support-truth/60-CONTEXT.md (D-02)
  </read_first>
  <action>
Add test after the reusable targeting block in `release_contract_test.exs`:

**Test name:** `"blast radius governance support truth stays bounded across root package and maintainer docs"`

**Assert root README contains:**
- `mix verify.phase60`
- `blast radius` (case-insensitive match via `=~`)
- `threshold`
- `protected environment` or `protected-environment`
- `change request`
- `fail closed`
- `host-owned` or `host-owned policy`
- `preview basis` or `explicit samples`
- `RULESTEAD_TEST_SCOPE=blast_radius_governance bash scripts/ci/test.sh`
- `mix verify.phase56` (v1.6 entry preserved)

**Assert rulestead/README.md contains:**
- `mix verify.phase60`
- `domain`, `validation`, `contracts`
- `fail closed`
- `host-owned`

**Assert rulestead_admin/README.md contains:**
- `mounted companion` or `mounted presentation`
- `not a standalone`
- `change request` or `governance`

**Assert MAINTAINING.md contains:**
- `Blast Radius Governance Proof` (section heading)
- `mix verify.phase60`
- `VER-01`
- `RULESTEAD_TEST_SCOPE=blast_radius_governance bash scripts/ci/test.sh`
- References to `.planning/phases/57-blast-radius-threshold-contract/` or `57-` handoff

**forbidden_phrases** (refute in root_readme, admin_readme, maintaining):
```
"standalone rulestead_admin",
"standalone control plane",
"parallel governance workflow",
"authoritative affected-user",
"real user population",
"built-in observability",
"Rulestead dashboard",
"metrics ingestion",
"automatic progressive delivery platform",
"auto-advance guarded rollouts"
```
Reuse existing forbidden phrase patterns from reusable targeting block where applicable.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/release_contract_test.exs --only line:338 2>/dev/null || cd rulestead && mix test test/rulestead/release_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test block name contains `blast radius governance support truth`
    - Test asserts `mix verify.phase60` in root_readme
    - Test asserts `mix verify.phase56` still in root_readme
    - `mix test test/rulestead/release_contract_test.exs` exits 0 after doc updates in 60-02-02
  </acceptance_criteria>
</task>

<task id="60-02-02" type="execute">
  <name>Update README and MAINTAINING support truth</name>
  <read_first>
    - README.md (Proof today section ~L148)
    - rulestead/README.md
    - rulestead_admin/README.md
    - MAINTAINING.md (Reusable Targeting Deepening Proof section as template)
    - .planning/phases/60-proof-docs-and-support-truth/60-CONTEXT.md (D-02)
  </read_first>
  <action>
**README.md — Proof today section:** Add new bullet after reusable targeting deepening entry:

```markdown
- **Blast radius governance (v1.7):** `cd rulestead && mix verify.phase60`
  proves threshold evaluation in protected environments, change-request
  proposal and execute for high-blast-radius audience mutations, stale-preview
  rejection, fail-closed behavior on missing or indeterminate inputs, and audit
  evidence. Previews use **preview basis** (authored references and **explicit
  samples** only) — they do not claim affected-user or population counts.
  Protected-environment mutations above threshold route through **change request**
  review; below-threshold mutations remain eligible for direct apply with fresh
  preview confirmation. **Host-owned policy** governs authorization; the mounted
  companion presents core truth — not a standalone admin product.
- `RULESTEAD_TEST_SCOPE=blast_radius_governance bash scripts/ci/test.sh`
  reruns the v1.7 blast radius governance proof bar in CI.
```

Do NOT remove existing `verify.phase56` or reusable targeting bullets.

**rulestead/README.md:** Add proof command reference and governance vocabulary in appropriate section (proof or governance subsection).

**rulestead_admin/README.md:** Add note that admin owns mounted presentation of blast-radius governance UX; core owns domain + validation.

**MAINTAINING.md:** Add section `## Blast Radius Governance Proof` with:
- Bounded scope description
- Commands: `mix verify.phase60`, `RULESTEAD_TEST_SCOPE=blast_radius_governance bash scripts/ci/test.sh`
- Upstream handoff refs: `.planning/phases/57-blast-radius-threshold-contract/`, `58-change-request-integration/`, `59-mounted-governance-workflows/`
- VER-01 requirement trace
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/release_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `mix verify.phase60` and `blast radius governance (v1.7)`
    - README.md still contains `mix verify.phase56`
    - MAINTAINING.md contains `## Blast Radius Governance Proof`
    - `mix test test/rulestead/release_contract_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead && mix test test/rulestead/release_contract_test.exs
grep -q 'mix verify.phase60' README.md
grep -q 'mix verify.phase56' README.md
grep -q 'Blast Radius Governance Proof' MAINTAINING.md
```
</verification>

<success_criteria>
- VER-02: Public docs and release-contract checks describe threshold semantics, protected-environment behavior, and host-owned policy consistently
- Linked-version sibling-package release model unchanged in all touched docs
</success_criteria>
