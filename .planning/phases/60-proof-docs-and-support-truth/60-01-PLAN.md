---
phase: 60-proof-docs-and-support-truth
plan: 60-01
type: verification
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/mix/tasks/verify.phase60.ex
  - rulestead/mix.exs
autonomous: true
requirements:
  - VER-01
must_haves:
  truths:
    - mix verify.phase60 runs v1.6 core union plus v1.7 governance delta in one maintainer command
    - Admin subprocess covers phase56 admin paths plus governance components, route contract, and CR show tests
    - Task does not delegate to verify.phase56 or other sub-tasks (no duplicate runs)
  artifacts:
    - rulestead/lib/mix/tasks/verify.phase60.ex
    - rulestead/mix.exs (preferred_envs entry)
---

# Plan 60-01: mix verify.phase60 Merge Gate

<objective>
Add `Mix.Tasks.Verify.Phase60` as the v1.7 merge gate — flat union of phase56 core tests plus governance delta, with admin subprocess matching Phase 59 proof scope.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-60-01: Delegating to verify.phase56 duplicates test runs | medium | Flat union only; never call sub-tasks |
| T-60-02: Missing governance tests leaves VER-01 unproven | high | Explicit delta paths from CONTEXT D-01 |
| T-60-03: Admin glob omits CR show or route contract | high | Add explicit paths outside audience_live/ directory |
</threat_model>

<tasks>

<task id="60-01-01" type="execute">
  <name>Create Mix.Tasks.Verify.Phase60</name>
  <read_first>
    - rulestead/lib/mix/tasks/verify.phase56.ex
    - .planning/phases/60-proof-docs-and-support-truth/60-CONTEXT.md (D-01)
    - .planning/phases/59-mounted-governance-workflows/59-VERIFICATION.md (Automated proof section)
  </read_first>
  <action>
Create `rulestead/lib/mix/tasks/verify.phase60.ex` mirroring `verify.phase56.ex` structure:

**Module:** `Mix.Tasks.Verify.Phase60`  
**@shortdoc:** `"Runs Phase 60 blast radius governance verification suite"`

**Comment at top:** Union of verify.phase56 core + v1.7 governance delta — do not call verify.phase56 or other sub-tasks (avoids duplicate runs).

**@phase60_core_tests** — all 17 paths from `@phase56_core_tests` in verify.phase56.ex PLUS:
```
"test/rulestead/governance/blast_radius_threshold_test.exs",
"test/rulestead/governance/audience_mutation_change_request_test.exs",
"test/rulestead/governance/audience_mutation_change_request_contract_test.exs",
"test/rulestead/governance/change_request_contract_test.exs",
"test/rulestead/admin_governance_policy_test.exs"
```

**@admin_test_paths** — all 8 entries from `@admin_test_paths` in verify.phase56.ex (includes `test/rulestead_admin/live/audience_live` directory glob) PLUS these paths outside that directory:
```
"test/rulestead_admin/components/governance_components_test.exs",
"test/rulestead_admin/live/governance_route_contract_test.exs",
"test/rulestead_admin/live/change_request_live/show_test.exs"
```

**run/1:** Same pattern as phase56 — `Mix.Task.run("test", @phase60_core_tests)` then admin subprocess via `Path.expand("../../../../rulestead_admin", __DIR__)`.
  </action>
  <verify>
    <automated>cd rulestead && mix verify.phase60</automated>
  </verify>
  <acceptance_criteria>
    - File contains `defmodule Mix.Tasks.Verify.Phase60`
    - File contains all 5 governance delta core test paths
    - File contains `governance_components_test.exs`, `governance_route_contract_test.exs`, `change_request_live/show_test.exs`
    - File does NOT contain `verify.phase56` or `Mix.Task.run("verify.phase56"`
    - `mix verify.phase60` exits 0
  </acceptance_criteria>
</task>

<task id="60-01-02" type="execute">
  <name>Register verify.phase60 in mix.exs preferred_envs</name>
  <read_first>
    - rulestead/mix.exs (preferred_envs line ~23)
  </read_first>
  <action>
In `rulestead/mix.exs`, extend `preferred_envs` tuple to include:
```elixir
{:"verify.phase60", :test}
```
alongside existing verify.phase54, verify.phase55, verify.phase56 entries.
  </action>
  <verify>
    <automated>grep -q 'verify.phase60' rulestead/mix.exs && cd rulestead && mix help verify.phase60</automated>
  </verify>
  <acceptance_criteria>
    - `rulestead/mix.exs` contains `{:"verify.phase60", :test}`
    - `mix help verify.phase60` shows task without error
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead && mix verify.phase60
grep -q 'verify.phase60' rulestead/mix.exs
! grep -q 'verify.phase56' rulestead/lib/mix/tasks/verify.phase60.ex
```
</verification>

<success_criteria>
- VER-01: Repo-local proof covers threshold, CR, stale-preview, fail-closed, and audit evidence in one command
- `mix verify.phase60` is the v1.7 superset; `mix verify.phase56` remains valid for v1.6 regression
</success_criteria>
