---
phase: 60-proof-docs-and-support-truth
plan: 60-03
type: docs
wave: 2
depends_on:
  - 60-01
files_modified:
  - guides/flows/admin-ui.md
  - guides/flows/multi-env.md
  - guides/introduction/getting-started.md
  - guides/introduction/user-flows-and-jtbd.md
  - README.md
  - rulestead/test/rulestead/release_contract_test.exs
autonomous: true
requirements:
  - VER-02
  - VER-03
must_haves:
  truths:
    - admin-ui.md teaches governed audience mutation workflow in protected environments
    - multi-env.md teaches threshold routing and fail-closed behavior
    - getting-started.md and README lead with payload-first Rulestead.evaluate/3 pattern
  artifacts:
    - guides/flows/admin-ui.md (governance section)
    - guides/flows/multi-env.md (protected environment section)
    - guides/introduction/getting-started.md (payload-first quickstart)
---

# Plan 60-03: Flow Guide Updates And Quickstart API Parity

<objective>
Update in-place flow guides for blast-radius governance operator workflows and restore payload-first quickstart parity consistent with `guides/flows/evaluation.md`.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-60-07: Docs teach conn-only API contradicting evaluation.md | medium | Payload-first primary example; conn as secondary convenience |
| T-60-08: Docs imply population counts from preview | high | Explicit preview-basis limits in admin-ui and multi-env |
| T-60-09: New standalone governance flow doc widens scope | low | In-place updates only per CONTEXT D-03 |
</threat_model>

<tasks>

<task id="60-03-01" type="execute">
  <name>Extend admin-ui.md and multi-env.md for governance</name>
  <read_first>
    - guides/flows/admin-ui.md
    - guides/flows/multi-env.md
    - .planning/phases/59-mounted-governance-workflows/59-CONTEXT.md
    - .planning/phases/60-proof-docs-and-support-truth/60-CONTEXT.md (D-03)
    - prompts/rulestead-admin-ux-and-operator-ia.md
  </read_first>
  <action>
**guides/flows/admin-ui.md** — Add section `## Blast radius governance in protected environments` covering:
1. Operator flow: preview impact → assess threshold → below threshold: Continue to confirm (direct apply) → above threshold: Continue to submit (change request) → review on CR show → execute
2. Blocked states: indeterminate assessment, partial visibility, stale preview — show "Cannot evaluate safely" / back only
3. Blast-radius panel shows verdict, threshold line, breach reasons — never audience definition/predicate
4. Preview basis limits: authored references + explicit samples only; no affected-user counts
5. Host-owned policy: `Rulestead.Admin.Policy` / Authorizer governs who can submit and approve

**guides/flows/multi-env.md** — Add section `## Protected environments and blast radius thresholds` covering:
1. Protected environments (e.g. production) trigger threshold evaluation on audience mutations
2. Direct apply vs change-request routing based on threshold verdict
3. Fail-closed when preview fingerprint stale, dependency truth unresolved, or required inputs missing
4. Same preview-basis limits as admin-ui section

**Optional:** If cross-role mental model needs it, add one paragraph to `guides/introduction/user-flows-and-jtbd.md` under operator JTBD describing blast-radius governance — skip if admin-ui section is sufficient.
  </action>
  <verify>
    <automated>grep -q 'change request' guides/flows/admin-ui.md && grep -q 'protected' guides/flows/multi-env.md && grep -q 'preview basis' guides/flows/admin-ui.md</automated>
  </verify>
  <acceptance_criteria>
    - `admin-ui.md` contains heading or section with `blast radius` or `governance`
    - `admin-ui.md` contains `change request` and `preview basis`
    - `multi-env.md` contains `fail closed` or `fail-closed`
    - `multi-env.md` contains `threshold`
  </acceptance_criteria>
</task>

<task id="60-03-02" type="execute">
  <name>Fix quickstart payload-first parity</name>
  <read_first>
    - guides/flows/evaluation.md (canonical API)
    - guides/introduction/getting-started.md
    - README.md (evaluation/quickstart section)
    - .planning/phases/60-proof-docs-and-support-truth/60-CONTEXT.md (D-04)
  </read_first>
  <action>
**guides/introduction/getting-started.md** — Restructure section "## 3. Gate a code path":

1. **Primary example (payload-first):**
```elixir
context =
  Rulestead.Context.new(
    environment: "production",
    targeting_key: "user-123",
    traits: %{plan: :pro}
  )

flag_payload = ... # from snapshot or store

with {:ok, result} <- Rulestead.evaluate(flag_payload, context) do
  if result.enabled? do
    render_v2(conn)
  else
    render_v1(conn)
  end
end
```

2. **Secondary note:** When using Phoenix with the snapshot cache, `Rulestead.enabled?("checkout_v2", conn)` and `Rulestead.get_variant/2` on `%Plug.Conn{}` are convenience wrappers — the explicit contract is payload + `%Rulestead.Context{}` per `guides/flows/evaluation.md`.

3. Keep conn-based example but move below payload-first block with "Convenience wrappers" subheading.

**README.md** — Ensure quickstart/evaluation section references `Rulestead.evaluate/3` and explicit context (align with evaluation.md if README has a code example).

**release_contract_test.exs** — Add asserts to existing quickstart/evaluation block OR add small test `"quickstart teaches payload-first evaluation"` asserting README and getting-started contain:
- `Rulestead.evaluate/3` or `Rulestead.evaluate(`
- `Rulestead.Context.new` or `%Rulestead.Context{}`
- `payload` or `flag_payload` (payload-first language)
  </action>
  <verify>
    <automated>grep -q 'Rulestead.evaluate' guides/introduction/getting-started.md && grep -q 'Rulestead.Context' guides/introduction/getting-started.md && cd rulestead && mix test test/rulestead/release_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `getting-started.md` contains `Rulestead.evaluate` before or above conn-only `enabled?` example
    - `getting-started.md` contains `Rulestead.Context.new` or `%Rulestead.Context{}`
    - `getting-started.md` contains note about conn convenience wrappers
    - `mix test test/rulestead/release_contract_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
grep -q 'blast radius\|governance' guides/flows/admin-ui.md
grep -q 'threshold' guides/flows/multi-env.md
grep -q 'Rulestead.evaluate' guides/introduction/getting-started.md
cd rulestead && mix test test/rulestead/release_contract_test.exs
```
</verification>

<success_criteria>
- VER-02: Flow guides describe threshold semantics and host-owned policy consistently
- VER-03: README and getting-started teach payload-first evaluation consistent with evaluation.md
</success_criteria>
