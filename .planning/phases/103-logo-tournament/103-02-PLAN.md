---
phase: 103
plan: 02
wave: 2
autonomous: false
objective: "Round 2: six variants of sole survivor A3 (baseline route thread), rendered as per-survivor context sheets (light/dark/mono, 36px admin strip, 16px favicon row), then maintainer checkpoint."
files_modified:
  - .planning/phases/103-logo-tournament/candidates/a3-*.svg
  - .planning/phases/103-logo-tournament/round-2-studio.html
  - .planning/phases/103-logo-tournament/103-TOURNAMENT.md
---

# Plan 103-02 — Tournament Round 2 (A3 variants)

## Maintainer brief (verbatim, R1 checkpoint)
"this direction is really cool, would love to see variants but in general it looks great!"

## Variant axes (6 candidates)
- **a3-1 refined original** — same architecture, tuned trace weight / corner radii / node spacing.
- **a3-2 single lit node** — trace rises through the d stem and exits to ONE copper node (calmer, favicon-friendlier).
- **a3-3 R-anchored entry** — trace originates from the R's leg (not under it), still threads baseline to the d.
- **a3-4 partial route** — trace starts mid-word (under "s" / "st" seam) to reduce bracket/cradle silhouette and width.
- **a3-5 copper trace** — route itself in Ember Copper (the "moment of change" is the path, nodes muted).
- **a3-6 fanned exit** — three stubs become a small fan/fork off the d stem top (closer to G4c geometry).

## Tasks
1. Produce the 6 variant SVGs (continue Axis A design agent — context intact) with the same
   technique/palette constraints (R-07/R-08). Self-render + iterate.
2. Assemble round-2-studio.html: per-variant context block — 480px light card, dark card,
   monochrome row (#0F1720 single-fill), 36px admin header strip, 16px favicon-crop row
   (the d-stem + node element). Render to round-2-sheet.png.
3. CHECKPOINT (autonomous: false): maintainer keep/cut + feedback per variant → log verbatim;
   ITERATE → 103-03-PLAN.md; WINNER → terminal plan + 103-WINNER.md.
