---
phase: 21-infrastructure-observability-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/runtime/health.ex
  - rulestead/lib/rulestead/runtime/diagnostics.ex
  - rulestead/lib/rulestead/runtime/refresh.ex
  - rulestead/lib/rulestead.ex
  - rulestead/test/rulestead/runtime/diagnostics_test.exs
  - rulestead/test/rulestead/runtime/health_test.exs
  - rulestead/test/rulestead/runtime/health_telemetry_test.exs
  - rulestead/test/rulestead/telemetry_test.exs
autonomous: true
requirements:
  - INF-01
  - INF-02
must_haves:
  truths:
    - "Operators can inspect a truthful current-node infrastructure health snapshot without the library claiming undiscovered cluster peers."
    - "Each environment reports cache freshness, sync latency, and connection-health signals derived from bounded runtime state instead of UI guesses."
    - "Phase 20 invalidation telemetry remains intact while any Phase 21 requirement-name compatibility is additive, not a rename."
    - "A host can provide peer health data explicitly later, but Phase 21 ships useful diagnostics without inventing cluster membership or discovery."
  artifacts:
    - path: "rulestead/lib/rulestead/runtime/health.ex"
      provides: "Bounded infrastructure health projection with current-node default scope and optional host-provided peer seam"
    - path: "rulestead/lib/rulestead/runtime/diagnostics.ex"
      provides: "Expanded diagnostics entrypoint that composes runtime health data for admin consumption"
    - path: "rulestead/lib/rulestead/runtime/refresh.ex"
      provides: "Additive invalidation compatibility telemetry emitted alongside the Phase 20 runtime invalidation family"
    - path: "rulestead/test/rulestead/runtime/health_test.exs"
      provides: "Executable lock on sync-latency math, topology-scope truthfulness, and adapter health projection"
    - path: "rulestead/test/rulestead/runtime/health_telemetry_test.exs"
      provides: "Executable lock on additive alias telemetry semantics"
  key_links:
    - from: "rulestead/lib/rulestead/runtime/health.ex"
      to: "rulestead/lib/rulestead/runtime/diagnostics.ex"
      via: "diagnostics compose cache metadata, refresh worker status, and optional peer input into one UI-safe snapshot"
      pattern: "sync_latency_ms|topology_scope|adapter_health"
    - from: "rulestead/lib/rulestead/runtime/refresh.ex"
      to: "rulestead/test/rulestead/telemetry_test.exs"
      via: "existing Phase 20 invalidation events stay emitted while alias events are asserted as additive"
      pattern: "runtime.*invalidation|cache.*invalidation|sync.*delta_received"
    - from: "rulestead/lib/rulestead.ex"
      to: "rulestead_admin/lib/rulestead_admin/live/diagnostics_live/index.ex"
      via: "admin UI loads the new bounded health snapshot through a stable public facade"
      pattern: "infrastructure_health|diagnostics"
---

<objective>
Ship the core infrastructure health projection and telemetry compatibility layer that Phase 21 depends on.

Purpose: Phase 20 already gives the repo node-local cache metadata and invalidation telemetry, but operators still lack a stable health snapshot that the admin UI can render honestly. This plan adds that projection without inventing peer discovery and preserves the shipped invalidation telemetry contract per the phase constraints.
Output: A bounded current-node health API, optional explicit peer seam, sync-latency and adapter-health projection, and additive telemetry aliases for INF-02.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v0.5.0-ROADMAP.md
@.planning/milestones/v0.5.0-REQUIREMENTS.md
@.planning/phases/20-pubsub-distributed-invalidation/20-01-PLAN.md
@.planning/phases/20-pubsub-distributed-invalidation/20-02-PLAN.md
@.planning/phases/20-pubsub-distributed-invalidation/20-03-SUMMARY.md
@.planning/phases/21-infrastructure-observability-ui/21-RESEARCH.md
@.planning/phases/21-infrastructure-observability-ui/21-PATTERNS.md
@prompts/rulestead-telemetry-observability-and-audit.md
@prompts/rulestead-testing-and-e2e-strategy.md
@prompts/rulestead-engineering-dna-from-prior-libs.md
@rulestead/lib/rulestead/runtime/diagnostics.ex
@rulestead/lib/rulestead/runtime/cache.ex
@rulestead/lib/rulestead/runtime/refresh.ex
@rulestead/test/rulestead/runtime/diagnostics_test.exs
@rulestead/test/rulestead/runtime/cluster_refresh_test.exs
@rulestead/test/rulestead/telemetry_test.exs

<interfaces>
From rulestead/lib/rulestead/runtime/diagnostics.ex:
```elixir
@spec current() :: map()
def current do
  %{
    node: node(),
    environments: Cache.diagnostics()
  }
end
```

From rulestead/lib/rulestead/runtime/cache.ex:
```elixir
@spec runtime_metadata(String.t() | atom()) :: {:ok, map()} | {:error, Rulestead.Error.t()}
@spec diagnostics() :: [map()]
@spec cache_age_ms(String.t() | atom()) :: {:ok, non_neg_integer() | nil} | {:error, Rulestead.Error.t()}
```

From rulestead/lib/rulestead/runtime/refresh.ex:
```elixir
@spec status(GenServer.server()) :: map()
def status(server), do: GenServer.call(server, :status)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a bounded infrastructure health snapshot with truthful topology scope</name>
  <files>rulestead/lib/rulestead/runtime/health.ex, rulestead/lib/rulestead/runtime/diagnostics.ex, rulestead/lib/rulestead.ex, rulestead/test/rulestead/runtime/diagnostics_test.exs, rulestead/test/rulestead/runtime/health_test.exs</files>
  <behavior>
    - Test 1: current-node health reports `cache_age_ms`, `sync_latency_ms`, `refresh_status`, `snapshot_version`, and adapter-health rows for each environment from bounded runtime state.
    - Test 2: the default topology scope is explicitly current-node only, and optional peer input stays an explicit host-provided seam instead of hidden discovery.
    - Test 3: the public facade returns the same bounded projection as the runtime module and omits raw snapshot payloads, raw context, or direct ETS/GenServer internals.
  </behavior>
  <action>Create `Rulestead.Runtime.Health` as the Phase 21 projection seam. It should compose the existing cache metadata from `Rulestead.Runtime.Cache`, derive `sync_latency_ms` from `published_at` and `applied_at`, and normalize connection-health signals for the configured Repo, Redis, and PubSub paths without turning diagnostics into an unbounded probe loop. Mark the default output as current-node scope and accept optional host-provided peer snapshots as a plain input seam only, per the phase constraint forbidding a new discovery service. Extend `Rulestead.Runtime.Diagnostics` and the public `Rulestead` facade so the admin package can load this snapshot through a stable API instead of reaching into ETS or refresh workers directly. Update the existing diagnostics test for facade parity and add focused health tests for truthfulness, freshness math, and bounded metadata.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/runtime/diagnostics_test.exs test/rulestead/runtime/health_test.exs -x</automated>
  </verify>
  <done>Rulestead exposes a stable, UI-safe infrastructure health snapshot that is honest about node scope, reports freshness and sync latency, and keeps peer data optional and host-owned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add INF-02 telemetry compatibility without renaming the shipped Phase 20 event family</name>
  <files>rulestead/lib/rulestead/runtime/refresh.ex, rulestead/test/rulestead/runtime/health_telemetry_test.exs, rulestead/test/rulestead/telemetry_test.exs</files>
  <behavior>
    - Test 1: Phase 20 events under `[:rulestead, :runtime, :invalidation, ...]` still fire unchanged for received, ignored, refresh-triggered, and refresh-failed outcomes.
    - Test 2: Phase 21 compatibility aliases emit alongside the existing family when requirement-name compatibility is needed, never instead of it.
    - Test 3: alias-event metadata stays bounded to environment, snapshot version, reason, and refresh status.
  </behavior>
  <action>Extend `Rulestead.Runtime.Refresh` so INF-02 is satisfied additively: preserve the shipped Phase 20 telemetry family `[:rulestead, :runtime, :invalidation, ...]` exactly as-is, and if requirement-name compatibility is needed emit alias events such as `[:rulestead, :cache, :invalidation]` and `[:rulestead, :sync, :delta_received]` from the same invalidation branches rather than renaming the existing contract. Keep the alias payloads metadata-only and derived from the same bounded invalidation facts. Add focused tests that assert both the old and new event names fire together and that existing telemetry expectations do not regress.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/runtime/health_telemetry_test.exs test/rulestead/telemetry_test.exs test/rulestead/runtime/cluster_refresh_test.exs -x</automated>
  </verify>
  <done>INF-02 is satisfied without breaking hosts already consuming the Phase 20 invalidation telemetry family.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime state -> operator diagnostics snapshot | Internal ETS/runtime metadata crosses into an operator-visible debug surface here |
| invalidation handler -> telemetry consumers | Internal invalidation outcomes are projected into host-consumed metrics events here |
| host-provided peer data -> health projection | Optional external peer reports enter the bounded health snapshot contract here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | I | health snapshot payload | mitigate | Keep diagnostics bounded to freshness, status, and adapter health; do not expose raw snapshot payloads, actor context, or ETS internals. |
| T-21-01-02 | S | topology presentation | mitigate | Default to `current_node` scope and require any peer data to be explicitly host-provided rather than discovered implicitly. |
| T-21-01-03 | T | telemetry contract | mitigate | Preserve the shipped `[:rulestead, :runtime, :invalidation, ...]` events and add aliases instead of renaming them. |
| T-21-01-04 | D | diagnostics probes | mitigate | Use bounded status checks and existing runtime metadata instead of synchronous unbounded Repo/Redis/PubSub introspection in hot paths. |
</threat_model>

<verification>
Run the targeted runtime diagnostics, telemetry, and cluster invalidation regression tests. Confirm the health snapshot remains truthful about node scope, exposes bounded freshness/latency/adapter metadata, keeps both the Phase 20 and Phase 21-compatible telemetry names available together, and does not regress the existing cross-node convergence harness.
</verification>

<success_criteria>
Phase 21 has a stable backend observability contract: `rulestead` can tell the admin UI what this node is serving, how fresh it is, whether invalidation is landing, and whether configured adapters appear healthy, without inventing cluster discovery or breaking existing telemetry consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/21-infrastructure-observability-ui/21-01-SUMMARY.md`
</output>
