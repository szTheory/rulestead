---
phase: 21-infrastructure-observability-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - rulestead_admin/lib/rulestead_admin/router.ex
  - rulestead_admin/lib/rulestead_admin/components/operator_components.ex
  - rulestead_admin/lib/rulestead_admin/live/diagnostics_live/index.ex
  - rulestead_admin/test/rulestead_admin/live/diagnostics_live/index_test.exs
  - rulestead_admin/test/rulestead_admin/live/diagnostics_live/accessibility_test.exs
autonomous: true
requirements:
  - INF-01
must_haves:
  truths:
    - "Operators can open a mounted diagnostics page in `rulestead_admin` and see current-node cache freshness and connection health without leaving the admin UI."
    - "The page states its topology scope clearly instead of implying undiscovered peers are healthy."
    - "The screen surfaces invalidation/sync health in summary-first operator language using the existing admin component system."
    - "The diagnostics screen keeps the standard mounted-admin session, environment picker, and accessibility posture."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/diagnostics_live/index.ex"
      provides: "Mounted diagnostics LiveView rendering infrastructure health"
    - path: "rulestead_admin/lib/rulestead_admin/router.ex"
      provides: "Mounted `/diagnostics` route inside the existing admin macro and live_session"
    - path: "rulestead_admin/lib/rulestead_admin/components/operator_components.ex"
      provides: "Reusable status presentation helpers needed by the diagnostics screen"
    - path: "rulestead_admin/test/rulestead_admin/live/diagnostics_live/index_test.exs"
      provides: "Rendering and refresh coverage for diagnostics copy, scope labeling, and environment summaries"
    - path: "rulestead_admin/test/rulestead_admin/live/diagnostics_live/accessibility_test.exs"
      provides: "Accessibility regression coverage matching existing admin test posture"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/diagnostics_live/index.ex"
      to: "rulestead/lib/rulestead.ex"
      via: "connected-only load or explicit refresh pulls the bounded health snapshot through the public facade"
      pattern: "assign_async|refresh|infrastructure_health"
    - from: "rulestead_admin/lib/rulestead_admin/router.ex"
      to: "rulestead_admin/lib/rulestead_admin/live/diagnostics_live/index.ex"
      via: "mounted admin macro adds `/diagnostics` within the existing session and policy envelope"
      pattern: "live \"/diagnostics\""
    - from: "rulestead_admin/test/rulestead_admin/live/diagnostics_live/index_test.exs"
      to: "rulestead_admin/lib/rulestead_admin/components/operator_components.ex"
      via: "summary-first copy and trace/detail panels render through existing admin component patterns"
      pattern: "Summary|current node|Cache age|Sync latency|PubSub|Redis|Repo"
---

<objective>
Add the operator-facing Infrastructure Observability UI in the mounted admin package.

Purpose: Phase 21’s backend projection is only useful if SREs and operators can inspect it quickly in the existing admin plane. This plan adds a diagnostics screen that reuses the shipped LiveView/session/component patterns and stays honest about scope, freshness, and invalidation health.
Output: A mounted `/diagnostics` LiveView, any small reusable operator components it needs, and focused LiveView/accessibility coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v0.5.0-ROADMAP.md
@.planning/milestones/v0.5.0-REQUIREMENTS.md
@.planning/phases/21-infrastructure-observability-ui/21-RESEARCH.md
@.planning/phases/21-infrastructure-observability-ui/21-PATTERNS.md
@.planning/phases/21-infrastructure-observability-ui/21-01-PLAN.md
@prompts/rulestead-admin-ux-and-operator-ia.md
@prompts/rulestead-telemetry-observability-and-audit.md
@prompts/rulestead-testing-and-e2e-strategy.md
@rulestead_admin/lib/rulestead_admin/router.ex
@rulestead_admin/lib/rulestead_admin/components/operator_components.ex
@rulestead_admin/lib/rulestead_admin/components/flag_components.ex
@rulestead_admin/lib/rulestead_admin/components/shell.ex
@rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex
@rulestead_admin/lib/rulestead_admin/live/flag_live/simulate.ex
@rulestead_admin/test/rulestead_admin/live/flag_live/show_test.exs
@rulestead_admin/test/rulestead_admin/live/flag_live/simulate_test.exs

<interfaces>
From rulestead_admin/lib/rulestead_admin/router.ex:
```elixir
scope path, as: :rulestead_admin do
  live_session live_session_name,
    session: %{"policy" => policy, "mount_path" => path},
    on_mount: [{RulesteadAdmin.Live.Session, :default}] do
```

From rulestead_admin/lib/rulestead_admin/components/operator_components.ex:
```elixir
def banner(assigns)
def summary_grid(assigns)
def trace_panel(assigns)
```

From rulestead_admin/lib/rulestead_admin/components/shell.ex:
```elixir
<Shell.page
  page_title={...}
  page_kicker={...}
  page_summary={...}
  current_environment={@current_environment}
  environments={@available_environments}
  env_links={@env_links}
>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount the diagnostics LiveView and render a summary-first health page</name>
  <files>rulestead_admin/lib/rulestead_admin/router.ex, rulestead_admin/lib/rulestead_admin/components/operator_components.ex, rulestead_admin/lib/rulestead_admin/live/diagnostics_live/index.ex, rulestead_admin/test/rulestead_admin/live/diagnostics_live/index_test.exs</files>
  <behavior>
    - Test 1: `/admin/diagnostics?env=prod` renders a summary-first page with current-node scope copy, cache age, sync latency, snapshot version, refresh state, and adapter health.
    - Test 2: the page keeps the mounted admin environment picker/session semantics and can refresh the health snapshot without becoming a mutation workflow.
    - Test 3: degraded, stale, or missing-health states render operator-facing warning or critical copy instead of crashing or silently implying success.
  </behavior>
  <action>Add `RulesteadAdmin.Live.DiagnosticsLive.Index` under the existing mounted admin router macro at `/diagnostics`. Reuse `Shell.page`, `OperatorComponents.banner`, `summary_grid`, `trace_panel`, and `FlagComponents.section_card` so the page matches the shipped operator IA instead of inventing a new dashboard style. Load the Phase 21 backend health snapshot through the public `rulestead` facade using a connected-only async load or an explicit refresh event, and present the topology scope as current-node by default unless host-supplied peer data is actually present. The screen should render compact summary cards first, then detail panels for freshness, sync latency, invalidation/refresh status, and Repo/Redis/PubSub health. Add LiveView tests that seed runtime state, mount the page through the existing endpoint/session harness, assert route wiring, copy, environment scoping, refresh behavior, and degraded-state rendering.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/diagnostics_live/index_test.exs -x</automated>
  </verify>
  <done>Operators can visit a mounted diagnostics page in the admin UI and inspect truthful current-node infrastructure health using the existing Rulestead admin visual language.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the diagnostics screen accessibility and mounted-admin behavior</name>
  <files>rulestead_admin/test/rulestead_admin/live/diagnostics_live/accessibility_test.exs</files>
  <behavior>
    - Test 1: the diagnostics page has the expected landmark structure, labels, and keyboard-safe links/buttons for refresh and navigation.
    - Test 2: async loading, empty state, and degraded state remain readable to assistive technology and do not depend on color alone.
  </behavior>
  <action>Add a focused accessibility regression test for the diagnostics page that mirrors the existing admin accessibility posture. Assert the page title, summary region, current-node scope text, health sections, and any refresh control are exposed with proper labels and landmarks. Keep the page inside the mounted-admin policy/session envelope; this task is verification only and should not add a parallel auth or routing mechanism.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/diagnostics_live/accessibility_test.exs -x</automated>
  </verify>
  <done>The diagnostics screen is covered by the same accessibility and mounted-admin expectations as the rest of `rulestead_admin`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend health snapshot -> mounted LiveView | Diagnostics data crosses from core runtime into the operator-facing UI here |
| session/policy envelope -> diagnostics route | Mounted admin authz/session context governs access to the diagnostics screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | I | diagnostics UI copy | mitigate | Render bounded health fields only and keep current-node scope explicit unless host-provided peer data is present. |
| T-21-02-02 | E | diagnostics route | mitigate | Mount `/diagnostics` inside the existing `rulestead_admin` router macro and live_session so current policy/on_mount checks still apply. |
| T-21-02-03 | T | operator interpretation | mitigate | Use summary-first status cards and explicit stale/degraded wording so the UI cannot imply healthy synchronization when data is unavailable. |
| T-21-02-04 | D | LiveView mount path | mitigate | Use connected-only async loading or explicit refresh actions so diagnostics probes do not block disconnected mount or degrade the rest of the admin surface. |
</threat_model>

<verification>
Run the diagnostics LiveView and accessibility tests in `rulestead_admin`. Confirm the page mounts under `/diagnostics`, keeps the existing session/environment picker behavior, labels its scope honestly, and surfaces stale/degraded health states without becoming a control surface.
</verification>

<success_criteria>
The Admin UI now includes an Infrastructure Observability screen that lets SREs and operators confirm cache freshness, sync health, and adapter status from the existing mounted admin package while staying truthful about current-node versus cluster-wide visibility.
</success_criteria>

<output>
After completion, create `.planning/phases/21-infrastructure-observability-ui/21-02-SUMMARY.md`
</output>
