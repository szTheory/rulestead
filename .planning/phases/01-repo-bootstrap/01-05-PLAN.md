---
phase: 01-repo-bootstrap
plan: 05
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release-please.yml
  - .github/workflows/publish-hex.yml
  - .github/workflows/pr-title.yml
  - .github/workflows/dependency-review.yml
  - .github/workflows/dependabot-automerge.yml
  - .github/workflows/actionlint.yml
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-05
  - DOC-03
must_haves:
  truths:
    - "GitHub Actions exposes the exact seven-workflow Phase 1 surface from D-04."
    - "CI runs the lint and two-cell test matrix, keeps an integration placeholder row, and aggregates required status into `release_gate`."
    - "Release automation can open linked-versions release PRs and a guarded manual publish path exists without enabling Phase 8 release verification yet."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Lint, test matrix, integration placeholder, and release gate workflow"
    - path: ".github/workflows/release-please.yml"
      provides: "Linked-versions release PR automation"
    - path: ".github/workflows/publish-hex.yml"
      provides: "Manual guarded publish fallback"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/*.sh"
      via: "local reproducible shell entrypoints"
      pattern: "scripts/ci/"
    - from: ".github/workflows/publish-hex.yml"
      to: "rulestead_admin/lib/rulestead_admin/router.ex"
      via: "guard against publishing the stub admin package"
      pattern: "Phases 6"
---

<objective>
Implement the Phase 1 GitHub Actions workflows for CI, release-please, publish fallback, PR-title linting, dependency review, Dependabot auto-merge, and actionlint.

Purpose: Give the repo a real merge gate and release surface from day one while preserving the locked Phase 5 and Phase 8 deferrals.
Output: Seven workflow files with stable job ids, SHA-pinned actions, and the exact Phase 1 job graph.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repo-bootstrap/01-CONTEXT.md
@.planning/phases/01-repo-bootstrap/01-PATTERNS.md
@prompts/rulestead-release-engineering-and-ci.md
@prompts/rulestead-engineering-dna-from-prior-libs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the core CI and release workflows</name>
  <files>.github/workflows/ci.yml, .github/workflows/release-please.yml, .github/workflows/publish-hex.yml, .github/workflows/pr-title.yml, .github/workflows/dependency-review.yml</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, prompts/rulestead-release-engineering-and-ci.md, release-please-config.json, .tool-versions, rulestead_admin/lib/rulestead_admin/router.ex</read_first>
  <action>Write the five core workflows per D-02, D-03, D-04, D-06, D-08, and D-10. In every workflow, add the job-id contract comment and SHA-pin third-party actions with trailing version comments. `ci.yml` must trigger on `push` and `pull_request` for `main` plus `workflow_dispatch`, set `concurrency.group` to `ci-${{ github.workflow }}-${{ github.ref }}` with `cancel-in-progress: true`, and define jobs `lint`, `test`, `integration-placeholder`, and `release_gate`. The `lint` job must use `.tool-versions`, run format, compile warnings-as-errors, `mix credo --strict`, `mix docs --warnings-as-errors`, `mix hex.audit`, `mix compile --no-optional-deps --warnings-as-errors`, and the single-cell Dialyzer invocation from D-06. The `test` job must be a two-cell matrix for `1.17.3/26.2.5` and `1.19.2/28.1.2` with a Postgres 15 service container and healthcheck. The `integration-placeholder` job must only call the placeholder script and keep the stable job id. `release_gate` must aggregate the exact required checks via `needs` and fail on any non-success result. `release-please.yml` must use linked versions and port the accrue-style lockstep fallback block so `rulestead_admin` can still produce a release when versions match but there are no qualifying admin commits. `publish-hex.yml` must be `workflow_dispatch` only and refuse to publish `rulestead_admin` while `rulestead_admin/lib/rulestead_admin/router.ex` still contains the stub guard from D-03. `pr-title.yml` must start on `pull_request`, not `pull_request_target`, and enforce conventional PR titles. `dependency-review.yml` must run GitHub dependency review on pull requests only.</action>
  <acceptance_criteria>
    - `rg -n '^  lint:|^  test:|^  integration-placeholder:|^  release_gate:' .github/workflows/ci.yml` returns all four job ids.
    - `rg -n 'elixir: "1\\.17\\.3"|otp: "26\\.2\\.5"|elixir: "1\\.19\\.2"|otp: "28\\.1\\.2"' .github/workflows/ci.yml` returns all matrix values.
    - `rg -n 'postgres:15' .github/workflows/ci.yml` returns one match.
    - `rg -n 'mix docs --warnings-as-errors|mix hex.audit|mix compile --no-optional-deps --warnings-as-errors|mix dialyzer' .github/workflows/ci.yml` returns matches for all four commands.
    - `rg -n 'linked-versions|release_created' .github/workflows/release-please.yml` returns matches proving the fallback is present.
    - `rg -n 'RULESTEAD_ADMIN_HEX_RELEASE|router\\.ex|workflow_dispatch' .github/workflows/publish-hex.yml` returns matches showing the admin publish guard and manual trigger.
    - `rg -n 'pull_request:' .github/workflows/pr-title.yml` returns one match and `rg -n 'pull_request_target' .github/workflows/pr-title.yml` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .github/workflows/ci.yml &amp;&amp; test -f .github/workflows/release-please.yml &amp;&amp; test -f .github/workflows/publish-hex.yml &amp;&amp; test -f .github/workflows/pr-title.yml &amp;&amp; test -f .github/workflows/dependency-review.yml &amp;&amp; rg -n "^  lint:|^  test:|^  integration-placeholder:|^  release_gate:" .github/workflows/ci.yml &amp;&amp; rg -n "elixir: \"1\\.17\\.3\"|otp: \"26\\.2\\.5\"|elixir: \"1\\.19\\.2\"|otp: \"28\\.1\\.2\"|postgres:15|mix docs --warnings-as-errors|mix hex.audit|mix compile --no-optional-deps --warnings-as-errors|mix dialyzer" .github/workflows/ci.yml &amp;&amp; rg -n "linked-versions|release_created" .github/workflows/release-please.yml &amp;&amp; rg -n "router\\.ex|workflow_dispatch" .github/workflows/publish-hex.yml &amp;&amp; rg -n "pull_request:" .github/workflows/pr-title.yml &amp;&amp; ! rg -n "pull_request_target" .github/workflows/pr-title.yml'</automated>
  </verify>
  <done>The repository has the exact Phase 1 CI/release workflow surface, including the stable `release_gate` aggregator and the guarded admin publish fallback, with no premature Phase 5 or Phase 8 jobs.</done>
</task>

<task type="auto">
  <name>Task 2: Add the remaining GitHub workflow guards</name>
  <files>.github/workflows/dependabot-automerge.yml, .github/workflows/actionlint.yml</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, prompts/rulestead-release-engineering-and-ci.md</read_first>
  <action>Create `dependabot-automerge.yml` so it only runs on Dependabot pull requests, checks `github.actor == 'dependabot[bot]'`, and only auto-merges patch updates. Create `actionlint.yml` as an advisory workflow that runs only when `.github/workflows/**` changes, uses actionlint/reviewdog, and intentionally stays outside D-08's required-check list because it is path-filtered and would otherwise leave non-workflow PRs pending. Keep permissions least-privilege in both files.</action>
  <acceptance_criteria>
    - `rg -n "dependabot\\[bot\\]" .github/workflows/dependabot-automerge.yml` returns one match.
    - `rg -n 'version-update:semver-patch|patch' .github/workflows/dependabot-automerge.yml` returns one match.
    - `rg -n '\\.github/workflows/\\*\\*' .github/workflows/actionlint.yml` returns one match.
    - `rg -n 'actionlint|reviewdog' .github/workflows/actionlint.yml` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .github/workflows/dependabot-automerge.yml &amp;&amp; test -f .github/workflows/actionlint.yml &amp;&amp; rg -n "dependabot\\[bot\\]|version-update:semver-patch|patch" .github/workflows/dependabot-automerge.yml &amp;&amp; rg -n "\\.github/workflows/\\*\\*|actionlint|reviewdog" .github/workflows/actionlint.yml'</automated>
  </verify>
  <done>The non-core GitHub workflow guards are in place and aligned with the required-check strategy documented in D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub event→workflow runner | Untrusted PR metadata and branch state trigger privileged CI automation |
| workflow runner→Hex/GitHub release surfaces | Release and publish workflows can mutate tags, PRs, and package publication state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | E | `publish-hex.yml` | mitigate | Keep the workflow manual-only and gate admin publish on the router stub content so an empty admin package cannot reach Hex. |
| T-01-14 | D | `ci.yml` required checks | mitigate | Preserve the stable `release_gate` aggregator and the placeholder integration job id, preventing branch-protection churn and broken required-check mappings as later phases add real jobs. |
| T-01-15 | T | workflow supply chain | mitigate | SHA-pin third-party actions with version comments and use least-privilege permissions at workflow/job scope. |
</threat_model>

<verification>
Run the grep-based workflow checks, then inspect the workflow names and job ids to confirm they align with `MAINTAINING.md` and D-08.
</verification>

<success_criteria>
The repository has a real GitHub Actions merge gate and release automation surface aligned with the locked Phase 1 decisions and deferred-job boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-bootstrap/01-05-SUMMARY.md`
</output>
