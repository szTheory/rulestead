---
phase: 01-repo-bootstrap
plan: 05
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - scripts/ci/lint.sh
  - scripts/ci/test.sh
  - scripts/ci/release_gate.sh
  - scripts/ci/integration_placeholder.sh
  - scripts/ci/check_package_whitelist.sh
  - scripts/ci/admin_publish_guard.sh
  - scripts/ci/release_please_dry_run.sh
  - .github/workflows/ci.yml
  - .github/workflows/release-please.yml
  - .github/workflows/publish-hex.yml
  - .github/workflows/pr-title.yml
  - .github/workflows/dependency-review.yml
  - .github/workflows/dependabot-automerge.yml
  - .github/workflows/actionlint.yml
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-05
  - DOC-03
must_haves:
  truths:
    - "Every non-trivial CI or publish check has a locally runnable `scripts/ci/*.sh` entrypoint before workflow wiring is added."
    - "GitHub Actions exposes the exact seven-workflow Phase 1 surface from D-04."
    - "CI runs the lint and two-cell test matrix, keeps an integration placeholder row, and aggregates required status into `release_gate`."
    - "Release automation can open linked-versions release PRs and a guarded manual publish path exists without enabling Phase 8 release verification yet."
    - "Phase 1 execution includes the release-please bootstrap instruction needed for the first release PR to target `v0.1.0` instead of the default `v1.0.0`."
    - "Phase 1 execution proves the CI surface locally before relying on the separate clean-branch PR / release-please smoke verification plan."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Lint, test matrix, integration placeholder, and release gate workflow"
    - path: "scripts/ci/lint.sh"
      provides: "Locally runnable lint entrypoint shared by CI"
    - path: ".github/workflows/release-please.yml"
      provides: "Linked-versions release PR automation"
    - path: ".github/workflows/publish-hex.yml"
      provides: "Manual guarded publish fallback"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/lint.sh"
      via: "workflow step shells into the locally runnable lint entrypoint"
      pattern: "scripts/ci/lint.sh"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/test.sh"
      via: "workflow matrix shells into the locally runnable test entrypoint"
      pattern: "scripts/ci/test.sh"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/release_gate.sh"
      via: "aggregator job evaluates predecessor results through the shared gate script"
      pattern: "scripts/ci/release_gate.sh"
    - from: ".github/workflows/publish-hex.yml"
      to: "scripts/ci/admin_publish_guard.sh"
      via: "manual publish path shells into the admin stub guard"
      pattern: "scripts/ci/admin_publish_guard.sh"
    - from: ".github/workflows/publish-hex.yml"
      to: "rulestead_admin/lib/rulestead_admin/router.ex"
      via: "guard against publishing the stub admin package"
      pattern: "Phases 6"
---

<objective>
Implement the Phase 1 GitHub Actions workflows for CI, release-please, publish fallback, PR-title linting, dependency review, Dependabot auto-merge, and actionlint.

Purpose: Give the repo a real merge gate and release surface from day one while preserving the locked Phase 5 and Phase 8 deferrals.
Output: Locally runnable CI scripts plus seven workflow files with stable job ids, SHA-pinned actions, and the exact Phase 1 job graph.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repo-bootstrap/01-CONTEXT.md
@.planning/phases/01-repo-bootstrap/01-PATTERNS.md
@prompts/rulestead-release-engineering-and-ci.md
@prompts/rulestead-engineering-dna-from-prior-libs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the locally runnable Phase 1 CI and publish scripts</name>
  <files>scripts/ci/lint.sh, scripts/ci/test.sh, scripts/ci/release_gate.sh, scripts/ci/integration_placeholder.sh, scripts/ci/check_package_whitelist.sh, scripts/ci/admin_publish_guard.sh, scripts/ci/release_please_dry_run.sh</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, .tool-versions, rulestead/mix.exs, rulestead_admin/mix.exs, rulestead_admin/lib/rulestead_admin/router.ex</read_first>
  <action>Create the seven shared shell entrypoints per D-04, D-05, and D-06 before wiring any workflow to them. Every script must start with `#!/usr/bin/env bash`, `set -euo pipefail`, and `RULESTEAD_REPO="${GITHUB_WORKSPACE:-$(pwd)}"`. `scripts/ci/lint.sh` must run the exact Phase 1 lint sequence: formatter check, compile warnings-as-errors, `mix credo --strict`, `mix docs --warnings-as-errors`, `mix hex.audit`, `mix compile --no-optional-deps --warnings-as-errors`, `scripts/ci/check_package_whitelist.sh`, and the single-cell Dialyzer invocation. `scripts/ci/test.sh` must run the two-package test flow expected by the CI matrix. `scripts/ci/release_gate.sh` must accept predecessor results and fail when any required job is not `success`. `scripts/ci/integration_placeholder.sh` must print the exact placeholder line `integration placeholder; real integration lands in Phase 5` and exit 0. `scripts/ci/check_package_whitelist.sh` must run `mix hex.publish --dry-run` in both packages and fail on cross-contamination (`rulestead_admin/` inside the core tarball or `rulestead/` inside the admin tarball). `scripts/ci/admin_publish_guard.sh` must fail if `rulestead_admin/lib/rulestead_admin/router.ex` still contains the D-03 stub message. `scripts/ci/release_please_dry_run.sh` must run the linked-versions release-please dry-run path against the local config/manifest and assert the bootstrap state includes `Release-As: 0.1.0` guidance plus both packages seeded at `0.0.0`. Keep the scripts locally runnable; do not duplicate this logic inline in workflow YAML.</action>
  <acceptance_criteria>
    - `rg -n 'set -euo pipefail|RULESTEAD_REPO=\"\\$\\{GITHUB_WORKSPACE:-\\$\\(pwd\\)\\}\"' scripts/ci/lint.sh scripts/ci/test.sh scripts/ci/release_gate.sh scripts/ci/integration_placeholder.sh scripts/ci/check_package_whitelist.sh scripts/ci/admin_publish_guard.sh scripts/ci/release_please_dry_run.sh` returns matches in all seven scripts.
    - `rg -n 'mix docs --warnings-as-errors|mix hex.audit|mix compile --no-optional-deps --warnings-as-errors|mix dialyzer|scripts/ci/check_package_whitelist.sh' scripts/ci/lint.sh` returns all required commands.
    - `rg -n 'integration placeholder; real integration lands in Phase 5' scripts/ci/integration_placeholder.sh` returns one match.
    - `rg -n 'mix hex.publish --dry-run|rulestead_admin/|rulestead/' scripts/ci/check_package_whitelist.sh` returns the dry-run and contamination checks.
    - `rg -n 'Phases 6.?7 of v0\\.1\\.0|router\\.ex' scripts/ci/admin_publish_guard.sh` returns a match proving the stub-content guard exists.
    - `rg -n 'Release-As: 0\\.1\\.0|0\\.0\\.0|release-please' scripts/ci/release_please_dry_run.sh` returns matches proving the bootstrap dry-run helper exists.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f scripts/ci/lint.sh &amp;&amp; test -f scripts/ci/test.sh &amp;&amp; test -f scripts/ci/release_gate.sh &amp;&amp; test -f scripts/ci/integration_placeholder.sh &amp;&amp; test -f scripts/ci/check_package_whitelist.sh &amp;&amp; test -f scripts/ci/admin_publish_guard.sh &amp;&amp; test -f scripts/ci/release_please_dry_run.sh &amp;&amp; bash -n scripts/ci/lint.sh scripts/ci/test.sh scripts/ci/release_gate.sh scripts/ci/integration_placeholder.sh scripts/ci/check_package_whitelist.sh scripts/ci/admin_publish_guard.sh scripts/ci/release_please_dry_run.sh &amp;&amp; rg -n "set -euo pipefail|RULESTEAD_REPO=\"\\$\\{GITHUB_WORKSPACE:-\\$\\(pwd\\)\\}\"" scripts/ci/lint.sh scripts/ci/test.sh scripts/ci/release_gate.sh scripts/ci/integration_placeholder.sh scripts/ci/check_package_whitelist.sh scripts/ci/admin_publish_guard.sh scripts/ci/release_please_dry_run.sh &amp;&amp; rg -n "mix docs --warnings-as-errors|mix hex.audit|mix compile --no-optional-deps --warnings-as-errors|mix dialyzer|scripts/ci/check_package_whitelist.sh" scripts/ci/lint.sh &amp;&amp; rg -n "integration placeholder; real integration lands in Phase 5" scripts/ci/integration_placeholder.sh &amp;&amp; rg -n "mix hex.publish --dry-run|rulestead_admin/|rulestead/" scripts/ci/check_package_whitelist.sh &amp;&amp; rg -n "Phases 6.?7 of v0\\.1\\.0|router\\.ex" scripts/ci/admin_publish_guard.sh &amp;&amp; rg -n "Release-As: 0\\.1\\.0|0\\.0\\.0|release-please" scripts/ci/release_please_dry_run.sh'</automated>
  </verify>
  <done>The repository has the exact locally runnable CI and publish script surface the workflows will call, including the package whitelist and admin publish guard logic.</done>
</task>

<task type="auto">
  <name>Task 2: Build the core CI and release workflows wired to the shared scripts</name>
  <files>.github/workflows/ci.yml, .github/workflows/release-please.yml, .github/workflows/publish-hex.yml, .github/workflows/pr-title.yml, .github/workflows/dependency-review.yml</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, prompts/rulestead-release-engineering-and-ci.md, release-please-config.json, .tool-versions, scripts/ci/lint.sh, scripts/ci/test.sh, scripts/ci/release_gate.sh, scripts/ci/integration_placeholder.sh, scripts/ci/check_package_whitelist.sh, scripts/ci/admin_publish_guard.sh</read_first>
  <action>Write the five core workflows per D-02, D-03, D-04, D-06, D-08, and D-10. In every workflow, add the job-id contract comment and SHA-pin third-party actions with trailing version comments. `ci.yml` must trigger on `push` and `pull_request` for `main` plus `workflow_dispatch`, set `concurrency.group` to `ci-${{ github.workflow }}-${{ github.ref }}` with `cancel-in-progress: true`, and define jobs `changes`, `lint`, `test`, `integration-placeholder`, and `release_gate`. `changes` must use `dorny/paths-filter@v3` to detect docs-only changes without using workflow-level `paths-ignore` on required checks. `lint`, `test`, and `integration-placeholder` must use job-level `if:` guards driven by that filter so docs-only PRs skip the heavy jobs safely, while `release_gate` still runs and treats a docs-only change as success when the filtered jobs are skipped by design. Wire each non-trivial check through the shared scripts instead of duplicating commands inline: the `lint` job must shell into `scripts/ci/lint.sh`; the matrix `test` job must shell into `scripts/ci/test.sh`; the `integration-placeholder` job must shell into `scripts/ci/integration_placeholder.sh`; and `release_gate` must shell into `scripts/ci/release_gate.sh` after collecting predecessor results. Keep the Postgres 15 service container and the 1.17.3/26.2.5 plus 1.19.2/28.1.2 matrix in `ci.yml`, and configure the Postgres service with an explicit healthcheck using `pg_isready` service options so the CI lane satisfies the locked REL-05 service-container requirement. `release-please.yml` must use linked versions, port the accrue-style lockstep fallback block so `rulestead_admin` can still produce a release when versions match but there are no qualifying admin commits, seed `.release-please-manifest.json` with both packages at `0.0.0`, and document in comments or step text that the Phase 1 bootstrap commit must include the exact footer `Release-As: 0.1.0`. `publish-hex.yml` must be `workflow_dispatch` only, export `RULESTEAD_ADMIN_HEX_RELEASE=1` for the admin publish path, call `scripts/ci/admin_publish_guard.sh` before any admin publish step, and use `scripts/ci/check_package_whitelist.sh` instead of inline tarball checks. `pr-title.yml` must start on `pull_request`, not `pull_request_target`, use `amannn/action-semantic-pull-request@v5`, and expose the stable job/check name `Validate PR title` so branch protection can lock to the exact D-08 requirement. `dependency-review.yml` must run GitHub dependency review on pull requests only.</action>
  <acceptance_criteria>
    - `rg -n '^  changes:|^  lint:|^  test:|^  integration-placeholder:|^  release_gate:' .github/workflows/ci.yml` returns all five job ids.
    - `rg -n 'elixir: "1\\.17\\.3"|otp: "26\\.2\\.5"|elixir: "1\\.19\\.2"|otp: "28\\.1\\.2"' .github/workflows/ci.yml` returns all matrix values.
    - `rg -n 'postgres:15|pg_isready|health-.*interval|health-.*timeout|health-.*retries' .github/workflows/ci.yml` returns matches proving the Postgres service healthcheck is configured.
    - `rg -n 'dorny/paths-filter@v3|docs-only|if:' .github/workflows/ci.yml` returns matches proving docs-only filtering is job-scoped instead of workflow-scoped.
    - `rg -n 'scripts/ci/lint\\.sh|scripts/ci/test\\.sh|scripts/ci/integration_placeholder\\.sh|scripts/ci/release_gate\\.sh' .github/workflows/ci.yml` returns all four workflow-to-script calls.
    - `rg -n 'linked-versions|release_created|Release-As: 0\\.1\\.0|0\\.0\\.0' .github/workflows/release-please.yml release-please-config.json .release-please-manifest.json` returns matches proving the linked-versions fallback and bootstrap versioning are present.
    - `rg -n 'RULESTEAD_ADMIN_HEX_RELEASE|scripts/ci/admin_publish_guard\\.sh|scripts/ci/check_package_whitelist\\.sh|workflow_dispatch' .github/workflows/publish-hex.yml` returns matches showing the admin publish guard, whitelist check, and manual trigger.
    - `rg -n 'name: Validate PR title|amannn/action-semantic-pull-request@v5|pull_request:' .github/workflows/pr-title.yml` returns matches proving the exact action and stable check name are pinned.
    - `rg -n 'pull_request_target' .github/workflows/pr-title.yml` returns no matches.
    - `rg -n 'uses: .+@[0-9a-f]{40} +# v' .github/workflows/ci.yml .github/workflows/release-please.yml .github/workflows/publish-hex.yml .github/workflows/pr-title.yml .github/workflows/dependency-review.yml` returns matches proving third-party actions are SHA-pinned with trailing version comments.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .github/workflows/ci.yml &amp;&amp; test -f .github/workflows/release-please.yml &amp;&amp; test -f .github/workflows/publish-hex.yml &amp;&amp; test -f .github/workflows/pr-title.yml &amp;&amp; test -f .github/workflows/dependency-review.yml &amp;&amp; test -f .release-please-manifest.json &amp;&amp; rg -n "^  changes:|^  lint:|^  test:|^  integration-placeholder:|^  release_gate:" .github/workflows/ci.yml &amp;&amp; rg -n "elixir: \"1\\.17\\.3\"|otp: \"26\\.2\\.5\"|elixir: \"1\\.19\\.2\"|otp: \"28\\.1\\.2\"|postgres:15|pg_isready|health-.*interval|health-.*timeout|health-.*retries|scripts/ci/lint\\.sh|scripts/ci/test\\.sh|scripts/ci/integration_placeholder\\.sh|scripts/ci/release_gate\\.sh|dorny/paths-filter@v3|docs-only|if:" .github/workflows/ci.yml &amp;&amp; rg -n "linked-versions|release_created|Release-As: 0\\.1\\.0" .github/workflows/release-please.yml &amp;&amp; rg -n "0\\.0\\.0" .release-please-manifest.json &amp;&amp; rg -n "RULESTEAD_ADMIN_HEX_RELEASE|scripts/ci/admin_publish_guard\\.sh|scripts/ci/check_package_whitelist\\.sh|workflow_dispatch" .github/workflows/publish-hex.yml &amp;&amp; rg -n "name: Validate PR title|amannn/action-semantic-pull-request@v5|pull_request:" .github/workflows/pr-title.yml &amp;&amp; ! rg -n "pull_request_target" .github/workflows/pr-title.yml &amp;&amp; rg -n "uses: .+@[0-9a-f]{40} +# v" .github/workflows/ci.yml .github/workflows/release-please.yml .github/workflows/publish-hex.yml .github/workflows/pr-title.yml .github/workflows/dependency-review.yml'</automated>
  </verify>
  <done>The repository has the exact Phase 1 CI/release workflow surface, and each workflow delegates non-trivial logic to the shared scripts instead of burying commands inside YAML.</done>
</task>

<task type="auto">
  <name>Task 3: Add the remaining GitHub workflow guards and local CI validation</name>
  <files>.github/workflows/dependabot-automerge.yml, .github/workflows/actionlint.yml</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, prompts/rulestead-release-engineering-and-ci.md, scripts/ci/lint.sh, scripts/ci/test.sh, scripts/ci/integration_placeholder.sh, .github/workflows/ci.yml</read_first>
  <action>Create `dependabot-automerge.yml` so it only runs on Dependabot pull requests, checks `github.actor == 'dependabot[bot]'`, and only auto-merges patch updates. Create `actionlint.yml` as an advisory workflow that runs only when `.github/workflows/**` changes, uses actionlint/reviewdog, and intentionally stays outside D-08's required-check list because it is path-filtered and would otherwise leave non-workflow PRs pending. As part of this task's verification loop, prove the new CI surface is executable locally: run `bash -n scripts/ci/*.sh`, run `scripts/ci/integration_placeholder.sh`, and lint `.github/workflows/*.yml` with `actionlint` if installed or the documented container fallback `docker run --rm -v \"$PWD:/work\" -w /work rhysd/actionlint:latest actionlint .github/workflows/*.yml` if not. This task is the local proof that the Phase 1 CI surface can go green before the GitHub PR smoke step.</action>
  <acceptance_criteria>
    - `rg -n "dependabot\\[bot\\]" .github/workflows/dependabot-automerge.yml` returns one match.
    - `rg -n 'version-update:semver-patch|patch' .github/workflows/dependabot-automerge.yml` returns one match.
    - `rg -n '\\.github/workflows/\\*\\*' .github/workflows/actionlint.yml` returns one match.
    - `rg -n 'actionlint|reviewdog' .github/workflows/actionlint.yml` returns at least one match.
    - `bash -n scripts/ci/lint.sh scripts/ci/test.sh scripts/ci/release_gate.sh scripts/ci/integration_placeholder.sh scripts/ci/check_package_whitelist.sh scripts/ci/admin_publish_guard.sh scripts/ci/release_please_dry_run.sh` exits 0.
    - `bash scripts/ci/integration_placeholder.sh` exits 0.
    - `actionlint .github/workflows/*.yml` exits 0, or the documented `docker run --rm -v "$PWD:/work" -w /work rhysd/actionlint:latest actionlint .github/workflows/*.yml` fallback exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .github/workflows/dependabot-automerge.yml &amp;&amp; test -f .github/workflows/actionlint.yml &amp;&amp; rg -n "dependabot\\[bot\\]|version-update:semver-patch|patch" .github/workflows/dependabot-automerge.yml &amp;&amp; rg -n "\\.github/workflows/\\*\\*|actionlint|reviewdog" .github/workflows/actionlint.yml &amp;&amp; bash -n scripts/ci/lint.sh scripts/ci/test.sh scripts/ci/release_gate.sh scripts/ci/integration_placeholder.sh scripts/ci/check_package_whitelist.sh scripts/ci/admin_publish_guard.sh scripts/ci/release_please_dry_run.sh &amp;&amp; bash scripts/ci/integration_placeholder.sh &amp;&amp; (actionlint .github/workflows/*.yml || docker run --rm -v "$PWD:/work" -w /work rhysd/actionlint:latest actionlint .github/workflows/*.yml)'</automated>
  </verify>
  <done>The non-core GitHub workflow guards are in place, and the Phase 1 CI surface has a concrete local validation path before GitHub runs it on a PR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub event→workflow runner | Untrusted PR metadata and branch state trigger privileged CI automation |
| workflow runner→Hex/GitHub release surfaces | Release and publish workflows can mutate tags, PRs, and package publication state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | E | `publish-hex.yml` | mitigate | Keep the workflow manual-only and gate admin publish on the router stub content so an empty admin package cannot reach Hex. |
| T-01-14 | D | `ci.yml` required checks | mitigate | Preserve the stable `release_gate` aggregator and the placeholder integration job id, preventing branch-protection churn and broken required-check mappings as later phases add real jobs. |
| T-01-15 | T | workflow supply chain | mitigate | SHA-pin third-party actions with version comments and use least-privilege permissions at workflow/job scope. |
</threat_model>

<verification>
Run the grep-based workflow checks, then inspect the workflow names and job ids to confirm they align with `MAINTAINING.md` and D-08.
</verification>

<success_criteria>
The repository has a real GitHub Actions merge gate and release automation surface aligned with the locked Phase 1 decisions and deferred-job boundaries, with all non-trivial CI logic shared through locally runnable scripts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-bootstrap/01-05-SUMMARY.md`
</output>
