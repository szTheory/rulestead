---
phase: 01-repo-bootstrap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/mix.exs
  - rulestead/.formatter.exs
  - rulestead/README.md
  - rulestead/CHANGELOG.md
  - rulestead/lib/rulestead.ex
  - rulestead/test/test_helper.exs
  - rulestead/test/rulestead_test.exs
autonomous: true
requirements:
  - REL-01
  - DOC-03
must_haves:
  truths:
    - "The core `rulestead` package exists as a standalone Mix project under the monorepo root."
    - "The core package compiles and has a green empty-skeleton ExUnit surface."
    - "The core package exposes package metadata and docs configuration that later phases can extend without reorganizing files."
  artifacts:
    - path: "rulestead/mix.exs"
      provides: "Core package metadata, docs config, aliases, and dialyzer config"
    - path: "rulestead/lib/rulestead.ex"
      provides: "Root public module stub"
    - path: "rulestead/test/rulestead_test.exs"
      provides: "Core skeleton compile/test proof"
  key_links:
    - from: "rulestead/mix.exs"
      to: "rulestead/lib/rulestead.ex"
      via: "application compile path and docs module references"
      pattern: "defmodule Rulestead"
    - from: "rulestead/mix.exs"
      to: "guides/"
      via: "later ExDoc extras configuration in Plan 07"
      pattern: "docs do"
---

<objective>
Create the core `rulestead` sibling package with its own Mix project, package metadata, docs scaffolding hooks, and a green empty-skeleton test surface.

Purpose: Establish the real package boundary that later runtime, schema, and docs work will build inside.
Output: `rulestead/` package skeleton with compile/test/docs-ready metadata.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repo-bootstrap/01-CONTEXT.md
@.planning/phases/01-repo-bootstrap/01-PATTERNS.md
@prompts/rulestead-release-engineering-and-ci.md
@prompts/rulestead-engineering-dna-from-prior-libs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the core Mix project metadata and package surface</name>
  <files>rulestead/mix.exs, rulestead/.formatter.exs, rulestead/README.md, rulestead/CHANGELOG.md</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, release-please-config.json, .tool-versions</read_first>
  <action>Create `rulestead/mix.exs` as the canonical core package definition with `@version "0.1.0"`, package name `:rulestead`, MIT license metadata, GitHub/HexDocs/Changelog/Guides links, and the exact Phase 1 package whitelist from D-05: `~w(lib priv/templates priv/repo/migrations guides .formatter.exs mix.exs README.md LICENSE CHANGELOG.md CONTRIBUTING.md SECURITY.md)`. Add docs config that already includes `source_ref: "v#{@version}"` and `skip_undefined_reference_warnings_on: &String.starts_with?(&1, "lib/")`; leave the full `extras` population for Plan 07. Add `dialyxir` in `only: [:dev, :test], runtime: false` and a `dialyzer/0` function exactly matching D-06 with `plt_local_path`, `plt_core_path`, `plt_add_apps: [:ex_unit, :mix, :eex]`, `flags: [:error_handling, :extra_return, :missing_return]`, `ignore_warnings: ".dialyzer_ignore.exs"`, and `list_unused_filters: true`. Write `rulestead/.formatter.exs` as the minimal package formatter. Write `rulestead/README.md` as a short package-local pointer back to the root README and guides, not a duplicate front door. Write `rulestead/CHANGELOG.md` in Keep-a-Changelog format with only `# Changelog` and `## [Unreleased]`.</action>
  <acceptance_criteria>
    - `rg -n '@version "0\\.1\\.0"' rulestead/mix.exs` returns one match.
    - `rg -n 'package-name|name: "rulestead"' release-please-config.json rulestead/mix.exs` shows the core package is named `rulestead`.
    - `rg -n 'priv/templates priv/repo/migrations guides \\.formatter\\.exs mix\\.exs README\\.md LICENSE CHANGELOG\\.md CONTRIBUTING\\.md SECURITY\\.md' rulestead/mix.exs` returns one match.
    - `rg -n 'plt_core_path: "priv/plts"' rulestead/mix.exs` returns one match.
    - `rg -n 'ignore_warnings: "\\.dialyzer_ignore\\.exs"' rulestead/mix.exs` returns one match.
    - `rg -n '^## \\[Unreleased\\]' rulestead/CHANGELOG.md` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f rulestead/mix.exs &amp;&amp; test -f rulestead/.formatter.exs &amp;&amp; test -f rulestead/README.md &amp;&amp; test -f rulestead/CHANGELOG.md &amp;&amp; rg -n "@version \"0\\.1\\.0\"|plt_core_path: \"priv/plts\"|ignore_warnings: \"\\.dialyzer_ignore\\.exs\"" rulestead/mix.exs &amp;&amp; rg -n "^## \\[Unreleased\\]" rulestead/CHANGELOG.md'</automated>
  </verify>
  <done>The core package has a valid Mix definition, package whitelist, dialyzer config, formatter config, and package-local docs pointers without introducing any runtime implementation beyond the Phase 1 skeleton.</done>
</task>

<task type="auto">
  <name>Task 2: Add the core module stub and empty-skeleton test harness</name>
  <files>rulestead/lib/rulestead.ex, rulestead/test/test_helper.exs, rulestead/test/rulestead_test.exs</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, rulestead/mix.exs</read_first>
  <action>Create `rulestead/lib/rulestead.ex` with the public root module `Rulestead`; keep the public surface intentionally small, include a version reflection helper if it is driven from `Application.spec(:rulestead, :vsn)` or the module attribute, and avoid any fake runtime API that would collide with Phase 3. Create `rulestead/test/test_helper.exs` that starts ExUnit only. Create `rulestead/test/rulestead_test.exs` with a single deterministic skeleton assertion proving the module loads and the package compiles; do not invent feature tests or Fake-adapter scaffolding yet because that is deferred to later phases.</action>
  <acceptance_criteria>
    - `rg -n '^defmodule Rulestead do$' rulestead/lib/rulestead.ex` returns one match.
    - `rg -n 'ExUnit\\.start\\(' rulestead/test/test_helper.exs` returns one match.
    - `rg -n '^defmodule RulesteadTest do$' rulestead/test/rulestead_test.exs` returns one match.
    - `rg -n 'assert' rulestead/test/rulestead_test.exs` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'cd rulestead &amp;&amp; mix test'</automated>
  </verify>
  <done>`cd rulestead && mix test` succeeds on the empty skeleton, proving the core package boundary is real and ready for Phase 2 code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex package→downstream app | Public package metadata and files become part of a consumer-facing artifact |
| package docs→contributors | Exposed README/docs hints influence downstream usage expectations before runtime code exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | I | `rulestead/mix.exs` package files | mitigate | Constrain `package.files` to the exact D-05 whitelist so internal planning files, sibling package files, and scripts never leak into Hex tarballs. |
| T-01-05 | T | `rulestead/lib/rulestead.ex` | mitigate | Keep the module stub minimal and avoid speculative public APIs, reducing the chance of accidental Phase 1 surface-area lock-in before REL-06 is finalized in Phase 8. |
| T-01-06 | R | `rulestead/test/*` | mitigate | Use a deterministic empty-skeleton assertion only, so CI proves compile viability without introducing misleading tests for unshipped behavior. |
</threat_model>

<verification>
Run `cd rulestead && mix test`. Confirm the package files and dialyzer config are present and that no runtime evaluator or schema stubs were added ahead of their planned phases.
</verification>

<success_criteria>
The core package exists as a valid, independently testable Mix project with the exact metadata and guardrails Phase 1 requires.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-bootstrap/01-02-SUMMARY.md`
</output>
