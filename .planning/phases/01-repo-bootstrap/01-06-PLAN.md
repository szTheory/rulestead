---
phase: 01-repo-bootstrap
plan: 06
type: execute
wave: 2
depends_on:
  - 01-05
files_modified:
  - .github/dependabot.yml
  - .github/pull_request_template.md
  - .github/CODEOWNERS
  - .github/ISSUE_TEMPLATE/bug_report.md
  - .github/ISSUE_TEMPLATE/feature_request.md
  - .github/ISSUE_TEMPLATE/release-parity-drift.md
  - scripts/ci/lint.sh
  - scripts/ci/test.sh
  - scripts/ci/release_gate.sh
  - scripts/ci/integration_placeholder.sh
  - scripts/ci/check_package_whitelist.sh
  - scripts/ci/admin_publish_guard.sh
autonomous: true
requirements:
  - REL-02
  - REL-05
must_haves:
  truths:
    - "GitHub repository support files match the workflow expectations and maintainer policy."
    - "Every non-trivial CI or publish check has a locally runnable `scripts/ci/*.sh` entrypoint."
    - "Hex packaging and admin publish guard logic are reproducible outside GitHub Actions."
  artifacts:
    - path: ".github/dependabot.yml"
      provides: "Weekly Dependabot coverage for Mix and GitHub Actions"
    - path: "scripts/ci/check_package_whitelist.sh"
      provides: "Dry-run Hex package whitelist verification"
    - path: "scripts/ci/admin_publish_guard.sh"
      provides: "Publish-time admin stub guard"
  key_links:
    - from: ".github/dependabot.yml"
      to: ".github/workflows/dependabot-automerge.yml"
      via: "shared Dependabot PR lifecycle"
      pattern: "github-actions"
    - from: "scripts/ci/admin_publish_guard.sh"
      to: ".github/workflows/publish-hex.yml"
      via: "manual publish guard execution"
      pattern: "rulestead_admin"
---

<objective>
Add the GitHub repository support files and the locally runnable CI shell scripts that back the workflows from Plan 05.

Purpose: Make the GitHub surface reproducible for contributors and ensure workflow logic is not trapped inside YAML.
Output: GitHub metadata files plus the Phase 1 `scripts/ci/*.sh` command surface.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repo-bootstrap/01-CONTEXT.md
@.planning/phases/01-repo-bootstrap/01-PATTERNS.md
@prompts/rulestead-release-engineering-and-ci.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GitHub repository support files</name>
  <files>.github/dependabot.yml, .github/pull_request_template.md, .github/CODEOWNERS, .github/ISSUE_TEMPLATE/bug_report.md, .github/ISSUE_TEMPLATE/feature_request.md</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, MAINTAINING.md, .github/workflows/dependabot-automerge.yml</read_first>
  <action>Create `.github/dependabot.yml` with weekly updates for the `mix` and `github-actions` ecosystems and patch-grouping behavior that matches D-04. Write `.github/pull_request_template.md` with a conventional-commit title reminder plus a short checklist for tests/docs. Write `.github/CODEOWNERS` with the maintainer handle already used in the repo. Add `bug_report.md` and `feature_request.md` issue templates using simple GSD/community defaults. Do not add external-contributor automation or Phase 8-specific templates here.</action>
  <acceptance_criteria>
    - `rg -n 'package-ecosystem: \"mix\"|package-ecosystem: \"github-actions\"' .github/dependabot.yml` returns both ecosystems.
    - `rg -n 'Conventional Commit|conventional commit' .github/pull_request_template.md` returns one match.
    - `test -s .github/CODEOWNERS` succeeds.
    - `test -f .github/ISSUE_TEMPLATE/bug_report.md` and `test -f .github/ISSUE_TEMPLATE/feature_request.md` succeed.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .github/dependabot.yml &amp;&amp; test -f .github/pull_request_template.md &amp;&amp; test -s .github/CODEOWNERS &amp;&amp; test -f .github/ISSUE_TEMPLATE/bug_report.md &amp;&amp; test -f .github/ISSUE_TEMPLATE/feature_request.md &amp;&amp; rg -n "package-ecosystem: \"mix\"|package-ecosystem: \"github-actions\"" .github/dependabot.yml &amp;&amp; rg -n "Conventional Commit|conventional commit" .github/pull_request_template.md'</automated>
  </verify>
  <done>The repository metadata files support the workflow and review model described in D-04 and D-08.</done>
</task>

<task type="auto">
  <name>Task 2: Add the release-parity issue template and primary CI scripts</name>
  <files>.github/ISSUE_TEMPLATE/release-parity-drift.md, scripts/ci/lint.sh, scripts/ci/test.sh, scripts/ci/release_gate.sh, scripts/ci/integration_placeholder.sh</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, .github/workflows/ci.yml, prompts/rulestead-release-engineering-and-ci.md</read_first>
  <action>Create `.github/ISSUE_TEMPLATE/release-parity-drift.md` now because D-04 lists it among the supporting GitHub files even though the daily drift cron is deferred to Phase 8; make the template inert until that workflow exists. Create `scripts/ci/lint.sh`, `scripts/ci/test.sh`, `scripts/ci/release_gate.sh`, and `scripts/ci/integration_placeholder.sh` with the required header `set -euo pipefail` and `RULESTEAD_REPO=\"${GITHUB_WORKSPACE:-$(pwd)}\"`. `lint.sh` must run the exact Phase 1 lint command sequence used in `ci.yml`, including the package-whitelist check script and single-cell Dialyzer. `test.sh` must run the Phase 1 two-package test flow expected by the matrix job. `release_gate.sh` must accept the needed job result inputs and fail when any required predecessor is not `success`. `integration_placeholder.sh` must print the exact placeholder message `integration placeholder; real integration lands in Phase 5` and exit 0.</action>
  <acceptance_criteria>
    - `rg -n 'RULESTEAD_REPO=\"\\$\\{GITHUB_WORKSPACE:-\\$\\(pwd\\)\\}\"|set -euo pipefail' scripts/ci/lint.sh scripts/ci/test.sh scripts/ci/release_gate.sh scripts/ci/integration_placeholder.sh` returns matches in all four scripts.
    - `rg -n 'mix docs --warnings-as-errors|mix hex.audit|mix dialyzer' scripts/ci/lint.sh` returns all three commands.
    - `rg -n 'integration placeholder; real integration lands in Phase 5' scripts/ci/integration_placeholder.sh` returns one match.
    - `test -f .github/ISSUE_TEMPLATE/release-parity-drift.md` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .github/ISSUE_TEMPLATE/release-parity-drift.md &amp;&amp; test -f scripts/ci/lint.sh &amp;&amp; test -f scripts/ci/test.sh &amp;&amp; test -f scripts/ci/release_gate.sh &amp;&amp; test -f scripts/ci/integration_placeholder.sh &amp;&amp; rg -n "RULESTEAD_REPO=\"\\$\\{GITHUB_WORKSPACE:-\\$\\(pwd\\)\\}\"|set -euo pipefail" scripts/ci/lint.sh scripts/ci/test.sh scripts/ci/release_gate.sh scripts/ci/integration_placeholder.sh &amp;&amp; rg -n "mix docs --warnings-as-errors|mix hex.audit|mix dialyzer" scripts/ci/lint.sh &amp;&amp; rg -n "integration placeholder; real integration lands in Phase 5" scripts/ci/integration_placeholder.sh'</automated>
  </verify>
  <done>The main GitHub metadata files and the primary locally runnable CI scripts exist and match the workflow contract from Plan 05.</done>
</task>

<task type="auto">
  <name>Task 3: Add package whitelist and admin publish guard scripts</name>
  <files>scripts/ci/check_package_whitelist.sh, scripts/ci/admin_publish_guard.sh</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, rulestead/mix.exs, rulestead_admin/mix.exs, rulestead_admin/lib/rulestead_admin/router.ex, .github/workflows/publish-hex.yml</read_first>
  <action>Create `scripts/ci/check_package_whitelist.sh` so it runs `mix hex.publish --dry-run` in both `rulestead/` and `rulestead_admin/` and checks the generated package contents for cross-contamination, specifically failing if the core tarball contains `rulestead_admin/` or if the admin tarball contains `rulestead/`. Create `scripts/ci/admin_publish_guard.sh` so it reads `rulestead_admin/lib/rulestead_admin/router.ex` and fails if the Phase 1 stub message is still present, enforcing D-03 before any admin publish path runs. Use the common shell-script header in both files.</action>
  <acceptance_criteria>
    - `rg -n 'mix hex.publish --dry-run' scripts/ci/check_package_whitelist.sh` returns matches for both packages.
    - `rg -n 'rulestead_admin/|rulestead/' scripts/ci/check_package_whitelist.sh` returns the cross-contamination checks.
    - `rg -n 'Phases 6.?7 of v0\\.1\\.0|router\\.ex' scripts/ci/admin_publish_guard.sh` returns one match proving the stub-content guard exists.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f scripts/ci/check_package_whitelist.sh &amp;&amp; test -f scripts/ci/admin_publish_guard.sh &amp;&amp; rg -n "mix hex.publish --dry-run|rulestead_admin/|rulestead/" scripts/ci/check_package_whitelist.sh &amp;&amp; rg -n "Phases 6.?7 of v0\\.1\\.0|router\\.ex" scripts/ci/admin_publish_guard.sh'</automated>
  </verify>
  <done>The publish-time package safety checks are callable outside GitHub Actions and enforce the same protections that the workflows expect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor→shell scripts | Locally editable scripts later execute inside CI and release workflows |
| package tarball→Hex consumers | Package contents become public artifacts once published |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | T | `scripts/ci/check_package_whitelist.sh` | mitigate | Explicitly inspect dry-run package contents for sibling-directory leakage before publication, matching the D-05 whitelist constraints. |
| T-01-17 | E | `scripts/ci/admin_publish_guard.sh` | mitigate | Fail if the router stub text still exists so the empty admin package cannot be promoted through a manual publish path. |
| T-01-18 | R | `scripts/ci/*.sh` | mitigate | Use a consistent header and repo-root variable in every script so workflow behavior is reproducible locally and failures are diagnosable outside GitHub. |
</threat_model>

<verification>
Run the grep checks and inspect that every script starts with the required shell safety header before considering the workflow surface complete.
</verification>

<success_criteria>
Phase 1 workflow logic is reproducible from the command line and the GitHub metadata around PRs, issues, and Dependabot is present and aligned with the repo policy.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-bootstrap/01-06-SUMMARY.md`
</output>
