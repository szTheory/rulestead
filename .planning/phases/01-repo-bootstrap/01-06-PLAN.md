---
phase: 01-repo-bootstrap
plan: 06
type: execute
wave: 5
depends_on:
  - 01-04
  - 01-05
files_modified:
  - .github/dependabot.yml
  - .github/pull_request_template.md
  - .github/CODEOWNERS
  - .github/ISSUE_TEMPLATE/bug_report.md
  - .github/ISSUE_TEMPLATE/feature_request.md
  - .github/ISSUE_TEMPLATE/release-parity-drift.md
autonomous: true
requirements:
  - REL-02
must_haves:
  truths:
    - "GitHub repository support files match the workflow expectations and maintainer policy."
    - "Dependabot, PR, CODEOWNERS, and issue-template metadata match the GitHub workflow and maintainer policy surface."
  artifacts:
    - path: ".github/dependabot.yml"
      provides: "Weekly Dependabot coverage for Mix and GitHub Actions"
    - path: ".github/pull_request_template.md"
      provides: "Conventional-commit and validation reminder for pull requests"
    - path: ".github/ISSUE_TEMPLATE/release-parity-drift.md"
      provides: "Issue intake template reserved for the later release-parity workflow"
  key_links:
    - from: ".github/dependabot.yml"
      to: ".github/workflows/dependabot-automerge.yml"
      via: "shared Dependabot PR lifecycle"
      pattern: "github-actions"
    - from: ".github/pull_request_template.md"
      to: ".github/workflows/pr-title.yml"
      via: "shared conventional-commit PR title expectation"
      pattern: "Conventional Commit"
---

<objective>
Add the GitHub repository support files and the locally runnable CI shell scripts that back the workflows from Plan 05.

Purpose: Make the GitHub surface reproducible for contributors and ensure workflow logic is not trapped inside YAML.
Output: GitHub metadata files aligned with the workflow and maintainer policy surface.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repo-bootstrap/01-CONTEXT.md
@.planning/phases/01-repo-bootstrap/01-PATTERNS.md
@prompts/rulestead-release-engineering-and-ci.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GitHub repository support files</name>
  <files>.github/dependabot.yml, .github/pull_request_template.md, .github/CODEOWNERS, .github/ISSUE_TEMPLATE/bug_report.md, .github/ISSUE_TEMPLATE/feature_request.md</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, MAINTAINING.md, .github/workflows/dependabot-automerge.yml</read_first>
  <action>Create `.github/dependabot.yml` with weekly updates for the `mix` and `github-actions` ecosystems and patch-grouping behavior that matches D-04. Write `.github/pull_request_template.md` with a conventional-commit title reminder plus a short checklist for tests/docs. Write `.github/CODEOWNERS` with the maintainer handle already used in the repo. Add `bug_report.md` and `feature_request.md` issue templates using simple GSD/community defaults. Do not add external-contributor automation or Phase 8-specific templates here.</action>
  <acceptance_criteria>
    - `rg -n 'package-ecosystem: \"mix\"|package-ecosystem: \"github-actions\"' .github/dependabot.yml` returns both ecosystems.
    - `rg -n 'Conventional Commit|conventional commit' .github/pull_request_template.md` returns one match.
    - `test -s .github/CODEOWNERS` succeeds.
    - `test -f .github/ISSUE_TEMPLATE/bug_report.md` and `test -f .github/ISSUE_TEMPLATE/feature_request.md` succeed.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .github/dependabot.yml &amp;&amp; test -f .github/pull_request_template.md &amp;&amp; test -s .github/CODEOWNERS &amp;&amp; test -f .github/ISSUE_TEMPLATE/bug_report.md &amp;&amp; test -f .github/ISSUE_TEMPLATE/feature_request.md &amp;&amp; rg -n "package-ecosystem: \"mix\"|package-ecosystem: \"github-actions\"" .github/dependabot.yml &amp;&amp; rg -n "Conventional Commit|conventional commit" .github/pull_request_template.md'</automated>
  </verify>
  <done>The repository metadata files support the workflow and review model described in D-04 and D-08.</done>
</task>

<task type="auto">
  <name>Task 2: Add the release-parity issue template and keep it dormant until Phase 8</name>
  <files>.github/ISSUE_TEMPLATE/release-parity-drift.md</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, MAINTAINING.md</read_first>
  <action>Create `.github/ISSUE_TEMPLATE/release-parity-drift.md` now because D-04 lists it among the supporting GitHub files even though the daily drift cron is deferred to Phase 8. Keep the template explicitly dormant: explain that it is reserved for the later release-parity verification workflow and should not imply that the cron or REL-03/REL-04 checks already exist in Phase 1.</action>
  <acceptance_criteria>
    - `test -f .github/ISSUE_TEMPLATE/release-parity-drift.md` succeeds.
    - `rg -n 'Phase 8|release parity|reserved for' .github/ISSUE_TEMPLATE/release-parity-drift.md` returns language that keeps the template dormant until the later workflow ships.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .github/ISSUE_TEMPLATE/release-parity-drift.md &amp;&amp; rg -n "Phase 8|release parity|reserved for" .github/ISSUE_TEMPLATE/release-parity-drift.md'</automated>
  </verify>
  <done>The remaining GitHub metadata is present, and the release-parity issue template is intentionally documented as a later-phase intake surface rather than a live Phase 1 workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor→shell scripts | Locally editable scripts later execute inside CI and release workflows |
| package tarball→Hex consumers | Package contents become public artifacts once published |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | T | `.github/dependabot.yml` | mitigate | Limit updates to the `mix` and `github-actions` ecosystems on a weekly cadence with patch grouping so dependency automation matches maintainer expectations. |
| T-01-17 | S | `.github/CODEOWNERS` | mitigate | Define the maintainer as the code owner so GitHub review surfaces route to the expected account. |
| T-01-18 | R | `.github/ISSUE_TEMPLATE/release-parity-drift.md` | mitigate | Mark the template as Phase 8-bound so users do not mistake the existence of the template for an already-running release-parity automation loop. |
</threat_model>

<verification>
Run the grep checks and inspect that the GitHub metadata aligns with the workflow names and maintainer policy before considering the repository support surface complete.
</verification>

<success_criteria>
The GitHub metadata around PRs, issues, CODEOWNERS, and Dependabot is present and aligned with the repo policy and the workflow surface created in Plan 05.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-bootstrap/01-06-SUMMARY.md`
</output>
