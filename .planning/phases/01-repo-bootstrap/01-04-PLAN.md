---
phase: 01-repo-bootstrap
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CONTRIBUTING.md
  - SECURITY.md
  - CODE_OF_CONDUCT.md
  - LICENSE
  - MAINTAINING.md
  - CLAUDE.md
  - AGENTS.md
  - guides/recipes/deployment.md
  - guides/recipes/context-propagation.md
autonomous: true
requirements:
  - DOC-01
  - REL-06
must_haves:
  truths:
    - "Contributors land on a real root README with a 60-second explanation and an honest pre-release quickstart."
    - "Maintainers and contributors have concrete written policy for branch protection, releases, reporting security issues, and repository norms."
    - "Phase 1 captures the future API-stability discipline as documented policy without prematurely generating the deferred Phase 8 files."
  artifacts:
    - path: "README.md"
      provides: "Root front-door documentation targeting Alex with pre-release quickstart"
    - path: "MAINTAINING.md"
      provides: "Release runbook and branch protection policy"
    - path: "CLAUDE.md"
      provides: "AI-agent project operating instructions"
  key_links:
    - from: "README.md"
      to: ".planning/ROADMAP.md"
      via: "pre-release status and progress tracking link"
      pattern: "ROADMAP"
    - from: "MAINTAINING.md"
      to: ".github/workflows/ci.yml"
      via: "required check names documented verbatim"
      pattern: "release_gate"
---

<objective>
Write the root-facing Phase 1 documentation and operational policy files, including the README quickstart, maintainer runbook, contributor/security/legal docs, and the AI-agent instructions expected in this repo.

Purpose: Make the repository credible and operable from commit 1 while capturing the partial REL-06 stability discipline without creating deferred Phase 8 files.
Output: Root docs and policy files with explicit branch-protection and release guidance.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repo-bootstrap/01-CONTEXT.md
@.planning/phases/01-repo-bootstrap/01-PATTERNS.md
@prompts/rulestead-personas-jtbd-and-onboarding.md
@prompts/rulestead-domain-language-field-guide.md
@prompts/rulestead-release-engineering-and-ci.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the root front door and community/legal docs</name>
  <files>README.md, CONTRIBUTING.md, SECURITY.md, CODE_OF_CONDUCT.md, LICENSE</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, .planning/ROADMAP.md, prompts/rulestead-personas-jtbd-and-onboarding.md, prompts/rulestead-domain-language-field-guide.md</read_first>
  <action>Write the root `README.md` in the exact C-Plus Phase 1 shape from `<specifics>`: tagline `Runtime decisions, made clear.`, a visible pre-release banner that says Phase 1 of 8 and links to `.planning/ROADMAP.md`, sections for `What this is (60 seconds)`, `Who it's for`, `15-minute quickstart`, and `Feature highlights`. Keep the quickstart concrete with the exact package names `{:rulestead, "~> 0.1"}` and `{:rulestead_admin, "~> 0.1"}`, `mix deps.get`, `mix rulestead.install`, `mix ecto.migrate`, `Rulestead.enabled?("checkout_v2", conn)`, and the `rulestead_admin "/admin/flags", policy: MyApp.RulesteadAdminPolicy` router example. Write `CONTRIBUTING.md` with the repo toolchain floor, `docker-compose.yml` usage, root and per-package test commands, conventional commit expectations, and a pointer to the Code of Conduct. Write `SECURITY.md` with the disclosure contact/process and pre-1.0 support policy. Use the full Contributor Covenant 2.1 text for `CODE_OF_CONDUCT.md`. Write the MIT template into `LICENSE`.</action>
  <acceptance_criteria>
    - `rg -n 'Runtime decisions, made clear' README.md` returns one match.
    - `rg -n 'Phase 1 of 8|Phase 1 of 8' README.md` returns at least one match.
    - `rg -n 'mix rulestead\\.install|mix ecto\\.migrate|Rulestead\\.enabled\\?\\("checkout_v2", conn\\)' README.md` returns matches for all three commands/examples.
    - `rg -n 'Conventional Commits|conventional commit' CONTRIBUTING.md` returns one match.
    - `rg -n 'Contributor Covenant' CODE_OF_CONDUCT.md` returns one match.
    - `rg -n 'MIT License' LICENSE` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f README.md &amp;&amp; test -f CONTRIBUTING.md &amp;&amp; test -f SECURITY.md &amp;&amp; test -f CODE_OF_CONDUCT.md &amp;&amp; test -f LICENSE &amp;&amp; rg -n "Runtime decisions, made clear|mix rulestead\\.install|mix ecto\\.migrate|Rulestead\\.enabled\\?\\(\"checkout_v2\", conn\\)" README.md &amp;&amp; rg -n "Conventional Commits|conventional commit" CONTRIBUTING.md &amp;&amp; rg -n "Contributor Covenant" CODE_OF_CONDUCT.md &amp;&amp; rg -n "MIT License" LICENSE'</automated>
  </verify>
  <done>The repository has a truthful root README and the expected contributor/security/legal baseline documents without claiming unshipped Phase 2–8 functionality exists today.</done>
</task>

<task type="auto">
  <name>Task 2: Write maintainer, agent, and deferred-recipe policy docs</name>
  <files>MAINTAINING.md, CLAUDE.md, AGENTS.md, guides/recipes/deployment.md, guides/recipes/context-propagation.md</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, .planning/ROADMAP.md, prompts/rulestead-release-engineering-and-ci.md</read_first>
  <action>Write `MAINTAINING.md` as the Phase 1 release/ops runbook. Include the D-08 branch-protection settings verbatim, with the exact required check names `release_gate`, `Validate PR title`, and `dependency-review`, and explicitly note that `actionlint` is not required because it is path-filtered. Document the linked-versions release-please flow, the Phase 8 rule that release PRs stay unmerged until the admin UI is real, the default `GITHUB_TOKEN` release-please caveat, and the publish recovery path through `publish-hex.yml`. Capture the partial REL-06 scaffold by documenting that `guides/api_stability.md`, `guides/cheatsheet.cheatmd`, and `guides/flows/extending-rulestead.md` are deferred to Phase 8 and that internal modules should use `@moduledoc false` until the public surface is explicitly locked. Write `CLAUDE.md` and `AGENTS.md` as matching repo-specific instructions that point agents at `.planning/`, `prompts/`, the sibling-package layout, and the rule that Phase 8-only docs/files must not be created early. Write `guides/recipes/deployment.md` and `guides/recipes/context-propagation.md` in the approved placeholder form: a title, the shipping-phase note, and a roadmap link, with no speculative runtime instructions.</action>
  <acceptance_criteria>
    - `rg -n 'release_gate|Validate PR title|dependency-review' MAINTAINING.md` returns all three required check names.
    - `rg -n 'actionlint' MAINTAINING.md` returns a note explaining it is not a required status check.
    - `rg -n 'api_stability\\.md|cheatsheet\\.cheatmd|extending-rulestead\\.md' MAINTAINING.md` returns one match covering the deferred REL-06 artifacts.
    - `rg -n '\\.planning/|prompts/|rulestead_admin/' CLAUDE.md AGENTS.md` returns matches in both files.
    - `rg -n 'See .*ROADMAP' guides/recipes/deployment.md guides/recipes/context-propagation.md` returns matches in both files.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f MAINTAINING.md &amp;&amp; test -f CLAUDE.md &amp;&amp; test -f AGENTS.md &amp;&amp; test -f guides/recipes/deployment.md &amp;&amp; test -f guides/recipes/context-propagation.md &amp;&amp; rg -n "release_gate|Validate PR title|dependency-review|actionlint|api_stability\\.md|cheatsheet\\.cheatmd|extending-rulestead\\.md" MAINTAINING.md &amp;&amp; rg -n "\\.planning/|prompts/|rulestead_admin/" CLAUDE.md AGENTS.md &amp;&amp; rg -n "ROADMAP" guides/recipes/deployment.md guides/recipes/context-propagation.md'</automated>
  </verify>
  <done>Maintainers and AI agents have explicit instructions, and the repo documents the Phase 8-only stability artifacts as deferred policy rather than creating premature placeholder files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs→contributors/users | Documentation shapes user expectations and maintainer operational behavior |
| maintainer policy→GitHub settings | Written branch-protection instructions are used to configure real repository controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | S | `README.md` quickstart | mitigate | Keep the pre-release banner explicit and link to the roadmap so users cannot mistake the target API for already-published behavior. |
| T-01-11 | T | `MAINTAINING.md` branch protection guidance | mitigate | Record the exact required check names from D-08 verbatim, reducing the chance of misconfigured protections that silently weaken merge gates. |
| T-01-12 | I | `SECURITY.md` and guide placeholders | mitigate | Avoid speculative runtime/security claims and provide only the actual disclosure process plus deferred-guide notices, preventing false assurances about unbuilt features. |
</threat_model>

<verification>
Run the task-level grep checks and manually scan the README banner plus MAINTAINING required-check list for exact wording before closing the plan.
</verification>

<success_criteria>
The repository presents a serious, honest public face and a usable maintainer playbook, including the partial REL-06 policy scaffold while leaving the deferred Phase 8 files absent.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-bootstrap/01-04-SUMMARY.md`
</output>
