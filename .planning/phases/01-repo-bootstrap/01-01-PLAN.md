---
phase: 01-repo-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .tool-versions
  - .formatter.exs
  - .credo.exs
  - .dialyzer_ignore.exs
  - docker-compose.yml
  - release-please-config.json
  - .release-please-manifest.json
autonomous: true
requirements:
  - REL-01
must_haves:
  truths:
    - "The repository exposes a locked Elixir/OTP toolchain and shared formatter/Credo settings at the root."
    - "Release metadata exists for a linked-version two-package release flow without publishing anything yet."
    - "Contributors can boot local Postgres and run the shared repo tooling from the monorepo root."
  artifacts:
    - path: ".tool-versions"
      provides: "Strict Elixir 1.19.2 / OTP 28.1.2 pinning"
    - path: "release-please-config.json"
      provides: "Linked-versions release-please configuration for rulestead and rulestead_admin"
    - path: ".release-please-manifest.json"
      provides: "Bootstrap version manifest seeded at 0.0.0 for both packages"
  key_links:
    - from: ".tool-versions"
      to: ".github/workflows/ci.yml"
      via: "erlef/setup-beam version-file usage in later CI plan"
      pattern: "version-file: \\.tool-versions"
    - from: "release-please-config.json"
      to: ".release-please-manifest.json"
      via: "shared linked-versions component names"
      pattern: "\"rulestead_admin\""
---

<objective>
Create the shared Phase 1 foundation files that every later plan assumes: strict toolchain pins, shared formatting/linting config, local Postgres bootstrap, and linked-versions release metadata for the two sibling packages.

Purpose: Lock the repository-wide defaults early so package, CI, and docs plans can build against stable root conventions.
Output: Shared root config files plus release-please bootstrap metadata.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repo-bootstrap/01-CONTEXT.md
@.planning/phases/01-repo-bootstrap/01-PATTERNS.md
@prompts/rulestead-release-engineering-and-ci.md
@prompts/rulestead-engineering-dna-from-prior-libs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared repo toolchain and linting foundation</name>
  <files>.tool-versions, .formatter.exs, .credo.exs, .dialyzer_ignore.exs, docker-compose.yml</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, prompts/rulestead-release-engineering-and-ci.md, prompts/rulestead-engineering-dna-from-prior-libs.md</read_first>
  <action>Implement the root toolchain and repo config exactly per D-05, D-06, and D-09. Write `.tool-versions` with `elixir 1.19.2-otp-28` and `erlang 28.1.2`. Write the root `.formatter.exs` so `import_deps` is exactly `[:phoenix, :ecto, :phoenix_live_view, :plug]` and the inputs cover root `{mix,.formatter}.exs` plus `rulestead/{config,lib,test,priv/repo/migrations}/**/*.{ex,exs}` and `rulestead_admin/{config,lib,test,priv/repo/migrations}/**/*.{ex,exs}`. Write `.credo.exs` with strict-mode execution and `requires: []` because custom checks are deferred per D-13. Commit an empty `.dialyzer_ignore.exs` so the later lint job can use `ignore_warnings: ".dialyzer_ignore.exs"` and `list_unused_filters: true` without creating the file on the fly. Add `docker-compose.yml` with a Postgres 15 service that uses `postgres/postgres/rulestead_dev`, publishes port `5432:5432`, and includes a `pg_isready` healthcheck for local contributor setup.</action>
  <acceptance_criteria>
    - `rg -n "^elixir 1\\.19\\.2-otp-28$" .tool-versions` returns one match.
    - `rg -n "^erlang 28\\.1\\.2$" .tool-versions` returns one match.
    - `rg -n "import_deps: \\[:phoenix, :ecto, :phoenix_live_view, :plug\\]" .formatter.exs` returns one match.
    - `rg -n "requires: \\[\\]" .credo.exs` returns one match.
    - `rg -n "image: postgres:15" docker-compose.yml` returns one match.
    - `test -f .dialyzer_ignore.exs` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .tool-versions && test -f .formatter.exs && test -f .credo.exs && test -f .dialyzer_ignore.exs && test -f docker-compose.yml &amp;&amp; rg -n "^elixir 1\\.19\\.2-otp-28$|^erlang 28\\.1\\.2$" .tool-versions &amp;&amp; rg -n "import_deps: \\[:phoenix, :ecto, :phoenix_live_view, :plug\\]" .formatter.exs &amp;&amp; rg -n "requires: \\[\\]" .credo.exs &amp;&amp; rg -n "postgres:15|pg_isready" docker-compose.yml'</automated>
  </verify>
  <done>The root repo has reproducible shared formatter/Credo/toolchain settings and a local Postgres bootstrap that later plans can reference without redefining values.</done>
</task>

<task type="auto">
  <name>Task 2: Add linked-versions release bootstrap metadata</name>
  <files>release-please-config.json, .release-please-manifest.json</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, prompts/rulestead-release-engineering-and-ci.md</read_first>
  <action>Write `release-please-config.json` exactly per D-02: `$schema` set to the upstream config schema URL, `"separate-pull-requests": false`, `"include-component-in-tag": true`, `"bump-minor-pre-major": true`, `"bump-patch-for-minor-pre-major": true`, and a `linked-versions` plugin named `rulestead-monorepo` with components `rulestead` and `rulestead_admin`. Define `packages.rulestead` with `release-type: "elixir"`, `package-name: "rulestead"`, `changelog-path: "rulestead/CHANGELOG.md"`, and `include-component-in-tag: true`. Define the matching `rulestead_admin` package stanza with `changelog-path: "rulestead_admin/CHANGELOG.md"`. Seed `.release-please-manifest.json` with `{"rulestead":"0.0.0","rulestead_admin":"0.0.0"}` so the first release PR can target `0.1.0` without an inferred `1.0.0` jump. Do not add any Phase 8 cron or publish behavior here.</action>
  <acceptance_criteria>
    - `rg -n '"type": "linked-versions"' release-please-config.json` returns one match.
    - `rg -n '"groupName": "rulestead-monorepo"' release-please-config.json` returns one match.
    - `rg -n '"changelog-path": "rulestead/CHANGELOG.md"' release-please-config.json` returns one match.
    - `rg -n '"changelog-path": "rulestead_admin/CHANGELOG.md"' release-please-config.json` returns one match.
    - `rg -n '"rulestead": "0.0.0"' .release-please-manifest.json` returns one match.
    - `rg -n '"rulestead_admin": "0.0.0"' .release-please-manifest.json` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'python3 -m json.tool release-please-config.json >/dev/null &amp;&amp; python3 -m json.tool .release-please-manifest.json >/dev/null &amp;&amp; rg -n "\"type\": \"linked-versions\"|\"groupName\": \"rulestead-monorepo\"|\"changelog-path\": \"rulestead/CHANGELOG.md\"|\"changelog-path\": \"rulestead_admin/CHANGELOG.md\"" release-please-config.json &amp;&amp; rg -n "\"rulestead\": \"0.0.0\"|\"rulestead_admin\": \"0.0.0\"" .release-please-manifest.json'</automated>
  </verify>
  <done>The monorepo advertises a valid two-package linked-versions release-please setup with both components bootstrapped at `0.0.0` and no Phase 8-only release flows pulled forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer→repo config | Trusted contributor writes files that later drive CI, Hex packaging, and local services |
| local docker→developer machine | Docker Compose starts a networked Postgres service from repo-managed config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `release-please-config.json` | mitigate | Hard-code only `rulestead` and `rulestead_admin` package entries per D-02 so later automation cannot drift into publishing extra components. |
| T-01-02 | D | `docker-compose.yml` | mitigate | Pin the compose service to `postgres:15` with a healthcheck and explicit credentials so contributors do not debug nondeterministic local boot failures. |
| T-01-03 | R | `.tool-versions` | mitigate | Use strict exact versions per D-05 so CI and local toolchains share the same version contract and Dialyzer cache keys remain stable. |
</threat_model>

<verification>
Run the task-level verification commands, then confirm the repo root contains only the expected shared bootstrap files and no Phase 5/8 artifacts such as `verify-published-release.yml`, `CONVENTIONS.md`, or `guides/api_stability.md`.
</verification>

<success_criteria>
Phase 1 execution can rely on a stable root toolchain, release metadata, and local Postgres bootstrap without reopening any layout or versioning decisions from D-01 through D-06.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-bootstrap/01-01-SUMMARY.md`
</output>
