---
phase: 01-repo-bootstrap
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead_admin/mix.exs
  - rulestead_admin/.formatter.exs
  - rulestead_admin/README.md
  - rulestead_admin/CHANGELOG.md
  - rulestead_admin/lib/rulestead_admin.ex
  - rulestead_admin/lib/rulestead_admin/router.ex
  - rulestead_admin/test/test_helper.exs
  - rulestead_admin/test/rulestead_admin_test.exs
autonomous: true
requirements:
  - REL-01
must_haves:
  truths:
    - "The sibling `rulestead_admin` package exists from day one without being publishable as an empty Hex artifact."
    - "The admin package can compile and test locally against the core package path dependency."
    - "The package exports a defensive router macro stub that blocks accidental production use before Phases 6 and 7 ship."
  artifacts:
    - path: "rulestead_admin/mix.exs"
      provides: "Admin package metadata and env-swapped core dependency"
    - path: "rulestead_admin/lib/rulestead_admin/router.ex"
      provides: "Stub `rulestead_admin/2` macro with an `ArgumentError` guard"
    - path: "rulestead_admin/test/rulestead_admin_test.exs"
      provides: "Admin skeleton compile/test proof"
  key_links:
    - from: "rulestead_admin/mix.exs"
      to: "rulestead_admin/lib/rulestead_admin/router.ex"
      via: "publish guard will later inspect router stub content"
      pattern: "defmacro rulestead_admin"
    - from: "rulestead_admin/mix.exs"
      to: "../rulestead"
      via: "local path dependency fallback"
      pattern: "path: \"../rulestead\""
---

<objective>
Create the Phase 1 `rulestead_admin` sibling package with the guarded router stub, local path dependency on `rulestead`, and empty-skeleton tests.

Purpose: Honor the locked sibling-package layout while preventing accidental publication or real admin usage before the admin UI phases.
Output: `rulestead_admin/` package skeleton with the D-03 guardrails built in.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repo-bootstrap/01-CONTEXT.md
@.planning/phases/01-repo-bootstrap/01-PATTERNS.md
@prompts/rulestead-release-engineering-and-ci.md
@prompts/rulestead-engineering-dna-from-prior-libs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the admin Mix project metadata and guarded package whitelist</name>
  <files>rulestead_admin/mix.exs, rulestead_admin/.formatter.exs, rulestead_admin/README.md, rulestead_admin/CHANGELOG.md</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, release-please-config.json, rulestead/mix.exs</read_first>
  <action>Create `rulestead_admin/mix.exs` with `@version "0.1.0"` and the D-03 env-swap dependency helper exactly: if `RULESTEAD_ADMIN_HEX_RELEASE == "1"` depend on `{:rulestead, "~> #{@version}"}`, otherwise depend on `{:rulestead, path: "../rulestead"}`. Configure package metadata for package name `rulestead_admin`, MIT license, and the Phase 1 admin whitelist `~w(lib priv .formatter.exs mix.exs README.md LICENSE CHANGELOG.md)`. Keep docs/basic aliases minimal; the admin UI does not ship yet. Write `rulestead_admin/.formatter.exs` in minimal package form without importing `:rulestead` yet, because that formatter export is deferred until Phase 2+. Write `rulestead_admin/README.md` as a short package-local note that the admin UI ships in Phases 6 and 7 and that the package remains unpublished until Phase 8. Write `rulestead_admin/CHANGELOG.md` with Keep-a-Changelog `Unreleased` scaffolding only.</action>
  <acceptance_criteria>
    - `rg -n 'RULESTEAD_ADMIN_HEX_RELEASE' rulestead_admin/mix.exs` returns one match.
    - `rg -n 'path: "\\.\\./rulestead"' rulestead_admin/mix.exs` returns one match.
    - `rg -n '~w\\(lib priv \\.formatter\\.exs mix\\.exs README\\.md LICENSE CHANGELOG\\.md\\)' rulestead_admin/mix.exs` returns one match.
    - `rg -n '^## \\[Unreleased\\]' rulestead_admin/CHANGELOG.md` returns one match.
    - `rg -n 'Phase 8|Phases 6 and 7' rulestead_admin/README.md` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f rulestead_admin/mix.exs &amp;&amp; test -f rulestead_admin/.formatter.exs &amp;&amp; test -f rulestead_admin/README.md &amp;&amp; test -f rulestead_admin/CHANGELOG.md &amp;&amp; rg -n "RULESTEAD_ADMIN_HEX_RELEASE|path: \"\\.\\./rulestead\"" rulestead_admin/mix.exs &amp;&amp; rg -n "^## \\[Unreleased\\]" rulestead_admin/CHANGELOG.md'</automated>
  </verify>
  <done>The admin package has the exact publish-time env swap and a safe Phase 1 package whitelist, without implying the real admin UI already exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add the admin module stub, guarded router macro, and empty-skeleton tests</name>
  <files>rulestead_admin/lib/rulestead_admin.ex, rulestead_admin/lib/rulestead_admin/router.ex, rulestead_admin/test/test_helper.exs, rulestead_admin/test/rulestead_admin_test.exs</files>
  <read_first>.planning/phases/01-repo-bootstrap/01-CONTEXT.md, .planning/phases/01-repo-bootstrap/01-PATTERNS.md, rulestead_admin/mix.exs</read_first>
  <action>Create `rulestead_admin/lib/rulestead_admin.ex` with `@moduledoc false` and only the minimal module reflection needed for the package skeleton. Create `rulestead_admin/lib/rulestead_admin/router.ex` exporting `defmacro rulestead_admin(path, opts \\\\ [])` that raises `ArgumentError` with the exact Phase 1 guard intent from D-03: `rulestead_admin: admin UI ships in Phases 6–7 of v0.1.0; track progress at <ROADMAP URL>`. Keep the macro signature stable because later phases will replace the body, not the mount API shape. Create `rulestead_admin/test/test_helper.exs` with `ExUnit.start()`. Create `rulestead_admin/test/rulestead_admin_test.exs` with a small assertion that the module loads and the router macro module exists; do not write fake LiveView tests before the UI package exists.</action>
  <acceptance_criteria>
    - `rg -n '^defmodule RulesteadAdmin do$' rulestead_admin/lib/rulestead_admin.ex` returns one match.
    - `rg -n '^defmodule RulesteadAdmin\\.Router do$' rulestead_admin/lib/rulestead_admin/router.ex` returns one match.
    - `rg -n '^defmacro rulestead_admin\\(path, opts \\\\ \\[\\]\\)' rulestead_admin/lib/rulestead_admin/router.ex` returns one match.
    - `rg -n 'Phases 6.?7 of v0\\.1\\.0' rulestead_admin/lib/rulestead_admin/router.ex` returns one match.
    - `rg -n 'ExUnit\\.start\\(' rulestead_admin/test/test_helper.exs` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'cd rulestead_admin &amp;&amp; mix test'</automated>
  </verify>
  <done>`cd rulestead_admin && mix test` succeeds and the sibling admin package cannot be mounted or published as a deceptive empty package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin package→Hex consumers | A package named `rulestead_admin` could mislead adopters if published before real UI functionality exists |
| admin package→core package | The sibling package depends on `rulestead` through a local path during development and Hex during publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | S | `rulestead_admin` package identity | mitigate | Use the D-03 router stub that raises immediately with a roadmap pointer, preventing the package from masquerading as a working admin UI. |
| T-01-08 | T | `rulestead_admin/mix.exs` dependency wiring | mitigate | Implement the exact `RULESTEAD_ADMIN_HEX_RELEASE` env swap so local development uses `path: "../rulestead"` and publish-time behavior cannot silently drift. |
| T-01-09 | R | accidental publish of empty admin package | mitigate | Keep the router stub content stable so Plan 05 can gate `publish-hex.yml` on the stub-vs-real implementation check before any admin publish happens. |
</threat_model>

<verification>
Run `cd rulestead_admin && mix test` and inspect the router stub error message to confirm it points at the roadmap rather than pretending the admin mount works.
</verification>

<success_criteria>
The repository now has the exact two-package Phase 1 layout from D-01 and D-03, with the admin package protected against accidental real-world use.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-bootstrap/01-03-SUMMARY.md`
</output>
