---
phase: 18-experimentation-ui-and-reporting
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/experiment_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/experiment_live/show.ex
  - rulestead_admin/lib/rulestead_admin/router.ex
  - rulestead_admin/test/rulestead_admin/live/experiment_live_test.exs
autonomous: true
requirements: [ANA-03]
must_haves:
  truths:
    - "Operators can view a list of active and historical experiments."
    - "Operators can see calculated conversion lifts and statistical significance."
    - "UI displays warnings for unexpected error rates."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/experiment_live/index.ex"
      provides: "Experiment list view"
    - path: "rulestead_admin/lib/rulestead_admin/live/experiment_live/show.ex"
      provides: "Experiment detail view with metrics"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/experiment_live/show.ex"
      to: "rulestead/lib/rulestead/analytics/query.ex"
      via: "Fetching aggregated stats for display"
---

<objective>
Build the Admin UI pages for Experimentation, allowing operators to monitor test results, lifts, and regressions.

Purpose: Expose Phase 16/17 metrics to users via Phoenix LiveView, leveraging established Rulestead UI components for consistency.
Output: Experiment Index and Show LiveViews.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-experimentation-ui-and-reporting/18-PATTERNS.md
@.planning/phases/18-experimentation-ui-and-reporting/RESEARCH.md
</context>

<interfaces>
<!-- Contracts expected from Plan 18-01. Executors will build against these. -->

From rulestead/lib/rulestead/analytics/query.ex:
```elixir
defmodule Rulestead.Analytics.Query do
  @doc "Fetches aggregated counts grouped by variation for a specific experiment and event."
  def get_variant_stats(experiment_id, target_event_name)
end
```

From rulestead/lib/rulestead/analytics/stats.ex:
```elixir
defmodule Rulestead.Analytics.Stats do
  @doc "Computes Z-score, p-value, and conversion lift given aggregated counts."
  def compute_significance(control_counts, variant_counts)
end
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Experiment Index LiveView</name>
  <files>rulestead_admin/lib/rulestead_admin/live/experiment_live/index.ex, rulestead_admin/lib/rulestead_admin/router.ex</files>
  <action>
    Implement `RulesteadAdmin.ExperimentLive.Index` using inline `~H` templates.
    Follow the exact structure defined in `18-PATTERNS.md` for `FlagLive.Index` (i.e. use `<Shell.page>`, `<section class="rs-inventory">`, and the `.rs-table` grid).
    Add the necessary routes to `router.ex` to expose `/environments/:env/experiments` (or equivalent per existing routing conventions).
  </action>
  <verify>
    <automated>mix test test/rulestead_admin/live/experiment_live_test.exs</automated>
  </verify>
  <done>Index route works and renders the basic shell and table.</done>
</task>

<task type="auto">
  <name>Task 2: Experiment Detail & Reporting UI LiveView</name>
  <files>rulestead_admin/lib/rulestead_admin/live/experiment_live/show.ex</files>
  <action>
    Implement `RulesteadAdmin.ExperimentLive.Show`.
    Follow the `FlagLive.Show` pattern from `18-PATTERNS.md`.
    Wire this view to use `Rulestead.Analytics.Query.get_variant_stats/2` and `Rulestead.Analytics.Stats.compute_significance/2` (built in Plan 18-01).
    Render statistical results using `<OperatorComponents.summary_grid>` and display guardrail metrics/warnings using `<OperatorComponents.banner tone="warning">`.
  </action>
  <verify>
    <automated>mix test test/rulestead_admin/live/experiment_live_test.exs</automated>
  </verify>
  <done>Show route resolves, fetches data from Analytics context, and renders UI components for results and guardrails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration Tests for Experiment UI</name>
  <files>rulestead_admin/test/rulestead_admin/live/experiment_live_test.exs</files>
  <behavior>
    - Test 1: Visits the experiment index and sees the list layout
    - Test 2: Visits a specific experiment show page and validates summary metrics are rendered
    - Test 3: Simulates a guardrail failure and ensures the warning banner is visible
  </behavior>
  <action>
    Write LiveView integration tests asserting the successful rendering of the new pages. Use standard ExUnit and Phoenix LiveViewTest patterns.
  </action>
  <verify>
    <automated>mix test test/rulestead_admin/live/experiment_live_test.exs</automated>
  </verify>
  <done>100% test pass rate for the new LiveView pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> Admin UI | Operators accessing experiment results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Elevation of Privilege | LiveView Router | mitigate | Ensure LiveView `on_mount` lifecycle hooks enforce standard admin access policies (RBAC/Auth) for new experiment routes. |
</threat_model>

<verification>
mix test test/rulestead_admin/live/experiment_live_test.exs
</verification>

<success_criteria>
- The `/experiments` route loads and lists experiments.
- The experiment detail page displays calculated lifts and significance.
- Unexpected error rates trigger a visible banner warning.
- Tests confirm end-to-end rendering and data fetching.
</success_criteria>

<output>
After completion, create `.planning/phases/18-experimentation-ui-and-reporting/18-02-SUMMARY.md`
</output>