---
phase: 18-experimentation-ui-and-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/analytics/query.ex
  - rulestead/test/rulestead/analytics/query_test.exs
  - rulestead/lib/rulestead/analytics/stats.ex
  - rulestead/test/rulestead/analytics/stats_test.exs
autonomous: true
requirements: [ANA-03]
must_haves:
  truths:
    - "System can calculate experiment variant conversion rates and exposure counts directly via database aggregation."
    - "System can calculate statistical significance (Z-score/p-value) for conversion lifts."
  artifacts:
    - path: "rulestead/lib/rulestead/analytics/query.ex"
      provides: "Ecto aggregation queries for exposures and conversions"
    - path: "rulestead/lib/rulestead/analytics/stats.ex"
      provides: "Statistical significance computations"
  key_links:
    - from: "rulestead/lib/rulestead/analytics/query.ex"
      to: "Postgres database"
      via: "Ecto.Query GROUP BY"
---

<objective>
Build backend capabilities for aggregating experiment data and calculating statistical significance.

Purpose: Offload heavy counting and grouping to the database via Ecto, and provide statistical tools to determine test significance without external dependencies.
Output: Tested Query and Stats modules within the Analytics context.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-experimentation-ui-and-reporting/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQL Aggregation Queries for Analytics</name>
  <files>rulestead/lib/rulestead/analytics/query.ex, rulestead/test/rulestead/analytics/query_test.exs</files>
  <behavior>
    - Test 1: Groups events by experiment variation using Ecto.Query
    - Test 2: Calculates total exposures per variant
    - Test 3: Calculates total conversions for a given target event per variant
  </behavior>
  <action>
    Create `Rulestead.Analytics.Query` to fetch exposure and conversion metrics for an experiment.
    Implement Ecto `GROUP BY` to aggregate metrics directly in Postgres, avoiding in-memory `Repo.all` followed by `Enum.filter`.
    Ensure queries filter by the relevant experiment identifier and target event names.
  </action>
  <verify>
    <automated>mix test test/rulestead/analytics/query_test.exs</automated>
  </verify>
  <done>Ecto query accurately groups and counts exposures/conversions per variant and passes all tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Statistical Significance Engine</name>
  <files>rulestead/lib/rulestead/analytics/stats.ex, rulestead/test/rulestead/analytics/stats_test.exs</files>
  <behavior>
    - Test 1: Computes conversion lift percentage between two variations
    - Test 2: Computes a Z-score and p-value for the conversion difference
    - Test 3: Determines if difference meets standard significance threshold (e.g. 95%)
  </behavior>
  <action>
    Create `Rulestead.Analytics.Stats` to compute standard A/B test statistics. 
    Accept aggregated counts (derived from Task 1) to calculate conversion rates, Z-scores, and p-values.
    Do not rely on external statistical APIs; implement math directly in Elixir.
  </action>
  <verify>
    <automated>mix test test/rulestead/analytics/stats_test.exs</automated>
  </verify>
  <done>Z-score and p-values correctly identify significance for provided aggregate counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal -> Database | Admin querying metrics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Info Disclosure | Analytics.Query | mitigate | Ensure metrics are scoped strictly to the requested experiment context |
</threat_model>

<verification>
mix test test/rulestead/analytics/
</verification>

<success_criteria>
- Exposures and conversions aggregated directly in DB.
- Statistical significance is accurately calculated based on those counts.
- 100% test pass rate for Query and Stats modules.
</success_criteria>

<output>
After completion, create `.planning/phases/18-experimentation-ui-and-reporting/18-01-SUMMARY.md`
</output>