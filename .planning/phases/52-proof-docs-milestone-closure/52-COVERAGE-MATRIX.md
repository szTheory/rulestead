# Phase 52 VER-01 Coverage Matrix

| Behavior | Existing Evidence | Gap | Plan 52 Action |
| --- | --- | --- | --- |
| stale signal | `rulestead/test/rulestead/guarded_rollout_test.exs` proves stale facts hold without mutating authored rollout state across fake and Ecto adapters. | Not bundled into a single repo-root proof scope. | Include `guarded_rollout_test.exs` in `guarded_rollout_foundations`. |
| insufficient sample | Guardrail normalization and decision tests cover `insufficient_sample` as a fail-closed fact. | Adapter-path evaluation was not proven through persisted rollout state. | Add adapter-parity test for `insufficient_sample` holding without authored-state mutation. |
| automatic hold | Guardrail decision persistence records held decisions and audit events. | Needed explicit proof bundle coverage for stale, insufficient sample, terminal host-seam fault, and missing rollback target. | Add focused adapter-path tests and include them in the bounded proof bar. |
| automatic rollback | Existing guarded rollout test proves rollback to a stable healthy stage on breach. | Not traced from requirement to proof command. | Reference existing rollback test in this matrix and run it through `guarded_rollout_foundations`. |
| terminal host-seam fault | Guardrail contract tests normalize unsupported host/provider facts as fail-closed. | Adapter-path terminal behavior needed to prove no healthy implication or authored-state mutation. | Add adapter-parity test for `unsupported_signal` holding without mutation. |
| breach without stable rollback target | Store logic degrades rollback-triggered decisions to held when no stable target exists. | Missing explicit adapter-path test for the degradation path. | Add adapter-parity test proving breach without stable target records hold. |
| bounded audit evidence | Guardrail decisions persist signal facts, guardrail evidence, and audit links for hold/rollback paths. | Needed a bounded proof scope rather than broad repo-wide claims. | Run guardrail contract, decision, guarded rollout, release contract, and release publish tests. |
| mounted status explanation | `rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_test.exs` covers mounted rollout status rendering. | Not included in VER-01 proof command. | Include mounted rollouts test in `guarded_rollout_foundations`. |
| mounted timeline explanation | `rulestead_admin/test/rulestead_admin/live/flag_live/timeline_test.exs` covers timeline explanation. | Not included in VER-01 proof command. | Include mounted timeline test in `guarded_rollout_foundations`. |
| authored-state boundary | Existing stale and rollback tests inspect active rollout percentages after guardrail evaluation. | Needed additional adapter-path checks for insufficient sample, terminal fault, and missing target. | Add percentage assertions for each new guarded rollout gap test. |
| support-truth drift guards | Release contract tests already guard public support posture. | Guarded rollout support truth was not locked across root, package, admin, and maintainer docs. | Extend release contract and publish-verification tests with guarded rollout bounded-support assertions. |
