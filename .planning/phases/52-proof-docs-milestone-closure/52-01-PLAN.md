---
phase: 52-proof-docs-milestone-closure
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/52-proof-docs-milestone-closure/52-COVERAGE-MATRIX.md
  - scripts/ci/test.sh
  - rulestead/test/rulestead/guarded_rollout_test.exs
  - rulestead/test/rulestead/release_contract_test.exs
  - rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs
  - README.md
  - rulestead/README.md
  - rulestead_admin/README.md
  - MAINTAINING.md
autonomous: true
requirements:
  - VER-01
must_haves:
  truths:
    - "Maintainers can run one bounded guarded rollout proof command from the repo root."
    - "Repo-local proof covers stale signal, insufficient sample, terminal host-seam fault, automatic hold, automatic rollback, mounted status/timeline explanation, and support-truth drift guards."
    - "Root and package docs describe guarded rollout support as host-owned, fail-closed, audited, and bounded."
  artifacts:
    - path: ".planning/phases/52-proof-docs-milestone-closure/52-COVERAGE-MATRIX.md"
      provides: "VER-01 behavior-to-evidence matrix"
      contains: "guarded_rollout_foundations"
    - path: "scripts/ci/test.sh"
      provides: "Named guarded rollout proof scope"
      contains: "guarded_rollout_foundations"
    - path: "rulestead/test/rulestead/guarded_rollout_test.exs"
      provides: "Adapter-path guarded rollout gap proof"
      contains: "insufficient_sample"
    - path: "rulestead/test/rulestead/release_contract_test.exs"
      provides: "Root/package/maintainer support-truth drift guards"
      contains: "guarded rollout foundations"
  key_links:
    - from: "scripts/ci/test.sh"
      to: "rulestead/test/rulestead/guarded_rollout_test.exs"
      via: "run_mix rulestead test"
      pattern: "guarded_rollout_foundations"
    - from: "scripts/ci/test.sh"
      to: "rulestead_admin/test/rulestead_admin/live/flag_live/rollouts_test.exs"
      via: "run_mix rulestead_admin test"
      pattern: "rollouts_test.exs"
    - from: "rulestead/test/rulestead/release_contract_test.exs"
      to: "README.md, rulestead/README.md, rulestead_admin/README.md, MAINTAINING.md"
      via: "File.read! assertions"
      pattern: "guarded rollout foundations"
---

<objective>
Create the bounded `guarded_rollout_foundations` proof bar, fill VER-01 proof gaps, and lock guarded rollout support truth into docs and drift tests.

Purpose: Phase 52 must prove the support surface already delivered by Phases 49-51 without adding rollout capability or widening Rulestead into observability.
Output: A coverage matrix, named repo-root proof scope, focused ExUnit gap tests, doc-contract assertions, and root/package/maintainer docs that agree on bounded guarded rollout support.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-proof-docs-milestone-closure/52-CONTEXT.md
@.planning/phases/52-proof-docs-milestone-closure/52-RESEARCH.md
@.planning/phases/52-proof-docs-milestone-closure/52-VALIDATION.md
@.planning/phases/52-proof-docs-milestone-closure/52-PATTERNS.md
@scripts/ci/test.sh
@rulestead/test/rulestead/guarded_rollout_test.exs
@rulestead/test/rulestead/release_contract_test.exs
@rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs
@README.md
@rulestead/README.md
@rulestead_admin/README.md
@MAINTAINING.md

<interfaces>
From `scripts/ci/test.sh`:
```bash
run_mix() {
  local package_dir="$1"
  shift
  (
    cd "${RULESTEAD_REPO}/${package_dir}"
    mix "$@"
  )
}

case "${TEST_SCOPE}" in
  mounted_admin_contract)
    echo "Running mounted lifecycle/admin contract proof bar"
    run_mounted_admin_contract
    ;;
  openfeature_companion)
    echo "Running OpenFeature companion provider proof bar"
    run_openfeature_companion
    ;;
esac
```

From `rulestead/test/rulestead/guarded_rollout_test.exs`:
```elixir
@adapters [Rulestead.Fake, StoreEcto]

Enum.each(@adapters, fn adapter ->
  reset_adapter!(adapter)
  flag_key = "checkout-held-#{adapter_suffix(adapter)}"
  seed_published_rollout!(adapter, flag_key)
end)
```

From `rulestead/lib/rulestead/guardrail_decision.ex`:
```elixir
field(:decision_state, Ecto.Enum, values: [:healthy, :pending_data, :held, :rollback_triggered])
field(:action_type, Ecto.Enum, values: [:advance, :evaluate, :hold, :rollback])
field(:signal_facts, {:array, :map}, default: [])
field(:guardrail_evidence, :map, default: %{})
field(:metadata, :map, default: %{})
```
</interfaces>
</context>

<source_audit>
| Source | Item | Coverage |
| --- | --- | --- |
| GOAL | Reclose support truth for guarded rollout foundations with bounded proof, docs, and traceability | Covered by Tasks 1-3 and Plan 02 |
| REQ | `VER-01` repo-local proof and docs cover stale-signal, insufficient-sample, hold, rollback, and bounded host-seam behavior | Covered by Tasks 1-3 |
| RESEARCH | Use `scripts/ci/test.sh`; aggregate existing suites; add only focused gap tests; extend docs drift guards | Covered by Tasks 1-3 |
| CONTEXT | D-01 through D-16 and D-21: bounded proof scope, matrix-first gaps, docs support truth, forbidden claims | Covered by Tasks 1-3 |
| CONTEXT | Deferred auto-advance, metrics ingestion, dashboards, provider adapters, standalone admin, broad browser/demo smoke bars | Excluded from all tasks |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create VER-01 matrix, proof scope, and core gap tests</name>
  <files>.planning/phases/52-proof-docs-milestone-closure/52-COVERAGE-MATRIX.md, scripts/ci/test.sh, rulestead/test/rulestead/guarded_rollout_test.exs</files>
  <read_first>
    .planning/phases/52-proof-docs-milestone-closure/52-CONTEXT.md
    .planning/phases/52-proof-docs-milestone-closure/52-RESEARCH.md
    .planning/phases/52-proof-docs-milestone-closure/52-PATTERNS.md
    scripts/ci/test.sh
    rulestead/test/rulestead/guarded_rollout_test.exs
    rulestead/test/rulestead/guardrails/contract_test.exs
    rulestead/test/rulestead/guardrails/decision_test.exs
    rulestead/lib/rulestead/guardrail_decision.ex
    rulestead/lib/rulestead/store/command.ex
  </read_first>
  <behavior>
    - Test 1: `RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh` prints `Running guarded rollout foundations proof bar`.
    - Test 2: adapter-path insufficient sample evaluates to `:held` with `decision_reason == "insufficient_sample"` and leaves authored rollout percentage unchanged.
    - Test 3: adapter-path terminal host-seam fault evaluates to `:held` with `decision_reason == "unsupported_signal"` and leaves authored rollout percentage unchanged.
    - Test 4: breached signal with no recorded stable target evaluates to `:held`, not `:rollback_triggered`, and leaves authored rollout percentage unchanged.
  </behavior>
  <action>
    Per D-01, D-02, D-03, D-05, D-06, D-07, and D-21, create `.planning/phases/52-proof-docs-milestone-closure/52-COVERAGE-MATRIX.md` with a markdown table containing these exact behavior labels: `stale signal`, `insufficient sample`, `automatic hold`, `automatic rollback`, `terminal host-seam fault`, `breach without stable rollback target`, `bounded audit evidence`, `mounted status explanation`, `mounted timeline explanation`, `authored-state boundary`, and `support-truth drift guards`. Each row must include `Existing Evidence`, `Gap`, and `Plan 52 Action`.

    Add a `run_guarded_rollout_foundations` function to `scripts/ci/test.sh` near the existing proof functions. It must run this exact bounded command set and no broad browser/demo/provider smoke:
    - `run_mix rulestead deps.get`
    - `prepare_rulestead_test_db`
    - `run_mix rulestead test test/rulestead/guardrails/contract_test.exs test/rulestead/guardrails/decision_test.exs test/rulestead/guarded_rollout_test.exs test/rulestead/release_contract_test.exs test/rulestead/mix/tasks/verify_release_publish_test.exs`
    - `run_mix rulestead_admin deps.get`
    - `run_mix rulestead_admin test test/rulestead_admin/live/flag_live/rollouts_test.exs test/rulestead_admin/live/flag_live/timeline_test.exs`

    Add a `guarded_rollout_foundations)` branch to the `case "${TEST_SCOPE}"` dispatch that echoes `Running guarded rollout foundations proof bar` and calls `run_guarded_rollout_foundations`. Update the unknown-scope support line to exactly include `all, mounted_admin_contract, openfeature_companion, guarded_rollout_foundations`.

    In `rulestead/test/rulestead/guarded_rollout_test.exs`, add focused adapter-parity tests using the existing `Enum.each(@adapters)` pattern and `seed_published_rollout!/2`. Add tests with these exact names: `insufficient sample after the monitoring window holds the rollout without mutating authored state`, `terminal host seam faults hold the rollout without mutating authored state`, and `breach without a recorded stable target degrades to hold`. Use signal facts with `status: :failed_closed, reason: :insufficient_sample`; `status: :failed_closed, reason: :unsupported_signal`; and `status: :breached, reason: :breached`. Assert the latest decision state and reason through the returned decision, then fetch the flag and assert the rollout percentage remains the advanced percentage for held cases.
  </action>
  <verify>
    <automated>RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh</automated>
    <automated>rg -n "guarded_rollout_foundations|Running guarded rollout foundations proof bar|insufficient sample after the monitoring window holds|terminal host seam faults hold|breach without a recorded stable target degrades to hold" .planning/phases/52-proof-docs-milestone-closure/52-COVERAGE-MATRIX.md scripts/ci/test.sh rulestead/test/rulestead/guarded_rollout_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/test.sh` contains `guarded_rollout_foundations)`.
    - `scripts/ci/test.sh` contains `Running guarded rollout foundations proof bar`.
    - `scripts/ci/test.sh` contains `test/rulestead/guardrails/contract_test.exs test/rulestead/guardrails/decision_test.exs test/rulestead/guarded_rollout_test.exs test/rulestead/release_contract_test.exs test/rulestead/mix/tasks/verify_release_publish_test.exs`.
    - `scripts/ci/test.sh` contains `test/rulestead_admin/live/flag_live/rollouts_test.exs test/rulestead_admin/live/flag_live/timeline_test.exs`.
    - `.planning/phases/52-proof-docs-milestone-closure/52-COVERAGE-MATRIX.md` contains all labels listed in the action.
    - `rulestead/test/rulestead/guarded_rollout_test.exs` contains the three exact test names listed in the action.
    - `RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh` exits 0.
  </acceptance_criteria>
  <done>The named proof scope exists, the VER-01 coverage matrix exists, core adapter-path proof gaps are covered, and the bounded proof command passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update root package and maintainer support truth</name>
  <files>README.md, rulestead/README.md, rulestead_admin/README.md, MAINTAINING.md</files>
  <read_first>
    .planning/phases/52-proof-docs-milestone-closure/52-CONTEXT.md
    .planning/phases/52-proof-docs-milestone-closure/52-RESEARCH.md
    README.md
    rulestead/README.md
    rulestead_admin/README.md
    MAINTAINING.md
    rulestead/test/rulestead/release_contract_test.exs
    rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs
  </read_first>
  <action>
    Per D-10 through D-16 and D-21, update only existing root/package/maintainer docs; do not create a new guide. In `README.md`, add or reconcile a bounded section using the heading text `Guarded rollout foundations`. Include these exact phrases: `host-supplied normalized guardrail facts`, `explicit threshold, freshness, and sample-size semantics`, `fail closed`, `pending_data`, `held`, `rollback_triggered`, `audited hold and rollback`, `mounted status inside the existing workflow`, and `RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh`.

    In `rulestead/README.md`, add or reconcile a runtime contract anchor using the exact phrase `host-owned metrics provider seam`. Include these exact phrases: `authored guardrail definitions`, `deterministic sticky rollout decisions`, `audited hold and rollback`, `no metrics ingestion`, `no dashboards`, `no statistics engine`, and `no built-in provider adapters`.

    In `rulestead_admin/README.md`, add or reconcile a mounted companion anchor using the exact phrase `mounted companion status contract`. Include these exact phrases: `reads core guardrail status and audit truth`, `thresholds, freshness, and reasons`, `fails closed on missing prerequisites`, `not a standalone admin`, and `mounted Phoenix host app`.

    In `MAINTAINING.md`, add a maintainer rerun entry using the exact phrase `guarded rollout foundations proof`. Include `VER-01` and `RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh`. State that this proof is bounded to stale signals, insufficient samples, automatic hold, automatic rollback, mounted explanation, and support-truth drift guards. Explicitly state that it does not prove metrics ingestion, dashboards, provider adapters, browser demo flows, or auto-advance.

    Across all four docs, avoid these exact forbidden phrases: `automatic progressive delivery platform`, `built-in observability`, `real-time dashboards`, `self-healing rollouts`, `vendor metrics integrations`, `experiment statistics`, `standalone rulestead_admin`, and `auto-advance`.
  </action>
  <verify>
    <automated>rg -n "Guarded rollout foundations|host-owned metrics provider seam|mounted companion status contract|guarded rollout foundations proof|RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh" README.md rulestead/README.md rulestead_admin/README.md MAINTAINING.md</automated>
    <automated>! rg -n "automatic progressive delivery platform|built-in observability|real-time dashboards|self-healing rollouts|vendor metrics integrations|experiment statistics|standalone rulestead_admin|auto-advance" README.md rulestead/README.md rulestead_admin/README.md MAINTAINING.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `Guarded rollout foundations`.
    - `README.md` contains `RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh`.
    - `rulestead/README.md` contains `host-owned metrics provider seam`.
    - `rulestead_admin/README.md` contains `mounted companion status contract`.
    - `MAINTAINING.md` contains `guarded rollout foundations proof`.
    - `MAINTAINING.md` contains `VER-01`.
    - The forbidden phrase `rg` command from `<verify>` returns no matches in the four docs.
  </acceptance_criteria>
  <done>Root, runtime package, mounted companion package, and maintainer docs present the same bounded guarded rollout support truth before drift guards are added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend support-truth drift guards</name>
  <files>rulestead/test/rulestead/release_contract_test.exs, rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs</files>
  <read_first>
    .planning/phases/52-proof-docs-milestone-closure/52-CONTEXT.md
    .planning/phases/52-proof-docs-milestone-closure/52-RESEARCH.md
    rulestead/test/rulestead/release_contract_test.exs
    rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs
    README.md
    rulestead/README.md
    rulestead_admin/README.md
    MAINTAINING.md
  </read_first>
  <behavior>
    - Test 1: root docs mention `RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh`.
    - Test 2: runtime docs mention host-owned metrics provider seam and explicitly reject metrics ingestion, dashboards, statistics engine, and built-in provider adapters.
    - Test 3: admin docs mention mounted companion status contract and `rulestead_admin` remains mounted, not standalone.
    - Test 4: docs and maintainer guidance reject forbidden guarded rollout claims from D-16.
  </behavior>
  <action>
    Per D-10 through D-16, after Task 2 has updated the docs, add a release contract test named exactly `guarded rollout support truth stays bounded across root package and maintainer docs` to `rulestead/test/rulestead/release_contract_test.exs`. The test must read `@root_readme_path`, `@runtime_readme_path`, `@admin_readme_path`, and `@maintaining_path`. Assert these exact strings:
    - root README: `guarded rollout foundations`, `host-supplied normalized guardrail facts`, `fail closed`, `pending_data`, `held`, `rollback_triggered`, `audited hold and rollback`, `RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh`
    - runtime README: `host-owned metrics provider seam`, `deterministic sticky rollout decisions`, `audited hold and rollback`, `no metrics ingestion`, `no dashboards`, `no statistics engine`, `no built-in provider adapters`
    - admin README: `mounted companion status contract`, `reads core guardrail status and audit truth`, `thresholds, freshness, and reasons`, `fails closed on missing prerequisites`, `not a standalone admin`
    - MAINTAINING: `guarded rollout foundations proof`, `RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh`, `VER-01`

    In the same test, iterate over this exact forbidden phrase list and refute every phrase across all four docs: `automatic progressive delivery platform`, `built-in observability`, `real-time dashboards`, `self-healing rollouts`, `vendor metrics integrations`, `experiment statistics`, `standalone rulestead_admin`, `auto-advance`.

    In `rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs`, extend `published release verification still depends on lifecycle doc discoverability` or add a nearby test named exactly `published release verification keeps guarded rollout support truth bounded`. It must assert the root, runtime, and admin README strings from this task, including `RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh`, and refute `built-in observability`, `real-time dashboards`, and `standalone rulestead_admin`.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/release_contract_test.exs test/rulestead/mix/tasks/verify_release_publish_test.exs</automated>
    <automated>RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh</automated>
    <automated>rg -n "guarded rollout support truth stays bounded|published release verification keeps guarded rollout support truth bounded|automatic progressive delivery platform|RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh" rulestead/test/rulestead/release_contract_test.exs rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rulestead/test/rulestead/release_contract_test.exs` contains `guarded rollout support truth stays bounded across root package and maintainer docs`.
    - `rulestead/test/rulestead/release_contract_test.exs` contains every required asserted string listed in the action.
    - `rulestead/test/rulestead/release_contract_test.exs` contains every forbidden phrase listed in the action as a `refute` target.
    - `rulestead/test/rulestead/mix/tasks/verify_release_publish_test.exs` contains `published release verification keeps guarded rollout support truth bounded`.
    - Both automated test commands in `<verify>` exit 0.
  </acceptance_criteria>
  <done>Guarded rollout support claims are protected by repo-local docs drift tests and publish-verification tests after the docs exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Host signal provider -> guarded decision proof | Untrusted or unavailable host-owned metrics truth is normalized into bounded signal facts. |
| Core guarded decision -> mounted admin docs/tests | Core status and audit truth cross into mounted UI proof without UI recomputing health. |
| Public docs -> adopter/support expectations | Public support claims can overstate what the package proves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-52-01 | Information Disclosure | `rulestead/test/rulestead/guarded_rollout_test.exs` and mounted proof suites | mitigate | Keep proof assertions on bounded decision fields, normalized signal facts, and redaction; do not assert or document raw provider payloads. |
| T-52-02 | Tampering | `scripts/ci/test.sh` | mitigate | Scope `guarded_rollout_foundations` to exact ExUnit suites and docs drift tests; exclude broad provider/dashboard/demo smoke paths. |
| T-52-03 | Repudiation | docs and release-contract tests | mitigate | Require exact rerunnable command `RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh` in root and maintainer docs. |
| T-52-04 | Information Disclosure | public docs | mitigate | Add forbidden-phrase drift guards for observability/dashboard/provider/statistics claims. |
| T-52-05 | Elevation of Privilege | mounted status reads | mitigate | Include existing mounted rollout/timeline suites in the proof scope so mounted status remains sourced from public core status/audit reads. |
| T-52-06 | Tampering | support-truth claims | mitigate | Extend `release_contract_test.exs` and `verify_release_publish_test.exs` with exact required and forbidden wording. |
</threat_model>

<verification>
Run:
```bash
RULESTEAD_TEST_SCOPE=guarded_rollout_foundations bash scripts/ci/test.sh
cd rulestead && mix test test/rulestead/release_contract_test.exs test/rulestead/mix/tasks/verify_release_publish_test.exs
```
</verification>

<success_criteria>
- `guarded_rollout_foundations` exists as a named bounded script scope.
- VER-01 coverage matrix exists and maps required behaviors to existing or new proof.
- Focused gap tests cover insufficient sample, terminal host-seam fault, and no-stable-target breach behavior through the guarded rollout adapter path.
- Docs and drift tests agree on host-owned metrics seam, fail-closed states, audited hold/rollback, mounted status explanation, and unsupported claims.
</success_criteria>

<output>
After completion, create `.planning/phases/52-proof-docs-milestone-closure/52-01-SUMMARY.md`.
</output>
