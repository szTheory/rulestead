---
phase: 12-webhook-ingress-outbound-notifications-and-operator-visibili
plan: 03
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - rulestead/priv/repo/migrations/TIMESTAMP_create_rulestead_webhook_destinations_events_and_deliveries.exs
  - rulestead/lib/rulestead/webhooks/destination.ex
  - rulestead/lib/rulestead/webhooks/outbound_event.ex
  - rulestead/lib/rulestead/webhooks/delivery.ex
  - rulestead/lib/rulestead/store.ex
  - rulestead/lib/rulestead/store/command.ex
  - rulestead/test/rulestead/store/command_webhook_outbound_test.exs
  - rulestead/test/rulestead/store/webhook_outbound_contract_test.exs
autonomous: true
requirements:
  - HOOK-03
must_haves:
  truths:
    - "Hosts can persist named outbound webhook destinations with environment scope and a small governance-focused subscription set."
    - "Outbound delivery state lives in durable destination, event, and delivery records instead of disappearing into Oban job rows."
    - "The outbound contract surface exposes destination management, event recording, and delivery inspection/retry commands without leaking secret material."
  artifacts:
    - path: "rulestead/priv/repo/migrations/TIMESTAMP_create_rulestead_webhook_destinations_events_and_deliveries.exs"
      provides: "Durable outbound destination, event, and delivery tables"
    - path: "rulestead/lib/rulestead/webhooks/destination.ex"
      provides: "Named destination contract with subscription preset vocabulary"
    - path: "rulestead/lib/rulestead/webhooks/delivery.ex"
      provides: "Delivery lifecycle contract and retry-visible state model"
    - path: "rulestead/lib/rulestead/store/command.ex"
      provides: "Command surface for destination management and delivery replay/inspection"
  key_links:
    - from: "rulestead/lib/rulestead/webhooks/destination.ex"
      to: "rulestead/lib/rulestead/store/command.ex"
      via: "destination subscriptions stay on a small explicit event enum and strong default preset"
      pattern: "all_high_impact_governance_events|subscriptions"
    - from: "rulestead/lib/rulestead/webhooks/outbound_event.ex"
      to: "rulestead/lib/rulestead/webhooks/delivery.ex"
      via: "durable outbound product truth separates event identity from per-attempt delivery state before worker wiring"
      pattern: "outbound_event|delivery|attempt_count|terminal_state"
---

<objective>
Add the durable outbound webhook destination, event, and delivery contracts plus the command surface the later delivery worker will execute against.

Purpose: Outbound notifications must have durable product truth and an explicit command surface before execution wiring lands; hosts should not need to infer destination or delivery state from worker implementation details.
Output: Outbound webhook schema, domain contracts, command/store surfaces, and focused contract tests for destination/event/delivery configuration and inspection.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-webhook-ingress-outbound-notifications-and-operator-visibili/12-CONTEXT.md
@.planning/phases/10-scheduled-changes-and-durable-execution/10-CONTEXT.md
@.planning/phases/10-scheduled-changes-and-durable-execution/10-04-PLAN.md
@prompts/rulestead-security-privacy-and-threat-model.md
@prompts/rulestead-telemetry-observability-and-audit.md
@prompts/rulestead-host-app-integration-seam.md
@rulestead/lib/rulestead/store.ex
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/store/ecto.ex

<interfaces>
From `rulestead/lib/rulestead/store.ex`:

```elixir
@callback list_scheduled_executions(Command.ListScheduledExecutions.t()) ::
            result(Command.Page.t(map()))
```

From `rulestead/lib/rulestead/store/command.ex`:

```elixir
def normalize_metadata(metadata), do: metadata |> normalize_map() |> drop_sensitive_keys()
def normalize_command(metadata), do: normalize_map(metadata)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create outbound destination, event, and delivery contracts</name>
  <files>rulestead/priv/repo/migrations/TIMESTAMP_create_rulestead_webhook_destinations_events_and_deliveries.exs, rulestead/lib/rulestead/webhooks/destination.ex, rulestead/lib/rulestead/webhooks/outbound_event.ex, rulestead/lib/rulestead/webhooks/delivery.ex, rulestead/test/rulestead/store/webhook_outbound_contract_test.exs</files>
  <behavior>
    - Test 1: destination contracts persist environment scope, endpoint URL, secret references, and subscription presets using a small governance-focused enum with a strong default preset.
    - Test 2: outbound event and delivery contracts preserve durable identity, attempt counts, terminal states, and operator-visible failure metadata independently of any Oban job row.
  </behavior>
  <action>Create the outbound product-truth model per D-12, D-13, D-14, D-16, and D-17. Add durable `webhook_destinations`, `webhook_events`, and `webhook_deliveries` tables plus contract modules for destinations, outbound governance events, and delivery attempts. The destination contract must support environment scope and a small fixed subscription enum with a strong default preset such as `all_high_impact_governance_events`; allow narrowing, but do not force checkbox-heavy setup on the happy path. Keep signing secrets bounded to write-only inputs or redacted references; they must not appear in immutable audit metadata, telemetry, or UI payloads. This task defines the durable contracts the later delivery plan will execute against rather than implementing worker behavior early.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/store/webhook_outbound_contract_test.exs</automated>
  </verify>
  <done>The repo has durable outbound destination/event/delivery vocabulary with secret-safe product truth and clear state semantics before adapter or worker execution is wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the outbound command and inspection surface without worker execution</name>
  <files>rulestead/lib/rulestead/store.ex, rulestead/lib/rulestead/store/command.ex, rulestead/test/rulestead/store/command_webhook_outbound_test.exs, rulestead/test/rulestead/store/webhook_outbound_contract_test.exs</files>
  <behavior>
    - Test 1: destination commands normalize environment scope, endpoint URL, secret references, and subscription presets into bounded string-keyed maps without leaking secret material.
    - Test 2: store commands expose create/update/list/fetch destination verbs, outbound record listing, and retry-intent commands against durable delivery identities without encoding worker-only concerns into the public contract.
  </behavior>
  <action>Extend `Rulestead.Store` and `Rulestead.Store.Command` per D-12, D-14, D-15, D-16, and D-17 with explicit verbs for create/update/list/fetch destinations, list/fetch webhook records, and retry a terminal delivery against the durable delivery identity created in Task 1. Keep the public contract focused on stable destination/event/delivery state and operator-safe replay intent, not on Oban payload details or transient worker internals. Do not implement HTTP delivery, telemetry emission, or adapter execution here; those belong in the follow-on delivery plan.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/store/command_webhook_outbound_test.exs test/rulestead/store/webhook_outbound_contract_test.exs</automated>
  </verify>
  <done>The repo exposes a secret-safe outbound command/store contract for destination management, event/delivery inspection, and explicit retry intent, ready for adapter and worker execution in the next plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| governance event emission -> outbound product state | internal high-impact events become deliverable external notifications here |
| delivery worker -> remote destination | signed outbound HTTP requests leave the host boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | I | destination secret handling | mitigate | Keep destination secrets out of audit, telemetry, and UI payloads; sign deliveries via bounded helper inputs or redacted references only per D-16. |
| T-12-08 | T | outbound contract boundary | mitigate | Separate durable destination/event/delivery product truth from later worker execution so retry intent and delivery identity are explicit before any Oban wiring per D-14 and D-15. |
| T-12-09 | I | command normalization | mitigate | Normalize outbound commands through the shared store helpers so secret references, not secret values, cross the public contract per D-16. |
</threat_model>

<verification>
Run the outbound contract and command suites. Confirm destination subscriptions stay small and governance-focused, that product truth lives in durable records before worker wiring, and that public commands preserve retry identity without leaking secrets.
</verification>

<success_criteria>
- Named outbound destinations and subscriptions are configurable through core-package contracts without secret leakage.
- Outbound destination, event, and delivery records exist as product truth before worker execution begins.
- The public command/store boundary is specific enough that the next plan can implement worker delivery, telemetry, and retry behavior without reopening contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/12-webhook-ingress-outbound-notifications-and-operator-visibili/12-webhook-ingress-outbound-notifications-and-operator-visibili-03-SUMMARY.md`
</output>
