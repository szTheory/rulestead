---
phase: 12-webhook-ingress-outbound-notifications-and-operator-visibili
plan: 06
type: execute
wave: 6
depends_on:
  - 12-05
files_modified:
  - rulestead_admin/test/rulestead_admin/integration/admin_mount_phase12_webhooks_test.exs
  - scripts/ci/verify_phase12_webhooks.sh
  - rulestead/doc/admin-ui.md
autonomous: true
requirements:
  - HOOK-04
must_haves:
  truths:
    - "Phase 12 closes with one scripts-first verifier that proves mounted UI visibility against the same core webhook contracts added in plans 12-01 through 12-05."
    - "Mounted webhook route docs stay honest about `/admin/flags/webhooks`, `/admin/flags/webhooks/:id`, and canonical `?env=` handling."
    - "The final mounted-entrypoint integration test exercises the real sibling-package admin seam rather than a disconnected component-only harness."
  artifacts:
    - path: "rulestead_admin/test/rulestead_admin/integration/admin_mount_phase12_webhooks_test.exs"
      provides: "Mounted-entrypoint integration test for the webhook hub"
    - path: "scripts/ci/verify_phase12_webhooks.sh"
      provides: "Scripts-first Phase 12 verification entrypoint"
    - path: "rulestead/doc/admin-ui.md"
      provides: "Updated mounted admin route contract including webhook visibility"
  key_links:
    - from: "scripts/ci/verify_phase12_webhooks.sh"
      to: "rulestead_admin/test/rulestead_admin/integration/admin_mount_phase12_webhooks_test.exs"
      via: "scripts-first mounted verification"
      pattern: "admin_mount_phase12_webhooks_test|webhook_live"
    - from: "rulestead/doc/admin-ui.md"
      to: "rulestead_admin/lib/rulestead_admin/router.ex"
      via: "documented mounted routes match the router-owned webhook path contract"
      pattern: "webhooks|env="
---

<objective>
Close Phase 12 with a scripts-first verifier and honest mounted route documentation.

Purpose: The webhook hub needs one final integration and documentation slice so the mounted entrypoint, route contract, and phase verifier stay auditable and reproducible for the sibling-package release design.
Output: Mounted-entrypoint integration test, one phase verifier script, and updated admin route docs.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-webhook-ingress-outbound-notifications-and-operator-visibili/12-CONTEXT.md
@.planning/phases/11-mounted-admin-governance-and-schedule-ui/11-04-PLAN.md
@prompts/rulestead-admin-ux-and-operator-ia.md
@prompts/rulestead-host-app-integration-seam.md
@rulestead_admin/lib/rulestead_admin/router.ex
@rulestead/doc/admin-ui.md

<interfaces>
From `rulestead_admin/lib/rulestead_admin/router.ex`:

```elixir
live "/change-requests", RulesteadAdmin.Live.ChangeRequestLive.Index, :index
live "/schedule", RulesteadAdmin.Live.ScheduleLive.Index, :index
live "/schedule/:scheduled_execution_id", RulesteadAdmin.Live.ScheduleLive.Show, :show
```

From `rulestead/doc/admin-ui.md`:

```markdown
The `env` query parameter is the canonical environment selector across the mounted UI.
Operators can treat these URL shapes as the stable mounted navigation layer...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the mounted-entrypoint integration test and phase verifier script</name>
  <files>rulestead_admin/test/rulestead_admin/integration/admin_mount_phase12_webhooks_test.exs, scripts/ci/verify_phase12_webhooks.sh</files>
  <action>Create a mounted-entrypoint integration test and a single readable verifier script per D-19, D-20, D-21, and D-24. `scripts/ci/verify_phase12_webhooks.sh` should rerun the `rulestead_admin` webhook route/detail/accessibility suites plus the core `rulestead` inbound and outbound webhook contract coverage from plans 12-01 through 12-05 in a readable order, keeping CI scripts-first and phase-scoped. Keep the verification surface anchored to the real sibling-package mount seam rather than a standalone-admin harness.</action>
  <verify>
    <automated>bash scripts/ci/verify_phase12_webhooks.sh</automated>
  </verify>
  <done>One scripts-first verifier proves the mounted webhook hub against the real sibling-package entrypoint and the core webhook contracts it depends on.</done>
</task>

<task type="auto">
  <name>Task 2: Update the mounted route contract docs for shipped webhook visibility</name>
  <files>rulestead/doc/admin-ui.md</files>
  <action>Update `rulestead/doc/admin-ui.md` so the stable route contract includes `/admin/flags/webhooks` and `/admin/flags/webhooks/:id`, preserves canonical `?env=` guidance, and describes the webhook hub in calm mounted-package language per D-18, D-19, D-20, and D-24. Keep docs honest about the linked-version two-package boundary, and do not add Phase 8-only docs, standalone-admin claims, or speculative future integrations workbench language.</action>
  <verify>
    <automated>bash scripts/ci/verify_phase12_webhooks.sh</automated>
  </verify>
  <done>The mounted admin docs accurately reflect the shipped Phase 12 webhook route contract and verification entrypoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mounted host session -> mounted-entrypoint integration test | the real admin mount seam is exercised here instead of a disconnected harness |
| public docs -> host integrators | route documentation becomes the stable mounted contract external hosts will rely on here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | R | phase verifier | mitigate | Add a scripts-first mounted verifier that reruns the core and admin webhook suites in one readable phase-scoped command per D-24. |
| T-12-17 | T | mounted route docs | mitigate | Document only the shipped `/webhooks` routes and canonical `?env=` behavior so host apps do not integrate against speculative surfaces per D-19 and D-24. |
</threat_model>

<verification>
Run the Phase 12 verifier script and confirm it exercises the mounted-entrypoint integration, the webhook LiveView/accessibility suites, and the core inbound/outbound webhook suites added by earlier Phase 12 plans.
</verification>

<success_criteria>
- Phase 12 closes with one scripts-first verifier that covers both packages through the real mount seam.
- Mounted route documentation is accurate, env-aware, and limited to shipped webhook surfaces.
- Verification and docs stay isolated from the LiveView implementation slice, keeping the mounted UI plan within a reasonable file budget.
</success_criteria>

<output>
After completion, create `.planning/phases/12-webhook-ingress-outbound-notifications-and-operator-visibili/12-webhook-ingress-outbound-notifications-and-operator-visibili-06-SUMMARY.md`
</output>
