---
phase: 12-webhook-ingress-outbound-notifications-and-operator-visibili
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - rulestead/lib/rulestead.ex
  - rulestead/lib/rulestead/fake.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/audit_event.ex
  - rulestead/lib/rulestead/telemetry.ex
  - rulestead/test/rulestead/webhooks/inbound_governance_test.exs
  - rulestead/test/rulestead/store/webhook_adapter_contract_test.exs
  - rulestead/test/rulestead/webhooks/inbound_threat_model_test.exs
autonomous: true
requirements:
  - HOOK-02
must_haves:
  truths:
    - "Accepted inbound deliveries enter the same governed mutation path already used by mounted admin actions and scheduled execution."
    - "Policy resolution still decides whether inbound intent becomes a local change request, a bounded schedule, or an allowed direct governed action."
    - "Conflicting, stale, or no-longer-valid inbound intent fails visibly with correlated audit and telemetry rather than mutating toward the nearest available state."
  artifacts:
    - path: "rulestead/lib/rulestead.ex"
      provides: "Public ingress normalization entrypoint that reuses existing governance facade verbs"
    - path: "rulestead/lib/rulestead/store/ecto.ex"
      provides: "Transactional normalization, receipt updates, and correlated governance execution in the real adapter"
    - path: "rulestead/lib/rulestead/fake.ex"
      provides: "Fake adapter parity for webhook-driven governance flows"
    - path: "rulestead/test/rulestead/webhooks/inbound_governance_test.exs"
      provides: "Facade-level proof that accepted inbound events stay on the public governance path"
  key_links:
    - from: "rulestead/lib/rulestead.ex"
      to: "rulestead/lib/rulestead/store/ecto.ex"
      via: "public ingress normalization reuses submit/schedule/direct governance commands instead of store-internal shortcuts"
      pattern: "submit_change_request|schedule_governed_action|receive_inbound_webhook"
    - from: "rulestead/lib/rulestead/store/ecto.ex"
      to: "rulestead/lib/rulestead/audit_event.ex"
      via: "accepted and rejected inbound outcomes persist correlated immutable audit metadata"
      pattern: "webhook|change_request_id|scheduled_execution_id|correlation_id"
    - from: "rulestead/lib/rulestead/telemetry.ex"
      to: "rulestead/test/rulestead/webhooks/inbound_threat_model_test.exs"
      via: "telemetry contracts prove visible rejection and normalization outcomes"
      pattern: "ops.*webhook|webhook_received|webhook_rejected"
---

<objective>
Normalize accepted inbound events through the existing governance facade, preserving policy gates, honest actor chains, and visible failure handling.

Purpose: Phase 12 only satisfies the roadmap if webhook-triggered intent stays on the same trusted mutation rails as the mounted admin and scheduler rather than creating a second mutation engine.
Output: Public ingress facade verb(s), fake and Ecto adapter parity, correlated audit/telemetry metadata, and focused governance/threat-model tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-webhook-ingress-outbound-notifications-and-operator-visibili/12-CONTEXT.md
@.planning/phases/09-governance-core-contracts-change-requests-and-approval-polic/09-03-SUMMARY.md
@.planning/phases/09-governance-core-contracts-change-requests-and-approval-polic/09-04-SUMMARY.md
@.planning/phases/10-scheduled-changes-and-durable-execution/10-CONTEXT.md
@prompts/rulestead-security-privacy-and-threat-model.md
@prompts/rulestead-telemetry-observability-and-audit.md
@rulestead/lib/rulestead.ex
@rulestead/lib/rulestead/store/ecto.ex
@rulestead/lib/rulestead/audit_event.ex
@rulestead/lib/rulestead/telemetry.ex
@rulestead/lib/rulestead/governance/scheduled_execution.ex
@rulestead/lib/rulestead/oban/scheduled_execution_worker.ex

<interfaces>
From `rulestead/lib/rulestead.ex`:

```elixir
def submit_change_request(%Command.SubmitChangeRequest{} = command)
def approve_change_request(%Command.ApproveChangeRequest{} = command)
def schedule_change_request(Command.ScheduleChangeRequest.t()) :: Store.result(map())
```

From `rulestead/lib/rulestead/telemetry.ex`:

```elixir
@optional_keys ~w(... change_request_id correlation_id audit_event_id resource_key governance_action environment_key attempt_count execution_mode executed_by)a
def governance_metadata(command, attrs \\ %{}) when is_struct(command) and is_map(attrs)
```

From `rulestead/lib/rulestead/audit_event.ex`:

```elixir
|> maybe_put("change_request_id", ...)
|> maybe_put("governance_action", ...)
|> maybe_put("scheduled_execution_id", ...)
|> maybe_put("failure_reason", ...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public inbound normalization verbs that reuse governance policy and execution rails</name>
  <files>rulestead/lib/rulestead.ex, rulestead/lib/rulestead/fake.ex, rulestead/lib/rulestead/store/ecto.ex, rulestead/test/rulestead/webhooks/inbound_governance_test.exs, rulestead/test/rulestead/store/webhook_adapter_contract_test.exs</files>
  <behavior>
    - Test 1: a verified inbound event requiring approvals becomes a local change request with explicit system actor identity and preserved upstream identity metadata.
    - Test 2: a verified inbound event that policy allows to schedule or directly execute follows the same bounded facade/store path used by Phase 9 and Phase 10 flows, with fake and Ecto adapters returning equivalent state.
  </behavior>
  <action>Extend the public core facade and adapters per D-06, D-07, D-08, D-09, D-10, and D-11. Add a public ingress normalization entrypoint on `Rulestead` that consumes the canonical `InboundEvent`/receipt output from 12-01 and then chooses one of the already-supported governance verbs: `submit_change_request`, `schedule_governed_action`, or the bounded direct governed action path resolved through the existing authorizer. Use an honest local actor chain such as `system:webhook:<endpoint_or_provider>` for execution while preserving upstream human/system identity only inside metadata. Keep the supported action set bounded to publish, rollout advance, kill-switch engage/release, and already-represented governed actions; reject arbitrary raw admin mutations or “best effort” stale-state reconciliation.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/webhooks/inbound_governance_test.exs test/rulestead/store/webhook_adapter_contract_test.exs</automated>
  </verify>
  <done>Accepted inbound events normalize through the same public governance path as mounted admin and scheduler flows, with fake/Ecto parity and no store-internal bypasses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist correlated audit and telemetry evidence for accepted and failed inbound intent</name>
  <files>rulestead/lib/rulestead/store/ecto.ex, rulestead/lib/rulestead/audit_event.ex, rulestead/lib/rulestead/telemetry.ex, rulestead/test/rulestead/webhooks/inbound_threat_model_test.exs, rulestead/test/rulestead/webhooks/inbound_governance_test.exs</files>
  <behavior>
    - Test 1: rejected, conflicting, or stale inbound intent emits canonical webhook telemetry and persists immutable audit/receipt metadata explaining why state did not change.
    - Test 2: accepted inbound intent links receipt id, change request id, scheduled execution id, audit event id, and correlation id without exposing secrets or session-shaped data.
  </behavior>
  <action>Harden the normalization path per D-04, D-09, D-10, and D-11. Extend `Rulestead.AuditEvent.metadata/1` and `Rulestead.Telemetry` so inbound receipts and downstream governance outcomes carry bounded fields for receipt identity, provider, endpoint key, correlation id, governance action, linked change request/scheduled execution ids, and explicit rejection or conflict reason. Emit `[:rulestead, :ops, :webhook, :received]` and `[:rulestead, :ops, :webhook, :rejected]` tuples in the same allowlisted metadata style already used for governance and scheduling. Ensure stale targets, invalid current state, and policy denials stay visible and terminal for that receipt instead of mutating toward a nearby valid state.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/webhooks/inbound_governance_test.exs test/rulestead/webhooks/inbound_threat_model_test.exs</automated>
  </verify>
  <done>Inbound webhook normalization has auditable, telemetry-visible outcomes for accepted, rejected, stale, and conflicting intent, and those outcomes preserve the same fail-closed posture as direct governed mutations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verified inbound envelope -> governance facade | transport trust is converted into bounded local mutation intent here |
| normalization result -> audit and telemetry | operator-visible evidence of accepted or rejected intent is recorded here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | E | ingress normalization facade | mitigate | Reuse `Rulestead` public governance verbs and existing authorizer resolution so verified transport never bypasses local policy, approvals, or conflict handling per D-06, D-07, and D-08. |
| T-12-05 | S | local actor chain | mitigate | Record a system webhook actor for local execution and keep upstream identity as metadata only, preventing human impersonation in audit history per D-09. |
| T-12-06 | T | stale/conflicting inbound intent | mitigate | Reject invalid current-state transitions with explicit receipt/audit failure metadata instead of mutating to the nearest available state per D-11. |
</threat_model>

<verification>
Run the inbound governance, adapter parity, and threat-model suites. Confirm accepted events use only existing governance verbs and that rejected/conflicting events still persist receipt, audit, and telemetry evidence.
</verification>

<success_criteria>
- Phase 12 ingress work now reaches production-safe governance outcomes only through existing public mutation rails.
- Policy resolution, change-request requirements, and conflict handling remain local product truth.
- Audit and telemetry make both successful and rejected webhook intent visible without leaking secret or session-shaped metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/12-webhook-ingress-outbound-notifications-and-operator-visibili/12-webhook-ingress-outbound-notifications-and-operator-visibili-02-SUMMARY.md`
</output>
