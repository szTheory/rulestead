---
phase: 127-adoption-guides
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/recipes/integrations-cookbook.md
autonomous: true
requirements:
  - GUIDE-02
user_setup: []

must_haves:
  truths:
    - "guides/recipes/integrations-cookbook.md ships with 4 persona/JTBD-grounded recipes (Stripe-tier audience, eval-telemetry -> Segment, staging->prod CR promotion, Oban-gated job)"
    - "Each recipe uses the fixed template Goal -> For -> Prerequisites -> Steps -> Verification -> Gotchas -> Related, carries an honest boundary line, and uses only shipped public seams"
    - "Each 'For' line maps to a named persona from guides/introduction/user-flows-and-jtbd.md"
    - "mix docs --warnings-as-errors is green and check_version_truth.py is green"
  artifacts:
    - path: "guides/recipes/integrations-cookbook.md"
      provides: "Four persona-grounded integration recipes on a fixed template"
      min_lines: 120
      contains: "## Goal"
  key_links:
    - from: "guides/recipes/integrations-cookbook.md"
      to: "guides/introduction/user-flows-and-jtbd.md"
      via: "each recipe's 'For' line names a canonical persona/flow"
      pattern: "user-flows-and-jtbd\\.md"
  prohibitions:
    - "MUST NOT headline Rulestead.submit_change_request/1 (or siblings) as stable 1.x API in the CR-promotion recipe"
    - "MUST NOT name Rulestead.Store.Redis, Rulestead.Store.Command.*, or Runtime cache internals as public seams"
    - "MUST NOT promise hosted analytics, warehouses, authoritative affected-user counts, or percentage-of-time rollouts (product-boundary out-of-scope)"
    - "MUST NOT contain 0.1.x, 0.1.7, ~> 0.1, 'future ... 1.0', '1.0 API freeze', or 'Two version lines'"
---

<objective>
Author `guides/recipes/integrations-cookbook.md` (GUIDE-02): EXACTLY 4 persona/JTBD-grounded integration recipes, each on the FIXED template **Goal -> For -> Prerequisites -> Steps -> Verification -> Gotchas -> Related**, each carrying an honest boundary line and using ONLY shipped public seams from the api_stability.md 1.x catalog.

Purpose: Give Stripe-tier adopter teams copy-paste-grade integration paths anchored to real personas and the product boundary, so they integrate via supported seams instead of internals.

Output: One new markdown file under `guides/recipes/` that auto-joins the Recipes extras group (wired in Plan 03) and passes the docs autolink gate plus the version-truth guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-adoption-guides/127-RESEARCH.md

# Seam catalog, personas, conventions, and boundary truth
@guides/api_stability.md
@guides/recipes/oban-background-jobs.md
@guides/recipes/telemetry.md
@guides/introduction/user-flows-and-jtbd.md
@guides/introduction/product-boundary.md
</context>

<artifacts_produced>
- `guides/recipes/integrations-cookbook.md` (new)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author the 4 cookbook recipes on the fixed template</name>
  <read_first>
    - .planning/phases/127-adoption-guides/127-RESEARCH.md — "The 4 Cookbook Recipes -> Seam Map (GUIDE-02)" table, "Verified Public Seams" table, "Non-Public / Landmine Seams" table, and "Code Examples"
    - guides/introduction/user-flows-and-jtbd.md — the six personas (App Developer, Tech Lead, Operator, Support Engineer, SRE/On-call, Maintainer) and six flows; confirm exact persona/flow names before writing each "For" line
    - guides/introduction/product-boundary.md — "Host always owns" and "Out of scope" lists; confirm each recipe's boundary line aligns
    - guides/recipes/oban-background-jobs.md and guides/recipes/telemetry.md — verified code patterns and recipe conventions
    - guides/api_stability.md — confirm exact arities for apply_audience_mutation, preview_audience_impact/3, Telemetry.attach_many/4, list_audit_events/1, Oban seams
  </read_first>
  <files>guides/recipes/integrations-cookbook.md</files>
  <action>
Create `guides/recipes/integrations-cookbook.md` with a single `# Integrations Cookbook` H1 and a short intro paragraph stating the fixed template and that every recipe uses only supported 1.x public seams. Then author EXACTLY 4 recipes. Each recipe is one `## ` section (the recipe name) and MUST contain these seven sub-sections IN ORDER, each as a `### ` heading: **Goal**, **For**, **Prerequisites**, **Steps**, **Verification**, **Gotchas**, **Related**. The **For** line names a persona + flow from user-flows-and-jtbd.md. Include an honest boundary line (state what the host owns / what Rulestead does NOT do) inside Gotchas (or Goal). Use these 4 recipes with the seams indicated (all from the RESEARCH "Verified Public Seams" table):

1. Stripe-tier audience — For: Tech Lead (Flow 2: Target The Right Audience). Seams: `Rulestead.apply_audience_mutation(attrs, opts)` (map form), `Rulestead.preview_audience_impact/3`, and `%Rulestead.Context{attributes: ...}` for the tier. Boundary: the host owns the Stripe webhook and population truth; previews declare basis + uncertainty — Rulestead gives NO authoritative affected-user count (product-boundary; footguns "preview evidence as census").
2. eval-telemetry -> Segment — For: Support Engineer / SRE (Flow 5: Explain One User's Reality). Seams: `Rulestead.Telemetry.attach_many/4` on `[:rulestead, :eval, :decide, :stop]` with documented metadata keys (`:flag_key`, `:environment`, `:reason`, ...). Boundary: telemetry is redacted by default — raw actor payloads/attrs are NOT in the contract; the host owns the Segment client.
3. staging->prod CR promotion — For: Operator / Tech Lead (Flow 3: Preview Before You Regret It / reviewable change). Frame the promotion by OUTCOME through the governance flow: `Rulestead.Admin.Policy.change_request_required?/4` gates the apply, the `[:rulestead, :admin, :mutation, :stop]` telemetry event observes it, and audit lands via `Rulestead.list_audit_events/1`. Per RESEARCH open-question A1/A2 safe default, describe promotion by outcome (governed, reviewable apply that lands an audit trail) WITHOUT headlining the `@moduledoc false` mix tasks as stable API and WITHOUT presenting `submit_change_request/1` as a stable catalog symbol. Boundary: Rulestead is governed apply + Promotion/GitOps, NOT a hosted control plane; protected-env gating is host-policy-driven.
4. Oban-gated job — For: App Developer (Flow 1: Ship Behind A Flag). Seams: `Rulestead.Oban.Middleware.attach/2`, `use Rulestead.Oban.Worker` + `rulestead_context/1`, `Rulestead.Runtime.enabled?/3`, and `config :rulestead, :host, oban: ...`. Boundary: the Oban seam carries bounded context only — not a promise of hosted rollout orchestration or hidden admin mutation pipelines.

Conventions: backtick all code refs so ExDoc autolinks them; ` ```elixir ` / ` ```bash ` fences (lean on the verified snippets in RESEARCH "Code Examples"); relative `.md` links in Related (e.g. `[Oban Background Jobs](oban-background-jobs.md)`, `[Telemetry](telemetry.md)`, `[User Flows & JTBD](../introduction/user-flows-and-jtbd.md)`, `[Product Boundary](../introduction/product-boundary.md)`); never absolute `file://`-style links. Any version string MUST be `1.x` / `~> 1.0`.
  </action>
  <verify>
    <automated>test "$(grep -c '^## ' guides/recipes/integrations-cookbook.md)" -eq 4 && test "$(grep -c '^### Goal' guides/recipes/integrations-cookbook.md)" -eq 4</automated>
  </verify>
  <done>integrations-cookbook.md exists with exactly 4 `## ` recipe sections, each containing the 7 `### ` template sub-sections (Goal, For, Prerequisites, Steps, Verification, Gotchas, Related) in order, with a persona-anchored For line and an honest boundary line, using only verified public seams.</done>
</task>

<task type="auto">
  <name>Task 2: Pass the docs autolink gate, seam allow-list, and version-truth guard</name>
  <read_first>
    - .planning/phases/127-adoption-guides/127-VALIDATION.md — "Quick run command" and "Validation Sign-Off"
    - guides/introduction/user-flows-and-jtbd.md — confirm each persona/flow name used in a "For" line matches the canonical text
  </read_first>
  <files>guides/recipes/integrations-cookbook.md</files>
  <action>
Verify and, if needed, correct the new file so all gates pass:

1. Docs autolink gate: run `cd rulestead && mix docs --warnings-as-errors`. Fix any undefined-reference warning by correcting to a symbol/arity in the RESEARCH "Verified Public Seams" table, or by confirming each relative `.md` link target exists.
2. Seam allow-list: re-scan for any prohibited landmine seam (`Rulestead.Store.Redis`, `Rulestead.Store.Command.*`, Runtime cache internals, `submit_change_request/1` and siblings as stable API). Reroute per the Task 1 rules. Re-scan for product-boundary violations (hosted analytics, warehouses, authoritative affected-user counts, percentage-of-time rollouts) and remove any.
3. Version-truth guard: run `python3 scripts/check_version_truth.py` from repo root and ensure exit 0.
  </action>
  <verify>
    <automated>cd rulestead && mix docs --warnings-as-errors && cd .. && python3 scripts/check_version_truth.py</automated>
  </verify>
  <done>`mix docs --warnings-as-errors` exits 0 with integrations-cookbook.md rendering and no broken autolinks; `check_version_truth.py` exits 0; no landmine seam or product-boundary violation remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only phase. No code, no inputs, no auth surface ships. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-03 | Information Disclosure (content rule) | eval-telemetry -> Segment recipe | mitigate | State telemetry is redacted by default; raw actor payloads/attrs are NOT in the contract; host owns the Segment client (api_stability.md). |
| T-127-04 | Repudiation / Elevation (content rule) | CR-promotion recipe | mitigate | Frame promotion as governed, audited apply via `change_request_required?/4` + audit events; host owns protected-env policy. Do not imply Rulestead is a hosted control plane or authenticates actors. |
</threat_model>

<verification>
- `test "$(grep -c '^## ' guides/recipes/integrations-cookbook.md)" -eq 4` (exactly 4 recipes)
- `test "$(grep -c '^### Goal' guides/recipes/integrations-cookbook.md)" -eq 4` (each carries the template's first sub-section)
- `cd rulestead && mix docs --warnings-as-errors` exits 0 (no broken autolinks)
- `python3 scripts/check_version_truth.py` exits 0 (no pre-1.0 drift)
- Manual: each "For" line maps to a real persona/flow; each recipe carries an honest boundary line; only public seams used; CR recipe framed by outcome.
</verification>

<success_criteria>
integrations-cookbook.md ships with exactly 4 persona/JTBD-grounded recipes on the fixed Goal -> For -> Prerequisites -> Steps -> Verification -> Gotchas -> Related template, each with an honest boundary line and only api_stability.md 1.x seams, passing both the docs autolink gate and the version-truth guard.
</success_criteria>

<output>
Create `.planning/phases/127-adoption-guides/127-02-SUMMARY.md` when done.
</output>
