---
phase: 127-adoption-guides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/recipes/troubleshooting.md
autonomous: true
requirements:
  - GUIDE-01
user_setup: []

must_haves:
  truths:
    - "guides/recipes/troubleshooting.md ships with 7 symptom-indexed patterns in Symptom -> Cause -> Fix -> Verify form (install/migration, payload-vs-keyed-runtime, snapshot boot race, context propagation, RBAC 403, change-request block, OpenFeature/Redis stale)"
    - "Each pattern cross-links footguns.md for the 'why' without duplicating its prose; tone is blame-free"
    - "mix docs --warnings-as-errors is green (no broken autolinks) and check_version_truth.py is green (no 0.1.x / ~> 0.1 drift)"
  artifacts:
    - path: "guides/recipes/troubleshooting.md"
      provides: "Blame-free symptom-indexed troubleshooting reference (7 patterns)"
      min_lines: 90
      contains: "Symptom"
  key_links:
    - from: "guides/recipes/troubleshooting.md"
      to: "guides/recipes/footguns.md"
      via: "relative markdown cross-link to footguns anchors for the conceptual why"
      pattern: "footguns\\.md#"
  prohibitions:
    - "MUST NOT headline Rulestead.submit_change_request/1 (or approve_/reject_/cancel_change_request) as stable 1.x API"
    - "MUST NOT name Rulestead.Store.Redis, Rulestead.Store.Command.*, or Rulestead.Runtime.Cache/Snapshot internals as public seams"
    - "MUST NOT contain 0.1.x, 0.1.7, ~> 0.1, 'future ... 1.0', '1.0 API freeze', or 'Two version lines'"
    - "MUST NOT duplicate footguns.md prose (cross-link the why, do not restate)"
---

<objective>
Author `guides/recipes/troubleshooting.md` (GUIDE-01): a blame-free, symptom-indexed troubleshooting reference with EXACTLY 7 patterns, each in **Symptom -> Cause -> Fix -> Verify** form, cross-linking `footguns.md` for the conceptual "why" without restating it.

Purpose: Adopters hitting a wall at 3am need a symptom-first path to a fix that uses only shipped public seams. This is the support-time companion to the conceptual footguns guide.

Output: One new markdown file under `guides/recipes/` that auto-joins the Recipes extras group (wired in Plan 03) and passes the docs autolink gate plus the version-truth guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-adoption-guides/127-RESEARCH.md

# Seam catalog (the ONLY symbols recipes may reference) and conventions
@guides/api_stability.md
@guides/recipes/footguns.md
@guides/recipes/context-propagation.md
@guides/recipes/deployment.md
@guides/introduction/product-boundary.md
</context>

<artifacts_produced>
- `guides/recipes/troubleshooting.md` (new)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author the 7 symptom-indexed troubleshooting patterns</name>
  <read_first>
    - .planning/phases/127-adoption-guides/127-RESEARCH.md — "The 7 Troubleshooting Patterns -> Seam Map (GUIDE-01)" table, "Verified Public Seams" table, and "Non-Public / Landmine Seams" table
    - guides/recipes/footguns.md — confirm the exact `##` heading text for the four overlap anchors before linking
    - guides/api_stability.md — confirm exact module/function arities and telemetry event tuples + Result/Error fields before backtick-referencing them
    - guides/recipes/context-propagation.md and guides/recipes/deployment.md — existing recipe conventions (H1 Title Case, `## ` headings, ` ```elixir ` / ` ```bash ` fences, relative `.md` links, backtick autolinks)
  </read_first>
  <files>guides/recipes/troubleshooting.md</files>
  <action>
Create `guides/recipes/troubleshooting.md` with a single `# Troubleshooting` H1, a one-paragraph blame-free intro framing this as a symptom-first companion to the conceptual footguns guide, then EXACTLY 7 patterns. Use one `## ` heading per pattern (the symptom phrased as a question or short symptom statement) and, inside each, four bolded sub-labels in order: **Symptom**, **Cause**, **Fix**, **Verify**. Author these 7 patterns in this order, using ONLY the seams listed for each (all confirmed in the RESEARCH "Verified Public Seams" table):

1. Install / migration — reference `mix rulestead.install` and `mix ecto.migrate` (bash fence); name `%Rulestead.Error{type: :repo_not_configured}` / `:store_not_configured` as the observable. Net-new content (no footguns link).
2. Payload-vs-keyed runtime — distinguish `Rulestead.evaluate/3` (payload form) from `Rulestead.Runtime.enabled?/3` (keyed runtime). Verify via the correct return shape. Cross-link the conceptual why: `[footguns](footguns.md#payload-first-vs-keyed-runtime-confusion)`. Give the symptom-first FIX path here; do NOT restate the footguns explanation.
3. Snapshot boot race — Fix points to supervision/refresh ordering (reference the existing [deployment](deployment.md) recipe's degraded-mode expectations); Verify via `Rulestead.Runtime.diagnostics/1` fields (`infrastructure_health`, `environments`) and `%Rulestead.Error{type: :snapshot_not_found}`. Cross-link `[footguns](footguns.md#snapshot-cache-before-readiness)`.
4. Context propagation — reference `Rulestead.Plug`, `Rulestead.Phoenix.context_from_conn/2`, `Rulestead.LiveView.assign_flags/3`, `Rulestead.Oban.Middleware.attach/2`; Verify via `Result.reason == :targeting_key_missing` and the `%Rulestead.Context{}` `targeting_key`. Point to the existing [context propagation](context-propagation.md) recipe and cross-link `[footguns](footguns.md#missing-or-unstable-targeting_key)`.
5. RBAC 403 — reference `Rulestead.Admin.Policy.can?/4` and the role catalogs (`viewer_actions/0`, `editor_actions/0`, `admin_actions/0`, `governance_actions/0`); Verify via `%Rulestead.Error{type: :unauthorized}` (domain `:auth`, with `plug_status`). State that the host owns authorization (product-boundary). No footguns link.
6. Change-request block — frame entirely through the governance flow: `Rulestead.Admin.Policy.change_request_required?/4` callback + the `[:rulestead, :admin, :mutation, :stop]` telemetry event; the CR-required outcome surfaces as a blocked mutation. Verify via that telemetry event firing / the blocked-mutation outcome. Per RESEARCH open-question A1 safe default, do NOT present `submit_change_request/1` (or its siblings) as a stable catalog symbol. No footguns link.
7. OpenFeature / Redis stale — frame through the PUBLIC telemetry events `[:rulestead, :runtime, :cache, :stale_used]`, `[:rulestead, :runtime, :cache, :miss]`, `[:rulestead, :runtime, :cache, :refresh]` and `Result.cache_age_ms`; describe the snapshot-freshness OUTCOME (mention `mix rulestead.redis.sync` only as the operational refresh task, by outcome). Per RESEARCH, do NOT name `Rulestead.Store.Redis` as a public module. Mention the `open_feature_rulestead` provider only as the consumer boundary. Cross-link `[footguns](footguns.md#snapshot-cache-before-readiness)` for the readiness why.

Conventions to match exactly: backtick all code refs (`` `Rulestead.Runtime.enabled?/3` ``) so ExDoc autolinks them; use ` ```elixir ` for Elixir and ` ```bash ` for shell; use relative `.md` links for guide cross-links; do NOT use absolute `file://`-style `:line` links. Version language MUST be `1.x` / `~> 1.0` if any version appears — never `0.1.x` or `~> 0.1`. Keep tone blame-free throughout (describe the situation, not operator error).
  </action>
  <verify>
    <automated>test "$(grep -c '^## ' guides/recipes/troubleshooting.md)" -eq 7</automated>
  </verify>
  <done>troubleshooting.md exists with exactly 7 `## ` pattern headings, each containing **Symptom**, **Cause**, **Fix**, and **Verify** sub-labels, covering the 7 required topics in order.</done>
</task>

<task type="auto">
  <name>Task 2: Pass the docs autolink gate, seam allow-list, and version-truth guard</name>
  <read_first>
    - .planning/phases/127-adoption-guides/127-VALIDATION.md — "Quick run command" and "Validation Sign-Off"
    - guides/recipes/footguns.md — verify each linked anchor slug matches an actual `## ` heading (GitHub/ExDoc auto-slug: lowercase, spaces -> hyphens, punctuation dropped, underscores kept)
  </read_first>
  <files>guides/recipes/troubleshooting.md</files>
  <action>
Verify and, if needed, correct the new file so all three gates pass:

1. Docs autolink gate: run `cd rulestead && mix docs --warnings-as-errors`. Fix any undefined-reference warning by correcting the symbol/arity to one in the RESEARCH "Verified Public Seams" table, or by confirming the relative `.md` link target exists under `guides/recipes/` or `guides/introduction/`.
2. Seam allow-list: re-scan the file for any of the prohibited landmine seams (`Rulestead.Store.Redis`, `Rulestead.Store.Command.*`, `Rulestead.Runtime.Cache`/`Snapshot` internals, and `submit_change_request/1` / `approve_change_request/1` / `reject_change_request/1` / `cancel_change_request/1` presented as stable API). Remove or reroute any that slipped in, per the Task 1 routing rules.
3. Version-truth guard: run `python3 scripts/check_version_truth.py` from repo root and ensure it exits 0 (no banned `0.1.x`, `0.1.7`, `~> 0.1`, `future ... 1.0`, `1.0 API freeze`, or `Two version lines` strings).
  </action>
  <verify>
    <automated>cd rulestead && mix docs --warnings-as-errors && cd .. && python3 scripts/check_version_truth.py</automated>
  </verify>
  <done>`mix docs --warnings-as-errors` exits 0 with troubleshooting.md rendering and no broken autolinks; `check_version_truth.py` exits 0; no landmine seam is headlined as stable API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only phase. No code, no inputs, no auth surface ships. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Information Disclosure (content rule) | RBAC-403 pattern + change-request pattern | mitigate | Reinforce that the host owns authorization (`Rulestead.Admin.Policy`); do not imply Rulestead authenticates actors (product-boundary.md). |
| T-127-02 | Tampering (seam truth) | Backtick autolinks / headline seams | mitigate | Allow-list = api_stability.md 1.x catalog; deny-list = CR functions, Store.Redis, Command.*, Cache/Snapshot internals. Enforced by `mix docs --warnings-as-errors` + reviewer scan. |
</threat_model>

<verification>
- `test "$(grep -c '^## ' guides/recipes/troubleshooting.md)" -eq 7` (exactly 7 patterns)
- `cd rulestead && mix docs --warnings-as-errors` exits 0 (no broken autolinks)
- `python3 scripts/check_version_truth.py` exits 0 (no pre-1.0 drift)
- Manual: each of the 4 overlap patterns links a real footguns.md anchor and does not restate footguns prose; tone is blame-free; no landmine seam is headlined as stable.
</verification>

<success_criteria>
troubleshooting.md ships with exactly 7 Symptom -> Cause -> Fix -> Verify patterns covering the required topics, cross-linking footguns.md for the "why" without duplication, using only api_stability.md 1.x seams, passing both the docs autolink gate and the version-truth guard.
</success_criteria>

<output>
Create `.planning/phases/127-adoption-guides/127-01-SUMMARY.md` when done.
</output>
