---
phase: 127-adoption-guides
plan: 03
type: execute
wave: 2
depends_on:
  - 127-01
  - 127-02
files_modified:
  - rulestead/mix.exs
autonomous: true
requirements:
  - GUIDE-03
user_setup: []

must_haves:
  truths:
    - "Both new guides are wired into the existing Recipes extras group: integrations-cookbook.md appears EARLY (top of the recipes block) and troubleshooting.md appears LAST in the recipes block"
    - "The 15-minute golden-path guide getting-started.md is untouched; no new extras group is added (groups_for_extras still matches ~r\"guides/recipes/\")"
    - "mix docs --warnings-as-errors is green with both new files rendering in the Recipes sidebar group"
  artifacts:
    - path: "rulestead/mix.exs"
      provides: "extras: list wiring both new recipes into the Recipes group in correct order"
      contains: "integrations-cookbook.md"
  key_links:
    - from: "rulestead/mix.exs"
      to: "guides/recipes/integrations-cookbook.md"
      via: "extras: list entry placed before testing.md (early)"
      pattern: "integrations-cookbook\\.md"
    - from: "rulestead/mix.exs"
      to: "guides/recipes/troubleshooting.md"
      via: "extras: list entry placed after migrating-from-funwithflags.md (last in recipes block)"
      pattern: "troubleshooting\\.md"
  prohibitions:
    - "MUST NOT add a new groups_for_extras entry (Recipes membership is regex-driven)"
    - "MUST NOT modify the getting-started.md extras line or any other introduction/flows entry"
    - "MUST NOT reorder existing recipe entries beyond inserting the two new files at the specified positions"
---

<objective>
Wire both new recipes into the EXISTING "Recipes" extras group in `rulestead/mix.exs` (GUIDE-03): insert `integrations-cookbook.md` EARLY (top of the recipes block) and `troubleshooting.md` LAST (after `migrating-from-funwithflags.md`), add NO new `groups_for_extras` group, and leave `getting-started.md` untouched.

Purpose: The `extras:` list order drives sidebar order within the Recipes group; `groups_for_extras` already matches `~r"guides/recipes/"` so both files auto-join with no group change. This makes the cookbook a leading entry and troubleshooting the closing reference.

Output: A two-line insertion in `rulestead/mix.exs` `extras:` list, verified by the docs render gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-adoption-guides/127-RESEARCH.md

# The exact edit site
@rulestead/mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the two new recipes into the extras list (cookbook early, troubleshooting last)</name>
  <read_first>
    - .planning/phases/127-adoption-guides/127-RESEARCH.md — "The mix.exs Edit (GUIDE-03) — exact" section (target ordering, no-new-group rationale)
    - rulestead/mix.exs — the `extras:` list (recipes block) and the `groups_for_extras` Recipes regex
  </read_first>
  <files>rulestead/mix.exs</files>
  <action>
In `rulestead/mix.exs`, edit ONLY the `extras:` list recipes block. Make two insertions:

1. Add `"../guides/recipes/integrations-cookbook.md",` as the FIRST entry of the recipes block — immediately before `"../guides/recipes/testing.md",`.
2. Add `"../guides/recipes/troubleshooting.md",` as the LAST entry of the recipes block — immediately after `"../guides/recipes/migrating-from-funwithflags.md",` and before `"../guides/api_stability.md",` (which stays in the "API & Stability" group, unchanged).

Do NOT add any `groups_for_extras` entry — the existing `"Recipes": ~r"guides/recipes/"` regex auto-includes both new files. Do NOT touch the `getting-started.md` line or any other extras entry. Do NOT reorder the existing recipe entries.
  </action>
  <verify>
    <automated>grep -q 'recipes/integrations-cookbook.md' rulestead/mix.exs && grep -q 'recipes/troubleshooting.md' rulestead/mix.exs && git diff --quiet -- guides/introduction/getting-started.md</automated>
  </verify>
  <done>Both `integrations-cookbook.md` (before testing.md) and `troubleshooting.md` (after migrating-from-funwithflags.md) appear in the extras recipes block; no new groups_for_extras entry; getting-started.md unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify sidebar order and the green docs gate</name>
  <read_first>
    - .planning/phases/127-adoption-guides/127-VALIDATION.md — sign-off checklist (cookbook early, troubleshooting last, golden path untouched, no new group)
  </read_first>
  <files>rulestead/mix.exs</files>
  <action>
Confirm the wiring is correct and the docs render:

1. Run `cd rulestead && mix docs --warnings-as-errors` — must exit 0 with both new files rendered. Because this plan depends on 127-01 and 127-02, both `guides/recipes/troubleshooting.md` and `guides/recipes/integrations-cookbook.md` already exist, so the autolink gate covers them under the live `extras:` wiring.
2. Confirm ordering: in `rulestead/mix.exs`, `integrations-cookbook.md` precedes `testing.md` and `troubleshooting.md` follows `migrating-from-funwithflags.md` (and precedes `api_stability.md`).
3. Confirm `groups_for_extras` is unchanged (still `"Recipes": ~r"guides/recipes/"`, no added group) and `getting-started.md` line is byte-identical to before this phase.
4. Run `python3 scripts/check_version_truth.py` from repo root as the phase-gate belt-and-suspenders (both new guide files are in its scan scope) — must exit 0.
  </action>
  <verify>
    <automated>cd rulestead && mix docs --warnings-as-errors && cd .. && python3 scripts/check_version_truth.py && awk '/recipes\/integrations-cookbook.md/{c=NR} /recipes\/testing.md/{t=NR} /recipes\/troubleshooting.md/{tr=NR} /recipes\/migrating-from-funwithflags.md/{m=NR} END{exit !(c<t && tr>m)}' rulestead/mix.exs</automated>
  </verify>
  <done>`mix docs --warnings-as-errors` exits 0 with both recipes in the Recipes sidebar group; cookbook precedes testing.md and troubleshooting follows migrating-from-funwithflags.md; no new group; getting-started.md untouched; version-truth guard green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Build-config edit only. No runtime code, inputs, or auth surface ships. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-05 | Tampering (config drift) | mix.exs extras/groups | accept | Scoped two-line insert; `git diff` + the awk ordering assertion catch accidental reorder or group addition; low blast radius. |
</threat_model>

<verification>
- `grep -q 'recipes/integrations-cookbook.md' rulestead/mix.exs` and `grep -q 'recipes/troubleshooting.md' rulestead/mix.exs`
- `git diff --quiet -- guides/introduction/getting-started.md` (golden path untouched)
- ordering assertion: cookbook before testing.md, troubleshooting after migrating-from-funwithflags.md
- `cd rulestead && mix docs --warnings-as-errors` exits 0 (both files render in Recipes group)
- `python3 scripts/check_version_truth.py` exits 0
</verification>

<success_criteria>
Both guides are wired into the existing Recipes extras group with cookbook early and troubleshooting last; no new extras group; getting-started.md untouched; docs render green and version-truth guard passes.
</success_criteria>

<output>
Create `.planning/phases/127-adoption-guides/127-03-SUMMARY.md` when done.
</output>
