---
phase: 42-runtime-contract-parity
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/experiment_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/experiment_live/show.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex
  - rulestead/test/fixtures/install_golden/STDOUT.txt
  - rulestead/test/rulestead/mix/tasks/rulestead_install_test.exs
  - rulestead/test/fixtures/install_golden/tree/priv/repo/migrations/
autonomous: true
requirements: [PAR-01, PAR-02]
must_haves:
  truths:
    - "Admin UI reads ownership and lifecycle from proper embeds"
    - "Installer outputs reflect the single squoshed migration"
    - "Golden tree fixtures precisely match the single migration file"
  artifacts:
    - path: "rulestead/test/fixtures/install_golden/STDOUT.txt"
      provides: "Expected installer output"
  key_links: []
---

<objective>
Update `rulestead_admin` views and sync the Golden tests to the new squoshed installer behavior.

Purpose: Fix downstream consumers of the `Flag` struct in the UI, and update integration tests to expect the single installer migration. (per D-05)
Output: Passing test suite across both `rulestead` and `rulestead_admin`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-runtime-contract-parity/42-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Rulestead Admin Views</name>
  <files>
    rulestead_admin/lib/rulestead_admin/live/experiment_live/index.ex
    rulestead_admin/lib/rulestead_admin/live/experiment_live/show.ex
    rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex
    rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex
    rulestead_admin/lib/rulestead_admin/live/flag_live/rollouts.ex
    rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex
  </files>
  <action>
    Search for uses of `.owner`, `lifecycle.owner`, and `.permanent` in `rulestead_admin` UI files.
    Update `entry.flag.owner` or `@detail.lifecycle.owner` to properly read from `ownership.owner_ref` (or `@detail.ownership.owner_ref`).
    In `flag_live/form.ex`, remove any fallback logic reading from `flag.owner` or `flag.permanent`, exclusively relying on `ownership` and `lifecycle` maps/structs.
  </action>
  <verify>
    <automated>cd rulestead_admin && mix compile --force</automated>
  </verify>
  <done>Admin views compile and correctly bind to the new struct format.</done>
</task>

<task type="auto">
  <name>Task 2: Update Golden Installer Fixtures & Tests</name>
  <files>
    rulestead/test/fixtures/install_golden/STDOUT.txt
    rulestead/test/fixtures/install_golden/tree/priv/repo/migrations/
    rulestead/test/rulestead/mix/tasks/rulestead_install_test.exs
  </files>
  <action>
    In `rulestead/test/fixtures/install_golden/tree/priv/repo/migrations/`, use shell commands to delete all old `.exs` files and copy `rulestead/priv/repo/migrations/20260524000000_create_rulestead_tables.exs` into it.
    In `STDOUT.txt`, delete the 16 legacy `TIMESTAMP_` migration lines and replace them with just `copy priv/repo/migrations/.keep` and `copy priv/repo/migrations/TIMESTAMP_create_rulestead_tables.exs`.
    In `rulestead_install_test.exs`, update the expected `copy_migrations` messages to assert only `.keep` and `20260524000000_create_rulestead_tables.exs` are copied.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/mix/tasks/rulestead_install_test.exs test/rulestead/integration/install_golden_test.exs && cd .. && mix test</automated>
  </verify>
  <done>Golden installer tests pass, accurately reflecting the squoshed migration, and entire workspace test suite passes.</done>
</task>

</tasks>
