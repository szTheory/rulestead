---
phase: 42-runtime-contract-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/priv/repo/migrations/20260524000000_create_rulestead_tables.exs
autonomous: true
requirements: [PAR-01, PAR-02]
must_haves:
  truths:
    - "Installer provides exactly one squoshed migration for initial setup"
    - "The single migration creates all tables and seeds environments"
    - "Legacy columns like owner are absent from the initial schema"
    - "tenant_key is properly included on environment_versions"
  artifacts:
    - path: "rulestead/priv/repo/migrations/20260524000000_create_rulestead_tables.exs"
      provides: "The clean GA database schema"
  key_links: []
---

<objective>
Squosh the 16 legacy migrations into a single GA-ready migration baseline.

Purpose: Provide a clean installer experience for adopters that doesn't copy 16 internal development migrations. (per D-01)
Output: A single cohesive Ecto migration replacing the old ones.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-runtime-contract-parity/42-CONTEXT.md
@.planning/phases/42-runtime-contract-parity/42-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create squoshed baseline migration</name>
  <files>rulestead/priv/repo/migrations/20260524000000_create_rulestead_tables.exs</files>
  <action>
    Read the existing migrations in `rulestead/priv/repo/migrations/`.
    Create a new file `20260524000000_create_rulestead_tables.exs` with a single `Rulestead.Repo.Migrations.CreateRulesteadTables` module.
    Combine all `up` (or `change`) logic from the legacy migrations into this file.
    CRITICAL (per D-02 & D-04): 
    - Omit the legacy `owner`, `expected_expiration`, and `permanent` columns from the `flags` table.
    - Ensure `tenant_key` is added as a `:string, null: false` to the `environment_versions` table.
    - Retain all check constraints (e.g., `flags_ownership_requires_owner_ref`) and the `seed_default_environments` inserts.

    Analog reference: `rulestead/priv/repo/migrations/20260423020100_create_rulestead_authoring_tables.exs`
    Pattern excerpt:
    ```elixir
    create table(:flags, primary_key: false) do
      add(:id, :uuid, primary_key: true, default: fragment("gen_random_uuid()"))
      add(:key, :text, null: false)
      # ...
      # add(:owner, :text, null: false) # OMIT THIS
      # add(:expected_expiration, :date) # OMIT THIS
    end
    ```
  </action>
  <verify>
    <automated>cd rulestead && mix format priv/repo/migrations/20260524000000_create_rulestead_tables.exs</automated>
  </verify>
  <done>New consolidated migration file exists and is cleanly formatted.</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy migrations</name>
  <files>rulestead/priv/repo/migrations/*.exs</files>
  <action>
    Delete all old `.exs` files in `rulestead/priv/repo/migrations/` EXCEPT the newly created `20260524000000_create_rulestead_tables.exs`.
    Do NOT delete `.keep`.
    Use `rm` to remove the 16 legacy migration files.
  </action>
  <verify>
    <automated>ls -1 rulestead/priv/repo/migrations/ | grep -c ".exs" | grep -q "1"</automated>
  </verify>
  <done>Only the new squoshed migration and .keep remain in the directory.</done>
</task>

</tasks>
