---
phase: 42-runtime-contract-parity
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - rulestead/lib/rulestead/flag.ex
  - rulestead/lib/rulestead/flag/ownership.ex
  - rulestead/lib/rulestead/flag/lifecycle_metadata.ex
  - rulestead/lib/mix/tasks/rulestead.lifecycle.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/fake.ex
  - rulestead/test/support/store_fixtures.ex
autonomous: true
requirements: [PAR-01, PAR-02]
must_haves:
  truths:
    - "Rulestead.Flag schema strictly matches the squoshed DB migration"
    - "Test factories supply correct embed maps without legacy field fallbacks"
  artifacts:
    - path: "rulestead/lib/rulestead/flag.ex"
      provides: "Ecto schema mapping for the flags table"
  key_links:
    - from: "rulestead/lib/rulestead/store/ecto.ex"
      to: "rulestead/lib/rulestead/flag.ex"
      via: "embed attributes"
      pattern: "ownership\\.owner_ref"
---

<objective>
Update `Rulestead.Flag` and internal data stores to drop legacy columns.

Purpose: Sync the application code schema and mock data stores to match the squoshed migration. (per D-03)
Output: Clean `Flag` struct relying only on `ownership` and `lifecycle` JSONB embeds.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-runtime-contract-parity/42-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy fields from Ecto schemas</name>
  <files>
    rulestead/lib/rulestead/flag.ex
    rulestead/lib/rulestead/flag/ownership.ex
    rulestead/lib/rulestead/flag/lifecycle_metadata.ex
  </files>
  <action>
    In `flag.ex`: Remove `:owner`, `:expected_expiration`, and `:permanent` from the `schema`, `cast`, and `validate_` lists. Remove fallback mappings like `normalize_embeds`. Ensure lifecycle/ownership validation reads from the embedded fields directly via `get_change` or `get_field`.
    In `ownership.ex` and `lifecycle_metadata.ex`: Remove backward-compatibility helpers like `default_from_owner/1` and `mode_from_flag/1`.

    Analog reference for embedded schemas: `rulestead/lib/rulestead/ruleset.ex`
    Pattern excerpt:
    ```elixir
    schema "rulesets" do
      # ...
      embeds_many(:rules, Rule, on_replace: :delete)
    end
    ```
  </action>
  <verify>
    <automated>cd rulestead && mix format lib/rulestead/flag.ex lib/rulestead/flag/ownership.ex lib/rulestead/flag/lifecycle_metadata.ex</automated>
  </verify>
  <done>Legacy fields are completely removed from Flag and its nested schemas.</done>
</task>

<task type="auto">
  <name>Task 2: Fix store and task references</name>
  <files>
    rulestead/lib/rulestead/store/ecto.ex
    rulestead/lib/rulestead/fake.ex
    rulestead/lib/mix/tasks/rulestead.lifecycle.ex
  </files>
  <action>
    Search for uses of `.owner`, `.permanent`, and `.expected_expiration` accessing flag maps/structs in these files.
    Replace `flag.owner` with `flag.ownership.owner_ref`.
    Replace `flag.permanent` with `flag.lifecycle.mode == :permanent`.
    Replace `flag.expected_expiration` with `flag.lifecycle.expected_expiration_date`.
    Ensure the internal `fake.ex` state also completely ignores tracking these old keys, relying strictly on the `ownership` and `lifecycle` embeds.
  </action>
  <verify>
    <automated>cd rulestead && mix compile --force</automated>
  </verify>
  <done>No compilation warnings or key errors remain regarding the removed fields.</done>
</task>

<task type="auto">
  <name>Task 3: Update factories and tests</name>
  <files>rulestead/test/support/store_fixtures.ex</files>
  <action>
    Update `StoreFixtures.valid_flag_attrs/1` to provide `ownership: %{owner_ref: "growth", ...}` and `lifecycle: %{mode: :permanent, ...}` directly instead of the top-level legacy fields.
    Adjust any other tests in `rulestead` (excluding `golden` tests) that manually assert on `.owner` or `.permanent`.

    Analog reference: `rulestead/test/support/store_fixtures.ex`
    Pattern excerpt:
    ```elixir
    def valid_flag_attrs(overrides \\ %{}) do
      defaults = %{
        key: "checkout-redesign",
        # ... (Replace legacy top-level keys like owner, permanent with embeds)
        ownership: %{owner_ref: "growth"},
        lifecycle: %{mode: :permanent}
      }
      Map.merge(defaults, overrides)
    end
    ```
  </action>
  <verify>
    <automated>cd rulestead && mix test --exclude golden</automated>
  </verify>
  <done>Core domain tests pass against the updated clean schema.</done>
</task>

</tasks>
