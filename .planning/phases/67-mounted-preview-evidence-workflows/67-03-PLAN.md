---
phase: 67-mounted-preview-evidence-workflows
plan: 67-03
type: feature
wave: 3
depends_on:
  - 67-01
files_modified:
  - rulestead_admin/test/rulestead_admin/live/audience_live/delete_preview_test.exs
  - rulestead_admin/test/rulestead_admin/live/audience_live/governance_test.exs
autonomous: true
requirements:
  - ADM-05
must_haves:
  truths:
    - Delete preview shows unsupported callout AND impact evidence when resolver on
    - Prod governance preview tests still pass with evidence resolver configured
    - GOV blast-radius panel semantics unchanged (no impression-weighted language)
  artifacts:
    - rulestead_admin/test/rulestead_admin/live/audience_live/delete_preview_test.exs
    - rulestead_admin/test/rulestead_admin/live/audience_live/governance_test.exs
---

# Plan 67-03: Delete Preview And Prod Governance Evidence Tests

<objective>
Cover delete preview evidence rendering alongside fail-closed unsupported-delete UX, and verify prod governance preview path still works when host evidence resolver is configured (CONTEXT D-03, D-05; Claude discretion: no new governance panel on DeletePreview).
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-67-05: Delete preview implies apply path | medium | Assert `refute html =~ "Apply delete"` retained |
| T-67-06: Governance tests break with resolver | low | Configure resolver only in new describe; assert blast panel unchanged |
</threat_model>

<tasks>

<task id="67-03-01" type="execute">
  <name>Delete preview evidence with unsupported callout</name>
  <read_first>
    - rulestead_admin/lib/rulestead_admin/live/audience_live/delete_preview.ex
    - rulestead_admin/test/rulestead_admin/live/audience_live/delete_preview_test.exs
    - rulestead/lib/rulestead/fake/preview_evidence_resolver.ex
    - .planning/phases/67-mounted-preview-evidence-workflows/67-CONTEXT.md (D-03, D-05)
  </read_first>
  <action>
Extend `delete_preview_test.exs`:

1. Add resolver setup helper (same as 67-02).

2. Add describe `"preview evidence"` async: false:

   **test "shows impact evidence and unsupported delete callout when resolver configured"**:
   - Configure `Rulestead.Fake.PreviewEvidenceResolver`
   - `live(..., "/admin/flags/audiences/vip-users/delete/preview?env=test")`
   - Retain: `assert html =~ "Audience delete is not available in mounted admin"`
   - Retain: `refute html =~ "Apply delete"`
   - Add: `assert html =~ "Sample cohort"`
   - Add: `assert html =~ "last_24h"`
   - Add: `assert html =~ "Impact preview"`

3. **test "host evidence unavailable basis still renders preview"** (optional if Fake delete_attempt returns preview without resolver):
   - Without resolver: preview may still render with `authored_state_and_explicit_samples` — assert `Impact preview` section present, `refute html =~ "Sample cohort"` when no samples

No changes to `delete_preview.ex`.
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/delete_preview_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Delete test asserts both `Audience delete is not available` and `Sample cohort` when resolver on
    - `mix test test/rulestead_admin/live/audience_live/delete_preview_test.exs` exits 0
  </acceptance_criteria>
</task>

<task id="67-03-02" type="execute">
  <name>Prod governance preview with resolver configured</name>
  <read_first>
    - rulestead_admin/test/rulestead_admin/live/audience_live/governance_test.exs
    - rulestead_admin/test/rulestead_admin/live/audience_live/edit_preview_test.exs (governance describe)
    - .planning/phases/66-evidence-carry-through-and-governance-boundary/66-CONTEXT.md (GOV-05)
  </read_first>
  <action>
In `governance_test.exs` (or extend edit_preview prod governance test if cleaner):

Add test **"prod edit preview shows governance panel and evidence when resolver configured"**:
- `Rulestead.Fake.Control.reset!()` + seed prod audience flags (reuse existing setup)
- Configure `Rulestead.Fake.PreviewEvidenceResolver`
- `live(conn, "/admin/flags/audiences/vip-users/edit/preview?env=prod")`
- Assert: `Change request required` OR `Governance required` (existing)
- Assert: `Sample cohort` and impression window `last_24h`
- Assert blast panel present: `Governance required` or `blast` panel marker from existing tests
- `refute html =~ "impression-weighted"`
- `refute html =~ "fleet dashboard"`

Do not change `GovernanceComponents` or `AudienceLive.Governance` module logic.
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/governance_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Governance or edit_preview prod test configures PreviewEvidenceResolver
    - Test asserts governance callout AND Sample cohort in same HTML
    - `mix test` for chosen file exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/delete_preview_test.exs test/rulestead_admin/live/audience_live/governance_test.exs
```
</verification>
