---
phase: 100
plan: 03
type: execute
wave: 3
depends_on:
  - "100-01"
  - "100-02"
files_modified:
  - brandbook/README.md
  - brandbook/docs/brand-usage.md
  - brandbook/BUDGET.md
  - .gitattributes
  - scripts/ci/lint.sh
autonomous: true
requirements:
  - REPO-01
  - REPO-02
---

<objective>
Finalize the repo-facing brandbook index and contributor guidance, document the live asset
budget, and add the root attributes guard for binary brand exports.
</objective>

<tasks>

<task type="auto">
  <name>Finalize brandbook docs</name>
  <files>brandbook/README.md brandbook/docs/brand-usage.md brandbook/BUDGET.md</files>
  <read_first>
    - brandbook/README.md
    - brandbook/docs/brand-usage.md
    - scripts/ci/lint.sh
    - prompts/rulestead-brand-book.md
  </read_first>
  <action>
    Rewrite the README as a self-contained directory index. Update brand usage docs so the
    Phase 98 re-skin is described as complete and the token check exits 0. Add BUDGET.md
    with the exact SVG limits already enforced by `scripts/ci/lint.sh`.
  </action>
  <acceptance_criteria>
    - README links to `brand-book.md` and the admin CSS.
    - brand usage docs contain the current `BRAND TOKENS SYNCED (68 tokens)` output.
    - BUDGET.md documents logo SVG <= 20480 bytes and specimen SVG <= 51200 bytes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Add repo bloat guard metadata</name>
  <files>.gitattributes scripts/ci/lint.sh</files>
  <action>
    Add `.gitattributes` at repo root for text-vs-binary classification and fix stale
    Phase 98 comments in `scripts/ci/lint.sh`.
  </action>
  <acceptance_criteria>
    - `.gitattributes` exists and marks SVG/Markdown/CSS/JSON as text.
    - Binary export extensions are marked binary.
    - `scripts/ci/lint.sh` no longer says `check_brand_tokens.py` intentionally exits 1.
  </acceptance_criteria>
</task>

</tasks>
