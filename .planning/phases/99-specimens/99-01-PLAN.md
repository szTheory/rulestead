---
phase: 99
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/assets/specimens/palette.svg
  - brandbook/assets/specimens/typography.svg
autonomous: true
requirements:
  - SPEC-01

must_haves:
  truths:
    - "brandbook/assets/specimens/ directory exists (nullglob guard defeated)"
    - "palette.svg shows every brand swatch with its hex value AND token name annotated"
    - "typography.svg shows the Sora/Inter/IBM Plex Mono type ramp labeled with --rs-text-* token names using live <text> elements"
    - "Both files have zero embedded raster and carry the accessible skeleton (role=img + title + desc + aria-labelledby)"
    - "Both files are ≤51200 bytes (SVGO-optimized)"
  artifacts:
    - path: "brandbook/assets/specimens/palette.svg"
      provides: "Brand color palette specimen — all swatches with hex + token name"
      contains: "#3A6F8F"
    - path: "brandbook/assets/specimens/typography.svg"
      provides: "Type ramp specimen — Sora/Inter/IBM Plex Mono with token labels"
      contains: "<text"
  key_links:
    - from: "brandbook/assets/specimens/palette.svg"
      to: "brandbook/tokens.json"
      via: "all fills are hard-coded hex literals sourced from tokens.json primitive.*"
      pattern: "#3A6F8F|#9b5931|#D2A94E"
    - from: "brandbook/assets/specimens/typography.svg"
      to: "brandbook/tokens.css"
      via: "font-family stacks match --rs-font-* invariant block values"
      pattern: "Sora.*Inter.*ui-sans-serif"
---

<objective>
Author the two SPEC-01 specimens — palette swatches and typography ramp — after creating the
specimens directory. These are the foundational brand-system reference files consumed by Phase
101's HTML brand book.

Purpose: Lock the palette and type ramp as auditable, source-controlled SVG references that
show every color token and every type role without requiring a build step.

Output:
- brandbook/assets/specimens/ directory (new)
- brandbook/assets/specimens/palette.svg (brand colors, all swatches annotated)
- brandbook/assets/specimens/typography.svg (type scale, live text elements, token labels)
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-specimens/99-RESEARCH.md
@.planning/phases/99-specimens/99-PATTERNS.md
</context>

<interfaces>
<!-- Key source files the executor MUST read before authoring either specimen. -->

From brandbook/assets/logo/rs-mark.svg — accessible SVG skeleton reference (short-form IDs):
  Actual file on disk: single-line SVG with aria-labelledby="rs-mark-title rs-mark-desc"
  Note: specimens use short-form IDs (aria-labelledby="t d"), matching rs-wordmark.svg convention.

From brandbook/assets/logo/svgo.config.mjs — SVGO config to reuse (do not create a new one):
  multipass: true; overrides: removeDesc:false, cleanupIds:false, convertColors:false

Critical hex values from tokens.json (PATTERNS.md lookup table):
  #3A6F8F  stead-blue.base (light primary)
  #5885a0  stead-blue.dark
  #9b5931  ember-copper.base
  #D2A94E  signal-gold.base (decorative-only)
  #183247  ink-blue.base
  #24313D  slate-stead.base
  #0F1720  basalt.base
  #E8ECE8  stone-mist.base
  #F5F7F6  rain-tint.base
  #C4CCD1  quarry.base
  #606d66  moss-grey.canonical
  #2d7753  success.canonical
  #8f601a  warning.canonical
  #b04848  danger.canonical
  #B44949  danger.white-rt
  #356E8C  info.base
  #ffffff  neutral-0 (light)
  #f4f6f8  neutral-50 (light)
  #e7ebf0  neutral-200
  #d8dee6  neutral-300
  #99a3af  neutral-500
  #5c6b7a  neutral-600
  #1a2332  neutral-900 (light text)
  #10161f  neutral-0 (dark base)
  #19222e  neutral-50 (dark)
  #2e3d52  neutral-300 (dark)
  #7a8fa3  neutral-500 (dark)
  #a8b9ca  neutral-600 (dark)
  #e8edf3  neutral-900 (dark text)

Font stacks from tokens.css :root invariant block:
  --rs-font-display: "Sora", "Inter", ui-sans-serif, system-ui, sans-serif
  --rs-font-sans:    "Inter", ui-sans-serif, system-ui, -apple-system, "Segoe UI", sans-serif
  --rs-font-mono:    "IBM Plex Mono", ui-monospace, "SFMono-Regular", Menlo, monospace

Type scale SVG px approximations (from PATTERNS.md):
  --rs-text-2xl → 32px (Sora 700)
  --rs-text-xl  → 22px (Sora 600)
  --rs-text-lg  → 18px (Sora 600)
  --rs-text-md  → 17px (Inter 500)
  --rs-text-base → 15px (Inter 400)
  --rs-text-sm  → 14px (Inter 400)
  --rs-text-xs  → 12px (Inter 400)
  --rs-text-2xs → 11px (IBM Plex Mono 500)
  Code line     → 12px (IBM Plex Mono 400)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create specimens directory + author palette.svg</name>
  <files>brandbook/assets/specimens/palette.svg</files>
  <read_first>
    - brandbook/tokens.json — read primitive.* section for every brand hex value + token name; read primitive.neutral-ramp for the neutral spot checks; read admin_css_mapping.light and admin_css_mapping.dark for semantic roles
    - brandbook/tokens.css — read :root invariant block lines for font-family stacks (--rs-font-mono for swatch labels)
    - brandbook/assets/logo/rs-mark.svg — read accessible SVG skeleton; note IDs pattern
    - brandbook/assets/logo/svgo.config.mjs — confirm config options (removeDesc:false, cleanupIds:false, convertColors:false); reuse verbatim for optimization
    - scripts/ci/lint.sh — confirm exact threshold (line 43: 51200 bytes) and loop structure
    - .planning/phases/99-specimens/99-PATTERNS.md — Pattern Assignment for palette.svg: swatch chip geometry (rect 112×48, rx=6, label IBM Plex Mono 9px) and the resolved hex lookup table
  </read_first>
  <action>
    Step 1 — Create directory:
    `mkdir -p brandbook/assets/specimens/`

    Verify with: `test -d brandbook/assets/specimens && echo ok`

    Step 2 — Author brandbook/assets/specimens/palette.svg:

    Use the accessible SVG skeleton from PATTERNS.md: viewBox="0 0 860 520", aria-labelledby="t d",
    role="img", with title id="t" "Rulestead — Palette Specimen" and desc id="d" describing the
    palette swatches. All visual content inside <g aria-hidden="true">.

    Organize swatches into two rows:
    - Row 1 (y=40): Brand primaries — stead-blue #3A6F8F, ember-copper #9b5931, ink-blue #183247,
      slate-stead #24313D, basalt #0F1720, signal-gold #D2A94E (add "(decorative only)" note in label)
    - Row 2 (y=140): Supporting — moss-grey #606d66, stone-mist #E8ECE8, rain-tint #F5F7F6,
      quarry #C4CCD1, success #2d7753, warning #8f601a, danger #b04848, info #356E8C
    - Row 3 (y=240): Light neutral ramp spot checks — #ffffff, #f4f6f8, #e7ebf0, #d8dee6,
      #99a3af, #5c6b7a, #1a2332 (left to right)
    - Row 4 (y=340): Dark neutral ramp — #10161f, #19222e, #2e3d52, #7a8fa3, #a8b9ca, #e8edf3,
      plus dark semantic variants stead-blue.dark #5885a0, ember-copper.dark #ba6b3c

    Per swatch: rect x=X y=Y width="112" height="48" fill=HEX rx="6"; hex label IBM Plex Mono
    9px fill="#1a2332" at y+66; token name label IBM Plex Mono 8px fill="#5c6b7a" at y+77.
    Swatch columns separated by 128px (112px chip + 16px gap).

    Add section header text labels ("Brand Primaries", "Supporting Palette", "Light Neutral Ramp",
    "Dark Neutral Ramp") above each row using Inter 11px fill="#5c6b7a".

    ALL fills are hard-coded hex literals. NEVER use var(--rs-*).
    NO base64, NO <image>, NO <use href="external...">, NO <script>.

    Step 3 — SVGO optimize:
    `npx svgo --config brandbook/assets/logo/svgo.config.mjs brandbook/assets/specimens/palette.svg`

    Step 4 — Verify size:
    `wc -c < brandbook/assets/specimens/palette.svg`
    Must be ≤51200. If over, reduce: drop duplicate neutral swatches or reduce label detail.
  </action>
  <verify>
    <automated>
      test -d brandbook/assets/specimens &amp;&amp; echo "dir ok" &amp;&amp;
      test -f brandbook/assets/specimens/palette.svg &amp;&amp; echo "file ok" &amp;&amp;
      grep -cE '#3[Aa]6[Ff]8[Ff]|#3A6F8F' brandbook/assets/specimens/palette.svg &amp;&amp;
      grep -c 'base64' brandbook/assets/specimens/palette.svg | grep -q '^0$' &amp;&amp; echo "no base64" &amp;&amp;
      grep -c '&lt;title' brandbook/assets/specimens/palette.svg | grep -qv '^0$' &amp;&amp; echo "has title" &amp;&amp;
      test $(wc -c &lt; brandbook/assets/specimens/palette.svg | tr -d ' ') -le 51200 &amp;&amp; echo "size ok"
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -d brandbook/assets/specimens` exits 0
    - `test -f brandbook/assets/specimens/palette.svg` exits 0
    - `grep -cE '#3[Aa]6[Ff]8[Ff]|#3A6F8F' brandbook/assets/specimens/palette.svg` returns ≥1
    - `grep -c 'base64' brandbook/assets/specimens/palette.svg` returns 0
    - `grep -c '<title' brandbook/assets/specimens/palette.svg` returns ≥1
    - `grep -c 'role="img"' brandbook/assets/specimens/palette.svg` returns ≥1
    - `grep -c 'aria-labelledby' brandbook/assets/specimens/palette.svg` returns ≥1
    - `wc -c < brandbook/assets/specimens/palette.svg` is ≤51200
  </acceptance_criteria>
  <done>brandbook/assets/specimens/ exists; palette.svg committed with all brand swatches annotated with hex + token name; accessible skeleton present; size ≤51200 bytes after SVGO.</done>
</task>

<task type="auto">
  <name>Task 2: Author typography.svg</name>
  <files>brandbook/assets/specimens/typography.svg</files>
  <read_first>
    - brandbook/tokens.css — read :root invariant block fully: --rs-font-display, --rs-font-sans, --rs-font-mono stacks; --rs-text-2xl through --rs-text-2xs pixel values; --rs-leading-tight
    - brandbook/assets/logo/svgo.config.mjs — reuse for SVGO optimization (same config as Task 1)
    - .planning/phases/99-specimens/99-PATTERNS.md — Pattern Assignment for typography.svg: live <text> element pattern, type scale px table, font-family stack values from tokens.css
    - .planning/phases/99-specimens/99-RESEARCH.md — Pattern 3 (Typography Ramp): CRITICAL constraint that outlined glyphs are forbidden for type ramp; live <text> required; LOGO-04 path-outlining is logo-only
    - scripts/ci/lint.sh — confirm 51200-byte threshold
  </read_first>
  <action>
    Author brandbook/assets/specimens/typography.svg with live <text> elements — NOT outlined
    paths. Outlining the full type ramp via fontTools would exceed 50 KB. This is a specimens file,
    not a logo file; LOGO-04 applies only to brandbook/assets/logo/*.svg.

    Accessible SVG skeleton: viewBox="0 0 760 580", aria-labelledby="t d", role="img",
    title id="t" "Rulestead — Typography Specimen",
    desc id="d" "Type scale ramp: Sora, Inter, and IBM Plex Mono at each --rs-text-* size with weight and token-name labels."
    All visual content inside <g aria-hidden="true">.

    Add section background for legibility: rect width="760" height="580" fill="#ffffff".

    Nine type rows, each with a specimen text line and a metadata label line below it.
    Row spacing: approximately 60px per row (sample text ~20px, label ~12px, then 28px gap).

    Rows (specimen text → label format "TOKEN · FAMILY · WEIGHT"):
    1. y≈50:  32px Sora 700  fill="#1a2332"  "The quick brown fox jumps"
               y+14: 9px IBM Plex Mono fill="#5c6b7a"  "--rs-text-2xl · Sora · 700 · --rs-leading-tight"
    2. y≈120: 22px Sora 600  fill="#1a2332"  "Feature Flag Management"
               label "--rs-text-xl · Sora · 600"
    3. y≈185: 18px Sora 600  fill="#1a2332"  "Evaluation Rules"
               label "--rs-text-lg · Sora · 600"
    4. y≈245: 17px Inter 500  fill="#1a2332"  "Card title or lead text"
               label "--rs-text-md · Inter · 500"
    5. y≈305: 15px Inter 400  fill="#1a2332"  "Body text at base scale — regular paragraph copy"
               label "--rs-text-base · Inter · 400 · 0.95rem"
    6. y≈360: 14px Inter 400  fill="#5c6b7a"  "Secondary body / helper label"
               label "--rs-text-sm · Inter · 400"
    7. y≈410: 12px Inter 400  fill="#5c6b7a"  "Caption or helper text"
               label "--rs-text-xs · Inter · 400"
    8. y≈455: 11px IBM Plex Mono 500  fill="#5c6b7a"  "OVERLINE · BADGE · MICRO"
               label "--rs-text-2xs · IBM Plex Mono · 500"
    9. y≈500: 12px IBM Plex Mono 400  fill="#1a2332"  "const flag = evaluate(ctx) // => true"
               label "code / pre · IBM Plex Mono · 400"

    font-family values verbatim from tokens.css:
    - Sora rows: font-family="Sora, Inter, ui-sans-serif, system-ui, sans-serif"
    - Inter rows: font-family="Inter, ui-sans-serif, system-ui, -apple-system, Segoe UI, sans-serif"
    - IBM Plex Mono rows: font-family="IBM Plex Mono, ui-monospace, SFMono-Regular, Menlo, monospace"
    - Label metadata rows always: font-family="IBM Plex Mono, ui-monospace, monospace"

    ALL fills are hard-coded hex literals (#1a2332 for primary text, #5c6b7a for labels/secondary).
    NO var(--rs-*). NO base64. NO outlined paths.

    SVGO optimize:
    `npx svgo --config brandbook/assets/logo/svgo.config.mjs brandbook/assets/specimens/typography.svg`

    Verify size:
    `wc -c < brandbook/assets/specimens/typography.svg`
    Must be ≤51200. Typography.svg with live text elements will be well under 5 KB; no risk of
    size budget breach.
  </action>
  <verify>
    <automated>
      test -f brandbook/assets/specimens/typography.svg &amp;&amp; echo "file ok" &amp;&amp;
      grep -c '&lt;text' brandbook/assets/specimens/typography.svg &amp;&amp;
      grep -c 'base64' brandbook/assets/specimens/typography.svg | grep -q '^0$' &amp;&amp; echo "no base64" &amp;&amp;
      grep -c '&lt;title' brandbook/assets/specimens/typography.svg | grep -qv '^0$' &amp;&amp; echo "has title" &amp;&amp;
      grep -c 'rs-text' brandbook/assets/specimens/typography.svg &amp;&amp;
      test $(wc -c &lt; brandbook/assets/specimens/typography.svg | tr -d ' ') -le 51200 &amp;&amp; echo "size ok"
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f brandbook/assets/specimens/typography.svg` exits 0
    - `grep -c '<text' brandbook/assets/specimens/typography.svg` returns ≥9 (one per type row minimum)
    - `grep -c 'base64' brandbook/assets/specimens/typography.svg` returns 0
    - `grep -c '<title' brandbook/assets/specimens/typography.svg` returns ≥1
    - `grep -c 'role="img"' brandbook/assets/specimens/typography.svg` returns ≥1
    - `grep -c 'rs-text' brandbook/assets/specimens/typography.svg` returns ≥1 (token labels present)
    - `grep -c 'Sora' brandbook/assets/specimens/typography.svg` returns ≥1 (display font referenced)
    - `grep -c 'IBM Plex Mono' brandbook/assets/specimens/typography.svg` returns ≥1 (mono font referenced)
    - `wc -c < brandbook/assets/specimens/typography.svg` is ≤51200
  </acceptance_criteria>
  <done>typography.svg committed with the full --rs-text-* scale (9 rows), Sora/Inter/IBM Plex Mono font families, token-name labels on each row, accessible skeleton; live text elements (not outlined paths); size ≤51200 bytes after SVGO.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed SVG → git repo | Static hand-authored asset; no user input, no runtime, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Tampering | palette.svg / typography.svg (embedded raster or external entity) | mitigate | `grep -c base64 brandbook/assets/specimens/*.svg` must return 0 per file; no `<image>`, no `<use href="external">` — enforced by acceptance_criteria on every task |
| T-99-02 | Tampering | svgo via npx (supply chain) | accept | svgo@4.0.1 audited Phase 97 with 12-year npm track record; no package.json install; ad-hoc npx resolution confirmed |
| T-99-03 | Information Disclosure | committed font binary violating v1.14 font policy | mitigate | Phase 99 uses live `<text>` elements only; no TTF/OTF committed; verify: `file brandbook/assets/specimens/*.svg` shows SVG XML only |
| T-99-SC | Tampering | npm/npx svgo install | accept | Existing Phase 97 audit covers svgo; no new packages installed; slopcheck not applicable (no pip/cargo) |

**Surface assessment:** Phase 99 is static SVG asset authoring with no runtime code, no user input, no network requests, and no untrusted data. The realistic threat surface is minimal. The one enforced guardrail — no embedded/base64 raster — is verified by grep assertion on every task.
</threat_model>

<verification>
After both tasks complete:

```bash
# File existence
test -f brandbook/assets/specimens/palette.svg && echo "palette ok"
test -f brandbook/assets/specimens/typography.svg && echo "typography ok"

# No base64 in either file
grep -c 'base64' brandbook/assets/specimens/palette.svg
grep -c 'base64' brandbook/assets/specimens/typography.svg
# Both must return 0

# Stead Blue present in palette
grep -cE '#3[Aa]6[Ff]8[Ff]|#3A6F8F' brandbook/assets/specimens/palette.svg

# Live text in typography
grep -c '<text' brandbook/assets/specimens/typography.svg

# Accessible titles
grep -c '<title' brandbook/assets/specimens/palette.svg
grep -c '<title' brandbook/assets/specimens/typography.svg

# Size budget spot checks
wc -c < brandbook/assets/specimens/palette.svg
wc -c < brandbook/assets/specimens/typography.svg
# Both must be ≤51200
```
</verification>

<success_criteria>
- brandbook/assets/specimens/ directory exists and contains palette.svg and typography.svg
- palette.svg: all brand swatches present with hex + token name annotations; no base64; size ≤51200; accessible title/desc/role=img
- typography.svg: ≥9 live <text> rows; token labels present; Sora + Inter + IBM Plex Mono referenced; no base64; size ≤51200; accessible title/desc/role=img
- Both files SVGO-optimized using brandbook/assets/logo/svgo.config.mjs (accessibility metadata preserved)
</success_criteria>

<output>
Create `.planning/phases/99-specimens/99-01-SUMMARY.md` when done.
</output>
