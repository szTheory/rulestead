---
phase: 28-e2e-demo-environments-ga-release
plan: 03
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - examples/demo/frontend/package.json
  - examples/demo/frontend/tsconfig.json
  - examples/demo/frontend/next.config.ts
  - examples/demo/frontend/Dockerfile
  - examples/demo/frontend/app/layout.tsx
  - examples/demo/frontend/app/page.tsx
  - examples/demo/frontend/lib/openfeature/rulestead-web-provider.ts
  - examples/demo/frontend/lib/openfeature/client.ts
  - examples/demo/frontend/tests/rulestead-web-provider.test.ts
autonomous: true
requirements:
  - GA-02

must_haves:
  truths:
    - "The sample frontend is an external Next.js app under `examples/demo/frontend`, not a new Rulestead product surface."
    - "The sample frontend consumes the Phoenix host bridge through an OpenFeature provider, proving cross-stack usage per D-03."
    - "The sample frontend is containerizable as a host/example app so the root Compose graph can run it without creating a publishable `rulestead_admin` posture."
    - "The provider and page state are ready for a later browser proof to observe Admin-driven updates without reworking the frontend contract."
  artifacts:
    - path: "examples/demo/frontend/lib/openfeature/rulestead-web-provider.ts"
      provides: "Custom OpenFeature web provider over the Phoenix bridge contract"
    - path: "examples/demo/frontend/app/page.tsx"
      provides: "Visible sample UI deltas driven by seeded flags"
    - path: "examples/demo/frontend/Dockerfile"
      provides: "Container build target consumed by the root Compose graph"
  key_links:
    - from: "examples/demo/frontend/lib/openfeature/rulestead-web-provider.ts"
      to: "examples/demo/backend/lib/rulestead_demo_web/controllers/flag_controller.ex"
      via: "OpenFeature provider fetches the host-owned `/api/flags` contract implemented in 28-02"
    - from: "examples/demo/frontend/lib/openfeature/rulestead-web-provider.ts"
      to: "examples/demo/backend/lib/rulestead_demo_web/controllers/flag_stream_controller.ex"
      via: "backend event stream from 28-02 is translated into OpenFeature configuration-changed events"
    - from: "examples/demo/frontend/Dockerfile"
      to: "docker-compose.yml"
      via: "frontend build target is consumed later by the root demo orchestration plan"
---

<objective>
Build the external demo frontend and freeze the sample-client contract that the later browser proof will exercise through OpenFeature integration.

Purpose: GA-02 is the key product proof for non-Elixir surfaces. This slice must show that Rulestead stays embedded in the Phoenix host while an external frontend consumes it through a simple, host-owned API.
Output: a Next.js demo app, a custom OpenFeature web provider, visible flag-driven UI states, and a frontend container build target that downstream orchestration can run unchanged.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-CONTEXT.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-RESEARCH.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-PATTERNS.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-01-PLAN.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-02-PLAN.md
@README.md
@open_feature_rulestead/README.md

<interfaces>
Locked frontend contract:
```text
D-01: frontend lives in examples/demo/frontend only.
D-03: frontend consumes the Phoenix bridge through HTTP/OpenFeature, not Redis/Postgres and not a new core OFREP server.
```

Backend contract frozen in 28-01 and implemented in 28-02:
```text
GET /api/flags?env={env}&flag_key={key}[&targeting_key=...]
GET /api/flags/stream?env={env}
```

Expected OpenFeature behavior:
```text
Provider resolves seeded demo flags from the bridge API and emits configuration-changed when the backend stream reports a relevant flag update.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the Next.js demo app and custom OpenFeature web provider</name>
  <files>examples/demo/frontend/package.json, examples/demo/frontend/tsconfig.json, examples/demo/frontend/next.config.ts, examples/demo/frontend/Dockerfile, examples/demo/frontend/app/layout.tsx, examples/demo/frontend/app/page.tsx, examples/demo/frontend/lib/openfeature/rulestead-web-provider.ts, examples/demo/frontend/lib/openfeature/client.ts, examples/demo/frontend/tests/rulestead-web-provider.test.ts</files>
  <behavior>
    - Test 1: The provider resolves seeded backend flags through `/api/flags` and maps them into OpenFeature values/details without talking to storage directly.
    - Test 2: Backend stream events trigger OpenFeature configuration-changed events so the sample UI can refresh immediately after an admin mutation.
    - Test 3: The sample page renders at least one obvious flag-driven delta from seeded data, making the toggle effect visible without extra setup.
    - Test 4: The frontend image can build from the repo root context without pulling in unpublished package artifacts or bypassing the example-app boundary.
  </behavior>
  <action>
    Create the external frontend in `examples/demo/frontend` per D-01 using a minimal App Router surface and a bespoke OpenFeature web provider per D-03. The provider must speak only to the host-owned Phoenix endpoints frozen in 28-01 and implemented in 28-02, normalize bounded flag details, and translate backend live-update events into OpenFeature configuration-changed events. Keep the UI intentionally small and obvious: a seeded flag toggle should produce an unmistakable visual or content change. Add the frontend Docker build target here so downstream Compose work consumes the finished app rather than claiming ownership before the app exists. Do not introduce direct database/cache access, a standalone control plane, or a core-library remote evaluation feature.
  </action>
  <verify>
    <automated>cd examples/demo/frontend && npm test -- --runInBand rulestead-web-provider.test.ts</automated>
  </verify>
  <done>The sample frontend proves that an external web app can consume Rulestead through a host-owned OpenFeature bridge and is ready to be run by the root Compose stack without further contract changes.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the frontend contract stable for downstream orchestration</name>
  <files>examples/demo/frontend/app/page.tsx, examples/demo/frontend/lib/openfeature/client.ts, examples/demo/frontend/lib/openfeature/rulestead-web-provider.ts</files>
  <action>
    Freeze the frontend contract expected by the downstream Compose and browser-proof slice: read `NEXT_PUBLIC_FLAGS_API_BASE` or equivalent host-owned configuration, keep the seeded environment selection explicit, and ensure the page can render an initial backend-driven state before the later orchestration plan adds browser automation and README guidance. Keep this plan independent of Compose ownership so 28-04 only consumes the finished frontend contract. Do not add broad UI coverage or standalone-product framing here.
  </action>
  <verify>
    <automated>cd examples/demo/frontend && npm test -- --runInBand rulestead-web-provider.test.ts</automated>
  </verify>
  <done>The frontend exposes a stable runtime/config contract that the downstream Compose and browser-proof slice can consume without revisiting the provider or page architecture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix bridge -> browser OpenFeature provider | Backend evaluation data becomes client-visible feature state here |
| backend event stream -> browser refresh logic | Server-side mutation notifications trigger client-side updates here |
| frontend runtime config -> browser provider | Host-owned API base and environment config become live client evaluation inputs here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | I | browser provider payload handling | mitigate | Keep the provider payload bounded to demo-safe evaluation fields and avoid exposing backend internals or secrets. |
| T-28-03-02 | T | live-update event handling | mitigate | Accept only the documented backend event shape and emit targeted OpenFeature refresh events for relevant flag/environment changes. |
| T-28-03-03 | D | downstream container/runtime drift | mitigate | Define the frontend image and runtime config here so the later Compose/browser-proof slice consumes a stable app contract instead of rebuilding it ad hoc. |
</threat_model>

<verification>
Run the frontend unit/provider suite. Confirm the sample frontend uses OpenFeature over the Phoenix bridge, reflects seeded flag state, and exposes the stable runtime/config contract the downstream Compose/browser-proof slice will exercise.
</verification>

<success_criteria>
Phase 28 has a stable external-client surface: an external Next.js app under `examples/demo/frontend` consumes Rulestead through a host-owned OpenFeature bridge and is ready for Compose-backed end-to-end verification without further contract changes.
</success_criteria>

<output>
After completion, create `.planning/phases/28-e2e-demo-environments-ga-release/28-03-SUMMARY.md`
</output>
