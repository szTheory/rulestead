---
phase: 28-e2e-demo-environments-ga-release
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - examples/demo/backend/lib/rulestead_demo_web/router.ex
  - examples/demo/backend/lib/rulestead_demo_web/controllers/flag_controller.ex
  - examples/demo/backend/lib/rulestead_demo_web/controllers/flag_json.ex
  - examples/demo/backend/lib/rulestead_demo_web/controllers/flag_stream_controller.ex
  - examples/demo/backend/priv/repo/seeds.exs
  - examples/demo/backend/test/rulestead_demo_web/controllers/flag_controller_test.exs
  - examples/demo/backend/test/rulestead_demo_web/controllers/flag_stream_controller_test.exs
  - examples/demo/backend/test/rulestead_demo/demo_seed_smoke_test.exs
autonomous: true
requirements:
  - GA-01
  - GA-02

must_haves:
  truths:
    - "The demo backend exposes the locked D-03 bridge routes `/api/flags` and `/api/flags/stream` from the host app."
    - "Bridge evaluation and live-update behavior delegates through public runtime APIs and returns bounded payloads that a browser OpenFeature provider can consume."
    - "The demo seed path creates a populated Demo Project, staging/production environments, initial flags, and the deterministic demo operator through supported admin/store seams."
    - "Backend-focused regression tests prove the bridge, fail-closed validation, seeded state, and sign-in-to-admin path before frontend/Compose work starts."
  artifacts:
    - path: "examples/demo/backend/lib/rulestead_demo_web/controllers/flag_controller.ex"
      provides: "Public `/api/flags` evaluation contract"
    - path: "examples/demo/backend/lib/rulestead_demo_web/controllers/flag_stream_controller.ex"
      provides: "Host-owned live change feed for browser consumers"
    - path: "examples/demo/backend/priv/repo/seeds.exs"
      provides: "Seeded demo project, environments, flags, and operator created through supported seams"
    - path: "examples/demo/backend/test/rulestead_demo_web/controllers/flag_controller_test.exs"
      provides: "Regression coverage for normalized bridge JSON and validation behavior"
    - path: "examples/demo/backend/test/rulestead_demo/demo_seed_smoke_test.exs"
      provides: "Regression coverage for seeded authored state and mounted-admin readiness"
  key_links:
    - from: "examples/demo/backend/lib/rulestead_demo_web/controllers/flag_controller.ex"
      to: "Rulestead.Runtime.evaluate/3"
      via: "environment-explicit host bridge JSON contract"
    - from: "examples/demo/backend/lib/rulestead_demo_web/controllers/flag_stream_controller.ex"
      to: "browser OpenFeature provider"
      via: "bounded SSE event shape for configuration-changed invalidation"
    - from: "examples/demo/backend/priv/repo/seeds.exs"
      to: "examples/demo/backend/lib/rulestead_demo_web/controllers/demo_session_controller.ex"
      via: "seeded demo operator identity is what `/demo/sign-in` uses for mounted-admin automation"
    - from: "examples/demo/backend/test/rulestead_demo/demo_seed_smoke_test.exs"
      to: "examples/demo/backend/lib/rulestead_demo_web/router.ex"
      via: "test proves the seeded operator can reach `/admin/flags?env=staging` through the host seam"
---

<objective>
Implement the backend behavior that the rest of the demo consumes: the host-owned bridge API, live-update stream, seeded authored state, and backend regression coverage.

Purpose: This slice removes the highest-risk runtime behavior from the bootstrap plan without delaying frontend work. After it lands, the Phoenix host exposes the exact D-03 contract and seeded data the browser and Compose proofs rely on.
Output: `/api/flags`, `/api/flags/stream`, `priv/repo/seeds.exs`, and focused backend tests covering bridge payloads, validation, seeded state, and mounted-admin readiness.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-CONTEXT.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-RESEARCH.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-PATTERNS.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-01-PLAN.md
@README.md
@guides/flows/multi-env.md

<interfaces>
Locked route and seed decisions:
```text
D-03: Phoenix owns `/api/flags`; Next.js does not talk to Redis or Postgres directly.
D-04: seeds create the Demo Project, environments, initial flags, and demo operator through supported admin/store seams.
```

Runtime bridge contract the backend must preserve:
```elixir
Rulestead.Runtime.evaluate(environment_key, flag_key, context)
Rulestead.Runtime.enabled?(environment_key, flag_key, context)
Rulestead.Runtime.get_variant(environment_key, flag_key, context)
Rulestead.Runtime.get_value(environment_key, flag_key, context, default)
```

Auth/session seam inherited from 28-01:
```text
/demo/sign-in -> writes current_actor, rulestead_admin_environments, rulestead_admin_last_env
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and test the host-owned bridge and live-update endpoints</name>
  <files>examples/demo/backend/lib/rulestead_demo_web/router.ex, examples/demo/backend/lib/rulestead_demo_web/controllers/flag_controller.ex, examples/demo/backend/lib/rulestead_demo_web/controllers/flag_json.ex, examples/demo/backend/lib/rulestead_demo_web/controllers/flag_stream_controller.ex, examples/demo/backend/test/rulestead_demo_web/controllers/flag_controller_test.exs, examples/demo/backend/test/rulestead_demo_web/controllers/flag_stream_controller_test.exs</files>
  <behavior>
    - Test 1: `GET /api/flags?env=staging&flag_key=enable-new-dashboard` returns normalized JSON sourced from `Rulestead.Runtime`, not storage tables.
    - Test 2: Missing or invalid `env` / `flag_key` inputs fail closed with bounded error JSON.
    - Test 3: The live-update endpoint emits a bounded event shape that a browser OpenFeature provider can convert into a configuration-changed event.
  </behavior>
  <action>
    Implement the host-owned `/api/flags` and `/api/flags/stream` endpoints per D-03 by extending the router reserved in 28-01. Delegate evaluation strictly to public `Rulestead.Runtime` APIs with explicit environment selection and normalized JSON/SSE payloads. Do not introduce direct Redis/Postgres reads in browser-facing code, do not rename the routes to `/api/v1/*`, and do not add Channels or an OFREP server.
  </action>
  <verify>
    <automated>cd examples/demo/backend && mix test test/rulestead_demo_web/controllers/flag_controller_test.exs test/rulestead_demo_web/controllers/flag_stream_controller_test.exs</automated>
  </verify>
  <done>The backend exposes the locked bridge routes with bounded payloads and regression coverage for evaluation, validation, and live-update behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement seeded authored state and prove mounted-admin readiness</name>
  <files>examples/demo/backend/priv/repo/seeds.exs, examples/demo/backend/test/rulestead_demo/demo_seed_smoke_test.exs</files>
  <behavior>
    - Test 1: The seed script creates a Demo Project, `staging` and `production` environments, initial flags, and the deterministic demo operator through supported seams.
    - Test 2: After seeding, the host-owned `/demo/sign-in` path yields an authorized session that can reach `/admin/flags?env=staging`.
  </behavior>
  <action>
    Add `priv/repo/seeds.exs` per D-04 so the seed path creates the Demo Project, environments, initial flags, and deterministic demo operator through supported `Rulestead.Admin` or store-command seams instead of direct table inserts. Back it with a focused smoke test that proves the seeded operator plus the 28-01 session seam make the mounted Admin UI non-empty and automatable on first boot. If a narrow supported-seam bug fix is truly required, keep it minimal and document it in the execution summary rather than broadening phase scope.
  </action>
  <verify>
    <automated>cd examples/demo/backend && mix test test/rulestead_demo/demo_seed_smoke_test.exs</automated>
  </verify>
  <done>The backend seed path guarantees immediate authored data plus an authorized demo operator, and backend tests prove the mounted admin is ready before frontend or Compose verification begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser request -> demo backend bridge | Untrusted query/body inputs become runtime evaluation requests here |
| backend seeds -> authored-state store | Boot-time seed data crosses into persistent authored state here |
| admin mutation -> live update stream | Security-relevant backend changes become browser-visible invalidation events here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | T | `/api/flags` request normalization | mitigate | Validate and normalize `env`, `flag_key`, and context at route entry, then delegate only to public runtime APIs. |
| T-28-02-02 | I | bridge JSON and SSE payloads | mitigate | Return bounded evaluation metadata and change signals only; do not leak raw storage internals, secrets, or runtime telemetry. |
| T-28-02-03 | E | seed/bootstrap path | mitigate | Seed through supported admin/store seams and keep mounted-admin authorization tied to the host session and policy contract. |
| T-28-02-04 | R | backend readiness claim | mitigate | Require focused controller and seed smoke tests before downstream plans consume the bridge or seeded data. |
</threat_model>

<verification>
Run the focused backend controller and seed smoke suites. Confirm the host app exposes the locked D-03 routes, rejects malformed inputs safely, seeds usable authored state, and supports the deterministic mounted-admin operator flow.
</verification>

<success_criteria>
Phase 28 has a stable backend behavior contract: the Phoenix host app exposes `/api/flags` and `/api/flags/stream`, seeds useful demo data automatically, and proves those seams through backend regression tests before any frontend or Compose proof depends on them.
</success_criteria>

<output>
After completion, create `.planning/phases/28-e2e-demo-environments-ga-release/28-02-SUMMARY.md`
</output>
