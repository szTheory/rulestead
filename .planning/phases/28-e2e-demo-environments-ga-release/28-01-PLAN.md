---
phase: 28-e2e-demo-environments-ga-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/demo/backend/mix.exs
  - examples/demo/backend/config/config.exs
  - examples/demo/backend/config/dev.exs
  - examples/demo/backend/config/rulestead.exs
  - examples/demo/backend/config/runtime.exs
  - examples/demo/backend/lib/rulestead_demo/application.ex
  - examples/demo/backend/lib/rulestead_demo/admin_policy.ex
  - examples/demo/backend/lib/rulestead_demo/repo.ex
  - examples/demo/backend/lib/rulestead_demo_web/endpoint.ex
  - examples/demo/backend/lib/rulestead_demo_web/controllers/demo_session_controller.ex
  - examples/demo/backend/lib/rulestead_demo_web/router.ex
  - examples/demo/backend/priv/repo/migrations/**/*
autonomous: true
requirements:
  - GA-01

must_haves:
  truths:
    - "The demo backend exists as a thin Phoenix host app under `examples/demo/backend`, not a core-package mutation or a standalone `rulestead_admin` product."
    - "The backend owns the install/bootstrap artifacts needed for `mix ecto.setup`, including repo wiring and install-generated migrations."
    - "The mounted Admin UI is reachable through the host app with an explicit host policy seam and deterministic demo sign-in path."
    - "The later bridge and frontend slices can build on stable host-owned routes, session keys, and persistence seams instead of inventing them ad hoc."
  artifacts:
    - path: "examples/demo/backend/mix.exs"
      provides: "Phoenix host app dependency and task wiring for local sibling packages"
    - path: "examples/demo/backend/lib/rulestead_demo/repo.ex"
      provides: "Host-owned Ecto repo required for `mix ecto.setup`"
    - path: "examples/demo/backend/lib/rulestead_demo_web/router.ex"
      provides: "Mounted `rulestead_admin` routes plus host-owned demo session entrypoint"
    - path: "examples/demo/backend/lib/rulestead_demo_web/controllers/demo_session_controller.ex"
      provides: "Deterministic demo operator session bootstrap"
    - path: "examples/demo/backend/priv/repo/migrations/**/*"
      provides: "Installer-generated persistence artifacts owned by the host app"
  key_links:
    - from: "examples/demo/backend/mix.exs"
      to: "rulestead and rulestead_admin"
      via: "local path dependencies preserving the sibling-package release design"
    - from: "examples/demo/backend/lib/rulestead_demo_web/router.ex"
      to: "rulestead_admin"
      via: "mounted host-admin seam with explicit `policy:` integration"
    - from: "examples/demo/backend/lib/rulestead_demo_web/controllers/demo_session_controller.ex"
      to: "examples/demo/backend/lib/rulestead_demo/admin_policy.ex"
      via: "host-owned session bootstrap satisfies the mounted-admin authorization seam"
    - from: "examples/demo/backend/lib/rulestead_demo/repo.ex"
      to: "examples/demo/backend/priv/repo/migrations/**/*"
      via: "repo plus installer output enable deterministic `mix ecto.setup`"
---

<objective>
Create the Phoenix demo backend skeleton and bootstrap contract: a host-owned app under `examples/demo/backend` with install-generated persistence artifacts, mounted admin wiring, and the deterministic demo operator session seam.

Purpose: The riskiest Phase 28 dependency is the host-app boundary itself. This plan isolates that work so later bridge, frontend, and Compose slices consume a finished host foundation instead of mixing bootstrap with higher-risk API/test work.
Output: a compiling Phoenix host app with local sibling-package deps, repo/config/install artifacts, mounted admin router wiring, and the `/demo/sign-in` auth/session seam.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-CONTEXT.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-RESEARCH.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-PATTERNS.md
@README.md
@guides/flows/admin-ui.md
@rulestead_admin/README.md

<interfaces>
Locked host-app decisions:
```text
D-01: examples/demo/backend is host/example code only.
D-04: backend boot path must own the artifacts required for `ecto.setup`.
```

Mounted admin seam:
```elixir
rulestead_admin "/admin/flags", policy: MyApp.RulesteadPolicy
```

Session keys preserved from the previous revision:
```text
"current_actor"
"rulestead_admin_environments"
"rulestead_admin_last_env"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the demo backend and capture installer-owned bootstrap artifacts</name>
  <files>examples/demo/backend/mix.exs, examples/demo/backend/config/config.exs, examples/demo/backend/config/dev.exs, examples/demo/backend/config/rulestead.exs, examples/demo/backend/config/runtime.exs, examples/demo/backend/lib/rulestead_demo/application.ex, examples/demo/backend/lib/rulestead_demo/repo.ex, examples/demo/backend/lib/rulestead_demo_web/endpoint.ex, examples/demo/backend/priv/repo/migrations/**/*</files>
  <action>
    Create the Phoenix demo backend in `examples/demo/backend` per D-01 using local path deps to `rulestead` and `rulestead_admin`. Capture the supported install path explicitly: run `mix rulestead.install` inside the demo app, keep the generated runtime config and `priv/repo/migrations/**/*` under this plan's ownership, and wire the host-owned `RulesteadDemo.Repo` so `mix ecto.setup` has concrete persistence/bootstrap artifacts. Do not move code into `rulestead/` or `rulestead_admin`, and do not invent any standalone publish posture for `rulestead_admin`.
  </action>
  <verify>
    <automated>cd examples/demo/backend && mix deps.get && mix compile</automated>
  </verify>
  <done>`examples/demo/backend` compiles as a normal Phoenix host app with installer-backed persistence artifacts owned locally under the example-app boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the mounted-admin and deterministic session seams</name>
  <files>examples/demo/backend/lib/rulestead_demo/admin_policy.ex, examples/demo/backend/lib/rulestead_demo_web/controllers/demo_session_controller.ex, examples/demo/backend/lib/rulestead_demo_web/router.ex</files>
  <action>
    Mount `rulestead_admin` through the documented router macro with an explicit host policy module, then add the deterministic host-owned `/demo/sign-in` path that writes `"current_actor"`, `"rulestead_admin_environments"`, and `"rulestead_admin_last_env"` before redirecting to `/admin/flags?env=staging`. Keep authorization flowing through the Phase 27 `policy:` seam instead of bypassing package-local auth or mutating session state from tests. Reserve the locked D-03 bridge paths in the router only as host-owned routes that the next plan will implement.
  </action>
  <verify>
    <automated>cd examples/demo/backend && mix compile</automated>
  </verify>
  <done>The backend exposes stable mounted-admin and host-owned demo session seams that downstream plans can exercise without revisiting auth/bootstrap ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session request -> demo backend | Untrusted browser traffic becomes mounted-admin session state here |
| demo backend install/bootstrap -> Postgres | Host bootstrap creates persistence artifacts and later schema state here |
| mounted admin route -> host policy seam | Authorization for admin mutations crosses from package UI into host-owned policy logic here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | E | mounted admin auth seam | mitigate | Keep mounted Admin authorization explicit through the host `policy:` seam rather than any package-local shortcut. |
| T-28-01-02 | T | install/bootstrap artifacts | mitigate | Capture `mix rulestead.install` output inside the example app so the host owns its repo/runtime/migration contract and later plans do not patch it ad hoc. |
| T-28-01-03 | R | deterministic demo sign-in path | mitigate | Restrict the session bootstrap to the documented host route and required session keys so browser automation proves the real host-owned seam. |
| T-28-01-04 | D | bootstrap compile/setup path | mitigate | Compile the host app immediately after wiring deps/config/repo to catch broken bootstrap ownership before bridge or frontend work begins. |
</threat_model>

<verification>
Compile the demo backend after dependency/config/install setup and again after mounting admin plus the demo sign-in seam. Confirm the host app boundary, install/bootstrap ownership, and mounted-admin auth/session seams are stable before downstream slices build on them.
</verification>

<success_criteria>
Phase 28 has a stable host-app foundation: `examples/demo/backend` compiles as a Phoenix host app, owns its `ecto.setup` artifacts, mounts `rulestead_admin` through the host policy seam, and exposes the deterministic `/demo/sign-in` route for later automation.
</success_criteria>

<output>
After completion, create `.planning/phases/28-e2e-demo-environments-ga-release/28-01-SUMMARY.md`
</output>
