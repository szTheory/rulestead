---
phase: 28-e2e-demo-environments-ga-release
plan: 04
type: execute
wave: 3
depends_on:
  - 28-02
  - 28-03
files_modified:
  - docker-compose.yml
  - examples/demo/backend/Dockerfile
  - examples/demo/backend/entrypoint.sh
  - examples/demo/frontend/playwright.config.ts
  - examples/demo/frontend/tests/demo-toggle.spec.ts
  - examples/demo/README.md
  - README.md
  - scripts/demo/smoke.sh
autonomous: true
requirements:
  - GA-01
  - GA-02

must_haves:
  truths:
    - "One root-level `docker-compose up` brings up Postgres, Redis, the Phoenix demo backend, and the Next.js demo frontend per D-02."
    - "The backend waits for infrastructure readiness, runs setup automatically, and exposes a seeded Admin UI without manual intervention."
    - "The frontend service is reachable through the same Compose boot path and renders seeded flag-driven state from the backend bridge."
    - "Compose-level healthchecks make the under-5-minute demo promise observable instead of relying on startup races or sleep loops."
    - "The documented browser proof can sign in as the deterministic demo operator, toggle a seeded flag in mounted Admin, and observe the frontend update without restarts."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Root demo orchestration with four services and readiness gating"
    - path: "examples/demo/backend/entrypoint.sh"
      provides: "Deterministic backend boot sequence with `ecto.setup` before server start"
    - path: "scripts/demo/smoke.sh"
      provides: "Automated proof that the one-command demo reaches healthy backend, admin, and frontend surfaces"
    - path: "examples/demo/frontend/tests/demo-toggle.spec.ts"
      provides: "Automated proof of the deterministic sign-in -> Admin toggle -> frontend refresh loop"
  key_links:
    - from: "docker-compose.yml"
      to: "examples/demo/backend/entrypoint.sh"
      via: "backend service bootstraps migrations and seeds automatically"
    - from: "docker-compose.yml"
      to: "examples/demo/frontend/Dockerfile"
      via: "root compose consumes the stable frontend image created in 28-03 without turning it into a product package"
    - from: "scripts/demo/smoke.sh"
      to: "docker-compose.yml"
      via: "smoke automation proves health, admin reachability, seeded demo data, and frontend reachability"
    - from: "examples/demo/frontend/tests/demo-toggle.spec.ts"
      to: "examples/demo/backend/lib/rulestead_demo_web/controllers/demo_session_controller.ex"
      via: "browser verification signs in through the host-owned demo session path before mutating seeded flags"
---

<objective>
Turn the finished backend and frontend contracts into a one-command local environment by adding root-level Compose orchestration, deterministic container boot, and an automated smoke plus browser proof path.

Purpose: GA-01 is not satisfied by code alone; the runtime promise is `docker-compose up` with real readiness gates and immediate operator value. This plan consumes the already-defined backend and frontend slices and makes that promise executable.
Output: root `docker-compose.yml`, backend boot automation, a smoke script that proves backend/admin/frontend reachability, and the browser/docs proof for the seeded Admin-to-frontend toggle loop.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-CONTEXT.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-RESEARCH.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-PATTERNS.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-02-PLAN.md
@.planning/phases/28-e2e-demo-environments-ga-release/28-03-PLAN.md
@docker-compose.yml
@guides/recipes/deployment.md

<interfaces>
Locked orchestration contract:
```text
D-02: root docker-compose.yml must run postgres, redis, backend, and frontend.
D-04: backend container entrypoint runs migrations + seeds automatically.
```

Upstream contracts from 28-02 and 28-03:
```text
postgres -> 5432
redis -> 6379
backend -> 4000
frontend -> 3000

Backend bridge routes: /api/flags and /api/flags/stream
Demo operator session route: /demo/sign-in -> redirects to /admin/flags?env=staging
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add root Compose orchestration and deterministic backend container boot</name>
  <files>docker-compose.yml, examples/demo/backend/Dockerfile, examples/demo/backend/entrypoint.sh</files>
  <action>
    Replace the current root compose file with the Phase 28 four-service graph per D-02: Postgres, Redis, backend, and frontend. Consume the already-created frontend app/image from 28-03 instead of owning frontend scaffolding here. Add explicit healthchecks and `depends_on` readiness conditions so startup depends on service health rather than timing luck, and wire the backend container to the host-owned `mix ecto.setup` boot path delivered by 28-01 and 28-02. Preserve the sibling-package design: `rulestead/` and `rulestead_admin/` remain the products, and `examples/demo/**` is host/example code only.
  </action>
  <verify>
    <automated>docker compose config</automated>
  </verify>
  <done>The repo has a root Compose graph that can build and orchestrate the full local demo stack without inventing new product surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the seeded backend, frontend, and browser toggle path</name>
  <files>scripts/demo/smoke.sh, examples/demo/frontend/playwright.config.ts, examples/demo/frontend/tests/demo-toggle.spec.ts, examples/demo/README.md, README.md</files>
  <action>
    Use the backend container boot path from Task 1 per D-04, keeping non-trivial orchestration logic in scripts rather than inline shell fragments in Compose. Add `scripts/demo/smoke.sh` to bring the stack up, wait for health, and verify concrete checks for backend health, mounted Admin reachability, seeded Demo Project/flag data, and frontend reachability/rendering. Then add one focused browser-level proof that signs in through the host-owned `/demo/sign-in` route from 28-01, toggles the seeded flag through the mounted Admin UI, and verifies the frontend updates through the OpenFeature path implemented by 28-02 and 28-03. Update the root README and `examples/demo/README.md` so the one-command demo, admin URL, sample-frontend URL, seeded demo credentials/route, and expected toggle loop are explicit and honest. Do not add manual setup steps, sleep-loop folklore, or post-boot seed commands.
  </action>
  <verify>
    <automated>scripts/demo/smoke.sh && cd examples/demo/frontend && npm run test:e2e</automated>
  </verify>
  <done>`docker compose up` becomes a reproducible one-command demo path that lands on healthy, seeded Admin and frontend surfaces with no manual migration or seed steps, and the browser proof demonstrates the real Admin-to-frontend toggle loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local developer command -> Compose graph | One command starts all containers and injects env/runtime config here |
| backend container -> Postgres/Redis | Boot automation crosses into persistent/stateful services here |
| smoke script -> local demo endpoints | Verification logic asserts seeded reachability across service boundaries here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | D | Compose startup ordering | mitigate | Use healthchecks and readiness-gated dependencies instead of unbounded startup races or sleeps. |
| T-28-04-02 | T | backend entrypoint | mitigate | Keep boot sequence explicit and fail fast on `ecto.setup` or runtime errors before serving traffic. |
| T-28-04-03 | R | demo readiness claim | mitigate | Add a smoke script that proves seeded backend, admin, and frontend reachability through actual HTTP checks rather than documentation alone. |
| T-28-04-04 | E | browser toggle proof auth bootstrap | mitigate | Force browser automation through the host-owned `/demo/sign-in` session route so the demo proves a real authorized operator path instead of bypassing Phase 27 posture. |
</threat_model>

<verification>
Validate the Compose graph, then run the smoke script and browser proof. Confirm the root command path starts all four services, waits for readiness, reaches seeded backend/admin/frontend surfaces, and exercises the documented Admin-to-frontend toggle loop without extra manual setup.
</verification>

<success_criteria>
GA-01 and the cross-stack proof are executable: a platform engineer can run the root Compose entrypoint and land on a healthy, seeded local stack with Postgres, Redis, backend, and frontend all started deterministically, then observe the mounted Admin change the sample app through the documented browser path.
</success_criteria>

<output>
After completion, create `.planning/phases/28-e2e-demo-environments-ga-release/28-04-SUMMARY.md`
</output>
