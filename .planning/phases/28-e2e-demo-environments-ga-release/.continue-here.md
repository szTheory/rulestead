---
context: phase
phase: 28-e2e-demo-environments-ga-release
task: 2
total_tasks: 2
status: paused
last_updated: 2026-05-21T16:18:45Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Playwright Discovery Drift — `npm run test:e2e` currently executes `tests/rulestead-web-provider.test.ts`, which is a Jest unit test and crashes on `jest is not defined` before any browser proof runs. Structural mitigation: narrow Playwright discovery to browser specs only.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Playwright test scope too broad | `playwright test` scans `./tests` and picks up Jest unit tests, producing a false "e2e failed" signal before the browser flow starts. | blocking | Add `testMatch` or equivalent scoping so Playwright runs only `*.spec.ts` browser files, or separate Jest and Playwright test directories. |
| Chasing Docker symptoms as repo bugs | `scripts/demo/smoke.sh` fails before startup because the daemon itself is unavailable, not because the Compose graph is malformed. | advisory | Confirm `docker info` is healthy before spending time on Compose-level debugging. |
| Treating the dirty worktree as pause-flow output | This repo already has extensive unrelated modified/generated files. Reverting broadly is risky and unnecessary. | advisory | Stage and change only the specific files required for the resumed verification task. |

<current_state>
Phase 28 is in verification only. The implementation plans `28-01` through `28-04` are complete. A new UAT artifact exists at `.planning/phases/28-e2e-demo-environments-ga-release/28-UAT.md` and marks the phase as partial with 3 passed checks, 1 blocked check, and 1 major repo issue.

Passed evidence:
- `docker compose config`
- `cd examples/demo/backend && mix test test/rulestead_demo_web/controllers/flag_controller_test.exs test/rulestead_demo_web/controllers/flag_stream_controller_test.exs test/rulestead_demo/demo_seed_smoke_test.exs`
- `cd examples/demo/frontend && npm test -- --runInBand tests/rulestead-web-provider.test.ts`
- `cd examples/demo/frontend && npm run build`

Outstanding:
- `scripts/demo/smoke.sh` still stops immediately because Docker Desktop is unable to start.
- `cd examples/demo/frontend && npm run test:e2e` fails because Playwright discovers the Jest unit file `tests/rulestead-web-provider.test.ts`.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Build Phase 28 UAT artifact - Done
- Task 2: Re-run blocked verification commands to separate external vs repo blockers - In progress, with both failure modes confirmed and documented
</completed_work>

<remaining_work>

- Fix Playwright discovery so `npm run test:e2e` only runs the browser proof in `examples/demo/frontend/tests/demo-toggle.spec.ts`
- Re-run `cd examples/demo/frontend && npm run test:e2e`
- After Docker Desktop is healthy, re-run `scripts/demo/smoke.sh`
- If both pass, update `28-UAT.md`, then update `.planning/ROADMAP.md` and `.planning/STATE.md` to close Phase 28 verification
</remaining_work>

<decisions_made>

- Kept Phase 28 UAT in `shift-left` mode because verification is primarily automation-backed, not manual exploratory testing.
- Recorded the Playwright issue as a diagnosed repo gap in `28-UAT.md` with root cause and missing work, so it is execution-ready for a follow-up fix.
- Did not attempt broad git cleanup because the worktree already contains many unrelated modified and generated files.
</decisions_made>

<blockers>
- Docker Desktop daemon unavailable: `scripts/demo/smoke.sh` fails with `Error response from daemon: Docker Desktop is unable to start`. No repo-side workaround.
- Playwright discovery misconfiguration: `npm run test:e2e` crashes on `ReferenceError: jest is not defined` because the Jest unit test file is inside Playwright's scan scope.
</blockers>

## Required Reading (in order)
1. [.planning/phases/28-e2e-demo-environments-ga-release/28-UAT.md](/Users/jon/projects/rulestead/.planning/phases/28-e2e-demo-environments-ga-release/28-UAT.md) — current verification truth, blockers, and diagnosed gap
1. [.planning/phases/28-e2e-demo-environments-ga-release/28-VALIDATION.md](/Users/jon/projects/rulestead/.planning/phases/28-e2e-demo-environments-ga-release/28-VALIDATION.md) — what Phase 28 must prove
1. [examples/demo/frontend/playwright.config.ts](/Users/jon/projects/rulestead/examples/demo/frontend/playwright.config.ts) — current source of the Playwright discovery bug
1. [examples/demo/frontend/package.json](/Users/jon/projects/rulestead/examples/demo/frontend/package.json) — `test:e2e` entrypoint
1. [examples/demo/frontend/tests/rulestead-web-provider.test.ts](/Users/jon/projects/rulestead/examples/demo/frontend/tests/rulestead-web-provider.test.ts) — Jest-only unit test currently being mis-executed by Playwright
1. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses; apply before any assumption analysis

## Critical Anti-Patterns (do NOT repeat these)
- PLAYWRIGHT-SCOPE-LEAK: assuming `npm run test:e2e` failure means the browser proof itself is broken -> first verify what files Playwright is collecting
- DOCKER-SYMPTOM-DEBUGGING: debugging Compose/build scripts before confirming the Docker daemon itself is healthy
- DIRTY-WORKTREE-CLEANUP: trying to normalize the entire repo state while only resuming Phase 28 verification

## Infrastructure State
- Docker daemon: unavailable at last check; `docker info` did not return a healthy result and `scripts/demo/smoke.sh` failed immediately on image lookup
- Background processes: none intentionally left running for this workflow
- Git worktree: heavily dirty with many unrelated modified and untracked files, including generated `_build`, `doc`, `.DS_Store`, and historical planning artifacts

## Pre-Execution Critique Required

_Not applicable. This pause is during verification, not between design and execution._

<context>
The key insight from the last verification pass is that Phase 28 has two independent blockers. One is external and environmental: Docker Desktop is still not starting, so the Compose smoke cannot run. The other is internal and actionable: Playwright is configured too broadly and is executing a Jest unit file. A resuming agent should fix the Playwright scope first because it can be completed without Docker and removes one real repo-side failure before the final smoke rerun.
</context>

<next_action>
Start with: update [examples/demo/frontend/playwright.config.ts](/Users/jon/projects/rulestead/examples/demo/frontend/playwright.config.ts) so Playwright only discovers browser specs, then rerun `cd examples/demo/frontend && npm run test:e2e`.
</next_action>
