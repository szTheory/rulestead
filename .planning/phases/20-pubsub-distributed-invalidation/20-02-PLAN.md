---
phase: 20-pubsub-distributed-invalidation
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - rulestead/lib/rulestead/runtime/refresh.ex
  - rulestead/lib/rulestead/runtime/supervisor.ex
  - rulestead/lib/rulestead/fake/control.ex
  - rulestead/lib/rulestead/runtime/cluster_case.ex
  - rulestead/test/rulestead/runtime/refresh_test.exs
  - rulestead/test/rulestead/runtime/cluster_refresh_test.exs
  - rulestead/test/rulestead/telemetry_test.exs
autonomous: true
requirements:
  - INV-01
  - INV-02
must_haves:
  truths:
    - "Nodes refresh immediately when they receive an invalidation notice for a newer snapshot version."
    - "Duplicate, stale, and out-of-order invalidation notices are ignored without regressing the currently served snapshot."
    - "If refresh after invalidation fails, the runtime keeps serving last-known-good ETS data and reports the failure distinctly."
    - "Operators and tests can distinguish invalidation-driven refreshes from ordinary polling refreshes through telemetry."
  artifacts:
    - path: "rulestead/lib/rulestead/runtime/refresh.ex"
      provides: "Notifier-driven invalidation handling with version gating and invalidation telemetry"
    - path: "rulestead/lib/rulestead/runtime/supervisor.ex"
      provides: "Per-environment runtime worker wiring for notifier and PubSub options"
    - path: "rulestead/test/rulestead/runtime/refresh_test.exs"
      provides: "Single-node invalidation, ignore, and stale-serving regression coverage"
    - path: "rulestead/test/rulestead/runtime/cluster_refresh_test.exs"
      provides: "Cross-node convergence coverage via the notifier seam"
    - path: "rulestead/test/rulestead/telemetry_test.exs"
      provides: "Telemetry contract tests for invalidation received/ignored/triggered/failure paths"
  key_links:
    - from: "rulestead/lib/rulestead/runtime/refresh.ex"
      to: "rulestead/lib/rulestead/runtime/notifier.ex"
      via: "refresh workers subscribe through the notifier instead of direct `Phoenix.PubSub` calls"
      pattern: "subscribe|snapshot_version|invalidation"
    - from: "rulestead/lib/rulestead/runtime/refresh.ex"
      to: "rulestead/lib/rulestead/telemetry.ex"
      via: "invalidation telemetry remains distinct from generic polling refresh events"
      pattern: "invalidation_received|invalidation_ignored|refresh_failed_after_invalidation"
---

<objective>
Wire the runtime to consume invalidation notices immediately, preserve stale-serving semantics, and lock the invalidation telemetry contract.

Purpose: After Plan 01 introduces the notifier seam and authoritative publish events, the runtime still needs to subscribe through that seam, gate refreshes by version, and emit invalidation-specific telemetry that operators can distinguish from ordinary polling refreshes.
Output: Refresh/supervisor updates plus runtime, cluster, and telemetry coverage for invalidation-driven convergence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v0.5.0-ROADMAP.md
@.planning/milestones/v0.5.0-REQUIREMENTS.md
@.planning/phases/20-pubsub-distributed-invalidation/20-CONTEXT.md
@.planning/phases/20-pubsub-distributed-invalidation/20-01-PLAN.md
@prompts/rulestead-host-app-integration-seam.md
@prompts/rulestead-testing-and-e2e-strategy.md
@prompts/rulestead-telemetry-observability-and-audit.md
@rulestead/lib/rulestead/runtime/refresh.ex
@rulestead/lib/rulestead/runtime/supervisor.ex
@rulestead/test/rulestead/runtime/refresh_test.exs
@rulestead/test/rulestead/runtime/cluster_refresh_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move runtime refresh workers onto the notifier seam and preserve monotonic stale-serving behavior</name>
  <files>rulestead/lib/rulestead/runtime/refresh.ex, rulestead/lib/rulestead/runtime/supervisor.ex, rulestead/lib/rulestead/fake/control.ex, rulestead/lib/rulestead/runtime/cluster_case.ex, rulestead/test/rulestead/runtime/refresh_test.exs, rulestead/test/rulestead/runtime/cluster_refresh_test.exs</files>
  <read_first>
    - rulestead/lib/rulestead/runtime/refresh.ex
    - rulestead/lib/rulestead/runtime/supervisor.ex
    - rulestead/lib/rulestead/runtime/cache.ex
    - rulestead/lib/rulestead/runtime/config.ex
    - rulestead/lib/rulestead/runtime/notifier.ex
    - rulestead/lib/rulestead/runtime/notifier/phoenix_pub_sub.ex
    - rulestead/lib/rulestead/fake/control.ex
    - rulestead/lib/rulestead/runtime/cluster_case.ex
    - .planning/phases/20-pubsub-distributed-invalidation/20-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rulestead/lib/rulestead/runtime/refresh.ex` subscribes through the Phase 20 notifier seam instead of calling `Phoenix.PubSub.subscribe(` directly.
    - The refresh worker emits a fast-path branch that ignores invalidation notices whose `snapshot_version` is missing, equal to, or lower than the current ETS version.
    - The refresh worker does not delete ETS entries on invalidation receipt; stale data remains available until `Cache.apply/2` succeeds with a newer snapshot.
    - `rulestead/test/rulestead/runtime/refresh_test.exs` covers newer, duplicate, and out-of-order invalidation notices plus a failed refresh after invalidation.
    - `rulestead/test/rulestead/runtime/cluster_refresh_test.exs` proves both nodes converge on the newer version after one invalidation notice through the notifier seam.
    - `cd rulestead && mix test test/rulestead/runtime/refresh_test.exs test/rulestead/runtime/cluster_refresh_test.exs` exits 0.
  </acceptance_criteria>
  <behavior>
    - Test 1: a newer invalidation notice triggers immediate refresh and applies the new snapshot version.
    - Test 2: duplicate or stale notices are ignored without touching ETS or incrementing backoff.
    - Test 3: a refresh failure after invalidation marks stale/degraded state and keeps the prior snapshot serving.
  </behavior>
  <action>Update `Rulestead.Runtime.Refresh` so `init/1` subscribes through the configured notifier when both `:notifier` and `:pubsub` are present, leaving polling/backoff unchanged when PubSub is absent. Keep the runtime message contract keyed on `environment_key` and `snapshot_version`, and gate the invalidation path on `incoming_version > current_version`. When a newer notice arrives, emit invalidation telemetry, trigger the existing refresh flow, and preserve last-known-good ETS data if refresh fails. Update `Rulestead.Runtime.Supervisor`, `Rulestead.Fake.Control`, and `Rulestead.Runtime.ClusterCase` so all helper/runtime entry points pass the notifier and explicit PubSub server consistently. Extend the runtime and cluster test suites to cover same-version duplicates, out-of-order notices, and convergence through the notifier seam rather than direct ad hoc PubSub payload construction.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/runtime/refresh_test.exs test/rulestead/runtime/cluster_refresh_test.exs</automated>
  </verify>
  <done>Runtime workers wake immediately on newer invalidation notices, ignore stale ones safely, and keep stale-serving semantics intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock invalidation telemetry and explicit host scaffolding</name>
  <files>rulestead/test/rulestead/telemetry_test.exs</files>
  <read_first>
    - rulestead/test/rulestead/telemetry_test.exs
    - rulestead/lib/rulestead/runtime/refresh.ex
    - rulestead/lib/rulestead/runtime/cache.ex
    - rulestead/lib/rulestead/telemetry.ex
    - prompts/rulestead-telemetry-observability-and-audit.md
    - .planning/phases/20-pubsub-distributed-invalidation/20-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rulestead/test/rulestead/telemetry_test.exs` asserts invalidation-specific events that distinguish `invalidation_received`, `invalidation_ignored`, `refresh_triggered_from_invalidation`, and `refresh_failed_after_invalidation`.
    - The telemetry assertions continue to treat invalidation notices as metadata-only events and do not require full snapshot payloads in telemetry metadata.
    - `cd rulestead && mix test test/rulestead/telemetry_test.exs` exits 0.
  </acceptance_criteria>
  <behavior>
    - Test 1: telemetry consumers can distinguish invalidation receipt, ignore, refresh-trigger, and failed-refresh outcomes from ordinary polling refreshes.
    - Test 2: telemetry metadata on invalidation paths remains bounded to environment, snapshot version, outcome reason, and refresh status fields.
  </behavior>
  <action>Extend the telemetry contract tests so they capture and assert the Phase 20 invalidation event family separately from `[:rulestead, :runtime, :cache, :refresh]`, using exact reason or event names that distinguish received, ignored, refresh-triggered, and failed-after-invalidation outcomes. Verify that invalidation paths only emit bounded metadata such as `environment`, `snapshot_version`, `reason`, and `refresh_status`, never full snapshot payloads. Keep these telemetry assertions tied to the runtime refresh behaviors added in Task 1 so operators can audit why a node refreshed, ignored a notice, or stayed stale after a failed refresh.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/telemetry_test.exs</automated>
  </verify>
  <done>The runtime and telemetry surface both express immediate invalidation-driven convergence without compromising stale-serving safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notifier event bus -> runtime refresh workers | Best-effort distributed wake-up signals cross into local cache refresh logic here |
| generated host config -> production runtime wiring | Installer scaffolding influences how hosts attach privileged infrastructure names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-01 | D | invalidation burst handling | mitigate | Ignore stale/duplicate versions and keep the existing serialized refresh worker so bursts coalesce into version-gated refreshes. |
| T-20-02-02 | T | ETS cache state | mitigate | Never pre-evict on invalidation receipt; apply only newer compiled snapshots through `Cache.apply/2`. |
| T-20-02-03 | R | telemetry ambiguity | mitigate | Emit invalidation-specific telemetry events distinct from polling refresh telemetry so operators can audit cause and outcome. |
| T-20-02-04 | S | host config wiring | mitigate | Scaffold explicit PubSub configuration in generated host config and avoid hidden runtime detection of Phoenix infrastructure. |
</threat_model>

<verification>
Run the runtime, cluster, and telemetry test files. Confirm the runtime still reconciles by polling when PubSub is absent and still serves stale last-known-good data when refresh after invalidation fails.
</verification>

<success_criteria>
Phase 20 runtime nodes converge immediately when invalidation is available, fall back safely when it is not, and expose enough telemetry to explain whether a node refreshed, ignored a notice, or stayed stale after a failed refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/20-pubsub-distributed-invalidation/20-02-SUMMARY.md`
</output>
