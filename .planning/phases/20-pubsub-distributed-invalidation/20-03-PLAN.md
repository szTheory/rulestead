---
phase: 20-pubsub-distributed-invalidation
plan: 03
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - rulestead/lib/rulestead/install/config_writer.ex
  - rulestead/test/fixtures/install_golden/tree/config/rulestead.exs
  - rulestead/test/rulestead/mix/tasks/rulestead_install_test.exs
  - rulestead/test/rulestead/integration/install_golden_test.exs
autonomous: true
requirements:
  - INV-02
must_haves:
  truths:
    - "Generated host config shows how Phoenix hosts wire runtime invalidation explicitly through their own PubSub server."
    - "The installer keeps Rulestead transport-optional: hosts may delete or override the PubSub wiring and still rely on polling."
    - "The scaffolded runtime config is produced by installer code, not by manual golden-file drift."
  artifacts:
    - path: "rulestead/lib/rulestead/install/config_writer.ex"
      provides: "Installer-generated runtime config that includes explicit Phase 20 PubSub wiring"
    - path: "rulestead/test/rulestead/mix/tasks/rulestead_install_test.exs"
      provides: "Direct installer assertions for the generated runtime config contract"
    - path: "rulestead/test/rulestead/integration/install_golden_test.exs"
      provides: "Golden snapshot coverage for the updated scaffold output"
  key_links:
    - from: "rulestead/lib/rulestead/install/config_writer.ex"
      to: "rulestead/lib/rulestead/config.ex"
      via: "installer renders the validated host runtime defaults with explicit pubsub wiring"
      pattern: "runtime|pubsub|notifier"
    - from: "rulestead/test/fixtures/install_golden/tree/config/rulestead.exs"
      to: "rulestead/lib/rulestead/install/config_writer.ex"
      via: "golden fixture must match real generator output"
      pattern: "HostApp.PubSub|runtime"
---

<objective>
Update the installer scaffold so explicit PubSub wiring is generated from code and locked by direct installer tests.

Purpose: Phase 20’s host-owned transport posture is not complete until the installer shows Phoenix hosts how to pass their own PubSub server into the runtime config. This needs to come from the actual generator path, not only from fixture edits.
Output: A real installer change, direct installer tests, and refreshed golden output for the generated `config/rulestead.exs`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v0.5.0-ROADMAP.md
@.planning/milestones/v0.5.0-REQUIREMENTS.md
@.planning/phases/20-pubsub-distributed-invalidation/20-CONTEXT.md
@.planning/phases/20-pubsub-distributed-invalidation/20-01-PLAN.md
@prompts/rulestead-host-app-integration-seam.md
@rulestead/lib/rulestead/install/config_writer.ex
@rulestead/test/fixtures/install_golden/tree/config/config.exs
@rulestead/test/fixtures/install_golden/tree/config/rulestead.exs
@rulestead/test/rulestead/mix/tasks/rulestead_install_test.exs
@rulestead/test/rulestead/integration/install_golden_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate explicit runtime PubSub wiring from the installer path</name>
  <files>rulestead/lib/rulestead/install/config_writer.ex, rulestead/test/fixtures/install_golden/tree/config/rulestead.exs, rulestead/test/rulestead/mix/tasks/rulestead_install_test.exs, rulestead/test/rulestead/integration/install_golden_test.exs</files>
  <read_first>
    - rulestead/lib/rulestead/install/config_writer.ex
    - rulestead/lib/rulestead/config.ex
    - rulestead/test/fixtures/install_golden/tree/config/config.exs
    - rulestead/test/fixtures/install_golden/tree/config/rulestead.exs
    - rulestead/test/rulestead/mix/tasks/rulestead_install_test.exs
    - rulestead/test/rulestead/integration/install_golden_test.exs
    - prompts/rulestead-host-app-integration-seam.md
    - .planning/phases/20-pubsub-distributed-invalidation/20-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rulestead/lib/rulestead/install/config_writer.ex` renders a `runtime:` block that includes explicit PubSub wiring for the generated host app config.
    - `rulestead/test/rulestead/mix/tasks/rulestead_install_test.exs` asserts the generated config includes the Phase 20 runtime PubSub shape before golden-file normalization.
    - `rulestead/test/fixtures/install_golden/tree/config/rulestead.exs` matches the real installer output and is not edited independently of the generator contract.
    - `cd rulestead && mix test test/rulestead/mix/tasks/rulestead_install_test.exs test/rulestead/integration/install_golden_test.exs` exits 0.
  </acceptance_criteria>
  <behavior>
    - Test 1: installer output includes explicit runtime PubSub wiring for Phoenix hosts.
    - Test 2: rerunning the installer remains idempotent after the new runtime config is generated.
  </behavior>
  <action>Update `Rulestead.Install.ConfigWriter` so the generated `config/rulestead.exs` runtime block includes explicit wiring for the host PubSub server and any Phase 20 runtime notifier defaults required by `Rulestead.Config`. Refresh the install golden fixture only after the generator output changes, and add/update direct assertions in `test/rulestead/mix/tasks/rulestead_install_test.exs` so the installer contract fails fast if the generated runtime config drifts. Preserve the linked-version monorepo shape and do not add any scaffolding that implies `rulestead_admin` owns transport infrastructure or that PubSub is mandatory for non-Phoenix hosts.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/mix/tasks/rulestead_install_test.exs test/rulestead/integration/install_golden_test.exs</automated>
  </verify>
  <done>The installer produces the explicit Phase 20 host wiring contract and the golden tree proves the scaffold remains stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer defaults -> host production config | Generated config seeds privileged infrastructure names in host applications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | S | generated host wiring | mitigate | Generate explicit `pubsub` config from code rather than relying on hidden runtime detection or undocumented manual edits. |
| T-20-03-02 | T | scaffold drift | mitigate | Add direct installer tests plus the golden snapshot so generated runtime config cannot drift silently. |
| T-20-03-03 | D | host fallback path | mitigate | Keep the generated config overrideable so hosts can disable PubSub wiring and fall back to polling when they do not run Phoenix.PubSub. |
</threat_model>

<verification>
Run the direct installer test and the install golden integration test. Confirm the generated config advertises explicit PubSub wiring while remaining idempotent on a second installer run.
</verification>

<success_criteria>
Phase 20’s host-facing contract is visible in generated code: Phoenix hosts see explicit PubSub wiring immediately, and the installer proves that this contract comes from real implementation code rather than from fixture-only edits.
</success_criteria>

<output>
After completion, create `.planning/phases/20-pubsub-distributed-invalidation/20-03-SUMMARY.md`
</output>
