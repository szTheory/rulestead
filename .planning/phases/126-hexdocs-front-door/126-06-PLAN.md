---
phase: 126-hexdocs-front-door
plan: 06
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - rulestead_admin/mix.exs
  - rulestead_admin/lib/rulestead_admin/router.ex
autonomous: true
requirements: [DOC-06]
must_haves:
  truths:
    - "Admin docs reach parity: same logo/favicon/assets + duplicated --main* head-tag (only og:image host differs)"
    - "Admin extras include flows/admin-ui.md + flows/explainability.md grouped as 'Operator Guides'"
    - "groups_for_modules exposes only RulesteadAdmin.Router under 'Public Admin Seam'"
    - "RulesteadAdmin.Router has a real @moduledoc leading with host-owns-auth + 3 contracted session keys"
    - "__using__/1 and live_session/3 are @doc false; no RulesteadAdmin.Live.* is autolinked"
    - "RULESTEAD_ADMIN_HEX_RELEASE=1 mix docs --warnings-as-errors green"
  artifacts:
    - path: "rulestead_admin/mix.exs"
      provides: "Admin docs parity config: logo/favicon/assets, --main* head-tag, Operator Guides extras, Public Admin Seam group"
      contains: "before_closing_head_tag"
    - path: "rulestead_admin/lib/rulestead_admin/router.ex"
      provides: "Real Router @moduledoc (host-owns-auth) + @doc false on internal macros"
      contains: "@moduledoc \""
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/router.ex @moduledoc"
      to: "guides/api_stability.md Required Host Session Keys"
      via: "documents the 3 contracted session keys verbatim"
      pattern: "current_actor"
    - from: "rulestead_admin/mix.exs files:"
      to: "rulestead_admin/brandbook (symlink, plan 01)"
      via: "plain-relative brandbook/assets/logo/*.svg glob"
      pattern: "brandbook/assets/logo/\\*.svg"
---

<objective>
Bring `rulestead_admin` docs to parity (DOC-06): expand the bare admin `docs:`/`files:` to mirror
core (logo/favicon/assets via its own committed symlink, a DUPLICATED `--main*` head-tag, admin
flow-guide extras, a single-module group), and replace the `RulesteadAdmin.Router`
`@moduledoc false` with a real host-owns-auth moduledoc (D-21/D-22/D-23).

Purpose: The admin package is a mounted companion; its HexDocs must teach safe mounting and lead
with the host-owns-auth contract that the exemplars (Oban Web, LiveDashboard) under-document — the
operator-trust differentiator. Depends on plan 01 (the `rulestead_admin/brandbook` symlink + PNG).
Output: rewritten `rulestead_admin/mix.exs` + a real Router moduledoc with autolink-safe internals.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-hexdocs-front-door/126-CONTEXT.md
@.planning/phases/126-hexdocs-front-door/126-RESEARCH.md
@rulestead_admin/mix.exs
@rulestead_admin/lib/rulestead_admin/router.ex
@guides/api_stability.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand admin mix.exs docs/files to parity (D-21)</name>
  <files>rulestead_admin/mix.exs</files>
  <read_first>
    - .planning/phases/126-hexdocs-front-door/126-CONTEXT.md (D-21; D-13 for the duplicated head-tag)
    - .planning/phases/126-hexdocs-front-door/126-RESEARCH.md (Pattern 1 + Pattern 2; "two mix.exs can't share code")
    - rulestead_admin/mix.exs (current bare docs L70-78, files L66)
    - rulestead/mix.exs (the core head-tag/asset wiring to mirror — written by plan 05)
  </read_first>
  <action>
    Per D-21, expand `rulestead_admin/mix.exs`:
    (a) `docs:` — add the SAME asset keys as core (plain-relative, via the committed
    `rulestead_admin/brandbook` symlink from plan 01):
      logo: "brandbook/assets/logo/rs-mark.svg", favicon: "brandbook/assets/logo/rs-favicon.svg",
      assets: %{"brandbook/assets/logo" => "assets"}
    (b) DUPLICATE `before_closing_head_tag/1` VERBATIM from core (the two mix.exs cannot share code)
    — copy the EXACT 0.40 `--main*` `:root` + `body.dark` re-tint and OG meta from D-13/plan 05.
    ONLY change `og:image`/`twitter:image` to
    `https://hexdocs.pm/rulestead_admin/assets/rs-social-card.png` (PNG, D-15). Define
    `before_closing_head_tag(:epub), do: ""`.
    (c) `package files:` — ADD `brandbook/assets/logo/*.svg` (plain-relative, NO `../`; D-09).
    (d) `extras:` — keep README.md + CHANGELOG.md and ADD `../guides/flows/admin-ui.md` and
    `../guides/flows/explainability.md` (both confirmed to exist, RESEARCH.md V3). Do NOT pull
    rollout/telemetry guides (those are core's — avoid a drift-prone duplicate surface).
    (e) `groups_for_extras: ["Operator Guides": ~r"guides/flows/"]`.
    (f) `groups_for_modules: ["Public Admin Seam": [RulesteadAdmin.Router]]` — Router ONLY (never
    list internal `RulesteadAdmin.Live.*`/`.Components.*`). Add a `skip_code_autolink_to` guard for
    stray `RulesteadAdmin.Live.` mentions (D-23 belt-and-suspenders). Do NOT use any `../` token in
    `files:` (D-09).
  </action>
  <verify>
    <automated>cd rulestead_admin && grep -q 'logo: "brandbook/assets/logo/rs-mark.svg"' mix.exs && grep -q 'before_closing_head_tag' mix.exs && grep -qE 'og:image.*rulestead_admin/assets/rs-social-card\.png' mix.exs && grep -q 'body.dark' mix.exs && grep -q 'brandbook/assets/logo/\*.svg' mix.exs && grep -q 'admin-ui.md' mix.exs && grep -q 'explainability.md' mix.exs && grep -q '"Public Admin Seam":' mix.exs && grep -q '"Operator Guides":' mix.exs && ! grep -q 'RulesteadAdmin.Live' mix.exs</automated>
  </verify>
  <done>Admin mix.exs mirrors core asset wiring + duplicated `--main*` head-tag (admin og:image host), adds the two flow extras under "Operator Guides", a single "Public Admin Seam" group, the `*.svg` files glob, and no `../` token / no `RulesteadAdmin.Live` listing.</done>
</task>

<task type="auto">
  <name>Task 2: Real RulesteadAdmin.Router @moduledoc + @doc false internals (D-22/D-23)</name>
  <files>rulestead_admin/lib/rulestead_admin/router.ex</files>
  <read_first>
    - .planning/phases/126-hexdocs-front-door/126-CONTEXT.md (D-22, D-23)
    - .planning/phases/126-hexdocs-front-door/126-RESEARCH.md (Code Examples — Admin Router @moduledoc; Pitfall 1 autolink gate)
    - guides/api_stability.md (Required Host Session Keys ~L463-468 — verbatim 3 keys)
    - rulestead_admin/lib/rulestead_admin/router.ex (L2 @moduledoc false; __using__/1 L4-8; live_session/3 L84+; Map.take L88-95)
  </read_first>
  <action>
    Per D-22, replace `@moduledoc false` (router.ex:2) with a real `@moduledoc` that LEADS with
    host-owns-auth and teaches mounting. Structure: 1-line intro -> a copy-paste
    `scope`/`pipe_through [:browser, :require_admin]`/`rulestead_admin "/flags", policy: ...` snippet
    -> "What you must provide" checklist (a `:browser` pipeline with session + CSRF; your own auth in
    FRONT of the scope — Rulestead does NOT authenticate; a required `policy:` implementing
    `Rulestead.Admin.Policy`; the session keys) -> "Options" (`:policy` required, `:mount_path`) ->
    "Session keys read from host": use the THREE CONTRACTED names VERBATIM from
    api_stability.md L463-468 — `"current_actor"`, `"rulestead_admin_environments"`,
    `"rulestead_admin_last_env"` — and document the tenant keys (`"rulestead_admin_tenants"`,
    `"rulestead_admin_last_tenant"`, `"rulestead_admin_default_tenant"`, which `Map.take` also reads)
    as OPTIONAL, not part of the frozen 1.x contract -> "Boundary" (internal Live/Components/DOM/CSS
    are NOT in the 1.x promise).
    Add a concise `@doc` on the `rulestead_admin/2` macro (not a moduledoc duplicate).
    Per D-23 (autolink `--warnings-as-errors` gate): backtick ONLY public symbols
    (`` `Rulestead.Admin.Policy` `` resolves cross-package — safe). NEVER backtick
    `RulesteadAdmin.Live.*`, `RulesteadAdmin.Components.*`, or `live_session/3` (hidden -> undefined
    reference fails the gate). Add `@doc false` to BOTH `__using__/1` (L4) AND `live_session/3` (L84)
    so they neither render nor autolink.
  </action>
  <verify>
    <automated>cd rulestead_admin && ! head -3 lib/rulestead_admin/router.ex | grep -q '@moduledoc false' && head -3 lib/rulestead_admin/router.ex | grep -qE '@moduledoc\s+"' && grep -q 'current_actor' lib/rulestead_admin/router.ex && grep -q 'rulestead_admin_last_env' lib/rulestead_admin/router.ex && grep -c '@doc false' lib/rulestead_admin/router.ex | grep -qE '[2-9]'</automated>
  </verify>
  <done>router.ex:2 is a real `@moduledoc` (host-owns-auth + 3 contracted session keys verbatim), `__using__/1` and `live_session/3` both carry `@doc false`, and no hidden symbol is backticked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app -> admin mount | the Router @moduledoc documents the auth contract; mis-stating it misleads operator auth setup |
| admin docs -> autolink gate | backticking a hidden symbol fails `mix docs --warnings-as-errors` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-09 | Spoofing / Elevation (downstream) | Router @moduledoc auth contract | mitigate | lead with host-owns-auth + "What you must provide"; 3 contracted session keys verbatim from api_stability.md (D-22) |
| T-126-10 | Tampering (CI gate failure) | autolink of hidden admin symbols | mitigate | `@doc false` on `__using__/1`+`live_session/3`; backtick only public symbols; `skip_code_autolink_to` guard (D-23) |
| T-126-08 | Information Disclosure (blank unfurl) | admin og:image type/host | mitigate | per-package PNG og:image `/rulestead_admin/assets/rs-social-card.png` (D-15/D-21) |
</threat_model>

<verification>
- `cd rulestead_admin && RULESTEAD_ADMIN_HEX_RELEASE=1 mix docs --warnings-as-errors` exits 0 (autolink gate — D-23 footguns).
- grep proves admin asset wiring, duplicated `--main*` head-tag with admin og:image host, "Operator
  Guides" + "Public Admin Seam", real Router moduledoc with the 3 session keys, two `@doc false`.
- `bash scripts/ci/check_package_whitelist.sh` stays green (no cross-package leakage).
- `release_contract_test.exs` (admin) stays green.
- Manual (UI hint yes): admin `mix docs` — logo/favicon resolve, mineral tint light+dark.
</verification>

<success_criteria>
`rulestead_admin` docs reach parity (logo/favicon/theming + flow guides + single seam group) and
`RulesteadAdmin.Router` renders a real host-owns-auth moduledoc, with
`RULESTEAD_ADMIN_HEX_RELEASE=1 mix docs --warnings-as-errors` green.
</success_criteria>

<output>
Create `.planning/phases/126-hexdocs-front-door/126-06-SUMMARY.md` when done
</output>
