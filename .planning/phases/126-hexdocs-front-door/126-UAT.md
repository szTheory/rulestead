---
status: partial
phase: 126-hexdocs-front-door
source: [126-VERIFICATION.md]
started: 2026-06-18T10:00:00Z
updated: 2026-06-18T11:05:00Z
---

## Current Test

[automated — no human UAT remains. Checks 2 & 3 pass now via the lint gate;
checks 1 & 4 are owned by the verify-published-release cron and gate on the next
publish (rulestead@1.0.0).]

## Tests

### 1. Admin Hex-release-mode docs gate
expected: Running `RULESTEAD_ADMIN_HEX_RELEASE=1 mix docs --warnings-as-errors` from `rulestead_admin/` exits 0 with no undefined-reference warnings. Provable only after rulestead@1.0.0 is published; dev-mode already verified green.
result: blocked
blocked_by: release-build
reason: "Automated by scripts/ci/check_docs_published.sh, wired into scripts/ci/verify_published_release.sh (verify-published-release cron). No human runs it. Currently RED against the stale published core (rulestead 0.1.7 hides Rulestead.Admin.Policy); the branch documents Policy, so the gate flips green once this work publishes as 1.0.0. Verified locally 2026-06-18 that the gate is real (exits 1 today for the expected hidden-module reason)."

### 2. Visual render — core docs
expected: Open `rulestead/doc/index.html` in a browser and toggle light/dark. The `rs-mark.svg` logo loads (no 404), favicon resolves, sidebar/links/focus ring are tinted Stead Blue (#3A6F8F family), dark mode applies via the `body.dark` selector.
result: pass
note: "Automated by scripts/ci/check_docs_render.sh (hard gate in scripts/ci/lint.sh). Static site: referenced assets all resolve on disk (== no 404), injected style carries --main: hsl(203,42%,39%) + --searchBarFocusColor:#3A6F8F and a body.dark{} block. Ran green locally 2026-06-18."

### 3. Visual render — admin docs
expected: Open `rulestead_admin/doc/index.html` in a browser and toggle light/dark. Logo/favicon resolve, Stead Blue tint and dark mode match core (parity), og:image points to the rulestead_admin host.
result: pass
note: "Automated by scripts/ci/check_docs_render.sh: same per-package assertions as core, plus og:image advertises the hexdocs.pm/rulestead_admin host and a byte-identical-palette parity diff against core (674 bytes, identical). Ran green locally 2026-06-18."

### 4. OG / social-card unfurl
expected: Paste a HexDocs page URL (or a GitHub link containing the README) into a social-card unfurler (e.g. cards-dev.twitter.com/validator or opengraph.xyz). `og:image` resolves to the 1200x630 `rs-social-card.png` and renders — not an empty/broken unfurl.
result: blocked
blocked_by: release-build
reason: "Automated by scripts/ci/check_docs_published.sh (verify-published-release cron): fetches the published readme.html, confirms the advertised og:image URL resolves 200 as image/png at 1200x630, and that logo/favicon resolve — the deterministic advertise<->resolve proxy for an unfurl. Gates on the next publish since it reads the live CDN. The on-disk 1200x630 raster + correct og:image host are already asserted pre-publish by check_docs_render.sh."

## Summary

total: 4
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 2

## Gaps

[none — no code gaps. The 2 blocked checks are not defects; they are release-gated
assertions now owned by CI (zero human UAT). They turn green automatically on the
next publish via the verify-published-release workflow.]
