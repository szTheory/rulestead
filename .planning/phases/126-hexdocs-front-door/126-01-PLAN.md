---
phase: 126-hexdocs-front-door
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/brandbook
  - rulestead_admin/brandbook
  - brandbook/assets/logo/rs-social-card.png
  - scripts/ci/check_logo_bytes.sh
  - scripts/ci/contributor.sh
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "Each package has a committed `brandbook` symlink resolving to ../brandbook"
    - "rs-social-card.png exists at 1200x630 (PNG bytes, not SVG)"
    - "CI fails the build if the logo SVG is missing/dangling in the hex.build tarball"
  artifacts:
    - path: "rulestead/brandbook"
      provides: "Symlink so plain-relative brandbook/* globs resolve in core package"
      contains: "symlink to ../brandbook"
    - path: "rulestead_admin/brandbook"
      provides: "Symlink so plain-relative brandbook/* globs resolve in admin package"
      contains: "symlink to ../brandbook"
    - path: "brandbook/assets/logo/rs-social-card.png"
      provides: "Rasterized 1200x630 OG/social card for og:image + GitHub social-preview"
    - path: "scripts/ci/check_logo_bytes.sh"
      provides: "D-10 tarball-content assertion: real SVG bytes in the built tarball"
  key_links:
    - from: "scripts/ci/check_logo_bytes.sh"
      to: "rulestead/mix.exs files: glob"
      via: "mix hex.build tarball extraction of brandbook/assets/logo/rs-mark.svg"
      pattern: "viewBox=\"0 0 62 62\""
---

<objective>
Wave-0 prerequisites for the HexDocs front door (DOC-02). Land the three artifacts every
later plan depends on BEFORE any mix.exs manifest touches the package shape:
1. committed `brandbook` symlinks in both packages (D-09) so plain-relative
   `brandbook/assets/logo/*.svg` globs resolve into the single source-of-truth `brandbook/`,
2. the rasterized `rs-social-card.png` (D-19) the OG meta + GitHub social-preview slot need,
3. the D-10 CI tarball-content assertion that fails the build on a missing/dangling symlink
   (critical: merge-to-main auto-publishes to hex.pm with NO human gate).

Purpose: De-risk the launch-day logo 404 by construction — the symlink/glob approach is
empirically proven (a real `mix hex.build` showed `../` tokens leak absolute tarball paths),
and the CI gate catches a non-materialized symlink before it reaches production.
Output: 2 committed symlinks, 1 PNG, 1 CI script wired into the existing CI surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-hexdocs-front-door/126-CONTEXT.md
@.planning/phases/126-hexdocs-front-door/126-RESEARCH.md
@scripts/ci/check_package_whitelist.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create committed brandbook symlinks in both packages (D-09)</name>
  <files>rulestead/brandbook, rulestead_admin/brandbook</files>
  <read_first>
    - .planning/phases/126-hexdocs-front-door/126-CONTEXT.md (D-09)
    - .planning/phases/126-hexdocs-front-door/126-RESEARCH.md (V2 — symlink target state)
  </read_first>
  <action>
    Per D-09, create one relative symlink per package pointing at the single source-of-truth
    brandbook directory: run `ln -sfn ../brandbook rulestead/brandbook` and
    `ln -sfn ../brandbook rulestead_admin/brandbook` from the repo root, then
    `git add rulestead/brandbook rulestead_admin/brandbook` (git stores them AS symlinks —
    RESEARCH.md V2 verified `brandbook/` is NOT gitignored). Do NOT copy the directory; do NOT
    use `../` path tokens inside any mix.exs `files:` (D-09 proved that leaks an absolute tarball
    path and 404s the logo). The symlink is what lets later plans use plain-relative
    `brandbook/assets/logo/*.svg` globs. Note the D-11 fallback (aliases copy-on-build) only if a
    runner reports `core.symlinks=false` — not needed for this plan; the D-10 gate (Task 3) catches it.
  </action>
  <verify>
    <automated>test -L rulestead/brandbook && test -L rulestead_admin/brandbook && test -f rulestead/brandbook/assets/logo/rs-mark.svg && test -f rulestead_admin/brandbook/assets/logo/rs-mark.svg && git ls-files --error-unmatch rulestead/brandbook rulestead_admin/brandbook</automated>
  </verify>
  <done>Both `brandbook` entries are symlinks (`test -L` true), resolve to a real `rs-mark.svg`, and are tracked by git as symlinks.</done>
</task>

<task type="auto">
  <name>Task 2: Rasterize rs-social-card.png at 1200x630 (D-19)</name>
  <files>brandbook/assets/logo/rs-social-card.png</files>
  <read_first>
    - .planning/phases/126-hexdocs-front-door/126-RESEARCH.md (V5 — rasterization simplification)
    - brandbook/assets/logo/rs-social-card.svg
  </read_first>
  <action>
    Per D-19 (and RESEARCH.md V5 simplification): `brandbook/assets/logo/rs-social-card.svg`
    already has `viewBox="0 0 1200 630"` and is pure `<path>` (0 `<text>` elements verified), so
    rasterize it directly with `npx @resvg/resvg-js brandbook/assets/logo/rs-social-card.svg --width 1200 --height 630 -o brandbook/assets/logo/rs-social-card.png`
    (or the resvg-js JS API). No HTML wrapper, no headless Chrome, no font-fetch, no text-flatten —
    those are unnecessary because the card is already path-flattened. If `@resvg/resvg-js` is
    unavailable on the host, fall back to the documented headless-Chrome HTML->PNG path
    (`--headless=new --screenshot --window-size=1200,630`, fonts via `curl` NOT urllib — per
    MEMORY brandbook-visual-rendering). Output PNG goes to `brandbook/assets/logo/` (it will ship
    via the core `assets:` map added in plan 05) and is referenced by og:image (D-15). `git add` it.
  </action>
  <verify>
    <automated>test -f brandbook/assets/logo/rs-social-card.png && file brandbook/assets/logo/rs-social-card.png | grep -q 'PNG image data, 1200 x 630'</automated>
  </verify>
  <done>`rs-social-card.png` exists, `file` reports `PNG image data, 1200 x 630`, and it is git-tracked.</done>
</task>

<task type="auto">
  <name>Task 3: Add D-10 tarball logo-bytes CI assertion and wire into CI (D-10)</name>
  <files>scripts/ci/check_logo_bytes.sh, scripts/ci/contributor.sh</files>
  <read_first>
    - scripts/ci/check_package_whitelist.sh (the tarball-inspection pattern to mirror)
    - .planning/phases/126-hexdocs-front-door/126-RESEARCH.md (D-10 code example, ~L450-457)
    - scripts/ci/contributor.sh (where to invoke the new gate)
  </read_first>
  <action>
    Per D-10, add a NEW script `scripts/ci/check_logo_bytes.sh` (sibling to
    `check_package_whitelist.sh`, `set -euo pipefail`, same `RULESTEAD_REPO` root resolution and
    `mix hex.build` + `tar xOf ... contents.tar.gz | tar tzf -` pattern). It must, for the core
    package: run `mix hex.build`, extract `brandbook/assets/logo/rs-mark.svg` from the tarball
    (`tar xOf <tarball> contents.tar.gz | tar xzO brandbook/assets/logo/rs-mark.svg`), and assert
    the extracted bytes contain `viewBox="0 0 62 62"` — failing with a clear "logo SVG
    missing/empty in tarball — broken symlink?" message and non-zero exit otherwise. This proves
    REAL SVG bytes shipped, not a 13-byte dangling-symlink text file (the D-11 `core.symlinks=false`
    failure mode). Then wire the script into the existing CI surface by adding a call to it in
    `scripts/ci/contributor.sh` after the existing whitelist check (do NOT inline the assertion into
    `check_package_whitelist.sh` — keep it a separate auditable script per CLAUDE.md scripts-first
    posture). NOTE: this gate will only pass once plan 05 adds the `brandbook/assets/logo/*.svg`
    glob to core `files:`; until then it is expected to fail — that is correct (the gate proves the
    manifest is right). Document this ordering in the script header comment.
    Do NOT echo the literal grep-target string in any explanatory comment that would itself satisfy
    the grep — keep the assertion target only in the live `grep` invocation.
  </action>
  <verify>
    <automated>test -x scripts/ci/check_logo_bytes.sh && bash -n scripts/ci/check_logo_bytes.sh && grep -q 'check_logo_bytes.sh' scripts/ci/contributor.sh</automated>
  </verify>
  <done>`check_logo_bytes.sh` exists, is executable, passes `bash -n` syntax check, asserts real logo bytes from a `mix hex.build` tarball, and is invoked from `contributor.sh`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> hex.pm tarball | `mix hex.build` packages files; a broken symlink or wrong glob ships unintended/missing bytes |
| repo -> npm (npx) | `npx @resvg/resvg-js` is a build-time-only tool, not a declared/shipped dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Tampering | brandbook symlink materialization on CI runner | mitigate | D-10 `check_logo_bytes.sh` asserts real `viewBox="0 0 62 62"` bytes from the built tarball; D-11 aliases-copy fallback documented |
| T-126-02 | Information Disclosure | `files:` glob over brandbook | mitigate | plan 05 uses narrow `*.svg` glob (excludes `.DS_Store`/`svgo.config.mjs`/`concepts/`); `check_package_whitelist.sh` guards cross-package leakage |
| T-126-SC | Tampering | npx `@resvg/resvg-js` build-time rasterizer | accept | not a project/shipped dep, not in either tarball; headless-Chrome fallback exists; RESEARCH.md Package Legitimacy Audit marks [ASSUMED — established rasterizer], no `npm install` added to manifests |
</threat_model>

<verification>
- `test -L rulestead/brandbook && test -L rulestead_admin/brandbook` (symlinks exist + tracked).
- `file brandbook/assets/logo/rs-social-card.png` reports `PNG image data, 1200 x 630`.
- `bash -n scripts/ci/check_logo_bytes.sh` clean; invoked from `contributor.sh`.
- Note: `check_logo_bytes.sh` passing end-to-end is gated on plan 05 (core `files:` glob) — expected red until then.
</verification>

<success_criteria>
Both `brandbook` symlinks committed and resolving; `rs-social-card.png` at 1200x630; D-10 CI
assertion script present, syntactically valid, and wired into `contributor.sh`.
</success_criteria>

<output>
Create `.planning/phases/126-hexdocs-front-door/126-01-SUMMARY.md` when done
</output>
