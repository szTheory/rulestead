---
phase: 126-hexdocs-front-door
plan: 05
type: execute
wave: 2
depends_on: ["126-01", "126-02", "126-04"]
files_modified:
  - rulestead/mix.exs
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-05]
must_haves:
  truths:
    - "D-01: Core docs ships exactly 6 module groups (Core API, Runtime, Testing, Behaviours & Seams, Store Adapters, Telemetry & Config)"
    - "The dangling Rulestead.Rule reference is gone; Rulestead.Tenancy/Ruleset/Flag are not grouped"
    - "groups_for_extras lists 'API & Stability' FIRST with an explicit file list; why-rulestead is the first Introduction extra"
    - "logo/favicon/assets resolve from brandbook/ via plain-relative paths (no ../ tokens) and ship in the tarball"
    - "before_closing_head_tag re-tints --main* (light + body.dark) and sets PNG og:image, with no <script> and no stylesheet file"
    - "mix docs --warnings-as-errors green; D-10 logo-bytes tarball assertion green"
  artifacts:
    - path: "rulestead/mix.exs"
      provides: "Full ExDoc front-door config: 6 module groups, funnel extras, asset wiring, --main* theming"
      contains: "before_closing_head_tag"
  key_links:
    - from: "rulestead/mix.exs files:"
      to: "rulestead/brandbook (symlink, plan 01)"
      via: "plain-relative glob brandbook/assets/logo/*.svg resolves through the committed symlink"
      pattern: "brandbook/assets/logo/\\*.svg"
    - from: "rulestead/mix.exs before_closing_head_tag"
      to: "deps/ex_doc 0.40.3 --main* CSS vars"
      via: "inline <style> re-tint cascades to links/sidebar/search/focus ring"
      pattern: "--main:"
---

<objective>
Rewrite `rulestead/mix.exs` `docs/0` and `package files:` into the full 1.0 HexDocs front door
(DOC-01/02/03/05): 6 module groups (D-01..D-05), funnel-ordered extras with the explicit
"API & Stability" first-match defuse (D-06/D-07), logo/favicon/assets wiring via the committed
symlink with plain-relative paths (D-09), and the ExDoc 0.40 `--main*` re-tint + PNG OG meta
head-tag (D-13..D-16).

Purpose: This is the central wiring plan — it turns the prerequisites (symlinks, PNG, un-hidden
modules) into a branded, grouped, funnel-ordered HexDocs surface and a tarball that ships the logo.
Depends on plan 01 (symlinks + PNG + D-10 gate), plan 02 (why-rulestead.md exists to be extra'd),
plan 04 (moduledoc flips so groups are non-empty).
Output: rewritten `rulestead/mix.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-hexdocs-front-door/126-CONTEXT.md
@.planning/phases/126-hexdocs-front-door/126-RESEARCH.md
@rulestead/mix.exs
@guides/api_stability.md
@brandbook/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set 6 module groups + funnel extras + asset files wiring (D-01..D-09)</name>
  <files>rulestead/mix.exs</files>
  <read_first>
    - .planning/phases/126-hexdocs-front-door/126-CONTEXT.md (D-01 through D-12)
    - .planning/phases/126-hexdocs-front-door/126-RESEARCH.md (Pattern 2; V3 guide inventory; V4 module table)
    - rulestead/mix.exs (current docs L86-159, package files L81-82)
    - guides/api_stability.md (stable module list — drives group membership)
  </read_first>
  <action>
    Edit `rulestead/mix.exs`:

    (a) `groups_for_modules` (D-01..D-05) — replace the current 4-group block with EXACTLY 6 groups:
      "Core API": [Rulestead, Rulestead.Context, Rulestead.Result, Rulestead.Error]
      "Runtime (cached lookup)": [Rulestead.Runtime]
      "Testing": [Rulestead.TestHelpers]
      "Behaviours & Seams": [Rulestead.Store, Rulestead.Admin.Policy]
      "Store Adapters": [Rulestead.Store.Ecto, Rulestead.Store.Redis]
      "Telemetry & Config": [Rulestead.Telemetry, Rulestead.Config]
    DELETE the dangling `Rulestead.Rule` (mix.exs:126 — module does not exist, the real one is
    Rulestead.Ruleset.Rule; D-04). DROP `Rulestead.Ruleset` and `Rulestead.Flag` (exist but not in
    the frozen stable list; D-04). Keep `Rulestead.Tenancy` OUT of all groups (only the config KEY
    is public; D-05). NOTE for the verifier: ROADMAP criterion-2 literally says "5" groups; D-02 is
    a user-confirmed 6 (the "Testing" group renders the contracted TestHelpers facade). Treat
    criterion-2 as satisfied-with-correction — do NOT flag the count.

    (b) `groups_for_extras` (D-06) — use an EXPLICIT ORDERED file list for "API & Stability" placed
    FIRST in the keyword list to defuse the first-match footgun (a broad
    `~r"guides/introduction/"` would otherwise swallow `upgrading.md`):
      "API & Stability": ["../guides/api_stability.md", "../guides/introduction/upgrading.md", "../guides/cheatsheet.cheatmd"]
      "Introduction": ~r"guides/introduction/"
      "Concepts & Guides": ~r"guides/flows/"
      "Recipes": ~r"guides/recipes/"
      "Contributing": ~r"CONVENTIONS"

    (c) `extras:` (D-07) — reorder in onboarding-funnel order with why-rulestead.md as the FIRST
    Introduction extra: README -> ../guides/introduction/why-rulestead.md -> installation ->
    getting-started -> phoenix-integration-spine -> domain_language -> product-boundary ->
    (remaining introduction extras) -> flows/* -> recipes/* (cookbook early, troubleshooting/footguns
    late) -> ../guides/api_stability.md -> ../guides/introduction/upgrading.md ->
    ../guides/cheatsheet.cheatmd -> ../CONVENTIONS.md. Keep all currently-listed extras; ADD the new
    why-rulestead.md. Per D-08, LEAVE `guides/recipes/telemetry.md` OUT (only flows/telemetry.md
    stays) — avoid a duplicate telemetry surface. Keep root extras as `../guides/...` (D-12: they
    render off-disk via `mix hex.publish docs`, so no `../guides` in `files:`).

    (d) `package files:` (D-09) — ADD plain-relative, NO-`../` narrow globs:
    `brandbook/assets/logo/*.svg brandbook/assets/specimens/readme-header.svg`. The `*.svg` glob is
    non-recursive so it excludes `.DS_Store`, `svgo.config.mjs`, and `concepts/`. These resolve
    through the committed `rulestead/brandbook` symlink (plan 01). Do NOT add `../guides` to `files:`
    (D-12). Do NOT use any `../` token in `files:` (D-09 — leaks an absolute tarball path -> 404).

    (e) `docs:` asset keys (D-09) — add plain-relative:
      logo: "brandbook/assets/logo/rs-mark.svg"        (square viewBox 0 0 62 62 — correct 48px slot)
      favicon: "brandbook/assets/logo/rs-favicon.svg"
      assets: %{"brandbook/assets/logo" => "assets"}

    Preserve the existing `skip_undefined_reference_warnings_on` and `skip_code_autolink_to`
    closures (they already guard `Rulestead.` autolinks). Do NOT introduce `../` anywhere in `files:`.
  </action>
  <verify>
    <automated>cd rulestead && grep -cE '"(Core API|Runtime \(cached lookup\)|Testing|Behaviours & Seams|Store Adapters|Telemetry & Config)":' mix.exs | grep -qx 6 && ! grep -q 'Rulestead.Rule,' mix.exs && ! grep -q '\.\./brandbook' mix.exs && grep -q 'brandbook/assets/logo/\*.svg' mix.exs && grep -q 'logo: "brandbook/assets/logo/rs-mark.svg"' mix.exs && grep -q 'why-rulestead.md' mix.exs && grep -q '"API & Stability":' mix.exs</automated>
  </verify>
  <done>mix.exs has exactly 6 named module groups, no `Rulestead.Rule,`, the `*.svg` files glob, plain-relative logo path, why-rulestead extra, and "API & Stability" extras group; no `../brandbook` token.</done>
</task>

<task type="auto">
  <name>Task 2: Add before_closing_head_tag --main* re-tint + PNG OG meta (D-13..D-16)</name>
  <files>rulestead/mix.exs</files>
  <read_first>
    - .planning/phases/126-hexdocs-front-door/126-CONTEXT.md (D-13, D-14, D-15, D-16)
    - .planning/phases/126-hexdocs-front-door/126-RESEARCH.md (V1 — var names + body.dark refinement; Pattern 1)
    - brandbook/tokens.css (mineral palette source values)
  </read_first>
  <action>
    Add `docs: [..., before_closing_head_tag: &before_closing_head_tag/1]` and define two private
    clauses. `before_closing_head_tag(:html)` returns an inline heredoc string with: OG/Twitter meta
    (`og:title`, `og:description`, `og:type=website`, `twitter:card=summary_large_image`) plus
    `og:image` AND `twitter:image` pointing at the PNG `https://hexdocs.pm/rulestead/assets/rs-social-card.png`
    (D-15 — MUST be `.png`, unfurlers don't render SVG); and an inline `<style>` re-tinting the
    ExDoc 0.40 `--main*` HSL family at `:root` (light) and `body.dark` (dark) — use `body.dark`
    NOT `.dark` (RESEARCH.md V1: ExDoc 0.40.3 specificity). Target these verified vars only:
    `--main`, `--mainDark`, `--mainDarkest`, `--mainLight`, `--mainLightest`, `--searchBarFocusColor`,
    `--searchBarBorderColor`, with the Stead Blue / mineral HSL values from D-13 (sourced from
    brandbook/tokens.css). Do NOT target `--main-color-darkened`/`--code-link-color`/`--main-background`
    (dead in 0.40, silently no-op — RESEARCH.md V1 confirmed absent). DROP any manual
    `a:focus-visible{outline:...}` block (D-14 — ExDoc ships `outline:2px solid var(--main)`; re-tinting
    --main themes the focus ring for free; a manual block would fight it). No theme `<script>`, no
    custom stylesheet file (D-16 — inline `<style>` + meta only). Do NOT wire Ember Copper into
    doc-chrome links (0.40 has no code-link var; copper stays the asset color). Define
    `before_closing_head_tag(:epub), do: ""` (D-16). Implementation check: re-grep
    `deps/ex_doc/formatters/html/dist/html-elixir-*.css` for the `--main*` names before locking
    (RESEARCH.md confirmed present in 0.40.3, but they can drift between minors).
  </action>
  <verify>
    <automated>cd rulestead && grep -q 'before_closing_head_tag' mix.exs && grep -qE 'defp before_closing_head_tag\(:html\)' mix.exs && grep -q -- '--main:' mix.exs && grep -q 'body.dark' mix.exs && grep -qE 'og:image.*rs-social-card\.png' mix.exs && grep -q 'before_closing_head_tag(:epub)' mix.exs && ! grep -q '<script' mix.exs && ! grep -q 'a:focus-visible' mix.exs</automated>
  </verify>
  <done>mix.exs defines `before_closing_head_tag/1` for `:html` (re-tints `--main*` at `:root` + `body.dark`, PNG og:image) and `:epub` (returns ""), with no `<script>` and no `a:focus-visible` block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> hex tarball | `files:` glob breadth + symlink resolution decide what ships |
| docs config -> rendered HexDocs | wrong CSS var names silently no-op; wrong og:image type breaks unfurl |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-02 | Information Disclosure | `files:` brandbook glob | mitigate | narrow non-recursive `*.svg` glob excludes `.DS_Store`/`svgo.config.mjs`/`concepts/`; `check_package_whitelist.sh` cross-package guard |
| T-126-07 | Tampering (silent no-op) | ExDoc CSS var targets | mitigate | target verified 0.40.3 `--main*` family; re-grep deps CSS before lock; not the dead 0.38 vars |
| T-126-08 | Information Disclosure (blank unfurl) | og:image type | mitigate | point og:image/twitter:image at the `.png` (D-15), never the SVG |
</threat_model>

<verification>
- `cd rulestead && mix docs --warnings-as-errors` exits 0 (autolink gate; D-23-style footguns).
- `cd rulestead && mix hex.build` then `bash scripts/ci/check_logo_bytes.sh` passes (D-10 — real
  `viewBox="0 0 62 62"` bytes in the tarball, proving the symlink+glob ship the logo).
- `bash scripts/ci/check_package_whitelist.sh` prints "package whitelist checks passed".
- grep proves 6 module groups, "API & Stability" first, `--main*`/`body.dark`/PNG og:image, no `../brandbook`.
- `release_contract_test.exs` stays green.
- Manual (UI hint yes): `mix docs`, open doc/index.html — logo loads (no 404), favicon, mineral
  tint in light AND dark (toggle), on-brand focus ring.
</verification>

<success_criteria>
Core `mix.exs` renders 6 module groups + funnel extras + branded `--main*` theming + PNG OG meta,
ships the logo in the tarball (D-10 green), and keeps `mix docs --warnings-as-errors` +
`check_package_whitelist.sh` + `release_contract_test.exs` green.
</success_criteria>

<output>
Create `.planning/phases/126-hexdocs-front-door/126-05-SUMMARY.md` when done
</output>
