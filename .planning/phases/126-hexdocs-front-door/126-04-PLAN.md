---
phase: 126-hexdocs-front-door
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/test_helpers.ex
  - rulestead/lib/rulestead/telemetry.ex
  - rulestead/lib/rulestead/config.ex
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Rulestead.TestHelpers renders a real @moduledoc (no longer hidden)"
    - "Rulestead.Telemetry renders a real @moduledoc"
    - "Rulestead.Config renders a real @moduledoc"
    - "mix docs --warnings-as-errors stays green (no undefined-reference autolink failures)"
  artifacts:
    - path: "rulestead/lib/rulestead/test_helpers.ex"
      provides: "Real @moduledoc so the 'Testing' module group (D-02) is non-empty"
      contains: "@moduledoc \""
    - path: "rulestead/lib/rulestead/telemetry.ex"
      provides: "Real @moduledoc so 'Telemetry & Config' group renders"
      contains: "@moduledoc \""
    - path: "rulestead/lib/rulestead/config.ex"
      provides: "Real @moduledoc so 'Telemetry & Config' group renders"
      contains: "@moduledoc \""
  key_links:
    - from: "rulestead/lib/rulestead/test_helpers.ex"
      to: "rulestead/mix.exs groups_for_modules"
      via: "TestHelpers becomes a visible member of the 'Testing' group (plan 05)"
      pattern: "@moduledoc"
---

<objective>
Flip the three contracted-but-hidden core modules from `@moduledoc false` to real `@moduledoc`
(DOC-01): `Rulestead.TestHelpers` (D-02), `Rulestead.Telemetry` and `Rulestead.Config` (D-03).
These are prerequisites for plan 05's module groups — without them the "Testing" and
"Telemetry & Config" groups render empty.

Purpose: Render the ALREADY-FROZEN api_stability.md contract surface truthfully (Phase 124
precedent — Context/Runtime/Admin.Policy already un-hidden). This widens NO public contract; all
three are in the frozen stable module list (api_stability.md L77+, L97-99).
Output: 3 source files with real moduledocs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-hexdocs-front-door/126-CONTEXT.md
@guides/api_stability.md
@rulestead/lib/rulestead/test_helpers.ex
@rulestead/lib/rulestead/telemetry.ex
@rulestead/lib/rulestead/config.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-hide Rulestead.TestHelpers (D-02)</name>
  <files>rulestead/lib/rulestead/test_helpers.ex</files>
  <read_first>
    - .planning/phases/126-hexdocs-front-door/126-CONTEXT.md (D-02)
    - guides/api_stability.md (L97-99 — "Supported adopter facade")
    - rulestead/lib/rulestead/test_helpers.ex (line 2 is `@moduledoc false`)
  </read_first>
  <action>
    Per D-02, replace `@moduledoc false` (test_helpers.ex:2) with a real `@moduledoc` documenting
    the public fake-backed test-helper facade contracted in api_stability.md L97-99 (a "Supported
    adopter facade" for host-app tests). Keep the existing explanatory `#` comment content as moduledoc
    prose where useful. Do NOT add or change any function/API — render the existing contract only.
    Do NOT backtick internal/hidden symbols (autolink gate); reference only public modules
    (e.g. `Rulestead`) if needed.
  </action>
  <verify>
    <automated>grep -A1 'defmodule Rulestead.TestHelpers' rulestead/lib/rulestead/test_helpers.ex | grep -q '@moduledoc "' || head -5 rulestead/lib/rulestead/test_helpers.ex | grep -qE '@moduledoc\s+"""?'; head -5 rulestead/lib/rulestead/test_helpers.ex | grep -vqE '@moduledoc false'</automated>
  </verify>
  <done>`test_helpers.ex` has a real `@moduledoc` (string/heredoc), `@moduledoc false` is gone, no API change.</done>
</task>

<task type="auto">
  <name>Task 2: Un-hide Rulestead.Telemetry and Rulestead.Config (D-03)</name>
  <files>rulestead/lib/rulestead/telemetry.ex, rulestead/lib/rulestead/config.ex</files>
  <read_first>
    - .planning/phases/126-hexdocs-front-door/126-CONTEXT.md (D-03)
    - guides/api_stability.md (L77+ — both are in the stable module list)
    - rulestead/lib/rulestead/telemetry.ex (line 3 is `@moduledoc false`)
    - rulestead/lib/rulestead/config.ex (line 2 is `@moduledoc false`)
  </read_first>
  <action>
    Per D-03, replace `@moduledoc false` with a real `@moduledoc` in BOTH telemetry.ex (line 3,
    below the `# credo:disable-for-this-file` line) and config.ex (line 2). Telemetry: document the
    locked public event catalog (the existing `#` comment names "Phase 4 public event catalog");
    Config: document the validated host-app seam configuration. Both are confirmed in
    api_stability.md L77+ — rendering the frozen surface, not adding API. No function/spec changes.
    Without this the "Telemetry & Config" module group (plan 05) renders empty. Do NOT backtick
    hidden symbols.
  </action>
  <verify>
    <automated>head -6 rulestead/lib/rulestead/telemetry.ex | grep -qE '@moduledoc\s+"' && ! head -6 rulestead/lib/rulestead/telemetry.ex | grep -q '@moduledoc false' && head -5 rulestead/lib/rulestead/config.ex | grep -qE '@moduledoc\s+"' && ! head -5 rulestead/lib/rulestead/config.ex | grep -q '@moduledoc false'</automated>
  </verify>
  <done>Both telemetry.ex and config.ex carry a real `@moduledoc`; neither has `@moduledoc false`; no API change.</done>
</task>

</tasks>

<verification>
- All three files: real `@moduledoc`, no `@moduledoc false`.
- `cd rulestead && mix docs --warnings-as-errors` exits 0 (no undefined-reference; the autolink gate).
- `release_contract_test.exs` stays green (no contract change — these are documentation flips only).
</verification>

<success_criteria>
TestHelpers, Telemetry, and Config each render a real moduledoc on HexDocs, satisfying the
"Testing" and "Telemetry & Config" group members for plan 05, with the autolink gate green.
</success_criteria>

<output>
Create `.planning/phases/126-hexdocs-front-door/126-04-SUMMARY.md` when done
</output>
