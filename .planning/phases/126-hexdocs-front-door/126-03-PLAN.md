---
phase: 126-hexdocs-front-door
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOC-05]
must_haves:
  truths:
    - "README opens with a centered brand hero using the wordmark+tagline asset"
    - "A centered row of exactly 5 clickable badges is present (Hex version, HexDocs, CI, License, Elixir)"
    - "The version badge is shields.io-driven, never a hardcoded version string"
    - "Install snippet uses ~> 1.0 (no ~> 0.1 reintroduced)"
  artifacts:
    - path: "README.md"
      provides: "Brand hero + 5-badge row + ~> 1.0 install snippet"
      contains: "rs-wordmark-tagline.svg"
  key_links:
    - from: "README.md"
      to: ".github/workflows/ci.yml"
      via: "CI badge URL actions/workflows/ci.yml/badge.svg"
      pattern: "ci.yml/badge.svg"
---

<objective>
Add the centered brand hero and 5-badge clickable row to the root README (DOC-05), using the
already-shipped `rs-wordmark-tagline.svg`. The `~> 1.0` install snippet and the deletion of the
"two version lines" callout already landed in Phase 125 — this plan adds the hero + badges and
guards against `~> 0.1` regression.

Purpose: The README is what hex.pm's package page and GitHub render — the literal front door.
A branded hero + self-healing badges is the 1.0 first impression.
Output: README.md hero block + badge row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-hexdocs-front-door/126-CONTEXT.md
@README.md
@brandbook/assets/logo/rs-wordmark-tagline.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add centered brand hero + 5-badge clickable row to README (D-20)</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/126-hexdocs-front-door/126-CONTEXT.md (D-20)
    - README.md (current top — hero goes above the existing `# Rulestead` content)
    - .github/workflows/ci.yml (confirm filename `ci.yml` for the CI badge URL — verified present)
  </read_first>
  <action>
    Per D-20, add a centered hero at the very top of README.md: a centered `rs-wordmark-tagline.svg`
    image (viewBox 0 0 340 96 — wordmark + tagline in one asset; reference it via its in-repo path
    e.g. `brandbook/assets/logo/rs-wordmark-tagline.svg`) followed by a centered row of EXACTLY 5
    badges, EACH wrapped in an `<a href>` so it is clickable:
      1. Hex version -> `https://img.shields.io/hexpm/v/rulestead` (link to hex.pm/packages/rulestead)
      2. HexDocs -> shields `badge/hex-docs` tinted `3A6F8F` (link to hexdocs.pm/rulestead)
      3. CI -> `https://github.com/szTheory/rulestead/actions/workflows/ci.yml/badge.svg`
         (filename verified `ci.yml`; link to the Actions page)
      4. License -> `https://img.shields.io/hexpm/l/rulestead` (self-heals from metadata)
      5. Elixir version -> shields `badge/elixir-~%3E%201.17` tinted `9b5931` (Ember Copper)
    NEVER hardcode the version number — the Hex-version badge is the shields URL (self-heals).
    Use a centered block (`<p align="center">` / `<div align="center">`) so hex.pm + GitHub both
    render it centered. Keep the existing `~> 1.0` install snippet (Phase 125 landed it) and do NOT
    reintroduce any `~> 0.1` string (Phase 125 guard `scripts/check_version_truth.py` enforces this).
    Do NOT re-add the deleted "two version lines" callout. Leave the rest of the README body intact.
  </action>
  <verify>
    <automated>grep -q 'rs-wordmark-tagline.svg' README.md && grep -q 'img.shields.io/hexpm/v/rulestead' README.md && grep -q 'ci.yml/badge.svg' README.md && grep -q 'img.shields.io/hexpm/l/rulestead' README.md && grep -q 'badge/elixir' README.md && grep -q '~> 1.0' README.md && ! grep -q '~> 0\.1' README.md</automated>
  </verify>
  <done>README has the wordmark-tagline hero, all 5 badge URLs (hexpm/v, hex-docs, ci.yml, hexpm/l, elixir), keeps `~> 1.0`, and contains no `~> 0.1`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> hex.pm/GitHub renderers | hardcoded version or wrong workflow filename produces a broken/stale badge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-05 | Tampering (stale info) | README version badge | mitigate | use shields `hexpm/v/rulestead` (self-heals); never hardcode the version string |
| T-126-06 | Tampering (broken link) | CI badge workflow filename | mitigate | filename verified `ci.yml`; badge URL `actions/workflows/ci.yml/badge.svg` |
</threat_model>

<verification>
- README grep proves wordmark hero + all 5 badge URLs present, `~> 1.0` kept, no `~> 0.1`.
- `scripts/check_version_truth.py` (Phase 125 guard) stays green.
- Manual: render README on GitHub/hex preview — hero centered, badges resolve.
</verification>

<success_criteria>
README presents a centered wordmark-tagline hero + 5 clickable badges with a self-healing version
badge and the `~> 1.0` install snippet; no `~> 0.1` regression.
</success_criteria>

<output>
Create `.planning/phases/126-hexdocs-front-door/126-03-SUMMARY.md` when done
</output>
