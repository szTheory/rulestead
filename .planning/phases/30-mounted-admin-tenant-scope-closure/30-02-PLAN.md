---
phase: 30-mounted-admin-tenant-scope-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex
  - rulestead_admin/test/rulestead_admin/live/environment_compare_live/index_test.exs
  - rulestead_admin/test/rulestead_admin/live/environment_compare_live/show_test.exs
  - rulestead/test/rulestead/promotion/compare_test.exs
  - rulestead/test/rulestead/store/compare_contract_test.exs
autonomous: true
requirements:
  - TEN-01
  - TEN-03
must_haves:
  truths:
    - "Mounted compare summary pages keep tenant scope explicit in URL state, compare calls, and operator-visible context."
    - "Mounted compare drill-in pages keep the same tenant scope through deep links and stale-token refresh paths."
    - "Targeted regression coverage proves mounted compare flows use the existing tenant-aware compare seam without changing package shape."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex"
      provides: "Tenant-aware compare summary route state and compare invocation"
      min_lines: 80
    - path: "rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex"
      provides: "Tenant-aware drill-in route state and trace metadata"
      min_lines: 80
    - path: "rulestead_admin/test/rulestead_admin/live/environment_compare_live/index_test.exs"
      provides: "Mounted compare summary regression coverage"
      min_lines: 80
    - path: "rulestead_admin/test/rulestead_admin/live/environment_compare_live/show_test.exs"
      provides: "Mounted compare drill-in regression coverage"
      min_lines: 80
    - path: "rulestead/test/rulestead/promotion/compare_test.exs"
      provides: "Public compare seam regression assertions for tenant-key carry-through"
      min_lines: 80
    - path: "rulestead/test/rulestead/store/compare_contract_test.exs"
      provides: "Adapter contract proof that compare tokens remain tenant-scoped"
      min_lines: 200
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex"
      to: "rulestead/lib/rulestead/promotion/compare.ex"
      via: "Rulestead.compare_environments(..., tenant_key: ...)"
      pattern: "tenant_key"
    - from: "rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex"
      to: "rulestead_admin/test/rulestead_admin/live/environment_compare_live/show_test.exs"
      via: "tenant-preserving route params and trace rows"
      pattern: "tenant"
---

<objective>
Thread the mounted tenant contract through compare summary and drill-in flows, then prove it with the smallest targeted regression suite.

Purpose: Close the compare-flow half of the Phase 30 gap per D-08 through D-12 so mounted operator compare routes preserve explicit tenant scope outside local simulation.
Output: Tenant-aware compare summary and drill-in LiveViews plus focused admin/core regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-mounted-admin-tenant-scope-closure/30-CONTEXT.md
@.planning/phases/30-mounted-admin-tenant-scope-closure/30-PATTERNS.md
@.planning/phases/29-tenancy-helpers-validation/29-01-SUMMARY.md
@.planning/phases/29-tenancy-helpers-validation/29-02-SUMMARY.md
@rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex
@rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex
@rulestead_admin/test/rulestead_admin/live/environment_compare_live/index_test.exs
@rulestead_admin/test/rulestead_admin/live/environment_compare_live/show_test.exs
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/promotion/compare.ex
@rulestead/test/rulestead/promotion/compare_test.exs
@rulestead/test/rulestead/store/compare_contract_test.exs

<interfaces>
From `rulestead/lib/rulestead/store/command.ex`:
```elixir
defmodule CompareEnvironments do
  defstruct [
    :source_environment_key,
    :target_environment_key,
    tenant_key: nil,
    flag_keys: nil,
    compare_token: nil
  ]
end
```

From `rulestead/lib/rulestead/promotion/compare.ex`:
```elixir
def compare_token(attrs) when is_map(attrs)
def new_result(attrs) when is_map(attrs)
# payload already includes tenant_key and uses it in compare-token generation
```

From `rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex`:
```elixir
page = build_page(socket, source_env, target_env, compare_token)
Rulestead.compare_environments(socket.assigns.source_env, socket.assigns.target_env, opts)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carry tenant scope through the compare summary route and invocation</name>
  <files>rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex, rulestead_admin/test/rulestead_admin/live/environment_compare_live/index_test.exs</files>
  <action>Update the compare summary page per D-08, D-09, D-10, D-11, and D-12. Read the resolved mounted tenant from the shared session assigns created in `30-01`, include `tenant` in the page params map handed to `Session.env_links/3` and `Session.current_path/3`, preserve it in `flag_path/2`, and pass `tenant_key:` into `Rulestead.compare_environments/3` without bypassing the public compare seam. Surface the active tenant in the compare context and trace-panel rows so operators can see tenant and environment together but still separate. Extend `index_test.exs` to seed host session tenant inputs, assert the mounted route keeps `tenant=...` in summary and drill-in URLs, and verify the page remains read-only.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/environment_compare_live/index_test.exs</automated>
  </verify>
  <done>The summary page preserves tenant scope in mounted URLs, compare requests include `tenant_key`, visible compare context shows tenant separately from environment, and the summary regression suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Carry tenant scope through drill-in routes and add narrow compare seam regressions</name>
  <files>rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex, rulestead_admin/test/rulestead_admin/live/environment_compare_live/show_test.exs, rulestead/test/rulestead/promotion/compare_test.exs, rulestead/test/rulestead/store/compare_contract_test.exs</files>
  <action>Finish the mounted compare carry-through per D-08 through D-12. Add `tenant` to the drill-in page params, env-switch links, trace rows, and `Rulestead.compare_environments/3` options in `show.ex`; ensure stale-token reloads and deep links do not drop tenant scope. Extend `show_test.exs` to assert tenant visibility and route preservation on the read-only drill-in page. In the core package, keep production code untouched unless a missing regression is exposed; instead, strengthen `compare_test.exs` and `compare_contract_test.exs` so they explicitly prove tenant-aware command normalization, compare-token stability changes when `tenant_key` changes, and parity across fake/ecto compare contracts. Do not widen this work into Phase 31 audit-provenance automation or new cross-tenant UI behavior.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/environment_compare_live/show_test.exs && cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/promotion/compare_test.exs test/rulestead/store/compare_contract_test.exs</automated>
  </verify>
  <done>The drill-in route keeps tenant scope through compare-token flows, visible trace metadata shows the tenant scope explicitly, and targeted admin/core regression suites prove the mounted compare path uses the existing tenant-aware compare contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mounted session assigns -> compare route params | Resolved tenant scope crosses into query-backed navigation and must not drift or disappear. |
| compare LiveViews -> `Rulestead.compare_environments/3` | Mounted admin passes scope into the shared compare seam and must preserve the bounded tenant contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | T | `rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex` | mitigate | Build one params map containing `tenant`, `env`, `source_env`, `target_env`, and optional `compare_token`, then reuse shared session helpers so mounted navigation cannot silently drop tenant scope per D-08 and D-09. |
| T-30-04 | R | `rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex` | mitigate | Surface tenant scope in compare context and trace rows, and verify compare calls pass `tenant_key:` so reviewed scope remains explicit and auditable per D-08, D-10, and D-11. |
</threat_model>

<verification>
- `cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/environment_compare_live/index_test.exs test/rulestead_admin/live/environment_compare_live/show_test.exs`
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/promotion/compare_test.exs test/rulestead/store/compare_contract_test.exs`
</verification>

<success_criteria>
- Compare summary and drill-in routes keep `tenant` alongside `env`, `source_env`, `target_env`, and `compare_token`.
- Mounted compare calls pass `tenant_key` through the existing public compare seam, not adapter-local code.
- Targeted admin and core tests prove tenant-aware compare tokens and route preservation without expanding the package or roadmap scope.
</success_criteria>

<output>
After completion, create `.planning/phases/30-mounted-admin-tenant-scope-closure/30-02-SUMMARY.md`
</output>
