---
phase: 30-mounted-admin-tenant-scope-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/session.ex
  - rulestead_admin/lib/rulestead_admin/components/shell.ex
  - rulestead_admin/test/rulestead_admin/live/session_test.exs
autonomous: true
requirements:
  - TEN-01
  - TEN-03
must_haves:
  truths:
    - "Mounted admin resolves tenant scope from host-bounded inputs before rendering operator pages."
    - "Tenant and environment remain separate visible scope axes in mounted admin chrome and URLs."
    - "Invalid tenant params never broaden mounted admin scope outside the allowed tenant set."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/session.ex"
      provides: "Shared mounted-admin tenant resolution, assigns, and route helpers"
      min_lines: 80
    - path: "rulestead_admin/lib/rulestead_admin/components/shell.ex"
      provides: "Separate tenant scope chrome alongside environment scope"
      min_lines: 30
    - path: "rulestead_admin/test/rulestead_admin/live/session_test.exs"
      provides: "Resolver, helper, and shell regression coverage for mounted tenant scope"
      min_lines: 80
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/session.ex"
      to: "rulestead_admin/lib/rulestead_admin/components/shell.ex"
      via: "placeholder assigns and shell props"
      pattern: "current_tenant|tenant_links|available_tenants"
    - from: "rulestead_admin/lib/rulestead_admin/live/session.ex"
      to: "rulestead_admin/test/rulestead_admin/live/session_test.exs"
      via: "resolve/current_path/env_links contract"
      pattern: "resolve\\(|current_path\\(|env_links\\("
---

<objective>
Define the mounted-admin tenant scope contract in the shared session seam before any compare page threads it through.

Purpose: Close the session-resolution half of the Phase 30 gap per D-04 through D-07 so tenant scope becomes explicit, host-bounded, visible, and fail-closed in the mounted admin shell.
Output: Tenant-aware mounted session helpers, shell scope chrome, and targeted session/shell regression tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-mounted-admin-tenant-scope-closure/30-CONTEXT.md
@.planning/phases/30-mounted-admin-tenant-scope-closure/30-PATTERNS.md
@.planning/phases/29-tenancy-helpers-validation/29-CONTEXT.md
@.planning/phases/29-tenancy-helpers-validation/29-VERIFICATION.md
@rulestead_admin/lib/rulestead_admin/live/session.ex
@rulestead_admin/lib/rulestead_admin/components/shell.ex
@rulestead_admin/test/rulestead_admin/live/session_test.exs

<interfaces>
From `rulestead_admin/lib/rulestead_admin/live/session.ex`:
```elixir
@type resolved :: %{
  actor: term(),
  environment: environment(),
  environments: [environment()],
  env_source: :url | :remembered | :default,
  mount_path: String.t(),
  policy: module()
}

def resolve(params, session, opts) when is_map(params) and is_map(session) and is_list(opts)
def current_path(socket_or_assigns, base_path, params \\ %{})
def env_links(socket_or_assigns, base_path, params \\ %{})
def placeholder_assigns(socket_or_assigns, opts)
```

From `rulestead_admin/lib/rulestead_admin/components/shell.ex`:
```elixir
attr(:current_environment, :map, required: true)
attr(:environments, :list, default: [])
attr(:env_links, :map, default: %{})
def page(assigns)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the shared mounted session resolver for tenant scope</name>
  <files>rulestead_admin/lib/rulestead_admin/live/session.ex</files>
  <action>Implement the Phase 30 mounted tenant contract in the shared session seam per D-04, D-05, D-06, D-07, and D-09. Add tenant fields to the `resolved` type and mounted assigns using concrete host-session keys `rulestead_admin_tenants`, `rulestead_admin_default_tenant`, and `rulestead_admin_last_tenant`. Resolve tenant with the same precedence as environment: URL `tenant` first if allowed, remembered tenant second if allowed, otherwise host default or first allowed tenant, otherwise fail closed by halting back to the mount root. Preserve tenant and environment as separate assigns and helper inputs; update `current_path/3`, `env_links/3`, and `placeholder_assigns/2` so every generated mounted-admin URL keeps both `env` and `tenant` without page-local query assembly. Do not derive tenant options from authored storage, and do not add implicit all-tenant behavior.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/session_test.exs</automated>
  </verify>
  <done>`Session.resolve/3` returns explicit tenant scope metadata, mounted assigns expose tenant state, invalid tenant params cannot widen scope, and shared route helpers preserve `env` plus `tenant` together.</done>
</task>

<task type="auto">
  <name>Task 2: Surface tenant scope in shell chrome and lock the session contract with tests</name>
  <files>rulestead_admin/lib/rulestead_admin/components/shell.ex, rulestead_admin/test/rulestead_admin/live/session_test.exs</files>
  <action>Update `Shell.page/1` and the session test suite per D-07, D-08, D-11, and D-12. Add explicit tenant chrome adjacent to but separate from the environment picker; if only one tenant is allowed, render it as read-only scope copy rather than implying a switcher. Extend `session_test.exs` with tenant precedence cases parallel to the existing environment tests, helper assertions that generated URLs preserve `tenant` and `env` together, and shell rendering assertions that show tenant scope without collapsing it into environment or policy state. Keep the change mounted-admin-only and do not add new publish or standalone-admin surfaces per D-02.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/session_test.exs</automated>
  </verify>
  <done>The shell visibly shows tenant scope as a distinct axis, session tests cover URL/remembered/default/fail-closed tenant behavior, and helper-generated links preserve the mounted tenant context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host session -> mounted admin session resolver | Untrusted URL params and host-provided session data cross into mounted admin scope resolution. |
| mounted session assigns -> shell chrome | Resolved scope is rendered back to operators and must stay explicit and non-broadened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | E | `rulestead_admin/lib/rulestead_admin/live/session.ex` | mitigate | Validate `tenant` only against `rulestead_admin_tenants`, prefer bounded fallback order, and halt to mount root when no allowed tenant exists per D-04 through D-06. |
| T-30-02 | I | `rulestead_admin/lib/rulestead_admin/components/shell.ex` | mitigate | Render tenant and environment as separate scope fields so the UI never implies hidden all-tenant or environment-collapsed state per D-07 and D-08. |
</threat_model>

<verification>
- `cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/session_test.exs`
</verification>

<success_criteria>
- Mounted admin resolves tenant scope from host-bounded URL/session/default inputs before downstream LiveViews read it.
- Shared route helpers keep `tenant` and `env` together across mounted navigation.
- Shell chrome shows tenant scope explicitly and separately from environment scope.
</success_criteria>

<output>
After completion, create `.planning/phases/30-mounted-admin-tenant-scope-closure/30-01-SUMMARY.md`
</output>
