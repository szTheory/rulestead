---
phase: 05-host-app-seams-plug-liveview-oban-installer-test-helpers
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - rulestead/test/rulestead/integration/install_smoke_test.exs
  - rulestead/test/rulestead/integration/install_golden_test.exs
  - rulestead/test/support/install_fixture.ex
  - rulestead/test/fixtures/install_golden/tree/
  - rulestead/test/fixtures/install_golden/STDOUT.txt
autonomous: true
requirements:
  - INST-03
must_haves:
  truths:
    - "A fresh Phoenix app can run `mix rulestead.install --yes`, migrate, and boot with rulestead wired in."
    - "Golden fixtures prove the installer tree and stdout byte-for-byte after timestamp normalization."
    - "The paired idempotency rerun emits only skip/already-present lines and produces no normalized tree diff."
  artifacts:
    - path: "rulestead/test/rulestead/integration/install_golden_test.exs"
      provides: "Golden-diff and paired idempotency proof harness"
    - path: "rulestead/test/support/install_fixture.ex"
      provides: "Fresh Phoenix app setup, tree normalization, and stdout normalization helpers"
    - path: "rulestead/test/fixtures/install_golden/tree/"
      provides: "Expected normalized generated host tree fixture for byte-for-byte install comparison"
    - path: "rulestead/test/fixtures/install_golden/STDOUT.txt"
      provides: "Expected normalized installer stdout fixture"
  key_links:
    - from: "rulestead/test/support/install_fixture.ex"
      to: "rulestead/test/rulestead/integration/install_golden_test.exs"
      via: "fresh host app setup, normalization, and stdout capture"
      pattern: "normalize_tree|normalize_stdout|setup_tmp_app!"
    - from: "rulestead/test/rulestead/integration/install_smoke_test.exs"
      to: "rulestead/test/fixtures/install_golden/tree/"
      via: "fresh-app proof and fixture parity cover the same installed host shape"
      pattern: "/admin/flags|Rulestead\\.Plug|rulestead\\.install"
---

<objective>
Prove the Phase 5 installer end to end on fresh Phoenix apps with smoke coverage plus a byte-for-byte golden harness.

Purpose: Isolate the expensive proof surface from installer implementation so execution stays within context budget while preserving the roadmap’s fresh-app and golden-diff guarantees.
Output: Fresh-app smoke coverage, reusable install-fixture helpers, and normalized golden tree/stdout fixtures.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-host-app-seams-plug-liveview-oban-installer-test-helpers/05-PATTERNS.md
@.planning/phases/05-host-app-seams-plug-liveview-oban-installer-test-helpers/05-02-PLAN.md
@prompts/rulestead-host-app-integration-seam.md
@prompts/rulestead-testing-and-e2e-strategy.md
@prompts/rulestead-engineering-dna-from-prior-libs.md

<interfaces>
Phase 5 install proof requirements:
- Generate a fresh Phoenix app, inject the path dependency, run `mix rulestead.install --yes`, and verify the mounted host seam boots cleanly.
- Normalize migration timestamps to `TIMESTAMP_` before comparing fixtures.
- Keep the proof Phase-5-scoped: install seam and mountability only, no speculative Phase 6/7 admin UI assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand fresh-app smoke coverage to full Phase 5 install proof</name>
  <files>rulestead/test/rulestead/integration/install_smoke_test.exs, rulestead/test/support/install_fixture.ex</files>
  <behavior>
    - Test 1: a fresh generated Phoenix app can install rulestead, migrate, compile, and boot with `/admin/flags` mounted, `Rulestead.Plug` active, and Oban middleware wiring present when Oban is detected.
    - Test 2: the shared fixture helper encapsulates fresh-app setup, path dependency injection, install execution, and normalization support instead of duplicating shell orchestration across tests.
    - Test 3: the smoke proof stays Phase-5-scoped and asserts mountability rather than speculative admin UI behavior.
  </behavior>
  <action>Expand the existing fresh-app smoke proof using a shared `test/support/install_fixture.ex` helper. Generate a Phoenix app, inject the path dependency, run `mix rulestead.install --yes`, run `mix ecto.migrate`, and verify the host app has the expected endpoint/router/config/admin seam and can boot cleanly with `/admin/flags` mounted and `Rulestead.Plug` active. If Oban is detected by the installer path used in the fixture, assert the installed config includes the middleware seam as well. Keep all assertions scoped to Phase 5 install/mount behavior only.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/integration/install_smoke_test.exs --timeout 300000</automated>
  </verify>
  <done>Fresh Phoenix app smoke coverage proves the installed host wiring boots and exposes the Phase 5 `/admin/flags` mount seam cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add golden-diff tree and stdout fixtures with paired idempotency proof</name>
  <files>rulestead/test/rulestead/integration/install_golden_test.exs, rulestead/test/support/install_fixture.ex, rulestead/test/fixtures/install_golden/tree/, rulestead/test/fixtures/install_golden/STDOUT.txt</files>
  <behavior>
    - Test 1: the golden harness normalizes migration timestamps to `TIMESTAMP_` and compares both stdout and generated tree byte-for-byte to fixtures.
    - Test 2: the normalized tree fixture includes the generated host files needed to prove the install seam, not stdout only.
    - Test 3: the paired idempotency golden rerun emits only skip/already-present lines and produces no normalized tree diff.
  </behavior>
  <action>Add `test/rulestead/integration/install_golden_test.exs` with `@moduletag :golden` and `@moduletag timeout: 300_000`, using `test/support/install_fixture.ex` to share fresh-app setup, stdout capture, tree normalization, and timestamp normalization. Add both `test/fixtures/install_golden/tree/` and `STDOUT.txt` as the expected normalized outputs. Ensure the second install run is a paired idempotency assertion, not a separate handwritten convention. Keep the fixture content and assertions limited to the installed Phase 5 host seam shape.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/integration/install_golden_test.exs --include golden --timeout 300000'</automated>
  </verify>
  <done>Golden tree and stdout fixtures lock the full installer output byte-for-byte after timestamp normalization, and the paired rerun proves idempotency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated host app -> installer proof harness | Integration tests execute against real generated host files and must compare normalized outputs deterministically |
| normalized fixture outputs -> regression suite | Golden fixtures become the exact expected host shape for future installer changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | T | golden fixture normalization | mitigate | Normalize migration timestamps and compare both stdout and tree outputs so fixture churn is bounded to meaningful installer changes. |
| T-05-11 | D | fresh-app smoke harness | mitigate | Encapsulate generator/install orchestration in `test/support/install_fixture.ex` so long-running proof remains deterministic and reusable. |
| T-05-12 | R | idempotency proof | mitigate | Pair the second installer run with the same normalized tree/stdout harness so rerun safety is proven against the exact golden contract. |
</threat_model>

<verification>
Run the smoke proof and the golden proof separately, confirming fresh-app bootability and byte-for-byte normalized fixture parity including the second-run idempotency case.
</verification>

<success_criteria>
Phase 5 has explicit, isolated proof that a fresh Phoenix app installs and boots cleanly and that installer output remains byte-stable after timestamp normalization.
</success_criteria>

<output>
After completion, create `.planning/phases/05-host-app-seams-plug-liveview-oban-installer-test-helpers/05-04-SUMMARY.md`
</output>
