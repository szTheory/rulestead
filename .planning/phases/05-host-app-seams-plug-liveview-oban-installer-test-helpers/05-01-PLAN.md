---
phase: 05-host-app-seams-plug-liveview-oban-installer-test-helpers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/plug.ex
  - rulestead/lib/rulestead/phoenix.ex
  - rulestead/lib/rulestead/live_view.ex
  - rulestead/lib/rulestead/oban.ex
  - rulestead/lib/rulestead/oban/middleware.ex
  - rulestead/lib/rulestead/oban/worker.ex
  - rulestead/test/rulestead/plug_test.exs
  - rulestead/test/rulestead/live_view_test.exs
  - rulestead/test/rulestead/oban_test.exs
  - guides/recipes/context-propagation.md
autonomous: true
requirements:
  - CTX-02
  - CTX-03
  - CTX-04
  - CTX-05
  - INST-04
  - INST-05
  - INST-06
must_haves:
  truths:
    - "Plug, LiveView, and Oban callers get explicit `%Rulestead.Context{}` builders instead of ambient context lookup."
    - "Host-facing seams evaluate through `Rulestead.Runtime` only; they do not bypass the Phase 4 runtime facade."
    - "LiveView can eager-assign multiple flags into socket assigns without reaching into store or cache internals."
    - "Oban enqueue and worker code can round-trip a serialized rulestead context without host boilerplate."
    - "Context propagation guidance stays opt-in and scoped, never hidden process magic."
  artifacts:
    - path: "rulestead/lib/rulestead/plug.ex"
      provides: "Request seam that builds and assigns `conn.assigns[:rulestead_context]`"
    - path: "rulestead/lib/rulestead/live_view.ex"
      provides: "Socket context builder and runtime-backed `assign_flags/2` helper"
    - path: "rulestead/lib/rulestead/oban/middleware.ex"
      provides: "Oban enqueue/perform seam for serialized rulestead context propagation"
    - path: "rulestead/lib/rulestead/oban/worker.ex"
      provides: "Explicit `use Rulestead.Oban.Worker` seam for worker-side context access"
    - path: "guides/recipes/context-propagation.md"
      provides: "Scoped process-tree propagation guidance for Phoenix, LiveView, and Oban"
  key_links:
    - from: "rulestead/lib/rulestead/plug.ex"
      to: "rulestead/lib/rulestead/phoenix.ex"
      via: "conn-to-context normalization"
      pattern: "context_from_conn"
    - from: "rulestead/lib/rulestead/live_view.ex"
      to: "rulestead/lib/rulestead/runtime.ex"
      via: "batched runtime-backed flag assignment"
      pattern: "Rulestead\\.Runtime\\.(enabled\\?|get_value|evaluate)"
    - from: "rulestead/lib/rulestead/oban/middleware.ex"
      to: "rulestead/lib/rulestead/oban.ex"
      via: "job meta serialization and recovery"
      pattern: "context_from_job|put_context"
    - from: "rulestead/lib/rulestead/oban/worker.ex"
      to: "rulestead/lib/rulestead/oban.ex"
      via: "worker macro delegates to explicit job context seam"
      pattern: "use Rulestead\\.Oban\\.Worker|context_from_job"
---

<objective>
Create the explicit Phoenix/Plug/LiveView/Oban host seams for Phase 5 without leaking store access, cache internals, or future admin UI behavior into the core package.

Purpose: Give app developers the concrete request/socket/job integration points Phase 3 and Phase 4 intentionally deferred, while preserving the explicit runtime boundary.
Output: Host seam modules, focused tests, and the scoped context-propagation recipe.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-host-app-seams-plug-liveview-oban-installer-test-helpers/05-PATTERNS.md
@.planning/phases/04-snapshot-cache-runtime-refresh-telemetry-explain-wiring/04-CONTEXT.md
@.planning/phases/04-snapshot-cache-runtime-refresh-telemetry-explain-wiring/04-03-SUMMARY.md
@.planning/phases/04-snapshot-cache-runtime-refresh-telemetry-explain-wiring/04-05-SUMMARY.md
@prompts/rulestead-host-app-integration-seam.md
@prompts/rulestead-testing-and-e2e-strategy.md
@prompts/rulestead-domain-language-field-guide.md
@prompts/rulestead-engineering-dna-from-prior-libs.md

<interfaces>
From `rulestead/lib/rulestead/context.ex`:
```elixir
@spec new(t() | keyword() | map()) :: t()
@spec normalize(t() | keyword() | map()) :: t()
```

From `rulestead/lib/rulestead/runtime.ex`:
```elixir
@spec enabled?(String.t() | atom(), String.t() | atom(), Context.t() | keyword() | map()) ::
        {:ok, boolean()} | {:error, Rulestead.Error.t()}
@spec get_value(String.t() | atom(), String.t() | atom(), Context.t() | keyword() | map(), term()) ::
        {:ok, term()} | {:error, Rulestead.Error.t()}
@spec evaluate(String.t() | atom(), String.t() | atom(), Context.t() | keyword() | map()) ::
        {:ok, Result.t()} | {:error, Rulestead.Error.t()}
```

From `rulestead/lib/rulestead/fake/control.ex`:
```elixir
@spec publish!(module() | atom(), String.t() | atom(), pos_integer()) :: :ok
@spec latest_snapshot!(String.t() | atom()) :: map()
```

Phase 4 locked boundary:
- D-01 through D-05 in `04-CONTEXT.md` require Phase 5 helpers to consume the explicit `Rulestead.Runtime` facade rather than store fetches, ETS access, or the pure payload-first evaluator.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build explicit Plug, Phoenix, and LiveView context seams over the runtime facade</name>
  <files>rulestead/lib/rulestead/plug.ex, rulestead/lib/rulestead/phoenix.ex, rulestead/lib/rulestead/live_view.ex, rulestead/test/rulestead/plug_test.exs, rulestead/test/rulestead/live_view_test.exs</files>
  <behavior>
    - Test 1: `Rulestead.Phoenix.context_from_conn/1` and `Rulestead.Plug` derive a normalized `%Rulestead.Context{}` from conn/session/header/cookie input and store it only in `conn.assigns[:rulestead_context]`.
    - Test 2: `Rulestead.LiveView.context_from_socket/1` derives a normalized context from socket assigns/session data without reading process state.
    - Test 3: `Rulestead.LiveView.assign_flags/2` batches runtime-backed flag resolution through `Rulestead.Runtime` for one environment and writes stable socket assigns without touching `Rulestead.Runtime.Cache`, `Rulestead.Store`, or `Rulestead.Evaluator`.
  </behavior>
  <action>Create `Rulestead.Plug`, `Rulestead.Phoenix`, and `Rulestead.LiveView` per CTX-02, CTX-03, INST-04, and INST-05. Keep all host-framework structs at the edge: convert conn/socket inputs into `%Rulestead.Context{}` with explicit builder functions, then call `Rulestead.Runtime.enabled?/3`, `get_value/4`, or `evaluate/3` per D-05 from Phase 4. Make the builder inputs configurable and explicit: environment key source, targeting key extraction order, request/session ids, and any optional assign keys should come from plug/helper opts or a validated config module, not hidden global assumptions. `assign_flags/2` should resolve a caller-provided list/map of flags into socket assigns in one helper call, but stay Phase-5-scoped: no admin LiveView behavior, no direct store lookups, no payload-first evaluator bypass, and no process-dictionary fallback.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/plug_test.exs test/rulestead/live_view_test.exs</automated>
  </verify>
  <done>Phoenix requests and LiveView sockets can build explicit rulestead contexts and evaluate flags through the runtime facade with no ambient magic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scoped Oban propagation, `use Rulestead.Oban.Worker`, and the explicit context-propagation recipe</name>
  <files>rulestead/lib/rulestead/oban.ex, rulestead/lib/rulestead/oban/middleware.ex, rulestead/lib/rulestead/oban/worker.ex, rulestead/test/rulestead/oban_test.exs, guides/recipes/context-propagation.md</files>
  <behavior>
    - Test 1: `Rulestead.Oban.context_from_job/1` reconstructs `%Rulestead.Context{}` only from explicit serialized job metadata and never from ambient worker state.
    - Test 2: `Rulestead.Oban.Middleware` attaches a serialized rulestead context at enqueue and a worker using `use Rulestead.Oban.Worker` can recover it without boilerplate.
    - Test 3: the propagation guide documents Plug -> LiveView -> Oban opt-in handoff and explicitly forbids hidden process-tree propagation.
  </behavior>
  <action>Create `Rulestead.Oban`, `Rulestead.Oban.Middleware`, and `Rulestead.Oban.Worker` per CTX-04, CTX-05, and INST-06. Keep serialization/deserialization in the Oban seam, not in `Rulestead.Context`: serialize only bounded context fields needed for later evaluation, restore them with `Context.normalize/1`, and expose helper functions a worker can call directly. Make the worker-facing public API explicit, not optional: `use Rulestead.Oban.Worker` must provide the no-boilerplate seam the roadmap success criterion names, and `rulestead/test/rulestead/oban_test.exs` must include a worker-shaped test proving the macro-enabled module can recover context from job args. Do not add scheduler, rollout automation, or any Phase 7 kill-switch/admin behavior. Add `guides/recipes/context-propagation.md` documenting the exact supported propagation chain and its limits per CTX-05: explicit opt-in for Plug/LiveView/Oban, no process-dictionary magic, no hidden cross-process mutation, and no raw conn/socket/job structs in telemetry or job payloads.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/oban_test.exs</automated>
  </verify>
  <done>Oban jobs can carry and recover a scoped rulestead context explicitly, `use Rulestead.Oban.Worker` is the documented worker-side seam, and the docs make the propagation rules narrow and predictable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host framework structs -> `%Rulestead.Context{}` | Untrusted conn/socket/job data is normalized into the runtime context |
| host seams -> `Rulestead.Runtime` | Integration helpers must not bypass the runtime facade or leak raw host objects into evaluation/telemetry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | S | `Rulestead.Plug` / `Rulestead.Phoenix` | mitigate | Normalize only explicit configured fields into `%Rulestead.Context{}` and keep targeting/session/environment sources caller-visible instead of inferred ambiently. |
| T-05-02 | T | `Rulestead.LiveView.assign_flags/2` | mitigate | Route all evaluations through `Rulestead.Runtime` per Phase 4 D-05 so socket helpers cannot bypass snapshot freshness or telemetry boundaries. |
| T-05-03 | I | `Rulestead.Oban.Middleware` | mitigate | Serialize only bounded context fields into job metadata and document the no-ambient-propagation rule in `guides/recipes/context-propagation.md`. |
</threat_model>

<verification>
Run the seam-focused tests, then confirm the new recipe documents Plug, LiveView, and Oban propagation as explicit opt-in steps rather than background magic.
</verification>

<success_criteria>
Phase 5 host seams exist as explicit adapters around the Phase 4 runtime facade, with request/socket/job context builders and a documented propagation model that stays narrow and predictable.
</success_criteria>

<output>
After completion, create `.planning/phases/05-host-app-seams-plug-liveview-oban-installer-test-helpers/05-01-SUMMARY.md`
</output>
