---
phase: 05-host-app-seams-plug-liveview-oban-installer-test-helpers
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - rulestead/lib/rulestead/config.ex
  - rulestead/lib/rulestead/install.ex
  - rulestead/lib/rulestead/install/config_writer.ex
  - rulestead/lib/rulestead/install/file_injector.ex
  - rulestead/lib/mix/tasks/rulestead.install.ex
  - rulestead_admin/lib/rulestead_admin/router.ex
  - rulestead_admin/test/rulestead_admin/router_test.exs
  - rulestead/test/rulestead/mix/tasks/rulestead_install_test.exs
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-06
must_haves:
  truths:
    - "Installer output is deterministic, line-oriented, and idempotent on the second run."
    - "Endpoint, router, Oban, and config injections are content-aware and skip safely when already present."
    - "Config defaults are validated through a NimbleOptions-backed schema rather than loose application env reads."
    - "The sibling-package admin mount seam compiles and boots as a Phase 5 install seam without pulling Phase 6/7 UI into scope."
  artifacts:
    - path: "rulestead/lib/rulestead/config.ex"
      provides: "NimbleOptions-backed host config defaults and validation for Phase 5 seams"
    - path: "rulestead/lib/rulestead/install/file_injector.ex"
      provides: "Shared content-based file injection and skip/conflict behavior for installer writers"
    - path: "rulestead_admin/lib/rulestead_admin/router.ex"
      provides: "Bootable sibling-package mount macro for the Phase 5 `/admin/flags` install seam"
  key_links:
    - from: "rulestead/lib/mix/tasks/rulestead.install.ex"
      to: "rulestead/lib/rulestead/install.ex"
      via: "deterministic line-oriented install runner"
      pattern: "Install\\.run"
    - from: "rulestead/lib/rulestead/install/config_writer.ex"
      to: "rulestead/lib/rulestead/install/file_injector.ex"
      via: "shared idempotent injection rules"
      pattern: "inject|skip|conflict"
    - from: "rulestead/lib/rulestead/install.ex"
      to: "rulestead_admin/lib/rulestead_admin/router.ex"
      via: "installer mounts the sibling-package router macro that must boot cleanly in a fresh host app"
      pattern: "rulestead_admin|/admin/flags"
---

<objective>
Expand the narrow Phase 2 installer into the full Phase 5 host-app installer with deterministic file injection, validated defaults, and a bootable sibling-package admin mount seam.

Purpose: Deliver Alex’s 15-minute install path with safe reruns, explicit injected seams, and strong regression proof around real generated host apps.
Output: Full installer/config/admin-mount seam and focused installer unit coverage, with fresh-app proof deferred to the next dependent plan.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-host-app-seams-plug-liveview-oban-installer-test-helpers/05-PATTERNS.md
@.planning/phases/04-snapshot-cache-runtime-refresh-telemetry-explain-wiring/04-CONTEXT.md
@.planning/phases/04-snapshot-cache-runtime-refresh-telemetry-explain-wiring/04-03-SUMMARY.md
@prompts/rulestead-host-app-integration-seam.md
@prompts/rulestead-testing-and-e2e-strategy.md
@prompts/rulestead-engineering-dna-from-prior-libs.md
@prompts/rulestead-personas-jtbd-and-onboarding.md
@rulestead/lib/rulestead/install.ex
@rulestead/lib/rulestead/install/config_writer.ex
@rulestead/lib/rulestead/install/migration_writer.ex
@rulestead/lib/mix/tasks/rulestead.install.ex

<interfaces>
From `rulestead/lib/rulestead/install.ex`:
```elixir
@spec run(keyword()) :: {:ok, [String.t()]} | {:error, Rulestead.Error.t()}
```

From `rulestead/lib/rulestead/install/config_writer.ex`:
```elixir
@import_line ~s(import_config "rulestead.exs")
```

From `rulestead/lib/mix/tasks/rulestead.install.ex`:
```elixir
{opts, _argv, _invalid} = OptionParser.parse(args, strict: @switches)
Enum.each(messages, fn message -> shell.info(message) end)
```

Installer constraints from source docs:
- Host-app integration prompt requires explicit stdout for every injection, idempotent reruns, and admin mount at `/admin/flags` behind host policy.
- Phase 4 D-05 requires the generated host code to consume `Rulestead.Runtime`, not direct store/cache access.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validated Phase 5 config defaults and full installer injection orchestration</name>
  <files>rulestead/lib/rulestead/config.ex, rulestead/lib/rulestead/install.ex, rulestead/lib/rulestead/install/config_writer.ex, rulestead/lib/rulestead/install/file_injector.ex, rulestead/lib/mix/tasks/rulestead.install.ex, rulestead_admin/lib/rulestead_admin/router.ex, rulestead_admin/test/rulestead_admin/router_test.exs, rulestead/test/rulestead/mix/tasks/rulestead_install_test.exs</files>
  <behavior>
    - Test 1: `mix rulestead.install` writes migrations plus Phase 5 config, endpoint, router, and Oban wiring with deterministic `write`/`skip` lines.
    - Test 2: config defaults are validated through a NimbleOptions schema and expose the explicit seam settings used by `Rulestead.Plug`, `Rulestead.LiveView`, and `Rulestead.Oban`.
    - Test 3: the sibling `rulestead_admin` mount seam is bootable in Phase 5, so `rulestead_admin \"/flags\"` no longer hard-raises and can compile in a fresh host app without pulling in Phase 6/7 UI.
    - Test 4: a second installer run emits only skip/already-present lines and preserves file bytes without duplicate injections.
  </behavior>
  <action>Create the full Phase 5 installer per INST-01 and INST-02. Add `Rulestead.Config` with a NimbleOptions-backed schema covering the explicit seam settings Phase 5 owns: environment key/default, Plug extraction order, LiveView assign defaults, Oban middleware toggle, and any runtime façade integration defaults required by the host app. Extend `Rulestead.Install` as a thin orchestrator over writers/injectors, not a blob: migrations stay in `MigrationWriter`, config stays in `ConfigWriter`, and new endpoint/router/Oban mutation logic should use a shared `FileInjector` that mirrors the existing content-based `write`/`skip` contract from `ConfigWriter`. Update the mix task to support `--yes` and deterministic CLI output, detect Oban before injecting middleware, and mount the admin seam at `/admin/flags` behind host-owned policy hooks only; do not scaffold Phase 6/7 admin UI or publish flows for `rulestead_admin`. Add the sibling-package admin seam work required to make that mount bootable in Phase 5: replace the current hard-raising `rulestead_admin/2` macro in `rulestead_admin/lib/rulestead_admin/router.ex` with a minimal compile-safe mount seam and add focused router tests proving the macro expands and compiles without implying any Phase 6/7 screen implementation. The generated code must consume the explicit Phase 4 runtime facade, not `Rulestead.Store`, `Rulestead.Runtime.Cache`, or payload-first evaluator calls.</action>
  <verify>
    <automated>bash -lc 'cd rulestead && mix test test/rulestead/mix/tasks/rulestead_install_test.exs && cd ../rulestead_admin && mix test test/rulestead_admin/router_test.exs'</automated>
  </verify>
  <done>The installer writes validated Phase 5 host wiring deterministically, the sibling-package `/admin/flags` mount seam boots without a hard raise, and rerunning the installer is safe and byte-stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer input/options -> host files | The mix task mutates host application source/config files |
| generated host code -> runtime/admin seams | Installed wiring must remain explicit, bounded, and free of hidden auth or runtime assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T | installer file injection | mitigate | Centralize content-based injection in `Rulestead.Install.FileInjector`, emit deterministic `write`/`skip` lines, and fail loudly on conflicts instead of duplicating or guessing. |
| T-05-05 | E | generated admin mount | mitigate | Mount only the Phase 5 admin seam at `/admin/flags` behind host-owned policy/pipeline hooks; do not add library-owned auth or Phase 6 UI behavior. |
| T-05-06 | D | config/runtime wiring | mitigate | Validate config keys with NimbleOptions and generate code that calls the explicit `Rulestead.Runtime` facade so install-time mistakes do not silently bypass runtime freshness guarantees. |
</threat_model>

<verification>
Run the installer unit suite and sibling router seam tests, confirming reruns emit only skip/already-present lines and the Phase 5 `/admin/flags` mount no longer hard-raises.
</verification>

<success_criteria>
The full Phase 5 installer produces validated host wiring, a bootable `/admin/flags` sibling mount seam, and safely idempotent reruns, ready for fresh-app proof in the next dependent plan.
</success_criteria>

<output>
After completion, create `.planning/phases/05-host-app-seams-plug-liveview-oban-installer-test-helpers/05-02-SUMMARY.md`
</output>
