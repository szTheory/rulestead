---
phase: 05-host-app-seams-plug-liveview-oban-installer-test-helpers
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/test_helpers.ex
  - rulestead/lib/rulestead/fake.ex
  - rulestead/lib/rulestead/fake/control.ex
  - rulestead/test/test_helper.exs
  - rulestead/test/rulestead/test_helpers_test.exs
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-05
must_haves:
  truths:
    - "Host-app tests can force flags and bucket assignments through public helpers without Postgres."
    - "Test helpers delegate to the Fake adapter/control seam instead of inventing a second test-only storage path."
    - "Telemetry-backed evaluation assertions verify runtime evaluation happened without leaking raw payloads."
    - "The default ExUnit harness stays compatible with Ecto sandbox manual mode while using Fake as the release-gate target."
  artifacts:
    - path: "rulestead/lib/rulestead/test_helpers.ex"
      provides: "Public flag test helper API"
    - path: "rulestead/test/rulestead/test_helpers_test.exs"
      provides: "Executable proof for `with_flag/3`, `put_flag/3`, `clear_flags/0`, `seed_bucket/3`, and `assert_flag_evaluated/2`"
    - path: "rulestead/test/test_helper.exs"
      provides: "Default fake-first test harness with sandbox compatibility"
  key_links:
    - from: "rulestead/lib/rulestead/test_helpers.ex"
      to: "rulestead/lib/rulestead/fake/control.ex"
      via: "thin helper delegation"
      pattern: "Rulestead\\.Fake\\.Control"
    - from: "rulestead/lib/rulestead/test_helpers.ex"
      to: "rulestead/lib/rulestead/telemetry.ex"
      via: "telemetry-backed assertion helper"
      pattern: "attach_many|detach"
---

<objective>
Ship the public fake-backed test helper API and the default harness posture Phase 5 promises to host apps.

Purpose: Make rulestead tests fast and ergonomic by default, with the Fake adapter as the main execution target and telemetry-backed assertions for evaluation behavior.
Output: Public test helper module, focused helper tests, and any fake-control/test-helper harness adjustments needed for deterministic use.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-host-app-seams-plug-liveview-oban-installer-test-helpers/05-PATTERNS.md
@.planning/phases/04-snapshot-cache-runtime-refresh-telemetry-explain-wiring/04-05-SUMMARY.md
@prompts/rulestead-testing-and-e2e-strategy.md
@prompts/rulestead-engineering-dna-from-prior-libs.md
@rulestead/lib/rulestead/fake.ex
@rulestead/lib/rulestead/fake/control.ex
@rulestead/test/rulestead/telemetry_test.exs

<interfaces>
From `rulestead/lib/rulestead/fake/control.ex`:
```elixir
@spec reset!(keyword()) :: :ok
@spec put_flag!(map()) :: map()
@spec put_flag(map()) :: {:ok, map()} | {:error, Rulestead.Error.t()}
@spec disconnect!() :: :ok
```

From `rulestead/lib/rulestead/telemetry.ex`:
```elixir
@spec attach_many(term(), [event_name()], :telemetry.handler_function(), term()) :: :ok | {:error, term()}
@spec detach(term()) :: :ok
```

Testing posture from source docs:
- Fake adapter is the default merge-blocking target.
- `with_flag/3`, `put_flag/3`, `clear_flags/0`, `seed_bucket/3`, and `assert_flag_evaluated/2` are public API for host-app tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement public flag helper APIs as thin fake-control delegates</name>
  <files>rulestead/lib/rulestead/test_helpers.ex, rulestead/lib/rulestead/fake.ex, rulestead/lib/rulestead/fake/control.ex, rulestead/test/rulestead/test_helpers_test.exs</files>
  <behavior>
    - Test 1: `with_flag "foo", true do ... end` is implemented as macro syntax, forces the flag value only for block scope, and always cleans up afterward.
    - Test 2: `put_flag/3`, `clear_flags/0`, and `seed_bucket/3` delegate to fake-control primitives and make deterministic evaluation possible without Postgres.
    - Test 3: helper behavior works against the existing runtime/evaluator contract instead of a second parallel fake API surface.
  </behavior>
  <action>Create `Rulestead.TestHelpers` as public Phase 5 API for TEST-01 through TEST-03. Keep it a thin convenience layer over `Rulestead.Fake.Control`: add or extend fake/control functions only where necessary to support helper semantics cleanly, but do not build a second in-memory store path or encode helper-only business logic into callers. Make `with_flag/3` explicit macro API, not just block-scoped behavior: implement `with_flag "foo", true do ... end` syntax in `rulestead/lib/rulestead/test_helpers.ex` and add a matching test in `rulestead/test/rulestead/test_helpers_test.exs` that uses the macro form exactly. `put_flag/3` should seed a fake-backed flag for the remainder of a test with explicit environment/default semantics consistent with the runtime facade. `clear_flags/0` should reset fake state for test isolation. `seed_bucket/3` should provide deterministic variant assignment for a specific targeting key. Preserve TEST-05 by keeping helpers compatible with existing ExUnit + Ecto sandbox manual mode and the Fake-first testing strategy from the prompts.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/test_helpers_test.exs</automated>
  </verify>
  <done>Host-app tests can use `with_flag "foo", true do ... end`, plus the other fake-backed helpers, through a stable public API with deterministic cleanup and bucket control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add telemetry-backed evaluation assertions and wire the default fake-first test harness</name>
  <files>rulestead/lib/rulestead/test_helpers.ex, rulestead/test/test_helper.exs, rulestead/test/rulestead/test_helpers_test.exs</files>
  <behavior>
    - Test 1: `assert_flag_evaluated/2` attaches to the Phase 4 telemetry surface, observes a matching evaluation event, and detaches safely.
    - Test 2: helper assertions tolerate additive telemetry metadata and rely only on the stable event contract from Phase 4.
    - Test 3: `test/test_helper.exs` starts Fake by default and preserves Ecto sandbox `:manual` compatibility instead of forcing Postgres for unit coverage.
  </behavior>
  <action>Extend `Rulestead.TestHelpers` with `assert_flag_evaluated/2` using the telemetry-safe attachment patterns already present in `rulestead/test/rulestead/telemetry_test.exs` and `Rulestead.Telemetry.attach_many/4`. Listen to the stable eval event family only, assert on bounded metadata such as `flag_key`/`environment`/`reason`, and ensure handlers always detach even on assertion failure. Update `test/test_helper.exs` only as needed so Fake starts by default, helper tests remain deterministic, and Ecto sandbox manual mode stays intact for tests that still need repo access. Keep this Phase-5-scoped: do not create CI aliases, browser fixtures, or Phase 8 API-stability docs here.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/test_helpers_test.exs test/rulestead/telemetry_test.exs</automated>
  </verify>
  <done>Telemetry-backed evaluation assertions are available to host tests, and the default harness remains fake-first while preserving sandbox compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host test code -> Fake control seam | Public helpers mutate fake state and must stay deterministic and isolated |
| helper assertions -> telemetry handler surface | Test assertion handlers observe runtime events and must not leak or destabilize instrumented code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | T | `Rulestead.TestHelpers` | mitigate | Delegate through `Rulestead.Fake.Control` only so helper mutations use one canonical fake state path with predictable cleanup behavior. |
| T-05-08 | D | `with_flag/3` / `clear_flags/0` | mitigate | Ensure block-scoped cleanup and explicit resets so one test’s fake mutations cannot silently bleed into another. |
| T-05-09 | I | `assert_flag_evaluated/2` | mitigate | Match only bounded Phase 4 telemetry metadata and detach handlers safely so helper assertions do not capture raw payloads or destabilize runtime operations. |
</threat_model>

<verification>
Run the helper suite and telemetry suite together, confirming fake-first helper usage works without Postgres and telemetry-backed assertions observe only the stable event contract.
</verification>

<success_criteria>
Phase 5 delivers the advertised public test-helper API on top of the Fake adapter/control seam, with deterministic cleanup and telemetry-backed assertion support that remains compatible with the default test harness.
</success_criteria>

<output>
After completion, create `.planning/phases/05-host-app-seams-plug-liveview-oban-installer-test-helpers/05-03-SUMMARY.md`
</output>
