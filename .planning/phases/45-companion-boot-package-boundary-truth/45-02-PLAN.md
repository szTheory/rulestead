---
phase: 45-companion-boot-package-boundary-truth
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - rulestead/lib/rulestead/application.ex
  - rulestead/lib/rulestead/redis.ex
  - rulestead/lib/rulestead/runtime/config.ex
  - rulestead/test/rulestead/runtime/startup_test.exs
autonomous: true
requirements:
  - PKG-01
  - PKG-02
must_haves:
  truths:
    - "Runtime boot ownership stays in `rulestead`; `rulestead_admin` remains a thin mounted package."
    - "Optional Redis/notifier/pubsub prerequisites are config-gated and never become silent hard requirements for mounted proof."
    - "Mounted companion startup either boots through the documented runtime contract or fails in an explicit, bounded way."
  artifacts:
    - path: "rulestead/lib/rulestead/application.ex"
      provides: "Concrete child-startup contract for the mounted companion runtime surface"
      min_lines: 25
    - path: "rulestead/lib/rulestead/redis.ex"
      provides: "Single source of truth for Redis optionality and connection spec"
      min_lines: 50
    - path: "rulestead/lib/rulestead/runtime/config.ex"
      provides: "Runtime config merge/gating semantics used by mounted startup"
      min_lines: 60
    - path: "rulestead/test/rulestead/runtime/startup_test.exs"
      provides: "Targeted runtime proof for degraded/optional startup behavior"
      min_lines: 100
  key_links:
    - from: "rulestead/lib/rulestead/application.ex"
      to: "rulestead/lib/rulestead/redis.ex"
      via: "startup child selection must flow through one explicit Redis-enabled gate"
      pattern: "Redis.enabled?|connection_spec|Publisher"
    - from: "rulestead/lib/rulestead/runtime/config.ex"
      to: "rulestead/test/rulestead/runtime/startup_test.exs"
      via: "config-gated boot behavior must be proven in runtime startup tests"
      pattern: "environment_keys|store|notifier|pubsub"
---

<objective>
Repair sibling-package boot/runtime/config wiring without widening the mounted admin posture.

Purpose: Once Phase 45 locks the host-facing contract, the runtime boot path must match it exactly. This plan keeps startup ownership in `rulestead`, keeps optional infrastructure explicitly config-gated, and prevents the mounted companion from depending on hidden runtime side effects.
Output: adjusted core runtime/config code plus startup tests that prove degraded and optional boot behavior without moving runtime concerns into `rulestead_admin`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-companion-boot-package-boundary-truth/45-RESEARCH.md
@.planning/phases/45-companion-boot-package-boundary-truth/45-VALIDATION.md
@AGENTS.md
@prompts/rulestead-host-app-integration-seam.md
@rulestead/lib/rulestead/application.ex
@rulestead/lib/rulestead/redis.ex
@rulestead/lib/rulestead/config.ex
@rulestead/lib/rulestead/runtime/config.ex
@rulestead/test/rulestead/runtime/startup_test.exs
@rulestead/config/test.exs
@rulestead_admin/lib/rulestead_admin/application.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize runtime startup so mounted boot uses one explicit config-gated path</name>
  <read_first>
    - rulestead/lib/rulestead/application.ex
    - rulestead/lib/rulestead/redis.ex
    - rulestead/lib/rulestead/runtime/config.ex
    - rulestead/lib/rulestead/config.ex
    - rulestead/test/rulestead/runtime/startup_test.exs
    - rulestead/config/test.exs
    - .planning/phases/45-companion-boot-package-boundary-truth/45-RESEARCH.md
  </read_first>
  <files>rulestead/lib/rulestead/application.ex, rulestead/lib/rulestead/redis.ex, rulestead/lib/rulestead/runtime/config.ex, rulestead/test/rulestead/runtime/startup_test.exs</files>
  <behavior>
    - Test 1: `Rulestead.Application` starts `StaleTracker`, `Analytics.Batcher`, and `RuntimeSupervisor` consistently, while Redis children are started only when the explicit gate says they should be.
    - Test 2: runtime config reads remain concrete and bounded: `environment_keys`, `store`, `notifier`, `pubsub`, and `pubsub_topic` come from the documented config merge path instead of ad hoc lookup.
    - Test 3: missing optional infrastructure degrades or denies cleanly instead of surfacing hidden boot crashes later in the mounted companion path.
  </behavior>
  <action>Audit `rulestead` startup and normalize any remaining ambiguity around optional runtime prerequisites. Keep `Rulestead.Application` as the only owner of runtime child boot, and keep `rulestead_admin` free of duplicate runtime startup logic. Route all Redis optionality through `Rulestead.Redis.enabled?/1` and `connection_spec/1`, preserve the existing test-environment default `enabled: false`, and make `Rulestead.Runtime.Config` the single concrete merge path for runtime options such as `environment_keys`, `store`, `notifier`, `pubsub`, and `pubsub_topic`. Extend `startup_test.exs` so it proves the desired degraded/optional behavior explicitly rather than relying on inference from broader test suites. Do not add new admin-owned children, new publish posture, or broader observability-product work.</action>
  <acceptance_criteria>
    - `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/runtime/startup_test.exs` passes.
    - `rg -n "Redis.enabled\\?|RuntimeSupervisor|Analytics.Batcher|StaleTracker" /Users/jon/projects/rulestead/rulestead/lib/rulestead/application.ex` finds the startup contract.
    - `rg -n "environment_keys|store|notifier|pubsub|pubsub_topic" /Users/jon/projects/rulestead/rulestead/lib/rulestead/runtime/config.ex /Users/jon/projects/rulestead/rulestead/test/rulestead/runtime/startup_test.exs` finds the config-gated behavior.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/runtime/startup_test.exs</automated>
  </verify>
  <done>The mounted companion runtime surface boots through one explicit `rulestead`-owned path with optional infra kept bounded and test-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime config -> mounted companion startup | Hidden config requirements can turn optional infrastructure into accidental hard dependencies |
| core runtime -> admin package | Boot ownership drifting into `rulestead_admin` would widen package posture and blur support truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-11 | D | `Rulestead.Application` child boot | mitigate | Keep startup child selection explicit and config-gated so unsupported infra does not crash the mounted companion path. |
| T-45-12 | T | runtime config merge path | mitigate | Centralize runtime option reads in `Rulestead.Runtime.Config` and prove them with startup tests. |
| T-45-13 | E | package boundary | mitigate | Keep runtime ownership in `rulestead` and prevent `rulestead_admin` from becoming a second runtime front door. |
</threat_model>

<verification>
- `cd /Users/jon/projects/rulestead/rulestead && mix test test/rulestead/runtime/startup_test.exs`
- `rg -n "Redis.enabled\\?|RuntimeSupervisor|Analytics.Batcher|StaleTracker" /Users/jon/projects/rulestead/rulestead/lib/rulestead/application.ex`
- `rg -n "environment_keys|store|notifier|pubsub|pubsub_topic" /Users/jon/projects/rulestead/rulestead/lib/rulestead/runtime/config.ex /Users/jon/projects/rulestead/rulestead/test/rulestead/runtime/startup_test.exs`
</verification>

<success_criteria>
- Runtime boot and optional infra rules are explicit, bounded, and package-correct.
- Startup tests prove degraded behavior instead of letting boot regressions hide behind broader suites.
- `rulestead_admin` remains a mounted companion, not a second runtime owner.
</success_criteria>

<output>
After completion, create `.planning/phases/45-companion-boot-package-boundary-truth/45-02-SUMMARY.md`
</output>
