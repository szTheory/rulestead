---
phase: 74-api-stability-catalog-sync
plan: 74-01
type: docs
wave: 1
depends_on: []
files_modified:
  - guides/api_stability.md
  - guides/introduction/product-boundary.md
autonomous: true
requirements:
  - API-01
  - API-03
must_haves:
  truths:
    - api_stability.md lists every @root_exports function name from release_contract_test.exs
    - api_stability.md documents post-GA supported facades Runtime and TestHelpers without contradicting closed core module list
    - product-boundary.md states Rulestead.Runtime semver posture on 0.1.x
  artifacts:
    - guides/api_stability.md
    - guides/introduction/product-boundary.md
---

# Plan 74-01: API Stability Catalog And Runtime Semver Posture

<objective>
Manually reconcile `guides/api_stability.md` with `release_contract_test.exs` constants and add honest post-GA facade documentation plus `product-boundary.md` Runtime semver language (API-01, API-03). No new product APIs.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-74-01: Catalog claims implementation modules as public | high | Facade-only section; list Runtime.Cache etc. under Non-Public |
| T-74-02: "No other Rulestead.*" contradicts Runtime quickstart | high | Qualify core v0.1.0 list vs additive post-GA facades (D-07) |
| T-74-03: Semver over-promise on Runtime internals | medium | product-boundary: function catalog stable; implementation modules not |
</threat_model>

<tasks>

<task id="74-01-01" type="execute">
  <name>Sync root facade, Store, Policy, Error, and Config sections (API-01)</name>
  <read_first>
    - rulestead/test/rulestead/release_contract_test.exs (@root_exports L20-78, @store_callbacks L97-116, @config_* L151-156, Error.leaf_types in test ~L841)
    - guides/api_stability.md (Stable Rulestead Function Catalog, Store, Policy, Error, Host Config)
    - .planning/phases/74-api-stability-catalog-sync/74-CONTEXT.md (D-01–D-03, D-15)
  </read_first>
  <action>
**Root function catalog** — add missing entries to `## Stable Rulestead Function Catalog` (preserve alphabetical or existing grouping):

- `apply_audience_mutation/1`
- `apply_audience_mutation/2`
- `preview_audience_impact/1`
- `preview_audience_impact/2`
- `preview_audience_impact/3`
- `list_audience_dependencies/0`
- `list_audience_dependencies/1`

**Store callbacks** — add to `### Rulestead.Store` closed catalog:

- `apply_audience_mutation/1`
- `preview_audience_impact/1`
- `list_audience_dependencies/1`

**Policy callbacks** — extend `### Rulestead.Admin.Policy` to list all three public callbacks:

- `can?/4`
- `allow_self_approval?/4`
- `change_request_required?/4`

**Error closed `:type` atoms** — add `:snapshot_not_found` to the list (between `:environment_not_found` and `:ruleset_not_found` or alphabetically consistent with test order).

**Host config** — under `## Stable Host Config Schema`:

- Add `:tenancy` to closed top-level keys list
- Add nested key `tenancy.module` (or bullet `tenancy` → `:module`) matching `@tenancy_keys [:module]`
- Add remaining `@runtime_keys` if any missing from doc (`:notifier`, `:health_peer_provider`, `:pubsub`, `:pubsub_topic` per test L155)

Do **not** add new functions to Elixir code — catalog prose only.
  </action>
  <verify>
    <automated>grep -E 'apply_audience_mutation|preview_audience_impact|list_audience_dependencies|:snapshot_not_found|:tenancy' guides/api_stability.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/api_stability.md` contains `apply_audience_mutation/1`
    - `guides/api_stability.md` contains `preview_audience_impact/3`
    - `guides/api_stability.md` contains `list_audience_dependencies/1`
    - `guides/api_stability.md` contains `:snapshot_not_found`
    - `guides/api_stability.md` contains `:tenancy` in Host Config section
    - `guides/api_stability.md` lists `allow_self_approval?/4` under Policy
  </acceptance_criteria>
</task>

<task id="74-01-02" type="execute">
  <name>Add Supported adopter facades section and revise closed-module statement (API-01, API-03)</name>
  <read_first>
    - guides/api_stability.md (Stable rulestead Modules ~L27-41)
    - rulestead/lib/rulestead/runtime.ex (exports)
    - guides/recipes/testing.md (TestHelpers surface L22-28)
    - .planning/phases/74-api-stability-catalog-sync/74-CONTEXT.md (D-04–D-07, D-06)
  </read_first>
  <action>
**Revise** `## Stable rulestead Modules` closing paragraph:

Replace blanket "No other `Rulestead.*` modules are public" with language that:

1. The **v0.1.0 core module list** above remains closed.
2. **Post-GA supported adopter facades** (below) are additionally public on `0.1.x` without opening implementation trees.

**Add new section** after core modules (before or after function catalog — pick clearest flow):

`## Supported adopter facades (post-GA)`

**`Rulestead.Runtime`** — supported keyed lookup for Phoenix apps using the snapshot cache. Closed function catalog:

- `evaluate/3`
- `enabled?/3`
- `get_value/4`
- `get_variant/3`
- `explain/3`
- `diagnostics/1`

State explicitly: `Rulestead.Runtime.Cache`, `Rulestead.Runtime.Snapshot`, and other `Rulestead.Runtime.*` implementation modules are **not** public.

**`Rulestead.TestHelpers`** — supported Fake-backed test facade. Document:

- `with_flag/3`
- `put_flag/3`
- `clear_flags/0`
- `seed_bucket/3`
- `assert_flag_evaluated/2`

One-line cross-reference: backed by `Rulestead.Fake` for in-memory state; do **not** catalog `Rulestead.Fake.Control` as public.

**Non-Public Surface** — add bullet that post-GA facade support does not make governance/manifest/admin LiveView modules public (D-16).
  </action>
  <verify>
    <automated>grep -E 'Supported adopter facades|Rulestead\.Runtime|Rulestead\.TestHelpers|implementation modules' guides/api_stability.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/api_stability.md` contains heading `Supported adopter facades`
    - `guides/api_stability.md` contains `Rulestead.Runtime` and `diagnostics/1`
    - `guides/api_stability.md` contains `Rulestead.TestHelpers` and `with_flag/3`
    - `guides/api_stability.md` refutes treating `Rulestead.Runtime.Cache` as public (non-public or not public wording)
    - Blanket "No other `Rulestead.*` modules" is qualified or removed
  </acceptance_criteria>
</task>

<task id="74-01-03" type="execute">
  <name>Add Runtime semver paragraph to product-boundary.md (API-03)</name>
  <read_first>
    - guides/introduction/product-boundary.md
    - guides/introduction/getting-started.md (Runtime quickstart)
    - .planning/phases/74-api-stability-catalog-sync/74-CONTEXT.md (D-08)
  </read_first>
  <action>
After the **In scope** table (or under Snapshot runtime row), add subsection:

`### Runtime semver (0.1.x)`

Content must include:

- `Rulestead.Runtime` is the supported **keyed lookup** path when using environment + flag key with the snapshot cache (link to getting-started / evaluation flow).
- The **six-function catalog** in `guides/api_stability.md` is stable for `0.1.x` patch releases.
- **Implementation modules** under `Rulestead.Runtime.*` (cache, snapshot, refresh) are not semver-locked and may change without notice.
- Payload-first `Rulestead.evaluate/3` remains the pure evaluation contract; Runtime is additive for cached lookup.

Keep tone consistent with existing product-boundary table style.
  </action>
  <verify>
    <automated>grep -E 'Runtime semver|0\.1\.x|implementation modules' guides/introduction/product-boundary.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/introduction/product-boundary.md` contains `Runtime semver`
    - `guides/introduction/product-boundary.md` contains `0.1.x`
    - `guides/introduction/product-boundary.md` states implementation modules are not semver-locked or not public
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead && mix test test/rulestead/release_contract_test.exs
```
Expect failures until Plan 74-02 adds bidirectional guards; after 74-02 both plans green.
</verification>
