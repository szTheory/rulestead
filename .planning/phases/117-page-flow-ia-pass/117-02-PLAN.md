---
phase: "117-page-flow-ia-pass"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "examples/demo/frontend/tests/admin-flow-ia.spec.ts"
autonomous: true
requirements: [FLOW-01, FLOW-03, FLOW-04]
requirements_addressed: [FLOW-01, FLOW-03, FLOW-04]
decision_coverage:
  - "D-07"
  - "D-08"
  - "D-09"
  - "D-10"
  - "D-11"
  - "D-12"
  - "D-13"
  - "D-18"
user_setup: []
must_haves:
  truths:
    - "FLOW-01: Browser evidence visits the selected route clusters rather than only the UI matrix."
    - "FLOW-03: Playwright proves keyboard/focus, command-palette, no-overflow, and narrow/mobile behavior for primary admin route clusters."
    - "FLOW-04: Browser evidence uses deterministic demo/matrix fixtures and does not require broader product seed semantics."
    - "D-10: Route-level Playwright evidence covers keyboard flow, focus order, command palette behavior, destructive-flow sequencing, and narrow viewport behavior across representative primary route clusters."
    - "D-08/D-09: Screenshots are generated artifacts through `testInfo.outputPath`, never checked-in baselines or `toHaveScreenshot` gates."
  artifacts:
    - path: "examples/demo/frontend/tests/admin-flow-ia.spec.ts"
      provides: "Route-level browser evidence for Phase 117."
      contains: "admin flow IA"
  key_links:
    - from: "examples/demo/frontend/tests/admin-flow-ia.spec.ts"
      to: "examples/demo/frontend/tests/support/admin.ts"
      via: "backendUrl helper"
      pattern: "backendUrl"
    - from: "examples/demo/frontend/tests/admin-flow-ia.spec.ts"
      to: "/admin/flags route cluster"
      via: "page.goto with DEMO_BACKEND_URL"
      pattern: "adminFlowRoutes"
---

<objective>
Add route-level Playwright evidence for Phase 117 browser-only concerns.

Purpose: Prove route flow, keyboard/focus behavior, command-palette reachability, mobile containment, and screenshot artifact generation across the selected route clusters.
Output: `examples/demo/frontend/tests/admin-flow-ia.spec.ts`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/117-page-flow-ia-pass/117-CONTEXT.md
@.planning/phases/117-page-flow-ia-pass/117-RESEARCH.md
@.planning/phases/117-page-flow-ia-pass/117-VALIDATION.md
@.planning/phases/117-page-flow-ia-pass/117-PATTERNS.md
@.planning/phases/117-page-flow-ia-pass/117-UI-SPEC.md
@examples/demo/frontend/tests/brand-ui-evidence.spec.ts
@examples/demo/frontend/tests/ui-matrix.spec.ts
@examples/demo/frontend/tests/support/admin.ts
@examples/demo/frontend/playwright.config.ts
@examples/demo/backend/lib/rulestead_demo_web/router.ex
@rulestead_admin/lib/rulestead_admin/router.ex
@rulestead_admin/lib/rulestead_admin/components/shell.ex
@rulestead_admin/lib/rulestead_admin/navigation.ex
</context>

<tasks>

<task id="117-02-01" type="auto" tdd="true">
  <name>Task 1: Create the route-flow screenshot and containment spec</name>
  <files>examples/demo/frontend/tests/admin-flow-ia.spec.ts</files>
  <read_first>
    @.planning/phases/117-page-flow-ia-pass/117-CONTEXT.md
    @.planning/phases/117-page-flow-ia-pass/117-VALIDATION.md
    @.planning/phases/117-page-flow-ia-pass/117-PATTERNS.md
    @.planning/phases/117-page-flow-ia-pass/117-UI-SPEC.md
    @examples/demo/frontend/tests/brand-ui-evidence.spec.ts
    @examples/demo/frontend/tests/ui-matrix.spec.ts
    @examples/demo/frontend/tests/support/admin.ts
    @examples/demo/frontend/playwright.config.ts
    @examples/demo/backend/lib/rulestead_demo_web/router.ex
    @rulestead_admin/lib/rulestead_admin/router.ex
  </read_first>
  <behavior>
    - Test 1: The spec signs in through `${backendUrl}/demo/sign-in`, then visits `/admin/flags`, `/admin/flags/flags?env=staging&view=all`, `/admin/flags/enable-new-dashboard/rules?env=staging`, `/admin/flags/enable-new-dashboard/kill?env=staging`, `/admin/flags/audiences?env=staging`, `/admin/flags/audit?env=staging`, `/admin/flags/enable-new-dashboard/explain?env=staging&targeting_key=support-user-42&tenant_key=acme`, and `/admin/flags/enable-new-dashboard/simulate?env=staging`.
    - Test 2: Each route renders `.rs-shell`, a visible main/page heading, no horizontal root overflow greater than 1px, and a screenshot artifact named with route, theme, and viewport.
    - Test 3: Theme cases include `light`, `dark`, and `system-dark`; viewport cases include `desktop` 1280x900 and `mobile` 390x844.
  </behavior>
  <action>Create `examples/demo/frontend/tests/admin-flow-ia.spec.ts`. Reuse the existing `backendUrl` import and the `openAdminSurface` pattern from `brand-ui-evidence.spec.ts`. Define `ViewportCase`, `ThemeCase`, `viewports`, `themes`, and an `adminFlowRoutes` array with route names `overview`, `inventory`, `rules`, `kill`, `audience`, `audit`, `explain`, and `simulate`. For each route/theme/viewport case, assert `.rs-shell` is visible, assert the page has no horizontal root overflow via the same `expectNoHorizontalOverflow` helper shape, assert at least one route-specific visible text or role from the route case, and write `flow-${route.name}-${theme.name}-${viewport.name}.png` through `page.screenshot({ fullPage: true, path: testInfo.outputPath(...) })`. Do not use `toHaveScreenshot`, `matchSnapshot`, `pixelmatch`, `visual-diff`, checked-in screenshots, Storybook, or PhoenixStorybook.</action>
  <verify>
    <automated>cd examples/demo/frontend && DEMO_BACKEND_URL=${DEMO_BACKEND_URL:-http://localhost:4061} npm run test:e2e -- admin-flow-ia.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `admin-flow-ia.spec.ts` imports `backendUrl` from `./support/admin`.
    - `adminFlowRoutes` contains the exact route names `overview`, `inventory`, `rules`, `kill`, `audience`, `audit`, `explain`, and `simulate`.
    - The spec defines desktop 1280x900 and mobile 390x844 viewport cases.
    - The spec defines light, dark, and system-dark theme cases.
    - Screenshots are written only through `testInfo.outputPath`.
    - Source does not contain `toHaveScreenshot`, `matchSnapshot`, `pixelmatch`, `visual-diff`, `pixel-baseline`, `Storybook`, or `PhoenixStorybook`.
  </acceptance_criteria>
  <done>Primary route clusters have browser evidence for shell rendering, route reachability, screenshot artifacts, and mobile containment.</done>
</task>

<task id="117-02-02" type="auto" tdd="true">
  <name>Task 2: Add keyboard, focus, command-palette, and forbidden-tooling checks</name>
  <files>examples/demo/frontend/tests/admin-flow-ia.spec.ts</files>
  <read_first>
    @.planning/phases/117-page-flow-ia-pass/117-CONTEXT.md
    @.planning/phases/117-page-flow-ia-pass/117-RESEARCH.md
    @.planning/phases/117-page-flow-ia-pass/117-PATTERNS.md
    @.planning/phases/117-page-flow-ia-pass/117-UI-SPEC.md
    @examples/demo/frontend/tests/admin-flow-ia.spec.ts
    @examples/demo/frontend/tests/ui-matrix.spec.ts
    @examples/demo/frontend/tests/brand-ui-evidence.spec.ts
    @rulestead_admin/lib/rulestead_admin/components/shell.ex
    @rulestead_admin/lib/rulestead_admin/navigation.ex
  </read_first>
  <behavior>
    - Test 1: The command palette trigger, `#rs-cmdk`, `#rs-cmdk-input`, and at least one `#rs-cmdk [role=option]` are present; option keywords include `audit` and `audiences`.
    - Test 2: A keyboard path through the kill-switch route reaches the state, action, and after-action context sections without root overflow.
    - Test 3: The spec source rejects forbidden visual-baseline and Storybook terms.
  </behavior>
  <action>Extend `admin-flow-ia.spec.ts` with focused tests for route-level browser-only concerns. Add a command-palette test on the overview route that asserts `.rs-shell__search`, `#rs-cmdk`, `#rs-cmdk-input`, and `#rs-cmdk [role=option]` exist, and assert option keywords include `audit` and `audiences` so the palette derives from the grouped navigation source. Add a destructive-flow keyboard/focus test on `/admin/flags/enable-new-dashboard/kill?env=staging` that verifies visible sections with accessible names `Kill switch state`, `Engage override` or `Release override`, and `After-action context`, then tabs through focusable controls and asserts focus does not leave the document body or jump into hidden command-palette controls. Add a source guard that reads `admin-flow-ia.spec.ts` and refutes the forbidden terms from the behavior block. Keep all route interactions semantic; do not add DOM helper code outside this test file.</action>
  <verify>
    <automated>cd examples/demo/frontend && DEMO_BACKEND_URL=${DEMO_BACKEND_URL:-http://localhost:4061} npm run test:e2e -- admin-flow-ia.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The spec asserts command-palette trigger, dialog root, input, and options on a real admin route.
    - The spec asserts command-palette option keywords include both `audit` and `audiences`.
    - The spec includes a kill-switch keyboard/focus test that references `Kill switch state` and `After-action context`.
    - The source guard refutes forbidden terms without using `grep -c` gates that count comments.
    - The spec still writes generated screenshot artifacts only to Playwright output paths.
  </acceptance_criteria>
  <done>FLOW-03 has route-level keyboard, focus, command-palette, destructive sequencing, and forbidden-tooling evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Browser to mounted admin | Playwright drives untrusted browser interactions against the mounted admin routes. |
| Test source to CI gate | The new spec can accidentally introduce visual-baseline or broad tooling requirements. |
| Command palette to navigation | Palette options must remain derived from the grouped navigation source, not ad hoc route lists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-117-05 | Denial of Service | mobile/narrow route layout | mitigate | Each route case asserts root horizontal overflow is <= 1px on mobile and desktop. |
| T-117-06 | Tampering | screenshot evidence posture | mitigate | Screenshots are generated through `testInfo.outputPath`; source guard rejects snapshot/pixel-baseline tooling. |
| T-117-07 | Spoofing | command-palette route options | mitigate | Tests assert command-palette options exist and include grouped navigation keywords such as audit and audiences. |
| T-117-08 | Elevation of privilege | destructive flow route | mitigate | Browser evidence checks sequencing and focus only; it does not replace server-side capability checks or validation. |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are planned; use existing `@playwright/test` and project helpers only. |
</threat_model>

<verification>
1. `test -f examples/demo/frontend/tests/admin-flow-ia.spec.ts`
2. `rg -n "adminFlowRoutes|overview|inventory|rules|kill|audience|audit|explain|simulate|testInfo.outputPath|rs-cmdk|Kill switch state|After-action context" examples/demo/frontend/tests/admin-flow-ia.spec.ts`
3. `cd examples/demo/frontend && DEMO_BACKEND_URL=${DEMO_BACKEND_URL:-http://localhost:4061} npm run test:e2e -- admin-flow-ia.spec.ts`
4. `git diff --check`
</verification>

<success_criteria>
- The new Playwright spec covers the selected route set across light, dark, system-dark, desktop, and mobile.
- The spec proves route shell rendering, no root overflow, command-palette reachability, destructive-flow focus order, and screenshot artifact generation.
- The spec stays within existing dependencies and rejects visual-baseline tooling in source.
- FLOW-01, FLOW-03, and FLOW-04 have executable browser evidence.
</success_criteria>

<output>
Create `.planning/phases/117-page-flow-ia-pass/117-02-SUMMARY.md` when done.
</output>
