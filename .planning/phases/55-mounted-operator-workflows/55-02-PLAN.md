---
phase: 55-mounted-operator-workflows
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/audience_live/edit_preview.ex
  - rulestead_admin/lib/rulestead_admin/live/audience_live/edit_confirm.ex
  - rulestead_admin/lib/rulestead_admin/live/audience_live/archive_preview.ex
  - rulestead_admin/lib/rulestead_admin/live/audience_live/archive_confirm.ex
  - rulestead_admin/lib/rulestead_admin/live/audience_live/delete_preview.ex
  - rulestead_admin/lib/rulestead_admin/components/audience_components.ex
  - rulestead_admin/test/rulestead_admin/live/audience_live/edit_preview_test.exs
  - rulestead_admin/test/rulestead_admin/live/audience_live/archive_confirm_test.exs
  - rulestead_admin/test/rulestead_admin/live/audience_live/delete_preview_test.exs
requirements:
  - ADM-02
autonomous: true
must_haves:
  truths:
    - "Audience edit and archive flows use route-backed preview → confirm → audit with core-issued `audprev_*` fingerprints."
    - "Stale or missing preview redirects to preview with `?drifted=true` and drift copy; confirm requires non-empty reason."
    - "Delete preview is fail-closed with no apply CTA and explicit copy that delete is unsupported."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/audience_live/edit_confirm.ex"
      provides: "Apply update with preview_fingerprint threading"
    - path: "rulestead_admin/lib/rulestead_admin/live/audience_live/archive_confirm.ex"
      provides: "Apply archive mutation"
    - path: "rulestead_admin/lib/rulestead_admin/live/audience_live/delete_preview.ex"
      provides: "Fail-closed delete education surface"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/audience_live/edit_confirm.ex"
      to: "Rulestead.apply_audience_mutation/1"
      via: "preview_fingerprint + preview_schema_version from query params"
      pattern: "preview_fingerprint"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-55-03 | TOCTOU bypass via client-recomputed preview signature | high | Thread core `preview_fingerprint` and `preview_schema_version` only; redirect on stale |
| T-55-04 | Destructive archive without audit reason | medium | Require non-whitespace reason before submit; core enforces on apply |
| T-55-05 | False delete affordance | medium | Delete preview has no submit; copy states archive path |
</threat_model>

<objective>
Implement mounted audience mutation workflows (ADM-02): preview, confirm, drift handling, and fail-closed delete preview.

Purpose: Operators must not mutate audiences without impact evidence and audit trail.
Output: Five mutation LiveViews, `AudienceComponents.impact_preview`, and LiveView tests for edit/archive/delete paths.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-mounted-operator-workflows/55-CONTEXT.md
@.planning/phases/55-mounted-operator-workflows/55-UI-SPEC.md
@rulestead_admin/lib/rulestead_admin/live/flag_live/cleanup_preview.ex
@rulestead_admin/lib/rulestead_admin/live/flag_live/cleanup_confirm.ex
</context>

<source_audit>
GOAL: preview → confirm → audit for audience mutations — covered.
REQ ADM-02: covered by edit/archive preview+confirm, delete fail-closed, drift and missing preview copy.
CONTEXT D-02, D-04, D-05, D-06, D-07: covered.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Align edit/archive preview and confirm LiveViews to UI-SPEC and core contract</name>
  <read_first>
    - rulestead_admin/lib/rulestead_admin/live/audience_live/edit_preview.ex
    - rulestead_admin/lib/rulestead_admin/live/audience_live/edit_confirm.ex
    - rulestead_admin/lib/rulestead_admin/live/audience_live/archive_preview.ex
    - rulestead_admin/lib/rulestead_admin/live/audience_live/archive_confirm.ex
    - rulestead_admin/lib/rulestead_admin/live/audience_live/shared.ex
    - rulestead/lib/rulestead/targeting/impact_preview.ex
    - .planning/phases/55-mounted-operator-workflows/55-UI-SPEC.md
  </read_first>
  <action>
    Edit preview kicker `Audience preview`, summary `Review impact evidence before confirming an audience update.` Archive preview kicker `Archive preview`. Confirm pages: edit kicker `Audience confirm`, summary `Apply an audience update only after reviewing preview evidence and entering a reason.` Archive confirm kicker `Archive confirm`, summary `Archive only after preview evidence and an operator reason are recorded.` Primary CTAs: `Apply update`, `Apply archive`. Reason label `Reason (required)`. Missing preview message `Run impact preview before confirming.` Missing reason `Reason is required.` Drift: `FlagComponents.callout` title `Preview refreshed`, body `Authored state changed since preview — review the latest impact evidence.` Confirm `handle_params` requires query params `preview_fingerprint` and `preview_schema_version` from preview link; if absent redirect to preview. On `Rulestead.apply_audience_mutation` `{:error, %{type: :stale_preview}}` (or equivalent), redirect to preview with `drifted=true`. Success flash: `Audience update applied.` / `Audience archived.`
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/edit_preview_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `edit_preview_test.exs` exits 0
    - Confirm URL from preview includes `preview_fingerprint=audprev_`
    - `edit_confirm.ex` contains `Reason (required)`
    - `edit_preview.ex` page_kicker is `Audience preview` or `Audience impact preview` per UI-SPEC section mapping
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fail-closed delete preview and archive confirm tests</name>
  <read_first>
    - rulestead_admin/lib/rulestead_admin/live/audience_live/delete_preview.ex
    - rulestead_admin/lib/rulestead_admin/live/audience_live/archive_confirm.ex
    - rulestead_admin/test/rulestead_admin/live/audience_live/edit_preview_test.exs
  </read_first>
  <action>
    Delete preview: kicker `Delete preview`, summary `Delete is not supported. This preview shows the fail-closed outcome operators would see.`, body `Audience delete is not available in mounted admin. Use archive when you need to retire an audience.`, only link `Back to audience` — no submit button. Add `delete_preview_test.exs` asserting no `Apply` or `type="submit"` for delete. Add `archive_confirm_test.exs`: seed audience+flag, run archive preview, follow confirm link with fingerprint, submit reason `retire vip`, assert flash contains `Audience archived.` and audience has `archived_at` in Fake state.
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/delete_preview_test.exs test/rulestead_admin/live/audience_live/archive_confirm_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Both new test files exist and exit 0
    - `delete_preview.ex` render has no `Apply delete` or submit for delete
    - `delete_preview.ex` includes `Audience delete is not available in mounted admin`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Edit confirm without fingerprint redirects to preview
- [ ] Archive path records reason in audit (inspect Fake audit events if exposed in test)
- [ ] Protected-environment governance errors surface core message in LiveView (match existing OperatorComponents pattern)
</verification>
