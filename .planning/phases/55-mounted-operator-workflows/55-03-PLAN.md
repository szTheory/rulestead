---
phase: 55-mounted-operator-workflows
plan: 03
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/flag_live/explain.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex
  - rulestead_admin/lib/rulestead_admin/components/rule_editor_components.ex
  - rulestead_admin/lib/rulestead_admin/components/simulate_components.ex
  - rulestead_admin/lib/rulestead_admin/components/audience_trace_components.ex
  - rulestead/lib/rulestead/explainer.ex
  - rulestead_admin/test/rulestead_admin/live/flag_live/explain_test.exs
  - rulestead_admin/test/rulestead_admin/live/flag_live/rules_test.exs
requirements:
  - ADM-03
autonomous: true
must_haves:
  truths:
    - "Flag explain LiveView at `/:key/explain` supports support-safe permalinks and renders audience trace steps."
    - "Rules workspace shows audience library, missing-reference copy, and links to audience detail."
    - "Simulate surfaces `Audience targeting` trace section with matched/missed/missing/archived statuses."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/explain.ex"
      provides: "Decision explainer with permalink push_patch"
    - path: "rulestead_admin/lib/rulestead_admin/components/audience_trace_components.ex"
      provides: "Structured audience_trace_steps"
    - path: "rulestead/lib/rulestead/explainer.ex"
      provides: "Optional CLI audience sentences"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/explain.ex"
      to: "Rulestead.explain_flag/2 or Rulestead.simulate_flag/2"
      via: "debug_trace with audience_trace on rule_traces"
      pattern: "audience_trace"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-55-06 | PII in explain permalinks | high | Query params limited to env, tenant, targeting_key, session_id, request_id — never traits |
| T-55-07 | Support confusion from raw trace blobs | low | Human labels matched/missed/missing from snapshot/archived only |
</threat_model>

<objective>
Carry reusable audience context through flag rules, simulate, and explain surfaces (ADM-03).

Purpose: Support and operators need one-click decision explanation with audience trace clarity.
Output: Explain LiveView, trace components on simulate/rules, explainer CLI parity, and tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-mounted-operator-workflows/55-CONTEXT.md
@.planning/phases/55-mounted-operator-workflows/55-UI-SPEC.md
@rulestead_admin/lib/rulestead_admin/live/flag_live/simulate.ex
</context>

<source_audit>
GOAL: rules/simulate/explain audience context — covered.
REQ ADM-03: covered.
CONTEXT D-11, D-12, D-13: covered.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Explain LiveView, permalinks, and audience trace rendering</name>
  <read_first>
    - rulestead_admin/lib/rulestead_admin/live/flag_live/explain.ex
    - rulestead_admin/lib/rulestead_admin/components/audience_trace_components.ex
    - rulestead_admin/lib/rulestead_admin/live/session.ex
    - rulestead/lib/rulestead/explainer.ex
    - rulestead_admin/test/rulestead_admin/live/flag_live/explain_test.exs
    - .planning/phases/55-mounted-operator-workflows/55-UI-SPEC.md
  </read_first>
  <action>
  Ensure `FlagLive.Explain` uses page_kicker `Decision explainer`, summary `Support-safe permalink for why a flag evaluated the way it did, including reusable audience steps.`, primary CTA `Run explain`, helper `Share this URL with support — it includes flag, environment, tenant, and targeting key only.` `push_patch` on run updates only allowed query keys. Render `AudienceTraceComponents.audience_trace_steps/1` from `debug_trace.rule_traces` audience_trace maps. Status words: `matched`, `missed`, `missing from snapshot`, `archived`. Empty state `No audience steps — rules on this flag do not reference reusable audiences.` Extend `Rulestead.Explainer.audience_trace_summary/1` to append one sentence when audience steps exist (for CLI parity). Add test asserting HTML contains `Audience targeting` or trace step with `vip-users` and permalink URL lacks `email` or trait keys.
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/flag_live/explain_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `explain_test.exs` exits 0
    - `explain.ex` contains `Decision explainer`
    - `explain_test` refutes `email=` in patched URL when present in form params
    - `audience_trace_components.ex` defines `audience_trace_steps`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rules and simulate audience affordances</name>
  <read_first>
    - rulestead_admin/lib/rulestead_admin/components/rule_editor_components.ex
    - rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex
    - rulestead_admin/lib/rulestead_admin/components/simulate_components.ex
    - rulestead_admin/test/rulestead_admin/live/flag_live/rules_test.exs
  </read_first>
  <action>
    Rules: audience library title `Audience library`; missing segment copy `Audience not found in snapshot — pick another audience or remove the reference before publish.`; link text `View audience {key}` pointing to `{mount_path}/audiences/{key}`. Simulate: section title `Audience targeting`; per-rule empty `no reusable audience on this rule`. Wire `SimulateComponents` to render `AudienceTraceComponents` when simulation result includes `audience_trace` on traces. Flag show page links to `/{key}/explain` with visible text `Explain decision`. Extend rules_test to assert missing-audience copy when segment_match references unknown key.
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/flag_live/rules_test.exs --only line:233 2>/dev/null || cd rulestead_admin && mix test test/rulestead_admin/live/flag_live/rules_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rule_editor_components.ex` contains `Audience not found in snapshot`
    - `simulate_components.ex` contains `Audience targeting`
    - `rules_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Explain page with segment_match rule shows at least one audience trace step
- [ ] Rules workspace link href matches `/audiences/{key}` pattern
</verification>
