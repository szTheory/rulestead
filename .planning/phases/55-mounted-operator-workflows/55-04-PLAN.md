---
phase: 55-mounted-operator-workflows
plan: 04
type: execute
wave: 3
depends_on:
  - 55-01
  - 55-02
  - 55-03
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex
  - rulestead_admin/test/rulestead_admin/live/environment_compare_live/index_test.exs
  - rulestead_admin/test/rulestead_admin/live/environment_compare_live/show_test.exs
  - rulestead/lib/mix/tasks/verify.phase55.ex
  - rulestead/test/rulestead/release_contract_test.exs
  - .planning/phases/55-mounted-operator-workflows/55-HANDOFF-CHECKLIST.md
requirements:
  - ADM-04
autonomous: true
must_haves:
  truths:
    - "Compare index and show render `dependency_findings` with scoped links to flags and audiences; stale compare shows banner without Apply/Publish."
    - "`mix verify.phase55` runs core dependency/impact contracts plus mounted admin workflow tests."
    - "Phase 55 handoff checklist documents core-vs-mounted boundaries for Phase 56."
  artifacts:
    - path: "rulestead/lib/mix/tasks/verify.phase55.ex"
      provides: "Merge-gate for Phase 55"
    - path: ".planning/phases/55-mounted-operator-workflows/55-HANDOFF-CHECKLIST.md"
      provides: "Support truth boundary for Phase 56"
    - path: "rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex"
      provides: "Per-flag audience dependency findings section"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex"
      to: "compare.dependency_findings"
      via: "Rulestead.compare_environments result"
      pattern: "dependency_findings"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-55-08 | Accidental publish from compare UI | high | No Apply/Publish controls; tests assert absence |
| T-55-09 | Leaking audience keys in compare when policy denies | medium | Render only findings core returns; link when audience_key present |
</threat_model>

<objective>
Finish compare dependency findings presentation (ADM-04), phase verification task, and Phase 56 handoff checklist.

Purpose: Promotion/compare operators must see audience blockers with actionable links without a standalone control plane.
Output: Compare UI polish, extended `verify.phase55`, handoff doc, and compare regression tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-mounted-operator-workflows/55-CONTEXT.md
@.planning/phases/54-dependency-truth-and-promotion-safety/54-HANDOFF-CHECKLIST.md
@.planning/phases/55-mounted-operator-workflows/55-VALIDATION.md
</context>

<source_audit>
GOAL: compare/promotion/manifest audience findings without standalone admin — covered (manifest via core + compare only per D-15).
REQ ADM-04: covered.
CONTEXT D-14, D-15, D-16: covered.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Compare dependency findings UI-SPEC alignment and tests</name>
  <read_first>
    - rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex
    - rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex
    - rulestead_admin/test/rulestead_admin/live/environment_compare_live/index_test.exs
    - rulestead_admin/test/rulestead_admin/live/environment_compare_live/show_test.exs
    - .planning/phases/55-mounted-operator-workflows/55-UI-SPEC.md
  </read_first>
  <action>
    Index section title `Audience dependencies for this flag` on show (or index list section per UI-SPEC). Empty copy `No audience dependency findings for this flag in the current compare.` Blocker row pattern visible: severity, code, message, audience link. Stale banner title `Staleness conflict`, body `This preview is stale. Re-run compare before any later governed apply handoff.` Links: `dependency_audience_path/2` and flag paths include `env` and `tenant` query params. Assert tests: index renders finding with `vip-users` audience link; show per-flag section lists finding; HTML does not contain `Apply` or `Publish` as button text on compare pages (preserve existing forbid tests).
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/environment_compare_live/index_test.exs test/rulestead_admin/live/environment_compare_live/show_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Compare tests exit 0
    - `show.ex` contains `Audience dependencies` or UI-SPEC section title
    - Compare HTML from tests does not match `>Apply<` or `>Publish<` button patterns for inline apply
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend mix verify.phase55 and add 55-HANDOFF-CHECKLIST</name>
  <read_first>
    - rulestead/lib/mix/tasks/verify.phase55.ex
    - rulestead/lib/mix/tasks/verify.phase54.ex
    - .planning/phases/54-dependency-truth-and-promotion-safety/54-HANDOFF-CHECKLIST.md
    - rulestead/test/rulestead/release_contract_test.exs
  </read_first>
  <action>
    Update `Mix.Tasks.Verify.Phase55` to run, in order: existing core tests in `@phase55_tests`, then admin tests: `../rulestead_admin/test/rulestead_admin/live/audience_live`, `../rulestead_admin/test/rulestead_admin/live/flag_live/explain_test.exs`, `../rulestead_admin/test/rulestead_admin/router_test.exs`, `../rulestead_admin/test/rulestead_admin/live/environment_compare_live/index_test.exs`. Use `Mix.Task.run("cmd", ["cd", admin_dir, "&&", "mix", "test", paths])` or equivalent repo-local pattern from other verify tasks. Create `.planning/phases/55-mounted-operator-workflows/55-HANDOFF-CHECKLIST.md` mirroring 54 checklist sections: core truth boundary, scope semantics, fail-closed, redaction, mounted presentation-only, with checkbox `Phase 55 plans reference 54-HANDOFF` and `mix verify.phase55` green. Add release_contract assertion that `rulestead` package files do not reference `RulesteadAdmin` module.
  </action>
  <verify>
    <automated>cd rulestead && mix verify.phase55</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.phase55` exits 0
    - `55-HANDOFF-CHECKLIST.md` exists under phase directory
    - `verify.phase55.ex` invokes rulestead_admin test paths
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd rulestead && mix verify.phase55` green
- [ ] `cd rulestead_admin && mix test` green
- [ ] 55-HANDOFF-CHECKLIST committed in phase dir
</verification>
