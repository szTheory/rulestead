---
phase: 55-mounted-operator-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/admin/dependency_visibility.ex
  - rulestead/lib/rulestead.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/fake.ex
  - rulestead_admin/lib/rulestead_admin/router.ex
  - rulestead_admin/lib/rulestead_admin/live/audience_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/audience_live/show.ex
  - rulestead_admin/lib/rulestead_admin/live/audience_live/shared.ex
  - rulestead_admin/lib/rulestead_admin/components/audience_components.ex
  - rulestead_admin/test/rulestead_admin/live/audience_live/index_test.exs
  - rulestead_admin/test/rulestead_admin/router_test.exs
requirements:
  - ADM-01
autonomous: true
must_haves:
  truths:
    - "Mounted `/audiences` list and detail screens show policy-aware reference counts, used-by tables with environment/tenant scope, lifecycle context, and rollout hints from core inventory."
    - "Audience routes register before `/:key` catch-all; env and tenant travel via Session query params."
    - "Used-by rendering never leaks redacted flag keys; denied reads show explicit operator copy."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/audience_live/index.ex"
      provides: "Audience library list with UI-SPEC copy"
    - path: "rulestead_admin/lib/rulestead_admin/live/audience_live/show.ex"
      provides: "Audience detail with governed action links"
    - path: "rulestead_admin/lib/rulestead_admin/components/audience_components.ex"
      provides: "Policy-aware used_by_table"
    - path: "rulestead/lib/rulestead/admin/dependency_visibility.ex"
      provides: "visibility_resolver for list_audience_dependencies"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/audience_live/show.ex"
      to: "Rulestead.list_audience_dependencies/1"
      via: "Shared.load_dependencies/2 with actor from socket"
      pattern: "hidden_reference_count"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-55-01 | Info disclosure via used-by table showing unauthorized flag keys | high | `DependencyVisibility.visibility_resolver/1` + redacted placeholders; tests with DenyFlagReadsPolicy |
| T-55-02 | Cross-tenant scope confusion in dependency rows | medium | Every row renders `environment_key` and `tenant_key`; scope opts on list calls |
</threat_model>

<objective>
Ship mounted audience library and detail surfaces (ADM-01) that render Phase 54 dependency inventory truth with policy-aware partial visibility.

Purpose: Operators need scannable audience IA before mutation or explain workflows.
Output: Routes, Index/Show LiveViews, `AudienceComponents.used_by_table`, core visibility resolver wiring, and list/detail tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-mounted-operator-workflows/55-CONTEXT.md
@.planning/phases/55-mounted-operator-workflows/55-UI-SPEC.md
@.planning/phases/55-mounted-operator-workflows/55-RESEARCH.md
@.planning/phases/54-dependency-truth-and-promotion-safety/54-HANDOFF-CHECKLIST.md
@prompts/rulestead-admin-ux-and-operator-ia.md

<boundary>
Presentation only in `rulestead_admin`. Core owns `list_audience_dependencies` redaction envelope. No Repo in LiveView.
</boundary>
</context>

<source_audit>
GOAL: policy-aware audience list/detail with used-by tables — covered.
REQ ADM-01: covered by index, show, used_by_table, lifecycle/rollout hints from inventory entries.
CONTEXT D-01, D-03, D-08, D-09, D-10: covered.
RESEARCH: route order, visibility_resolver, UI-SPEC copy.
Deferred: graph visualizer, bulk automation.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Wire core dependency visibility resolver into list_audience_dependencies</name>
  <read_first>
    - rulestead/lib/rulestead/admin/dependency_visibility.ex
    - rulestead/lib/rulestead/targeting/dependency_inventory.ex
    - rulestead/lib/rulestead/admin/redaction.ex
    - rulestead/test/rulestead/admin/dependency_visibility_test.exs
    - rulestead/test/rulestead/admin_security_contract_test.exs
  </read_first>
  <action>
    Ensure `Rulestead.list_audience_dependencies/1` passes `visibility_resolver: Rulestead.Admin.DependencyVisibility.visibility_resolver(actor)` (or equivalent) into inventory projection for both `Rulestead.Fake` and `Rulestead.Store.Ecto`. Resolver must return `false` when `Authorizer.authorize(actor, :read_flags, %{resource_type: :flag, resource_key: flag_key}, environment_key)` is not `:ok`. Redacted entries use `flag_key: "[REDACTED]"` or empty visible key with `visibility.reason: "policy_denied"` — never the real flag key.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/admin/dependency_visibility_test.exs test/rulestead/admin_security_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `dependency_visibility_test.exs` exits 0
    - `grep -n "visibility_resolver" rulestead/lib/rulestead/store/ecto.ex rulestead/lib/rulestead/fake.ex` returns at least one match per file
    - `Rulestead.Admin.DependencyVisibility` exports `visibility_resolver/1`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Complete audience index/show LiveViews and used_by_table UI-SPEC alignment</name>
  <read_first>
    - rulestead_admin/lib/rulestead_admin/live/audience_live/index.ex
    - rulestead_admin/lib/rulestead_admin/live/audience_live/show.ex
    - rulestead_admin/lib/rulestead_admin/live/audience_live/shared.ex
    - rulestead_admin/lib/rulestead_admin/components/audience_components.ex
    - rulestead_admin/lib/rulestead_admin/router.ex
    - .planning/phases/55-mounted-operator-workflows/55-UI-SPEC.md
  </read_first>
  <action>
    Align copy to UI-SPEC: index empty state `No audiences in this scope` (not `No audiences found`); row link text `Open audience`; show page kicker `Audience detail`; used-by denied `Dependency list unavailable — you do not have permission to view audience dependencies in this scope.`; hidden count `At least {N} references are hidden by your permissions.`; empty used-by `No authored references in this environment and tenant scope.` Register routes in router **before** `live("/:key", ...)`: `/audiences`, `/audiences/:audience_key`, and mutation preview routes stubs if missing. Show summary includes owner from audience map when present, ruleset_status/rollout_context from dependency inventory summary when returned. Link row keys to `/audiences/:key` with env/tenant query via `AudienceLive.Shared.path/2`.
  </action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/index_test.exs test/rulestead_admin/router_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `index_test.exs` exits 0
    - `router_test.exs` exits 0 and asserts `/audiences` route ordering before `/:key`
    - `index.ex` render includes `No audiences in this scope`
    - `audience_components.ex` includes `Hidden reference` for redacted flag keys
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd rulestead_admin && mix test test/rulestead_admin/live/audience_live/index_test.exs`
- [ ] Policy-denied fixture shows hidden reference copy without real flag key in HTML
- [ ] Visible fixture shows `environment_key` and `tenant_key` columns in used-by table
</verification>
