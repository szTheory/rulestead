---
phase: 06-admin-ui-flag-list-detail-rule-editor-environments-lifecycle
plan: 05
type: execute
wave: 4
depends_on:
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - rulestead/lib/rulestead.ex
  - rulestead/lib/rulestead/store.ex
  - rulestead/lib/rulestead/store/command.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/fake.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex
  - rulestead_admin/lib/rulestead_admin/components/rule_editor_components.ex
  - rulestead_admin/test/rulestead_admin/live/flag_live/accessibility_test.exs
  - rulestead_admin/test/rulestead_admin/live/flag_live/rules_test.exs
  - rulestead_admin/test/rulestead_admin/integration/admin_mount_test.exs
  - rulestead_admin/README.md
  - rulestead/test/rulestead/integration/admin_lifecycle_runtime_test.exs
autonomous: true
requirements:
  - ADMIN-03
  - ADMIN-10
  - LIFE-01
  - LIFE-04
must_haves:
  truths:
    - "Operators can open a dedicated `/admin/flags/:key/rules` workspace to create, edit, reorder, and archive rules for the selected environment."
    - "The dedicated rules workspace includes audience/segment selection for reusable conditions per D-07, not just inline condition rows."
    - "Save draft and publish remain distinct actions and the UI makes that boundary explicit."
    - "Variant weight validation is live and blocks invalid 99/101 totals before save or publish."
    - "Automated accessibility proof covers the rules workspace so Phase 6 has scan coverage across list, detail, and rules pages."
    - "Archived flags are read-only in the workspace and the end-to-end proof covers both mountability and archived runtime exclusion."
  artifacts:
    - path: "rulestead/lib/rulestead.ex"
      provides: "Root public segment/audience listing seam the rules workspace can load"
    - path: "rulestead/lib/rulestead/store.ex"
      provides: "Store callback contract for reusable audience lookup in the rules workspace"
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex"
      provides: "Dedicated rules workspace LiveView"
    - path: "rulestead_admin/lib/rulestead_admin/components/rule_editor_components.ex"
      provides: "Condition builder, audience picker, variant editor, reorder handles, and draft/publish banners"
    - path: "rulestead_admin/test/rulestead_admin/live/flag_live/rules_test.exs"
      provides: "Behavioral proof for rule editing, audience selection, reordering, and weight validation"
    - path: "rulestead_admin/test/rulestead_admin/live/flag_live/accessibility_test.exs"
      provides: "Automated accessibility proof for the rules workspace to complete Phase 6 page coverage"
    - path: "rulestead_admin/test/rulestead_admin/integration/admin_mount_test.exs"
      provides: "Mounted-package proof for the Phase 6 route set and policy/env/session wiring"
    - path: "rulestead/test/rulestead/integration/admin_lifecycle_runtime_test.exs"
      provides: "Cross-package proof that archived flags stay read-only and excluded from evaluation"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex"
      to: "rulestead/lib/rulestead.ex"
      via: "workspace reuses save-draft, publish, archive, and audience-list verbs rather than inventing new mutation paths"
      pattern: "save_draft_ruleset|publish_ruleset|archive_flag|list_audiences"
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex"
      to: "rulestead_admin/lib/rulestead_admin/components/rule_editor_components.ex"
      via: "rule editor UI is composed from dedicated function components, an audience picker, and a small reorder hook"
      pattern: "rule_editor_components"
    - from: "rulestead_admin/test/rulestead_admin/integration/admin_mount_test.exs"
      to: "rulestead_admin/lib/rulestead_admin/router.ex"
      via: "mounted-route integration proof"
      pattern: "rulestead_admin"
    - from: "rulestead_admin/test/rulestead_admin/live/flag_live/accessibility_test.exs"
      to: "rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex"
      via: "automated accessibility scan covers the rules page"
      pattern: "axe|accessibility"
---

<objective>
Ship the dedicated rules workspace and close Phase 6 with end-to-end proof that the mounted admin package and the lifecycle/runtime boundaries work together.

Purpose: This is the highest-risk operator workflow in Phase 6. It needs its own workspace, explicit draft/publish boundary, and proof that the resulting lifecycle/archive behavior is honest across packages.
Output: Rules workspace LiveView, audience-picker loading, rule-editor components, workspace/a11y/integration tests, and a focused README update for the mounted package.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-ui-flag-list-detail-rule-editor-environments-lifecycle/06-CONTEXT.md
@prompts/rulestead-admin-ux-and-operator-ia.md
@prompts/rulestead-domain-language-field-guide.md
@prompts/phoenix-live-view-best-practices-deep-research.md
@rulestead/lib/rulestead.ex
@rulestead/lib/rulestead/store.ex
@rulestead/lib/rulestead/store/command.ex
@rulestead/lib/rulestead/ruleset.ex
@rulestead/lib/rulestead/ruleset/rule.ex
@rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex

<interfaces>
From `rulestead/lib/rulestead.ex`:
```elixir
@spec save_draft_ruleset(Command.SaveDraftRuleset.t()) :: Store.result(map())
@spec publish_ruleset(Command.PublishRuleset.t()) :: Store.result(map())
@spec archive_flag(Command.ArchiveFlag.t()) :: Store.result(map())
@spec list_audiences(keyword()) :: Store.result([map()])
```

From `rulestead/lib/rulestead/ruleset/rule.ex`:
```elixir
field(:strategy, Ecto.Enum, values: @strategies)
embeds_many(:conditions, Condition, on_replace: :delete)
embeds_many(:variants, Variant, on_replace: :delete)
embeds_one(:rollout, Rollout, on_replace: :update)
```

Phase 6 decisions to implement here:
- D-01 through D-04: draft-first editing with distinct save and publish actions.
- D-06 and D-07: serious authoring lives in `/admin/flags/:key/rules`, including reorder, conditions, audience/segment selection, and variant weights.
- Deferred ideas to exclude: simulation workbench, rollout controls, kill switch UI, rich audit wiring, autosaved persisted drafts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reusable audience loading and build the dedicated rules workspace with live validation</name>
  <files>rulestead/lib/rulestead.ex, rulestead/lib/rulestead/store.ex, rulestead/lib/rulestead/store/command.ex, rulestead/lib/rulestead/store/ecto.ex, rulestead/lib/rulestead/fake.ex, rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex, rulestead_admin/lib/rulestead_admin/components/rule_editor_components.ex, rulestead_admin/test/rulestead_admin/live/flag_live/rules_test.exs</files>
  <behavior>
    - Test 1: operators can create/edit/archive rules, reorder them, choose a reusable audience/segment for `:segment_match` rules, and persist the resulting ruleset as a draft for the selected environment.
    - Test 2: variant weight totals validate live and block save/publish when totals are not exactly 100.
    - Test 3: the workspace shows distinct `Save draft` and `Publish` actions, warns when a draft exists, becomes read-only for archived flags, and rejects saving a segment-match rule without a valid audience selection.
  </behavior>
  <action>Close the D-07 gap explicitly. Add the minimal core loading seam the workspace needs for reusable audience/segment targeting, preferably a root public `Rulestead.list_audiences/1` verb backed by store callbacks and fake/Ecto parity rather than adapter-private queries. Then implement ADMIN-03 in the dedicated `/rules` workspace per D-06 and D-07. Keep this workflow explicit: load one environment-scoped ruleset plus available audiences, edit in a dedicated page, and call the existing core `save_draft_ruleset`, `publish_ruleset`, and `archive_flag` verbs rather than inventing UI-local mutation paths. Use function components for the condition rows, audience picker, variant editor, lifecycle banners, and action bars. For reordering, use the smallest coherent LiveView approach: a tiny colocated hook or HTML5 drag helper that posts reordered keys back to the LiveView. Do not add a heavyweight frontend framework and do not sneak in simulation, rollout, or kill-switch controls from Phase 7.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/flag_live/rules_test.exs</automated>
  </verify>
  <done>The dedicated rules workspace can edit real rulesets safely, makes draft versus published state obvious, supports reusable audience targeting, and prevents invalid variant totals or archived mutations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add rules-page accessibility coverage, end-to-end mountability proof, and README updates</name>
  <files>rulestead_admin/test/rulestead_admin/live/flag_live/accessibility_test.exs, rulestead_admin/test/rulestead_admin/integration/admin_mount_test.exs, rulestead_admin/README.md, rulestead/test/rulestead/integration/admin_lifecycle_runtime_test.exs</files>
  <behavior>
    - Test 1: a host-style Phoenix test can mount the full Phase 6 route set through the one-line router macro and reach list/detail/rules screens under policy/env session wiring.
    - Test 2: the rules page passes an automated axe-core or equivalent accessibility scan, completing Phase 6 coverage for list, detail, and rules pages.
    - Test 3: archived flags remain read-only in admin-facing flows and stay excluded from evaluation in the core runtime integration proof, and `rulestead_admin/README.md` documents the real Phase 6 mount seam, policy requirement, and environment query-param model without advertising Phase 7 features.
  </behavior>
  <action>Close the phase with proof, not prose. Extend the accessibility test suite from 06-04 to scan the rules workspace as well, so the roadmap success criterion is fully met across list, detail, and rules pages. Add one integration test in `rulestead_admin` that mounts the package the way a host app will, and extend the core runtime/lifecycle integration test to cover the UI-facing archive/read-only invariant from LIFE-04. Update `rulestead_admin/README.md` narrowly: show the one-line mount macro, the required `policy:` seam, the URL-driven environment model, and the fact that rule targeting can reference reusable audiences. Keep the README honest about Phase 7 features still being absent and do not add any publish-readiness or Phase 8 release language.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/flag_live/accessibility_test.exs test/rulestead_admin/integration/admin_mount_test.exs && cd ../rulestead && mix test test/rulestead/integration/admin_lifecycle_runtime_test.exs</automated>
  </verify>
  <done>The mounted admin package is proven in a host-like integration test, accessibility proof covers the rules page, archive behavior is enforced across UI and runtime surfaces, and the package README matches the real Phase 6 surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator rule-edit input -> ruleset mutations | Untrusted rule/variant/condition input crosses into authoring writes |
| mounted UI -> core archive/runtime behavior | UI mutation flows must preserve the runtime archive invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | T | rules workspace reorder/edit flows | mitigate | Persist rule edits only through validated draft/publish commands and enforce live variant-weight validation before mutation. |
| T-06-14 | E | archived flag workspace access | mitigate | Make archived flags read-only in the workspace and prove the invariant in both UI and runtime integration tests. |
| T-06-15 | R | mounted package documentation | mitigate | Update `rulestead_admin/README.md` only with the real Phase 6 mount/policy/env contract so operators are not promised Phase 7 controls prematurely. |
</threat_model>

<verification>
Run the rules workspace tests, the rules-page accessibility scan, and the cross-package integration proof. Confirm the README documents only the shipped Phase 6 mount seam and that no deferred Phase 7 workflows appear in the workspace.
</verification>

<success_criteria>
Phase 6 ends with a real mounted rules workspace, reusable audience targeting, explicit draft/publish behavior, and end-to-end proof that archive/lifecycle and accessibility requirements hold across both packages.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-ui-flag-list-detail-rule-editor-environments-lifecycle/06-05-SUMMARY.md`
</output>
