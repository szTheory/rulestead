---
phase: 06-admin-ui-flag-list-detail-rule-editor-environments-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - rulestead/lib/rulestead.ex
  - rulestead/lib/rulestead/admin/stale_tracker.ex
  - rulestead/lib/rulestead/application.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/fake.ex
  - rulestead/test/rulestead/admin_test.exs
  - rulestead/test/rulestead/store_ecto_admin_test.exs
  - rulestead/test/rulestead/integration/admin_lifecycle_runtime_test.exs
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - LIFE-02
  - LIFE-03
  - LIFE-04
must_haves:
  truths:
    - "The admin package can fetch list and detail payloads with environment, owner, tag, lifecycle, stale filters, and cursor pagination from `rulestead`, not by composing raw Ecto queries in LiveViews."
    - "Lifecycle state appears in persisted payloads for both Ecto and fake adapters, including stale and potentially-stale classifications."
    - "Archived flags remain read-only and are excluded from runtime evaluation snapshots."
    - "Evaluation freshness updates flow from bounded telemetry into persisted `last_evaluated_at` data without adding DB work to the evaluation hot path."
    - "Create/update flag metadata operations run through `Rulestead` root verbs so the admin UI does not depend on a new accidental public `Rulestead.Admin` facade."
  artifacts:
    - path: "rulestead/lib/rulestead.ex"
      provides: "Root public Phase 6 admin verbs with adapter-agnostic pagination and metadata operations"
    - path: "rulestead/lib/rulestead/store/ecto.ex"
      provides: "Phase 6 admin read/write implementation over the authoring database"
    - path: "rulestead/lib/rulestead/fake.ex"
      provides: "Fake-adapter parity for admin list/detail/lifecycle workflows"
    - path: "rulestead/lib/rulestead/admin/stale_tracker.ex"
      provides: "Telemetry-driven freshness update path for stale classification inputs"
    - path: "rulestead/test/rulestead/integration/admin_lifecycle_runtime_test.exs"
      provides: "Proof that archived flags stay out of runtime evaluation and stale inputs update correctly"
  key_links:
    - from: "rulestead/lib/rulestead.ex"
      to: "rulestead/lib/rulestead/store/ecto.ex"
      via: "root public facade delegates to adapter implementations"
      pattern: "def (list_flags|fetch_flag|create_flag|update_flag|list_environments|record_)"
    - from: "rulestead/lib/rulestead/admin/stale_tracker.ex"
      to: "rulestead/lib/rulestead/application.ex"
      via: "application supervision attaches the telemetry-driven freshness tracker"
      pattern: "StaleTracker"
    - from: "rulestead/lib/rulestead/store/ecto.ex"
      to: "rulestead/lib/rulestead/runtime.ex"
      via: "archived filtering and freshness updates affect runtime-visible payloads"
      pattern: "archived_at|last_evaluated_at"
    - from: "rulestead/lib/rulestead/store/ecto.ex"
      to: "rulestead/lib/rulestead/fake.ex"
      via: "both adapters implement the same cursor/page contract so list navigation behaves identically in CI and production"
      pattern: "next_cursor|prev_cursor|has_more"
---

<objective>
Implement the Phase 6 core read/write models and lifecycle freshness wiring inside `rulestead` so the admin UI can stay thin and fake-backed.

Purpose: The UI plans should consume stable payloads, not invent core behavior. This plan turns the Phase 6 contracts into real Ecto/fake implementations and closes the runtime/lifecycle loop.
Output: Working root-facade implementations, stale tracker supervision, fake parity, pagination support, and integration proof around archived and stale behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-ui-flag-list-detail-rule-editor-environments-lifecycle/06-CONTEXT.md
@.planning/phases/05-host-app-seams-plug-liveview-oban-installer-test-helpers/05-03-SUMMARY.md
@rulestead/lib/rulestead/application.ex
@rulestead/lib/rulestead/store/ecto.ex
@rulestead/lib/rulestead/fake.ex
@rulestead/lib/rulestead/runtime.ex

<interfaces>
From `rulestead/lib/rulestead/application.ex`:
```elixir
children = [
  {RuntimeSupervisor, Config.runtime_options()}
]
```

From `rulestead/lib/rulestead/runtime.ex`:
```elixir
Telemetry.span(
  [:rulestead, :eval, :decide],
  Telemetry.metadata(Telemetry.base_metadata(flag_payload, context)),
  fn -> ...
```

Existing Ecto payload shape from `rulestead/lib/rulestead/store/ecto.ex`:
```elixir
%{
  flag: flag_summary(flag),
  environment: environment_summary(environment),
  flag_environment: flag_environment_summary(flag_environment),
  active_ruleset: ...,
  draft_rulesets: ...
}
```

Phase 6 decisions to honor here:
- D-01 through D-04: draft save and publish stay separate actions.
- D-18 through D-22: list surfaces need dense operator data, URL-backed filters, and current-environment defaults.
- D-09 through D-12: metadata create/update paths must preserve required owner plus expiration/permanent semantics for the form flow added in 06-04.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the Phase 6 root facade and adapter-backed list/detail/lifecycle payloads</name>
  <files>rulestead/lib/rulestead.ex, rulestead/lib/rulestead/store/ecto.ex, rulestead/lib/rulestead/fake.ex, rulestead/test/rulestead/admin_test.exs, rulestead/test/rulestead/store_ecto_admin_test.exs</files>
  <behavior>
    - Test 1: `Rulestead.list_flags/1` filters by environment, lifecycle state, owner, stale status, and tags, returns dense operator payloads with active/draft ruleset summaries, and emits cursor/page metadata for next/previous navigation.
    - Test 2: `Rulestead.fetch_flag/2` plus new root metadata verbs return the calm detail payload and save metadata for one flag/environment, including per-environment status, lifecycle card data, active ruleset, draft presence, recent-owner suggestions, and environment lists.
    - Test 3: create/update flag paths enforce read-only archived behavior and keep fake/Ecto parity for creation, metadata updates, and cursor pagination semantics.
  </behavior>
  <action>Implement the contracts from 06-01 in `Rulestead`, `Rulestead.Store.Ecto`, and `Rulestead.Fake`. Keep the admin package thin: enrich the list/detail payloads here with lifecycle state, stale indicators, per-environment summaries, active-versus-draft metadata, and page cursors so LiveViews render rather than reconstruct domain logic. Preserve D-01 through D-03 by reusing `save_draft_ruleset` and `publish_ruleset` as distinct operations; do not hide publish inside save flows. Implement create/update flag metadata verbs here as the public authoring surface per the Phase 6 pattern guidance, while keeping any `Rulestead.Admin` helper module internal-only if it exists. Replace the offset-only list behavior with a keyset-style or otherwise stable cursor contract that the list LiveView can serialize into URL state without page drift at operator scale. Stay fake-first and CI-friendly: every new Ecto behavior needs fake parity, and tests should target the shared facade before touching adapter-specific assertions.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/admin_test.exs test/rulestead/store_ecto_admin_test.exs</automated>
  </verify>
  <done>`rulestead` can serve the Phase 6 flag list/detail screens, lifecycle edits, and paginated inventory browsing through one shared root facade with fake/Ecto parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire telemetry-driven freshness updates and prove archived flags stay out of runtime evaluation</name>
  <files>rulestead/lib/rulestead/admin/stale_tracker.ex, rulestead/lib/rulestead/application.ex, rulestead/test/rulestead/integration/admin_lifecycle_runtime_test.exs</files>
  <behavior>
    - Test 1: the stale tracker subscribes to bounded evaluation telemetry and records `last_evaluated_at` without introducing synchronous store work into the evaluator call stack.
    - Test 2: a recently evaluated flag moves out of `:stale` or `:potentially_stale` according to the configured threshold.
    - Test 3: archived flags never appear in runtime evaluation snapshots, so `Rulestead.Runtime.enabled?/3` and related evaluation flows cannot resolve them.
  </behavior>
  <action>Create a small supervised freshness writer, e.g. `Rulestead.Admin.StaleTracker`, and add it to `Rulestead.Application`. The tracker should attach to the existing eval-stop telemetry, batch or debounce writes enough to avoid hot-path database coupling, and update only bounded freshness data (`environment_key`, `flag_key`, `last_evaluated_at`). Do not move authoring logic into runtime evaluation and do not add Phase 7 audit/security work here. Finish with an integration test that proves LIFE-04 end to end: archiving removes the flag from runtime snapshots and evaluation surfaces, while freshness updates still feed the lifecycle classifier used by Phase 6 UI pages.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/integration/admin_lifecycle_runtime_test.exs</automated>
  </verify>
  <done>Lifecycle freshness is fed by telemetry in a bounded way, and archived flags are provably excluded from runtime evaluation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluation telemetry -> stale tracker | High-volume runtime events cross into persisted lifecycle state updates |
| admin facade -> store adapters | Mounted UI calls into persistent authoring reads and writes through the core facade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | D | `Rulestead.Admin.StaleTracker` | mitigate | Use a supervised, bounded telemetry consumer that batches/debounces writes so evaluation traffic cannot block or flood the authoring store. |
| T-06-05 | T | adapter list/detail payloads | mitigate | Centralize lifecycle state and archive filtering in the `Rulestead` root facade plus adapter implementations so LiveViews cannot drift or bypass read-only archive rules. |
| T-06-06 | R | archived runtime behavior | mitigate | Add an integration proof that archived flags are omitted from runtime snapshots and evaluation lookups, not merely hidden in the admin list. |
</threat_model>

<verification>
Run the new facade, adapter, and runtime integration tests. Confirm the tracker uses existing telemetry boundaries and that no Phase 7 audit timeline or rollout behavior is introduced.
</verification>

<success_criteria>
`rulestead` now exposes the real data model and lifecycle behavior Phase 6 needs: list/detail payloads are ready, metadata mutations and pagination live on the root facade, stale inputs update from telemetry, and archived flags are removed from evaluation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-ui-flag-list-detail-rule-editor-environments-lifecycle/06-02-SUMMARY.md`
</output>
