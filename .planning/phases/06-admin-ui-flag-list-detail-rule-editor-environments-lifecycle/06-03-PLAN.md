---
phase: 06-admin-ui-flag-list-detail-rule-editor-environments-lifecycle
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - rulestead_admin/mix.exs
  - rulestead_admin/lib/rulestead_admin/application.ex
  - rulestead_admin/lib/rulestead_admin/router.ex
  - rulestead_admin/lib/rulestead_admin/live/session.ex
  - rulestead_admin/lib/rulestead_admin/components/shell.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex
  - rulestead_admin/test/test_helper.exs
  - rulestead_admin/test/support/conn_case.ex
  - rulestead_admin/test/rulestead_admin/router_test.exs
  - rulestead_admin/test/rulestead_admin/live/session_test.exs
autonomous: true
requirements:
  - ADMIN-08
  - ADMIN-10
must_haves:
  truths:
    - "A host Phoenix app can mount the admin UI with one router macro call and a host-supplied `Rulestead.Admin.Policy` module."
    - "The admin package treats `env` in the URL as canonical state and only uses remembered environment preference as a fallback."
    - "Production is visually explicit in the shared shell so environment state cannot become a hidden footgun."
    - "The package compiles and tests as a real LiveView package before list/detail/editor behavior is filled in, including route space for later `/new` and `/edit` flag metadata screens."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/router.ex"
      provides: "Mount macro and route/session wiring for `/admin/flags` surfaces"
    - path: "rulestead_admin/lib/rulestead_admin/live/session.ex"
      provides: "Policy checks, actor/env/session normalization, and shared mount helpers"
    - path: "rulestead_admin/lib/rulestead_admin/components/shell.ex"
      provides: "Shared admin chrome with environment picker and production emphasis"
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex"
      provides: "Compile-safe placeholder contract for the list screen"
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex"
      provides: "Compile-safe placeholder contract for the detail screen"
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex"
      provides: "Compile-safe placeholder contract for the rules workspace"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/router.ex"
      to: "rulestead/lib/rulestead/admin/policy.ex"
      via: "router macro accepts and forwards host policy module"
      pattern: "policy:"
    - from: "rulestead_admin/lib/rulestead_admin/live/session.ex"
      to: "rulestead_admin/lib/rulestead_admin/components/shell.ex"
      via: "URL env state and remembered fallback feed the shared shell"
      pattern: "env"
    - from: "rulestead_admin/lib/rulestead_admin/router.ex"
      to: "rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex"
      via: "mounted routes compile against explicit screen modules"
      pattern: "live .*FlagLive"
---

<objective>
Turn `rulestead_admin` from a compile-safe stub into a mountable Phoenix LiveView package with the shared shell, route/session wiring, and host-policy seam Phase 6 requires.

Purpose: The list, detail, and rules plans should build on a working mounted package, not invent package bootstrapping and policy/env handling in parallel.
Output: LiveView-capable package foundation, router macro, shared shell, placeholder screen modules, and test harness.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-ui-flag-list-detail-rule-editor-environments-lifecycle/06-CONTEXT.md
@.planning/phases/05-host-app-seams-plug-liveview-oban-installer-test-helpers/05-04-SUMMARY.md
@prompts/rulestead-admin-ux-and-operator-ia.md
@prompts/rulestead-brand-book.md
@prompts/rulestead-personas-jtbd-and-onboarding.md
@prompts/phoenix-live-view-best-practices-deep-research.md
@rulestead_admin/mix.exs
@rulestead_admin/lib/rulestead_admin/router.ex

<interfaces>
From `rulestead_admin/lib/rulestead_admin/router.ex`:
```elixir
defmacro rulestead_admin(path, opts \\ []) do
  quote bind_quoted: [path: path, opts: opts] do
    scope path, as: :rulestead_admin do
    end
  end
end
```

From `rulestead/lib/rulestead/admin/policy.ex` after 06-01:
```elixir
@callback can?(actor :: term(), action :: atom(), resource :: term(), environment_key :: String.t()) ::
            boolean()
```

Phase 6 decisions to implement here:
- D-13 through D-17: one global environment picker, `env` query param canonical, remembered env fallback only, no env-in-path routing, prod visually explicit.
- D-18 through D-22: admin shell should feel scanning-first and calm, not card-first or marketing-styled.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LiveView package wiring and a real test harness to `rulestead_admin`</name>
  <files>rulestead_admin/mix.exs, rulestead_admin/lib/rulestead_admin/application.ex, rulestead_admin/test/test_helper.exs, rulestead_admin/test/support/conn_case.ex</files>
  <action>Add the Phoenix/LiveView dependencies and application wiring needed for a real mountable package, but keep the package unpublished and aligned with the sibling-path dependency rule. Reuse the linked-version design from earlier phases; do not add any publish workflow or Phase 8 release-prep changes. Set up the minimum test harness needed for LiveView route/session tests inside `rulestead_admin`, keeping fake-backed reproducibility intact and avoiding a second copy of runtime/store setup logic.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin_test.exs test/rulestead_admin/router_test.exs</automated>
  </verify>
  <done>`rulestead_admin` is a real LiveView-capable sibling package with working test support, while remaining outside any publish flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the mount macro, shared session/env shell, and compile-safe screen modules</name>
  <files>rulestead_admin/lib/rulestead_admin/router.ex, rulestead_admin/lib/rulestead_admin/live/session.ex, rulestead_admin/lib/rulestead_admin/components/shell.ex, rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex, rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex, rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex, rulestead_admin/test/rulestead_admin/router_test.exs, rulestead_admin/test/rulestead_admin/live/session_test.exs</files>
  <behavior>
    - Test 1: `rulestead_admin "/flags", policy: MyApp.FlagsPolicy` expands to a mounted route set with the list/detail/rules screens and a host-policy-aware live session.
    - Test 2: the shared session helper resolves actor + environment with URL `env` as canonical state and remembered environment only as fallback.
    - Test 3: the shared shell renders a global environment picker with a visually explicit production state and compile-safe placeholders for the three Phase 6 screens.
  </behavior>
  <action>Replace the stub router macro with a real mount seam for ADMIN-10. Use a shared LiveView session module or on-mount helper to centralize host policy checks, actor lookup, environment normalization, and remembered-env fallback; do not scatter those concerns across every screen. Follow the LiveView best-practices prompt: route-backed pages, `handle_params`-driven URL state, function components by default, and minimal JS. The shell should carry the calm operator styling direction from the brand prompt and keep production visibly distinct per D-17. Create compile-safe placeholder LiveViews for list/detail/rules so the package mounts before later plans fill the screens in, and shape the router so 06-04 can add `/admin/flags/new` and `/admin/flags/:key/edit` without reworking the mount/session contract.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/router_test.exs test/rulestead_admin/live/session_test.exs</automated>
  </verify>
  <done>A host app can mount the admin package via one macro call, policy and environment state are centralized, and the package compiles with explicit screen contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host router/session -> mounted admin LiveViews | Host session and query-param input crosses into mounted admin screen state |
| host policy module -> admin route access | Authorization decisions are delegated outside the package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | S | mounted admin session helper | mitigate | Centralize actor + environment resolution in one shared mount helper and require explicit `policy:` configuration in the router macro. |
| T-06-08 | T | environment URL state | mitigate | Treat `env` as canonical, validate it against known environments, and use remembered environment only as a fallback so hidden state cannot override the URL. |
| T-06-09 | E | router macro | mitigate | Wire all Phase 6 routes through the shared policy-aware live session so no screen bypasses host authorization checks. |
</threat_model>

<verification>
Run the package routing/session tests and confirm the new mount seam keeps `rulestead_admin` unpublished, uses one-line router mounting, and creates no Phase 7 routes.
</verification>

<success_criteria>
`rulestead_admin` becomes a real mounted LiveView package with centralized policy/env handling and compile-safe screen shells for the remaining Phase 6 UI work.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-ui-flag-list-detail-rule-editor-environments-lifecycle/06-03-SUMMARY.md`
</output>
