---
phase: 06-admin-ui-flag-list-detail-rule-editor-environments-lifecycle
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - rulestead_admin/lib/rulestead_admin/router.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex
  - rulestead_admin/lib/rulestead_admin/components/flag_components.ex
  - rulestead_admin/test/rulestead_admin/live/flag_live/index_test.exs
  - rulestead_admin/test/rulestead_admin/live/flag_live/show_test.exs
  - rulestead_admin/test/rulestead_admin/live/flag_live/form_test.exs
  - rulestead_admin/test/rulestead_admin/live/flag_live/accessibility_test.exs
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-08
  - ADMIN-10
  - LIFE-01
  - LIFE-02
  - LIFE-03
must_haves:
  truths:
    - "Operators can browse a dense flag table scoped to the current environment with URL-backed search and filters for lifecycle, owner, tags, and stale status."
    - "Operators can move across paginated flag inventory pages without losing URL-backed filter and environment state."
    - "Operators can create a flag and edit flag metadata in the admin UI, with owner plus expected expiration or permanent required per D-09 through D-12."
    - "The detail page is a calm read surface that clearly distinguishes active ruleset versus draft ruleset for the selected environment."
    - "Lifecycle state, owner, expected expiration/permanent state, and stale warnings are visible in both list and detail surfaces."
    - "Automated accessibility checks pass on the list, detail, and metadata form screens before the rules workspace lands."
    - "The detail page links cleanly into the dedicated `/rules` workspace and shows only an audit placeholder, not Phase 7 audit wiring."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex"
      provides: "Flag list LiveView with URL-backed filter state, keyboard navigation, and cursor pagination controls"
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex"
      provides: "Flag detail LiveView with per-environment summary and active/draft distinction"
    - path: "rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex"
      provides: "Flag metadata create/edit LiveView for owner, expiration/permanent, description, tags, type, and default value"
    - path: "rulestead_admin/lib/rulestead_admin/components/flag_components.ex"
      provides: "Reusable badges, table cells, pagination controls, lifecycle cards, and metadata-form components"
    - path: "rulestead_admin/test/rulestead_admin/live/flag_live/index_test.exs"
      provides: "Behavioral proof for list filtering, pagination, navigation, and stale badges"
    - path: "rulestead_admin/test/rulestead_admin/live/flag_live/form_test.exs"
      provides: "Behavioral proof for invalid metadata saves and successful creation/update flows"
    - path: "rulestead_admin/test/rulestead_admin/live/flag_live/accessibility_test.exs"
      provides: "Automated axe-core or equivalent accessibility proof for list, detail, and form screens"
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex"
      to: "rulestead/lib/rulestead.ex"
      via: "list screen consumes the root public read model and cursor contract rather than querying adapters directly"
      pattern: "Rulestead\\.list_flags"
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex"
      to: "rulestead/lib/rulestead.ex"
      via: "metadata create/edit form uses root public create/update verbs with lifecycle validation"
      pattern: "Rulestead\\.(create_flag|update_flag)"
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex"
      to: "rulestead/lib/rulestead.ex"
      via: "detail screen fetches one environment-scoped flag payload"
      pattern: "Rulestead\\.fetch_flag"
    - from: "rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex"
      to: "rulestead_admin/lib/rulestead_admin/live/flag_live/rules.ex"
      via: "detail page deep-links into the dedicated rules workspace"
      pattern: "/rules"
    - from: "rulestead_admin/test/rulestead_admin/live/flag_live/accessibility_test.exs"
      to: "rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex"
      via: "automated accessibility scan exercises rendered list/detail/form pages"
      pattern: "axe|accessibility"
---

<objective>
Implement the first operator-facing screens: the flag inventory and the calm detail read surface.

Purpose: These screens deliver the primary Phase 6 operator JTBD for Priya and Sam and prove the core admin read model is sufficient before the heavier rules workspace lands.
Output: LiveView list/detail/form screens, shared screen components, pagination/a11y coverage, and focused behavior tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-ui-flag-list-detail-rule-editor-environments-lifecycle/06-CONTEXT.md
@prompts/rulestead-admin-ux-and-operator-ia.md
@prompts/rulestead-brand-book.md
@prompts/rulestead-personas-jtbd-and-onboarding.md
@prompts/phoenix-live-view-best-practices-deep-research.md
@rulestead_admin/lib/rulestead_admin/router.ex
@rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex
@rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex

<interfaces>
From `rulestead/lib/rulestead.ex` after 06-02:
```elixir
@spec list_flags(keyword()) :: {:ok, %{entries: [map()], next_cursor: term(), prev_cursor: term(), has_more: boolean()}} | {:error, Rulestead.Error.t()}
@spec fetch_flag(String.t() | atom(), String.t() | atom()) :: {:ok, map()} | {:error, Rulestead.Error.t()}
@spec create_flag(keyword()) :: {:ok, map()} | {:error, Rulestead.Error.t()}
@spec update_flag(String.t() | atom(), keyword()) :: {:ok, map()} | {:error, Rulestead.Error.t()}
```

Phase 6 screen-shape decisions:
- D-05 and D-06: `/admin/flags/:key` stays the calm read surface and `/admin/flags/:key/rules` is the dedicated authoring workspace.
- D-09 through D-12: new/edit metadata screens must require owner and expiration/permanent, keep owner free-text, and represent permanence explicitly.
- D-18 through D-22: list is a dense operator table, defaulting to current environment and excluding archived flags unless explicitly requested.

LiveView guidance to apply from the prompt:
- Use `handle_params/3` for filter/env URL state.
- Prefer function components over LiveComponents.
- Use streams for the flag list rather than large list assigns.
- Keep operator-scale pagination stable and shareable in the URL.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the dense flag list LiveView with URL-backed filters, cursor pagination, and stale/lifecycle badges</name>
  <files>rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex, rulestead_admin/lib/rulestead_admin/components/flag_components.ex, rulestead_admin/test/rulestead_admin/live/flag_live/index_test.exs</files>
  <behavior>
    - Test 1: `/admin/flags?env=prod` defaults to the current environment, excludes archived flags by default, and keeps search/filter state in the URL.
    - Test 2: operators can filter by lifecycle state, owner, tags, and stale status, move to the next/previous page via cursor-backed controls, and preserve URL state across pagination.
    - Test 3: keyboard navigation and accessible table semantics work on the dense list without introducing a second card view or hidden environment state.
  </behavior>
  <action>Implement ADMIN-01 and LIFE-02 on the list surface using the Phase 6 core read model. Keep the table dense and scanning-first per D-18 through D-22; do not add alternate card modes or Phase 7 rollout/kill-switch controls. Drive list state through `handle_params/3` and URL query params, not temporary server-only assigns. Use `Phoenix.LiveView.stream/3` for the row collection and function components for badges/cells. Wire the list to the cursor/page contract from 06-02 so operators can advance through inventory without offset drift; the next/previous controls must serialize cursor plus current filters into the URL and survive refresh/share. The visible columns should prioritize real operator work: monospace key, type, owner, lifecycle badge, environment status, stale indicator, and last changed. Production should remain visually explicit via the shared shell.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/flag_live/index_test.exs</automated>
  </verify>
  <done>The list page is a usable operator inventory with URL-shareable filter state, stable pagination, and visible lifecycle/stale information.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the flag metadata create/edit flow and the calm detail surface</name>
  <files>rulestead_admin/lib/rulestead_admin/router.ex, rulestead_admin/lib/rulestead_admin/live/flag_live/form.ex, rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex, rulestead_admin/lib/rulestead_admin/components/flag_components.ex, rulestead_admin/test/rulestead_admin/live/flag_live/form_test.exs, rulestead_admin/test/rulestead_admin/live/flag_live/show_test.exs</files>
  <behavior>
    - Test 1: `/admin/flags/new` and `/admin/flags/:key/edit` require owner plus either `expected_expiration` or `permanent`, reject invalid saves, and successfully create/update flag metadata including description, tags, type, and default value.
    - Test 2: the detail page shows description, type, default value, owner, tags, lifecycle state, and per-environment status for the selected flag.
    - Test 3: the current environment clearly distinguishes active ruleset from draft ruleset, links into `/admin/flags/:key/rules` for authoring, and renders an audit timeline placeholder only.
  </behavior>
  <action>Add the Phase 6 metadata routes and screens the checker identified as missing: `/admin/flags/new` for creation and `/admin/flags/:key/edit` for metadata edits, implemented per D-09 through D-12. The form must call the root public `Rulestead.create_flag`/`update_flag` verbs, require `owner`, require either `expected_expiration` or explicit `permanent`, represent permanence honestly, and provide only lightweight assists such as owner normalization/recent suggestions. Keep the detail page aligned with D-05 through D-08 as the calm read surface: lifecycle card, environment tabs, compact rules snapshot, active/draft banners, and a clear call to open the dedicated rules workspace. Use function components in `flag_components.ex` for lifecycle cards, metadata fields, ruleset snapshots, draft banners, and placeholder blocks. Do not overload the detail page with inline heavy editing, modals, or Phase 7 controls. The audit area should be an honest placeholder stating the full timeline arrives in Phase 7.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/flag_live/form_test.exs test/rulestead_admin/live/flag_live/show_test.exs</automated>
  </verify>
  <done>Operators can create and edit flag metadata with required lifecycle validation, and the detail page makes the current environment and active-versus-draft state obvious while pointing into the dedicated rules workspace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add automated accessibility coverage for list, detail, and metadata form pages</name>
  <files>rulestead_admin/test/rulestead_admin/live/flag_live/accessibility_test.exs</files>
  <behavior>
    - Test 1: the flag list page passes an automated axe-core or equivalent accessibility scan with keyboard-reachable filter and pagination controls.
    - Test 2: the flag detail page passes the same automated scan with environment tabs, lifecycle badges, and `/rules` link rendered.
    - Test 3: the new/edit metadata form passes the same automated scan in both invalid and valid-render states.
  </behavior>
  <action>Add a dedicated automated accessibility proof for the Phase 6 read/metadata surfaces, using axe-core or an equivalent scanner that runs inside CI. Cover the list, detail, and metadata form pages explicitly because the roadmap success criteria require automated proof, not manual review. Reuse the mounted package/session helpers from 06-03 so the scan runs against the real rendered pages and catches missing labels, invalid `aria-*`, focus traps, and keyboard-navigation regressions before 06-05 adds the rules-page scan.</action>
  <verify>
    <automated>cd rulestead_admin && mix test test/rulestead_admin/live/flag_live/accessibility_test.exs</automated>
  </verify>
  <done>The list, detail, and metadata form pages have automated accessibility proof in CI rather than only LiveView behavior tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query params -> list/detail state | Untrusted filter and environment state drives LiveView reads |
| core admin payloads -> operator-facing UI | Persisted lifecycle/ruleset data is rendered directly to operators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | T | list/detail query params | mitigate | Normalize query params in `handle_params/3`, validate `env` against known environments, and fall back safely rather than trusting arbitrary URL state. |
| T-06-11 | I | detail page rules/audit area | mitigate | Render only the bounded Phase 6 summary payload and an explicit audit placeholder; do not surface raw audit payloads or future Phase 7 controls. |
| T-06-12 | S | keyboard navigation actions | mitigate | Keep row navigation and action targets route-backed and policy-checked via mounted LiveViews instead of client-only shortcuts. |
</threat_model>

<verification>
Run the list/detail/form LiveView tests plus the automated accessibility scan, then inspect that the routes, filter URLs, pagination cursors, production badge, lifecycle badges, and active-versus-draft banners all match the Phase 6 decisions.
</verification>

<success_criteria>
Operators can browse and inspect flags confidently: list state is shareable and paginated, lifecycle/stale information is visible, metadata create/edit flows enforce owner plus expiration/permanent, and automated accessibility proof exists for the read surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-ui-flag-list-detail-rule-editor-environments-lifecycle/06-04-SUMMARY.md`
</output>
