---
phase: 81-doc-contract-hardening
plan: 81-01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/test/rulestead/intro_integration_spine_contract_test.exs
  - .planning/phases/76-phoenix-integration-spine-doc/76-VALIDATION.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - intro_integration_spine_contract_test.exs asserts evaluation.md Runtime API strings (DOC-01 regression guard)
    - 76-VALIDATION.md present with Nyquist-compliant per-task verification map for both 76-01 tasks
    - mix verify.phase76 green including new contract assertions
  artifacts:
    - rulestead/test/rulestead/intro_integration_spine_contract_test.exs
    - .planning/phases/76-phoenix-integration-spine-doc/76-VALIDATION.md
---

# Plan 81-01: Doc Contract Hardening

<objective>
Close v1.11 optional audit hardening gaps: extend the intro doc contract test to guard `evaluation.md` Runtime strings (DOC-01 regression guard beyond grep), and backfill Phase 76 Nyquist validation artifact (`76-VALIDATION.md`). No guide edits, no `verify.phase76.ex` changes.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-81-01: Contract asserts wrong Runtime strings | medium | Match `77-01-PLAN.md` verify block exactly: `Rulestead.Runtime.enabled?/3`, `Rulestead.Runtime.evaluate/3`, `Rulestead.evaluate/3` |
| T-81-02: Scope creep into guide edits | low | Read-only on `guides/flows/evaluation.md`; test only |
| T-81-03: 76-VALIDATION maps wrong tasks | medium | Map exactly 2 tasks from `76-01-PLAN.md` (76-01-01, 76-01-02) with shipped verify commands |
| T-81-04: verify.phase76 union drift | low | Do not edit `verify.phase76.ex`; test file already in union |
</threat_model>

<tasks>

<task id="81-01-01" type="execute">
  <name>Extend intro contract test for evaluation.md Runtime (DOC-01)</name>
  <read_first>
    - .planning/phases/81-doc-contract-hardening/81-CONTEXT.md (D-01–D-04)
    - rulestead/test/rulestead/intro_integration_spine_contract_test.exs (existing 4-test module)
    - guides/flows/evaluation.md (Runtime subsection — read-only)
    - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-01-PLAN.md (authoritative verify block)
    - .planning/phases/78-doc-contract-guards-and-milestone-closure/78-01-PLAN.md (contract test pattern)
  </read_first>
  <action>
Edit `rulestead/test/rulestead/intro_integration_spine_contract_test.exs`:

1. Add module attribute after existing path constants:
   ```elixir
   @evaluation_path Path.expand("../../../guides/flows/evaluation.md", __DIR__)
   ```

2. Add fifth test (name along lines of `"evaluation.md documents Runtime keyed lookup APIs (DOC-01)"`):
   ```elixir
   test "evaluation.md documents Runtime keyed lookup APIs (DOC-01)" do
     evaluation = File.read!(@evaluation_path)

     assert evaluation =~ "Rulestead.Runtime.enabled?/3"
     assert evaluation =~ "Rulestead.Runtime.evaluate/3"
     assert evaluation =~ "Rulestead.evaluate/3"
   end
   ```

Do **not** edit `guides/flows/evaluation.md`, `verify.phase76.ex`, or `release_contract_test.exs`.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/intro_integration_spine_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `intro_integration_spine_contract_test.exs` contains `@evaluation_path` module attribute
    - Test module contains test name matching `evaluation.md documents Runtime keyed lookup APIs (DOC-01)`
    - Test asserts `Rulestead.Runtime.enabled?/3` via `=~` on evaluation file content
    - Test asserts `Rulestead.Runtime.evaluate/3` via `=~` on evaluation file content
    - Test asserts `Rulestead.evaluate/3` via `=~` on evaluation file content
    - `cd rulestead && mix test test/rulestead/intro_integration_spine_contract_test.exs` exits 0
  </acceptance_criteria>
</task>

<task id="81-01-02" type="execute">
  <name>Backfill 76-VALIDATION.md Nyquist artifact</name>
  <read_first>
    - .planning/phases/81-doc-contract-hardening/81-CONTEXT.md (D-05–D-08)
    - .planning/phases/76-phoenix-integration-spine-doc/76-01-PLAN.md (2 tasks with verify commands)
    - .planning/phases/77-evaluation-and-lifecycle-doc-alignment/77-VALIDATION.md (docs-only backfill pattern)
    - .planning/phases/79-lifecycle-deep-link-anchor-fix/79-VALIDATION.md (contract-test phase pattern)
    - .planning/phases/76-phoenix-integration-spine-doc/76-VERIFICATION.md (existing verification from Phase 80)
  </read_first>
  <action>
Create `.planning/phases/76-phoenix-integration-spine-doc/76-VALIDATION.md` mirroring 77/79 VALIDATION shape:

**Frontmatter:**
```yaml
---
phase: 76
slug: phoenix-integration-spine-doc
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-28
---
```

**Sections:** Test Infrastructure, Sampling Rate, Per-Task Verification Map, Wave 0 Requirements, Validation Sign-Off.

**Test Infrastructure table:**
- Framework: none — documentation edits (Phase 76 was docs-only)
- Quick run: `test -f guides/introduction/phoenix-integration-spine.md && grep -q 'Rulestead.Runtime' guides/introduction/phoenix-integration-spine.md`
- Full suite: `cd rulestead && mix verify.phase76`
- Estimated runtime: ~5 seconds (grep) / ~2–5 minutes (full suite)

**Per-Task Verification Map — exactly 2 rows:**

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 76-01-01 | 01 | 1 | INT-01, INT-02 | grep | `test -f guides/introduction/phoenix-integration-spine.md && grep -q 'Rulestead.Runtime' guides/introduction/phoenix-integration-spine.md && grep -q 'owner_ref' guides/introduction/phoenix-integration-spine.md && grep -q 'expected_expiration' guides/introduction/phoenix-integration-spine.md` | ✅ | ✅ done |
| 76-01-02 | 01 | 1 | INT-03 | grep | `grep -q 'phoenix-integration-spine' guides/introduction/getting-started.md guides/introduction/installation.md README.md` | ✅ | ✅ done |

**Wave 0 Requirements:** Existing infrastructure covers all phase requirements (no new test files in Phase 76).

**Validation Sign-Off:** All checkboxes checked; `nyquist_compliant: true`; approval dated 2026-05-28 (Phase 81 backfill — tasks shipped in Phase 76).

Do **not** edit guides, `76-VERIFICATION.md`, or `v1.11-MILESTONE-AUDIT.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/76-phoenix-integration-spine-doc/76-VALIDATION.md && grep -q 'nyquist_compliant: true' .planning/phases/76-phoenix-integration-spine-doc/76-VALIDATION.md && grep -q '76-01-01' .planning/phases/76-phoenix-integration-spine-doc/76-VALIDATION.md && grep -q '76-01-02' .planning/phases/76-phoenix-integration-spine-doc/76-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/76-phoenix-integration-spine-doc/76-VALIDATION.md`
    - Frontmatter contains `status: complete` and `nyquist_compliant: true`
    - Per-task map contains exactly 2 rows: 76-01-01 and 76-01-02
    - 76-01-01 row references INT-01, INT-02 with spine grep verify command
    - 76-01-02 row references INT-03 with hub cross-link grep verify command
    - All task rows marked `✅ done`
    - Sign-off dated 2026-05-28 with Phase 81 backfill note
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead && mix test test/rulestead/intro_integration_spine_contract_test.exs
test -f .planning/phases/76-phoenix-integration-spine-doc/76-VALIDATION.md
grep -q 'Rulestead.Runtime.enabled?/3' rulestead/test/rulestead/intro_integration_spine_contract_test.exs
cd rulestead && mix verify.phase76
```
</verification>

<success_criteria>
- DOC-01 contract guard extension: evaluation.md Runtime strings asserted in intro contract test
- 76-VALIDATION.md present with Nyquist-compliant per-task verification map
- `mix verify.phase76` green including new contract assertions
- No edits outside files_modified
</success_criteria>
