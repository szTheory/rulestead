---
phase: "124-api-surface-lock-stability-contract"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/context.ex
  - rulestead/lib/rulestead/runtime.ex
  - rulestead/lib/rulestead/admin/policy.ex
  - rulestead/mix.exs
autonomous: true
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "Rulestead.Context no longer has @moduledoc false; it renders as a navigable HexDocs module page"
    - "Rulestead.Runtime no longer has @moduledoc false; it renders as a navigable HexDocs module page"
    - "Rulestead.Admin.Policy no longer has @moduledoc false; it renders as a navigable HexDocs module page"
    - "Every public function in the three modules carries a real @doc (not @doc false)"
    - "The four Admin.Policy *_actions/0 helpers carry @doc strings (D-10 promotion)"
    - "Rulestead.Runtime is listed in groups_for_modules; Rulestead.Runtime.Snapshot is removed from the Extensibility group (D-09: Criterion 5's two coupled mix.exs edits)"
  artifacts:
    - path: "rulestead/lib/rulestead/context.ex"
      provides: "@moduledoc + @doc on new/1, normalize/1"
      contains: "@moduledoc \"\"\""
    - path: "rulestead/lib/rulestead/runtime.ex"
      provides: "@moduledoc + @doc on evaluate/3, enabled?/3, get_value/4, get_variant/3, explain/3, diagnostics/1"
      contains: "@moduledoc \"\"\""
    - path: "rulestead/lib/rulestead/admin/policy.ex"
      provides: "@moduledoc + @doc on 3 callbacks and 4 *_actions/0 helpers"
      contains: "@moduledoc \"\"\""
    - path: "rulestead/mix.exs"
      provides: "groups_for_modules updated: Runtime added, Snapshot removed"
      contains: "Rulestead.Runtime"
  key_links:
    - from: "rulestead/lib/rulestead/runtime.ex"
      to: "rulestead/mix.exs groups_for_modules"
      via: "module listed in Public API or Runtime group"
      pattern: "Rulestead\\.Runtime"
    - from: "rulestead/lib/rulestead/admin/policy.ex"
      to: "guides/api_stability.md"
      via: "@doc on governance_actions/0, viewer_actions/0, editor_actions/0, admin_actions/0"
      pattern: "governance_actions|viewer_actions|editor_actions|admin_actions"
---

<objective>
Add @moduledoc and @doc to the three public modules currently hidden behind @moduledoc false, and update mix.exs groups_for_modules so Rulestead.Runtime is listed and Rulestead.Runtime.Snapshot is removed.

Purpose: These three modules (Rulestead.Context, Rulestead.Runtime, Rulestead.Admin.Policy) are listed as public in api_stability.md but are invisible on HexDocs. This plan makes them render. No new specs, no new APIs — only doc authoring on already-callable symbols.

Output: Three modules gain @moduledoc and per-function @doc strings; mix.exs groups_for_modules gains Runtime and loses Snapshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-api-surface-lock-stability-contract/124-CONTEXT.md

<interfaces>
<!-- Key types and signatures the executor needs. Extracted from codebase. -->

From rulestead/lib/rulestead/context.ex (lines 1-49):
  @moduledoc false  -- REPLACE with real @moduledoc
  @spec new(t() | keyword() | map()) :: t()
  @spec normalize(t() | keyword() | map()) :: t()

From rulestead/lib/rulestead/runtime.ex (lines 1-97):
  @moduledoc false  -- REPLACE with real @moduledoc
  @spec evaluate(String.t() | atom(), String.t() | atom(), Context.t() | keyword() | map()) :: {:ok, Result.t()} | {:error, Rulestead.Error.t()}
  @spec enabled?(String.t() | atom(), String.t() | atom(), Context.t() | keyword() | map()) :: {:ok, boolean()} | {:error, Rulestead.Error.t()}
  @spec get_value(String.t() | atom(), String.t() | atom(), Context.t() | keyword() | map(), term()) :: {:ok, term()} | {:error, Rulestead.Error.t()}
  @spec get_variant(String.t() | atom(), String.t() | atom(), Context.t() | keyword() | map()) :: {:ok, String.t() | nil} | {:error, Rulestead.Error.t()}
  @spec explain(String.t() | atom(), String.t() | atom(), Context.t() | keyword() | map()) :: {:ok, String.t()} | {:error, Rulestead.Error.t()}
  @spec diagnostics(keyword()) :: map()   -- def diagnostics(opts \\ [])

From rulestead/lib/rulestead/admin/policy.ex (lines 109-143):
  @spec governance_actions() :: [governance_action()]
  @spec viewer_actions() :: [viewer_action()]
  @spec editor_actions() :: [editor_action()]
  @spec admin_actions() :: [admin_action()]
  @callback can?(actor, action, resource, environment_key) :: boolean()
  @callback change_request_required?(actor, governance_action, resource, environment_key) :: boolean()
  @callback allow_self_approval?(actor, governance_action, resource, environment_key) :: boolean()
  @optional_callbacks change_request_required?: 4, allow_self_approval?: 4

From rulestead/mix.exs groups_for_modules (lines 122-140):
  "Public API": [Rulestead, Rulestead.Ruleset, Rulestead.Rule, Rulestead.Flag, Rulestead.Result, Rulestead.Error]
  "Store Adapters": [Rulestead.Store.Ecto, Rulestead.Store.Redis]
  Extensibility: [Rulestead.Store, Rulestead.Runtime.Snapshot, Rulestead.Tenancy]
  -- Target: add "Runtime (cached lookup)": [Rulestead.Runtime]; remove Rulestead.Runtime.Snapshot from Extensibility
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author @moduledoc and @doc for Rulestead.Context and Rulestead.Runtime</name>
  <files>rulestead/lib/rulestead/context.ex, rulestead/lib/rulestead/runtime.ex</files>
  <read_first>
    - rulestead/lib/rulestead/context.ex (full file — understand struct fields, function signatures, and existing @spec)
    - rulestead/lib/rulestead/runtime.ex (full file — understand all 6 public fns and their return shapes)
    - .planning/research/HEXDOCS.md (lines ~260-332 — the planned @moduledoc prose for both modules)
    - .planning/phases/124-api-surface-lock-stability-contract/124-CONTEXT.md (D-06, D-07, D-08 — no new @spec, no @deprecated, autolink safety rules)
  </read_first>
  <action>
    In rulestead/lib/rulestead/context.ex:
    - Replace the `@moduledoc false` line (line 2) with the full @moduledoc string. The moduledoc intent (D-08): "Explicit evaluation context: who is asking, in which environment, and with what attributes." Use the draft from HEXDOCS.md lines ~260-293 as the source. Include the iex doctest for Context.new/1 (the pure-function doctest pattern from HEXDOCS.md:277-285 is appropriate — building a context has no store/config dependency). Include "## Stable fields" pointing at api_stability.md.
    - Add @doc on def new/1 (line 29 area): describe that it accepts a keyword list, map, or an existing Context struct, normalizes aliases (:subject → :actor, :traits → :attributes), and returns a %Rulestead.Context{}. State that targeting_key defaults to the actor's :key or :id when not supplied.
    - Add @doc on def normalize/1 (line 48 area): describe that it accepts the same union as new/1 and ensures all scalar fields are strings or nil, actor is a map or struct, attributes is a map. State it is idempotent on an already-normalized context.
    - Do NOT add @doc to any private (defp) function. Do NOT add new @spec lines (D-07 — specs already exist).

    In rulestead/lib/rulestead/runtime.ex:
    - Replace the `@moduledoc false` line (line 3) with the full @moduledoc string. Intent (D-08): "Cached, keyed flag lookup for running Phoenix and Plug applications." Use the draft from HEXDOCS.md lines ~298-328 as the source. Keep the payload-first vs cached-lookup comparison table. State that Rulestead.Runtime.* submodules (Cache, Snapshot, Diagnostics) are implementation detail and not public. The note in HEXDOCS.md:331 clarifies that diagnostics/1 is the public arity (the def uses a default arg, so diagnostics/0 and diagnostics/1 are both callable, but api_stability.md lists diagnostics/1 — document it as diagnostics/1 with opts keyword list).
    - Add @doc on def evaluate/3: describe the three args (environment_key, flag_key, context), what it does (resolves the flag from the snapshot cache and evaluates it), and what it returns ({:ok, %Rulestead.Result{}} or {:error, %Rulestead.Error{}}). Include a plain (non-doctest) fenced elixir code example showing a Runtime.evaluate call — do not make it a doctest (it needs a running cache per HEXDOCS.md:378).
    - Add @doc on def enabled?/3: derives :enabled? from evaluate/3. Returns {:ok, boolean()} or {:error, ...}.
    - Add @doc on def get_value/4: fourth arg is the default returned when result.value is nil or reason is :default. Returns {:ok, term()} or {:error, ...}.
    - Add @doc on def get_variant/3: returns {:ok, String.t() | nil} — the matched variant key, or nil when no variant applies.
    - Add @doc on def explain/3: returns {:ok, String.t()} — a human-readable explanation of the evaluation decision. Requires a running cache/runtime_metadata.
    - Add @doc on def diagnostics/1 (the `opts \\ []` form): describe that opts is a keyword list (currently unused; reserved for future filtering), returns a map of current runtime cache state for all known environments. Useful for health checks and debugging.
    - Do NOT add @doc to any private (defp) function. Do NOT add new @spec lines.
  </action>
  <verify>
    <automated>cd rulestead && grep -c "@moduledoc false" lib/rulestead/context.ex lib/rulestead/runtime.ex</automated>
  </verify>
  <acceptance_criteria>
    - rulestead/lib/rulestead/context.ex does NOT contain `@moduledoc false` (the line is replaced, not commented out)
    - rulestead/lib/rulestead/runtime.ex does NOT contain `@moduledoc false`
    - context.ex contains `@doc` before `def new(` and before `def normalize(`
    - runtime.ex contains `@doc` before each of: `def evaluate(`, `def enabled?(`, `def get_value(`, `def get_variant(`, `def explain(`, `def diagnostics(`
    - No new `@spec` lines appear in either file (verify with: grep -n "@spec" lib/rulestead/context.ex lib/rulestead/runtime.ex and compare line counts to baseline)
    - No `@deprecated` attribute appears in either file (D-05)
    - The iex doctest block in context.ex is valid Elixir syntax (no fenced code blocks inside @moduledoc — use literal iex> prefix inline in the heredoc)
  </acceptance_criteria>
  <done>Both context.ex and runtime.ex have @moduledoc (not @moduledoc false) and all listed public functions have @doc strings. No new @spec. No @deprecated.</done>
</task>

<task type="auto">
  <name>Task 2: Author @moduledoc and @doc for Rulestead.Admin.Policy + update mix.exs groups_for_modules</name>
  <files>rulestead/lib/rulestead/admin/policy.ex, rulestead/mix.exs</files>
  <read_first>
    - rulestead/lib/rulestead/admin/policy.ex (full file — understand the 4 type definitions, 4 module-attribute action catalogs, 4 *_actions/0 helpers, and 3 callbacks)
    - .planning/research/HEXDOCS.md (lines ~333-367 — planned @moduledoc for Admin.Policy including the canonical role model section and the @doc guidance for *_actions/0 helpers)
    - .planning/phases/124-api-surface-lock-stability-contract/124-CONTEXT.md (D-10, D-11, D-12 — promotion rationale for the 4 helpers; D-08 — module doc style)
    - rulestead/mix.exs (lines ~122-140 — current groups_for_modules to understand what to add and remove)
    - examples/demo/backend/lib/rulestead_demo/admin_policy.ex (the host-facing consumer pattern — read to understand how hosts use viewer_actions/0 etc. so @doc examples are grounded)
  </read_first>
  <action>
    In rulestead/lib/rulestead/admin/policy.ex:
    - Replace `@moduledoc false` (line 2) with the full @moduledoc string. Use the HEXDOCS.md draft lines ~336-364 as the source. The moduledoc must include: (1) the "host-owned authorization seam" framing; (2) a fenced elixir code example showing a minimal MyApp.RulesteadPolicy module that uses viewer_actions/0 inside can?/4; (3) a "## Canonical role model" subsection stating the Viewer/Editor/Admin model and that action catalogs are introspectable via the four *_actions/0 functions; (4) a "## Callbacks" subsection noting that can?/4 is required, the other two are optional with safe defaults. Do NOT include an iex doctest here (the module is a behaviour — not a pure function — per HEXDOCS.md:377-378 guidelines).
    - Add @doc on def governance_actions/0: describe that it returns the list of 5 governance_action() atoms representing high-impact mutations (:publish_ruleset, :advance_rollout, :engage_kill_switch, :release_kill_switch, :promote_environment) that typically require change-request approval. Frame as a read-only catalog — "role vocabulary / introspection helper."
    - Add @doc on def viewer_actions/0: returns the 14-element list of viewer_action() atoms. These are read-only or safe simulation actions.
    - Add @doc on def editor_actions/0: returns the 8-element list of editor_action() atoms representing authoring operations.
    - Add @doc on def admin_actions/0: returns the 10-element list of admin_action() atoms (approvals, schedule management, audit, settings). Note these include the review side of the change-request workflow.
    - For the three @callback declarations (can?/4, change_request_required?/4, allow_self_approval?/4): Elixir allows @doc before @callback for callback documentation. Add @doc before each @callback. For can?/4: required; maps actor + action + resource + environment_key → boolean; no implicit role inference. For change_request_required?/4: optional; defaults to false (no change request required); return true for governance_actions/0 that require human approval. For allow_self_approval?/4: optional; defaults to false (the submitter cannot approve their own change request); return true to allow self-approval.
    - Keep @optional_callbacks line unchanged.
    - Do NOT add new @spec lines. Do NOT add @deprecated.
    - The four helper groups in the @moduledoc and in the individual @doc strings MUST be framed as "role-vocabulary / introspection helpers (read-only catalogs)" — a visually distinct sub-group from the three decision callbacks (per D-10 and CONTEXT.md:170-173).

    In rulestead/mix.exs:
    - D-09: In the `groups_for_modules:` list (lines ~122-140), add a new group entry "Runtime (cached lookup)": [Rulestead.Runtime] positioned between "Public API" and "Store Adapters".
    - In the existing Extensibility group (line ~136-139), remove the Rulestead.Runtime.Snapshot entry. The Extensibility group after edit should be: Extensibility: [Rulestead.Store, Rulestead.Tenancy].
    - Do NOT change any other part of mix.exs. Do NOT change groups_for_extras, extras, skip_undefined_reference_warnings_on, or skip_code_autolink_to.
  </action>
  <verify>
    <automated>cd rulestead && grep -c "@moduledoc false" lib/rulestead/admin/policy.ex && grep "Runtime.Snapshot" mix.exs && echo "Snapshot should NOT appear above" || echo "Snapshot removed correctly"</automated>
  </verify>
  <acceptance_criteria>
    - rulestead/lib/rulestead/admin/policy.ex does NOT contain `@moduledoc false`
    - policy.ex contains @doc before each of: `def governance_actions`, `def viewer_actions`, `def editor_actions`, `def admin_actions`
    - policy.ex contains @doc before each @callback declaration (can?/4, change_request_required?/4, allow_self_approval?/4)
    - No new `@spec` lines appear in policy.ex
    - No `@deprecated` attribute appears in policy.ex
    - rulestead/mix.exs contains `Rulestead.Runtime` in a groups_for_modules entry (e.g., grep "Rulestead.Runtime" mix.exs returns a line that is NOT Rulestead.Runtime.Snapshot)
    - rulestead/mix.exs does NOT contain `Rulestead.Runtime.Snapshot` in groups_for_modules (grep "Runtime.Snapshot" mix.exs returns no match inside the groups_for_modules list)
    - The Extensibility group in mix.exs still contains Rulestead.Store and Rulestead.Tenancy
    - mix.exs compiles cleanly: cd rulestead && mix compile exits 0
  </acceptance_criteria>
  <done>Admin.Policy has @moduledoc and @doc on all 7 public symbols (4 helpers + 3 callbacks). mix.exs groups_for_modules has Runtime group added and Snapshot removed. mix compile is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → ExDoc renderer | @doc/@moduledoc string content passes through ExDoc at build time |
| mix.exs → ExDoc config | groups_for_modules entries control which modules appear in which HexDocs group |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | @doc string autolinks in context.ex/runtime.ex/policy.ex | mitigate | Keep all `Module.fun/arity` cross-links pointed at already-public symbols; do not link to Rulestead.Runtime.Cache or other @moduledoc false modules (D-06); mix.exs:147 skip_undefined_reference_warnings_on/skip_code_autolink_to already absorbs most internal-link risk |
| T-124-02 | Information Disclosure | @doc exposing internal implementation detail | accept | The @doc prose documents the public contract framing only; private defp functions receive no @doc; internal types stay private |
| T-124-03 | Denial of Service | mix compile --warnings-as-errors failing due to @doc syntax error | mitigate | Executor must verify `cd rulestead && mix compile` exits 0 after each file edit before proceeding |
| T-124-SC | Tampering | No npm/pip/cargo installs in this plan | accept | No package manager installs; no slopcheck needed |

No new attack surface is introduced. This plan edits documentation attributes and a docs-config list. The bidirectional release_contract_test.exs guard (addressed in P02) enforces that the published contract truthfully matches the code.
</threat_model>

<verification>
After completing both tasks:

1. `cd rulestead && mix compile` exits 0 — no compilation errors introduced by @doc syntax
2. Neither context.ex, runtime.ex, nor policy.ex contains `@moduledoc false`
3. `grep -c "@doc" rulestead/lib/rulestead/context.ex` is at least 2 (for new/1 and normalize/1, plus the @moduledoc heredoc which also contains the string)
4. `grep "Rulestead.Runtime.Snapshot" rulestead/mix.exs` returns no match inside the groups_for_modules block
5. `grep "Rulestead.Runtime" rulestead/mix.exs` returns a match that is NOT Runtime.Snapshot
6. No @deprecated attribute in any of the three modified files
</verification>

<success_criteria>
All three modules have @moduledoc (not @moduledoc false) and all listed public functions have real @doc strings. The four Admin.Policy *_actions/0 helpers have @doc (D-10 promotion). mix.exs groups_for_modules lists Rulestead.Runtime and no longer lists Rulestead.Runtime.Snapshot. mix compile is clean. No @deprecated. No new @spec.
</success_criteria>

<output>
Create `.planning/phases/124-api-surface-lock-stability-contract/124-01-SUMMARY.md` when done
</output>
