---
phase: "124-api-surface-lock-stability-contract"
plan: 2
type: execute
wave: 2
depends_on:
  - "124-P01"
files_modified:
  - guides/api_stability.md
  - rulestead/test/rulestead/release_contract_test.exs
autonomous: true
requirements:
  - API-03

must_haves:
  truths:
    - "api_stability.md opens with a 1.x contract statement (not v0.1.0)"
    - "api_stability.md contains the full Versioning & Deprecation Policy with breaking-change table, telemetry stability rules, worked soft-deprecation example, and empty deprecations-table skeleton"
    - "api_stability.md lists the four Admin.Policy *_actions/0 helpers under a distinct sub-group (D-10 promotion)"
    - "release_contract_test.exs line ~181 anchor string matches the new 1.x opening sentence (D-01)"
    - "release_contract_test.exs Admin.Policy assertion includes the four promoted *_actions/0 helpers (D-12)"
    - "All other contract assertions are unchanged (D-02, D-03)"
    - "No @deprecated attribute exists anywhere in the rulestead package"
  artifacts:
    - path: "guides/api_stability.md"
      provides: "1.x contract + Versioning & Deprecation Policy"
      contains: "1.x"
    - path: "rulestead/test/rulestead/release_contract_test.exs"
      provides: "Updated anchor string + promoted helpers in Admin.Policy assertion"
      contains: "governance_actions"
  key_links:
    - from: "rulestead/test/rulestead/release_contract_test.exs"
      to: "guides/api_stability.md"
      via: "assert contract =~ opening sentence (line ~181)"
      pattern: "assert contract =~"
    - from: "guides/api_stability.md"
      to: "rulestead/lib/rulestead/admin/policy.ex"
      via: "*_actions/0 helpers listed under Admin.Policy entry"
      pattern: "governance_actions|viewer_actions|editor_actions|admin_actions"
---

<objective>
Rewrite guides/api_stability.md from the "0.1.x contract" to the "1.x contract" and add the full Versioning & Deprecation Policy. Update release_contract_test.exs in lockstep: flip the opening-sentence anchor and add the four promoted Admin.Policy helpers to the bidirectional guard.

Purpose: This is the contract-truth deliverable of Phase 124. The existing api_stability.md self-describes as "v0.1.0 release contract for the 0.1.x Hex package line" — for 1.0 GA this must flip to the 1.x contract. The test at line ~181 hard-asserts the literal opening sentence; both must change atomically (D-01). The four *_actions/0 helpers promoted in P01 must also be added to the contract doc and the bidirectional test guard (D-12).

Output: guides/api_stability.md is the 1.x stability contract with Versioning & Deprecation Policy; release_contract_test.exs has the updated anchor string and Admin.Policy helpers; all existing contract assertions remain green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-api-surface-lock-stability-contract/124-CONTEXT.md

<interfaces>
<!-- Key assertions and patterns from release_contract_test.exs that the executor must preserve. -->

From rulestead/test/rulestead/release_contract_test.exs:

Line 181 (the anchor that MUST change in lockstep with the api_stability.md rewrite — D-01):
  assert contract =~ "`guides/api_stability.md` is the v0.1.0 release contract"

Lines ~183-196 (section headers and boundary markers — MUST remain true after rewrite; ensure api_stability.md keeps these exact strings):
  assert contract =~ "## Stable `rulestead` Modules"
  assert contract =~ "## Stable `rulestead_admin` Boundary"
  assert contract =~ "## Non-Public Surface"
  assert contract =~ "RulesteadAdmin.Live.*"
  assert contract =~ "RulesteadAdmin.Components.*"
  assert contract =~ "socket assigns"
  assert contract =~ "DOM structure, CSS classes, and test selectors"
  assert contract =~ "The `env` query parameter is the canonical environment selector"
  assert contract =~ "Rulestead.RuleEngine"
  assert contract =~ "Rulestead.EvaluationCache"
  assert contract =~ "Rulestead.AuditStore"
  assert contract =~ "Rulestead.ActorResolver"
  assert contract =~ "excluded from the stability contract"

Lines ~904-909 (telemetry events — carry forward verbatim in api_stability.md, D-02):
  for event <- @telemetry_events do assert contract =~ "`#{inspect(event)}`" end

Lines ~912-937 (root exports, store callbacks, Policy callbacks, Error types, config keys — carry forward verbatim, D-02):
  for {name, arity} <- @root_exports do assert contract =~ "`#{name}/#{arity}`" end
  for {name, arity} <- @store_callbacks do assert contract =~ "`#{name}/#{arity}`" end
  for {name, arity} <- Policy.behaviour_info(:callbacks) do assert contract =~ "`#{name}/#{arity}`" or contract =~ "`#{name}`" end

Lines ~939-961 (facades — carry forward verbatim, D-02):
  for module <- @documented_supported_facades do assert contract =~ module end
  for fun <- @documented_runtime_functions do assert contract =~ "`#{fun}/" end

Lines ~233,249,254,262,265,285 (README/upgrading/demo asserts that reference "0.1.x" — D-03: do NOT change these; they describe the historical Hex release line):
  assert root_readme =~ "0.1.x"
  assert runtime_readme =~ "0.1.x"
  assert admin_readme =~ "0.1.x"
  assert upgrading =~ "0.1.x"
  assert demo_readme =~ "0.1.x"
  assert maintaining =~ "0.1.x"

D-12 promotion target — add to the Admin.Policy section of api_stability.md and to the test's Admin.Policy assertion check:
  governance_actions/0, viewer_actions/0, editor_actions/0, admin_actions/0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite guides/api_stability.md to the 1.x contract with Versioning & Deprecation Policy</name>
  <files>guides/api_stability.md</files>
  <read_first>
    - guides/api_stability.md (full file — this is the rewrite target; understand every section so nothing is lost)
    - rulestead/test/rulestead/release_contract_test.exs (lines 1-200 and lines 900-970 — understand every assertion that keys on api_stability.md content to ensure the rewrite keeps those assertions green)
    - .planning/phases/124-api-surface-lock-stability-contract/124-CONTEXT.md (D-01 through D-12, and especially the Specifics section on the *_actions/0 sub-group framing)
    - prompts/rulestead-engineering-dna-from-prior-libs.md (line 150 — api_stability.md structure: list @doc-annotated arities + deviations)
  </read_first>
  <action>
    Rewrite guides/api_stability.md with these changes:

    OPENING PARAGRAPH (the text asserted at test line 181 — D-01):
    Replace the current opening sentence "`guides/api_stability.md` is the v0.1.0 release contract for Rulestead's public API catalog, carried forward on the **`0.1.x` Hex package line**." with the new 1.x opening sentence. The new sentence must include the literal substring "`guides/api_stability.md` is the 1.x release contract" (or equivalent phrasing that removes the v0.1.0/0.1.x self-reference). The exact new sentence is your discretion but must satisfy D-01 — the test anchor at line 181 will be updated to match in Task 2, so choose the new wording now and make both changes atomically in this plan.
    The opening paragraph should briefly state: this is the 1.x public API contract; locked surface listed here is stable across minor and patch releases; symbols not listed may change; the asymmetric boundary (rulestead broad runtime contract, rulestead_admin narrow host contract) is unchanged.

    VERSIONING POSTURE SECTION:
    Replace the current minimal "## Versioning Posture" section with a fuller "## Versioning & Deprecation Policy" section. Include:
    1. Breaking-change table with columns: Change Type | Version Required | Example. Cover: removing a listed symbol (major), renaming a listed symbol (major), changing argument count of a listed symbol (major), adding a new public module (minor), adding a new function to a listed module (minor), additive telemetry keys (patch), doc-only changes (patch).
    2. Telemetry-event stability rules: events in the catalog below are stable for the 1.x line; adding new events is a minor change; removing or renaming an event name/key is a breaking change.
    3. Soft-deprecation worked example (docs-only — D-05: NO real @deprecated attribute). Show: when a function will be superseded, the soft-deprecation approach is to add a note to the @doc of the old function saying "Soft-deprecated: use X/N instead. Will be removed in 2.0." and list it in the Deprecations table below. Do NOT use the @deprecated macro until all internal callers have been migrated (the mix compile --warnings-as-errors gate would fail otherwise).
    4. Empty deprecations-table skeleton: a markdown table with columns "Function | Deprecated in | Removal target | Replacement" and one empty row or a note "No deprecations in 1.x" to establish the skeleton.

    STABLE RULESTEAD MODULES SECTION:
    Keep heading "## Stable `rulestead` Modules" (test asserts this exact string). Change the module list intro from "These modules are public in v0.1.0" to "These modules are public in 1.x". Keep the same 8 modules in the list. Remove or update the paragraph "The **v0.1.0 core module list** above remains closed. **Post-GA supported adopter facades** (below) are additionally public on `0.1.x`..." — update it to the 1.x framing, e.g. "The **1.x core module list** above remains closed. **Supported adopter facades** (below) are additionally public on `1.x`...". The test at line 182 does NOT assert this paragraph, so you have discretion.

    ADMIN.POLICY SECTION — D-10 PROMOTION (critical):
    The current "### `Rulestead.Admin.Policy`" entry in the Stable Behavior Seams section lists only the 3 callbacks. Add a distinct "Role vocabulary / introspection helpers" sub-group (a sub-subsection or clearly labeled list) under this entry listing:
    - `governance_actions/0`
    - `viewer_actions/0`
    - `editor_actions/0`
    - `admin_actions/0`
    Frame the sub-group clearly as read-only catalog helpers, visually separate from the three decision callbacks (per CONTEXT.md:170-173). The seam intro sentence "The admin policy seam is public and intentionally small" must remain true — the decision callbacks are intentionally small; the helpers are an introspection catalog. One framing: keep the "intentionally small" sentence referring to the decision callbacks, then add: "Role vocabulary is also introspectable via four read-only catalog helpers:" followed by the list.
    The test at line 923-924 asserts: `for {name, arity} <- Policy.behaviour_info(:callbacks) do assert contract =~ ...`. The helpers are NOT callbacks — they are regular functions. Their arities must appear in the contract for D-12 (the test Task 2 will add them to the attribute list to make the assertion cover them).

    ALL OTHER SECTIONS: carry forward verbatim. The telemetry event catalog, struct field lists, store behavior callbacks, root facade function list, config key lists, supported facades list, TestHelpers list, stable lifecycle seams, admin boundary, non-public surface — all must be preserved exactly as they are today. The test assertions at lines ~904-961 key on exact substrings; any deviation will break the test.

    Do NOT change the references to "v0.1.0" or "0.1.x" in the following test-asserted strings that appear INSIDE the non-public surface section or referenced historical contexts:
    - The planned seams list (Rulestead.RuleEngine, Rulestead.EvaluationCache, Rulestead.AuditStore, Rulestead.ActorResolver, "excluded from the stability contract") — keep verbatim.
    - "RulesteadAdmin.Live.*", "RulesteadAdmin.Components.*", "socket assigns", "DOM structure, CSS classes, and test selectors", "The `env` query parameter is the canonical environment selector" — keep verbatim.

    Note on `rulestead/doc/api_stability.md`: this is a generated copy; only guides/api_stability.md is the live source (CONTEXT.md canonical_refs). Do not edit the doc/ copy.
  </action>
  <verify>
    <automated>grep -c "1\.x release contract" guides/api_stability.md</automated>
  </verify>
  <acceptance_criteria>
    - guides/api_stability.md does NOT contain the string "v0.1.0 release contract" in the opening sentence (the old anchor is gone)
    - guides/api_stability.md contains a new opening sentence with "1.x release contract" (or equivalent 1.x self-description)
    - guides/api_stability.md contains a "## Versioning & Deprecation Policy" section (or "## Versioning Posture" updated to include the breaking-change table, telemetry stability rules, soft-deprecation example, and empty deprecations skeleton)
    - guides/api_stability.md contains "governance_actions/0" under the Admin.Policy entry (D-10 promotion)
    - guides/api_stability.md contains "viewer_actions/0", "editor_actions/0", "admin_actions/0" under the Admin.Policy entry
    - guides/api_stability.md still contains all 17 telemetry events from the current file (verify a sample: grep "rulestead, :eval, :decide" guides/api_stability.md returns matches)
    - guides/api_stability.md still contains "## Stable `rulestead` Modules", "## Stable `rulestead_admin` Boundary", "## Non-Public Surface" (exact heading strings)
    - guides/api_stability.md still contains "Rulestead.RuleEngine", "Rulestead.EvaluationCache", "Rulestead.AuditStore", "Rulestead.ActorResolver", "excluded from the stability contract"
    - guides/api_stability.md still contains "RulesteadAdmin.Live.*", "RulesteadAdmin.Components.*", "DOM structure, CSS classes, and test selectors"
    - guides/api_stability.md still contains "The `env` query parameter is the canonical environment selector"
    - No `@deprecated` Elixir attribute syntax appears anywhere in the file
  </acceptance_criteria>
  <done>guides/api_stability.md is the 1.x contract with Versioning & Deprecation Policy, Admin.Policy *_actions/0 helpers listed, and all pre-existing test-asserted strings preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update release_contract_test.exs — flip anchor string + add promoted *_actions/0 helpers</name>
  <files>rulestead/test/rulestead/release_contract_test.exs</files>
  <read_first>
    - rulestead/test/rulestead/release_contract_test.exs (full file — understand every assertion to avoid accidental drift)
    - guides/api_stability.md (as just rewritten in Task 1 — the EXACT new opening sentence is needed to write the updated assert at line 181)
    - .planning/phases/124-api-surface-lock-stability-contract/124-CONTEXT.md (D-01, D-02, D-03, D-12 — the only changes allowed and forbidden)
  </read_first>
  <action>
    Two surgical changes only — do NOT touch any other assertions:

    CHANGE 1 — D-01: Update the opening-sentence assert at line ~181.
    The current assertion is:
      assert contract =~ "`guides/api_stability.md` is the v0.1.0 release contract"
    Replace the asserted substring with the EXACT new opening-sentence text written in Task 1. The substring must match literally what Task 1 placed at the start of guides/api_stability.md. If Task 1 wrote "`guides/api_stability.md` is the 1.x release contract" as the opening, the assert becomes:
      assert contract =~ "`guides/api_stability.md` is the 1.x release contract"
    Use whatever exact new substring was written in Task 1 — there is no leeway here; the assert is a literal substring match.

    CHANGE 2 — D-12: Add the four promoted *_actions/0 helpers to the Admin.Policy coverage.
    Find the block that iterates Policy.behaviour_info(:callbacks) (lines ~923-925):
      for {name, arity} <- Policy.behaviour_info(:callbacks) do
        assert contract =~ "`#{name}/#{arity}`" or contract =~ "`#{name}`"
      end
    This loop covers only the 3 callbacks (can?/4, change_request_required?/4, allow_self_approval?/4). The promoted helpers are regular functions, not callbacks, so they will not appear in behaviour_info(:callbacks). Add a NEW assertion block AFTER the existing callback loop (not replacing it) that covers the four helpers by name+arity:
      for {fun, arity} <- [governance_actions: 0, viewer_actions: 0, editor_actions: 0, admin_actions: 0] do
        assert contract =~ "`#{fun}/#{arity}`"
      end
    Position this block logically adjacent to the Policy callback loop (still inside the "documented public surfaces stay listed in the api stability contract" test).

    DO NOT CHANGE anything else. Specifically:
    - D-02: Do NOT touch the telemetry events loop (~L904-909), root exports loop (~L912-918), store callbacks loop (~L919-921), or config keys assertions (~L931-937).
    - D-03: Do NOT touch the lines ~233,249,254,262,265,285 that assert "0.1.x" in README/upgrading/demo files — those describe the historical Hex release line.
    - Do NOT add any new module-attribute lists, test functions, or imports.
    - Do NOT change @api_stability_path or any other path constant.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/release_contract_test.exs --no-start 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The assert at line ~181 no longer contains "v0.1.0 release contract" — it contains the new 1.x substring
    - The new assert at line ~181 exactly matches the new opening sentence in guides/api_stability.md (a literal substring)
    - A new assertion block for governance_actions/0, viewer_actions/0, editor_actions/0, admin_actions/0 is present inside the "documented public surfaces stay listed" test
    - All previously-passing assertions in release_contract_test.exs still compile (no syntax errors introduced)
    - `cd rulestead && mix test test/rulestead/release_contract_test.exs` exits 0 (all tests in the file pass)
    - The "0.1.x" assertions at lines ~233,249,254,262,265,285 are unchanged (D-03)
    - No new @spec, no new imports, no new test functions
  </acceptance_criteria>
  <done>release_contract_test.exs has the updated 1.x anchor string and the four promoted *_actions/0 helpers in the Admin.Policy coverage assertion. All tests in the file pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guides/api_stability.md → release_contract_test.exs | The test performs literal substring matching against the contract doc; any drift breaks the bidirectional guard |
| Contract doc → published HexDocs | The rewritten contract must honestly describe the actual public surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-04 | Tampering | Anchor string drift between api_stability.md and release_contract_test.exs | mitigate | D-01 mandates atomic lockstep change; Task 2 reads the exact new opening sentence from Task 1's output before writing the assert |
| T-124-05 | Repudiation | api_stability.md contract claiming 1.x stability for surface not actually contracted | mitigate | D-12 adds the promoted helpers to the bidirectional test guard; the test enforces that anything asserted in the contract doc actually exists in the code |
| T-124-06 | Information Disclosure | "Worked soft-deprecation example" containing a real @deprecated that trips mix compile --warnings-as-errors | mitigate | D-05 explicitly forbids real @deprecated; example must be docs-only prose or a fenced code comment |
| T-124-07 | Tampering | Overwriting 0.1.x assertions at test lines ~233,249,254,262,265,285 | mitigate | D-03 forbids touching those lines; only the line ~181 anchor changes |
| T-124-SC | Tampering | No npm/pip/cargo installs in this plan | accept | No package manager installs; no slopcheck needed |

No new runtime attack surface. The integrity concern for this phase is that the published contract truthfully matches the code, enforced by the bidirectional release_contract_test.exs guard.
</threat_model>

<verification>
After completing both tasks:

1. `cd rulestead && mix test test/rulestead/release_contract_test.exs` exits 0 — all bidirectional guard tests pass including the updated anchor and promoted helpers
2. guides/api_stability.md does NOT contain "v0.1.0 release contract" as the opening sentence
3. guides/api_stability.md contains "governance_actions/0", "viewer_actions/0", "editor_actions/0", "admin_actions/0" under the Admin.Policy entry
4. guides/api_stability.md contains a Versioning & Deprecation Policy section with breaking-change table and soft-deprecation example
5. release_contract_test.exs line ~181 assert string matches the actual api_stability.md opening sentence
6. No @deprecated attribute in any file modified by this phase
</verification>

<success_criteria>
guides/api_stability.md is the 1.x stability contract with Versioning & Deprecation Policy. The four *_actions/0 helpers are listed under Admin.Policy as a distinct read-only catalog sub-group. release_contract_test.exs has the updated anchor string and the new helpers assertion. `mix test test/rulestead/release_contract_test.exs` exits 0.
</success_criteria>

<output>
Create `.planning/phases/124-api-surface-lock-stability-contract/124-02-SUMMARY.md` when done
</output>
