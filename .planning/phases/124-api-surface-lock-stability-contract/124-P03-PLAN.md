---
phase: "124-api-surface-lock-stability-contract"
plan: 3
type: execute
wave: 3
depends_on:
  - "124-P01"
  - "124-P02"
files_modified: []
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03

must_haves:
  truths:
    - "mix docs --warnings-as-errors exits 0 (no undefined-reference warnings from new @doc/@moduledoc autolinks)"
    - "mix dialyzer exits 0 (public surface dialyzer remains clean after @doc changes)"
    - "mix test test/rulestead/release_contract_test.exs exits 0 (all bidirectional guard assertions pass)"
    - "All three modules render as navigable HexDocs module pages (not 404, not missing from index)"
    - "No @deprecated attribute exists in rulestead/lib/rulestead/ or guides/api_stability.md"
  artifacts:
    - path: "rulestead/lib/rulestead/context.ex"
      provides: "Confirmed: renders as a module page under mix docs"
    - path: "rulestead/lib/rulestead/runtime.ex"
      provides: "Confirmed: renders as a module page under mix docs"
    - path: "rulestead/lib/rulestead/admin/policy.ex"
      provides: "Confirmed: renders as a module page under mix docs"
  key_links:
    - from: "scripts/ci/lint.sh:31-37"
      to: "rulestead/lib/**/*.ex + guides/api_stability.md"
      via: "mix compile --warnings-as-errors + mix docs --warnings-as-errors + mix dialyzer"
      pattern: "mix (compile|docs|dialyzer)"
---

<objective>
Run the release-gate verification suite (mix docs --warnings-as-errors, mix dialyzer, release_contract_test.exs) and confirm all three green. This plan produces no file edits — it is the final verification gate for Phase 124, run after P01 and P02 complete.

Purpose: D-04 specifies that the verification gates run LAST, after all @doc/@moduledoc edits (P01) and the contract/test updates (P02). Running them early risks false failures from a partially-edited state. Running them now confirms the full edit set is clean.

Output: Evidence that mix docs --warnings-as-errors, mix dialyzer, and release_contract_test.exs are all green. Any failure surfaces an actionable error for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-api-surface-lock-stability-contract/124-CONTEXT.md
@.planning/phases/124-api-surface-lock-stability-contract/124-01-SUMMARY.md
@.planning/phases/124-api-surface-lock-stability-contract/124-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run mix docs --warnings-as-errors and fix any undefined-reference warnings</name>
  <files></files>
  <read_first>
    - rulestead/mix.exs (lines ~146-155 — skip_undefined_reference_warnings_on and skip_code_autolink_to, to understand what is already suppressed)
    - .planning/phases/124-api-surface-lock-stability-contract/124-CONTEXT.md (D-06 — the only live release-gate risk is broken autolinks in the new @doc/@moduledoc bodies)
    - rulestead/lib/rulestead/context.ex (as edited in P01 — review @moduledoc/@doc for any Module.fun/arity cross-links)
    - rulestead/lib/rulestead/runtime.ex (as edited in P01)
    - rulestead/lib/rulestead/admin/policy.ex (as edited in P01)
  </read_first>
  <action>
    Run from the rulestead/ directory:
      mix deps.get && mix docs --warnings-as-errors

    The command runs ExDoc and treats undefined-reference warnings as errors (same posture as scripts/ci/lint.sh:33).

    If it exits 0: record the exit code and confirm all three modules appear in the generated doc index. The generated docs land in rulestead/doc/. Confirm the following files exist after the run:
      - rulestead/doc/Rulestead.Context.html
      - rulestead/doc/Rulestead.Runtime.html
      - rulestead/doc/Rulestead.Admin.Policy.html

    If it exits non-zero due to undefined-reference warnings:
    1. Read the warning lines to identify which autolink is broken (e.g., "`Rulestead.Runtime.Cache.foo/1`" in a @doc body).
    2. Fix the broken autolink in the @doc text: either remove the backtick-autolink, wrap it in a fenced code block, or change it to plain text. Keep all cross-links pointed at already-public, already-rendered symbols (D-06). Do NOT link to @moduledoc false modules.
    3. Re-run mix docs --warnings-as-errors until it exits 0.
    4. Re-run mix compile after any @doc edits to confirm compilation is still clean.

    If it exits non-zero for any other reason (compilation error, ExDoc crash): report the full error in the SUMMARY.
  </action>
  <verify>
    <automated>cd rulestead && mix docs --warnings-as-errors 2>&1 | tail -3 && echo EXIT:$?</automated>
  </verify>
  <acceptance_criteria>
    - `cd rulestead && mix docs --warnings-as-errors` exits 0
    - rulestead/doc/Rulestead.Context.html exists after the run
    - rulestead/doc/Rulestead.Runtime.html exists after the run
    - rulestead/doc/Rulestead.Admin.Policy.html exists after the run
    - No "undefined reference" warning lines appear in the mix docs output
    - If any @doc fix was required: the fix removes or replaces only the broken autolink; no other content changes
  </acceptance_criteria>
  <done>mix docs --warnings-as-errors exits 0 and all three module HTML pages are present in rulestead/doc/.</done>
</task>

<task type="auto">
  <name>Task 2: Run mix dialyzer and release_contract_test.exs; confirm both green</name>
  <files></files>
  <read_first>
    - rulestead/mix.exs (lines ~159-168 — dialyzer config: plt paths, flags, ignore_warnings file)
    - rulestead/test/rulestead/release_contract_test.exs (the full test file — to understand any failure if it occurs)
    - .planning/phases/124-api-surface-lock-stability-contract/124-CONTEXT.md (D-07 — no new @spec, so dialyzer should remain clean; D-01/D-02/D-03/D-12 — what the contract test checks)
  </read_first>
  <action>
    Run BOTH commands from the rulestead/ directory:

    COMMAND 1 — Dialyzer:
      mix dialyzer

    This runs the same dialyzer as scripts/ci/lint.sh:37. Because no new @spec lines were added (D-07 forbids new specs), and the @doc/@moduledoc additions do not change function signatures, dialyzer should remain clean. If dialyzer fails:
    - Read the error carefully. Dialyzer failures after only @doc changes are unexpected. If a new warning appears, check whether any @doc heredoc accidentally introduced invalid Elixir syntax that the compiler accepted but dialyzer rejects. Fix accordingly.
    - Do NOT add new entries to .dialyzer_ignore.exs to suppress real warnings — only suppress if the warning is a known false positive already documented in that file.

    COMMAND 2 — Contract test:
      mix test test/rulestead/release_contract_test.exs

    This runs the full bidirectional guard suite. If any test fails:
    - Read the failure output to identify which assertion failed.
    - Determine whether the failure is in: (a) the new anchor assert at line ~181 (the opening sentence substring doesn't match — fix by aligning api_stability.md and the assert to use the same substring), (b) the new *_actions/0 helpers assertion (the helper arities are not listed in api_stability.md — fix the contract doc), or (c) a previously-passing assertion that broke due to an unintended content change in api_stability.md (restore the affected verbatim section).
    - After fixing, re-run both commands until both exit 0.

    Record the exit codes and any meaningful output in the SUMMARY.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/release_contract_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd rulestead && mix dialyzer` exits 0 with no new warnings (warnings present before Phase 124 that are already in .dialyzer_ignore.exs do not count)
    - `cd rulestead && mix test test/rulestead/release_contract_test.exs` exits 0 — ALL tests in the file pass, including the updated anchor assert and the new *_actions/0 helpers assertion
    - No new entries added to .dialyzer_ignore.exs to suppress real warnings introduced by this phase
    - The "0.1.x" assertions at test lines ~233,249,254,262,265,285 still pass (D-03 — those reference README/upgrading/demo files that were not changed in this phase)
  </acceptance_criteria>
  <done>mix dialyzer exits 0 and mix test test/rulestead/release_contract_test.exs exits 0. Phase 124 release-gate verification is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mix docs output → published HexDocs tarball | Undefined-reference warnings caught here prevent broken autolinks in the published 1.0 docs |
| mix dialyzer → public surface type safety | Confirms no type regression from the @doc-only changes |
| release_contract_test.exs → api_stability.md | Bidirectional guard confirmed green; published contract matches code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-08 | Denial of Service | Broken autolink in @doc body tripping mix docs --warnings-as-errors (the release gate) | mitigate | Task 1 runs the gate and fixes any broken autolinks before the phase is declared complete |
| T-124-09 | Repudiation | Dialyzer false-positive suppressed by adding to ignore file | mitigate | Task 2 acceptance criteria forbids new entries in .dialyzer_ignore.exs for real warnings introduced by this phase |
| T-124-10 | Tampering | release_contract_test.exs failure silently ignored | mitigate | Task 2 requires exit 0 from the test; non-zero exit must be investigated and fixed, not skipped |
| T-124-SC | Tampering | No npm/pip/cargo installs in this plan | accept | No package manager installs; no slopcheck needed |

This plan introduces no new code, no new APIs, and no new attack surface. It is a pure verification gate confirming that the doc and contract changes made in P01 and P02 are coherent and release-ready.
</threat_model>

<verification>
This plan IS the verification. Its own acceptance criteria are the success signal for Phase 124.

Final state check after completing both tasks:

1. `cd rulestead && mix docs --warnings-as-errors` → exits 0
2. `ls rulestead/doc/Rulestead.Context.html rulestead/doc/Rulestead.Runtime.html rulestead/doc/Rulestead.Admin.Policy.html` → all three exist
3. `cd rulestead && mix dialyzer` → exits 0
4. `cd rulestead && mix test test/rulestead/release_contract_test.exs` → exits 0
5. `grep -r "@deprecated" rulestead/lib/rulestead/ guides/api_stability.md` → no matches (D-05)
6. `grep "@moduledoc false" rulestead/lib/rulestead/context.ex rulestead/lib/rulestead/runtime.ex rulestead/lib/rulestead/admin/policy.ex` → no matches

When all six checks pass, Phase 124 is complete.
</verification>

<success_criteria>
The release-gate trio is green: mix docs --warnings-as-errors exits 0 (no undefined-reference warnings, all three module HTML pages generated), mix dialyzer exits 0 (no new warnings), and mix test test/rulestead/release_contract_test.exs exits 0 (all bidirectional guard assertions pass including the 1.x anchor and promoted helpers). Phase 124 success criteria 1-5 from ROADMAP.md are satisfied.
</success_criteria>

<output>
Create `.planning/phases/124-api-surface-lock-stability-contract/124-03-SUMMARY.md` when done
</output>
