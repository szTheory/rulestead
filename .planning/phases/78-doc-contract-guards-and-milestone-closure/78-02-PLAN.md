---
phase: 78-doc-contract-guards-and-milestone-closure
plan: 78-02
type: execute
wave: 2
depends_on:
  - 78-01
files_modified:
  - MAINTAINING.md
  - README.md
  - rulestead/README.md
  - guides/introduction/product-boundary.md
  - rulestead/test/rulestead/release_contract_test.exs
  - .planning/threads/2026-05-28-path-to-done-milestones.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - VER-02
must_haves:
  truths:
    - Maintainer and adopter docs name mix verify.phase76 as the current post-GA merge gate
    - release_contract_test post-GA block asserts phase76 strings
    - Historical phase73 references remain in v1.10.1 archive sections only
  artifacts:
    - Updated MAINTAINING.md proof matrix
    - release_contract_test.exs with phase76 asserts
---

# Plan 78-02: Doc Proof Matrix And Release Contract Bump

<objective>
Complete VER-02 doc half: align maintainer proof matrix, README post-GA bullets, product-boundary gate, path-to-done thread, ROADMAP proof spine, and `release_contract_test.exs` post-GA band closure test with `mix verify.phase76` as the v1.11 adopter bar. Depends on 78-01 shipping the task.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-78-05: Removing phase73 from historical v1.10.1 narrative | low | Only replace **current** gate strings; leave v1.10.1 audit/archive mentions |
| T-78-06: release_contract and intro test duplicate all asserts | low | release_contract asserts phase76 + spine **routing**; intro test owns file content |
</threat_model>

<tasks>

<task id="78-02-01" type="execute">
  <name>Bump maintainer and adopter doc proof matrix to phase76</name>
  <read_first>
    - .planning/phases/78-doc-contract-guards-and-milestone-closure/78-CONTEXT.md (D-10–D-12)
    - .planning/phases/75-proof-umbrella-and-milestone-closure/75-02-PLAN.md
    - MAINTAINING.md (Post-GA section + proof matrix table)
    - README.md (post-GA band closure bullet)
    - rulestead/README.md (post-GA section)
    - guides/introduction/product-boundary.md
    - .planning/threads/2026-05-28-path-to-done-milestones.md
    - .planning/ROADMAP.md (active milestone proof spine)
  </read_first>
  <action>
Replace **current** merge-gate command `mix verify.phase73` → `mix verify.phase76` in:

**MAINTAINING.md:**
- Post-GA proof section command block
- Proof matrix table row for `mix verify.phase73` / adopter → `mix verify.phase76` / adopter
- Add proof-matrix note: phase73 = historical v1.10.1 gate (optional one-line if not present)

**README.md:**
- Post-GA band closure bullet: `mix verify.phase76` (alias `mix verify.adopter`)
- Mention v1.11 integration spine / `phoenix-integration-spine` if not already adjacent

**rulestead/README.md:**
- Post-GA band closure line → `mix verify.phase76`

**guides/introduction/product-boundary.md:**
- Band closure maintainer gate → `mix verify.phase76`

**.planning/threads/2026-05-28-path-to-done-milestones.md:**
- v1.11 row exit criteria: `mix verify.phase76` / `mix verify.adopter`
- Mark INV-INTRO-01 closure target met (Phase 78)

**.planning/ROADMAP.md:**
- Active milestone proof spine: `mix verify.phase76` · `mix verify.adopter`

**Do not** change `.planning/v1.10.1-MILESTONE-AUDIT.md` or shipped v1.10.1 historical proof strings.
  </action>
  <verify>
    <automated>grep -q verify.phase76 README.md MAINTAINING.md rulestead/README.md guides/introduction/product-boundary.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `mix verify.phase76`
    - `MAINTAINING.md` contains `mix verify.phase76` at least twice
    - `rulestead/README.md` contains `mix verify.phase76`
    - `guides/introduction/product-boundary.md` contains `mix verify.phase76`
    - `.planning/ROADMAP.md` proof spine lists `verify.phase76`
    - v1.10.1 audit file still contains `verify.phase73` (historical)
  </acceptance_criteria>
</task>

<task id="78-02-02" type="execute">
  <name>Update release_contract_test post-GA and v1.11 spine asserts</name>
  <read_first>
    - rulestead/test/rulestead/release_contract_test.exs (test "post-GA band closure support truth..." ~L634)
    - rulestead/test/rulestead/intro_integration_spine_contract_test.exs
    - guides/introduction/phoenix-integration-spine.md
  </read_first>
  <action>
In `release_contract_test.exs`:

1. In test `"post-GA band closure support truth stays bounded across root package and maintainer docs"`:
   - Change `assert root_readme =~ "mix verify.phase73"` → `"mix verify.phase76"`
   - Change `assert runtime_readme =~ "mix verify.phase73"` → `"mix verify.phase76"`
   - Change `assert maintaining =~ "mix verify.phase73"` (both) → `"mix verify.phase76"`
   - Keep `mix verify.adopter` asserts
   - Keep `mix verify.phase68` historical chain asserts

2. Add new test `"v1.11 integration spine support truth routes adopters to first-hour path"`:
   - root_readme contains `phoenix-integration-spine` and `mix verify.phase76`
   - maintaining contains `mix verify.phase76` and `phoenix-integration-spine` OR `integration spine`
   - runtime_readme contains `phoenix-integration-spine` (link to spine from package readme)

Run release contract tests after edit.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/release_contract_test.exs --only line:634 2>/dev/null || cd rulestead && mix test test/rulestead/release_contract_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'mix verify.phase76' rulestead/test/rulestead/release_contract_test.exs`
    - `grep -q 'phoenix-integration-spine' rulestead/test/rulestead/release_contract_test.exs`
    - `cd rulestead && mix test test/rulestead/release_contract_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
grep -q verify.phase76 README.md MAINTAINING.md rulestead/README.md
cd rulestead && mix test test/rulestead/release_contract_test.exs
cd rulestead && mix verify.phase76
```
</verification>

<success_criteria>
- Live adopter/maintainer surfaces cite phase76 as current gate
- release_contract guards phase76 + spine routing
- phase73 preserved in v1.10.1 historical docs
</success_criteria>
