---
phase: 78-doc-contract-guards-and-milestone-closure
plan: 78-01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/test/rulestead/intro_integration_spine_contract_test.exs
  - rulestead/lib/mix/tasks/verify.phase76.ex
  - rulestead/lib/mix/tasks/verify.adopter.ex
  - rulestead/mix.exs
  - scripts/ci/test.sh
autonomous: true
requirements:
  - VER-01
  - VER-02
must_haves:
  truths:
    - intro_integration_spine_contract_test.exs fails if spine or lifecycle callouts regress
    - mix verify.phase76 runs phase73 flat union plus intro contract test without delegating to verify.phase73
    - mix verify.adopter delegates only to verify.phase76
    - post_ga_band_closure CI scope invokes verify.phase76
  artifacts:
    - rulestead/test/rulestead/intro_integration_spine_contract_test.exs
    - rulestead/lib/mix/tasks/verify.phase76.ex
---

# Plan 78-01: Intro Contract Test And verify.phase76

<objective>
Ship VER-01 and VER-02 core: dedicated intro spine contract test, flat `mix verify.phase76` merge gate (phase73 union + v1.11 intro guards), and retarget `mix verify.adopter` plus CI `post_ga_band_closure` to that gate. No new product APIs.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-78-01: phase76 delegates to phase73 causing duplicate runs | medium | Copy test path lists; call `Mix.Task.run("test", paths)` only — never `Mix.Task.run("verify.phase73")` |
| T-78-02: Contract asserts wrong field names | medium | Use `owner_ref` + `expected_expiration` per Phase 77 shipped docs |
| T-78-03: CI still runs phase73 after gate bump | medium | Update `scripts/ci/test.sh` runner and remediation strings |
</threat_model>

<tasks>

<task id="78-01-01" type="execute">
  <name>Add intro integration spine contract test (VER-01)</name>
  <read_first>
    - .planning/phases/78-doc-contract-guards-and-milestone-closure/78-CONTEXT.md (D-01–D-02)
    - guides/introduction/phoenix-integration-spine.md
    - guides/introduction/getting-started.md
    - guides/introduction/installation.md
    - README.md
    - rulestead/test/rulestead/context_test.exs (contract test style reference)
  </read_first>
  <action>
Create `rulestead/test/rulestead/intro_integration_spine_contract_test.exs`:

- `use ExUnit.Case, async: true`
- Path constants via `Path.expand("../../../guides/...", __DIR__)` and root README
- **Test "phoenix integration spine documents first-hour Phoenix path"** — read spine file; assert:
  - `Rulestead.Runtime`
  - `Rulestead.Plug`
  - `owner_ref`
  - `expected_expiration`
  - `flag-lifecycle`
- **Test "intro hubs link spine and lifecycle-required fields"** — getting-started + installation:
  - both contain `phoenix-integration-spine`
  - both contain `owner_ref` and `expected_expiration`
- **Test "root readme routes Phoenix integrators to the spine"** — root README contains `phoenix-integration-spine`

Do not edit the guide files in this task — test only.
  </action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/intro_integration_spine_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `rulestead/test/rulestead/intro_integration_spine_contract_test.exs`
    - `cd rulestead && mix test test/rulestead/intro_integration_spine_contract_test.exs` exits 0
    - Test module contains `owner_ref` and `expected_expiration` assertions
    - Test module contains `Rulestead.Runtime` assertion on spine content
  </acceptance_criteria>
</task>

<task id="78-01-02" type="execute">
  <name>Add mix verify.phase76 flat union and retarget adopter + CI (VER-02)</name>
  <read_first>
    - rulestead/lib/mix/tasks/verify.phase73.ex (full @phase73_core_tests and @admin_test_paths)
    - rulestead/lib/mix/tasks/verify.adopter.ex
    - rulestead/mix.exs (preferred_envs)
    - scripts/ci/test.sh (run_post_ga_band_closure, print_post_ga_band_closure_failure_guidance)
    - .planning/phases/75-proof-umbrella-and-milestone-closure/75-01-PLAN.md
  </read_first>
  <action>
Create `rulestead/lib/mix/tasks/verify.phase76.ex`:

- `use Mix.Task`, `@moduledoc false`, `@shortdoc "Runs Phase 78 v1.11 integration-spine verification suite"`
- Copy `@phase73_core_tests` from `verify.phase73.ex` **verbatim** into `@phase76_core_tests`
- Append at end of core list: `"test/rulestead/intro_integration_spine_contract_test.exs"`
- Copy `@admin_test_paths` unchanged from phase73
- `@impl run/1`: same as phase73 (core test run + admin subprocess)
- Module comment: flat union of phase73 + v1.11 intro spine guards; **do not** delegate to `verify.phase73`

Update `rulestead/mix.exs` `cli/0` `preferred_envs` — add `{:"verify.phase76", :test}` after `verify.phase73` entry.

**verify.adopter.ex:**

- Change `Mix.Task.run("verify.phase73", args)` → `Mix.Task.run("verify.phase76", args)`
- Update `@moduledoc`: integrator entrypoint for **v1.11 integration spine**; delegates to `mix verify.phase76`
- Update `@shortdoc`: mention phase76

**scripts/ci/test.sh:**

- In `run_post_ga_band_closure`, replace `verify.phase73` with `verify.phase76` in `run_mix_logged` call
- In `print_post_ga_band_closure_failure_guidance`, remediation line: `cd rulestead && mix verify.phase76`

**Do not** remove or edit `verify.phase73.ex`.
  </action>
  <verify>
    <automated>cd rulestead && mix verify.phase76 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `rulestead/lib/mix/tasks/verify.phase76.ex`
    - `grep -q 'intro_integration_spine_contract_test.exs' rulestead/lib/mix/tasks/verify.phase76.ex`
    - `grep -q 'verify.phase73' rulestead/lib/mix/tasks/verify.phase76.ex` returns exit 1 (no delegation)
    - `grep -q 'verify.phase76' rulestead/mix.exs`
    - `grep -q 'verify.phase76' rulestead/lib/mix/tasks/verify.adopter.ex`
    - `grep 'verify.phase73' rulestead/lib/mix/tasks/verify.adopter.ex` returns exit 1
    - `grep -q 'verify.phase76' scripts/ci/test.sh`
    - `cd rulestead && mix verify.phase76` exits 0
    - `cd rulestead && mix verify.adopter` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd rulestead && mix test test/rulestead/intro_integration_spine_contract_test.exs
cd rulestead && mix verify.phase76
cd rulestead && mix verify.adopter
```
</verification>

<success_criteria>
- VER-01 and VER-02 core satisfied
- phase76 is strict superset of phase73 (one additional test file)
- CI post_ga_band_closure and adopter share the same gate
</success_criteria>
