---
phase: 76-phoenix-integration-spine-doc
plan: 76-01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/introduction/phoenix-integration-spine.md
  - guides/introduction/getting-started.md
  - guides/introduction/installation.md
  - README.md
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
must_haves:
  truths:
    - First-hour Phoenix path exists as a single spine doc with supervision → config → Plug → Runtime eval
    - Spine documents lifecycle-required owner_ref + expected_expiration at flag create
    - README, getting-started, and installation link to the spine
  artifacts:
    - guides/introduction/phoenix-integration-spine.md
---

# Plan 76-01: Phoenix Integration Spine And Intro Cross-Links

<objective>
Ship INT-01–INT-03: author `guides/introduction/phoenix-integration-spine.md` and wire it from intro docs. Docs only — no `lib/`, `evaluation.md`, or release-contract changes.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-76-01: Spine claims install injects host supervision children | high | RESEARCH + D-02: OTP `:rulestead` only |
| T-76-02: Config examples drift from installer | medium | Source `install_golden/tree/config/rulestead.exs` |
| T-76-03: `traits:` in spine fences | medium | `attributes:` only; spot-check with grep |
| T-76-04: Scope creep into evaluation.md | low | Do not edit `guides/flows/evaluation.md` |
</threat_model>

<tasks>

<task id="76-01-01" type="execute">
  <name>Author phoenix-integration-spine.md (INT-01, INT-02)</name>
  <read_first>
    - .planning/phases/76-phoenix-integration-spine-doc/76-CONTEXT.md (D-01–D-10)
    - .planning/phases/76-phoenix-integration-spine-doc/76-RESEARCH.md
    - rulestead/fixtures/install_golden/tree/config/rulestead.exs
    - rulestead/fixtures/install_golden/tree/lib/host_app_web/endpoint.ex
    - rulestead/fixtures/install_golden/tree/lib/host_app_web/router.ex
    - guides/recipes/context-propagation.md (Plug section)
    - guides/flows/flag-lifecycle.md (birth / What To Record Up Front)
    - rulestead/lib/rulestead/application.ex
  </read_first>
  <action>
Create `guides/introduction/phoenix-integration-spine.md` with:

**Front matter tone:** ~15-minute first-hour Phoenix path (Alex JTBD).

**Sections (numbered):**

1. **Before you start** — deps + `mix rulestead.install` + migrate; link [installation.md](installation.md).

2. **How Rulestead runs in your BEAM** — `:rulestead` OTP app → `Rulestead.Application` supervises `Rulestead.Runtime.Supervisor` (snapshot cache), analytics batcher, stale tracker. Explicit: installer does **not** add Rulestead to host `application.ex` children.

3. **Host config after install** — abbreviated `config/rulestead.exs` from golden (store, repo, `:host` plug/runtime keys). Note `import_config "rulestead.exs"` in host `config.exs`.

4. **Request boundary: Plug** — `plug Rulestead.Plug` placement; `conn.assigns[:rulestead_context]`. Link [context-propagation.md](../recipes/context-propagation.md) for LiveView/Oban (do not duplicate).

5. **First runtime evaluation** — `Rulestead.Runtime.enabled?/3` example with context from assigns; one line: payload-first `Rulestead.evaluate/3` for tests/simulation → [evaluation.md](../flows/evaluation.md).

6. **Create your first flag (lifecycle required)** — required `owner_ref` + `expected_expiration`; host-owned owner honesty; link [flag-lifecycle.md](../flows/flag-lifecycle.md). Prefer admin UI walkthrough ("create flag form") plus optional comment that Store APIs enforce the same fields.

7. **Optional: mount the admin UI** — pointer to golden router / [rulestead_admin/README.md](../../rulestead_admin/README.md).

8. **Next steps** — footguns, product-boundary, multi-env, demo proof script.

All Elixir fences: `attributes:` not `traits:`.
  </action>
  <verify>
    <automated>test -f guides/introduction/phoenix-integration-spine.md && ! grep -q 'traits: %{' guides/introduction/phoenix-integration-spine.md && grep -q 'Rulestead.Runtime' guides/introduction/phoenix-integration-spine.md && grep -q 'owner_ref' guides/introduction/phoenix-integration-spine.md && grep -q 'expected_expiration' guides/introduction/phoenix-integration-spine.md</automated>
  </verify>
  <acceptance_criteria>
    - Spine file exists with all 8 section headings
    - Contains `Rulestead.Application` / OTP supervision language (not host children injection)
    - Contains `owner_ref` and `expected_expiration`
    - No `traits: %{` in file
  </acceptance_criteria>
</task>

<task id="76-01-02" type="execute">
  <name>Wire spine from intro docs (INT-03)</name>
  <read_first>
    - guides/introduction/getting-started.md
    - guides/introduction/installation.md
    - README.md
    - .planning/phases/76-phoenix-integration-spine-doc/76-CONTEXT.md (D-05–D-07)
  </read_first>
  <action>
**getting-started.md:**

- After §2 Install and migrate, add callout: **Phoenix first-hour path → [Phoenix Integration Spine](phoenix-integration-spine.md)**.
- In §3, add one line at top: for the ordered Plug → Runtime path, follow the spine.
- Shorten § "Snapshot runtime lookup" to ~4 lines: context from assigns + link spine for full walkthrough (keep minimal Runtime example OR defer entirely to spine — prefer minimal 3-line example + spine link).

**installation.md:**

- In "What happens next", insert spine as first bullet before getting-started/evaluation.

**README.md:**

- In "15-minute quickstart", after install block, add: **Phoenix integrators:** follow [Phoenix Integration Spine](guides/introduction/phoenix-integration-spine.md) for supervision → Plug → first flag.
- Do not remove payload-first evaluate block.

**product-boundary.md (optional one-line):** If a "Getting started" link list exists, add spine — only if already listing getting-started (minimal churn).
  </action>
  <verify>
    <automated>grep -q 'phoenix-integration-spine' guides/introduction/getting-started.md guides/introduction/installation.md README.md</automated>
  </verify>
  <acceptance_criteria>
    - All three hub files link to phoenix-integration-spine.md
    - README still contains payload-first `Rulestead.evaluate` example
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
test -f guides/introduction/phoenix-integration-spine.md
grep -q phoenix-integration-spine guides/introduction/getting-started.md guides/introduction/installation.md README.md
! grep -q 'traits: %{' guides/introduction/phoenix-integration-spine.md
```
</verification>

<success_criteria>
- INT-01, INT-02, INT-03 satisfied
- Phase 76 roadmap success criteria 1–3 met
- No edits outside files_modified (+ optional product-boundary one-liner)
</success_criteria>
