---
phase: 15-lifecycle-hygiene-and-code-references
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/code_refs/scanner.ex
  - rulestead/test/rulestead/code_refs/scanner_test.exs
  - rulestead/lib/mix/tasks/rulestead.code_refs.ex
  - rulestead/lib/rulestead/webhooks/code_refs_plug.ex
  - rulestead/test/rulestead/webhooks/code_refs_plug_test.exs
autonomous: true
requirements:
  - LCH-02

must_haves:
  truths:
    - CI environment can natively execute a Mix task to find flag evaluation references
    - Rulestead platform can ingest and store code references securely from CI
  artifacts:
    - path: rulestead/lib/rulestead/code_refs/scanner.ex
      provides: Native Elixir AST traversal to find Rulestead.evaluate calls
    - path: rulestead/lib/mix/tasks/rulestead.code_refs.ex
      provides: CLI Mix task for CI/CD integration
    - path: rulestead/lib/rulestead/webhooks/code_refs_plug.ex
      provides: Authenticated webhook endpoint to receive AST results
  key_links:
    - from: rulestead/lib/mix/tasks/rulestead.code_refs.ex
      to: rulestead/lib/rulestead/webhooks/code_refs_plug.ex
      via: HTTP POST with API token authentication
---

<objective>
Create a passive CI scanner via Mix task and an ingress API to ingest code references.

Purpose: Allow operators to trace exact file and line number locations of flag usage securely without Rulestead requiring access to the VCS (D-01).
Output: AST Scanner, Mix Task, and Webhook Ingress Plug.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-lifecycle-hygiene-and-code-references/15-CONTEXT.md
@.planning/phases/15-lifecycle-hygiene-and-code-references/15-PATTERNS.md
@.planning/phases/15-lifecycle-hygiene-and-code-references/15-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AST Code Reference Scanner</name>
  <files>rulestead/lib/rulestead/code_refs/scanner.ex, rulestead/test/rulestead/code_refs/scanner_test.exs</files>
  <behavior>
    - Test 1: Parses `Rulestead.evaluate/2` calls and returns correct line numbers.
    - Test 2: Safely ignores non-matching code without crashing.
    - Test 3: Limits directory traversal to specific paths.
  </behavior>
  <action>Implement native AST traversal using `Code.string_to_quoted` and `Macro.prewalk` to identify `Rulestead.evaluate` usages. Return a list of files and exact line numbers mapping flag keys to their locations. Do not rely on Regex.</action>
  <verify>
    <automated>mix test test/rulestead/code_refs/scanner_test.exs</automated>
  </verify>
  <done>Scanner correctly identifies references from source code strings.</done>
</task>

<task type="auto">
  <name>Task 2: Code Refs Mix Task</name>
  <files>rulestead/lib/mix/tasks/rulestead.code_refs.ex</files>
  <action>Implement the `mix rulestead.code_refs` task. It should scan the host application's target directories (default to `lib/`), use the Scanner from Task 1, format the results as JSON, and HTTP POST to the configured Rulestead ingress endpoint. Support a `--token` argument or `RULESTEAD_CI_TOKEN` env var for authentication. Reference its analog `lib/mix/tasks/rulestead.install.ex` for the Mix task structure as defined in 15-PATTERNS.md.</action>
  <verify>
    <automated>mix help rulestead.code_refs</automated>
  </verify>
  <done>Mix task is executable and performs network push on run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Code Refs Ingress Plug</name>
  <files>rulestead/lib/rulestead/webhooks/code_refs_plug.ex, rulestead/test/rulestead/webhooks/code_refs_plug_test.exs</files>
  <behavior>
    - Test 1: Accepts valid JSON payload with valid token and returns 200.
    - Test 2: Rejects requests with missing/invalid tokens with 401.
    - Test 3: Validates the shape of the incoming JSON to prevent malformed data insertion.
  </behavior>
  <action>Create a Plug to handle incoming webhook requests from the Mix task. Verify the `RULESTEAD_CI_TOKEN`. Create a database schema/migration if necessary to persist these code references, and save the ingested data. Reference its analog `lib/rulestead/webhooks/ingress_plug.ex` for the plug structure as defined in 15-PATTERNS.md.</action>
  <verify>
    <automated>mix test test/rulestead/webhooks/code_refs_plug_test.exs</automated>
  </verify>
  <done>Ingress endpoint securely ingests code reference payloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI -> API | Untrusted JSON payload from external CI crosses into the Admin API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Spoofing | Code Refs Plug | mitigate | Enforce API token validation on the endpoint. |
| T-15-03 | Information Disclosure | AST Scanner | mitigate | Restrict file scanning to `lib/` to prevent path traversal leaks. |
| T-15-04 | Tampering | Code Refs Plug | mitigate | Use Ecto schema validation on incoming JSON data before DB insertion. |
</threat_model>

<verification>
mix test passes for the scanner and ingress plug. Mix task compiles and runs.
</verification>

<success_criteria>
AST parser accurately finds flag usages, Mix task executes locally, and Webhook plug securely validates and accepts the data.
</success_criteria>

<output>
After completion, create `.planning/phases/15-lifecycle-hygiene-and-code-references/15-02-SUMMARY.md`
</output>
