---
phase: 15-lifecycle-hygiene-and-code-references
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead/lib/rulestead/telemetry/cache.ex
  - rulestead/test/rulestead/telemetry/cache_test.exs
  - rulestead/lib/rulestead/oban/stale_flag_worker.ex
  - rulestead/test/rulestead/oban/stale_flag_worker_test.exs
  - rulestead/lib/rulestead.ex
autonomous: true
requirements:
  - LCH-01

must_haves:
  truths:
    - Flag evaluation silently updates a lightweight memory cache
    - Cache is periodically flushed to persistent storage via background worker
  artifacts:
    - path: rulestead/lib/rulestead/telemetry/cache.ex
      provides: ETS write-behind caching for telemetry
    - path: rulestead/lib/rulestead/oban/stale_flag_worker.ex
      provides: Oban worker to flush cache to DB
  key_links:
    - from: rulestead/lib/rulestead.ex
      to: rulestead/lib/rulestead/telemetry/cache.ex
      via: synchronous cache update on evaluate
---

<objective>
Implement detection mechanisms for stale flags using a high-throughput ETS write-behind cache and Oban worker.

Purpose: Accurately track `last_evaluated_at` and variants served without introducing database bottlenecks, enabling safe identification of stale flags (D-02).
Output: ETS Cache module, Oban flush worker, and evaluation wiring.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-lifecycle-hygiene-and-code-references/15-CONTEXT.md
@.planning/phases/15-lifecycle-hygiene-and-code-references/15-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Telemetry ETS Cache</name>
  <files>rulestead/lib/rulestead/telemetry/cache.ex, rulestead/test/rulestead/telemetry/cache_test.exs</files>
  <behavior>
    - Test 1: Initializes an ETS table on startup
    - Test 2: Bumps evaluation count and updates `last_evaluated_at` for a given flag_key
    - Test 3: Can return a snapshot of the cache and clear it for flushing
  </behavior>
  <action>Implement an ETS-backed cache (`Rulestead.Telemetry.Cache`) to store `last_evaluated_at` and `variants_served` for each flag evaluated. Use write-behind patterns to ensure high throughput.</action>
  <verify>
    <automated>mix test test/rulestead/telemetry/cache_test.exs</automated>
  </verify>
  <done>ETS table is created, values are correctly updated in memory, and snapshot/clear functions are available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Telemetry Flush Worker</name>
  <files>rulestead/lib/rulestead/oban/stale_flag_worker.ex, rulestead/test/rulestead/oban/stale_flag_worker_test.exs</files>
  <behavior>
    - Test 1: Reads snapshot from ETS cache
    - Test 2: Updates database records (e.g., flags table or a telemetry schema) with new `last_evaluated_at` and variant counts
    - Test 3: Flushes ETS cache correctly
  </behavior>
  <action>Create an Oban worker that periodically fetches the ETS snapshot and flushes the aggregated telemetry data to Postgres asynchronously. Follow the `scheduled_execution_worker.ex` pattern defined in 15-PATTERNS.md. Create a migration if a new telemetry table or columns are needed.</action>
  <verify>
    <automated>mix test test/rulestead/oban/stale_flag_worker_test.exs</automated>
  </verify>
  <done>Worker successfully processes ETS snapshot and persists to database.</done>
</task>

<task type="auto">
  <name>Task 3: Evaluation Hook Wiring</name>
  <files>rulestead/lib/rulestead.ex</files>
  <action>Hook into the core `Rulestead.evaluate/2` flow (or underlying module) to synchronously update the `Rulestead.Telemetry.Cache` before returning the resolution. Ensure this does not block the main process unnecessarily.</action>
  <verify>
    <automated>mix test --filter "evaluation"</automated>
  </verify>
  <done>Evaluation updates ETS cache seamlessly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cache -> Worker -> DB | Memory to persistent storage boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | Telemetry Cache | mitigate | Use ETS for high-throughput, non-blocking updates. Oban ensures DB isn't overloaded. |
</threat_model>

<verification>
mix test passes for telemetry cache and flush worker.
</verification>

<success_criteria>
Evaluating a flag successfully writes to the ETS cache and is periodically synced to the DB via Oban.
</success_criteria>

<output>
After completion, create `.planning/phases/15-lifecycle-hygiene-and-code-references/15-01-SUMMARY.md`
</output>
