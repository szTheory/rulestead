---
phase: 15-lifecycle-hygiene-and-code-references
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex
  - rulestead_admin/lib/rulestead_admin/live/flag_live/cleanup.ex
  - rulestead_admin/test/rulestead_admin/live/flag_live/cleanup_test.exs
autonomous: true
requirements:
  - LCH-03

must_haves:
  truths:
    - User can visually identify stale flags in the Admin UI via passive drift detection
    - User can navigate to a cleanup view to see exact code usage locations
    - Cleanup requires strict manual confirmation, especially in production
  artifacts:
    - path: rulestead_admin/lib/rulestead_admin/live/flag_live/cleanup.ex
      provides: LiveView for contextual manual cleanup action
  key_links:
    - from: rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex
      to: rulestead_admin/lib/rulestead_admin/live/flag_live/cleanup.ex
      via: navigation link from stale flag badge
---

<objective>
Expose stale flag management and code reference discovery workflows in the Rulestead Admin UI to guide operators in safely removing obsolete flags.

Purpose: Provide operators with contextual manual actions (D-03) and clear visibility to safely eliminate technical debt.
Output: UI badges for stale flags and a strict pre-flight cleanup checklist modal/view.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-lifecycle-hygiene-and-code-references/15-CONTEXT.md
@.planning/phases/15-lifecycle-hygiene-and-code-references/15-PATTERNS.md
@.planning/phases/15-lifecycle-hygiene-and-code-references/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stale Flag UI Badges</name>
  <files>rulestead_admin/lib/rulestead_admin/live/flag_live/index.ex, rulestead_admin/lib/rulestead_admin/live/flag_live/show.ex</files>
  <action>Update the flag list and detail views to query telemetry state. Display a subtle "possibly stale" badge if a flag's configuration has been in a terminal state (e.g., 100% rollout) for > 30 days and has only served one variant (per D-02). Exempt "Kill Switch" and "Operational" flags. Wire the stale badge to navigate to the cleanup view.</action>
  <verify>
    <automated>mix test test/rulestead_admin/live/flag_live/index_test.exs</automated>
  </verify>
  <done>Badges render conditionally based on stale telemetry data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cleanup Confirmation Workflow</name>
  <files>rulestead_admin/lib/rulestead_admin/live/flag_live/cleanup.ex, rulestead_admin/test/rulestead_admin/live/flag_live/cleanup_test.exs</files>
  <behavior>
    - Test 1: Renders code references (files and lines) for the flag.
    - Test 2: In production environment, requires exact flag key typed to confirm.
    - Test 3: Proceeds with archival only when confirmation validation passes.
  </behavior>
  <action>Implement the `cleanup.ex` LiveView. It must surface remaining code references queried from the database. Implement the strict pre-flight checklist. Follow the validation pattern in `kill.ex` (from 15-PATTERNS.md) to require typing the exact flag key if the environment is production.</action>
  <verify>
    <automated>mix test test/rulestead_admin/live/flag_live/cleanup_test.exs</automated>
  </verify>
  <done>Cleanup view securely enforces manual confirmation before archiving a flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> Admin UI | Privileged manual action boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Elevation of Privilege | Cleanup LiveView | mitigate | Require explicit confirmation (typing the flag key) for production changes to prevent accidental archival. |
</threat_model>

<verification>
mix test passes for the LiveViews and the UI successfully renders mock stale data.
</verification>

<success_criteria>
Operators can visually identify stale flags and navigate a safe, informative manual cleanup process.
</success_criteria>

<output>
After completion, create `.planning/phases/15-lifecycle-hygiene-and-code-references/15-03-SUMMARY.md`
</output>
