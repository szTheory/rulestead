---
phase: 10-scheduled-changes-and-durable-execution
plan: 04
type: execute
wave: 4
depends_on:
  - 10-02
  - 10-03
files_modified:
  - rulestead/lib/rulestead/audit_event.ex
  - rulestead/lib/rulestead/telemetry.ex
  - rulestead/lib/rulestead/store/ecto.ex
  - rulestead/lib/rulestead/oban/scheduled_execution_worker.ex
  - rulestead/test/rulestead/scheduled_execution_audit_contract_test.exs
  - rulestead/test/rulestead/scheduled_execution_threat_model_test.exs
  - scripts/ci/verify_phase10_scheduling.sh
autonomous: true
requirements:
  - SCH-02
  - SCH-04
must_haves:
  truths:
    - "Every scheduled execution records audit correlation, attempt counts, requested time, actual time, outcome, and operator-meaningful failure reason."
    - "Telemetry reports scheduled execution lifecycle events without leaking unredacted session or secret-shaped data."
    - "The phase ships with one scripts-first verifier proving the durable execution, failure handling, and threat-model guarantees."
  artifacts:
    - path: "rulestead/lib/rulestead/audit_event.ex"
      provides: "Scheduled-execution audit metadata normalization and redaction"
    - path: "rulestead/lib/rulestead/telemetry.ex"
      provides: "Canonical scheduled execution telemetry metadata and event names"
    - path: "rulestead/test/rulestead/scheduled_execution_threat_model_test.exs"
      provides: "Executable proof of replay safety, visible failure handling, and audit correlation"
    - path: "scripts/ci/verify_phase10_scheduling.sh"
      provides: "Readable phase verifier for scheduling behavior and failure handling"
  key_links:
    - from: "rulestead/lib/rulestead/store/ecto.ex"
      to: "rulestead/lib/rulestead/audit_event.ex"
      via: "scheduled execution writes append immutable audit rows with top-level correlation and timing fields"
      pattern: "scheduled_execution_id|attempt_count|failure_reason|executed_at"
    - from: "rulestead/lib/rulestead/oban/scheduled_execution_worker.ex"
      to: "rulestead/lib/rulestead/telemetry.ex"
      via: "worker lifecycle emits scheduled execution telemetry around actual runtime execution"
      pattern: "scheduled_execution\\.(started|succeeded|failed|quarantined)"
    - from: "scripts/ci/verify_phase10_scheduling.sh"
      to: "rulestead/test/rulestead/scheduled_execution_threat_model_test.exs"
      via: "phase verifier proves the operator-visible safety contract end to end"
      pattern: "verify_phase10_scheduling|scheduled_execution_threat_model_test"
---

<objective>
Finish the audit, telemetry, and scripts-first verification layer for scheduled execution.

Purpose: Scheduled changes only build operator trust if outcomes, failures, and retries are observable in the same precise language the product state uses.
Output: Audit metadata extensions, canonical telemetry names, threat-model tests, and a readable phase verifier script.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-scheduled-changes-and-durable-execution/10-CONTEXT.md
@.planning/phases/10-scheduled-changes-and-durable-execution/10-02-PLAN.md
@.planning/phases/10-scheduled-changes-and-durable-execution/10-03-PLAN.md
@prompts/rulestead-telemetry-observability-and-audit.md
@prompts/rulestead-security-privacy-and-threat-model.md

<interfaces>
From `rulestead/lib/rulestead/audit_event.ex`:
```elixir
|> maybe_put("change_request_id", governance_value(attrs, context, :change_request_id))
|> maybe_put("approval_id", governance_value(attrs, context, :approval_id))
|> maybe_put("governance_action", governance_value(attrs, context, :governance_action))
|> maybe_put("execution_stage", governance_value(attrs, context, :execution_stage))
```

From `scripts/ci/verify_phase09_governance.sh`:
```bash
log_step "running Phase 09 governance safety contract suites in rulestead"
cd "$ROOT/rulestead"
mix test \
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scheduled-execution audit and telemetry metadata with explicit actor-chain wording</name>
  <files>rulestead/lib/rulestead/audit_event.ex, rulestead/lib/rulestead/telemetry.ex, rulestead/lib/rulestead/store/ecto.ex, rulestead/lib/rulestead/oban/scheduled_execution_worker.ex, rulestead/test/rulestead/scheduled_execution_audit_contract_test.exs</files>
  <read_first>
    - rulestead/lib/rulestead/audit_event.ex
    - rulestead/lib/rulestead/telemetry.ex
    - rulestead/lib/rulestead/store/ecto.ex
    - rulestead/lib/rulestead/oban/scheduled_execution_worker.ex
    - prompts/rulestead-telemetry-observability-and-audit.md
    - prompts/rulestead-security-privacy-and-threat-model.md
  </read_first>
  <acceptance_criteria>
    - Audit metadata includes `scheduled_execution_id`, `attempt_count`, `scheduled_for`, `executed_at`, `failure_reason`, `execution_mode`, and `executed_by`.
    - Telemetry supports the exact event names `scheduled_execution.scheduled`, `scheduled_execution.started`, `scheduled_execution.succeeded`, `scheduled_execution.failed`, `scheduled_execution.quarantined`, `scheduled_execution.cancelled`, and `scheduled_execution.requeued`.
    - Telemetry metadata includes `change_request_id`, `correlation_id`, `governance_action`, `environment_key`, `attempt_count`, and `audit_event_id` where applicable.
    - Audit/telemetry redaction still drops session-shaped keys and does not persist raw session payloads.
    - Scheduled execution audit payloads distinguish human provenance (`scheduled_by`, `approved_by`) from runtime actor (`executed_by: scheduler`).
  </acceptance_criteria>
  <behavior>
    - Test 1: successful scheduled execution emits correlated audit and telemetry data with both requested and actual timing.
    - Test 2: failed/quarantined execution emits redacted failure metadata and records the scheduler/system executor honestly instead of impersonating the original human.
  </behavior>
  <action>Extend audit and telemetry surfaces per D-12, D-13, and D-14. In `Rulestead.AuditEvent`, promote scheduled-execution metadata to top-level fields `scheduled_execution_id`, `attempt_count`, `scheduled_for`, `executed_at`, `failure_reason`, `execution_mode`, and `executed_by` while preserving session-key stripping. In `Rulestead.Telemetry`, add canonical scheduling lifecycle events `scheduled_execution.scheduled`, `scheduled_execution.started`, `scheduled_execution.succeeded`, `scheduled_execution.failed`, `scheduled_execution.quarantined`, `scheduled_execution.cancelled`, and `scheduled_execution.requeued` with metadata fields `change_request_id`, `correlation_id`, `governance_action`, `environment_key`, `attempt_count`, and `audit_event_id`. Emit these events from the Ecto execution path and worker lifecycle so the operator-facing state and the ephemeral observability stream say the same thing, including explicit actor wording: `scheduled_by`, `approved_by`, and `executed_by: scheduler`.</action>
  <verify>
    <automated>cd rulestead && mix test test/rulestead/scheduled_execution_audit_contract_test.exs</automated>
  </verify>
  <done>Scheduled execution outcomes are now observable through immutable audit and canonical telemetry with honest actor-chain semantics and redacted metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Package Phase 10 verification around durable execution and failure handling</name>
  <files>rulestead/test/rulestead/scheduled_execution_threat_model_test.exs, scripts/ci/verify_phase10_scheduling.sh</files>
  <read_first>
    - scripts/ci/verify_phase09_governance.sh
    - rulestead/test/rulestead/governance_threat_model_test.exs
    - rulestead/test/rulestead/store/scheduled_execution_adapter_contract_test.exs
    - rulestead/test/rulestead/scheduled_execution_conflict_test.exs
    - prompts/rulestead-security-privacy-and-threat-model.md
  </read_first>
  <acceptance_criteria>
    - `scripts/ci/verify_phase10_scheduling.sh` exists and prints phase-specific step labels beginning with `[verify_phase10_scheduling]`.
    - The verifier runs the scheduling contract, conflict, audit, Oban, and threat-model suites from `rulestead`.
    - Threat-model tests prove replay-safe execution, visible quarantine after bounded retries, and correlated audit metadata for scheduled failures.
    - The verifier does not claim Phase 11 admin schedule UI or Phase 12 webhook coverage.
  </acceptance_criteria>
  <behavior>
    - Test 1: the threat-model suite proves duplicate job delivery does not duplicate a completed scheduled mutation.
    - Test 2: verifier output stays scripts-first, readable, and scoped to Phase 10 core-package behavior only.
  </behavior>
  <action>Create `rulestead/test/rulestead/scheduled_execution_threat_model_test.exs` to prove replay safety, bounded retry exhaustion into `quarantined`, and correlated audit/failure-reason persistence per D-09, D-10, D-15, and D-16. Add `scripts/ci/verify_phase10_scheduling.sh` in the same scripts-first style as Phase 09, with exact step labels for migration discoverability, Oban seam tests, adapter/facade/conflict/audit suites, and the new threat-model suite. Keep the verifier Phase 10 honest: core package only, no claims about mounted schedule UI or webhook delivery work that belongs to later phases.</action>
  <verify>
    <automated>bash scripts/ci/verify_phase10_scheduling.sh</automated>
  </verify>
  <done>Phase 10 has a single readable verification entrypoint that proves durable execution, replay safety, and operator-visible failure handling without overstating future-phase coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execution outcome -> audit/telemetry surfaces | Durable and ephemeral observability records are emitted here and must stay correlated without leaking secrets |
| verifier script -> claimed phase coverage | Release/readiness evidence is asserted here and must remain scoped to shipped core-package behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | I | audit/telemetry metadata | mitigate | Reuse audit-event and command metadata redaction so session-shaped and secret-like keys are stripped before emission. |
| T-10-04-02 | R | actor provenance | mitigate | Record `scheduled_by`, `approved_by`, and `executed_by: scheduler` explicitly in audit/telemetry instead of impersonating the human actor, per D-12 and D-14. |
| T-10-04-03 | D | hidden failure loops | mitigate | Emit `scheduled_execution.failed` and `scheduled_execution.quarantined` with `attempt_count` and `failure_reason`, and prove bounded retry exhaustion in threat-model tests, per D-09 and D-13. |
| T-10-04-04 | T | verification overclaim | mitigate | Keep `verify_phase10_scheduling.sh` limited to Phase 10 core-package suites and explicitly exclude Phase 11 UI / Phase 12 webhook claims. |
</threat_model>

<verification>
Run the new phase verifier and inspect emitted test names and script step labels to confirm the phase evidence stays core-package only and covers both happy-path and failure-path scheduling behavior.
</verification>

<success_criteria>
Scheduled execution now has the observability and proof layer operators need: audit and telemetry agree on what happened, failures stay legible, and one script demonstrates the phase’s durable execution guarantees.
</success_criteria>

<output>
After completion, create `.planning/phases/10-scheduled-changes-and-durable-execution/10-04-SUMMARY.md`
</output>
