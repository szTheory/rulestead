---
phase: 33-compare-drill-in-preview-identity-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex
  - rulestead_admin/test/rulestead_admin/live/environment_compare_live/index_test.exs
  - rulestead_admin/test/rulestead_admin/live/environment_compare_live/show_test.exs
autonomous: true
requirements:
  - TEN-03
must_haves:
  truths:
    - "Mounted compare summary links preserve `compare_token` when routing into flag drill-in pages."
    - "Drill-in pages continue to render reviewed-preview and stale-preview states against the same preview identity."
    - "Verification stays bounded to the mounted compare route without widening the release surface or admin package role."
  artifacts:
    - path: "rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex"
      provides: "Summary-to-detail compare-token carry-through in mounted compare drill-in links"
      min_lines: 120
    - path: "rulestead_admin/test/rulestead_admin/live/environment_compare_live/index_test.exs"
      provides: "Mounted compare summary regression coverage for preview-identity link preservation"
      min_lines: 80
    - path: "rulestead_admin/test/rulestead_admin/live/environment_compare_live/show_test.exs"
      provides: "Mounted compare drill-in regression coverage for reviewed/stale preview identity"
      min_lines: 70
  key_links:
    - from: "rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex"
      to: "rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex"
      via: "`flag_path/2` preserves `compare_token` so drill-in `handle_params/3` can reload the same preview identity"
      pattern: "compare_token|flag_path|handle_params"
    - from: "rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex"
      to: "rulestead/lib/rulestead/promotion/compare.ex"
      via: "drill-in stale-preview rendering stays driven by compare findings from the shared compare token contract"
      pattern: "compare_token|staleness_conflict"
---

<objective>
Preserve compare preview identity across mounted summary-to-detail navigation and prove drill-in pages still render the intended reviewed or stale preview state.

Purpose: Phase 33 closes the remaining mounted compare gap by carrying the existing `compare_token` through flag drill-in URLs instead of silently dropping reviewed preview identity between summary and detail pages.
Output: a narrow route carry-through fix in `rulestead_admin` plus targeted mounted compare regressions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.1.0-MILESTONE-AUDIT.md
@.planning/phases/33-compare-drill-in-preview-identity-closure/33-RESEARCH.md
@.planning/phases/33-compare-drill-in-preview-identity-closure/33-PATTERNS.md
@.planning/phases/30-mounted-admin-tenant-scope-closure/30-02-PLAN.md
@rulestead_admin/lib/rulestead_admin/live/session.ex
@rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex
@rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex
@rulestead_admin/test/rulestead_admin/live/environment_compare_live/index_test.exs
@rulestead_admin/test/rulestead_admin/live/environment_compare_live/show_test.exs
@rulestead/lib/rulestead/promotion/compare.ex

<interfaces>
From `rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex`:
```elixir
defp flag_path(page, flag_key) do
  "#{page.mount_path}/compare/#{flag_key}?" <>
    URI.encode_query(%{
      "env" => page.current_environment.key,
      "tenant" => page.current_tenant && page.current_tenant.key,
      "source_env" => page.source_env,
      "target_env" => page.target_env
    })
end
```

From `rulestead_admin/lib/rulestead_admin/live/environment_compare_live/show.ex`:
```elixir
compare_token = blank_to_nil(params["compare_token"])
...
|> maybe_put_opt(:compare_token, socket.assigns.compare_token_param)
```

From `rulestead/lib/rulestead/promotion/compare.ex`:
```elixir
provided_token when provided_token == compare_token -> findings
...
finding(:blocker, :staleness_conflict, "compare_token_stale", ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carry compare preview identity from summary links into drill-in routes</name>
  <files>rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex, rulestead_admin/test/rulestead_admin/live/environment_compare_live/index_test.exs</files>
  <behavior>
    - Test 1: summary-generated flag drill-in links keep `compare_token` alongside `env`, `tenant`, `source_env`, and `target_env`.
    - Test 2: reviewed preview drill-ins stay deep-linkable from the summary page without introducing hidden state.
    - Test 3: the change remains read-only and does not widen compare scope beyond the current mounted route params.
  </behavior>
  <action>Update `flag_path/2` in `EnvironmentCompareLive.Index` so it preserves the active `compare_token` when building drill-in links, using the same explicit route-backed posture already used for mounted scope params. Extend `index_test.exs` to assert summary HTML now includes the correct compare-token drill-in URL and keeps the existing read-only compare behavior. Do not change `Rulestead.compare_environments/3`, token generation, or session resolution logic for this task.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/environment_compare_live/index_test.exs</automated>
  </verify>
  <done>The compare summary page no longer drops `compare_token`, and mounted summary-link regressions prove preview identity survives navigation into drill-in routes.</done>
</task>

<task type="auto">
  <name>Task 2: Prove drill-in pages keep reviewed and stale preview state against the preserved identity</name>
  <files>rulestead_admin/test/rulestead_admin/live/environment_compare_live/show_test.exs</files>
  <behavior>
    - Test 1: a drill-in route reached with the preserved summary `compare_token` still renders the expected compare payload and metadata.
    - Test 2: stale-preview drill-ins continue to surface the critical stale warning when the carried token no longer matches the recomputed preview identity.
    - Test 3: verification remains strictly bounded to mounted compare route tests and does not introduce additional production edits beyond Task 1.
  </behavior>
  <action>Strengthen `show_test.exs` so it explicitly proves the drill-in page behavior depends on the carried preview identity: keep the existing stale-preview assertion, and add or tighten reviewed-preview assertions so the page shows compare-token metadata and remains read-only when loaded with a preserved token from the summary route shape. Keep this task test-only.</action>
  <verify>
    <automated>cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/environment_compare_live/show_test.exs</automated>
  </verify>
  <done>The drill-in regression suite proves both reviewed-preview and stale-preview states remain bound to the intended compare identity after summary-to-detail navigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compare summary state -> drill-in URL | Reviewed preview identity crosses into deep-link navigation and must not be dropped |
| drill-in route params -> shared compare seam | The drill-in page must continue to forward the intended preview identity into compare without inventing a second stale-state mechanism |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | T | `rulestead_admin/lib/rulestead_admin/live/environment_compare_live/index.ex` | mitigate | Preserve `compare_token` in `flag_path/2` so summary-generated drill-in URLs cannot silently switch preview identity. |
| T-33-02 | R | mounted compare drill-in verification | mitigate | Add targeted LiveView regressions that prove reviewed-preview and stale-preview drill-ins stay tied to the carried token and remain read-only. |
</threat_model>

<verification>
- `cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/environment_compare_live/index_test.exs`
- `cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/environment_compare_live/show_test.exs`
- `cd /Users/jon/projects/rulestead/rulestead_admin && mix test test/rulestead_admin/live/environment_compare_live/index_test.exs test/rulestead_admin/live/environment_compare_live/show_test.exs`
</verification>

<success_criteria>
- Compare summary links preserve `compare_token` when routing into drill-in pages.
- Drill-in pages continue to handle reviewed-preview and stale-preview states against the same preview identity.
- Verification covers the mounted compare summary-to-detail path without changing the release boundary or introducing standalone-admin drift.
</success_criteria>

<output>
After completion, create `.planning/phases/33-compare-drill-in-preview-identity-closure/33-01-SUMMARY.md`
</output>
