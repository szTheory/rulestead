# Thread: 2026-05-27 Post-v1.7 Milestone Assessment

## Status

- Milestone initialized — v1.8.0 Phases 61-64 defined; `MILESTONE-CONTEXT.md` consumed
- Supersedes open investigations in `2026-05-27-next-milestone-assessment.md` for GOV-01 / quickstart (both closed)

## Assessment Lens

- Scope: sibling packages + guides + demo + bounded proof bars
- Audience: serious Phoenix SaaS adopter evaluating production use
- Evidence: repo inspection 2026-05-27 (lib/, tests, CI, README, phase 60 verification)

## Current Verdict

- Rough done band: **90–92%**
- Band label: **strong, approaching upper band; one high-leverage product wedge + optional deepening remain**
- v1.7.0 closed GOV-01 and quickstart payload-first parity (VER-03); prior 87% band understated shipped state

## Confidence Caveats

- `JTBD-MAP.md` body (2026-05-21) still stale on lifecycle/tenancy/guardrails; **delta section updated** — prefer milestone archives + this thread
- Hex consumer line is `0.1.x` while repo milestone docs say `v1.0.0+` GA — intentional, documented in README
- `MAINTAINING.md` mounted proof file list still cites `form_test.exs`; CI/release_contract use `session_test.exs` — docs drift, low adopter impact

## Adopter Coverage (post-v1.7)

| Flow | Status |
|------|--------|
| Runtime evaluation + explain | Well served |
| Mounted authoring + RBAC | Well served |
| Governed prod mutations | Well served |
| Compare + promotion + GitOps | Well served |
| Lifecycle hygiene + cleanup | Well served |
| Tenancy scope | Well served |
| Guarded rollout hold/rollback | Well served (host wires provider) |
| Reusable audiences + impact preview | Well served |
| Protected-env audience governance (GOV-01) | **Well served (v1.7)** |
| Auto-advance guarded rollouts (ROL-04) | **Not built** |
| Richer host preview evidence (IMP-05) | Partial — core accepts samples; mounted does not wire |
| Draft targeting presets (ADM-05) | Not built — defer |

## Recommended Next Milestone

**Pick:** `v1.8.0 — Guarded Rollout Auto-Advance` (ROL-04)

**Done enough for v1.8.0:**

- Opt-in per-rollout auto-advance after closed observation window with all guardrails `:healthy`
- Fail closed on `:pending_data`, `:held`, stale/weak signals — no advance
- Reuse `ScheduledExecution` / Oban worker pattern; orchestrate evaluate → governed `advance_rollout`
- Protected environments respect change-request envelope (same as manual advance)
- Mounted UX: pending observation, auto-advance toggle, timeline distinguishes automation
- Proof scope extends `guarded_rollout_foundations` or new `mix verify.phase61`; release-contract allows bounded auto-advance claims
- Host integration seam doc subsection (metrics remain host-owned)

## Ranking After v1.7

1. **v1.8.0 ROL-04** — activate now
2. **IMP-05** — v1.9 or defer (integrator-opt-in; mounted doesn't pass samples today)
3. **MAINTAINING mounted proof list drift** — small docs fix, bundle anytime
4. **ADM-05 presets** — defer (scope creep)
5. **Default CI runs verify.phase60** — diminishing returns (full suite + release_contract already merge-blocking)

## Open Investigations

- **ROL-04 stage plan:** authored next-stage metadata vs admin ladder `[5,25,50,100]` suggestion only — need explicit stage plan in authored state before auto-advance
- **ROL-04 scheduling:** extend `ScheduledExecution` with evaluation tick vs dedicated guardrail worker — prefer single governed envelope
- **IMP-05 vs blast-radius:** keep GOV thresholds reference-count only even if IMP-05 ships impression summaries

## Graduation Candidates (for Phase 61+ LEARNINGS)

- Documented proof bars are product contract; release-contract forbidden phrases prevent support overclaim
- Auto-advance must not bypass v1.7 governance or v1.5 fail-closed guardrail semantics
- `GuardrailDecision.action_type :advance` exists for manual/monitoring paths — ROL-04 needs orchestration, not a parallel decision model
- Milestone `verify.phaseNN` tasks are maintainer entrypoints; default CI value is full `mix test` + `release_contract_test`, not phase aliases

## Blunt Maintainer Takeaway

Ship v1.8.0 ROL-04 next. The library is already credible for serious Phoenix SaaS teams; the largest honest gap in the differentiated story is completing guarded rollouts (hold/rollback shipped, auto-advance not). Defer IMP-05 until an adopter commits to host-supplied bounded evidence. Fix MAINTAINING mounted file list when convenient. Do not start ADM-05 or observability features.

## Next Step

User runs `/gsd-new-milestone` (optionally `v1.8.0 Guarded Rollout Auto-Advance`); phases continue at 61.
