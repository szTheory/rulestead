# Handoff: Post-0.1.1 release + maintenance default (2026-05-28)

**Purpose:** Session continuity after context clear. Read this + `.planning/STATE.md` before picking up work.

## Status snapshot

| Area | State |
|------|--------|
| **Product band** | v1.1–v1.11 post-GA band **complete** (~93–95% done for stated scope) |
| **Hex** | `rulestead` + `rulestead_admin` **0.1.1** live (linked sibling release) |
| **Git** | `main` — release/verify fixes through `97fda07` |
| **Tags** | `rulestead-v0.1.1`, `rulestead_admin-v0.1.1` (force-updated to include verify fixes) |
| **Post-publish verify** | Trio green locally + CI — run `26593552126` |
| **Drift issue** | #17 **closed** (auto-closed on successful verify workflow) |
| **Planning mode** | **Maintenance default** — no feature milestone without deferred trigger |

## What shipped in the 0.1.1 release session

- Merged release-please PR #15; published both packages to Hex
- Fixed publish pipeline: dynamic tarball whitelist, `mix deps.get` in publish jobs, `ex_doc` on admin, idempotent core publish skip
- Fixed post-publish verify trio:
  - Admin consumer fixture: mount **inside** Router module, `/admin` scope, `AdminConsumer.RulesteadPolicy` stub
  - Parity: git tag `rulestead-vX.Y.Z` (not `vX.Y.Z`); compare Hex-whitelist paths only
  - CI: `fetch-depth: 0`, `phx_new` archive install, drift issue auto-close on success
- Key commits: `7bfea96` (verify trio fixes), `97fda07` (phx_new on CI)

## Adopter / maintainer proof bars

```bash
# Merge gate (every PR)
cd rulestead && mix verify.adopter

# After each Hex cut
bash scripts/ci/verify_published_release.sh <version>
```

CI also runs `verify-published-release.yml` on schedule + post-publish handoff.

## What's next (priority order)

### 1. Maintenance (default)

- Adopter issues, patch releases via release-please
- Keep `ci.yml` + `mix verify.adopter` green
- No v2 feature work without a real trigger

### 2. Optional v1.11.1 docs polish (not blocking)

Small graduation items from STATE.md:

- README local demo header / v1.11 closure wording
- Spine first-flag create walkthrough
- `environment_key: "dev"` vs installer friction
- `upgrading.md` → mention `verify.adopter`
- Hex 0.1.x vs v1.x milestone narrative in public docs

### 3. Next release-please cut

Recent verify/CI commits will accumulate. Expect next linked PR (possibly 0.1.2 patch or 0.2.0 minor depending on changelog). Flow:

1. Green CI on release PR
2. Merge → approve `hex-publish` environment
3. `bash scripts/ci/verify_published_release.sh <version>`

### 4. v2.0+ (triggered only)

See `.planning/DEFERRED.md`. Default wedge order when triggers fire:

**GOV-02-ext** (per-env thresholds) → **ROL-08** (guardrail baselines) → **ADM-06** (draft presets)

Open with `/gsd-new-milestone` only when a trigger is real.

## Do not reopen

- Phase 1–78 feature/doc bands (shipped)
- Release prep checklist in `2026-05-28-release-readiness.md` (complete for 0.1.1)
- Drift issue #17 (closed; reopen only if verify trio fails again)

## Key file pointers

| Topic | Path |
|-------|------|
| Current state | `.planning/STATE.md` |
| v2 backlog | `.planning/DEFERRED.md` |
| Release engineering | `prompts/rulestead-release-engineering-and-ci.md` |
| Post-publish script | `scripts/ci/verify_published_release.sh` |
| Admin consumer fixture | `rulestead/test/support/release_publish_fixture.ex` |
| Parity task | `rulestead/lib/mix/tasks/verify.release_parity.ex` |
| Verify workflow | `.github/workflows/verify-published-release.yml` |

## Agent resume one-liner

Rulestead is post-path-to-done on maintenance default; **0.1.1 is on Hex** with **green post-publish verify**. Next work is patches/adopter support, optional doc polish, or v2 only when `.planning/DEFERRED.md` triggers fire.
