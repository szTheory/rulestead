# Handoff: Post-0.1.2 maintenance + CI hygiene (2026-05-28)

**Purpose:** Session continuity after context clear. Read this + `.planning/STATE.md` before picking up work.

## Status snapshot

| Area | State |
|------|--------|
| **Product band** | v1.1–v1.11 post-GA band **complete** (~93–95% done for stated scope) |
| **Hex** | `rulestead` + `rulestead_admin` **0.1.2** live (linked sibling release) |
| **Git** | `main` — CI hygiene + doc truth through maintenance session |
| **Tags** | `rulestead-v0.1.2`, `rulestead_admin-v0.1.2` |
| **Post-publish verify** | Trio green locally + CI for **0.1.2** |
| **CI** | Green; Mix + Dialyzer PLT caches in `ci.yml`; `release-pr-ci` dispatch fixed |
| **Ecto 3.14** | Coordinated `ecto_sql ~> 3.14` bump **done** (mix.exs + locks + CI) |
| **Planning mode** | **Maintenance default** — no feature milestone without deferred trigger |

## What shipped in the 0.1.2 maintenance session

- **0.1.2 release:** merged release-please PR #16; published both packages; post-publish verify green
- **CI unblock:** format fix in `verify_release_publish_test.exs`; `release-pr-ci.yml` checkout + `--repo`
- **CI polish:** Mix deps/build caches + Dialyzer PLT restore/save; PLTs gitignored locally
- **Publish hardening:** `gate-ci-green` polls for merge CI completion (fixes release race)
- **Doc truth:** public installable line updated to **0.1.2**; `@published_smoke_version` aligned

## Adopter / maintainer proof bars

```bash
# Merge gate (every PR)
cd rulestead && mix verify.adopter

# After each Hex cut
bash scripts/ci/verify_published_release.sh <version>
```

## What's next (priority order)

1. **Maintenance default** — adopter issues, patch releases via release-please
2. **After each Hex cut** — post-publish verify trio (local or CI workflow)
3. **Triggered only** — v2 wedges per `.planning/DEFERRED.md` (GOV-02-ext → ROL-08 → ADM-06)

## Deferred (explicit)

| Item | Notes |
|------|-------|
| Ecto 3.14 | **Done** — coordinated `~> 3.14` + full CI (0.1.3) |
| Dialyzer cleanup | `.dialyzer_ignore.exs` tracks future milestone |
| v2 features | Only when deferred triggers fire |

## One-liner for the next agent

Post-path-to-done maintenance mode; **0.1.2 on Hex** with **green CI + post-publish verify**; default next work is patches/adopter support unless a v2 trigger fires.
