# rulestead_admin

`rulestead_admin` is the optional admin package for Rulestead.

The real admin UI ships in Phases 6 and 7, and the package remains
unpublished until Phase 8. Until then, this package exists to lock the
sibling-package layout and publish-time guardrails.
